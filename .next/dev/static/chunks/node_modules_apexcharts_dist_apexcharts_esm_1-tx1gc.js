(globalThis["TURBOPACK"] || (globalThis["TURBOPACK"] = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/apexcharts/dist/apexcharts.esm.js [app-client] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>ApexCharts
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = /*#__PURE__*/ __turbopack_context__.i("[project]/node_modules/next/dist/compiled/buffer/index.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_call_super.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_class_call_check.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_create_class.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_define_property.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_inherits.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_instanceof.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_sliced_to_array.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_to_consumable_array.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_type_of.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_wrap_native_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@swc/helpers/esm/_wrap_native_super.js [app-client] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__$5f$_generator__as__$5f3e$__ = __turbopack_context__.i("[project]/node_modules/tslib/tslib.es6.mjs [app-client] (ecmascript) <export __generator as _>");
;
;
;
;
;
;
;
;
;
;
;
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = function __defNormalProp(obj, key, value) {
    return key in obj ? __defProp(obj, key, {
        enumerable: true,
        configurable: true,
        writable: true,
        value: value
    }) : obj[key] = value;
};
var __spreadValues = function __spreadValues(a, b) {
    for(var prop in b || (b = {}))if (__hasOwnProp.call(b, prop)) __defNormalProp(a, prop, b[prop]);
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    if (__getOwnPropSymbols) try {
        for(var _iterator = __getOwnPropSymbols(b)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var prop = _step.value;
            if (__propIsEnum.call(b, prop)) __defNormalProp(a, prop, b[prop]);
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return a;
};
var __spreadProps = function __spreadProps(a, b) {
    return __defProps(a, __getOwnPropDescs(b));
};
var __objRest = function __objRest(source, exclude) {
    var target = {};
    for(var prop in source)if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0) target[prop] = source[prop];
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    if (source != null && __getOwnPropSymbols) try {
        for(var _iterator = __getOwnPropSymbols(source)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var prop = _step.value;
            if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop)) target[prop] = source[prop];
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return target;
};
var __publicField = function __publicField(obj, key, value) {
    return __defNormalProp(obj, (typeof key === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(key)) !== "symbol" ? key + "" : key, value);
};
var __async = function __async(__this, __arguments, generator) {
    return new Promise(function(resolve, reject) {
        var fulfilled = function fulfilled(value) {
            try {
                step(generator.next(value));
            } catch (e) {
                reject(e);
            }
        };
        var rejected = function rejected(value) {
            try {
                step(generator.throw(value));
            } catch (e) {
                reject(e);
            }
        };
        var step = function step(x) {
            return x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
        };
        step((generator = generator.apply(__this, __arguments)).next());
    });
};
/*!
 * ApexCharts v6.8.0
 * (c) 2018-2026 ApexCharts
 */ var Environment = /*#__PURE__*/ function() {
    "use strict";
    function Environment() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Environment);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Environment, null, [
        {
            key: "isSSR",
            value: /**
   * Check if running in server-side rendering environment (Node.js)
   * @returns {boolean} True if in SSR/Node.js, false if in browser
   */ function isSSR() {
                return typeof window === "undefined" || typeof document === "undefined";
            }
        },
        {
            key: "isBrowser",
            value: /**
   * Check if running in browser environment
   * @returns {boolean} True if in browser, false if in SSR/Node.js
   */ function isBrowser() {
                return !this.isSSR();
            }
        },
        {
            key: "hasAPI",
            value: /**
   * Check if a specific browser API is available
   * @param {string} api - Name of the API to check (e.g., 'ResizeObserver')
   * @returns {boolean} True if API is available
   */ function hasAPI(api) {
                if (this.isSSR()) return false;
                return typeof /** @type {any} */ window[api] !== "undefined";
            }
        },
        {
            key: "getApex",
            value: /**
   * Returns the global Apex config object regardless of environment.
   * In browser: window.Apex; in SSR/Node.js: global.Apex; fallback: {}.
   * @returns {any}
   */ function getApex() {
                if (typeof window !== "undefined" && window.Apex) return window.Apex;
                if (("TURBOPACK compile-time value", "object") !== "undefined" && /*TURBOPACK member replacement*/ __turbopack_context__.g.Apex) return /*TURBOPACK member replacement*/ __turbopack_context__.g.Apex;
                return {};
            }
        }
    ]);
    return Environment;
}();
var SSRElement = /*#__PURE__*/ function() {
    "use strict";
    function SSRElement(nodeName) {
        var namespaceURI = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SSRElement);
        this.nodeName = nodeName;
        this.namespaceURI = namespaceURI;
        this.attributes = /* @__PURE__ */ new Map();
        this.children = [];
        this.textContent = "";
        this.style = {};
        this.classList = new SSRClassList();
        this.parentNode = /** @type {SSRElement | null} */ null;
        this._ssrWidth = void 0;
        this._ssrHeight = void 0;
        this._ssrMode = void 0;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SSRElement, [
        {
            /**
   * @param {string} name
   * @param {any} value
   */ key: "setAttribute",
            value: function setAttribute(name2, value) {
                this.attributes.set(name2, value);
            }
        },
        {
            /**
   * @param {string} name
   */ key: "getAttribute",
            value: function getAttribute(name2) {
                return this.attributes.get(name2);
            }
        },
        {
            /**
   * @param {string} name
   */ key: "removeAttribute",
            value: function removeAttribute(name2) {
                this.attributes.delete(name2);
            }
        },
        {
            /**
   * @param {string} name
   */ key: "hasAttribute",
            value: function hasAttribute(name2) {
                return this.attributes.has(name2);
            }
        },
        {
            /**
   * @param {any} child
   */ key: "appendChild",
            value: function appendChild(child) {
                if (child && child !== this) {
                    if (child.parentNode && child.parentNode !== this) {
                        child.parentNode.removeChild(child);
                    } else if (child.parentNode === this) {
                        var index = this.children.indexOf(child);
                        if (index !== -1) this.children.splice(index, 1);
                    }
                    child.parentNode = this;
                    this.children.push(child);
                }
                return child;
            }
        },
        {
            /**
   * @param {any} child
   */ key: "removeChild",
            value: function removeChild(child) {
                var index = this.children.indexOf(child);
                if (index !== -1) {
                    this.children.splice(index, 1);
                    child.parentNode = null;
                }
                return child;
            }
        },
        {
            /**
   * @param {any} newNode
   * @param {any} referenceNode
   */ key: "insertBefore",
            value: function insertBefore(newNode, referenceNode) {
                if (!referenceNode) {
                    return this.appendChild(newNode);
                }
                if (newNode.parentNode && newNode.parentNode !== this) {
                    newNode.parentNode.removeChild(newNode);
                } else if (newNode.parentNode === this) {
                    var existingIndex = this.children.indexOf(newNode);
                    if (existingIndex !== -1) this.children.splice(existingIndex, 1);
                }
                var index = this.children.indexOf(referenceNode);
                if (index !== -1) {
                    newNode.parentNode = this;
                    this.children.splice(index, 0, newNode);
                }
                return newNode;
            }
        },
        {
            key: "cloneNode",
            value: function cloneNode() {
                var deep = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
                var clone = new SSRElement(this.nodeName, this.namespaceURI);
                clone.textContent = this.textContent;
                this.attributes.forEach(function(value, key) {
                    clone.attributes.set(key, value);
                });
                Object.assign(clone.style, this.style);
                if (deep) {
                    this.children.forEach(function(child) {
                        if (child.cloneNode) {
                            clone.appendChild(child.cloneNode(true));
                        }
                    });
                }
                return clone;
            }
        },
        {
            key: "getBoundingClientRect",
            value: function getBoundingClientRect() {
                return {
                    width: this._ssrWidth || 0,
                    height: this._ssrHeight || 0,
                    top: 0,
                    left: 0,
                    right: this._ssrWidth || 0,
                    bottom: this._ssrHeight || 0,
                    x: 0,
                    y: 0
                };
            }
        },
        {
            key: "getRootNode",
            value: function getRootNode() {
                var root = this;
                while(root.parentNode){
                    root = root.parentNode;
                }
                return root;
            }
        },
        {
            key: "querySelector",
            value: function querySelector() {
                return null;
            }
        },
        {
            key: "querySelectorAll",
            value: function querySelectorAll() {
                return [];
            }
        },
        {
            key: "getElementsByClassName",
            value: function getElementsByClassName() {
                return [];
            }
        },
        {
            key: "addEventListener",
            value: function addEventListener() {}
        },
        {
            key: "removeEventListener",
            value: function removeEventListener() {}
        },
        {
            key: "childNodes",
            get: function get() {
                return this.children;
            }
        },
        {
            key: "toString",
            value: function toString() {
                var attrs = "";
                this.attributes.forEach(function(value, key) {
                    attrs += " ".concat(key, '="').concat(value, '"');
                });
                if (this.children.length === 0 && !this.textContent) {
                    return "<".concat(this.nodeName).concat(attrs, "/>");
                }
                var childrenStr = this.children.map(function(c) {
                    return c.toString();
                }).join("");
                return "<".concat(this.nodeName).concat(attrs, ">").concat(this.textContent).concat(childrenStr, "</").concat(this.nodeName, ">");
            }
        },
        {
            key: "innerHTML",
            get: // Property getters/setters
            function get() {
                return this.children.map(function(c) {
                    return c.toString();
                }).join("");
            },
            set: function set(value) {
                this.children = [];
                this.textContent = value;
            }
        },
        {
            key: "outerHTML",
            get: function get() {
                return this.toString();
            }
        },
        {
            key: "isConnected",
            get: function get() {
                return true;
            }
        }
    ]);
    return SSRElement;
}();
var SSRClassList = /*#__PURE__*/ function() {
    "use strict";
    function SSRClassList() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SSRClassList);
        this.classes = /* @__PURE__ */ new Set();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SSRClassList, [
        {
            key: "add",
            value: function add() {
                var _this = this;
                for(var _len = arguments.length, classNames = new Array(_len), _key = 0; _key < _len; _key++){
                    classNames[_key] = arguments[_key];
                }
                classNames.forEach(function(name2) {
                    return _this.classes.add(name2);
                });
            }
        },
        {
            key: "remove",
            value: function remove() {
                var _this = this;
                for(var _len = arguments.length, classNames = new Array(_len), _key = 0; _key < _len; _key++){
                    classNames[_key] = arguments[_key];
                }
                classNames.forEach(function(name2) {
                    return _this.classes.delete(name2);
                });
            }
        },
        {
            /**
   * @param {string} className
   */ key: "contains",
            value: function contains(className) {
                return this.classes.has(className);
            }
        },
        {
            /**
   * @param {string} className
   * @param {any} force
   */ key: "toggle",
            value: function toggle(className, force) {
                if (force === true) {
                    this.classes.add(className);
                    return true;
                } else if (force === false) {
                    this.classes.delete(className);
                    return false;
                } else {
                    if (this.classes.has(className)) {
                        this.classes.delete(className);
                        return false;
                    } else {
                        this.classes.add(className);
                        return true;
                    }
                }
            }
        },
        {
            key: "toString",
            value: function toString() {
                return Array.from(this.classes).join(" ");
            }
        }
    ]);
    return SSRClassList;
}();
var SSRDOMShim = /*#__PURE__*/ function() {
    "use strict";
    function SSRDOMShim() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SSRDOMShim);
        this.SVGNS = "http://www.w3.org/2000/svg";
        this.XLINKNS = "http://www.w3.org/1999/xlink";
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SSRDOMShim, [
        {
            /**
   * Create SVG element with namespace
   * @param {string} namespaceURI - Namespace URI
   * @param {string} qualifiedName - Element tag name
   * @returns {SSRElement} Mock SVG element
   */ key: "createElementNS",
            value: function createElementNS(namespaceURI, qualifiedName) {
                return new SSRElement(qualifiedName, namespaceURI);
            }
        },
        {
            /**
   * Create text node
   * @param {string} data - Text content
   * @returns {object} Text node mock
   */ key: "createTextNode",
            value: function createTextNode(data) {
                var node = {
                    nodeName: "#text",
                    nodeType: 3,
                    textContent: data,
                    toString: function toString() {
                        return node.textContent;
                    }
                };
                return node;
            }
        },
        {
            /**
   * Query selector (returns null in SSR)
   * @returns {null}
   */ key: "querySelector",
            value: function querySelector() {
                return null;
            }
        },
        {
            /**
   * Query selector all (returns empty array in SSR)
   * @returns {any[]}
   */ key: "querySelectorAll",
            value: function querySelectorAll() {
                return [];
            }
        },
        {
            /**
   * Get computed style (returns empty object in SSR)
   * @returns {object}
   */ key: "getComputedStyle",
            value: function getComputedStyle1() {
                return {};
            }
        },
        {
            /**
   * Get bounding client rect for element
   * @param {SSRElement} element - Element to measure
   * @returns {object} Mock dimensions
   */ key: "getBoundingClientRect",
            value: function getBoundingClientRect(element) {
                if (element && element.getBoundingClientRect) {
                    return element.getBoundingClientRect();
                }
                return {
                    width: 0,
                    height: 0,
                    top: 0,
                    left: 0,
                    right: 0,
                    bottom: 0,
                    x: 0,
                    y: 0
                };
            }
        },
        {
            /**
   * Create mock XMLSerializer for SSR
   * @returns {object} XMLSerializer mock
   */ key: "createXMLSerializer",
            value: function createXMLSerializer() {
                return {
                    /**
       * @param {Element} element
       */ serializeToString: function serializeToString(element) {
                        return element.toString ? element.toString() : "";
                    }
                };
            }
        },
        {
            /**
   * Create mock DOMParser for SSR
   * @returns {object} DOMParser mock
   */ key: "createDOMParser",
            value: function createDOMParser() {
                return {
                    /**
       * @param {string} str
       * @param {string} _type
       */ parseFromString: function parseFromString(str, _type) {
                        var root = new SSRElement("root");
                        root.innerHTML = str;
                        return {
                            documentElement: root
                        };
                    }
                };
            }
        }
    ]);
    return SSRDOMShim;
}();
var shim = null;
var xmlSerializerInstance = null;
var domParserInstance = null;
var BrowserAPIs = /*#__PURE__*/ function() {
    "use strict";
    function BrowserAPIs() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, BrowserAPIs);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(BrowserAPIs, null, [
        {
            key: "init",
            value: /**
   * Initialize the SSR shim if in SSR environment
   * Must be called before using other methods
   */ function init() {
                if (Environment.isSSR() && !shim) {
                    shim = new SSRDOMShim();
                }
            }
        },
        {
            key: "createElement",
            value: /**
   * Create an HTML element
   * @param {string} tagName - Element tag name
   * @returns {HTMLElement} HTML element
   */ function createElement(tagName) {
                if (Environment.isSSR()) {
                    if (!shim) this.init();
                    return shim.createElementNS(null, tagName);
                }
                return document.createElement(tagName);
            }
        },
        {
            key: "createElementNS",
            value: /**
   * Create an SVG element with namespace
   * @param {string} namespaceURI - Namespace URI
   * @param {string} qualifiedName - Element tag name
   * @returns {HTMLElement} created element
   */ function createElementNS(namespaceURI, qualifiedName) {
                if (Environment.isSSR()) {
                    if (!shim) this.init();
                    return shim.createElementNS(namespaceURI, qualifiedName);
                }
                return /** @type {HTMLElement} */ document.createElementNS(namespaceURI, qualifiedName);
            }
        },
        {
            key: "createTextNode",
            value: /**
   * Create a text node
   * @param {string} data - Text content
   * @returns {Text|object} Text node
   */ function createTextNode(data) {
                if (Environment.isSSR()) {
                    if (!shim) this.init();
                    return shim.createTextNode(data);
                }
                return document.createTextNode(data);
            }
        },
        {
            key: "querySelector",
            value: /**
   * Query selector
   * @param {string} selector - CSS selector
   * @returns {Element|null}
   */ function querySelector(selector) {
                if (Environment.isSSR()) {
                    return null;
                }
                return document.querySelector(selector);
            }
        },
        {
            key: "querySelectorAll",
            value: /**
   * Query selector all
   * @param {string} selector - CSS selector
   * @returns {NodeList|any[]}
   */ function querySelectorAll(selector) {
                if (Environment.isSSR()) {
                    return [];
                }
                return document.querySelectorAll(selector);
            }
        },
        {
            key: "getComputedStyle",
            value: /**
   * Get computed style for an element
   * @param {Element} element - Element to get styles for
   * @returns {CSSStyleDeclaration|object}
   */ function getComputedStyle1(element) {
                if (Environment.isSSR()) {
                    return {};
                }
                return window.getComputedStyle(element);
            }
        },
        {
            key: "matchMedia",
            value: /**
   * Evaluate a media query. Returns the live MediaQueryList in a browser (so
   * callers can attach a `change` listener) or null under SSR / when
   * unsupported. Facet (#13) uses this for `theme.follow: 'os'`.
   * @param {string} query
   * @returns {MediaQueryList|null}
   */ function matchMedia(query) {
                if (Environment.isSSR() || typeof window.matchMedia !== "function") {
                    return null;
                }
                try {
                    return window.matchMedia(query);
                } catch (e) {
                    return null;
                }
            }
        },
        {
            key: "getBoundingClientRect",
            value: /**
   * Get bounding client rect for an element
   * @param {Element} element - Element to measure
   * @returns {DOMRect|object}
   */ function getBoundingClientRect(element) {
                if (Environment.isSSR()) {
                    if (!shim) this.init();
                    return shim.getBoundingClientRect(element);
                }
                return element ? element.getBoundingClientRect() : {
                    width: 0,
                    height: 0,
                    top: 0,
                    left: 0,
                    right: 0,
                    bottom: 0,
                    x: 0,
                    y: 0
                };
            }
        },
        {
            key: "getXMLSerializer",
            value: /**
   * Get XMLSerializer instance
   * @returns {XMLSerializer|object}
   */ function getXMLSerializer() {
                if (Environment.isSSR()) {
                    if (!shim) this.init();
                    if (!xmlSerializerInstance) {
                        xmlSerializerInstance = shim.createXMLSerializer();
                    }
                    return xmlSerializerInstance;
                }
                if (!xmlSerializerInstance) {
                    xmlSerializerInstance = new XMLSerializer();
                }
                return xmlSerializerInstance;
            }
        },
        {
            key: "getDOMParser",
            value: /**
   * Get DOMParser instance
   * @returns {DOMParser|object}
   */ function getDOMParser() {
                if (Environment.isSSR()) {
                    if (!shim) this.init();
                    if (!domParserInstance) {
                        domParserInstance = shim.createDOMParser();
                    }
                    return domParserInstance;
                }
                if (!domParserInstance) {
                    domParserInstance = new DOMParser();
                }
                return domParserInstance;
            }
        },
        {
            key: "addWindowEventListener",
            value: /**
   * Add event listener to window
   * @param {string} event - Event name
   * @param {EventListenerOrEventListenerObject} handler - Event handler
   * @param {object} options - Event options
   */ function addWindowEventListener(event, handler, options2) {
                if (Environment.isBrowser()) {
                    window.addEventListener(event, handler, options2);
                }
            }
        },
        {
            key: "removeWindowEventListener",
            value: /**
   * Remove event listener from window
   * @param {string} event - Event name
   * @param {EventListenerOrEventListenerObject} handler - Event handler
   * @param {object} options - Event options
   */ function removeWindowEventListener(event, handler, options2) {
                if (Environment.isBrowser()) {
                    window.removeEventListener(event, handler, options2);
                }
            }
        },
        {
            key: "requestAnimationFrame",
            value: /**
   * Request animation frame
   * @param {FrameRequestCallback} callback - Callback function
   * @returns {number|null}
   */ function requestAnimationFrame1(callback) {
                if (Environment.isBrowser()) {
                    return window.requestAnimationFrame(callback);
                }
                callback(0);
                return null;
            }
        },
        {
            key: "cancelAnimationFrame",
            value: /**
   * Cancel animation frame
   * @param {number} id - Animation frame ID
   */ function cancelAnimationFrame1(id) {
                if (Environment.isBrowser() && id) {
                    window.cancelAnimationFrame(id);
                }
            }
        },
        {
            key: "elementExists",
            value: /**
   * Check if element exists
   * @param {Element} element - Element to check
   * @returns {boolean}
   */ function elementExists(element) {
                if (!element) return false;
                if (Environment.isSSR()) {
                    return element._ssrMode === true || element.nodeName !== void 0;
                }
                return element.getRootNode ? element.getRootNode({
                    composed: true
                }) === document || element.isConnected : false;
            }
        },
        {
            key: "getWindow",
            value: /**
   * Get window object (or null in SSR)
   * @returns {Window|null}
   */ function getWindow() {
                return Environment.isBrowser() ? window : null;
            }
        },
        {
            key: "getDocument",
            value: /**
   * Get document object (or null in SSR)
   * @returns {Document|null}
   */ function getDocument() {
                return Environment.isBrowser() ? document : null;
            }
        },
        {
            key: "_getShim",
            value: /**
   * Get the shim instance (for testing purposes)
   * @returns {SSRDOMShim|null}
   */ function _getShim() {
                return shim;
            }
        },
        {
            key: "_resetShim",
            value: /**
   * Reset the shim instance (for testing purposes)
   */ function _resetShim() {
                shim = null;
                xmlSerializerInstance = null;
                domParserInstance = null;
            }
        }
    ]);
    return BrowserAPIs;
}();
var Utils$1 = /*#__PURE__*/ function() {
    "use strict";
    function Utils() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Utils);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Utils, [
        {
            /**
   * @param {number} percent
   * @param {string} color
   */ key: "shadeRGBColor",
            value: function shadeRGBColor(percent, color) {
                var f = color.split(","), t = percent < 0 ? 0 : 255, p = percent < 0 ? percent * -1 : percent, R = parseInt(f[0].slice(4), 10), G = parseInt(f[1], 10), B = parseInt(f[2], 10);
                return "rgb(" + (Math.round((t - R) * p) + R) + "," + (Math.round((t - G) * p) + G) + "," + (Math.round((t - B) * p) + B) + ")";
            }
        },
        {
            /**
   * @param {number} percent
   * @param {string} color
   */ key: "shadeHexColor",
            value: function shadeHexColor(percent, color) {
                var f = parseInt(color.slice(1), 16), t = percent < 0 ? 0 : 255, p = percent < 0 ? percent * -1 : percent, R = f >> 16, G = f >> 8 & 255, B = f & 255;
                return "#" + (16777216 + (Math.round((t - R) * p) + R) * 65536 + (Math.round((t - G) * p) + G) * 256 + (Math.round((t - B) * p) + B)).toString(16).slice(1);
            }
        },
        {
            // beautiful color shading blending code
            // http://stackoverflow.com/questions/5560248/programmatically-lighten-or-darken-a-hex-color-or-rgb-and-blend-colors
            /**
   * @param {number} p
   * @param {string} color
   */ key: "shadeColor",
            value: function shadeColor(p, color) {
                if (Utils.isColorHex(color)) {
                    return this.shadeHexColor(p, color);
                } else {
                    return this.shadeRGBColor(p, color);
                }
            }
        }
    ], [
        {
            key: "isObject",
            value: /**
   * @param {*} item
   */ function isObject(item) {
                return item && (typeof item === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(item)) === "object" && !Array.isArray(item);
            }
        },
        {
            key: "is",
            value: // Type checking that works across different window objects
            /**
   * @param {string} type
   * @param {string} val
   */ function is(type, val) {
                return Object.prototype.toString.call(val) === "[object " + type + "]";
            }
        },
        {
            key: "isSafari",
            value: function isSafari() {
                return Environment.isBrowser() && /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
            }
        },
        {
            key: "extend",
            value: // to extend defaults with user options
            // credit: http://stackoverflow.com/questions/27936772/deep-object-merging-in-es6-es7#answer-34749873
            /**
   * @param {any} target
   * @param {any} source
   */ function extend(target, source) {
                var _this = this;
                if (!this.isObject(target) && this.isObject(source)) {
                    return this.clone(source);
                }
                var output = Object.assign({}, target);
                if (this.isObject(target) && this.isObject(source)) {
                    Object.keys(source).forEach(function(key) {
                        if (_this.isObject(source[key])) {
                            if (!(key in target)) {
                                Object.assign(output, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])({}, key, source[key]));
                            } else {
                                output[key] = _this.extend(target[key], source[key]);
                            }
                        } else {
                            Object.assign(output, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])({}, key, source[key]));
                        }
                    });
                }
                return output;
            }
        },
        {
            key: "extendArray",
            value: /**
   * @param {any[]} arrToExtend
   * @param {any} resultArr
   */ function extendArray(arrToExtend, resultArr) {
                var extendedArr = [];
                arrToExtend.map(function(item) {
                    extendedArr.push(Utils.extend(resultArr, item));
                });
                arrToExtend = extendedArr;
                return arrToExtend;
            }
        },
        {
            key: "monthMod",
            value: // If month counter exceeds 12, it starts again from 1
            /**
   * @param {number} month
   */ function monthMod(month) {
                return month % 12;
            }
        },
        {
            key: "clone",
            value: /**
   * clone object with optional shallow copy for performance
   * @param {*} source - Source object to clone
   * @param {WeakMap<any, any>} visited - Circular reference tracker
   * @param {boolean} shallow - If true, performs shallow copy (default: false)
   * @returns {*} Cloned object
   */ function clone(source) {
                var visited = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : /* @__PURE__ */ new WeakMap(), shallow = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
                if (source === null || (typeof source === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(source)) !== "object") {
                    return source;
                }
                if (visited.has(source)) {
                    return visited.get(source);
                }
                var cloneResult;
                if (Array.isArray(source)) {
                    if (shallow) {
                        cloneResult = source.slice();
                    } else {
                        cloneResult = [];
                        visited.set(source, cloneResult);
                        for(var i = 0; i < source.length; i++){
                            cloneResult[i] = this.clone(source[i], visited, false);
                        }
                    }
                } else if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(source, Date)) {
                    cloneResult = new Date(source.getTime());
                } else {
                    if (shallow) {
                        cloneResult = Object.assign({}, source);
                    } else {
                        cloneResult = {};
                        visited.set(source, cloneResult);
                        for(var prop in source){
                            if (Object.prototype.hasOwnProperty.call(source, prop)) {
                                cloneResult[prop] = this.clone(/** @type {Record<string,any>} */ source[prop], visited, false);
                            }
                        }
                    }
                }
                return cloneResult;
            }
        },
        {
            key: "shallowClone",
            value: /**
   * Shallow clone for performance when deep clone isn't needed
   * @param {*} source - Source to clone
   * @returns {*} Shallow cloned object
   */ function shallowClone(source) {
                if (source === null || (typeof source === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(source)) !== "object") {
                    return source;
                }
                if (Array.isArray(source)) {
                    return source.slice();
                }
                return Object.assign({}, source);
            }
        },
        {
            key: "shallowEqual",
            value: /**
   * Fast shallow equality check for objects
   * @param {Object} obj1 - First object
   * @param {Object} obj2 - Second object
   * @returns {boolean} True if shallowly equal
   */ function shallowEqual(obj1, obj2) {
                if (obj1 === obj2) return true;
                if (!obj1 || !obj2) return false;
                if ((typeof obj1 === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(obj1)) !== "object" || (typeof obj2 === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(obj2)) !== "object") {
                    return obj1 === obj2;
                }
                var keys1 = Object.keys(obj1);
                var keys2 = Object.keys(obj2);
                if (keys1.length !== keys2.length) return false;
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = keys1[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var key = _step.value;
                        if (/** @type {Record<string,any>} */ obj1[key] !== /** @type {Record<string,any>} */ obj2[key]) return false;
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                return true;
            }
        },
        {
            key: "log10",
            value: /**
   * @param {number} x
   */ function log10(x) {
                return Math.log(x) / Math.LN10;
            }
        },
        {
            key: "roundToBase10",
            value: /**
   * @param {number} x
   */ function roundToBase10(x) {
                return Math.pow(10, Math.floor(Math.log10(x)));
            }
        },
        {
            key: "roundToBase",
            value: /**
   * @param {number} x
   * @param {number} base
   */ function roundToBase(x, base) {
                return Math.pow(base, Math.floor(Math.log(x) / Math.log(base)));
            }
        },
        {
            key: "parseNumber",
            value: /**
   * @param {any} val
   */ function parseNumber(val) {
                if (typeof val === "number" || val === null) return val;
                return parseFloat(val);
            }
        },
        {
            key: "stripNumber",
            value: /**
   * @param {number} num
   */ function stripNumber(num) {
                var precision = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 2;
                return Number.isInteger(num) ? num : parseFloat(num.toPrecision(precision));
            }
        },
        {
            key: "randomId",
            value: function randomId() {
                return (Math.random() + 1).toString(36).substring(4);
            }
        },
        {
            key: "noExponents",
            value: /**
   * @param {number} num
   */ function noExponents(num) {
                if (num.toString().includes("e")) {
                    return Math.round(num);
                }
                return num;
            }
        },
        {
            key: "elementExists",
            value: /**
   * @param {any} element
   */ function elementExists(element) {
                if (!element || !element.isConnected) {
                    return false;
                }
                return true;
            }
        },
        {
            key: "getDimensions",
            value: /**
   * @param {any} el
   */ function getDimensions(el) {
                if (!el) return [
                    0,
                    0
                ];
                if (Environment.isSSR()) {
                    return [
                        el._ssrWidth || 400,
                        el._ssrHeight || 300
                    ];
                }
                var computedStyle;
                try {
                    computedStyle = getComputedStyle(el, null);
                } catch (e) {
                    return [
                        el.clientWidth || 0,
                        el.clientHeight || 0
                    ];
                }
                var elementWidth = el.clientWidth;
                var elementHeight = el.clientHeight;
                if (!elementWidth || !elementHeight) {
                    var rect = el.getBoundingClientRect();
                    elementWidth = elementWidth || rect.width;
                    elementHeight = elementHeight || rect.height;
                }
                elementHeight -= parseFloat(computedStyle.paddingTop) + parseFloat(computedStyle.paddingBottom);
                elementWidth -= parseFloat(computedStyle.paddingLeft) + parseFloat(computedStyle.paddingRight);
                return [
                    elementWidth,
                    elementHeight
                ];
            }
        },
        {
            key: "getBoundingClientRect",
            value: /**
   * @returns {any}
   * @param {any} element
   */ function getBoundingClientRect(element) {
                if (!element) {
                    return {
                        top: 0,
                        right: 0,
                        bottom: 0,
                        left: 0,
                        width: 0,
                        height: 0,
                        x: 0,
                        y: 0
                    };
                }
                if (Environment.isSSR()) {
                    return BrowserAPIs.getBoundingClientRect(element);
                }
                var rect = element.getBoundingClientRect();
                return {
                    top: rect.top,
                    right: rect.right,
                    bottom: rect.bottom,
                    left: rect.left,
                    width: element.clientWidth,
                    height: element.clientHeight,
                    x: rect.left,
                    y: rect.top
                };
            }
        },
        {
            key: "getLargestStringFromArr",
            value: /**
   * @param {any[]} arr
   */ function getLargestStringFromArr(arr) {
                return arr.reduce(function(a, b) {
                    if (Array.isArray(b)) {
                        b = b.reduce(function(aa, bb) {
                            return aa.length > bb.length ? aa : bb;
                        });
                    }
                    return a.length > b.length ? a : b;
                }, 0);
            }
        },
        {
            key: "hexToRgba",
            value: // http://stackoverflow.com/questions/5623838/rgb-to-hex-and-hex-to-rgb#answer-12342275
            function hexToRgba() {
                var hex = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "#999999", opacity = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0.6;
                if (hex.substring(0, 1) !== "#") {
                    hex = "#999999";
                }
                var hexStr = hex.replace("#", "");
                var h = hexStr.match(new RegExp("(.{" + hexStr.length / 3 + "})", "g")) || [];
                for(var i = 0; i < h.length; i++){
                    h[i] = parseInt(h[i].length === 1 ? h[i] + h[i] : h[i], 16);
                }
                if (typeof opacity !== "undefined") h.push(opacity);
                return "rgba(" + h.join(",") + ")";
            }
        },
        {
            key: "getOpacityFromRGBA",
            value: /**
   * @param {string} rgba
   */ function getOpacityFromRGBA(rgba) {
                return parseFloat(rgba.replace(/^.*,(.+)\)/, "$1"));
            }
        },
        {
            key: "parseHex",
            value: /**
   * Parse a #RGB or #RRGGBB hex colour string into [r,g,b] in 0–255.
   * Returns null for invalid input. Used by getContrastRatio.
   * @param {string} hex
   * @returns {[number, number, number] | null}
   */ function parseHex(hex) {
                if (typeof hex !== "string") return null;
                var h = hex.trim().replace("#", "");
                if (h.length === 3) {
                    h = h.split("").map(function(c) {
                        return c + c;
                    }).join("");
                }
                if (!/^[0-9a-fA-F]{6}$/.test(h)) return null;
                return [
                    parseInt(h.slice(0, 2), 16),
                    parseInt(h.slice(2, 4), 16),
                    parseInt(h.slice(4, 6), 16)
                ];
            }
        },
        {
            key: "relativeLuminance",
            value: /**
   * Relative luminance per WCAG 2.x (https://www.w3.org/TR/WCAG22/#dfn-relative-luminance).
   * @param {[number, number, number]} rgb 0–255 sRGB triplet
   * @returns {number} 0.0–1.0
   */ function relativeLuminance(param) {
                var _param = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(param, 3), r = _param[0], g = _param[1], b = _param[2];
                var channel = function channel(c) {
                    var v = c / 255;
                    return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4);
                };
                return 0.2126 * channel(r) + 0.7152 * channel(g) + 0.0722 * channel(b);
            }
        },
        {
            key: "getContrastRatio",
            value: /**
   * WCAG contrast ratio between two hex colours. Returns 0 for invalid input.
   * Range: 1 (identical) … 21 (#000 vs #fff).
   * WCAG AA requires ≥ 4.5 for normal text and ≥ 3.0 for large text / UI components.
   * @param {string} hex1
   * @param {string} hex2
   * @returns {number}
   */ function getContrastRatio(hex1, hex2) {
                var rgb1 = Utils.parseHex(hex1);
                var rgb2 = Utils.parseHex(hex2);
                if (!rgb1 || !rgb2) return 0;
                var l1 = Utils.relativeLuminance(rgb1);
                var l2 = Utils.relativeLuminance(rgb2);
                var lighter = Math.max(l1, l2);
                var darker = Math.min(l1, l2);
                return (lighter + 0.05) / (darker + 0.05);
            }
        },
        {
            key: "rgb2hex",
            value: /**
   * @param {any} rgb
   */ function rgb2hex(rgb) {
                rgb = rgb.match(/^rgba?[\s+]?\([\s+]?(\d+)[\s+]?,[\s+]?(\d+)[\s+]?,[\s+]?(\d+)[\s+]?/i);
                return rgb && rgb.length === 4 ? "#" + ("0" + parseInt(rgb[1], 10).toString(16)).slice(-2) + ("0" + parseInt(rgb[2], 10).toString(16)).slice(-2) + ("0" + parseInt(rgb[3], 10).toString(16)).slice(-2) : "";
            }
        },
        {
            key: "isColorHex",
            value: /**
   * @param {string} color
   */ function isColorHex(color) {
                return /(^#[0-9A-F]{6}$)|(^#[0-9A-F]{3}$)|(^#[0-9A-F]{8}$)/i.test(color);
            }
        },
        {
            key: "isCSSVariable",
            value: /**
   * @param {string} color
   */ function isCSSVariable(color) {
                if (typeof color !== "string") return false;
                var value = color.trim();
                return value.startsWith("var(") && value.endsWith(")");
            }
        },
        {
            key: "getThemeColor",
            value: /**
   * @param {string} color
   */ function getThemeColor(color) {
                if (!Utils.isCSSVariable(color)) return color;
                if (Environment.isSSR()) return color;
                var tempElem = document.createElement("div");
                tempElem.style.cssText = "position:fixed; left: -9999px; visibility:hidden;";
                tempElem.style.color = color;
                document.body.appendChild(tempElem);
                var computedColor;
                try {
                    computedColor = window.getComputedStyle(tempElem).color;
                } finally{
                    if (tempElem.parentNode) {
                        tempElem.parentNode.removeChild(tempElem);
                    }
                }
                return computedColor;
            }
        },
        {
            key: "applyOpacityToColor",
            value: /**
   * @param {string} color
   * @param {number} opacity
   */ function applyOpacityToColor(color, opacity) {
                var value = Number(opacity);
                if (!Number.isFinite(value)) return color;
                if (value <= 0) return "transparent";
                if (value >= 1) return color;
                var percent = Math.round(value * 100);
                return "color-mix(in srgb, ".concat(color, " ").concat(percent, "%, transparent)");
            }
        },
        {
            key: "getPolygonPos",
            value: /**
   * @param {number} size
   * @param {number} dataPointsLen
   */ function getPolygonPos(size, dataPointsLen) {
                var dotsArray = [];
                var angle = Math.PI * 2 / dataPointsLen;
                for(var i = 0; i < dataPointsLen; i++){
                    var curPos = {};
                    curPos.x = size * Math.sin(i * angle);
                    curPos.y = -size * Math.cos(i * angle);
                    dotsArray.push(curPos);
                }
                return dotsArray;
            }
        },
        {
            key: "polarToCartesian",
            value: /**
   * @param {number} centerX
   * @param {number} centerY
   * @param {number} radius
   * @param {number} angleInDegrees
   */ function polarToCartesian(centerX, centerY, radius, angleInDegrees) {
                var angleInRadians = (angleInDegrees - 90) * Math.PI / 180;
                return {
                    x: centerX + radius * Math.cos(angleInRadians),
                    y: centerY + radius * Math.sin(angleInRadians)
                };
            }
        },
        {
            key: "escapeString",
            value: /**
   * @param {string} str
   */ function escapeString(str) {
                var escapeWith = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "x";
                var newStr = str.toString().slice();
                newStr = newStr.replace(/[` ~!@#$%^&*()|+=?;:'",.<>{}[\]\\/]/gi, escapeWith);
                return newStr;
            }
        },
        {
            key: "negToZero",
            value: /**
   * @param {number} val
   */ function negToZero(val) {
                return val < 0 ? 0 : val;
            }
        },
        {
            key: "moveIndexInArray",
            value: /**
   * @param {any[]} arr
   * @param {number} old_index
   * @param {number} new_index
   */ function moveIndexInArray(arr, old_index, new_index) {
                if (new_index >= arr.length) {
                    var k = new_index - arr.length + 1;
                    while(k--){
                        arr.push(void 0);
                    }
                }
                arr.splice(new_index, 0, arr.splice(old_index, 1)[0]);
                return arr;
            }
        },
        {
            key: "extractNumber",
            value: /**
   * @param {string} s
   */ function extractNumber(s) {
                return parseFloat(s.replace(/[^\d.]*/g, ""));
            }
        },
        {
            key: "findAncestor",
            value: /**
   * @param {any} el
   * @param {string} cls
   */ function findAncestor(el, cls) {
                while((el = el.parentElement) && !el.classList.contains(cls));
                return el;
            }
        },
        {
            key: "setELstyles",
            value: /**
   * @param {any} el
   * @param {Record<string, any>} styles
   */ function setELstyles(el, styles) {
                for(var key in styles){
                    if (Object.prototype.hasOwnProperty.call(styles, key)) {
                        el.style[key] = styles[key];
                    }
                }
            }
        },
        {
            key: "preciseAddition",
            value: // prevents JS prevision errors when adding
            /**
   * @param {number} a
   * @param {number} b
   */ function preciseAddition(a, b) {
                var aDecimals = (String(a).split(".")[1] || "").length;
                var bDecimals = (String(b).split(".")[1] || "").length;
                var factor = Math.pow(10, Math.max(aDecimals, bDecimals));
                return (Math.round(a * factor) + Math.round(b * factor)) / factor;
            }
        },
        {
            key: "isNumber",
            value: /**
   * @param {any} value
   */ function isNumber(value) {
                return !isNaN(value) && parseFloat(String(Number(value))) === value && !isNaN(parseInt(value, 10));
            }
        },
        {
            key: "isFloat",
            value: /**
   * @param {number} n
   */ function isFloat(n) {
                return Number(n) === n && n % 1 !== 0;
            }
        },
        {
            key: "isMsEdge",
            value: function isMsEdge() {
                if (Environment.isSSR()) return false;
                var ua = window.navigator.userAgent;
                var edge = ua.indexOf("Edge/");
                if (edge > 0) {
                    return parseInt(ua.substring(edge + 5, ua.indexOf(".", edge)), 10);
                }
                return false;
            }
        },
        {
            key: "getGCD",
            value: //
            // Find the Greatest Common Divisor of two numbers
            //
            /**
   * @param {number} a
   * @param {number} b
   */ function getGCD(a, b) {
                var p = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 7;
                var factor = Math.pow(10, p - Math.floor(Math.log10(Math.max(a, b))));
                if (factor > 1) {
                    a = Math.round(Math.abs(a) * factor);
                    b = Math.round(Math.abs(b) * factor);
                } else {
                    factor = 1;
                }
                while(b){
                    var t = b;
                    b = a % b;
                    a = t;
                }
                return a / factor;
            }
        },
        {
            key: "getPrimeFactors",
            value: /**
   * @param {number} n
   */ function getPrimeFactors(n) {
                var factors = [];
                var divisor = 2;
                while(n >= 2){
                    if (n % divisor == 0) {
                        factors.push(divisor);
                        n = n / divisor;
                    } else {
                        divisor++;
                    }
                }
                return factors;
            }
        },
        {
            key: "mod",
            value: /**
   * @param {number} a
   * @param {number} b
   */ function mod(a, b) {
                var p = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 7;
                var big = Math.pow(10, p - Math.floor(Math.log10(Math.max(a, b))));
                a = Math.round(Math.abs(a) * big);
                b = Math.round(Math.abs(b) * big);
                return a % b / big;
            }
        }
    ]);
    return Utils;
}();
var DateTime = /*#__PURE__*/ function() {
    "use strict";
    function DateTime(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, DateTime);
        this.w = w;
        this.months31 = [
            1,
            3,
            5,
            7,
            8,
            10,
            12
        ];
        this.months30 = [
            2,
            4,
            6,
            9,
            11
        ];
        this.daysCntOfYear = [
            0,
            31,
            59,
            90,
            120,
            151,
            181,
            212,
            243,
            273,
            304,
            334
        ];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(DateTime, [
        {
            /**
   * @param {any} date
   */ key: "isValidDate",
            value: function isValidDate(date) {
                if (typeof date === "number") {
                    return false;
                }
                return !isNaN(this.parseDate(date));
            }
        },
        {
            /**
   * @param {any} dateStr
   */ key: "getTimeStamp",
            value: function getTimeStamp(dateStr) {
                if (isNaN(Date.parse(dateStr))) {
                    return dateStr;
                }
                var utc = this.w.config.xaxis.labels.datetimeUTC;
                return !utc ? new Date(dateStr).getTime() : new Date(new Date(dateStr).toISOString().substr(0, 25)).getTime();
            }
        },
        {
            /**
   * @param {any} timestamp
   */ key: "getDate",
            value: function getDate(timestamp) {
                var utc = this.w.config.xaxis.labels.datetimeUTC;
                return utc ? new Date(new Date(timestamp).toUTCString()) : new Date(timestamp);
            }
        },
        {
            /**
   * @param {string} dateStr
   */ key: "parseDate",
            value: function parseDate(dateStr) {
                var parsed = Date.parse(dateStr);
                if (!isNaN(parsed)) {
                    return this.getTimeStamp(dateStr);
                }
                var output = Date.parse(dateStr.replace(/-/g, "/").replace(/[a-z]+/gi, " "));
                output = this.getTimeStamp(output);
                return output;
            }
        },
        {
            // This fixes the difference of x-axis labels between chrome/safari
            // Fixes #1726, #1544, #1485, #1255
            /**
   * @param {string} dateStr
   */ key: "parseDateWithTimezone",
            value: function parseDateWithTimezone(dateStr) {
                return Date.parse(dateStr.replace(/-/g, "/").replace(/[a-z]+/gi, " "));
            }
        },
        {
            // http://stackoverflow.com/questions/14638018/current-time-formatting-with-javascript#answer-14638191
            /**
   * @param {Date} date
   * @param {string} format
   */ key: "formatDate",
            value: function formatDate(date, format) {
                var locale = this.w.globals.locale;
                var utc = this.w.config.xaxis.labels.datetimeUTC;
                var MMMM = [
                    "\0"
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(locale.months));
                var MMM = [
                    ""
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(locale.shortMonths));
                var dddd = [
                    ""
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(locale.days));
                var ddd = [
                    ""
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(locale.shortDays));
                function ii(i) {
                    var len = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 2;
                    var s2 = i + "";
                    while(s2.length < len)s2 = "0" + s2;
                    return s2;
                }
                var y = utc ? date.getUTCFullYear() : date.getFullYear();
                format = format.replace(/(^|[^\\])yyyy+/g, "$1" + y);
                format = format.replace(/(^|[^\\])yy/g, "$1" + y.toString().substr(2, 2));
                format = format.replace(/(^|[^\\])y/g, "$1" + y);
                var M = (utc ? date.getUTCMonth() : date.getMonth()) + 1;
                format = format.replace(/(^|[^\\])MMMM+/g, "$1" + MMMM[0]);
                format = format.replace(/(^|[^\\])MMM/g, "$1" + MMM[0]);
                format = format.replace(/(^|[^\\])MM/g, "$1" + ii(M));
                format = format.replace(/(^|[^\\])M/g, "$1" + M);
                var d = utc ? date.getUTCDate() : date.getDate();
                format = format.replace(/(^|[^\\])dddd+/g, "$1" + dddd[0]);
                format = format.replace(/(^|[^\\])ddd/g, "$1" + ddd[0]);
                format = format.replace(/(^|[^\\])dd/g, "$1" + ii(d));
                format = format.replace(/(^|[^\\])d/g, "$1" + d);
                var H = utc ? date.getUTCHours() : date.getHours();
                format = format.replace(/(^|[^\\])HH+/g, "$1" + ii(H));
                format = format.replace(/(^|[^\\])H/g, "$1" + H);
                var h = H > 12 ? H - 12 : H === 0 ? 12 : H;
                format = format.replace(/(^|[^\\])hh+/g, "$1" + ii(h));
                format = format.replace(/(^|[^\\])h/g, "$1" + h);
                var m = utc ? date.getUTCMinutes() : date.getMinutes();
                format = format.replace(/(^|[^\\])mm+/g, "$1" + ii(m));
                format = format.replace(/(^|[^\\])m/g, "$1" + m);
                var s = utc ? date.getUTCSeconds() : date.getSeconds();
                format = format.replace(/(^|[^\\])ss+/g, "$1" + ii(s));
                format = format.replace(/(^|[^\\])s/g, "$1" + s);
                var f = utc ? date.getUTCMilliseconds() : date.getMilliseconds();
                format = format.replace(/(^|[^\\])fff+/g, "$1" + ii(f, 3));
                f = Math.round(f / 10);
                format = format.replace(/(^|[^\\])ff/g, "$1" + ii(f));
                f = Math.round(f / 10);
                format = format.replace(/(^|[^\\])f/g, "$1" + f);
                var T = H < 12 ? "AM" : "PM";
                format = format.replace(/(^|[^\\])TT+/g, "$1" + T);
                format = format.replace(/(^|[^\\])T/g, "$1" + T.charAt(0));
                var t = T.toLowerCase();
                format = format.replace(/(^|[^\\])tt+/g, "$1" + t);
                format = format.replace(/(^|[^\\])t/g, "$1" + t.charAt(0));
                var tz = -date.getTimezoneOffset();
                var K = utc || !tz ? "Z" : tz > 0 ? "+" : "-";
                if (!utc) {
                    tz = Math.abs(tz);
                    var tzHrs = Math.floor(tz / 60);
                    var tzMin = tz % 60;
                    K += ii(tzHrs) + ":" + ii(tzMin);
                }
                format = format.replace(/(^|[^\\])K/g, "$1" + K);
                var day = (utc ? date.getUTCDay() : date.getDay()) + 1;
                format = format.replace(new RegExp(dddd[0], "g"), dddd[day]);
                format = format.replace(new RegExp(ddd[0], "g"), ddd[day]);
                format = format.replace(new RegExp(MMMM[0], "g"), MMMM[M]);
                format = format.replace(new RegExp(MMM[0], "g"), MMM[M]);
                format = format.replace(/\\(.)/g, "$1");
                return format;
            }
        },
        {
            /**
   * @param {number} minX
   * @param {number} maxX
   */ key: "getTimeUnitsfromTimestamp",
            value: function getTimeUnitsfromTimestamp(minX, maxX) {
                var w = this.w;
                if (w.config.xaxis.min !== void 0) {
                    minX = w.config.xaxis.min;
                }
                if (w.config.xaxis.max !== void 0) {
                    maxX = w.config.xaxis.max;
                }
                var tsMin = this.getDate(minX);
                var tsMax = this.getDate(maxX);
                var minD = this.formatDate(tsMin, "yyyy MM dd HH mm ss fff").split(" ");
                var maxD = this.formatDate(tsMax, "yyyy MM dd HH mm ss fff").split(" ");
                return {
                    minMillisecond: parseInt(minD[6], 10),
                    maxMillisecond: parseInt(maxD[6], 10),
                    minSecond: parseInt(minD[5], 10),
                    maxSecond: parseInt(maxD[5], 10),
                    minMinute: parseInt(minD[4], 10),
                    maxMinute: parseInt(maxD[4], 10),
                    minHour: parseInt(minD[3], 10),
                    maxHour: parseInt(maxD[3], 10),
                    minDate: parseInt(minD[2], 10),
                    maxDate: parseInt(maxD[2], 10),
                    minMonth: parseInt(minD[1], 10) - 1,
                    maxMonth: parseInt(maxD[1], 10) - 1,
                    minYear: parseInt(minD[0], 10),
                    maxYear: parseInt(maxD[0], 10)
                };
            }
        },
        {
            /**
   * @param {number} year
   */ key: "isLeapYear",
            value: function isLeapYear(year) {
                return year % 4 === 0 && year % 100 !== 0 || year % 400 === 0;
            }
        },
        {
            /**
   * @param {number} month
   * @param {number} year
   * @param {number} subtract
   */ key: "calculcateLastDaysOfMonth",
            value: function calculcateLastDaysOfMonth(month, year, subtract) {
                var days = this.determineDaysOfMonths(month, year);
                return days - subtract;
            }
        },
        {
            /**
   * @param {number} year
   */ key: "determineDaysOfYear",
            value: function determineDaysOfYear(year) {
                var days = 365;
                if (this.isLeapYear(year)) {
                    days = 366;
                }
                return days;
            }
        },
        {
            /**
   * @param {number} year
   * @param {number} month
   * @param {number} date
   */ key: "determineRemainingDaysOfYear",
            value: function determineRemainingDaysOfYear(year, month, date) {
                var dayOfYear = this.daysCntOfYear[month] + date;
                if (month > 1 && this.isLeapYear(year)) dayOfYear++;
                return dayOfYear;
            }
        },
        {
            /**
   * @param {number} month
   * @param {number} year
   */ key: "determineDaysOfMonths",
            value: function determineDaysOfMonths(month, year) {
                var days = 30;
                month = Utils$1.monthMod(month);
                switch(true){
                    case this.months30.indexOf(month) > -1:
                        if (month === 2) {
                            if (this.isLeapYear(year)) {
                                days = 29;
                            } else {
                                days = 28;
                            }
                        }
                        break;
                    case this.months31.indexOf(month) > -1:
                        days = 31;
                        break;
                    default:
                        days = 31;
                        break;
                }
                return days;
            }
        },
        {
            // ───────────────────────────────────────────────────────────────────────────
            // Pure date helpers used by the v6 single-interval TimeScale.
            // Native Date setters/getters do all rollover work (leap years, month
            // length, year crossings), eliminating the manual rollover code that was
            // the source of the historical TimeScale boundary bugs.
            // ───────────────────────────────────────────────────────────────────────────
            /**
   * Extract calendar fields from a timestamp. Month is 0-indexed (matches
   * Date.getMonth / getUTCMonth).
   *
   * @param {number} timestamp
   * @param {boolean} isUTC
   * @returns {{ year: number, month: number, date: number, hour: number, minute: number, second: number, ms: number, weekday: number }}
   */ key: "getDateFields",
            value: function getDateFields(timestamp, isUTC) {
                var d = new Date(timestamp);
                return isUTC ? {
                    year: d.getUTCFullYear(),
                    month: d.getUTCMonth(),
                    date: d.getUTCDate(),
                    hour: d.getUTCHours(),
                    minute: d.getUTCMinutes(),
                    second: d.getUTCSeconds(),
                    ms: d.getUTCMilliseconds(),
                    weekday: d.getUTCDay()
                } : {
                    year: d.getFullYear(),
                    month: d.getMonth(),
                    date: d.getDate(),
                    hour: d.getHours(),
                    minute: d.getMinutes(),
                    second: d.getSeconds(),
                    ms: d.getMilliseconds(),
                    weekday: d.getDay()
                };
            }
        },
        {
            /**
   * Advance a timestamp by `count` units. Native setters handle cross-month,
   * cross-year, leap-year, and DST rollover correctly.
   *
   * @param {number} timestamp
   * @param {'year'|'month'|'week'|'day'|'hour'|'minute'|'second'} unit
   * @param {number} count  may be negative
   * @param {boolean} isUTC
   * @returns {number}
   */ key: "addInterval",
            value: function addInterval(timestamp, unit, count, isUTC) {
                var d = new Date(timestamp);
                if (isUTC) {
                    switch(unit){
                        case "year":
                            d.setUTCFullYear(d.getUTCFullYear() + count);
                            break;
                        case "month":
                            d.setUTCMonth(d.getUTCMonth() + count);
                            break;
                        case "week":
                            d.setUTCDate(d.getUTCDate() + count * 7);
                            break;
                        case "day":
                            d.setUTCDate(d.getUTCDate() + count);
                            break;
                        case "hour":
                            d.setUTCHours(d.getUTCHours() + count);
                            break;
                        case "minute":
                            d.setUTCMinutes(d.getUTCMinutes() + count);
                            break;
                        case "second":
                            d.setUTCSeconds(d.getUTCSeconds() + count);
                            break;
                    }
                } else {
                    switch(unit){
                        case "year":
                            d.setFullYear(d.getFullYear() + count);
                            break;
                        case "month":
                            d.setMonth(d.getMonth() + count);
                            break;
                        case "week":
                            d.setDate(d.getDate() + count * 7);
                            break;
                        case "day":
                            d.setDate(d.getDate() + count);
                            break;
                        case "hour":
                            d.setHours(d.getHours() + count);
                            break;
                        case "minute":
                            d.setMinutes(d.getMinutes() + count);
                            break;
                        case "second":
                            d.setSeconds(d.getSeconds() + count);
                            break;
                    }
                }
                return d.getTime();
            }
        },
        {
            /**
   * Snap a timestamp UP to the next boundary aligned with `step` units of
   * `unit`. Returns the input unchanged if already on a boundary. Alignment
   * rules per unit:
   *   - second: 0/step/2*step/... seconds within a minute (step must divide 60)
   *   - minute: 0/step/2*step/... minutes within an hour (step must divide 60)
   *   - hour:   0/step/... hours within a day (step must divide 24)
   *   - day:    every day (step always 1)
   *   - week:   next Monday on a `step`-week stride from a fixed epoch Monday
   *   - month:  Jan/Apr/Jul/Oct for step=3, Jan/Jul for step=6 (step must divide 12)
   *   - year:   year % step === 0
   *
   * @param {number} timestamp
   * @param {'year'|'month'|'week'|'day'|'hour'|'minute'|'second'} unit
   * @param {number} step
   * @param {boolean} isUTC
   * @returns {number}
   */ key: "ceilToBoundary",
            value: function ceilToBoundary(timestamp, unit, step, isUTC) {
                var d = new Date(timestamp);
                if (isUTC) {
                    switch(unit){
                        case "second":
                            {
                                var s = d.getUTCSeconds();
                                var aligned = Math.ceil(s / step) * step;
                                if (aligned === s && d.getUTCMilliseconds() === 0) return timestamp;
                                d.setUTCMilliseconds(0);
                                d.setUTCSeconds(aligned);
                                return d.getTime();
                            }
                        case "minute":
                            {
                                var m = d.getUTCMinutes();
                                var aligned1 = Math.ceil(m / step) * step;
                                if (aligned1 === m && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0) return timestamp;
                                d.setUTCMilliseconds(0);
                                d.setUTCSeconds(0);
                                d.setUTCMinutes(aligned1);
                                return d.getTime();
                            }
                        case "hour":
                            {
                                var h = d.getUTCHours();
                                var aligned2 = Math.ceil(h / step) * step;
                                if (aligned2 === h && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0) return timestamp;
                                d.setUTCMilliseconds(0);
                                d.setUTCSeconds(0);
                                d.setUTCMinutes(0);
                                d.setUTCHours(aligned2);
                                return d.getTime();
                            }
                        case "day":
                            {
                                if (d.getUTCHours() === 0 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0) return timestamp;
                                d.setUTCMilliseconds(0);
                                d.setUTCSeconds(0);
                                d.setUTCMinutes(0);
                                d.setUTCHours(0);
                                d.setUTCDate(d.getUTCDate() + 1);
                                return d.getTime();
                            }
                        case "week":
                            {
                                var MS_PER_WEEK = 7 * 24 * 60 * 60 * 1e3;
                                var REF_MONDAY_UTC = Date.UTC(1970, 0, 5);
                                d.setUTCMilliseconds(0);
                                d.setUTCSeconds(0);
                                d.setUTCMinutes(0);
                                d.setUTCHours(0);
                                var startOfDay = d.getTime();
                                var weeksSinceRef = Math.ceil((startOfDay - REF_MONDAY_UTC) / MS_PER_WEEK);
                                var alignedWeeks = Math.ceil(weeksSinceRef / step) * step;
                                var aligned3 = REF_MONDAY_UTC + alignedWeeks * MS_PER_WEEK;
                                if (aligned3 >= timestamp) return aligned3;
                                return aligned3 + step * MS_PER_WEEK;
                            }
                        case "month":
                            {
                                var m1 = d.getUTCMonth();
                                var aligned4 = Math.ceil(m1 / step) * step;
                                if (aligned4 === m1 && d.getUTCDate() === 1 && d.getUTCHours() === 0 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0) return timestamp;
                                d.setUTCMilliseconds(0);
                                d.setUTCSeconds(0);
                                d.setUTCMinutes(0);
                                d.setUTCHours(0);
                                d.setUTCDate(1);
                                d.setUTCMonth(aligned4);
                                return d.getTime();
                            }
                        case "year":
                            {
                                var y = d.getUTCFullYear();
                                var aligned5 = Math.ceil(y / step) * step;
                                if (aligned5 === y && d.getUTCMonth() === 0 && d.getUTCDate() === 1 && d.getUTCHours() === 0 && d.getUTCMinutes() === 0 && d.getUTCSeconds() === 0 && d.getUTCMilliseconds() === 0) return timestamp;
                                return Date.UTC(aligned5, 0, 1);
                            }
                    }
                } else {
                    switch(unit){
                        case "second":
                            {
                                var s1 = d.getSeconds();
                                var aligned6 = Math.ceil(s1 / step) * step;
                                if (aligned6 === s1 && d.getMilliseconds() === 0) return timestamp;
                                d.setMilliseconds(0);
                                d.setSeconds(aligned6);
                                return d.getTime();
                            }
                        case "minute":
                            {
                                var m2 = d.getMinutes();
                                var aligned7 = Math.ceil(m2 / step) * step;
                                if (aligned7 === m2 && d.getSeconds() === 0 && d.getMilliseconds() === 0) return timestamp;
                                d.setMilliseconds(0);
                                d.setSeconds(0);
                                d.setMinutes(aligned7);
                                return d.getTime();
                            }
                        case "hour":
                            {
                                var h1 = d.getHours();
                                var aligned8 = Math.ceil(h1 / step) * step;
                                if (aligned8 === h1 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0) return timestamp;
                                d.setMilliseconds(0);
                                d.setSeconds(0);
                                d.setMinutes(0);
                                d.setHours(aligned8);
                                return d.getTime();
                            }
                        case "day":
                            {
                                if (d.getHours() === 0 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0) return timestamp;
                                d.setMilliseconds(0);
                                d.setSeconds(0);
                                d.setMinutes(0);
                                d.setHours(0);
                                d.setDate(d.getDate() + 1);
                                return d.getTime();
                            }
                        case "week":
                            {
                                var MS_PER_WEEK1 = 7 * 24 * 60 * 60 * 1e3;
                                var REF_MONDAY_LOCAL = new Date(1970, 0, 5).getTime();
                                d.setMilliseconds(0);
                                d.setSeconds(0);
                                d.setMinutes(0);
                                d.setHours(0);
                                var startOfDay1 = d.getTime();
                                var weeksSinceRef1 = Math.ceil((startOfDay1 - REF_MONDAY_LOCAL) / MS_PER_WEEK1);
                                var alignedWeeks1 = Math.ceil(weeksSinceRef1 / step) * step;
                                var aligned9 = REF_MONDAY_LOCAL + alignedWeeks1 * MS_PER_WEEK1;
                                if (aligned9 >= timestamp) return aligned9;
                                return aligned9 + step * MS_PER_WEEK1;
                            }
                        case "month":
                            {
                                var m3 = d.getMonth();
                                var aligned10 = Math.ceil(m3 / step) * step;
                                if (aligned10 === m3 && d.getDate() === 1 && d.getHours() === 0 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0) return timestamp;
                                d.setMilliseconds(0);
                                d.setSeconds(0);
                                d.setMinutes(0);
                                d.setHours(0);
                                d.setDate(1);
                                d.setMonth(aligned10);
                                return d.getTime();
                            }
                        case "year":
                            {
                                var y1 = d.getFullYear();
                                var aligned11 = Math.ceil(y1 / step) * step;
                                if (aligned11 === y1 && d.getMonth() === 0 && d.getDate() === 1 && d.getHours() === 0 && d.getMinutes() === 0 && d.getSeconds() === 0 && d.getMilliseconds() === 0) return timestamp;
                                return new Date(aligned11, 0, 1).getTime();
                            }
                    }
                }
                return timestamp;
            }
        },
        {
            /**
   * Test whether a timestamp falls exactly on a unit boundary (start of year,
   * start of month, start of day, etc.). Used by the multi-resolution
   * formatter to upgrade a tick's display unit to a coarser scale.
   *
   * @param {number} timestamp
   * @param {'year'|'month'|'day'|'hour'|'minute'|'second'} unit
   * @param {boolean} isUTC
   * @returns {boolean}
   */ key: "isAtBoundary",
            value: function isAtBoundary(timestamp, unit, isUTC) {
                var f = this.getDateFields(timestamp, isUTC);
                switch(unit){
                    case "year":
                        return f.month === 0 && f.date === 1 && f.hour === 0 && f.minute === 0 && f.second === 0 && f.ms === 0;
                    case "month":
                        return f.date === 1 && f.hour === 0 && f.minute === 0 && f.second === 0 && f.ms === 0;
                    case "day":
                        return f.hour === 0 && f.minute === 0 && f.second === 0 && f.ms === 0;
                    case "hour":
                        return f.minute === 0 && f.second === 0 && f.ms === 0;
                    case "minute":
                        return f.second === 0 && f.ms === 0;
                    case "second":
                        return f.ms === 0;
                }
                return false;
            }
        }
    ]);
    return DateTime;
}();
var Formatters = /*#__PURE__*/ function() {
    "use strict";
    function Formatters(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Formatters);
        this.w = w;
        this.tooltipKeyFormat = "dd MMM";
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Formatters, [
        {
            /**
   * @param {Function} fn
   * @param {any} val
   * @param {any} timestamp
   * @param {any} _opts
   */ key: "xLabelFormat",
            value: function xLabelFormat(fn, val, timestamp, _opts) {
                var w = this.w;
                if (w.config.xaxis.type === "datetime") {
                    if (w.config.xaxis.labels.formatter === void 0) {
                        if (w.config.tooltip.x.formatter === void 0) {
                            var datetimeObj = new DateTime(this.w);
                            return datetimeObj.formatDate(datetimeObj.getDate(val), w.config.tooltip.x.format);
                        }
                    }
                }
                return fn(val, timestamp, _opts);
            }
        },
        {
            /**
   * @param {any} val
   */ key: "defaultGeneralFormatter",
            value: function defaultGeneralFormatter(val) {
                if (Array.isArray(val)) {
                    return val.map(function(v) {
                        return v;
                    });
                } else {
                    return val;
                }
            }
        },
        {
            /**
   * @param {any} v
   * @param {ApexYAxis} yaxe
   */ key: "defaultYFormatter",
            value: function defaultYFormatter(v, yaxe) {
                var w = this.w;
                if (Utils$1.isNumber(v)) {
                    if (w.globals.yValueDecimal !== 0) {
                        v = v.toFixed(yaxe.decimalsInFloat !== void 0 ? yaxe.decimalsInFloat : w.globals.yValueDecimal);
                    } else {
                        var f = v.toFixed(0);
                        v = Number(f) === v ? f : v.toFixed(1);
                    }
                }
                return v;
            }
        },
        {
            key: "setLabelFormatters",
            value: function setLabelFormatters() {
                var _this = this;
                var w = this.w;
                var fmt = w.formatters;
                fmt.xaxisTooltipFormatter = function(val) {
                    return _this.defaultGeneralFormatter(val);
                };
                fmt.ttKeyFormatter = function(val) {
                    return _this.defaultGeneralFormatter(val);
                };
                fmt.ttZFormatter = function(val) {
                    return val;
                };
                fmt.legendFormatter = function(val) {
                    return _this.defaultGeneralFormatter(val);
                };
                if (w.config.xaxis.labels.formatter !== void 0) {
                    fmt.xLabelFormatter = w.config.xaxis.labels.formatter;
                } else {
                    fmt.xLabelFormatter = function(val) {
                        if (Utils$1.isNumber(val)) {
                            if (!w.config.xaxis.convertedCatToNumeric && w.config.xaxis.type === "numeric") {
                                if (Utils$1.isNumber(w.config.xaxis.decimalsInFloat)) {
                                    return val.toFixed(w.config.xaxis.decimalsInFloat);
                                } else {
                                    var diff = w.globals.maxX - w.globals.minX;
                                    if (diff > 0) {
                                        var step = diff / 10;
                                        var decimals = Math.max(0, -Math.floor(Math.log10(step)));
                                        return val.toFixed(decimals);
                                    }
                                    return val.toFixed(0);
                                }
                            }
                            if (w.globals.isBarHorizontal) {
                                var range = w.globals.maxY - w.globals.minY;
                                if (range < 4) {
                                    return val.toFixed(1);
                                }
                            }
                            return val.toFixed(0);
                        }
                        return val;
                    };
                }
                if (typeof w.config.tooltip.x.formatter === "function") {
                    fmt.ttKeyFormatter = w.config.tooltip.x.formatter;
                } else {
                    fmt.ttKeyFormatter = fmt.xLabelFormatter;
                }
                if (typeof w.config.xaxis.tooltip.formatter === "function") {
                    fmt.xaxisTooltipFormatter = w.config.xaxis.tooltip.formatter;
                }
                if (Array.isArray(w.config.tooltip.y)) {
                    fmt.ttVal = w.config.tooltip.y;
                } else {
                    if (w.config.tooltip.y.formatter !== void 0) {
                        fmt.ttVal = w.config.tooltip.y;
                    }
                }
                if (w.config.tooltip.z.formatter !== void 0) {
                    fmt.ttZFormatter = w.config.tooltip.z.formatter;
                }
                if (w.config.legend.formatter !== void 0) {
                    fmt.legendFormatter = w.config.legend.formatter;
                }
                fmt.yLabelFormatters = [];
                w.config.yaxis.forEach(function(yaxe, i) {
                    if (yaxe.labels.formatter !== void 0) {
                        fmt.yLabelFormatters[i] = yaxe.labels.formatter;
                    } else if (w.config.chart.type === "violin") {
                        var round = function round(v) {
                            return typeof v === "number" && isFinite(v) ? "".concat(Math.round(v * 100) / 100) : v;
                        };
                        fmt.yLabelFormatters[i] = function(val) {
                            if (!w.globals.xyCharts) return val;
                            return Array.isArray(val) ? val.map(round) : round(val);
                        };
                    } else {
                        fmt.yLabelFormatters[i] = function(val) {
                            if (!w.globals.xyCharts) return val;
                            if (Array.isArray(val)) {
                                return val.map(function(v) {
                                    return _this.defaultYFormatter(v, yaxe);
                                });
                            } else {
                                return _this.defaultYFormatter(val, yaxe);
                            }
                        };
                    }
                });
                return w.globals;
            }
        },
        {
            key: "heatmapLabelFormatters",
            value: function heatmapLabelFormatters() {
                var w = this.w;
                if (w.config.chart.type === "heatmap") {
                    w.globals.yAxisScale[0].result = /** @type {any} */ w.seriesData.seriesNames.slice();
                    var longest = /** @type {any} */ w.seriesData.seriesNames.reduce(function(a, b) {
                        return a.length > b.length ? a : b;
                    }, 0);
                    w.globals.yAxisScale[0].niceMax = longest;
                    w.globals.yAxisScale[0].niceMin = longest;
                }
            }
        }
    ]);
    return Formatters;
}();
var getRangeValues = function getRangeValues(param) {
    var isTimeline = param.isTimeline, seriesIndex = param.seriesIndex, dataPointIndex = param.dataPointIndex, y1 = param.y1, y2 = param.y2, w = param.w;
    var _a;
    var start = w.rangeData.seriesRangeStart[seriesIndex][dataPointIndex];
    var end = w.rangeData.seriesRangeEnd[seriesIndex][dataPointIndex];
    var ylabel = w.labelData.labels[dataPointIndex];
    var seriesName = w.config.series[seriesIndex].name ? w.config.series[seriesIndex].name : "";
    var yLbFormatter = w.formatters.ttKeyFormatter;
    var yLbTitleFormatter = w.config.tooltip.y.title.formatter;
    var opts = {
        w: w,
        seriesIndex: seriesIndex,
        dataPointIndex: dataPointIndex,
        start: start,
        end: end
    };
    if (typeof yLbTitleFormatter === "function") {
        seriesName = yLbTitleFormatter(seriesName, opts);
    }
    if ((_a = w.config.series[seriesIndex].data[dataPointIndex]) == null ? void 0 : _a.x) {
        ylabel = w.config.series[seriesIndex].data[dataPointIndex].x;
    }
    if (!isTimeline) {
        if (w.config.xaxis.type === "datetime") {
            var xFormat = new Formatters(w);
            ylabel = xFormat.xLabelFormat(w.formatters.ttKeyFormatter, ylabel, ylabel, {
                i: void 0,
                dateFormatter: new DateTime(w).formatDate,
                w: w
            });
        }
    }
    if (typeof yLbFormatter === "function") {
        ylabel = yLbFormatter(ylabel, opts);
    }
    if (Number.isFinite(y1) && Number.isFinite(y2)) {
        start = y1;
        end = y2;
    }
    var startVal = "";
    var endVal = "";
    var color = w.globals.colors[seriesIndex];
    if (w.config.tooltip.x.formatter === void 0) {
        if (w.config.xaxis.type === "datetime") {
            var datetimeObj = new DateTime(w);
            startVal = datetimeObj.formatDate(datetimeObj.getDate(start), w.config.tooltip.x.format);
            endVal = datetimeObj.formatDate(datetimeObj.getDate(end), w.config.tooltip.x.format);
        } else {
            startVal = start;
            endVal = end;
        }
    } else {
        startVal = w.config.tooltip.x.formatter(start);
        endVal = w.config.tooltip.x.formatter(end);
    }
    return {
        start: start,
        end: end,
        startVal: startVal,
        endVal: endVal,
        ylabel: ylabel,
        color: color,
        seriesName: seriesName
    };
};
var buildRangeTooltipHTML = function buildRangeTooltipHTML(opts) {
    var color = opts.color, seriesName = opts.seriesName, ylabel = opts.ylabel, start = opts.start, end = opts.end, seriesIndex = opts.seriesIndex, dataPointIndex = opts.dataPointIndex;
    var formatter = opts.w.globals.tooltip.tooltipLabels.getFormatters(seriesIndex);
    start = formatter.yLbFormatter(start);
    end = formatter.yLbFormatter(end);
    var val = formatter.yLbFormatter(opts.w.seriesData.series[seriesIndex][dataPointIndex]);
    var valueHTML = "";
    var rangeValues = '<span class="value start-value">\n  '.concat(start, '\n  </span> <span class="separator">-</span> <span class="value end-value">\n  ').concat(end, "\n  </span>");
    if (opts.w.globals.comboCharts) {
        if (opts.w.config.series[seriesIndex].type === "rangeArea" || opts.w.config.series[seriesIndex].type === "rangeBar") {
            valueHTML = rangeValues;
        } else {
            valueHTML = "<span>".concat(val, "</span>");
        }
    } else {
        valueHTML = rangeValues;
    }
    return '<div class="apexcharts-tooltip-rangebar"><div> <span class="series-name" style="color: ' + color + '">' + (seriesName ? seriesName : "") + '</span></div><div> <span class="category">' + ylabel + ": </span> " + valueHTML + " </div></div>";
};
var Defaults = /*#__PURE__*/ function() {
    "use strict";
    function Defaults(opts) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Defaults);
        this.opts = opts;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Defaults, [
        {
            key: "hideYAxis",
            value: function hideYAxis() {
                this.opts.yaxis[0].show = false;
                this.opts.yaxis[0].title.text = "";
                this.opts.yaxis[0].axisBorder.show = false;
                this.opts.yaxis[0].axisTicks.show = false;
                this.opts.yaxis[0].floating = true;
            }
        },
        {
            key: "line",
            value: function line() {
                return {
                    dataLabels: {
                        enabled: false
                    },
                    stroke: {
                        width: 5,
                        curve: "straight"
                    },
                    markers: {
                        size: 0,
                        hover: {
                            sizeOffset: 6
                        }
                    },
                    xaxis: {
                        crosshairs: {
                            width: 1
                        }
                    }
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} defaults
   */ key: "sparkline",
            value: function sparkline(defaults) {
                this.hideYAxis();
                var ret = {
                    grid: {
                        show: false,
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    },
                    legend: {
                        show: false
                    },
                    xaxis: {
                        labels: {
                            show: false
                        },
                        tooltip: {
                            enabled: false
                        },
                        axisBorder: {
                            show: false
                        },
                        axisTicks: {
                            show: false
                        }
                    },
                    chart: {
                        toolbar: {
                            show: false
                        },
                        zoom: {
                            enabled: false
                        }
                    },
                    dataLabels: {
                        enabled: false
                    }
                };
                return Utils$1.extend(defaults, ret);
            }
        },
        {
            key: "slope",
            value: function slope() {
                this.hideYAxis();
                return {
                    chart: {
                        toolbar: {
                            show: false
                        },
                        zoom: {
                            enabled: false
                        }
                    },
                    dataLabels: {
                        enabled: true,
                        /**
         * @param {any} val
         * @param {Record<string, any>} opts
         */ formatter: function formatter(val, opts) {
                            var seriesName = opts.w.config.series[opts.seriesIndex].name;
                            return val !== null ? seriesName + ": " + val : "";
                        },
                        background: {
                            enabled: false
                        },
                        offsetX: -5
                    },
                    grid: {
                        xaxis: {
                            lines: {
                                show: true
                            }
                        },
                        yaxis: {
                            lines: {
                                show: false
                            }
                        }
                    },
                    xaxis: {
                        position: "top",
                        labels: {
                            style: {
                                fontSize: 14,
                                fontWeight: 900
                            }
                        },
                        tooltip: {
                            enabled: false
                        },
                        crosshairs: {
                            show: false
                        }
                    },
                    markers: {
                        size: 8,
                        hover: {
                            sizeOffset: 1
                        }
                    },
                    legend: {
                        show: false
                    },
                    tooltip: {
                        shared: false,
                        intersect: true,
                        followCursor: true
                    },
                    stroke: {
                        width: 5,
                        curve: "straight"
                    }
                };
            }
        },
        {
            key: "bar",
            value: function bar() {
                return {
                    chart: {
                        stacked: false
                    },
                    plotOptions: {
                        bar: {
                            dataLabels: {
                                position: "center"
                            }
                        }
                    },
                    dataLabels: {
                        style: {
                            colors: [
                                "#fff"
                            ]
                        },
                        background: {
                            enabled: false
                        }
                    },
                    stroke: {
                        width: 0,
                        lineCap: "square"
                    },
                    fill: {
                        opacity: 0.85
                    },
                    legend: {
                        markers: {
                            shape: "square"
                        }
                    },
                    tooltip: {
                        shared: false,
                        intersect: true
                    },
                    xaxis: {
                        tooltip: {
                            enabled: false
                        },
                        tickPlacement: "between",
                        crosshairs: {
                            width: "barWidth",
                            position: "back",
                            fill: {
                                type: "gradient"
                            },
                            dropShadow: {
                                enabled: false
                            },
                            stroke: {
                                width: 0
                            }
                        }
                    }
                };
            }
        },
        {
            key: "funnel",
            value: function funnel() {
                this.hideYAxis();
                return __spreadProps(__spreadValues({}, this.bar()), {
                    chart: {
                        animations: {
                            speed: 800,
                            animateGradually: {
                                enabled: false
                            }
                        }
                    },
                    plotOptions: {
                        bar: {
                            horizontal: true,
                            borderRadiusApplication: "around",
                            borderRadius: 0,
                            dataLabels: {
                                position: "center"
                            }
                        }
                    },
                    grid: {
                        show: false,
                        padding: {
                            left: 0,
                            right: 0
                        }
                    },
                    xaxis: {
                        labels: {
                            show: false
                        },
                        tooltip: {
                            enabled: false
                        },
                        axisBorder: {
                            show: false
                        },
                        axisTicks: {
                            show: false
                        }
                    }
                });
            }
        },
        {
            key: "pyramid",
            value: function pyramid() {
                return this.funnel();
            }
        },
        {
            key: "gauge",
            value: function gauge() {
                var base = this.radialBar();
                return __spreadProps(__spreadValues({}, base), {
                    plotOptions: {
                        radialBar: {
                            startAngle: -135,
                            endAngle: 135,
                            hollow: {
                                margin: 0,
                                size: "60%"
                            },
                            track: {
                                background: "#e7e7e7",
                                strokeWidth: "100%",
                                margin: 5
                            },
                            dataLabels: {
                                name: {
                                    show: false
                                },
                                value: {
                                    show: true,
                                    fontSize: "32px",
                                    fontWeight: 600,
                                    offsetY: 8
                                }
                            }
                        }
                    }
                });
            }
        },
        {
            key: "candlestick",
            value: function candlestick() {
                var _this = this;
                return {
                    stroke: {
                        width: 1
                    },
                    fill: {
                        opacity: 1
                    },
                    dataLabels: {
                        enabled: false
                    },
                    tooltip: {
                        shared: true,
                        custom: function custom(param) {
                            var seriesIndex = param.seriesIndex, dataPointIndex = param.dataPointIndex, w = param.w;
                            return _this._getBoxTooltip(w, seriesIndex, dataPointIndex, [
                                "Open",
                                "High",
                                "",
                                "Low",
                                "Close"
                            ], "candlestick");
                        }
                    },
                    states: {
                        active: {
                            filter: {
                                type: "none"
                            }
                        }
                    },
                    xaxis: {
                        crosshairs: {
                            width: 1
                        }
                    }
                };
            }
        },
        {
            key: "boxPlot",
            value: function boxPlot() {
                var _this = this;
                return {
                    chart: {
                        animations: {
                            dynamicAnimation: {
                                enabled: false
                            }
                        }
                    },
                    stroke: {
                        width: 1,
                        colors: [
                            "#24292e"
                        ]
                    },
                    dataLabels: {
                        enabled: false
                    },
                    tooltip: {
                        shared: true,
                        custom: function custom(param) {
                            var seriesIndex = param.seriesIndex, dataPointIndex = param.dataPointIndex, w = param.w;
                            return _this._getBoxTooltip(w, seriesIndex, dataPointIndex, [
                                "Minimum",
                                "Q1",
                                "Median",
                                "Q3",
                                "Maximum"
                            ], "boxPlot");
                        }
                    },
                    markers: {
                        size: 7,
                        strokeWidth: 1,
                        strokeColors: "#111"
                    },
                    xaxis: {
                        crosshairs: {
                            width: 1
                        }
                    }
                };
            }
        },
        {
            key: "violin",
            value: function violin() {
                var _this = this;
                return {
                    chart: {
                        // Violins are a per-category distribution plot (discrete category
                        // x-axis), so range zooming/panning is meaningless — off by default.
                        zoom: {
                            enabled: false
                        },
                        animations: {
                            dynamicAnimation: {
                                enabled: false
                            }
                        }
                    },
                    stroke: {
                        width: 1,
                        colors: [
                            "#24292e"
                        ]
                    },
                    fill: {
                        opacity: 0.7
                    },
                    dataLabels: {
                        enabled: false
                    },
                    tooltip: {
                        shared: true,
                        custom: function custom(param) {
                            var seriesIndex = param.seriesIndex, dataPointIndex = param.dataPointIndex, w = param.w;
                            return _this._getViolinTooltip(w, seriesIndex, dataPointIndex);
                        }
                    },
                    states: {
                        active: {
                            filter: {
                                type: "none"
                            }
                        }
                    },
                    xaxis: {
                        crosshairs: {
                            width: 1
                        }
                    }
                };
            }
        },
        {
            key: "rangeBar",
            value: function rangeBar() {
                var handleTimelineTooltip = function handleTimelineTooltip(opts) {
                    var _getRangeValues = getRangeValues(__spreadProps(__spreadValues({}, opts), {
                        isTimeline: true
                    })), color = _getRangeValues.color, seriesName = _getRangeValues.seriesName, ylabel = _getRangeValues.ylabel, startVal = _getRangeValues.startVal, endVal = _getRangeValues.endVal;
                    return buildRangeTooltipHTML(__spreadProps(__spreadValues({}, opts), {
                        color: color,
                        seriesName: seriesName,
                        ylabel: ylabel,
                        start: startVal,
                        end: endVal
                    }));
                };
                var handleRangeColumnTooltip = function handleRangeColumnTooltip(opts) {
                    var _getRangeValues = getRangeValues(opts), color = _getRangeValues.color, seriesName = _getRangeValues.seriesName, ylabel = _getRangeValues.ylabel, start = _getRangeValues.start, end = _getRangeValues.end;
                    return buildRangeTooltipHTML(__spreadProps(__spreadValues({}, opts), {
                        color: color,
                        seriesName: seriesName,
                        ylabel: ylabel,
                        start: start,
                        end: end
                    }));
                };
                return {
                    chart: {
                        animations: {
                            animateGradually: false
                        }
                    },
                    stroke: {
                        width: 0,
                        lineCap: "square"
                    },
                    plotOptions: {
                        bar: {
                            borderRadius: 0,
                            dataLabels: {
                                position: "center"
                            }
                        }
                    },
                    dataLabels: {
                        enabled: false,
                        /**
         * @param {any} val
         */ formatter: function formatter(val, param) {
                            var seriesIndex = param.seriesIndex, dataPointIndex = param.dataPointIndex, w = param.w;
                            var getVal = function getVal() {
                                var start = w.rangeData.seriesRangeStart[seriesIndex][dataPointIndex];
                                var end = w.rangeData.seriesRangeEnd[seriesIndex][dataPointIndex];
                                return end - start;
                            };
                            if (w.globals.comboCharts) {
                                if (w.config.series[seriesIndex].type === "rangeBar" || w.config.series[seriesIndex].type === "rangeArea") {
                                    return getVal();
                                } else {
                                    return val;
                                }
                            } else {
                                return getVal();
                            }
                        },
                        background: {
                            enabled: false
                        },
                        style: {
                            colors: [
                                "#fff"
                            ]
                        }
                    },
                    markers: {
                        size: 10
                    },
                    tooltip: {
                        shared: false,
                        followCursor: true,
                        /**
         * @param {Record<string, any>} opts
         */ custom: function custom(opts) {
                            if (opts.w.config.plotOptions && opts.w.config.plotOptions.bar && opts.w.config.plotOptions.bar.horizontal) {
                                return handleTimelineTooltip(opts);
                            } else {
                                return handleRangeColumnTooltip(opts);
                            }
                        }
                    },
                    xaxis: {
                        tickPlacement: "between",
                        tooltip: {
                            enabled: false
                        },
                        crosshairs: {
                            stroke: {
                                width: 0
                            }
                        }
                    }
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "dumbbell",
            value: function dumbbell(opts) {
                var _a, _b;
                if (!((_a = opts.plotOptions.bar) == null ? void 0 : _a.barHeight)) {
                    opts.plotOptions.bar.barHeight = 2;
                }
                if (!((_b = opts.plotOptions.bar) == null ? void 0 : _b.columnWidth)) {
                    opts.plotOptions.bar.columnWidth = 2;
                }
                return opts;
            }
        },
        {
            key: "area",
            value: function area() {
                return {
                    stroke: {
                        width: 4,
                        fill: {
                            type: "solid",
                            gradient: {
                                inverseColors: false,
                                shade: "light",
                                type: "vertical",
                                opacityFrom: 0.65,
                                opacityTo: 0.5,
                                stops: [
                                    0,
                                    100,
                                    100
                                ]
                            }
                        }
                    },
                    fill: {
                        type: "gradient",
                        gradient: {
                            inverseColors: false,
                            shade: "light",
                            type: "vertical",
                            opacityFrom: 0.65,
                            opacityTo: 0.5,
                            stops: [
                                0,
                                100,
                                100
                            ]
                        }
                    },
                    markers: {
                        size: 0,
                        hover: {
                            sizeOffset: 6
                        }
                    },
                    tooltip: {
                        followCursor: false
                    }
                };
            }
        },
        {
            key: "rangeArea",
            value: function rangeArea() {
                var handleRangeAreaTooltip = function handleRangeAreaTooltip(opts) {
                    var _getRangeValues = getRangeValues(opts), color = _getRangeValues.color, seriesName = _getRangeValues.seriesName, ylabel = _getRangeValues.ylabel, start = _getRangeValues.start, end = _getRangeValues.end;
                    return buildRangeTooltipHTML(__spreadProps(__spreadValues({}, opts), {
                        color: color,
                        seriesName: seriesName,
                        ylabel: ylabel,
                        start: start,
                        end: end
                    }));
                };
                return {
                    stroke: {
                        curve: "straight",
                        width: 0
                    },
                    fill: {
                        type: "solid",
                        opacity: 0.6
                    },
                    markers: {
                        size: 0
                    },
                    states: {
                        hover: {
                            filter: {
                                type: "none"
                            }
                        },
                        active: {
                            filter: {
                                type: "none"
                            }
                        }
                    },
                    tooltip: {
                        intersect: false,
                        shared: true,
                        followCursor: true,
                        /**
         * @param {Record<string, any>} opts
         */ custom: function custom(opts) {
                            return handleRangeAreaTooltip(opts);
                        }
                    }
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} defaults
   */ key: "brush",
            value: function brush(defaults) {
                var ret = {
                    chart: {
                        toolbar: {
                            autoSelected: "selection",
                            show: false
                        },
                        zoom: {
                            enabled: false
                        }
                    },
                    dataLabels: {
                        enabled: false
                    },
                    stroke: {
                        width: 1
                    },
                    tooltip: {
                        enabled: false
                    },
                    xaxis: {
                        tooltip: {
                            enabled: false
                        }
                    }
                };
                return Utils$1.extend(defaults, ret);
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "stacked100",
            value: function stacked100(opts) {
                opts.dataLabels = opts.dataLabels || {};
                opts.dataLabels.formatter = opts.dataLabels.formatter || void 0;
                var existingDataLabelFormatter = opts.dataLabels.formatter;
                opts.yaxis.forEach(function(yaxe, index) {
                    opts.yaxis[index].min = 0;
                    opts.yaxis[index].max = 100;
                });
                var isBar = opts.chart.type === "bar";
                if (isBar) {
                    opts.dataLabels.formatter = existingDataLabelFormatter || /**
       * @param {any} val
       */ function(val) {
                        if (typeof val === "number") {
                            return val ? val.toFixed(0) + "%" : val;
                        }
                        return val;
                    };
                }
                return opts;
            }
        },
        {
            key: "stackedBars",
            value: function stackedBars() {
                var barDefaults = this.bar();
                return __spreadProps(__spreadValues({}, barDefaults), {
                    plotOptions: __spreadProps(__spreadValues({}, barDefaults.plotOptions), {
                        bar: __spreadProps(__spreadValues({}, barDefaults.plotOptions.bar), {
                            borderRadiusApplication: "end",
                            borderRadiusWhenStacked: "last"
                        })
                    })
                });
            }
        },
        {
            // This function removes the left and right spacing in chart for line/area/scatter if xaxis type = category for those charts by converting xaxis = numeric. Numeric/Datetime xaxis prevents the unnecessary spacing in the left/right of the chart area
            /**
   * @param {Record<string, any>} opts
   */ key: "convertCatToNumeric",
            value: function convertCatToNumeric(opts) {
                opts.xaxis.convertedCatToNumeric = true;
                return opts;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   * @param {any} cats
   */ key: "convertCatToNumericXaxis",
            value: function convertCatToNumericXaxis(opts, cats) {
                opts.xaxis.type = "numeric";
                opts.xaxis.labels = opts.xaxis.labels || {};
                opts.xaxis.labels.formatter = opts.xaxis.labels.formatter || /**
     * @param {any} val
     */ function(val) {
                    return Utils$1.isNumber(val) ? Math.floor(val) : val;
                };
                var defaultFormatter = opts.xaxis.labels.formatter;
                var labels = opts.xaxis.categories && opts.xaxis.categories.length ? opts.xaxis.categories : opts.labels;
                if (cats && cats.length) {
                    labels = cats.map(function(c) {
                        return Array.isArray(c) ? c : String(c);
                    });
                }
                if (labels && labels.length) {
                    opts.xaxis.labels.formatter = function(val) {
                        return Utils$1.isNumber(val) ? defaultFormatter(labels[Math.floor(val) - 1]) : defaultFormatter(val);
                    };
                }
                opts.xaxis.categories = [];
                opts.labels = [];
                opts.xaxis.tickAmount = opts.xaxis.tickAmount || "dataPoints";
                return opts;
            }
        },
        {
            key: "bubble",
            value: function bubble() {
                return {
                    dataLabels: {
                        style: {
                            colors: [
                                "#fff"
                            ]
                        }
                    },
                    tooltip: {
                        shared: false,
                        intersect: true
                    },
                    xaxis: {
                        crosshairs: {
                            width: 0
                        }
                    },
                    fill: {
                        type: "solid",
                        gradient: {
                            shade: "light",
                            inverse: true,
                            shadeIntensity: 0.55,
                            opacityFrom: 0.4,
                            opacityTo: 0.8
                        }
                    }
                };
            }
        },
        {
            key: "scatter",
            value: function scatter() {
                return {
                    dataLabels: {
                        enabled: false
                    },
                    tooltip: {
                        shared: false,
                        intersect: true
                    },
                    markers: {
                        size: 6,
                        strokeWidth: 1,
                        hover: {
                            sizeOffset: 2
                        }
                    }
                };
            }
        },
        {
            key: "heatmap",
            value: function heatmap() {
                return {
                    chart: {
                        stacked: false,
                        // A heatmap is a fixed grid: zooming/panning only distorts the cells
                        // and (on a datetime axis) collapses the month labels to repeats, so
                        // it is off by default, mirroring treemap. Users can opt back in with
                        // chart.zoom.enabled: true.
                        zoom: {
                            enabled: false
                        }
                    },
                    fill: {
                        opacity: 1
                    },
                    dataLabels: {
                        style: {
                            colors: [
                                "#fff"
                            ]
                        }
                    },
                    stroke: {
                        colors: [
                            "#fff"
                        ]
                    },
                    tooltip: {
                        // Anchor the tooltip above the hovered cell with a downward arrow
                        // (flipping below near the top edge), the same treatment horizontal
                        // bars get, rather than trailing the cursor. Opt back into the old
                        // behavior with tooltip.followCursor: true.
                        followCursor: false,
                        marker: {
                            show: false
                        },
                        x: {
                            show: false
                        }
                    },
                    legend: {
                        position: "top",
                        markers: {
                            shape: "square"
                        }
                    },
                    grid: {
                        padding: {
                            right: 20
                        }
                    }
                };
            }
        },
        {
            key: "treemap",
            value: function treemap() {
                return {
                    chart: {
                        zoom: {
                            enabled: false
                        }
                    },
                    dataLabels: {
                        style: {
                            fontSize: 14,
                            fontWeight: 600,
                            colors: [
                                "#fff"
                            ]
                        }
                    },
                    stroke: {
                        show: true,
                        width: 2,
                        colors: [
                            "#fff"
                        ]
                    },
                    legend: {
                        show: false
                    },
                    fill: {
                        opacity: 1,
                        gradient: {
                            stops: [
                                0,
                                100
                            ]
                        }
                    },
                    tooltip: {
                        followCursor: true,
                        x: {
                            show: false
                        }
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0
                        }
                    },
                    xaxis: {
                        crosshairs: {
                            show: false
                        },
                        tooltip: {
                            enabled: false
                        }
                    }
                };
            }
        },
        {
            key: "unit",
            value: function unit() {
                return {
                    chart: {
                        toolbar: {
                            show: false
                        }
                    },
                    dataLabels: {
                        enabled: false
                    },
                    stroke: {
                        show: false,
                        width: 0
                    },
                    fill: {
                        opacity: 1
                    },
                    tooltip: {
                        followCursor: true,
                        x: {
                            show: false
                        }
                    },
                    legend: {
                        show: true,
                        position: "bottom"
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    }
                };
            }
        },
        {
            key: "sunburst",
            value: function sunburst() {
                return {
                    chart: {
                        toolbar: {
                            show: false
                        }
                    },
                    dataLabels: {
                        style: {
                            colors: [
                                "#fff"
                            ]
                        },
                        dropShadow: {
                            enabled: true
                        }
                    },
                    stroke: {
                        colors: [
                            "#fff"
                        ]
                    },
                    fill: {
                        opacity: 1
                    },
                    // Unlike pie, sunburst keeps the STANDARD themed tooltip (light/dark).
                    // Slice-coloured tooltips (fillSeriesColor) wash out here because child
                    // arcs are tinted toward white per depth; users can still opt in.
                    legend: {
                        position: "right"
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    }
                };
            }
        },
        {
            key: "pie",
            value: function pie() {
                return {
                    chart: {
                        toolbar: {
                            show: false
                        }
                    },
                    plotOptions: {
                        pie: {
                            donut: {
                                labels: {
                                    show: false
                                }
                            }
                        }
                    },
                    dataLabels: {
                        /**
         * @param {number} val
         */ formatter: function formatter(val) {
                            return val.toFixed(1) + "%";
                        },
                        style: {
                            colors: [
                                "#fff"
                            ]
                        },
                        background: {
                            enabled: false
                        },
                        dropShadow: {
                            enabled: true
                        }
                    },
                    stroke: {
                        colors: [
                            "#fff"
                        ]
                    },
                    fill: {
                        opacity: 1,
                        gradient: {
                            shade: "light",
                            stops: [
                                0,
                                100
                            ]
                        }
                    },
                    tooltip: {
                        theme: "dark",
                        fillSeriesColor: true
                    },
                    legend: {
                        position: "right"
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    }
                };
            }
        },
        {
            key: "donut",
            value: function donut() {
                return {
                    chart: {
                        toolbar: {
                            show: false
                        }
                    },
                    dataLabels: {
                        /**
         * @param {number} val
         */ formatter: function formatter(val) {
                            return val.toFixed(1) + "%";
                        },
                        style: {
                            colors: [
                                "#fff"
                            ]
                        },
                        background: {
                            enabled: false
                        },
                        dropShadow: {
                            enabled: true
                        }
                    },
                    stroke: {
                        colors: [
                            "#fff"
                        ]
                    },
                    fill: {
                        opacity: 1,
                        gradient: {
                            shade: "light",
                            shadeIntensity: 0.35,
                            stops: [
                                80,
                                100
                            ],
                            opacityFrom: 1,
                            opacityTo: 1
                        }
                    },
                    tooltip: {
                        theme: "dark",
                        fillSeriesColor: true
                    },
                    legend: {
                        position: "right"
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    }
                };
            }
        },
        {
            key: "polarArea",
            value: function polarArea() {
                return {
                    chart: {
                        toolbar: {
                            show: false
                        }
                    },
                    dataLabels: {
                        /**
         * @param {number} val
         */ formatter: function formatter(val) {
                            return val.toFixed(1) + "%";
                        },
                        enabled: false
                    },
                    stroke: {
                        show: true,
                        width: 2
                    },
                    fill: {
                        opacity: 0.7
                    },
                    tooltip: {
                        theme: "dark",
                        fillSeriesColor: true
                    },
                    legend: {
                        position: "right"
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    }
                };
            }
        },
        {
            key: "radar",
            value: function radar() {
                this.opts.yaxis[0].labels.offsetY = this.opts.yaxis[0].labels.offsetY ? this.opts.yaxis[0].labels.offsetY : 6;
                return {
                    dataLabels: {
                        enabled: false,
                        style: {
                            fontSize: "11px"
                        }
                    },
                    stroke: {
                        width: 2
                    },
                    markers: {
                        size: 5,
                        strokeWidth: 1,
                        strokeOpacity: 1
                    },
                    fill: {
                        opacity: 0.2
                    },
                    tooltip: {
                        shared: false,
                        intersect: true,
                        followCursor: true
                    },
                    grid: {
                        show: false,
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    },
                    xaxis: {
                        labels: {
                            formatter: function formatter(val) {
                                return val;
                            },
                            style: {
                                colors: [
                                    "#a8a8a8"
                                ],
                                fontSize: "11px"
                            }
                        },
                        tooltip: {
                            enabled: false
                        },
                        crosshairs: {
                            show: false
                        }
                    }
                };
            }
        },
        {
            key: "radialBar",
            value: function radialBar() {
                return {
                    chart: {
                        animations: {
                            dynamicAnimation: {
                                enabled: true,
                                speed: 800
                            }
                        },
                        toolbar: {
                            show: false
                        }
                    },
                    stroke: {
                        // Radial value arcs are stroked open arcs; square/round caps would
                        // extend the stroke half a stroke-width past each endpoint, making
                        // the "starting edge" visibly stick out past the geometric arc.
                        // Butt cap is the only one that aligns with the arc's true angular
                        // span. Without this, a chart that previously was a bar (whose
                        // defaults set lineCap='square') would carry that cap across into
                        // the radial render after a type morph.
                        lineCap: "butt"
                    },
                    fill: {
                        gradient: {
                            shade: "dark",
                            shadeIntensity: 0.4,
                            inverseColors: false,
                            type: "diagonal2",
                            opacityFrom: 1,
                            opacityTo: 1,
                            stops: [
                                70,
                                98,
                                100
                            ]
                        }
                    },
                    legend: {
                        show: false,
                        position: "right"
                    },
                    tooltip: {
                        enabled: false,
                        fillSeriesColor: true
                    },
                    grid: {
                        padding: {
                            left: 0,
                            right: 0,
                            top: 0,
                            bottom: 0
                        }
                    }
                };
            }
        },
        {
            /**
   * @param {import('../../types/internal').ChartStateW} w
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   * @param {any[]} labels
   * @param {string} chartType
   */ key: "_getBoxTooltip",
            value: function _getBoxTooltip(w, seriesIndex, dataPointIndex, labels, chartType) {
                var o = w.candleData.seriesCandleO[seriesIndex][dataPointIndex];
                var h = w.candleData.seriesCandleH[seriesIndex][dataPointIndex];
                var m = w.candleData.seriesCandleM[seriesIndex][dataPointIndex];
                var l = w.candleData.seriesCandleL[seriesIndex][dataPointIndex];
                var c = w.candleData.seriesCandleC[seriesIndex][dataPointIndex];
                var _si = /** @type {Record<string,any>} */ w.config.series[seriesIndex];
                if (_si.type && _si.type !== chartType) {
                    return '<div class="apexcharts-custom-tooltip">\n          '.concat(_si.name ? _si.name : "series-" + (seriesIndex + 1), ": <strong>").concat(w.seriesData.series[seriesIndex][dataPointIndex], "</strong>\n        </div>");
                } else {
                    return '<div class="apexcharts-tooltip-box apexcharts-tooltip-'.concat(w.config.chart.type, '"><div>').concat(labels[0], ': <span class="value">') + o + "</span></div><div>".concat(labels[1], ': <span class="value">') + h + "</span></div>" + (m ? "<div>".concat(labels[2], ': <span class="value">') + m + "</span></div>" : "") + "<div>".concat(labels[3], ': <span class="value">') + l + "</span></div><div>".concat(labels[4], ': <span class="value">') + c + "</span></div></div>";
                }
            }
        },
        {
            /**
   * Shared tooltip for a violin: distribution value range and observation
   * count. Per-point hover is intentionally unsupported (jitter renders as a
   * single path), so the tooltip summarizes the violin as a whole.
   *
   * @param {import('../../types/internal').ChartStateW} w
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   */ key: "_getViolinTooltip",
            value: function _getViolinTooltip(w, seriesIndex, dataPointIndex) {
                var _a, _b, _c;
                var minV = (_a = w.violinData.seriesViolinMin[seriesIndex]) == null ? void 0 : _a[dataPointIndex];
                var maxV = (_b = w.violinData.seriesViolinMax[seriesIndex]) == null ? void 0 : _b[dataPointIndex];
                var pts = ((_c = w.violinData.seriesViolinPoints[seriesIndex]) == null ? void 0 : _c[dataPointIndex]) || [];
                var name2 = /** @type {Record<string,any>} */ w.config.series[seriesIndex].name || "series-" + (seriesIndex + 1);
                return '<div class="apexcharts-tooltip-box apexcharts-tooltip-'.concat(w.config.chart.type, '"><div class="apexcharts-tooltip-violin-name">').concat(name2, '</div><div>Min: <span class="value">').concat(minV, '</span></div><div>Max: <span class="value">').concat(maxV, '</span></div><div>Observations: <span class="value">').concat(pts.length, "</span></div></div>");
            }
        }
    ]);
    return Defaults;
}();
var name = "en";
var options = {
    "months": [
        "January",
        "February",
        "March",
        "April",
        "May",
        "June",
        "July",
        "August",
        "September",
        "October",
        "November",
        "December"
    ],
    "shortMonths": [
        "Jan",
        "Feb",
        "Mar",
        "Apr",
        "May",
        "Jun",
        "Jul",
        "Aug",
        "Sep",
        "Oct",
        "Nov",
        "Dec"
    ],
    "days": [
        "Sunday",
        "Monday",
        "Tuesday",
        "Wednesday",
        "Thursday",
        "Friday",
        "Saturday"
    ],
    "shortDays": [
        "Sun",
        "Mon",
        "Tue",
        "Wed",
        "Thu",
        "Fri",
        "Sat"
    ],
    "toolbar": {
        "exportToSVG": "Download SVG",
        "exportToPNG": "Download PNG",
        "exportToCSV": "Download CSV",
        "menu": "Menu",
        "selection": "Selection",
        "selectionZoom": "Selection Zoom",
        "zoomIn": "Zoom In",
        "zoomOut": "Zoom Out",
        "pan": "Panning",
        "reset": "Reset Zoom",
        "measure": "Measure"
    }
};
var en = {
    name: name,
    options: options
};
var Options = /*#__PURE__*/ function() {
    "use strict";
    function Options() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Options);
        this.yAxis = {
            show: true,
            showAlways: false,
            showForNullSeries: true,
            seriesName: void 0,
            opposite: false,
            reversed: false,
            logarithmic: false,
            logBase: 10,
            tickAmount: void 0,
            stepSize: void 0,
            forceNiceScale: false,
            alignZero: false,
            max: void 0,
            min: void 0,
            floating: false,
            decimalsInFloat: void 0,
            labels: {
                show: true,
                showDuplicates: false,
                minWidth: 0,
                maxWidth: 160,
                offsetX: 0,
                offsetY: 0,
                align: void 0,
                rotate: 0,
                padding: 20,
                style: {
                    colors: [],
                    fontSize: "11px",
                    fontWeight: 400,
                    fontFamily: void 0,
                    cssClass: ""
                },
                formatter: void 0
            },
            axisBorder: {
                show: false,
                color: "#e0e0e0",
                width: 1,
                offsetX: 0,
                offsetY: 0
            },
            axisTicks: {
                show: false,
                color: "#e0e0e0",
                width: 6,
                offsetX: 0,
                offsetY: 0
            },
            title: {
                text: void 0,
                rotate: -90,
                offsetY: 0,
                offsetX: 0,
                style: {
                    color: void 0,
                    fontSize: "11px",
                    fontWeight: 900,
                    fontFamily: void 0,
                    cssClass: ""
                }
            },
            tooltip: {
                enabled: false,
                offsetX: 0
            },
            crosshairs: {
                show: true,
                position: "front",
                stroke: {
                    color: "#b6b6b6",
                    width: 1,
                    dashArray: 0
                }
            }
        };
        this.pointAnnotation = {
            id: void 0,
            x: 0,
            y: null,
            yAxisIndex: 0,
            seriesIndex: void 0,
            mouseEnter: void 0,
            mouseLeave: void 0,
            click: void 0,
            marker: {
                size: 4,
                fillColor: "#fff",
                strokeWidth: 2,
                strokeColor: "#333",
                shape: "circle",
                offsetX: 0,
                offsetY: 0,
                // radius: 2, // DEPRECATED
                cssClass: ""
            },
            label: {
                borderColor: "#c2c2c2",
                borderWidth: 1,
                borderRadius: 2,
                text: void 0,
                textAnchor: "middle",
                offsetX: 0,
                offsetY: 0,
                mouseEnter: void 0,
                mouseLeave: void 0,
                click: void 0,
                style: {
                    background: "#fff",
                    color: void 0,
                    fontSize: "11px",
                    fontFamily: void 0,
                    fontWeight: 400,
                    cssClass: "",
                    padding: {
                        left: 5,
                        right: 5,
                        top: 2,
                        bottom: 2
                    }
                }
            },
            customSVG: {
                // this will be deprecated in the next major version as it is going to be replaced with a better alternative below (image)
                SVG: void 0,
                cssClass: void 0,
                offsetX: 0,
                offsetY: 0
            },
            image: {
                path: void 0,
                width: 20,
                height: 20,
                offsetX: 0,
                offsetY: 0
            },
            tooltip: {
                // Show a hover tooltip over the annotation marker, like a regular
                // data point. Lets you surface richer detail than fits in the label.
                enabled: false,
                // Static tooltip content (HTML allowed; array joined with <br/>).
                // Falls back to label.text when omitted.
                text: void 0,
                // formatter({ annotation, seriesIndex, id, w }) => string (HTML).
                // Takes precedence over `text` when provided.
                formatter: void 0,
                // 'light' | 'dark'. Falls back to the global tooltip.theme.
                theme: void 0,
                offsetX: 0,
                offsetY: 0
            }
        };
        this.yAxisAnnotation = {
            id: void 0,
            y: 0,
            y2: null,
            strokeDashArray: 1,
            fillColor: "#c2c2c2",
            borderColor: "#c2c2c2",
            borderWidth: 1,
            opacity: 0.3,
            offsetX: 0,
            offsetY: 0,
            width: "100%",
            yAxisIndex: 0,
            label: {
                borderColor: "#c2c2c2",
                borderWidth: 1,
                borderRadius: 2,
                text: void 0,
                textAnchor: "end",
                position: "right",
                offsetX: 0,
                offsetY: -3,
                mouseEnter: void 0,
                mouseLeave: void 0,
                click: void 0,
                style: {
                    background: "#fff",
                    color: void 0,
                    fontSize: "11px",
                    fontFamily: void 0,
                    fontWeight: 400,
                    cssClass: "",
                    padding: {
                        left: 5,
                        right: 5,
                        top: 2,
                        bottom: 2
                    }
                }
            }
        };
        this.xAxisAnnotation = {
            id: void 0,
            x: 0,
            x2: null,
            strokeDashArray: 1,
            fillColor: "#c2c2c2",
            borderColor: "#c2c2c2",
            borderWidth: 1,
            opacity: 0.3,
            offsetX: 0,
            offsetY: 0,
            label: {
                borderColor: "#c2c2c2",
                borderWidth: 1,
                borderRadius: 2,
                text: void 0,
                textAnchor: "middle",
                orientation: "vertical",
                position: "top",
                offsetX: 0,
                offsetY: 0,
                mouseEnter: void 0,
                mouseLeave: void 0,
                click: void 0,
                style: {
                    background: "#fff",
                    color: void 0,
                    fontSize: "11px",
                    fontFamily: void 0,
                    fontWeight: 400,
                    cssClass: "",
                    padding: {
                        left: 5,
                        right: 5,
                        top: 2,
                        bottom: 2
                    }
                }
            }
        };
        this.text = {
            x: 0,
            y: 0,
            text: "",
            textAnchor: "start",
            foreColor: void 0,
            fontSize: "13px",
            fontFamily: void 0,
            fontWeight: 400,
            appendTo: ".apexcharts-annotations",
            backgroundColor: "transparent",
            borderColor: "#c2c2c2",
            borderRadius: 0,
            borderWidth: 0,
            paddingLeft: 4,
            paddingRight: 4,
            paddingTop: 2,
            paddingBottom: 2
        };
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Options, [
        {
            key: "init",
            value: function init() {
                return {
                    annotations: {
                        yaxis: [
                            this.yAxisAnnotation
                        ],
                        xaxis: [
                            this.xAxisAnnotation
                        ],
                        points: [
                            this.pointAnnotation
                        ],
                        texts: [],
                        images: [],
                        shapes: []
                    },
                    // Weave (#1): public plugin platform. Per-chart activation list:
                    // { name, options?, order? }. Requires the Weave host to be bundled
                    // (`import 'apexcharts/features/weave'`, included in the full bundle) and
                    // the plugin registered via ApexCharts.registerPlugin().
                    plugins: [],
                    chart: {
                        animations: {
                            // Master switch — set false to render charts without any animation.
                            // Each chart type gets a tailored initial-mount animation by default:
                            //   line/area/rangeArea/radar: pen-stroke draw + radial fill bloom
                            //   bar/stacked/range/funnel:  grow from baseline (+ stagger)
                            //   scatter/bubble:            scale-up pop with overshoot
                            //   heatmap:                   diagonal-wave cell reveal
                            //   treemap:                   largest-tile-first cascade
                            //   pie/donut/polar/gauge:     arc sweep + needle settle
                            // Speed is controlled by `speed`; per-element stagger by
                            // `animateGradually.enabled` / `animateGradually.delay`.
                            enabled: true,
                            speed: 800,
                            // Cadence (#6): easing for the generic tweens (data-update value
                            // transitions, path morphs, marker animate). A registered name, a
                            // cubic-bezier [x1,y1,x2,y2] array, or a function (t in [0,1]).
                            // 'easeInOutSine' is the historical curve, so the default is
                            // behavior-neutral. The tuned initial-draw pen/pop easings are fixed.
                            easing: "easeInOutSine",
                            animateGradually: {
                                // Drives per-element stagger across all chart types. When enabled,
                                // bars/heatmap-cells/scatter-points/treemap-tiles reveal in
                                // sequence; line/area markers fade in progressively along the
                                // draw. `delay` is the requested step in ms (auto-capped per
                                // chart so total stagger ≤ ~half the animation duration).
                                delay: 150,
                                enabled: true
                            },
                            dynamicAnimation: {
                                // Data-change (updateSeries) animation. Independent from the
                                // initial-mount animations above.
                                enabled: true,
                                speed: 350,
                                // Easing for data-change morphs only (same accepted forms as
                                // `animations.easing`). Unset -> inherit the chart-wide easing,
                                // except detected streaming scrolls (rolling window / append
                                // under xaxis.range) which default to 'linear' so the window
                                // slides at constant velocity instead of pulsing each tick.
                                easing: void 0
                            },
                            chartTypeMorph: {
                                // Cross-type morph (updateOptions changing chart.type). Bridges
                                // the destroy+recreate flicker by sampling source + target paths
                                // into N evenly-spaced perimeter points and tweening point-by-point
                                // with rotation-search alignment, so the transition is always smooth
                                // and non-self-intersecting even between very different shapes (bar
                                // rect ↔ pie wedge / radial arc). Supported pairs include bar ↔
                                // pie / donut / radialBar / polarArea / funnel / pyramid (plus the
                                // trivial pie ↔ donut ↔ polarArea cases). Falls back to instant
                                // snap when types or data shape are incompatible.
                                enabled: true,
                                speed: 600
                            },
                            // Honor the OS-level prefers-reduced-motion setting. When true (default)
                            // and the user has the accessibility preference enabled, all initial-mount
                            // animations are skipped and the chart renders instantly.
                            respectReducedMotion: true,
                            // Above this many data points, per-element morph + stagger (which
                            // spins up one JS-driven animation timeline per path — three chained
                            // tweens each in morphSVG) is replaced by a single GPU-composited
                            // opacity fade of the whole series. Thousands of candlesticks/bars
                            // otherwise jank the main thread on initial render and on every zoom.
                            // The fade reuses the existing delayedElements reveal so the result
                            // still animates in, just at O(1) cost instead of O(n). Set to 0 to
                            // always animate per-element regardless of dataset size.
                            largeDatasetThreshold: 1e3
                        },
                        background: "",
                        locales: [
                            en
                        ],
                        defaultLocale: "en",
                        // Perspectives (#10): serializable/shareable view state. Passive:
                        // requires `import 'apexcharts/features/perspectives'`. serializeOptions
                        // is the whitelist of function-free option paths stored in a token.
                        perspectives: {
                            serializeOptions: [
                                "theme",
                                "xaxis",
                                "yaxis",
                                "title",
                                "subtitle"
                            ]
                        },
                        // Rewind (#3): undo/redo history. Opt-in (bundle + behavior): requires
                        // `import 'apexcharts/features/history'` AND chart.history.enabled.
                        history: {
                            enabled: false,
                            maxDepth: 100,
                            coalesceMs: 250,
                            keyboard: true
                        },
                        // Strata (#2): hybrid SVG+canvas series renderer. 'svg' (default) |
                        // 'canvas' | 'auto'. 'auto'/'canvas' need the canvas renderer feature
                        // (`import 'apexcharts/features/renderer-canvas'`); without it, or with
                        // a canvas-unsupported feature (pattern/image fill, color-matrix state
                        // filters), selection falls back to 'svg'. Only the series layer is
                        // canvas-capable in v1; chrome stays SVG.
                        renderer: "svg",
                        rendererThreshold: 8e3,
                        layers: {
                            series: "auto",
                            grid: "svg",
                            annotations: "svg",
                            dataLabels: "svg"
                        },
                        dropShadow: {
                            enabled: false,
                            enabledOnSeries: void 0,
                            top: 2,
                            left: 2,
                            blur: 4,
                            color: "#000",
                            opacity: 0.7
                        },
                        events: {
                            animationEnd: void 0,
                            beforeMount: void 0,
                            mounted: void 0,
                            updated: void 0,
                            click: void 0,
                            mouseMove: void 0,
                            mouseLeave: void 0,
                            xAxisLabelClick: void 0,
                            legendClick: void 0,
                            markerClick: void 0,
                            selection: void 0,
                            dataPointSelection: void 0,
                            dataPointMouseEnter: void 0,
                            dataPointMouseLeave: void 0,
                            beforeZoom: void 0,
                            beforeResetZoom: void 0,
                            zoomed: void 0,
                            scrolled: void 0,
                            brushScrolled: void 0,
                            crossFilter: void 0,
                            filterChange: void 0,
                            annotationDragged: void 0,
                            annotationEdited: void 0,
                            annotationCreated: void 0,
                            annotationStyled: void 0,
                            annotationDeleted: void 0,
                            measured: void 0,
                            keyDown: void 0,
                            keyUp: void 0
                        },
                        foreColor: "#373d3f",
                        fontFamily: "Helvetica, Arial, sans-serif",
                        height: "auto",
                        parentHeightOffset: 15,
                        redrawOnParentResize: true,
                        redrawOnWindowResize: true,
                        id: void 0,
                        group: void 0,
                        nonce: void 0,
                        // Per-chart license key override for the gated premium features
                        // (storyboard, link/crossfilter, ink, measure, contextMenu,
                        // perspectives, history). Most specific wins: chart.license ->
                        // ApexCharts.setLicense() -> window.Apex.license -> unlicensed (trial
                        // watermark). Shared across the ApexCharts family. See setLicense().
                        license: void 0,
                        offsetX: 0,
                        offsetY: 0,
                        injectStyleSheet: true,
                        selection: {
                            enabled: false,
                            type: "x",
                            // selectedPoints: undefined, // default datapoints that should be selected automatically
                            fill: {
                                color: "#24292e",
                                opacity: 0.1
                            },
                            stroke: {
                                width: 1,
                                color: "#24292e",
                                opacity: 0.4,
                                dashArray: 3
                            },
                            xaxis: {
                                min: void 0,
                                max: void 0
                            },
                            yaxis: {
                                min: void 0,
                                max: void 0
                            }
                        },
                        sparkline: {
                            enabled: false
                        },
                        brush: {
                            enabled: false,
                            autoScaleYaxis: true,
                            target: void 0,
                            targets: void 0
                        },
                        // Linked Views (#4): crossfilter / linked highlighting. Requires the
                        // `link` feature. Two modes:
                        //   HIGHLIGHT (P1): `enabled` with no `dimension`. Charts sharing a
                        //   `chart.group` form a set; brushing a range (needs
                        //   `chart.selection.enabled`) dims out-of-range marks in place.
                        //   FILTER (P2): set `dimension` (its presence selects this path). Each
                        //   chart declares a dimension + reduction over a shared record set
                        //   registered with ApexCharts.crossfilter({ id, records }); clicking a
                        //   bucket re-aggregates the other charts. See docs/spec.
                        link: {
                            enabled: false,
                            mode: "highlight",
                            dimOpacity: 0.2,
                            // FILTER-mode config (all optional except dimension):
                            id: void 0,
                            // crossfilter coordinator id (defaults to chart.group)
                            dimension: void 0,
                            // (row) => key; presence selects filter mode
                            reduce: void 0,
                            // 'count' | { sum|avg|min|max: field } | (rows)=>n
                            type: void 0,
                            // 'category' | 'range' (else inferred from bins)
                            bins: void 0,
                            // range dims: { width } | { count } | { thresholds }
                            order: void 0,
                            // category order: 'first-seen' | 'asc' | 'desc' | fn
                            seriesName: void 0
                        },
                        // Ink Layer (#7): direct-manipulation annotations. When enabled, every
                        // point annotation is draggable (unless it sets draggable:false); or opt
                        // in per annotation with annotations.points[].draggable. Clicking an
                        // ink-managed annotation opens a floating editor card: rename inline,
                        // recolor, toggle bold, step the font size, size/reshape the marker, or
                        // delete the note. Axis-line annotations get separate Label and Line
                        // color rows, so restyling the label chip never touches the stroke.
                        // Requires the `ink` feature. Fires annotationDragged,
                        // annotationEdited, annotationStyled and annotationDeleted.
                        ink: {
                            enabled: false,
                            // Show a minimal "add note" tool palette; clicking it arms create
                            // mode (the next plot click drops an editable, draggable annotation).
                            palette: false,
                            // Snap a dragged point / axis-line annotation to the nearest gridline
                            // (numeric x + linear y). Undo/redo of ink edits is automatic when the
                            // history (Rewind) feature is enabled.
                            snap: false,
                            // Accent swatches offered by the floating note editor; defaults to a
                            // built-in 6-color palette when undefined.
                            noteColors: void 0
                        },
                        // Measure ruler (#18): a measure/delta ruler. Requires the `measure`
                        // feature. Hold `key` (default 'm') and drag A->B on the plot, or call
                        // chart.startMeasure()/chart.stopMeasure() to arm it. The live ruler
                        // reads dx, dy, %change and slope; on release it pins as a data-anchored
                        // overlay that re-projects on zoom/resize. Fires `measured`.
                        measure: {
                            enabled: false,
                            // 'span' (default): finance-style vertical band between two x-points
                            // with a "change (%) + range" readout, endpoints snapped to the first
                            // series. 'free': a diagonal ruler between two arbitrary points.
                            mode: "span",
                            key: "m",
                            pinOnRelease: true,
                            // Styling tokens. Every element also carries a stable CSS class
                            // (apexcharts-measure-band / -vline / -line / -label-bg / -label,
                            // group gets apexcharts-measure-up|down|flat). Colors are left
                            // undefined so they resolve config -> `--apx-measure-*` CSS custom
                            // property -> built-in default; set them here to force a color from JS.
                            colors: {
                                up: void 0,
                                down: void 0,
                                neutral: void 0,
                                guide: void 0
                            },
                            band: true,
                            // span mode: shaded band between the two x-positions
                            guides: true,
                            // span mode: vertical dashed reference lines
                            markers: true,
                            // endpoint dots on the series line
                            // Content: value formatters and a full readout override. `label`
                            // receives { from, to, dx, dy, percentChange, slope, mode } and
                            // returns a string or string[] (lines).
                            format: {
                                x: void 0,
                                y: void 0,
                                percent: void 0
                            },
                            label: void 0
                        },
                        // Radial Actions (#chrome): right-click / long-press context menu.
                        // Requires the `contextMenu` feature. Each action receives the clicked
                        // data coordinates, so verbs act at the point (not chart-wide like a
                        // toolbar button). `items` is an ordered list of built-in ids
                        // ('annotate' | 'xline' | 'yline' | 'measure') and/or custom
                        // { id, label, icon, onClick(ctx, { x, y, seriesIndex, dataPointIndex,
                        // clientX, clientY }) }. 'measure' is shown only when the measure tool
                        // is enabled. `labels` overrides built-in text; `noteText` is the label
                        // dropped by 'annotate'.
                        contextMenu: {
                            enabled: false,
                            items: [
                                "annotate",
                                "xline",
                                "yline",
                                "measure"
                            ],
                            labels: {
                                annotate: void 0,
                                xline: void 0,
                                yline: void 0,
                                measure: void 0
                            },
                            noteText: "Note",
                            // 'xline' ("Annotate here") drops a dashed vertical LINE at the
                            // clicked x; 'yline' ("Mark this level") a dashed horizontal line at
                            // the clicked y. Lines only, never a range rectangle. Like the note,
                            // a line is ink-managed when the ink feature is bundled: it opens
                            // the floating editor (rename; separate Label and Line color rows,
                            // so restyling the chip cannot blank the stroke; delete), drags
                            // along its axis, and undoes via Rewind. `line` styles both items.
                            line: {
                                text: "",
                                // label drawn on the line; empty for no label
                                strokeDashArray: 4,
                                color: void 0
                            }
                        },
                        stacked: false,
                        stackOnlyBar: true,
                        // mixed chart with stacked bars and line series - incorrect line draw #907
                        stackType: "normal",
                        // Real-time streaming mode. When enabled, appendData() bounds memory
                        // automatically: each series is trimmed to `maxPoints` (when set) or
                        // to the visible `xaxis.range` window plus a two-point off-screen
                        // runway (so exiting segments slide off the left edge instead of
                        // popping). The scroll animation itself needs no opt-in; updates
                        // that continue the previous window (appendData, or updateSeries with
                        // a shifted fixed-length window) always translate at constant
                        // velocity; see modules/animations/StreamScroll.
                        streaming: {
                            enabled: false,
                            maxPoints: void 0
                        },
                        toolbar: {
                            show: true,
                            offsetX: 0,
                            offsetY: 0,
                            tools: {
                                download: true,
                                selection: true,
                                zoom: true,
                                zoomin: true,
                                zoomout: true,
                                pan: true,
                                reset: true,
                                // Shown only when the measure ruler is active (chart.measure.enabled
                                // and the `measure` feature bundled). Toggles the measure tool; set
                                // false to keep the ruler key-driven only. See chart.measure.
                                measure: true,
                                customIcons: []
                            },
                            export: {
                                csv: {
                                    filename: void 0,
                                    columnDelimiter: ",",
                                    headerCategory: "category",
                                    headerValue: "value",
                                    categoryFormatter: void 0,
                                    valueFormatter: void 0
                                },
                                png: {
                                    filename: void 0
                                },
                                svg: {
                                    filename: void 0
                                },
                                scale: void 0,
                                width: void 0
                            },
                            autoSelected: "zoom"
                        },
                        type: "line",
                        width: "100%",
                        zoom: {
                            enabled: true,
                            type: "x",
                            autoScaleYaxis: false,
                            allowMouseWheelZoom: true,
                            // Momentum: two-finger pinch-zoom on touch devices. Zooms the x-axis
                            // around the pinch centroid (matching the x-only wheel/toolbar zoom),
                            // frame-by-frame rather than the 400ms wheel throttle.
                            pinch: true,
                            zoomedArea: {
                                fill: {
                                    color: "#90CAF9",
                                    opacity: 0.4
                                },
                                stroke: {
                                    color: "#0D47A1",
                                    opacity: 0.4,
                                    width: 1
                                }
                            }
                        },
                        // Momentum: kinetic panning on touch. When a one-finger pan is released
                        // with velocity, the chart keeps gliding and decelerates by `friction`
                        // each frame, clamping (no elastic overshoot) at the data edges.
                        pan: {
                            inertia: true,
                            friction: 0.92
                        },
                        accessibility: {
                            enabled: true,
                            description: void 0,
                            announcements: {
                                enabled: true
                            },
                            keyboard: {
                                enabled: true,
                                navigation: {
                                    enabled: true,
                                    wrapAround: false
                                }
                            }
                        },
                        dataReducer: {
                            enabled: false,
                            algorithm: "lttb",
                            targetPoints: 250,
                            threshold: 500
                        }
                    },
                    parsing: {
                        x: void 0,
                        y: void 0
                    },
                    plotOptions: {
                        line: {
                            isSlopeChart: false,
                            colors: {
                                threshold: 0,
                                colorAboveThreshold: void 0,
                                colorBelowThreshold: void 0
                            }
                        },
                        area: {
                            fillTo: "origin"
                        },
                        bar: {
                            horizontal: false,
                            columnWidth: "70%",
                            // should be in percent 0 - 100
                            barHeight: "70%",
                            // should be in percent 0 - 100
                            distributed: false,
                            borderRadius: 0,
                            borderRadiusApplication: "around",
                            // [around, end]
                            borderRadiusWhenStacked: "last",
                            // [all, last]
                            rangeBarOverlap: true,
                            rangeBarGroupRows: false,
                            hideZeroBarsWhenGrouped: false,
                            isDumbbell: false,
                            dumbbellColors: void 0,
                            isFunnel: false,
                            isFunnel3d: true,
                            colors: {
                                ranges: [],
                                backgroundBarColors: [],
                                backgroundBarOpacity: 1,
                                backgroundBarRadius: 0
                            },
                            dataLabels: {
                                position: "top",
                                // top, center, bottom
                                maxItems: 100,
                                hideOverflowingLabels: true,
                                orientation: "horizontal",
                                total: {
                                    enabled: false,
                                    formatter: void 0,
                                    offsetX: 0,
                                    offsetY: 0,
                                    style: {
                                        color: "#373d3f",
                                        fontSize: "12px",
                                        fontFamily: void 0,
                                        fontWeight: 600
                                    }
                                }
                            }
                        },
                        bubble: {
                            zScaling: true,
                            minBubbleRadius: void 0,
                            maxBubbleRadius: void 0
                        },
                        scatter: {
                            // Spread overlapping points apart ("jitter"). Two uses, one engine:
                            //  - Strip plot: supply data as { x: 'Category', y: [v1, v2, ...] }.
                            //    Each category becomes a band and the values are scattered
                            //    horizontally within it. Marker styling comes from the standard
                            //    `markers` / `colors` config.
                            //  - Overplotting: ordinary { x, y } points get a small random offset
                            //    so dense clusters fan out. The underlying data (and tooltip
                            //    values) stay exact — only the drawn position moves.
                            // Offsets are in axis units (x: 1 = one category step) and are
                            // deterministic (stable across re-renders, SSR-safe).
                            jitter: {
                                enabled: false,
                                x: 0,
                                // max ± horizontal offset, in x-axis units
                                y: 0,
                                // max ± vertical offset, in y-axis units
                                distributed: false,
                                // single series: colour each band differently
                                maxPoints: 5e3
                            }
                        },
                        candlestick: {
                            colors: {
                                upward: "#00B746",
                                downward: "#EF403C"
                            },
                            wick: {
                                useFillColor: true
                            }
                        },
                        boxPlot: {
                            colors: {
                                upper: "#00E396",
                                lower: "#008FFB"
                            },
                            // Optional individual observations ("jitter") overlaid on each box.
                            // Inert unless a data point supplies a `points: number[]` array; off
                            // by default so existing boxPlot charts are unchanged.
                            points: {
                                show: false,
                                shape: "circle",
                                // 'circle' | 'square'
                                size: 2.5,
                                // radius (px)
                                jitter: 0.5,
                                // 0..1 fraction of the box half-width to scatter within
                                maxPoints: 3e3,
                                // cap per box; excess is stride-thinned
                                opacity: 0.9,
                                // 'series-dark' (default) → a darker shade of the series colour,
                                // 'series' → the series colour, or any literal colour string.
                                fillColor: "series-dark",
                                strokeColor: "#fff",
                                strokeWidth: 1
                            }
                        },
                        violin: {
                            // Multiply the density-derived half-width. 1 = density's own maxWeight
                            // maps to half the category slot.
                            bandwidthScale: 1,
                            // 'individual' → every violin uses the full slot width (scaled to its
                            // own peak). 'group' → all violins share one scale (the densest in the
                            // series), so widths stay proportional to density across categories.
                            normalize: "individual",
                            // Individual observations ("jitter") overlaid on the violin shape.
                            points: {
                                show: true,
                                shape: "circle",
                                // 'circle' | 'square'
                                size: 2.5,
                                // radius (px)
                                jitter: 0.5,
                                // 0..1 fraction of the half-width to scatter within
                                constrainToViolin: true,
                                // clamp jitter to the density width at each value
                                maxPoints: 3e3,
                                // cap per violin; excess is stride-thinned
                                opacity: 0.9,
                                // Default: a darker shade of each violin's own colour, with a white
                                // outline. fillColor accepts 'series-dark' (default), 'series' (the
                                // violin's colour as-is), or any literal colour string.
                                fillColor: "series-dark",
                                strokeColor: "#fff",
                                strokeWidth: 1
                            }
                        },
                        heatmap: {
                            radius: 2,
                            enableShades: true,
                            shadeIntensity: 0.5,
                            reverseNegativeShade: false,
                            distributed: false,
                            useFillColorAsStroke: false,
                            colorScale: {
                                inverse: false,
                                ranges: [],
                                min: void 0,
                                max: void 0,
                                // Replaces the default categorical legend with a continuous
                                // gradient stripe + a hover indicator arrow. Honors
                                // `chart.legend.position` (top / right / bottom / left).
                                gradientLegend: {
                                    enabled: false,
                                    // Strip length along the legend's long axis. Accepts a number
                                    // (pixels) or a percentage string. For top/bottom placement the
                                    // percentage is resolved against the chart's SVG width; for
                                    // left/right placement, against the SVG height.
                                    width: "70%",
                                    height: "70%",
                                    thickness: 12,
                                    // Alignment of the strip within the legend area:
                                    //  - top/bottom: 'start' = left, 'center', 'end' = right
                                    //  - left/right: 'start' = top,  'center', 'end' = bottom
                                    align: "center",
                                    // Number of gradient stops sampled from the shade function when
                                    // no explicit `ranges` are provided.
                                    stops: 16,
                                    // Show min/max labels at the ends of the strip.
                                    showLabels: true,
                                    // Show a value tooltip next to the arrow when hovering a cell.
                                    showHoverValue: true,
                                    labelStyle: {
                                        fontSize: "11px",
                                        fontFamily: void 0,
                                        colors: void 0
                                    },
                                    arrow: {
                                        size: 8,
                                        color: void 0
                                    },
                                    formatter: void 0
                                }
                            }
                        },
                        funnel: {
                            // 'rectangle' preserves the existing centered-rectangle funnel
                            // geometry. 'trapezoid' produces continuous sloped sides between
                            // consecutive stages (each stage's bottom width matches the next
                            // stage's top width).
                            shape: "rectangle",
                            // For shape: 'trapezoid' only — what to do with the last stage's
                            // bottom edge: 'flat' (parallel sides) or 'taper' (taper to a point).
                            lastShape: "flat"
                        },
                        treemap: {
                            enableShades: true,
                            shadeIntensity: 0.5,
                            distributed: false,
                            reverseNegativeShade: false,
                            useFillColorAsStroke: false,
                            borderRadius: 4,
                            dataLabels: {
                                format: "scale"
                            },
                            colorScale: {
                                inverse: false,
                                ranges: [],
                                min: void 0,
                                max: void 0
                            },
                            seriesTitle: {
                                show: true,
                                offsetY: 1,
                                offsetX: 1,
                                borderColor: "#000",
                                borderWidth: 1,
                                borderRadius: 2,
                                style: {
                                    background: "rgba(0, 0, 0, 0.6)",
                                    color: "#fff",
                                    fontSize: "12px",
                                    fontFamily: void 0,
                                    fontWeight: 400,
                                    cssClass: "",
                                    padding: {
                                        left: 6,
                                        right: 6,
                                        top: 2,
                                        bottom: 2
                                    }
                                }
                            }
                        },
                        unit: {
                            // 'grouped' (each category is its own cluster, laid out in a row) |
                            // 'packed' (one blob; categories coloured + sorted, minority centred) |
                            // 'columns' (each category is a vertical bar built from stacked dots) |
                            // 'grid' (one lattice of cells filled in category order - a waffle /
                            // part-to-whole square "pie"; `chart.type:'waffle'` presets this).
                            layout: "grouped",
                            // Update transition, controlling which previous dot each new dot
                            // tweens from. 'group' (default): keyed per category, so dots stay in
                            // their group and category-level enters/exits fade. 'flow': keyed by
                            // global order, so the anonymous crowd migrates across a regroup (the
                            // circles-to-bars effect). 'identity': keyed by each datum's id/name,
                            // so a SPECIFIC unit migrates across any regroup/relayout keeping its
                            // colour and size (needs the object form with unique ids/names).
                            transition: "group",
                            // 'circle' | 'square' | 'image' (isotype pictogram).
                            shape: "circle",
                            // Icon used when shape:'image'. Each unit renders this icon at the
                            // given size. Set `tint:true` to recolour a monochrome icon to the
                            // category colour (or a per-unit fillColor) so the pictogram matches
                            // the legend; leave it off for multi-colour icons that should keep
                            // their own colours.
                            image: {
                                src: void 0,
                                width: 20,
                                height: 20,
                                tint: false
                            },
                            // dot radius in px, or 'auto' to size dots so the largest cluster
                            // fits its allotted box.
                            size: "auto",
                            // The 'columns' layout can size its dots independently of `size`
                            // (which the circle layouts / storyboard beats often pin to a
                            // constant so dots do not resize while migrating). 'inherit' uses
                            // `size`; 'auto' sizes the dots to fill the plot height; a number
                            // pins a columns-only size. Circle / square only (image icons keep
                            // their intrinsic size).
                            columns: {
                                size: "inherit"
                            },
                            // The 'grid' (waffle) layout: one lattice of cells filled in category
                            // order. `columns` = cells per row. `total` (optional) fixes the cell
                            // budget - e.g. 100 for a percentage waffle - and largest-remainder
                            // allocates the cells to categories; leave it undefined for one cell
                            // per unit (respects unitValue / maxUnits). `fillFrom` picks the first
                            // row: 'bottom' (default) or 'top'.
                            //
                            // `split:true` switches to SMALL MULTIPLES: one mini-waffle per
                            // category, arranged in a near-square trellis (or `tileColumns` per
                            // row). Each tile then has `total` cells (default 100) and fills a
                            // fraction equal to the category's value over `max` (default = the
                            // largest count, so the leader fills its tile; set `max:100` with
                            // percentage data for true "of 100" tiles). The unfilled cells show as
                            // a faint `trackColor` backdrop, and each tile gets its own label.
                            grid: {
                                columns: 10,
                                total: void 0,
                                fillFrom: "bottom",
                                // small-multiple (one waffle per category) mode + its knobs:
                                split: false,
                                // tiles per row; undefined = auto (near-square).
                                tileColumns: void 0,
                                // value -> filled-cell denominator; undefined = the largest count.
                                max: void 0,
                                // colour of the empty "track" cells; undefined = a neutral grey.
                                trackColor: void 0
                            },
                            // The 'scatter' layout places each unit on real value axes (needs the
                            // object-form data). Two modes via `y`:
                            //  - `y:'lanes'` (default): a BEESWARM. X is the per-unit value axis,
                            //    Y is a category lane. `spread:'swarm'` packs dots off the centre
                            //    line so equal values do not overlap; 'jitter' scatters randomly.
                            //  - `y:'value'`: a 2D value-value scatter. X = each datum's `x`, Y =
                            //    each datum's `y`, on two numeric axes; category = colour.
                            // `sizeRange:[min,max]` turns dots into BUBBLES scaled (by area) from
                            // each datum's `sizeField` (default 'z') - a bubble scatter / bubble
                            // beeswarm. `tickAmount`/`xMin`/`xMax`/`xTitle`/`xFormatter` control the
                            // X axis; the `y*` twins the Y axis (2D only); `laneLabelWidth` the
                            // lane-label gutter (lanes mode); `gridlines` the grid.
                            scatter: {
                                y: "lanes",
                                spread: "swarm",
                                // Beeswarm orientation (1D lanes mode only): 'horizontal' lays the
                                // value on the X axis with category lanes stacked on Y (default);
                                // 'vertical' lays the value on the Y axis with category lanes as
                                // columns across X. Ignored for the 2D value-value scatter (y:'value').
                                orientation: "horizontal",
                                tickAmount: 5,
                                xMin: void 0,
                                xMax: void 0,
                                xTitle: void 0,
                                // (value) => string
                                xFormatter: void 0,
                                yTickAmount: 5,
                                yMin: void 0,
                                yMax: void 0,
                                yTitle: void 0,
                                // (value) => string
                                yFormatter: void 0,
                                // bubble sizing: datum key for the size value + [minR, maxR] in px.
                                sizeField: "z",
                                sizeRange: void 0,
                                laneLabelWidth: void 0,
                                gridlines: true
                            },
                            // Opt-in bubble sizing: scale each dot's radius by its per-unit value
                            // (needs the object-form data). Circle shape only; the lattice is
                            // spaced for the largest bubble so dots never overlap.
                            sizeByValue: {
                                enabled: false,
                                // radius (px) for the largest value, or 'auto' to fit the largest
                                // bubble to the plot like uniform auto-sizing.
                                maxRadius: "auto",
                                // radius (px) for the smallest value; defaults to ~35% of maxRadius.
                                minRadius: void 0,
                                // 'area' (bubble AREA proportional to value) | 'linear'.
                                scale: "area"
                            },
                            // packing gap factor between spiral shells (1 = dots touch).
                            spacing: 1.05,
                            // corner radius for shape:'square'.
                            borderRadius: 0,
                            // The gather tween that moves marks between layouts on an update, and
                            // where entering marks come from. `easing`: 'outCubic' (default:
                            // decelerate to a stop) | 'inOutCubic' (accelerate gently out of rest,
                            // weighted travel) | 'outBack' (overshoot each mark past its slot and
                            // spring back - a per-mark settle), with `overshoot` tuning the spring
                            // strength. `enter`: where a fresh / appearing mark animates FROM -
                            // 'burst' (default: fly out from the cluster centre) | 'fade'
                            // (materialise in place) | 'rise' (fade in while drifting gently up
                            // into the slot). Colour, radius and opacity always stay on the
                            // out-cubic (a back ease overshoots past 1, which would push RGB
                            // channels / radii out of range).
                            gather: {
                                easing: "outCubic",
                                overshoot: 1.70158,
                                enter: "burst"
                            },
                            // The 'arc' layout arranges marks as a PARLIAMENT / hemicycle: seats in
                            // concentric arced rows across an annulus, filled in category order so
                            // each category is a contiguous wedge (the classic seating chart). Angles
                            // use the radialBar convention (0 = top, clockwise); the default sweep is
                            // a top semicircle (a full circle = startAngle 0, endAngle 360).
                            // `innerRadiusRatio` is the donut hole (inner radius / outer); `rows`
                            // fixes the number of seat rows, or 'auto' to size the dots as large as
                            // fit. Like 'packed' the legend carries the category names (no per-wedge
                            // labels).
                            arc: {
                                startAngle: -90,
                                endAngle: 90,
                                innerRadiusRatio: 0.4,
                                rows: "auto"
                            },
                            // 1 dot represents this many units of value (waffle scaling).
                            unitValue: 1,
                            // safety cap on total dots; counts scale down proportionally above it.
                            maxUnits: 5e3,
                            // packed layout: order categories smallest-first so the minority
                            // group nests in the centre of the blob.
                            sortByGroup: true,
                            clusterLabels: {
                                show: true,
                                // Label placement relative to the cluster/bar: 'top' (default) or
                                // 'bottom'. A 'bottom' label is always straight; the curved arc
                                // (below) rides the top crown only.
                                position: "top",
                                curved: true,
                                fontSize: "13px",
                                fontFamily: void 0,
                                fontWeight: 600,
                                // defaults to the cluster's own colour when undefined.
                                color: void 0,
                                offsetY: 0,
                                // (name, { seriesIndex, value, percent, w }) => string
                                formatter: void 0
                            },
                            tooltip: {
                                // Per-unit tooltip body. Each dot carries its category (seriesIndex)
                                // and its index within that category (dataPointIndex), so the
                                // formatter can look up per-unit data and return a string/HTML.
                                // ({ seriesName, seriesIndex, dataPointIndex, count, unitValue,
                                //    color, w }) => string
                                // Default: "#<dataPointIndex+1> of <count>".
                                formatter: void 0
                            }
                        },
                        radialBar: {
                            inverseOrder: false,
                            startAngle: 0,
                            endAngle: 360,
                            offsetX: 0,
                            offsetY: 0,
                            // Gauge sub-shape. 'arc' (default) renders the existing filled
                            // value-arc gauge; 'needle' replaces the value-arc with a rotating
                            // pointer/needle. Bands and ticks are independent and work for both
                            // shapes.
                            shape: "arc",
                            // Value-to-angle mapping. Defaults to the existing 0..100 range
                            // used by radialBar. Override for gauges that need a custom domain
                            // (e.g. min: 0, max: 240 for a speedometer).
                            min: 0,
                            max: 100,
                            // Threshold bands rendered as colored arc segments along the gauge
                            // arc (e.g. [{from:0,to:30,color:'#FF4560'}, ...]). Bands draw
                            // behind the value-arc and tick marks. Set to [] (default) to
                            // disable.
                            bands: [],
                            bandsStyle: {
                                // % of arc radius. Slightly less than the value-arc stroke so
                                // the value-arc reads on top by default.
                                strokeWidth: "40%",
                                // px gap between consecutive bands.
                                gap: 0,
                                // Hide the track when bands cover the full range; the bands
                                // themselves act as the visual backdrop.
                                hideTrackWhenPresent: true
                            },
                            // Tick marks rendered along (outside) the gauge arc.
                            ticks: {
                                show: false,
                                major: {
                                    count: 11,
                                    length: 10,
                                    width: 2,
                                    color: "#666",
                                    // 'inside' draws ticks from `radius - length` to `radius`;
                                    // 'outside' draws from `radius` to `radius + length`.
                                    placement: "outside"
                                },
                                minor: {
                                    count: 4,
                                    // minor ticks BETWEEN each pair of major ticks
                                    length: 5,
                                    width: 1,
                                    color: "#999",
                                    placement: "outside"
                                },
                                labels: {
                                    show: false,
                                    offset: 6,
                                    fontSize: "11px",
                                    fontFamily: void 0,
                                    fontWeight: 400,
                                    color: "#666",
                                    /** @param {number} v */ formatter: function formatter(v) {
                                        return String(v);
                                    }
                                }
                            },
                            // Needle/dial configuration. Only applies when `shape: 'needle'`.
                            needle: {
                                color: "#333",
                                // Needle length as a % of the gauge radius (string like '85%')
                                // or as an absolute px number.
                                length: "85%",
                                // px width of the needle line at the base.
                                baseWidth: 4,
                                // px width of the needle tip (tapered if smaller than baseWidth).
                                tipWidth: 1,
                                // px offset from the geometric arc center on Y. Positive values
                                // push the needle base down (toward the chord midpoint of a
                                // ∩-shape gauge); negative pushes up. The needle rotates around
                                // this shifted point.
                                offsetY: 0,
                                // When true, also render the filled value-arc alongside the
                                // needle. Default false preserves the previous needle-only
                                // behavior. Useful for gauges that want a progress ring plus a
                                // pointer indicator.
                                showValueArc: false,
                                animation: {
                                    enabled: true,
                                    duration: 800,
                                    easing: "ease-out"
                                }
                            },
                            hollow: {
                                margin: 5,
                                size: "50%",
                                background: "transparent",
                                image: void 0,
                                imageWidth: 150,
                                imageHeight: 150,
                                imageOffsetX: 0,
                                imageOffsetY: 0,
                                imageClipped: true,
                                position: "front",
                                // Optional stroke around the hollow ring. Combined with
                                // `strokeDasharray` this produces a dashed indicator circle
                                // around the value text — useful for gauge designs where the
                                // value sits inside its own boundary.
                                stroke: void 0,
                                strokeWidth: 1,
                                strokeDasharray: void 0,
                                dropShadow: {
                                    enabled: false,
                                    top: 0,
                                    left: 0,
                                    blur: 3,
                                    color: "#000",
                                    opacity: 0.5
                                }
                            },
                            track: {
                                show: true,
                                startAngle: void 0,
                                endAngle: void 0,
                                background: "#f2f2f2",
                                strokeWidth: "97%",
                                opacity: 1,
                                margin: 5,
                                // margin is in pixels
                                dropShadow: {
                                    enabled: false,
                                    top: 0,
                                    left: 0,
                                    blur: 3,
                                    color: "#000",
                                    opacity: 0.5
                                }
                            },
                            dataLabels: {
                                show: true,
                                name: {
                                    show: true,
                                    fontSize: "16px",
                                    fontFamily: void 0,
                                    fontWeight: 600,
                                    color: void 0,
                                    offsetY: 0,
                                    /**
               * @param {any} val
               */ formatter: function formatter(val) {
                                        return val;
                                    }
                                },
                                value: {
                                    show: true,
                                    fontSize: "14px",
                                    fontFamily: void 0,
                                    fontWeight: 400,
                                    color: void 0,
                                    offsetY: 16,
                                    /**
               * @param {any} val
               */ formatter: function formatter(val) {
                                        return val + "%";
                                    }
                                },
                                total: {
                                    show: false,
                                    label: "Total",
                                    fontSize: "16px",
                                    fontWeight: 600,
                                    fontFamily: void 0,
                                    color: void 0,
                                    /**
               * @param {import('../../types/internal').ChartStateW} w
               */ formatter: function formatter(w) {
                                        return(/**
                   * @param {number} a
                   * @param {number} b
                   */ w.globals.seriesTotals.reduce(function(a, b) {
                                            return a + b;
                                        }, 0) / w.seriesData.series.length + "%");
                                    }
                                }
                            },
                            barLabels: {
                                enabled: false,
                                offsetX: 0,
                                offsetY: 0,
                                useSeriesColors: true,
                                fontFamily: void 0,
                                fontWeight: 600,
                                fontSize: "16px",
                                /**
             * @param {any} val
             */ formatter: function formatter(val) {
                                    return val;
                                },
                                onClick: void 0
                            }
                        },
                        pie: {
                            customScale: 1,
                            offsetX: 0,
                            offsetY: 0,
                            startAngle: 0,
                            endAngle: 360,
                            expandOnClick: true,
                            // Rounds the corners of each slice (in px). Applies to pie, donut and
                            // polarArea (all rendered by the Pie module). 0 = sharp corners
                            // (default, unchanged behavior). The value is clamped per slice so
                            // opposing corner fillets never cross on thin/narrow slices.
                            borderRadius: 0,
                            // Gap between adjacent slices (in px). Applies to pie, donut and
                            // polarArea. 0 = slices touch (default). Each slice is inset
                            // symmetrically, so its mid-angle (data label + hit region) is kept.
                            spacing: 0,
                            dataLabels: {
                                // These are the percentage values which are displayed on slice
                                offset: 0,
                                // offset by which labels will move outside
                                minAngleToShowLabel: 10,
                                // External (outer) labels: render the category/series name outside
                                // the slice, joined to it by a leader (connector) line, so users
                                // don't have to map legend colors back to slices. The percentage
                                // keeps rendering inside the slice (governed by dataLabels.enabled).
                                // Pie + donut only; ignored for polarArea (radial length already
                                // encodes the value there).
                                external: {
                                    show: false,
                                    // master switch for the external (outer) labels
                                    offsetX: 0,
                                    offsetY: 0,
                                    fontSize: void 0,
                                    // falls back to dataLabels.style.fontSize
                                    fontFamily: void 0,
                                    // falls back to dataLabels.style.fontFamily
                                    fontWeight: void 0,
                                    // falls back to dataLabels.style.fontWeight
                                    color: void 0,
                                    // defaults to chart.foreColor (readable text)
                                    /**
               * Return a string (single line) or an array of strings (stacked
               * lines, e.g. [name, percent + '%']).
               * @param {string} name
               * @param {{ seriesIndex: number, percent: number, value: number, w: any }} opts
               * @returns {string | string[]}
               */ formatter: void 0,
                                    connector: {
                                        show: true,
                                        width: 1,
                                        color: void 0,
                                        // defaults to the slice color
                                        length: 16,
                                        // horizontal run after the radial elbow (px)
                                        gap: 6
                                    }
                                }
                            },
                            donut: {
                                size: "65%",
                                background: "transparent",
                                labels: {
                                    // These are the inner labels appearing inside donut
                                    show: false,
                                    name: {
                                        show: true,
                                        fontSize: "16px",
                                        fontFamily: void 0,
                                        fontWeight: 600,
                                        color: void 0,
                                        offsetY: -10,
                                        /**
                 * @param {any} val
                 */ formatter: function formatter(val) {
                                            return val;
                                        }
                                    },
                                    value: {
                                        show: true,
                                        fontSize: "20px",
                                        fontFamily: void 0,
                                        fontWeight: 400,
                                        color: void 0,
                                        offsetY: 10,
                                        /**
                 * @param {any} val
                 */ formatter: function formatter(val) {
                                            return val;
                                        }
                                    },
                                    total: {
                                        show: false,
                                        showAlways: false,
                                        label: "Total",
                                        fontSize: "16px",
                                        fontWeight: 400,
                                        fontFamily: void 0,
                                        color: void 0,
                                        /**
                 * @param {import('../../types/internal').ChartStateW} w
                 */ formatter: function formatter(w) {
                                            return w.globals.seriesTotals.reduce(function(a, b) {
                                                return a + b;
                                            }, 0);
                                        }
                                    }
                                }
                            }
                        },
                        polarArea: {
                            rings: {
                                strokeWidth: 1,
                                strokeColor: "#e8e8e8"
                            },
                            spokes: {
                                strokeWidth: 1,
                                connectorColors: "#e8e8e8"
                            }
                        },
                        sunburst: {
                            // Sunburst / nested pie-donut (hierarchical radial). Rings go from the
                            // centre hole outward, one per hierarchy level; each child arc is
                            // nested inside its parent's angular wedge.
                            offsetX: 0,
                            offsetY: 0,
                            startAngle: 0,
                            endAngle: 360,
                            // Centre hole radius, as a % of the max radius (like donut size).
                            innerSize: "15%",
                            // Corner rounding + inter-arc gap (px), same semantics as the pie
                            // family (see plotOptions.pie.borderRadius / .spacing).
                            borderRadius: 0,
                            spacing: 1,
                            // How a branch that bottoms out before the deepest level is drawn:
                            // 'extend' stretches the leaf arc out to the rim (default, matches
                            // d3); 'stop' leaves the outer rings empty behind it.
                            leaf: "extend",
                            // Angular partition of a parent's wedge among its children:
                            // 'normalize' splits by each child's share of its siblings (safe for
                            // any data); 'strict' expects children to sum to the parent's value.
                            partition: "normalize",
                            // Each depth level is tinted this much lighter than its parent colour
                            // (0 = keep parent colour, 1 = white). A per-node `color` overrides.
                            tint: 0.14,
                            // Click a wedge to zoom into that branch (its subtree fills the
                            // chart; a breadcrumb walks back). Click the focused inner ring to
                            // zoom out. Set false to disable.
                            zoomOnClick: true,
                            dataLabels: {
                                show: true,
                                // Hide the label on any arc narrower than this (degrees), so tiny
                                // wedges do not overflow with text.
                                minAngleToShow: 8,
                                style: {
                                    fontSize: "12px",
                                    fontFamily: void 0,
                                    fontWeight: 400,
                                    colors: void 0
                                }
                            }
                        },
                        radar: {
                            size: void 0,
                            offsetX: 0,
                            offsetY: 0,
                            polygons: {
                                // strokeColor: '#e8e8e8', // should be deprecated in the minor version i.e 3.2
                                strokeWidth: 1,
                                strokeColors: "#e8e8e8",
                                connectorColors: "#e8e8e8",
                                fill: {
                                    colors: void 0
                                }
                            }
                        }
                    },
                    colors: void 0,
                    dataLabels: {
                        enabled: true,
                        enabledOnSeries: void 0,
                        /**
         * @param {any} val
         */ formatter: function formatter(val) {
                            return val !== null ? val : "";
                        },
                        textAnchor: "middle",
                        distributed: false,
                        offsetX: 0,
                        offsetY: 0,
                        style: {
                            fontSize: "12px",
                            fontFamily: void 0,
                            fontWeight: 600,
                            colors: void 0
                        },
                        background: {
                            enabled: true,
                            foreColor: "#fff",
                            backgroundColor: void 0,
                            borderRadius: 2,
                            padding: 4,
                            opacity: 0.9,
                            borderWidth: 1,
                            borderColor: "#fff",
                            dropShadow: {
                                enabled: false,
                                top: 1,
                                left: 1,
                                blur: 1,
                                color: "#000",
                                opacity: 0.8
                            }
                        },
                        dropShadow: {
                            enabled: false,
                            top: 1,
                            left: 1,
                            blur: 1,
                            color: "#000",
                            opacity: 0.8
                        },
                        // Ride data labels to their new position on a data-change update
                        // (e.g. a bar chart race), instead of snapping. Off by default so
                        // existing charts are unchanged. Speed/easing follow
                        // chart.animations.dynamicAnimation. Bar/column only.
                        animate: {
                            enabled: false
                        },
                        // Count the numeric value up/down from its previous value on update,
                        // like countUp.js. Off by default. The dataLabels.formatter runs each
                        // frame, so number formatting (decimals, separators, prefixes) is
                        // preserved. Bar/column only.
                        countUp: {
                            enabled: false
                        }
                    },
                    fill: {
                        type: "solid",
                        colors: void 0,
                        // array of colors
                        opacity: 0.85,
                        gradient: {
                            shade: "dark",
                            type: "horizontal",
                            shadeIntensity: 0.5,
                            gradientToColors: void 0,
                            inverseColors: true,
                            opacityFrom: 1,
                            opacityTo: 1,
                            stops: [
                                0,
                                50,
                                100
                            ],
                            colorStops: []
                        },
                        image: {
                            src: [],
                            width: void 0,
                            // optional
                            height: void 0
                        },
                        pattern: {
                            style: "squares",
                            // String | Array of Strings
                            width: 6,
                            height: 6,
                            strokeWidth: 2
                        }
                    },
                    forecastDataPoints: {
                        count: 0,
                        fillOpacity: 0.5,
                        strokeWidth: void 0,
                        dashArray: 4
                    },
                    grid: {
                        show: true,
                        borderColor: "#e0e0e0",
                        strokeDashArray: 0,
                        position: "back",
                        xaxis: {
                            lines: {
                                show: false
                            }
                        },
                        yaxis: {
                            lines: {
                                show: true
                            }
                        },
                        row: {
                            colors: void 0,
                            // takes as array which will be repeated on rows
                            opacity: 0.5
                        },
                        column: {
                            colors: void 0,
                            // takes an array which will be repeated on columns
                            opacity: 0.5
                        },
                        padding: {
                            top: 0,
                            right: 10,
                            bottom: 0,
                            left: 12
                        }
                    },
                    labels: [],
                    drilldown: {
                        // Opt-in. When false, the Drilldown feature module stays inert even if
                        // it was imported. Requires `import 'apexcharts/features/drilldown'`.
                        enabled: false,
                        // Child levels referenced by a data point's `drilldown: '<id>'` field.
                        // Each: { id, name?, data, chart?, xaxis?, yaxis?, colors?, plotOptions? }
                        series: [],
                        breadcrumb: {
                            show: true,
                            position: "top-left",
                            // 'top-left' | 'top-right'
                            separator: " / ",
                            rootLabel: "All",
                            offsetX: 0,
                            offsetY: 0
                        },
                        // Animation is delegated to the chart's update pipeline; `enabled`
                        // gates whether the drill transition animates at all.
                        animation: {
                            enabled: true,
                            // Anchor the drill transition at the clicked point: the child unfolds
                            // outward from it (and settles back on drill-up) instead of the chart
                            // simply re-rendering. A gentle scale layered on the SVG; opt-in.
                            zoomFromPoint: false,
                            // Base duration (ms) of the transition, used only when zoomFromPoint
                            // is true. The fade-out phase runs a little shorter than this.
                            speed: 260
                        }
                    },
                    legend: {
                        show: true,
                        showForSingleSeries: false,
                        showForNullSeries: true,
                        showForZeroSeries: true,
                        floating: false,
                        position: "bottom",
                        // whether to position legends in 1 of 4
                        // direction - top, bottom, left, right
                        horizontalAlign: "center",
                        // when position top/bottom, you can specify whether to align legends left, right or center
                        inverseOrder: false,
                        fontSize: "12px",
                        fontFamily: void 0,
                        fontWeight: 400,
                        width: void 0,
                        height: void 0,
                        formatter: void 0,
                        tooltipHoverFormatter: void 0,
                        offsetX: -20,
                        offsetY: 4,
                        customLegendItems: [],
                        clusterGroupedSeries: true,
                        clusterGroupedSeriesOrientation: "vertical",
                        labels: {
                            colors: void 0,
                            useSeriesColors: false
                        },
                        markers: {
                            size: 7,
                            fillColors: void 0,
                            strokeWidth: 1,
                            shape: void 0,
                            offsetX: 0,
                            offsetY: 0,
                            customHTML: void 0,
                            onClick: void 0
                        },
                        itemMargin: {
                            horizontal: 5,
                            vertical: 4
                        },
                        onItemClick: {
                            toggleDataSeries: true
                        },
                        onItemHover: {
                            highlightDataSeries: true
                        }
                    },
                    markers: {
                        discrete: [],
                        size: 0,
                        colors: void 0,
                        strokeColors: "#fff",
                        strokeWidth: 2,
                        strokeOpacity: 0.9,
                        strokeDashArray: 0,
                        fillOpacity: 1,
                        shape: "circle",
                        offsetX: 0,
                        offsetY: 0,
                        showNullDataPoints: true,
                        onClick: void 0,
                        onDblClick: void 0,
                        hover: {
                            size: void 0,
                            sizeOffset: 3
                        }
                    },
                    noData: {
                        text: void 0,
                        align: "center",
                        offsetX: 0,
                        offsetY: 0,
                        style: {
                            color: void 0,
                            fontSize: "14px",
                            fontFamily: void 0
                        }
                    },
                    responsive: [],
                    // breakpoints should follow ascending order 400, then 700, then 1000
                    series: void 0,
                    states: {
                        hover: {
                            filter: {
                                type: "lighten",
                                value: 0.15
                            }
                        },
                        active: {
                            allowMultipleDataPointsSelection: false,
                            filter: {
                                type: "darken",
                                value: 0.35
                            }
                        }
                    },
                    title: {
                        text: void 0,
                        align: "left",
                        margin: 5,
                        offsetX: 0,
                        offsetY: 0,
                        floating: false,
                        style: {
                            fontSize: "14px",
                            fontWeight: 900,
                            fontFamily: void 0,
                            color: void 0
                        }
                    },
                    subtitle: {
                        text: void 0,
                        align: "left",
                        margin: 5,
                        offsetX: 0,
                        offsetY: 30,
                        floating: false,
                        style: {
                            fontSize: "12px",
                            fontWeight: 400,
                            fontFamily: void 0,
                            color: void 0
                        }
                    },
                    stroke: {
                        show: true,
                        curve: "smooth",
                        // "smooth" / "straight" / "monotoneCubic" / "stepline" / "linestep"
                        lineCap: "butt",
                        // round, butt , square
                        width: 2,
                        colors: void 0,
                        // array of colors
                        dashArray: 0,
                        // single value or array of values
                        fill: {
                            type: "solid",
                            colors: void 0,
                            // array of colors
                            opacity: 0.85,
                            gradient: {
                                shade: "dark",
                                type: "horizontal",
                                shadeIntensity: 0.5,
                                gradientToColors: void 0,
                                inverseColors: true,
                                opacityFrom: 1,
                                opacityTo: 1,
                                stops: [
                                    0,
                                    50,
                                    100
                                ],
                                colorStops: []
                            }
                        }
                    },
                    tooltip: {
                        enabled: true,
                        enabledOnSeries: void 0,
                        shared: true,
                        hideEmptySeries: false,
                        followCursor: false,
                        // when disabled, the tooltip will show on top of the series instead of mouse position
                        intersect: false,
                        // when enabled, tooltip will only show when user directly hovers over point
                        inverseOrder: false,
                        arrow: true,
                        custom: void 0,
                        fillSeriesColor: false,
                        theme: "light",
                        cssClass: "",
                        style: {
                            fontSize: "12px",
                            fontFamily: void 0,
                            background: void 0
                        },
                        onDatasetHover: {
                            highlightDataSeries: false
                        },
                        x: {
                            // x value
                            show: true,
                            format: "dd MMM",
                            // dd/MM, dd MMM yy, dd MMM yyyy
                            formatter: void 0
                        },
                        y: {
                            formatter: void 0,
                            title: {
                                /**
             * @param {string} seriesName
             */ formatter: function formatter(seriesName) {
                                    return seriesName ? seriesName + ": " : "";
                                }
                            }
                        },
                        z: {
                            formatter: void 0,
                            title: "Size: "
                        },
                        marker: {
                            show: true,
                            fillColors: void 0
                        },
                        items: {
                            display: "flex"
                        },
                        fixed: {
                            enabled: false,
                            position: "topRight",
                            // topRight, topLeft, bottomRight, bottomLeft
                            offsetX: 0,
                            offsetY: 0
                        }
                    },
                    xaxis: {
                        type: "category",
                        categories: [],
                        convertedCatToNumeric: false,
                        // internal property which should not be altered outside
                        offsetX: 0,
                        offsetY: 0,
                        overwriteCategories: void 0,
                        labels: {
                            show: true,
                            rotate: -45,
                            rotateAlways: false,
                            hideOverlappingLabels: true,
                            trim: false,
                            minHeight: void 0,
                            maxHeight: 120,
                            showDuplicates: true,
                            style: {
                                colors: [],
                                fontSize: "12px",
                                fontWeight: 400,
                                fontFamily: void 0,
                                cssClass: ""
                            },
                            offsetX: 0,
                            offsetY: 0,
                            format: void 0,
                            formatter: void 0,
                            // custom formatter function which will override format
                            datetimeUTC: true,
                            datetimeFormatter: {
                                // Base format per interval unit. TimeScale.formatDates folds in
                                // coarser context automatically when the data range spans it
                                // (e.g. month-scale across years → 'MMM yyyy', hour-scale across
                                // days → 'dd MMM HH:mm'). Customizing a base format that already
                                // includes the higher-unit token disables the auto-expansion for
                                // that level.
                                year: "yyyy",
                                month: "MMM",
                                day: "dd MMM",
                                hour: "HH:mm",
                                minute: "HH:mm",
                                second: "HH:mm:ss"
                            }
                        },
                        group: {
                            groups: [],
                            style: {
                                colors: [],
                                fontSize: "12px",
                                fontWeight: 400,
                                fontFamily: void 0,
                                cssClass: ""
                            }
                        },
                        axisBorder: {
                            show: true,
                            color: "#e0e0e0",
                            width: "100%",
                            height: 1,
                            offsetX: 0,
                            offsetY: 0
                        },
                        axisTicks: {
                            show: true,
                            color: "#e0e0e0",
                            height: 6,
                            offsetX: 0,
                            offsetY: 0
                        },
                        stepSize: void 0,
                        tickAmount: void 0,
                        tickPlacement: "on",
                        min: void 0,
                        max: void 0,
                        range: void 0,
                        floating: false,
                        decimalsInFloat: void 0,
                        position: "bottom",
                        title: {
                            text: void 0,
                            offsetX: 0,
                            offsetY: 0,
                            style: {
                                color: void 0,
                                fontSize: "12px",
                                fontWeight: 900,
                                fontFamily: void 0,
                                cssClass: ""
                            }
                        },
                        crosshairs: {
                            show: true,
                            width: 1,
                            // tickWidth/barWidth or an integer
                            position: "back",
                            opacity: 0.9,
                            stroke: {
                                color: "#b6b6b6",
                                width: 1,
                                dashArray: 3
                            },
                            fill: {
                                type: "solid",
                                // solid, gradient
                                color: "#B1B9C4",
                                gradient: {
                                    colorFrom: "#D8E3F0",
                                    colorTo: "#BED1E6",
                                    stops: [
                                        0,
                                        100
                                    ],
                                    opacityFrom: 0.4,
                                    opacityTo: 0.5
                                }
                            },
                            dropShadow: {
                                enabled: false,
                                left: 0,
                                top: 0,
                                blur: 1,
                                opacity: 0.8
                            }
                        },
                        tooltip: {
                            enabled: false,
                            offsetY: 0,
                            formatter: void 0,
                            style: {
                                fontSize: "12px",
                                fontFamily: void 0
                            }
                        }
                    },
                    yaxis: this.yAxis,
                    theme: {
                        mode: "",
                        palette: "palette1",
                        // If defined, it will overwrite globals.colors variable
                        // Facet (#13): read `--apx-*` CSS design tokens from the cascade
                        // (accent/fore/grid/surface + series-1..N). true (default) reads any
                        // present (absence is a no-op); false disables. Tokens top the
                        // resolution chain below explicit config. Tokens are re-read on every
                        // render; call chart.refreshTokens() to pick up a runtime CSS change
                        // that does not itself trigger a render.
                        tokens: true,
                        // Facet (#13): 'os' follows prefers-color-scheme + prefers-contrast
                        // reactively (SSR-safe, cleaned up on destroy). false disables.
                        follow: false,
                        // 'os' | false
                        // Facet (#13): a theme registered via ApexCharts.registerTheme(name, def)
                        name: "",
                        // '' | registered theme name
                        monochrome: {
                            // monochrome allows you to select just 1 color and fill out the rest with light/dark shade (intensity can be selected)
                            enabled: false,
                            color: "#008FFB",
                            shadeTo: "light",
                            shadeIntensity: 0.65
                        },
                        accessibility: {
                            colorBlindMode: ""
                        }
                    }
                };
            }
        }
    ]);
    return Options;
}();
var Config = /*#__PURE__*/ function() {
    "use strict";
    function Config(opts) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Config);
        this.opts = opts;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Config, [
        {
            /** @param {{responsiveOverride: any}} opts */ key: "init",
            value: function init(param) {
                var responsiveOverride = param.responsiveOverride;
                var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
                var opts = this.opts;
                var options2 = new Options();
                var defaults = new Defaults(opts);
                opts = this.normalizeAliasedChartType(opts);
                this.chartType = opts.chart.type;
                opts = this.extendYAxis(opts);
                opts = this.extendAnnotations(opts);
                var config = options2.init();
                var newDefaults = {};
                if (opts && (typeof opts === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(opts)) === "object") {
                    var chartDefaults = {};
                    var chartTypes = [
                        "line",
                        "area",
                        "bar",
                        "candlestick",
                        "boxPlot",
                        "violin",
                        "rangeBar",
                        "rangeArea",
                        "bubble",
                        "scatter",
                        "heatmap",
                        "treemap",
                        "unit",
                        "sunburst",
                        "pie",
                        "polarArea",
                        "donut",
                        "radar",
                        "radialBar"
                    ];
                    var requestedType = opts.chart.requestedType;
                    if (requestedType === "funnel" || requestedType === "pyramid") {
                        chartDefaults = /** @type {any} */ defaults[requestedType]();
                    } else if (requestedType === "gauge") {
                        chartDefaults = defaults.gauge();
                    } else if (chartTypes.indexOf(opts.chart.type) !== -1) {
                        chartDefaults = /** @type {any} */ defaults[opts.chart.type]();
                    } else {
                        chartDefaults = defaults.line();
                    }
                    if ((_b = (_a = opts.plotOptions) == null ? void 0 : _a.bar) == null ? void 0 : _b.isFunnel) {
                        chartDefaults = defaults.funnel();
                    }
                    if (opts.chart.stacked && opts.chart.type === "bar") {
                        chartDefaults = defaults.stackedBars();
                    }
                    if ((_c = opts.chart.brush) == null ? void 0 : _c.enabled) {
                        chartDefaults = defaults.brush(chartDefaults);
                    }
                    if ((_e = (_d = opts.plotOptions) == null ? void 0 : _d.line) == null ? void 0 : _e.isSlopeChart) {
                        chartDefaults = defaults.slope();
                    }
                    if (opts.chart.stacked && opts.chart.stackType === "100%") {
                        opts = defaults.stacked100(opts);
                    }
                    if ((_g = (_f = opts.plotOptions) == null ? void 0 : _f.bar) == null ? void 0 : _g.isDumbbell) {
                        opts = defaults.dumbbell(opts);
                    }
                    this.checkForDarkTheme(Environment.getApex());
                    this.checkForDarkTheme(opts);
                    opts.xaxis = opts.xaxis || Environment.getApex().xaxis || {};
                    if (!responsiveOverride) {
                        opts.xaxis.convertedCatToNumeric = false;
                    }
                    opts = this.checkForCatToNumericXAxis(this.chartType, chartDefaults, opts);
                    if (((_h = opts.chart.sparkline) == null ? void 0 : _h.enabled) || ((_j = (_i = Environment.getApex().chart) == null ? void 0 : _i.sparkline) == null ? void 0 : _j.enabled)) {
                        chartDefaults = defaults.sparkline(chartDefaults);
                    }
                    newDefaults = Utils$1.extend(config, chartDefaults);
                }
                var mergedWithDefaultConfig = Utils$1.extend(newDefaults, Environment.getApex());
                config = Utils$1.extend(mergedWithDefaultConfig, opts);
                config = this.handleUserInputErrors(config);
                return config;
            }
        },
        {
            /**
   * Promoted chart-type aliases — `funnel`, `pyramid`, `gauge` — render via
   * the existing `bar` (with `isFunnel`) and `radialBar` pathways. To keep
   * the ~20 internal `chart.type === 'bar' | 'radialBar'` checks working
   * unchanged, we normalize `chart.type` to the base renderer name here and
   * preserve the user-facing name on `chart.requestedType` for the public
   * API and for default selection.
   *
   * @param {Record<string, any>} opts
   * @returns {Record<string, any>}
   */ key: "normalizeAliasedChartType",
            value: function normalizeAliasedChartType(opts) {
                if (!opts || !opts.chart) return opts;
                var requested = opts.chart.type;
                if (requested !== "funnel" && requested !== "pyramid" && requested !== "gauge" && requested !== "waffle") {
                    return opts;
                }
                opts.chart.requestedType = requested;
                if (requested === "waffle") {
                    opts.plotOptions = opts.plotOptions || {};
                    opts.plotOptions.unit = opts.plotOptions.unit || {};
                    if (opts.plotOptions.unit.layout == null) {
                        opts.plotOptions.unit.layout = "grid";
                    }
                    if (opts.plotOptions.unit.shape == null) {
                        opts.plotOptions.unit.shape = "square";
                    }
                    opts.chart.type = "unit";
                } else if (requested === "funnel" || requested === "pyramid") {
                    opts.plotOptions = opts.plotOptions || {};
                    opts.plotOptions.bar = opts.plotOptions.bar || {};
                    opts.plotOptions.bar.isFunnel = true;
                    opts.plotOptions.bar.horizontal = true;
                    opts.chart.type = "bar";
                    if (requested === "pyramid") {
                        opts.plotOptions.bar.isPyramid = true;
                    } else {
                        opts.plotOptions.bar.isPyramid = false;
                    }
                } else if (requested === "gauge") {
                    opts.chart.type = "radialBar";
                }
                return opts;
            }
        },
        {
            /**
   * @param {string} chartType
   * @param {Record<string, any>} chartDefaults
   * @param {Record<string, any>} opts
   */ key: "checkForCatToNumericXAxis",
            value: function checkForCatToNumericXAxis(chartType, chartDefaults, opts) {
                var _a, _b, _c, _d, _e;
                var defaults = new Defaults(opts);
                var isBarHorizontal = (chartType === "bar" || chartType === "boxPlot" || chartType === "violin") && ((_b = (_a = opts.plotOptions) == null ? void 0 : _a.bar) == null ? void 0 : _b.horizontal);
                var unsupportedZoom = chartType === "pie" || chartType === "polarArea" || chartType === "donut" || chartType === "radar" || chartType === "radialBar" || chartType === "heatmap" || chartType === "unit" || chartType === "sunburst";
                var notNumericXAxis = opts.xaxis.type !== "datetime" && opts.xaxis.type !== "numeric";
                var isScatterJitter = (chartType === "scatter" || chartType === "bubble") && ((_e = (_d = (_c = opts.plotOptions) == null ? void 0 : _c.scatter) == null ? void 0 : _d.jitter) == null ? void 0 : _e.enabled);
                var tickPlacement = opts.xaxis.tickPlacement ? opts.xaxis.tickPlacement : chartDefaults.xaxis && chartDefaults.xaxis.tickPlacement;
                if (!isBarHorizontal && !unsupportedZoom && !isScatterJitter && notNumericXAxis && tickPlacement !== "between") {
                    opts = defaults.convertCatToNumeric(opts);
                }
                return opts;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   * @param {import('../../types/internal').ChartStateW} [w]
   */ key: "extendYAxis",
            value: function extendYAxis(opts, w) {
                var options2 = new Options();
                if (typeof opts.yaxis === "undefined" || !opts.yaxis || Array.isArray(opts.yaxis) && opts.yaxis.length === 0) {
                    opts.yaxis = {};
                }
                var globalApex = Environment.getApex();
                if (opts.yaxis.constructor !== Array && globalApex.yaxis && globalApex.yaxis.constructor !== Array) {
                    opts.yaxis = Utils$1.extend(opts.yaxis, globalApex.yaxis);
                }
                if (opts.yaxis.constructor !== Array) {
                    opts.yaxis = [
                        Utils$1.extend(options2.yAxis, opts.yaxis)
                    ];
                } else {
                    opts.yaxis = Utils$1.extendArray(opts.yaxis, options2.yAxis);
                }
                var isLogY = false;
                opts.yaxis.forEach(function(y) {
                    if (y.logarithmic) {
                        isLogY = true;
                    }
                });
                var series = opts.series;
                if (w && !series) {
                    series = w.config.series;
                }
                if (isLogY && series.length !== opts.yaxis.length && series.length) {
                    opts.yaxis = series.map(function(s, i) {
                        if (!s.name) {
                            series[i].name = "series-".concat(i + 1);
                        }
                        if (opts.yaxis[i]) {
                            opts.yaxis[i].seriesName = series[i].name;
                            return opts.yaxis[i];
                        } else {
                            var newYaxis = Utils$1.extend(options2.yAxis, opts.yaxis[0]);
                            newYaxis.show = false;
                            return newYaxis;
                        }
                    });
                }
                if (isLogY && series.length > 1 && series.length !== opts.yaxis.length) {
                    console.warn("A multi-series logarithmic chart should have equal number of series and y-axes");
                }
                return opts;
            }
        },
        {
            // annotations also accepts array, so we need to extend them manually
            /**
   * @param {Record<string, any>} opts
   */ key: "extendAnnotations",
            value: function extendAnnotations(opts) {
                if (typeof opts.annotations === "undefined") {
                    opts.annotations = {};
                    opts.annotations.yaxis = [];
                    opts.annotations.xaxis = [];
                    opts.annotations.points = [];
                }
                opts = this.extendYAxisAnnotations(opts);
                opts = this.extendXAxisAnnotations(opts);
                opts = this.extendPointAnnotations(opts);
                return opts;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "extendYAxisAnnotations",
            value: function extendYAxisAnnotations(opts) {
                var options2 = new Options();
                opts.annotations.yaxis = Utils$1.extendArray(typeof opts.annotations.yaxis !== "undefined" ? opts.annotations.yaxis : [], options2.yAxisAnnotation);
                return opts;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "extendXAxisAnnotations",
            value: function extendXAxisAnnotations(opts) {
                var options2 = new Options();
                opts.annotations.xaxis = Utils$1.extendArray(typeof opts.annotations.xaxis !== "undefined" ? opts.annotations.xaxis : [], options2.xAxisAnnotation);
                return opts;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "extendPointAnnotations",
            value: function extendPointAnnotations(opts) {
                var options2 = new Options();
                opts.annotations.points = Utils$1.extendArray(typeof opts.annotations.points !== "undefined" ? opts.annotations.points : [], options2.pointAnnotation);
                return opts;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "checkForDarkTheme",
            value: function checkForDarkTheme(opts) {
                if (opts.theme && opts.theme.mode === "dark") {
                    if (!opts.tooltip) {
                        opts.tooltip = {};
                    }
                    if (opts.tooltip.theme !== "light") {
                        opts.tooltip.theme = "dark";
                    }
                    if (!opts.chart.foreColor) {
                        opts.chart.foreColor = "#f6f7f8";
                    }
                    if (!opts.theme.palette) {
                        opts.theme.palette = "palette4";
                    }
                }
            }
        },
        {
            /**
   * @param {any} opts
   */ key: "handleUserInputErrors",
            value: function handleUserInputErrors(opts) {
                var config = opts;
                if (config.tooltip.shared && config.tooltip.intersect) {
                    throw new Error("tooltip.shared cannot be enabled when tooltip.intersect is true. Turn off any other option by setting it to false.");
                }
                if (config.chart.type === "bar" && config.plotOptions.bar.horizontal) {
                    if (config.yaxis.length > 1) {
                        throw new Error("Multiple Y Axis for bars are not supported. Switch to column chart by setting plotOptions.bar.horizontal=false");
                    }
                    if (config.yaxis[0].reversed) {
                        config.yaxis[0].opposite = true;
                    }
                    config.xaxis.tooltip.enabled = false;
                    config.yaxis[0].tooltip.enabled = false;
                    config.chart.zoom.enabled = false;
                }
                if (config.chart.type === "bar" || config.chart.type === "rangeBar") {
                    if (config.tooltip.shared) {
                        if (config.xaxis.crosshairs.width === "barWidth" && config.series.length > 1) {
                            config.xaxis.crosshairs.width = "tickWidth";
                        }
                    }
                }
                if (config.chart.type === "candlestick" || config.chart.type === "boxPlot") {
                    if (config.yaxis[0].reversed) {
                        console.warn("Reversed y-axis in ".concat(config.chart.type, " chart is not supported."));
                        config.yaxis[0].reversed = false;
                    }
                }
                return config;
            }
        }
    ]);
    return Config;
}();
var LINE_HEIGHT_RATIO = 1.618;
var NICE_SCALE_ALLOWED_MAG_MSD = [
    [
        1,
        1,
        2,
        5,
        5,
        5,
        10,
        10,
        10,
        10,
        10
    ],
    [
        1,
        1,
        2,
        5,
        5,
        5,
        10,
        10,
        10,
        10,
        10
    ]
];
var NICE_SCALE_DEFAULT_TICKS = [
    1,
    2,
    4,
    4,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    6,
    12,
    12,
    12,
    12,
    12,
    12,
    12,
    12,
    12,
    24
];
var Globals = /*#__PURE__*/ function() {
    "use strict";
    function Globals() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Globals);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Globals, [
        {
            /**
   * @param {any} gl
   */ key: "initGlobalVars",
            value: function initGlobalVars(gl) {
                gl.series = [];
                gl.seriesCandleO = [];
                gl.seriesCandleH = [];
                gl.seriesCandleM = [];
                gl.seriesCandleL = [];
                gl.seriesCandleC = [];
                gl.seriesRangeStart = [];
                gl.seriesRangeEnd = [];
                gl.seriesRange = [];
                gl.seriesPercent = [];
                gl.seriesGoals = [];
                gl.seriesX = [];
                gl.seriesZ = [];
                gl.seriesNames = [];
                gl.seriesTotals = [];
                gl.seriesLog = [];
                gl.seriesColors = [];
                gl.stackedSeriesTotals = [];
                gl.seriesXvalues = [];
                gl.seriesYvalues = [];
                gl.dataWasParsed = false;
                gl.originalSeries = null;
                gl.maxValsInArrayIndex = 0;
                gl.yValueDecimal = 0;
                gl.allSeriesHasEqualX = true;
                gl.hasNullValues = false;
                gl.invalidLogScale = false;
                gl.seriesRangeName = {};
                gl.labels = [];
                gl.hasXaxisGroups = false;
                gl.groups = [];
                gl.barGroups = [];
                gl.lineGroups = [];
                gl.areaGroups = [];
                gl.hasSeriesGroups = false;
                gl.seriesGroups = [];
                gl.categoryLabels = [];
                gl.timescaleLabels = [];
                gl.noLabelsProvided = false;
                gl.isXNumeric = false;
                gl.skipLastTimelinelabel = false;
                gl.skipFirstTimelinelabel = false;
                gl.isDataXYZ = false;
                gl.isMultiLineX = false;
                gl.isMultipleYAxis = false;
                gl.maxY = -Number.MAX_VALUE;
                gl.minY = Number.MIN_VALUE;
                gl.minYArr = [];
                gl.maxYArr = [];
                gl.maxX = -Number.MAX_VALUE;
                gl.minX = Number.MAX_VALUE;
                gl.initialMaxX = -Number.MAX_VALUE;
                gl.initialMinX = Number.MAX_VALUE;
                gl.maxDate = 0;
                gl.minDate = Number.MAX_VALUE;
                gl.minZ = Number.MAX_VALUE;
                gl.maxZ = -Number.MAX_VALUE;
                gl.minXDiff = Number.MAX_VALUE;
                gl.yAxisScale = [];
                gl.xAxisScale = null;
                gl.xAxisTicksPositions = [];
                gl.xRange = 0;
                gl.yRange = [];
                gl.zRange = 0;
                gl.dataPoints = 0;
                gl.xTickAmount = 0;
                gl.multiAxisTickAmount = 0;
                gl.disableZoomIn = false;
                gl.disableZoomOut = false;
                gl.yLabelsCoords = [];
                gl.yTitleCoords = [];
                gl.barPadForNumericAxis = 0;
                gl.padHorizontal = 0;
                gl.rotateXLabels = false;
                gl.overlappingXLabels = false;
                gl.radialSize = 0;
                gl.barHeight = 0;
                gl.barWidth = 0;
                gl.animationEnded = false;
                gl.isDestroyed = false;
                gl.bulkRevealScheduled = false;
                gl.resizeTimer = null;
                gl.selectionResizeTimer = null;
                gl.delayedElements = [];
                gl.pointsArray = [];
                gl.barCanvasCoords = null;
                gl.activeRenderer = null;
                gl.dataLabelsRects = [];
                gl.lastDrawnDataLabelsIndexes = [];
                gl.textRectsCache = /* @__PURE__ */ new Map();
                gl.domCache = /* @__PURE__ */ new Map();
                gl.dimensionCache = {};
                gl.cachedSelectors = {};
                if (!gl.seriesNS) {
                    this._attachNamespaces(gl);
                }
            }
        },
        {
            /**
   * Attach domain-grouped namespace sub-objects onto gl.
   * Each sub-object is a plain object whose properties are defined as
   * getters/setters that read/write the canonical flat properties on gl.
   * This means there is exactly ONE storage location per value — no copies,
   * no sync needed.
   *
   * Namespaces:
   *   gl.series  — parsed series data and chart-type-specific arrays
   *   gl.axes    — axis bounds, scales, ranges, tick state
   *   gl.layout  — SVG/grid dimensions, translations, label sizes
   *   gl.cache   — DOM caches, timers, observers, drawing scratch space
   *
   * Note: interact state lives on w.interact (not gl) — see Base.js.
   * @param {any} gl
   */ key: "_attachNamespaces",
            value: function _attachNamespaces(gl) {
                var proxy = function proxy(ns, key) {
                    var nsKey = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : key;
                    Object.defineProperty(ns, nsKey, {
                        get: function get() {
                            return gl[key];
                        },
                        set: function set(v) {
                            gl[key] = v;
                        },
                        enumerable: true,
                        configurable: true
                    });
                };
                var seriesNS = {};
                proxy(seriesNS, "series", "data");
                for(var _i = 0, _iter = [
                    "seriesNames",
                    "seriesX",
                    "seriesZ",
                    "seriesXvalues",
                    "seriesYvalues",
                    "seriesGoals",
                    "seriesLog",
                    "seriesColors",
                    "seriesPercent",
                    "seriesTotals",
                    "stackedSeriesTotals",
                    "seriesCandleO",
                    "seriesCandleH",
                    "seriesCandleM",
                    "seriesCandleL",
                    "seriesCandleC",
                    "seriesRangeStart",
                    "seriesRangeEnd",
                    "seriesRange",
                    "seriesYAxisMap",
                    "seriesYAxisReverseMap",
                    "seriesGroups",
                    "barGroups",
                    "lineGroups",
                    "areaGroups",
                    "originalSeries",
                    "collapsedSeries",
                    "collapsedSeriesIndices",
                    "ancillaryCollapsedSeries",
                    "ancillaryCollapsedSeriesIndices",
                    "allSeriesCollapsed",
                    "risingSeries",
                    "previousPaths",
                    "ignoreYAxisIndexes",
                    "labels",
                    "categoryLabels",
                    "timescaleLabels",
                    "groups"
                ]; _i < _iter.length; _i++){
                    var key = _iter[_i];
                    proxy(seriesNS, key);
                }
                Object.defineProperty(gl, "seriesNS", {
                    value: seriesNS,
                    writable: false,
                    enumerable: false,
                    configurable: true
                });
                var axesNS = {};
                for(var _i1 = 0, _iter1 = [
                    "minX",
                    "maxX",
                    "initialMinX",
                    "initialMaxX",
                    "minY",
                    "maxY",
                    "minYArr",
                    "maxYArr",
                    "minZ",
                    "maxZ",
                    "minDate",
                    "maxDate",
                    "minXDiff",
                    "xRange",
                    "yRange",
                    "zRange",
                    "xAxisScale",
                    "yAxisScale",
                    "xAxisTicksPositions",
                    "xTickAmount",
                    "multiAxisTickAmount",
                    "dataPoints",
                    "maxValsInArrayIndex",
                    "isXNumeric",
                    "isMultipleYAxis",
                    "isMultiLineX",
                    "isDataXYZ",
                    "dataFormatXNumeric",
                    "allSeriesHasEqualX",
                    "hasNullValues",
                    "dataWasParsed",
                    "hasXaxisGroups",
                    "hasSeriesGroups",
                    "skipFirstTimelinelabel",
                    "skipLastTimelinelabel",
                    "yValueDecimal",
                    "invalidLogScale",
                    "noLabelsProvided"
                ]; _i1 < _iter1.length; _i1++){
                    var key1 = _iter1[_i1];
                    proxy(axesNS, key1);
                }
                Object.defineProperty(gl, "axes", {
                    value: axesNS,
                    writable: false,
                    enumerable: false,
                    configurable: true
                });
                var layoutNS = {};
                for(var _i2 = 0, _iter2 = [
                    "svgWidth",
                    "svgHeight",
                    "gridWidth",
                    "gridHeight",
                    "translateX",
                    "translateY",
                    "translateXAxisX",
                    "translateXAxisY",
                    "translateYAxisX",
                    "xAxisLabelsHeight",
                    "xAxisGroupLabelsHeight",
                    "xAxisLabelsWidth",
                    "yAxisLabelsWidth",
                    "yAxisWidths",
                    "yLabelsCoords",
                    "yTitleCoords",
                    "padHorizontal",
                    "barPadForNumericAxis",
                    "rotateXLabels",
                    "scaleX",
                    "scaleY",
                    "radialSize",
                    "defaultLabels",
                    "overlappingXLabels"
                ]; _i2 < _iter2.length; _i2++){
                    var key2 = _iter2[_i2];
                    proxy(layoutNS, key2);
                }
                Object.defineProperty(gl, "layout", {
                    value: layoutNS,
                    writable: false,
                    enumerable: false,
                    configurable: true
                });
                var cacheNS = {};
                for(var _i3 = 0, _iter3 = [
                    "domCache",
                    "dimensionCache",
                    "cachedSelectors",
                    "textRectsCache",
                    "pointsArray",
                    "dataLabelsRects",
                    "lastDrawnDataLabelsIndexes",
                    "delayedElements",
                    "resizeTimer",
                    "selectionResizeTimer",
                    "resizeObserver"
                ]; _i3 < _iter3.length; _i3++){
                    var key3 = _iter3[_i3];
                    proxy(cacheNS, key3);
                }
                Object.defineProperty(gl, "cache", {
                    value: cacheNS,
                    writable: false,
                    enumerable: false,
                    configurable: true
                });
            }
        },
        {
            /**
   * Persistent chart state — set ONCE at chart construction and intentionally NOT
   * reset by initGlobalVars.  These values must survive updateSeries / re-render.
   *
   * Rule: if a value is recalculated fresh on every render it belongs in
   * initGlobalVars instead, not here.
   * @returns {import('../../types/internal').ChartGlobals}
   * @param {Record<string, any>} config
   */ key: "globalVars",
            value: function globalVars(config) {
                var globals = {
                    // ── Identity (set once, never changes) ───────────────────────────────────
                    chartID: null,
                    // full chart ID: "apexcharts-<cuid>"
                    cuid: null,
                    // random suffix only
                    // ── Event registry (accumulates listeners, never reset) ───────────────────
                    events: {
                        beforeMount: [],
                        mounted: [],
                        updated: [],
                        clicked: [],
                        selection: [],
                        dataPointSelection: [],
                        zoomed: [],
                        scrolled: []
                    },
                    // ── Theme colors (set by Theme module after config merge) ─────────────────
                    colors: [],
                    fill: {
                        colors: []
                    },
                    // ── Animation-frame handles cancellable across re-renders ─────────────────
                    // The chart-type instances that own these loops (Radial needle, Unit
                    // gather/exit) are recreated every render, so the handle lives here: a new
                    // render cancels the previous render's loop before starting its own,
                    // instead of leaving it to animate detached nodes. NOT reset per render.
                    radialNeedleRAF: null,
                    unitGatherRAF: null,
                    unitExitRAF: null,
                    stroke: {
                        colors: []
                    },
                    dataLabels: {
                        style: {
                            colors: []
                        }
                    },
                    radarPolygons: {
                        fill: {
                            colors: []
                        }
                    },
                    markers: {
                        colors: [],
                        size: config.markers.size,
                        largestSize: 0
                    },
                    // ── Device / environment detected once at startup ─────────────────────────
                    // Note: isTouchDevice lives on w.interact — see Base.js. Shim installed there.
                    LINE_HEIGHT_RATIO: LINE_HEIGHT_RATIO,
                    // ── Chart-type flags (derived from config, set during Core.mount) ─────────
                    axisCharts: true,
                    // false for pie/radial/treemap etc.
                    isSlopeChart: config.plotOptions.line.isSlopeChart,
                    comboCharts: false,
                    // true when mixing line + column series
                    // ── Config snapshots (backups for zoom-reset / updateOptions) ────────────
                    initialConfig: null,
                    // deep clone of the original user config
                    initialSeries: [],
                    lastXAxis: [],
                    lastYAxis: [],
                    // ── User interaction state (must survive re-renders) ──────────────────────
                    // Note: zoomEnabled, panEnabled, selectionEnabled, zoomed, selection,
                    //       visibleXRange, selectedDataPoints, mousedown, clientX, clientY,
                    //       lastClientPosition, capturedSeriesIndex, capturedDataPointIndex,
                    //       disableZoomIn, disableZoomOut, isTouchDevice
                    //       live on w.interact — see Base.js. Backward-compat shims installed there.
                    // Series collapse state (user-driven, must persist across re-renders)
                    allSeriesCollapsed: false,
                    collapsedSeries: [],
                    collapsedSeriesIndices: [],
                    ancillaryCollapsedSeries: [],
                    ancillaryCollapsedSeriesIndices: [],
                    risingSeries: [],
                    // series being re-shown after collapse
                    ignoreYAxisIndexes: [],
                    // y-axis indices excluded during series collapse
                    // ── Lifecycle / update flags ──────────────────────────────────────────────
                    isDirty: false,
                    // true when user called an update method manually
                    isExecCalled: false,
                    // true when update came via exec()
                    dataChanged: false,
                    // true when series data was changed dynamically
                    resized: false,
                    // true after a container resize
                    // ── Data format flags (derived from config/series, stable between renders) ─
                    // Note: dataFormatXNumeric lives on w.axisFlags — see Base.js. Shim installed there.
                    // hasNullValues / invalidLogScale are recomputed every render and reset in
                    // initGlobalVars (they are NOT stable between renders).
                    // Persistent data tracking
                    columnSeries: null,
                    // tracks which series are rendered as bars/columns
                    yaxis: null,
                    // resolved yaxis config array
                    total: 0,
                    // running total (used by pie/radial)
                    // ── Animation control ─────────────────────────────────────────────────────
                    shouldAnimate: true,
                    previousPaths: [],
                    // paths from previous render — source for enter animation
                    // Streaming scroll: previous frame's parsed rows + pixel positions,
                    // captured by Series.getPreviousPaths(). Consulted (like previousPaths)
                    // only while a data-change morph renders. See StreamScroll.
                    prevStreamFrame: null,
                    // Axis-chrome snapshot (tick label texts/positions + gridline positions)
                    // captured alongside prevStreamFrame; consumed once by AxisTransition
                    // after a variable-length re-render mounts.
                    prevChromeFrame: null,
                    // ── SVG viewport (set by Dimensions, but persistent as layout anchor) ─────
                    svgWidth: 0,
                    svgHeight: 0,
                    // Fingerprint of the container inputs that fed the last rendered size, so
                    // the window-resize handler can skip a redraw (and the animation teardown
                    // it causes) when a resize does not change the chart's drawing box.
                    lastResizeSignature: null,
                    // Note: gridWidth, gridHeight, translateX, translateY, translateXAxisX,
                    // translateXAxisY, xAxisLabelsHeight, xAxisGroupLabelsHeight, xAxisLabelsWidth,
                    // rotateXLabels, xAxisHeight, yLabelsCoords, yTitleCoords live on w.layout —
                    // see Base.js. Backward-compat shims installed there.
                    defaultLabels: false,
                    // Note: formatter properties (xLabelFormatter, yLabelFormatters, etc.) live on
                    // w.formatters — see Base.js. Backward-compat shims installed there.
                    yAxisLabelsWidth: 0,
                    scaleX: 1,
                    scaleY: 1,
                    translateYAxisX: [],
                    yAxisWidths: [],
                    // ── Instances (created once, replaced only on full re-init) ──────────────
                    tooltip: null,
                    resizeObserver: null,
                    // ── Locale (loaded once; changes only via setLocale()) ───────────────────
                    locale: {},
                    // ── Method queue (deferred calls during async operations) ────────────────
                    memory: {
                        methodsToExec: []
                    },
                    // ── Scale configuration constants — imported from utils/Constants.js ──────
                    niceScaleAllowedMagMsd: NICE_SCALE_ALLOWED_MAG_MSD,
                    niceScaleDefaultTicks: NICE_SCALE_DEFAULT_TICKS,
                    // ── Multi-axis series mapping ─────────────────────────────────────────────
                    seriesYAxisMap: [],
                    // yAxis index → series indices[]
                    seriesYAxisReverseMap: [],
                    // series index → yAxis index
                    noData: false
                };
                return /** @type {import('../../types/internal').ChartGlobals} */ /** @type {unknown} */ globals;
            }
        },
        {
            /**
   * Lazy initial-series snapshot. Capturing `initialSeries` used to deep-clone
   * the entire series on every parse and every update: the single largest CPU
   * bucket at 50k+ points (profiled at ~23ms per 50k update). The setter now
   * stores a cheap per-series shallow copy (series OBJECTS copied, data arrays
   * shared) and the deep snapshot materializes only when something actually
   * reads it (resetSeries, toolbar reset-home, tooltip same-length check).
   *
   * Why sharing the data arrays is safe: internal "mutations" of a series'
   * data are property REPLACEMENTS (`series[i].data = []` on legend collapse),
   * which cannot reach the captured copies because each series object was
   * copied at capture time. The one in-place mutator (appendData's push loop)
   * re-captures immediately after mutating, so the pending snapshot never
   * spans the mutation.
   *
   * @param {Record<string, any>} globals
   */ key: "defineLazyInitialSeries",
            value: function defineLazyInitialSeries(globals) {
                var src = [];
                var snap = null;
                Object.defineProperty(globals, "initialSeries", {
                    configurable: true,
                    enumerable: true,
                    get: function get() {
                        if (snap === null) {
                            snap = Utils$1.clone(src);
                        }
                        return snap;
                    },
                    set: function set(value) {
                        src = Array.isArray(value) ? value.map(function(s) {
                            return s && (typeof s === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(s)) === "object" && !Array.isArray(s) ? __spreadValues({}, s) : s;
                        }) : value;
                        snap = null;
                        globals._initialSeriesPeek = src;
                    }
                });
                globals._initialSeriesPeek = src;
            }
        },
        {
            /**
   * @param {Record<string, any>} config
   */ key: "init",
            value: function init(config) {
                var globals = this.globalVars(config);
                this.initGlobalVars(globals);
                this.defineLazyInitialSeries(globals);
                globals.initialConfig = Utils$1.extend({}, config);
                globals.initialSeries = config.series;
                globals.lastXAxis = Utils$1.clone(/** @type {NonNullable<typeof globals.initialConfig>} */ globals.initialConfig.xaxis);
                globals.lastYAxis = Utils$1.clone(/** @type {NonNullable<typeof globals.initialConfig>} */ globals.initialConfig.yaxis);
                return globals;
            }
        }
    ]);
    return Globals;
}();
var Base = /*#__PURE__*/ function() {
    "use strict";
    function Base(opts) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Base);
        this.opts = opts;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Base, [
        {
            /**
   * Build and return the full chart state object `w`.
   * @returns {import('../types/internal').ChartStateW}
   */ key: "init",
            value: function init() {
                var _loop = function(_i, _iter) {
                    var key = _iter[_i];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.formatters[key];
                        },
                        set: function set(v) {
                            w.formatters[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop1 = function(_i1, _iter1) {
                    var key = _iter1[_i1];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.interact[key];
                        },
                        set: function set(v) {
                            w.interact[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop2 = function(_i2, _iter2) {
                    var key = _iter2[_i2];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.layout[key];
                        },
                        set: function set(v) {
                            w.layout[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop3 = function(_i3, _iter3) {
                    var key = _iter3[_i3];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.seriesData[key];
                        },
                        set: function set(v) {
                            w.seriesData[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop4 = function(_i4, _iter4) {
                    var key = _iter4[_i4];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.axisFlags[key];
                        },
                        set: function set(v) {
                            w.axisFlags[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop5 = function(_i5, _iter5) {
                    var key = _iter5[_i5];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.labelData[key];
                        },
                        set: function set(v) {
                            w.labelData[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop6 = function(_i6, _iter6) {
                    var key = _iter6[_i6];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.rangeData[key];
                        },
                        set: function set(v) {
                            w.rangeData[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop7 = function(_i7, _iter7) {
                    var key = _iter7[_i7];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.candleData[key];
                        },
                        set: function set(v) {
                            w.candleData[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                }, _loop8 = function(_i8, _iter8) {
                    var key = _iter8[_i8];
                    Object.defineProperty(globals, key, {
                        get: function get() {
                            return /** @type {Record<string,any>} */ w.violinData[key];
                        },
                        set: function set(v) {
                            w.violinData[key] = v;
                        },
                        enumerable: false,
                        configurable: true
                    });
                };
                var config = new Config(this.opts).init({
                    responsiveOverride: false
                });
                var globals = new Globals().init(config);
                var w = {
                    config: config,
                    globals: globals,
                    dom: {},
                    // DOM node cache — lives here, not inside globals
                    interact: {
                        // Tool mode (derived from toolbar config at construction, updated by Toolbar)
                        zoomEnabled: config.chart.toolbar.autoSelected === "zoom" && config.chart.toolbar.tools.zoom && config.chart.zoom.enabled,
                        panEnabled: config.chart.toolbar.autoSelected === "pan" && config.chart.toolbar.tools.pan,
                        selectionEnabled: config.chart.toolbar.autoSelected === "selection" && config.chart.toolbar.tools.selection,
                        // Measure tool pre-selected via toolbar.autoSelected: 'measure'. Armed by
                        // the Measure module (self-arms on mount) and the toolbar button.
                        measureEnabled: config.chart.toolbar.autoSelected === "measure" && !!config.chart.toolbar.tools.measure && !!(config.chart.measure && config.chart.measure.enabled),
                        // Zoom / pan state (user-driven, must persist across re-renders)
                        zoomed: false,
                        selection: void 0,
                        visibleXRange: void 0,
                        selectedDataPoints: [],
                        // Mouse / pointer state
                        mousedown: false,
                        clientX: null,
                        clientY: null,
                        lastClientPosition: {},
                        // Tooltip capture state
                        capturedSeriesIndex: -1,
                        capturedDataPointIndex: -1,
                        // Timescale zoom bounds (reset per render by TimeScale)
                        disableZoomIn: false,
                        disableZoomOut: false,
                        // Device detection (set once at construction)
                        isTouchDevice: Environment.isBrowser() ? "ontouchstart" in window || navigator.maxTouchPoints > 0 : false
                    },
                    formatters: {
                        // Populated by Formatters.setLabelFormatters() each render
                        xLabelFormatter: void 0,
                        yLabelFormatters: [],
                        xaxisTooltipFormatter: void 0,
                        ttKeyFormatter: void 0,
                        ttVal: void 0,
                        ttZFormatter: void 0,
                        legendFormatter: void 0
                    },
                    // Candlestick / boxplot OHLC arrays — written by Data.handleCandleStickBoxData()
                    // each render; empty for all other chart types.
                    candleData: {
                        seriesCandleO: [],
                        seriesCandleH: [],
                        seriesCandleM: [],
                        seriesCandleL: [],
                        seriesCandleC: [],
                        // Optional raw observations per boxPlot data point ([i][j] = number[]),
                        // rendered as jitter dots. Empty unless boxPlot points are supplied.
                        seriesBoxPoints: []
                    },
                    // Range chart arrays — written by Data.handleRangeData() each render;
                    // empty for all other chart types.
                    rangeData: {
                        seriesRangeStart: [],
                        seriesRangeEnd: [],
                        seriesRange: []
                    },
                    // Violin distribution arrays — written by Data.handleViolinData() each
                    // render; empty for all other chart types.
                    //   seriesViolinDensity[i][j] = { values:number[], weights:number[], maxWeight:number }
                    //   seriesViolinPoints[i][j]  = number[] (raw observations, value-axis units)
                    //   seriesViolinMin/Max[i][j] = number (extent of density + points — drives Range.js)
                    violinData: {
                        seriesViolinDensity: [],
                        seriesViolinPoints: [],
                        seriesViolinMin: [],
                        seriesViolinMax: []
                    },
                    // Label / category data — written by Data.parseData() and TimeScale each render.
                    labelData: {
                        labels: [],
                        categoryLabels: [],
                        timescaleLabels: [],
                        // written by TimeScale.calculateTimeScaleMinMax()
                        hasXaxisGroups: false,
                        groups: [],
                        seriesGroups: []
                    },
                    // Axis / parsing behaviour flags — written by Data.parseData() each render.
                    axisFlags: {
                        isXNumeric: false,
                        dataFormatXNumeric: false,
                        isDataXYZ: false,
                        isRangeData: false,
                        isRangeBar: false,
                        isMultiLineX: false,
                        noLabelsProvided: false,
                        dataWasParsed: false
                    },
                    // Parsed series data — written by Data.parseData() each render.
                    // Note: initialSeries and originalSeries are intentionally excluded —
                    // they are persistent (survive re-renders) and remain on w.globals.
                    seriesData: {
                        series: [],
                        // main y-values array
                        seriesNames: [],
                        seriesX: [],
                        seriesZ: [],
                        seriesColors: [],
                        seriesGoals: [],
                        stackedSeriesTotals: [],
                        stackedSeriesTotalsByGroups: [],
                        unitData: []
                    },
                    // Grid / axis layout computed by Dimensions.plotCoords() each render.
                    // gridWidth/gridHeight/translateX/translateY are also used as starting
                    // points by Dimensions on the next render (accumulated values), so the
                    // shim must be bidirectional — reads and writes both route correctly.
                    layout: {
                        gridHeight: 0,
                        gridWidth: 0,
                        translateX: 0,
                        translateY: 0,
                        translateXAxisX: 0,
                        translateXAxisY: 0,
                        rotateXLabels: false,
                        xAxisHeight: 0,
                        xAxisLabelsHeight: 0,
                        xAxisGroupLabelsHeight: 0,
                        xAxisLabelsWidth: 0,
                        yLabelsCoords: [],
                        yTitleCoords: [],
                        gridPad: {
                            top: 0,
                            right: 0,
                            bottom: 0,
                            left: 0
                        }
                    }
                };
                Object.defineProperty(globals, "dom", {
                    get: function get() {
                        return w.dom;
                    },
                    set: function set(v) {
                        w.dom = v;
                    },
                    enumerable: false,
                    configurable: true
                });
                for(var _i = 0, _iter = [
                    "xLabelFormatter",
                    "yLabelFormatters",
                    "xaxisTooltipFormatter",
                    "ttKeyFormatter",
                    "ttVal",
                    "ttZFormatter",
                    "legendFormatter"
                ]; _i < _iter.length; _i++)_loop(_i, _iter);
                for(var _i1 = 0, _iter1 = [
                    "zoomEnabled",
                    "panEnabled",
                    "selectionEnabled",
                    "zoomed",
                    "selection",
                    "visibleXRange",
                    "selectedDataPoints",
                    "mousedown",
                    "clientX",
                    "clientY",
                    "lastClientPosition",
                    "capturedSeriesIndex",
                    "capturedDataPointIndex",
                    "disableZoomIn",
                    "disableZoomOut",
                    "isTouchDevice"
                ]; _i1 < _iter1.length; _i1++)_loop1(_i1, _iter1);
                for(var _i2 = 0, _iter2 = [
                    "gridHeight",
                    "gridWidth",
                    "translateX",
                    "translateY",
                    "translateXAxisX",
                    "translateXAxisY",
                    "rotateXLabels",
                    "xAxisHeight",
                    "xAxisLabelsHeight",
                    "xAxisGroupLabelsHeight",
                    "xAxisLabelsWidth",
                    "yLabelsCoords",
                    "yTitleCoords",
                    "gridPad"
                ]; _i2 < _iter2.length; _i2++)_loop2(_i2, _iter2);
                for(var _i3 = 0, _iter3 = [
                    "series",
                    "seriesNames",
                    "seriesX",
                    "seriesZ",
                    "seriesColors",
                    "seriesGoals",
                    "stackedSeriesTotals",
                    "stackedSeriesTotalsByGroups"
                ]; _i3 < _iter3.length; _i3++)_loop3(_i3, _iter3);
                for(var _i4 = 0, _iter4 = [
                    "isXNumeric",
                    "dataFormatXNumeric",
                    "isDataXYZ",
                    "isRangeData",
                    "isRangeBar",
                    "isMultiLineX",
                    "noLabelsProvided",
                    "dataWasParsed"
                ]; _i4 < _iter4.length; _i4++)_loop4(_i4, _iter4);
                for(var _i5 = 0, _iter5 = [
                    "labels",
                    "categoryLabels",
                    "timescaleLabels",
                    "hasXaxisGroups",
                    "groups",
                    "seriesGroups"
                ]; _i5 < _iter5.length; _i5++)_loop5(_i5, _iter5);
                for(var _i6 = 0, _iter6 = [
                    "seriesRangeStart",
                    "seriesRangeEnd",
                    "seriesRange"
                ]; _i6 < _iter6.length; _i6++)_loop6(_i6, _iter6);
                for(var _i7 = 0, _iter7 = [
                    "seriesCandleO",
                    "seriesCandleH",
                    "seriesCandleM",
                    "seriesCandleL",
                    "seriesCandleC"
                ]; _i7 < _iter7.length; _i7++)_loop7(_i7, _iter7);
                for(var _i8 = 0, _iter8 = [
                    "seriesViolinDensity",
                    "seriesViolinPoints",
                    "seriesViolinMin",
                    "seriesViolinMax"
                ]; _i8 < _iter8.length; _i8++)_loop8(_i8, _iter8);
                return /** @type {import('../types/internal').ChartStateW} */ /** @type {unknown} */ w;
            }
        }
    ]);
    return Base;
}();
var CoreUtils = /*#__PURE__*/ function() {
    "use strict";
    function CoreUtils(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CoreUtils);
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CoreUtils, [
        {
            /**
   * @memberof CoreUtils
   * returns the sum of all individual values in a multiple stacked series
   * Eg. w.seriesData.series = [[32,33,43,12], [2,3,5,1]]
   *  @return [34,36,48,13]
   * @param {number[]} excludedSeriesIndices
   **/ key: "getStackedSeriesTotals",
            value: function getStackedSeriesTotals() {
                var excludedSeriesIndices = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [];
                var w = this.w;
                var total = [];
                if (w.seriesData.series.length === 0) return total;
                for(var i = 0; i < w.seriesData.series[w.globals.maxValsInArrayIndex].length; i++){
                    var t = 0;
                    for(var j = 0; j < w.seriesData.series.length; j++){
                        if (typeof w.seriesData.series[j][i] !== "undefined" && excludedSeriesIndices.indexOf(j) === -1) {
                            t += w.seriesData.series[j][i];
                        }
                    }
                    total.push(t);
                }
                return total;
            }
        },
        {
            // get total of the all values inside all series
            /**
   * @param {number | null} [index]
   */ key: "getSeriesTotalByIndex",
            value: function getSeriesTotalByIndex() {
                var index = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
                if (index === null) {
                    return /** @type {any[]} */ this.w.config.series.reduce(function(acc, cur) {
                        return acc + cur;
                    }, 0);
                } else {
                    var seriesAtIndex = this.w.seriesData.series[index];
                    if (!Array.isArray(seriesAtIndex)) {
                        return seriesAtIndex != null ? seriesAtIndex : 0;
                    }
                    return seriesAtIndex.reduce(function(acc, cur) {
                        return acc + cur;
                    }, 0);
                }
            }
        },
        {
            /**
   * @memberof CoreUtils
   * returns the sum of values in a multiple stacked grouped charts
   * Eg. w.seriesData.series = [[32,33,43,12], [2,3,5,1], [43, 23, 34, 22]]
   * series 1 and 2 are in a group, while series 3 is in another group
   *  @return [[34, 36, 48, 12], [43, 23, 34, 22]]
   **/ key: "getStackedSeriesTotalsByGroups",
            value: function getStackedSeriesTotalsByGroups() {
                var _this = this;
                var w = this.w;
                var total = [];
                w.labelData.seriesGroups.forEach(function(sg) {
                    var includedIndexes = [];
                    w.config.series.forEach(function(s, si) {
                        if (sg.indexOf(w.seriesData.seriesNames[si]) > -1) {
                            includedIndexes.push(si);
                        }
                    });
                    var excludedIndices = w.seriesData.series.map(function(_, fi) {
                        return includedIndexes.indexOf(fi) === -1 ? fi : -1;
                    }).filter(function(f) {
                        return f !== -1;
                    });
                    total.push(_this.getStackedSeriesTotals(excludedIndices));
                });
                return total;
            }
        },
        {
            key: "setSeriesYAxisMappings",
            value: function setSeriesYAxisMappings() {
                var gl = this.w.globals;
                var cnf = this.w.config;
                var axisSeriesMap = [];
                var seriesYAxisReverseMap = [];
                var unassignedSeriesIndices = [];
                var seriesNameArrayStyle = this.w.seriesData.series.length > cnf.yaxis.length || /**
     * @param {ApexYAxis} a
     */ cnf.yaxis.some(function(a) {
                    return Array.isArray(a.seriesName);
                });
                cnf.series.forEach(function(_s, i) {
                    unassignedSeriesIndices.push(i);
                    seriesYAxisReverseMap.push(null);
                });
                cnf.yaxis.forEach(function(_yaxe, yi) {
                    axisSeriesMap[yi] = [];
                });
                var unassignedYAxisIndices = [];
                cnf.yaxis.forEach(function(yaxe, yi) {
                    var assigned = false;
                    if (yaxe.seriesName) {
                        var seriesNames = [];
                        if (Array.isArray(yaxe.seriesName)) {
                            seriesNames = yaxe.seriesName;
                        } else {
                            seriesNames.push(yaxe.seriesName);
                        }
                        seriesNames.forEach(function(name2) {
                            cnf.series.forEach(function(s, si) {
                                if (/** @type {any} */ s.name === name2) {
                                    var remove = si;
                                    if (yi === si || seriesNameArrayStyle) {
                                        if (!seriesNameArrayStyle || unassignedSeriesIndices.indexOf(si) > -1) {
                                            axisSeriesMap[yi].push([
                                                yi,
                                                si
                                            ]);
                                        } else {
                                            console.warn("Series '" + /** @type {any} */ s.name + "' referenced more than once in what looks like the new style. That is, when using either seriesName: [], or when there are more series than yaxes.");
                                        }
                                    } else {
                                        axisSeriesMap[si].push([
                                            si,
                                            yi
                                        ]);
                                        remove = yi;
                                    }
                                    assigned = true;
                                    remove = unassignedSeriesIndices.indexOf(remove);
                                    if (remove !== -1) {
                                        unassignedSeriesIndices.splice(remove, 1);
                                    }
                                }
                            });
                        });
                    }
                    if (!assigned) {
                        unassignedYAxisIndices.push(yi);
                    }
                });
                axisSeriesMap = axisSeriesMap.map(function(yaxe) {
                    var ra = [];
                    yaxe.forEach(function(sa) {
                        seriesYAxisReverseMap[sa[1]] = sa[0];
                        ra.push(sa[1]);
                    });
                    return ra;
                });
                var lastUnassignedYAxis = cnf.yaxis.length - 1;
                for(var i = 0; i < unassignedYAxisIndices.length; i++){
                    lastUnassignedYAxis = unassignedYAxisIndices[i];
                    axisSeriesMap[lastUnassignedYAxis] = [];
                    if ("TURBOPACK compile-time truthy", 1) {
                        var si = unassignedSeriesIndices[0];
                        unassignedSeriesIndices.shift();
                        axisSeriesMap[lastUnassignedYAxis].push(si);
                        seriesYAxisReverseMap[si] = lastUnassignedYAxis;
                    } else {
                        break;
                    }
                }
                unassignedSeriesIndices.forEach(function(i) {
                    axisSeriesMap[lastUnassignedYAxis].push(i);
                    seriesYAxisReverseMap[i] = lastUnassignedYAxis;
                });
                gl.seriesYAxisMap = axisSeriesMap.map(function(x) {
                    return x;
                });
                gl.seriesYAxisReverseMap = seriesYAxisReverseMap.map(function(x) {
                    return x;
                });
                gl.seriesYAxisMap.forEach(function(axisSeries, ai) {
                    axisSeries.forEach(function(si) {
                        if (/** @type {any} */ cnf.series[si] && /** @type {any} */ cnf.series[si].group === void 0) {
                            var _series = /** @type {any} */ cnf.series[si];
                            _series.group = "apexcharts-axis-".concat(ai.toString());
                        }
                    });
                });
            }
        },
        {
            /**
   * @param {number | null} [index]
   */ key: "isSeriesNull",
            value: function isSeriesNull() {
                var index = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
                var r = [];
                var series = /** @type {any[]} */ this.w.config.series;
                if (index === null) {
                    r = series.filter(function(d) {
                        return d !== null;
                    });
                } else if (series[index] && Array.isArray(series[index].data)) {
                    r = series[index].data.filter(function(d) {
                        return d !== null;
                    });
                } else {
                    r = series[index] !== null && series[index] !== void 0 ? [
                        series[index]
                    ] : [];
                }
                return r.length === 0;
            }
        },
        {
            /**
   * @param {number} index
   */ key: "seriesHaveSameValues",
            value: function seriesHaveSameValues(index) {
                var seriesAtIndex = this.w.seriesData.series[index];
                if (!Array.isArray(seriesAtIndex)) {
                    return true;
                }
                return seriesAtIndex.every(function(val, i, arr) {
                    return val === arr[0];
                });
            }
        },
        {
            /**
   * @param {any[]} labels
   */ key: "getCategoryLabels",
            value: function getCategoryLabels(labels) {
                var w = this.w;
                var catLabels = labels.slice();
                if (w.config.xaxis.convertedCatToNumeric) {
                    catLabels = labels.map(function(i) {
                        return w.config.xaxis.labels.formatter(i - w.globals.minX + 1);
                    });
                }
                return catLabels;
            }
        },
        {
            // maxValsInArrayIndex is the index of series[] which has the largest number of items
            key: "getLargestSeries",
            value: function getLargestSeries() {
                var w = this.w;
                w.globals.maxValsInArrayIndex = w.seriesData.series.map(function(a) {
                    return a.length;
                }).indexOf(Math.max.apply(Math, /**
         * @param {number[]} a
         */ w.seriesData.series.map(function(a) {
                    return a.length;
                })));
            }
        },
        {
            key: "getLargestMarkerSize",
            value: function getLargestMarkerSize() {
                var w = this.w;
                var size = 0;
                w.globals.markers.size.forEach(function(m) {
                    size = Math.max(size, m);
                });
                if (w.config.markers.discrete && w.config.markers.discrete.length) {
                    w.config.markers.discrete.forEach(function(m) {
                        size = Math.max(size, m.size);
                    });
                }
                if (size > 0) {
                    if (w.config.markers.hover.size > 0) {
                        size = w.config.markers.hover.size;
                    } else {
                        size += w.config.markers.hover.sizeOffset;
                    }
                }
                w.globals.markers.largestSize = size;
                return size;
            }
        },
        {
            /**
   * @memberof Core
   * returns the sum of all values in a series
   * Eg. w.seriesData.series = [[32,33,43,12], [2,3,5,1]]
   *  @return [120, 11]
   **/ key: "getSeriesTotals",
            value: function getSeriesTotals() {
                var w = this.w;
                w.globals.seriesTotals = w.seriesData.series.map(function(ser) {
                    var total = 0;
                    if (Array.isArray(ser)) {
                        for(var j = 0; j < ser.length; j++){
                            total += ser[j];
                        }
                    } else {
                        total += ser;
                    }
                    return total;
                });
            }
        },
        {
            /**
   * @param {number} minX
   * @param {number} maxX
   */ key: "getSeriesTotalsXRange",
            value: function getSeriesTotalsXRange(minX, maxX) {
                var w = this.w;
                var seriesTotalsXRange = w.seriesData.series.map(function(ser, index) {
                    var total = 0;
                    for(var j = 0; j < ser.length; j++){
                        if (w.seriesData.seriesX[index][j] > minX && w.seriesData.seriesX[index][j] < maxX) {
                            total += ser[j];
                        }
                    }
                    return total;
                });
                return seriesTotalsXRange;
            }
        },
        {
            /**
   * @memberof CoreUtils
   * returns the percentage value of all individual values which can be used in a 100% stacked series
   * Eg. w.seriesData.series = [[32, 33, 43, 12], [2, 3, 5, 1]]
   *  @return [[94.11, 91.66, 89.58, 92.30], [5.88, 8.33, 10.41, 7.7]]
   **/ key: "getPercentSeries",
            value: function getPercentSeries() {
                var w = this.w;
                w.globals.seriesPercent = w.seriesData.series.map(function(ser) {
                    var seriesPercent = [];
                    if (Array.isArray(ser)) {
                        for(var j = 0; j < ser.length; j++){
                            var total = w.seriesData.stackedSeriesTotals[j];
                            var percent = 0;
                            if (total) {
                                percent = 100 * ser[j] / total;
                            }
                            seriesPercent.push(percent);
                        }
                    } else {
                        var total1 = w.globals.seriesTotals.reduce(function(acc, val) {
                            return acc + val;
                        }, 0);
                        var percent1 = 100 * ser / total1;
                        seriesPercent.push(percent1);
                    }
                    return seriesPercent;
                });
            }
        },
        {
            key: "getCalculatedRatios",
            value: function getCalculatedRatios() {
                var _this = this;
                var w = this.w;
                var gl = w.globals;
                var yRatio = [];
                var invertedYRatio = 0;
                var xRatio = 0;
                var invertedXRatio = 0;
                var zRatio = 0;
                var baseLineY = [];
                var baseLineInvertedY = 0.1;
                var baseLineX = 0;
                gl.yRange = [];
                if (gl.isMultipleYAxis) {
                    for(var i = 0; i < gl.minYArr.length; i++){
                        gl.yRange.push(Math.abs(gl.minYArr[i] - gl.maxYArr[i]));
                        baseLineY.push(0);
                    }
                } else {
                    gl.yRange.push(Math.abs(gl.minY - gl.maxY));
                }
                gl.xRange = Math.abs(gl.maxX - gl.minX);
                gl.zRange = Math.abs(gl.maxZ - gl.minZ);
                for(var i1 = 0; i1 < gl.yRange.length; i1++){
                    yRatio.push(gl.yRange[i1] / this.w.layout.gridHeight);
                }
                xRatio = gl.xRange / this.w.layout.gridWidth;
                invertedYRatio = /** @type {any} */ gl.yRange / this.w.layout.gridWidth;
                invertedXRatio = gl.xRange / this.w.layout.gridHeight;
                zRatio = gl.zRange / this.w.layout.gridHeight * 16;
                if (!zRatio) {
                    zRatio = 1;
                }
                if (gl.minY !== Number.MIN_VALUE && Math.abs(gl.minY) !== 0) {
                    var _hasNegsGl = /** @type {any} */ gl;
                    _hasNegsGl.hasNegs = true;
                }
                if (w.globals.seriesYAxisReverseMap.length > 0) {
                    var scaleBaseLineYScale = function scaleBaseLineYScale(y, i) {
                        var yAxis = w.config.yaxis[w.globals.seriesYAxisReverseMap[i]];
                        if (!yAxis) return 0;
                        var sign = y < 0 ? -1 : 1;
                        y = Math.abs(y);
                        if (yAxis.logarithmic) {
                            y = _this.getBaseLog(yAxis.logBase, y);
                        }
                        return -sign * y / yRatio[i];
                    };
                    if (gl.isMultipleYAxis) {
                        baseLineY = [];
                        for(var i2 = 0; i2 < yRatio.length; i2++){
                            baseLineY.push(scaleBaseLineYScale(gl.minYArr[i2], i2));
                        }
                    } else {
                        baseLineY = [];
                        baseLineY.push(scaleBaseLineYScale(gl.minY, 0));
                        if (gl.minY !== Number.MIN_VALUE && Math.abs(gl.minY) !== 0) {
                            baseLineInvertedY = -gl.minY / invertedYRatio;
                            baseLineX = gl.minX / xRatio;
                        }
                    }
                } else {
                    baseLineY = [];
                    baseLineY.push(0);
                    baseLineInvertedY = 0;
                    baseLineX = 0;
                }
                return {
                    yRatio: yRatio,
                    invertedYRatio: invertedYRatio,
                    zRatio: zRatio,
                    xRatio: xRatio,
                    invertedXRatio: invertedXRatio,
                    baseLineInvertedY: baseLineInvertedY,
                    baseLineY: baseLineY,
                    baseLineX: baseLineX
                };
            }
        },
        {
            /**
   * @param {any[]} series
   */ key: "getLogSeries",
            value: function getLogSeries(series) {
                var _this = this;
                var w = this.w;
                w.globals.seriesLog = series.map(function(s, i) {
                    var yAxisIndex = w.globals.seriesYAxisReverseMap[i];
                    if (w.config.yaxis[yAxisIndex] && w.config.yaxis[yAxisIndex].logarithmic) {
                        return s.map(function(d) {
                            if (d === null) return null;
                            return _this.getLogVal(w.config.yaxis[yAxisIndex].logBase, d, i);
                        });
                    } else {
                        return s;
                    }
                });
                return w.globals.invalidLogScale ? series : w.globals.seriesLog;
            }
        },
        {
            /**
   * @param {number} val
   * @param {number} seriesIndex
   * @returns {number}
   */ key: "getLogValAtSeriesIndex",
            value: function getLogValAtSeriesIndex(val, seriesIndex) {
                if (val === null) return /** @type {any} */ null;
                var w = this.w;
                var yAxisIndex = w.globals.seriesYAxisReverseMap[seriesIndex];
                if (w.config.yaxis[yAxisIndex] && w.config.yaxis[yAxisIndex].logarithmic) {
                    return this.getLogVal(w.config.yaxis[yAxisIndex].logBase, val, seriesIndex);
                }
                return val;
            }
        },
        {
            /**
   * @param {number} base
   * @param {number} value
   */ key: "getBaseLog",
            value: function getBaseLog(base, value) {
                return Math.log(value) / Math.log(base);
            }
        },
        {
            /**
   * @param {number} b
   * @param {number} d
   * @param {number} seriesIndex
   */ key: "getLogVal",
            value: function getLogVal(b, d, seriesIndex) {
                if (d <= 0) {
                    return 0;
                }
                var w = this.w;
                var min_log_val = w.globals.minYArr[seriesIndex] === 0 ? -1 : this.getBaseLog(b, w.globals.minYArr[seriesIndex]);
                var max_log_val = w.globals.maxYArr[seriesIndex] === 0 ? 0 : this.getBaseLog(b, w.globals.maxYArr[seriesIndex]);
                var number_of_height_levels = max_log_val - min_log_val;
                if (d < 1) return d / number_of_height_levels;
                var log_height_value = this.getBaseLog(b, d) - min_log_val;
                return log_height_value / number_of_height_levels;
            }
        },
        {
            /**
   * @param {number[]} yRatio
   */ key: "getLogYRatios",
            value: function getLogYRatios(yRatio) {
                var _this = this;
                var w = this.w;
                var gl = this.w.globals;
                var _gl = /** @type {any} */ gl;
                _gl.yLogRatio = yRatio.slice();
                _gl.logYRange = /** @type {any[]} */ gl.yRange.map(function(_, i) {
                    var yAxisIndex = w.globals.seriesYAxisReverseMap[i];
                    if (w.config.yaxis[yAxisIndex] && _this.w.config.yaxis[yAxisIndex].logarithmic) {
                        var maxY = -Number.MAX_VALUE;
                        var minY = Number.MIN_VALUE;
                        var range = 1;
                        gl.seriesLog.forEach(function(s, si) {
                            s.forEach(function(v) {
                                if (w.config.yaxis[si] && w.config.yaxis[si].logarithmic) {
                                    maxY = Math.max(v, maxY);
                                    minY = Math.min(v, minY);
                                }
                            });
                        });
                        range = Math.pow(gl.yRange[i], Math.abs(minY - maxY) / gl.yRange[i]);
                        _gl.yLogRatio[i] = range / _this.w.layout.gridHeight;
                        return range;
                    }
                });
                return _gl.invalidLogScale ? yRatio.slice() : _gl.yLogRatio;
            }
        },
        {
            // Series of the same group and type can be stacked together distinct from
            // other series of the same type on the same axis.
            /**
   * @param {Record<string, any>} typeSeries
   * @param {string[]} typeGroups
   * @param {string} type
   * @param {string} chartClass
   */ key: "drawSeriesByGroup",
            value: function drawSeriesByGroup(typeSeries, typeGroups, type, chartClass) {
                var w = this.w;
                var graph = [];
                if (typeSeries.series.length > 0) {
                    typeGroups.forEach(function(gn) {
                        var gs = [];
                        var gi = [];
                        typeSeries.i.forEach(function(i, ii) {
                            if (/** @type {Record<string,any>} */ w.config.series[i].group === gn) {
                                gs.push(typeSeries.series[ii]);
                                gi.push(i);
                            }
                        });
                        gs.length > 0 && graph.push(/** @type {any} */ chartClass.draw(gs, type, gi));
                    });
                }
                return graph;
            }
        }
    ], [
        {
            key: "checkComboSeries",
            value: /**
   * @param {any[]} series
   * @param {string} chartType
   */ function checkComboSeries(series, chartType) {
                var comboCharts = false;
                var comboBarCount = 0;
                var comboCount = 0;
                if (chartType === void 0) {
                    chartType = "line";
                }
                if (series.length && typeof series[0].type !== "undefined") {
                    series.forEach(function(s) {
                        if (s.type === "bar" || s.type === "column" || s.type === "candlestick" || s.type === "boxPlot" || s.type === "violin") {
                            comboBarCount++;
                        }
                        if (typeof s.type !== "undefined" && s.type !== chartType) {
                            comboCount++;
                        }
                    });
                }
                if (comboCount > 0) {
                    comboCharts = true;
                }
                return {
                    comboBarCount: comboBarCount,
                    comboCharts: comboCharts
                };
            }
        },
        {
            key: "extendArrayProps",
            value: // Some config objects can be array - and we need to extend them correctly
            /**
   * @param {any} configInstance
   * @param {Record<string, any>} options
   * @param {import('../types/internal').ChartStateW} w
   */ function extendArrayProps(configInstance, options2, w) {
                var _a, _b;
                if (options2 == null ? void 0 : options2.yaxis) {
                    options2 = configInstance.extendYAxis(options2, w);
                }
                if (options2 == null ? void 0 : options2.annotations) {
                    if (options2.annotations.yaxis) {
                        options2 = configInstance.extendYAxisAnnotations(options2);
                    }
                    if ((_a = options2 == null ? void 0 : options2.annotations) == null ? void 0 : _a.xaxis) {
                        options2 = configInstance.extendXAxisAnnotations(options2);
                    }
                    if ((_b = options2 == null ? void 0 : options2.annotations) == null ? void 0 : _b.points) {
                        options2 = configInstance.extendPointAnnotations(options2);
                    }
                }
                return options2;
            }
        }
    ]);
    return CoreUtils;
}();
var SVGNS$2 = "http://www.w3.org/2000/svg";
var Point = /*#__PURE__*/ function() {
    "use strict";
    function Point(x, y) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Point);
        if ((typeof x === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(x)) === "object") {
            this.x = x.x;
            this.y = x.y;
        } else {
            this.x = x || 0;
            this.y = y || 0;
        }
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Point, [
        {
            /**
   * @param {Matrix} matrix
   */ key: "transform",
            value: function transform(matrix) {
                return matrix.apply(this);
            }
        },
        {
            key: "clone",
            value: function clone() {
                return new Point(this.x, this.y);
            }
        }
    ]);
    return Point;
}();
var Matrix = /*#__PURE__*/ function() {
    "use strict";
    function Matrix(a, b, c, d, e, f) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Matrix);
        this.a = a != null ? a : 1;
        this.b = b != null ? b : 0;
        this.c = c != null ? c : 0;
        this.d = d != null ? d : 1;
        this.e = e != null ? e : 0;
        this.f = f != null ? f : 0;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Matrix, [
        {
            /**
   * @param {number} deg
   */ key: "rotate",
            value: function rotate(deg) {
                var rad = deg * Math.PI / 180;
                var cos = Math.cos(rad);
                var sin = Math.sin(rad);
                return this.multiply(new Matrix(cos, sin, -sin, cos, 0, 0));
            }
        },
        {
            /**
   * @param {number} sx
   * @param {number} sy
   */ key: "scale",
            value: function scale(sx, sy) {
                return this.multiply(new Matrix(sx, 0, 0, sy != null ? sy : sx, 0, 0));
            }
        },
        {
            /**
   * @param {Matrix} m
   */ key: "multiply",
            value: function multiply(m) {
                return new Matrix(this.a * m.a + this.c * m.b, this.b * m.a + this.d * m.b, this.a * m.c + this.c * m.d, this.b * m.c + this.d * m.d, this.a * m.e + this.c * m.f + this.e, this.b * m.e + this.d * m.f + this.f);
            }
        },
        {
            /**
   * @param {Point} point
   */ key: "apply",
            value: function apply(point) {
                return new Point(this.a * point.x + this.c * point.y + this.e, this.b * point.x + this.d * point.y + this.f);
            }
        }
    ]);
    return Matrix;
}();
var Box = function Box(x, y, w, h) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Box);
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
    this.width = w;
    this.height = h;
    this.x2 = x + w;
    this.y2 = y + h;
};
/*!
 * Path morphing for SVG path animations
 * Based on svg.pathmorphing.js by Ulrich-Matthias Schäfer (MIT License)
 * Refactored to be standalone (no SVG.js dependency)
 *
 * Two algorithms are exported:
 *   - morphPaths()    — command-level interpolation; preserves curves but can
 *                       produce "wings/flips" when two shapes have very
 *                       different topology (e.g. bar rect → pie arc).
 *   - morphPolygons() — resamples both shapes into N evenly-spaced perimeter
 *                       points and tweens point-by-point with rotation-search
 *                       alignment; always smooth and non-self-intersecting,
 *                       at the cost of throwing away curve smoothness.
 */ function parsePath(d) {
    if (!d || typeof d !== "string") return [
        [
            "M",
            0,
            0
        ]
    ];
    var commands = [];
    var re = /([MmLlHhVvCcSsQqTtAaZz])\s*/g;
    var numRe = /[+-]?(?:\d+\.?\d*|\.\d+)(?:e[+-]?\d+)?/gi;
    var match;
    var letters = [];
    var positions = [];
    while((match = re.exec(d)) !== null){
        letters.push(match[1]);
        positions.push(match.index);
    }
    for(var i = 0; i < letters.length; i++){
        var start = positions[i] + letters[i].length;
        var end = i + 1 < positions.length ? positions[i + 1] : d.length;
        var paramStr = d.substring(start, end);
        var nums = [];
        var numMatch = void 0;
        numRe.lastIndex = 0;
        while((numMatch = numRe.exec(paramStr)) !== null){
            nums.push(parseFloat(numMatch[0]));
        }
        var cmd = letters[i].toUpperCase();
        if (cmd === "Z") {
            commands.push([
                "Z"
            ]);
        } else if (cmd === "M" || cmd === "L" || cmd === "T") {
            for(var j = 0; j < nums.length; j += 2){
                commands.push([
                    cmd,
                    nums[j],
                    nums[j + 1]
                ]);
            }
        } else if (cmd === "H") {
            for(var j1 = 0; j1 < nums.length; j1++){
                commands.push([
                    cmd,
                    nums[j1]
                ]);
            }
        } else if (cmd === "V") {
            for(var j2 = 0; j2 < nums.length; j2++){
                commands.push([
                    cmd,
                    nums[j2]
                ]);
            }
        } else if (cmd === "C") {
            for(var j3 = 0; j3 < nums.length; j3 += 6){
                commands.push([
                    cmd,
                    nums[j3],
                    nums[j3 + 1],
                    nums[j3 + 2],
                    nums[j3 + 3],
                    nums[j3 + 4],
                    nums[j3 + 5]
                ]);
            }
        } else if (cmd === "S" || cmd === "Q") {
            for(var j4 = 0; j4 < nums.length; j4 += 4){
                commands.push([
                    cmd,
                    nums[j4],
                    nums[j4 + 1],
                    nums[j4 + 2],
                    nums[j4 + 3]
                ]);
            }
        } else if (cmd === "A") {
            for(var j5 = 0; j5 < nums.length; j5 += 7){
                commands.push([
                    cmd,
                    nums[j5],
                    nums[j5 + 1],
                    nums[j5 + 2],
                    nums[j5 + 3],
                    nums[j5 + 4],
                    nums[j5 + 5],
                    nums[j5 + 6]
                ]);
            }
        }
    }
    if (commands.length === 0) {
        commands.push([
            "M",
            0,
            0
        ]);
    }
    return commands;
}
function pathBbox(arr) {
    var minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    arr.forEach(function(cmd) {
        for(var i = 1; i < cmd.length; i += 2){
            if (i + 1 <= cmd.length) {
                var x = cmd[i];
                var y = cmd[i + 1];
                if (typeof x === "number" && typeof y === "number") {
                    if (x < minX) minX = x;
                    if (x > maxX) maxX = x;
                    if (y < minY) minY = y;
                    if (y > maxY) maxY = y;
                }
            }
        }
    });
    if (minX === Infinity) {
        return {
            x: 0,
            y: 0,
            width: 0,
            height: 0
        };
    }
    return {
        x: minX,
        y: minY,
        width: maxX - minX,
        height: maxY - minY
    };
}
function arrayToPath(arr) {
    return arr.map(function(cmd) {
        return cmd.join(" ");
    }).join(" ");
}
function simplify(val) {
    switch(val[0]){
        case "z":
        case "Z":
            val[0] = "L";
            val[1] = this.start[0];
            val[2] = this.start[1];
            break;
        case "H":
            val[0] = "L";
            val[2] = this.pos[1];
            break;
        case "V":
            val[0] = "L";
            val[2] = val[1];
            val[1] = this.pos[0];
            break;
        case "T":
            val[0] = "Q";
            val[3] = val[1];
            val[4] = val[2];
            val[1] = this.reflection[1];
            val[2] = this.reflection[0];
            break;
        case "S":
            val[0] = "C";
            val[6] = val[4];
            val[5] = val[3];
            val[4] = val[2];
            val[3] = val[1];
            val[2] = this.reflection[1];
            val[1] = this.reflection[0];
            break;
    }
    return val;
}
function setPosAndReflection(val) {
    var len = val.length;
    this.pos = [
        val[len - 2],
        val[len - 1]
    ];
    if ("SCQT".indexOf(val[0]) != -1) {
        this.reflection = [
            2 * this.pos[0] - val[len - 4],
            2 * this.pos[1] - val[len - 3]
        ];
    }
    return val;
}
function toBezier(val) {
    var _a;
    var retVal = [
        val
    ];
    switch(val[0]){
        case "M":
            this.pos = this.start = [
                val[1],
                val[2]
            ];
            return retVal;
        case "L":
            val[5] = val[3] = val[1];
            val[6] = val[4] = val[2];
            val[1] = this.pos[0];
            val[2] = this.pos[1];
            break;
        case "Q":
            val[6] = val[4];
            val[5] = val[3];
            val[4] = val[4] * 1 / 3 + val[2] * 2 / 3;
            val[3] = val[3] * 1 / 3 + val[1] * 2 / 3;
            val[2] = this.pos[1] * 1 / 3 + val[2] * 2 / 3;
            val[1] = this.pos[0] * 1 / 3 + val[1] * 2 / 3;
            break;
        case "A":
            retVal = arcToBezier((_a = this.pos) != null ? _a : [], val);
            val = retVal[0];
            break;
    }
    val[0] = "C";
    this.pos = [
        val[5],
        val[6]
    ];
    this.reflection = [
        2 * val[5] - val[3],
        2 * val[6] - val[4]
    ];
    return retVal;
}
function findNextM(arr, offset) {
    if (offset === false) return false;
    for(var i = offset, len = arr.length; i < len; ++i){
        if (arr[i][0] == "M") return i;
    }
    return false;
}
function arcToBezier(pos, val) {
    var rx = Math.abs(val[1]), ry = Math.abs(val[2]), xAxisRotation = val[3] % 360, largeArcFlag = val[4], sweepFlag = val[5], x = val[6], y = val[7], A = new Point(pos[0], pos[1]), B = new Point(x, y), primedCoord, lambda, mat, k, c, cSquare, t, O, OA, OB, tetaStart, tetaEnd, deltaTeta, nbSectors, f, arcSegPoints, angle, sinAngle, cosAngle, pt, i, il, retVal = [], x1, y1, x2, y2;
    if (rx === 0 || ry === 0 || A.x === B.x && A.y === B.y) {
        return [
            [
                "C",
                A.x,
                A.y,
                B.x,
                B.y,
                B.x,
                B.y
            ]
        ];
    }
    primedCoord = new Point((A.x - B.x) / 2, (A.y - B.y) / 2).transform(// Start with the identity matrix (no args → Matrix defaults a=d=1, others 0).
    // Passing all-zero args here would produce a degenerate zero matrix, since
    // `0 ?? 1` is `0`, not `1` — every subsequent transform then yields (0,0)
    // and the arc-to-bezier conversion crashes on a NaN cascade.
    /** @type {any} */ new Matrix().rotate(xAxisRotation));
    lambda = primedCoord.x * primedCoord.x / (rx * rx) + primedCoord.y * primedCoord.y / (ry * ry);
    if (lambda > 1) {
        lambda = Math.sqrt(lambda);
        rx = lambda * rx;
        ry = lambda * ry;
    }
    mat = /** @type {any} */ new Matrix().rotate(xAxisRotation).scale(1 / rx, 1 / ry).rotate(-xAxisRotation);
    A = A.transform(mat);
    B = B.transform(mat);
    k = [
        B.x - A.x,
        B.y - A.y
    ];
    cSquare = k[0] * k[0] + k[1] * k[1];
    c = Math.sqrt(cSquare);
    k[0] /= c;
    k[1] /= c;
    t = cSquare < 4 ? Math.sqrt(1 - cSquare / 4) : 0;
    if (largeArcFlag === sweepFlag) {
        t *= -1;
    }
    O = new Point((B.x + A.x) / 2 + t * -k[1], (B.y + A.y) / 2 + t * k[0]);
    OA = new Point(A.x - O.x, A.y - O.y);
    OB = new Point(B.x - O.x, B.y - O.y);
    tetaStart = Math.acos(OA.x / Math.sqrt(OA.x * OA.x + OA.y * OA.y));
    if (OA.y < 0) tetaStart *= -1;
    tetaEnd = Math.acos(OB.x / Math.sqrt(OB.x * OB.x + OB.y * OB.y));
    if (OB.y < 0) tetaEnd *= -1;
    if (sweepFlag && tetaStart > tetaEnd) {
        tetaEnd += 2 * Math.PI;
    }
    if (!sweepFlag && tetaStart < tetaEnd) {
        tetaEnd -= 2 * Math.PI;
    }
    nbSectors = Math.ceil(Math.abs(tetaStart - tetaEnd) * 2 / Math.PI);
    arcSegPoints = [];
    angle = tetaStart;
    deltaTeta = (tetaEnd - tetaStart) / nbSectors;
    f = 4 * Math.tan(deltaTeta / 4) / 3;
    for(i = 0; i <= nbSectors; i++){
        cosAngle = Math.cos(angle);
        sinAngle = Math.sin(angle);
        pt = new Point(O.x + cosAngle, O.y + sinAngle);
        arcSegPoints[i] = [
            new Point(pt.x + f * sinAngle, pt.y - f * cosAngle),
            pt,
            new Point(pt.x - f * sinAngle, pt.y + f * cosAngle)
        ];
        angle += deltaTeta;
    }
    arcSegPoints[0][0] = arcSegPoints[0][1].clone();
    arcSegPoints[arcSegPoints.length - 1][2] = arcSegPoints[arcSegPoints.length - 1][1].clone();
    mat = /** @type {any} */ new Matrix().rotate(xAxisRotation).scale(rx, ry).rotate(-xAxisRotation);
    for(i = 0, il = arcSegPoints.length; i < il; i++){
        arcSegPoints[i][0] = arcSegPoints[i][0].transform(mat);
        arcSegPoints[i][1] = arcSegPoints[i][1].transform(mat);
        arcSegPoints[i][2] = arcSegPoints[i][2].transform(mat);
    }
    for(i = 1, il = arcSegPoints.length; i < il; i++){
        pt = arcSegPoints[i - 1][2];
        x1 = pt.x;
        y1 = pt.y;
        pt = arcSegPoints[i][0];
        x2 = pt.x;
        y2 = pt.y;
        pt = arcSegPoints[i][1];
        x = pt.x;
        y = pt.y;
        retVal.push([
            "C",
            x1,
            y1,
            x2,
            y2,
            x,
            y
        ]);
    }
    return retVal;
}
function handleBlock(startArr, startOffsetM, startOffsetNextM, destArr, destOffsetM, destOffsetNextM) {
    var startArrTemp = startArr.slice(startOffsetM, startOffsetNextM || void 0);
    var destArrTemp = destArr.slice(destOffsetM, destOffsetNextM || void 0);
    var i = 0, posStart = {
        pos: [
            0,
            0
        ],
        start: [
            0,
            0
        ]
    }, posDest = {
        pos: [
            0,
            0
        ],
        start: [
            0,
            0
        ]
    };
    while(true){
        startArrTemp[i] = simplify.call(posStart, startArrTemp[i]);
        destArrTemp[i] = simplify.call(posDest, destArrTemp[i]);
        if (startArrTemp[i][0] != destArrTemp[i][0] || startArrTemp[i][0] == "M" || startArrTemp[i][0] == "A" && (startArrTemp[i][4] != destArrTemp[i][4] || startArrTemp[i][5] != destArrTemp[i][5])) {
            Array.prototype.splice.apply(startArrTemp, /** @type {[number, number, ...any[]]} */ [
                i,
                1
            ].concat(/** @type {any} */ toBezier.call(posStart, startArrTemp[i])));
            Array.prototype.splice.apply(destArrTemp, /** @type {[number, number, ...any[]]} */ [
                i,
                1
            ].concat(/** @type {any} */ toBezier.call(posDest, destArrTemp[i])));
        } else {
            startArrTemp[i] = /** @type {any} */ setPosAndReflection.call(posStart, startArrTemp[i]);
            destArrTemp[i] = /** @type {any} */ setPosAndReflection.call(posDest, destArrTemp[i]);
        }
        if (++i == startArrTemp.length && i == destArrTemp.length) break;
        if (i == startArrTemp.length) {
            startArrTemp.push([
                "C",
                posStart.pos[0],
                posStart.pos[1],
                posStart.pos[0],
                posStart.pos[1],
                posStart.pos[0],
                posStart.pos[1]
            ]);
        }
        if (i == destArrTemp.length) {
            destArrTemp.push([
                "C",
                posDest.pos[0],
                posDest.pos[1],
                posDest.pos[0],
                posDest.pos[1],
                posDest.pos[0],
                posDest.pos[1]
            ]);
        }
    }
    return {
        start: startArrTemp,
        dest: destArrTemp
    };
}
function synchronizePaths(fromD, toD) {
    var startArr = parsePath(fromD);
    var destArr = parsePath(toD);
    var startOffsetM = 0;
    var destOffsetM = 0;
    var startOffsetNextM = false;
    var destOffsetNextM = false;
    var result;
    while(true){
        if (startOffsetM === false && destOffsetM === false) break;
        startOffsetNextM = findNextM(startArr, startOffsetM === false ? false : startOffsetM + 1);
        destOffsetNextM = findNextM(destArr, destOffsetM === false ? false : destOffsetM + 1);
        if (startOffsetM === false) {
            var bbox = pathBbox(/** @type {any} */ result.start);
            if (bbox.height == 0 || bbox.width == 0) {
                startOffsetM = startArr.push(startArr[0]) - 1;
            } else {
                startOffsetM = startArr.push([
                    "M",
                    bbox.x + bbox.width / 2,
                    bbox.y + bbox.height / 2
                ]) - 1;
            }
        }
        if (destOffsetM === false) {
            var bbox1 = pathBbox(/** @type {any} */ result.dest);
            if (bbox1.height == 0 || bbox1.width == 0) {
                destOffsetM = destArr.push(destArr[0]) - 1;
            } else {
                destOffsetM = destArr.push([
                    "M",
                    bbox1.x + bbox1.width / 2,
                    bbox1.y + bbox1.height / 2
                ]) - 1;
            }
        }
        result = handleBlock(startArr, startOffsetM, startOffsetNextM, destArr, destOffsetM, destOffsetNextM);
        startArr = startArr.slice(0, startOffsetM).concat(result.start, startOffsetNextM === false ? [] : startArr.slice(startOffsetNextM));
        destArr = destArr.slice(0, destOffsetM).concat(result.dest, destOffsetNextM === false ? [] : destArr.slice(destOffsetNextM));
        startOffsetM = startOffsetNextM === false ? false : startOffsetM + result.start.length;
        destOffsetM = destOffsetNextM === false ? false : destOffsetM + result.dest.length;
    }
    return {
        start: startArr,
        dest: destArr
    };
}
function morphPaths(fromD, toD) {
    var synced = synchronizePaths(fromD, toD);
    var startArr = synced.start;
    var destArr = synced.dest;
    return function(pos) {
        var result = startArr.map(function(from, idx) {
            return destArr[idx].map(function(to, toIdx) {
                if (toIdx === 0) return to;
                return from[toIdx] + (destArr[idx][toIdx] - from[toIdx]) * pos;
            });
        });
        return arrayToPath(result);
    };
}
var _measureSvg = null;
var _measurePath = null;
function samplePathPoints(d, n) {
    var pts = new Array(n);
    if (!Environment.isBrowser()) {
        var arr = parsePath(d);
        var bbox = pathBbox(arr);
        var cx = bbox.x + bbox.width / 2;
        var cy = bbox.y + bbox.height / 2;
        for(var i = 0; i < n; i++)pts[i] = {
            x: cx,
            y: cy
        };
        return pts;
    }
    if (!_measureSvg) {
        _measureSvg = /** @type {SVGSVGElement} */ document.createElementNS("http://www.w3.org/2000/svg", "svg");
        _measureSvg.setAttribute("width", "0");
        _measureSvg.setAttribute("height", "0");
        _measureSvg.setAttribute("style", "position:absolute;width:0;height:0;visibility:hidden;pointer-events:none;");
        _measurePath = /** @type {SVGPathElement} */ document.createElementNS("http://www.w3.org/2000/svg", "path");
        _measureSvg.appendChild(_measurePath);
        document.body.appendChild(_measureSvg);
    }
    _measurePath.setAttribute("d", d || "M0 0");
    var len = 0;
    try {
        len = _measurePath.getTotalLength();
    } catch (e) {
        len = 0;
    }
    if (!len || !isFinite(len)) {
        var arr1 = parsePath(d);
        var bbox1 = pathBbox(arr1);
        var cx1 = bbox1.x + bbox1.width / 2;
        var cy1 = bbox1.y + bbox1.height / 2;
        for(var i1 = 0; i1 < n; i1++)pts[i1] = {
            x: cx1,
            y: cy1
        };
        return pts;
    }
    for(var i2 = 0; i2 < n; i2++){
        try {
            var p = _measurePath.getPointAtLength(i2 / n * len);
            pts[i2] = {
                x: p.x,
                y: p.y
            };
        } catch (e) {
            pts[i2] = {
                x: 0,
                y: 0
            };
        }
    }
    return pts;
}
function morphPolygons(fromD, toD) {
    var n = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 96;
    var fromPts = samplePathPoints(fromD, n);
    var toPts = samplePathPoints(toD, n);
    var bestOffset = 0;
    var bestDist = Infinity;
    for(var off = 0; off < n; off++){
        var dist = 0;
        for(var i = 0; i < n; i++){
            var a = fromPts[(i + off) % n];
            var b = toPts[i];
            var dx = a.x - b.x;
            var dy = a.y - b.y;
            dist += dx * dx + dy * dy;
            if (dist >= bestDist) break;
        }
        if (dist < bestDist) {
            bestDist = dist;
            bestOffset = off;
        }
    }
    var aligned = new Array(n);
    for(var i1 = 0; i1 < n; i1++){
        aligned[i1] = fromPts[(i1 + bestOffset) % n];
    }
    return function(pos) {
        var out = "";
        for(var i = 0; i < n; i++){
            var a = aligned[i];
            var b = toPts[i];
            var x = a.x + (b.x - a.x) * pos;
            var y = a.y + (b.y - a.y) * pos;
            out += (i === 0 ? "M" : "L") + x.toFixed(3) + " " + y.toFixed(3) + " ";
        }
        return out + "Z";
    };
}
function easeInOut(t) {
    return -Math.cos(t * Math.PI) / 2 + 0.5;
}
var _defaultEasing = easeInOut;
function setDefaultEasing(fn) {
    _defaultEasing = typeof fn === "function" ? fn : easeInOut;
}
function parseColor(str) {
    if (!str || typeof str !== "string") return null;
    if (str[0] === "#") {
        var hex = str.slice(1);
        if (hex.length === 3) hex = hex[0] + hex[0] + hex[1] + hex[1] + hex[2] + hex[2];
        var n = parseInt(hex, 16);
        return [
            n >> 16 & 255,
            n >> 8 & 255,
            n & 255,
            1
        ];
    }
    var m = str.match(/rgba?\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*(?:,\s*([\d.]+))?\s*\)/);
    if (m) return [
        +m[1],
        +m[2],
        +m[3],
        m[4] !== void 0 ? +m[4] : 1
    ];
    return null;
}
function interpolateColor(from, to, pos) {
    return "rgba(".concat(Math.round(from[0] + (to[0] - from[0]) * pos), ",").concat(Math.round(from[1] + (to[1] - from[1]) * pos), ",").concat(Math.round(from[2] + (to[2] - from[2]) * pos), ",").concat(from[3] + (to[3] - from[3]) * pos, ")");
}
var SVGAnimationRunner = /*#__PURE__*/ function() {
    "use strict";
    function SVGAnimationRunner(element, duration, delay) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGAnimationRunner);
        this.el = element;
        this.duration = duration != null ? duration : 300;
        this.delay = delay || 0;
        this._attrTarget = null;
        this._plotTarget = null;
        this._plotSnap = null;
        this._plotAlgorithm = "commands";
        this._afterCb = null;
        this._duringCb = null;
        this._easing = null;
        this._next = null;
        this._root = null;
        this._scheduled = false;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGAnimationRunner, [
        {
            /**
   * Override the easing for this runner (else the module default is used).
   * @param {(t:number)=>number} fn
   */ key: "ease",
            value: function ease(fn) {
                if (typeof fn === "function") this._easing = fn;
                this._schedule();
                return this;
            }
        },
        {
            /**
   * @param {Record<string, any>} to
   */ key: "attr",
            value: function attr(to) {
                this._attrTarget = to;
                this._schedule();
                return this;
            }
        },
        {
            /**
   * @param {string} d
   * @param {'commands' | 'polygons'} [algorithm] - morph engine to use for
   *   the d→d interpolation. 'commands' (default) is the legacy
   *   per-command lerp; 'polygons' resamples both paths into N evenly
   *   spaced points and tweens point-by-point (smoother for shapes with
   *   very different anchor-point counts).
   * @param {string} [snapTo] - final d to land on when it differs from the
   *   interpolation target. Used by reconciled length-change morphs: the
   *   tween runs against a padded path (extra anchors lying exactly on the
   *   final geometry) but the element must end with the renderer's clean,
   *   un-padded d so later captures and morphs stay stable.
   */ key: "plot",
            value: function plot(d, algorithm, snapTo) {
                this._plotTarget = d;
                if (algorithm) this._plotAlgorithm = algorithm;
                this._plotSnap = snapTo || null;
                this._schedule();
                return this;
            }
        },
        {
            /**
   * @param {Function} fn
   */ key: "after",
            value: function after(fn) {
                this._afterCb = fn;
                this._schedule();
                return this;
            }
        },
        {
            /**
   * @param {Function} fn
   */ key: "during",
            value: function during(fn) {
                this._duringCb = fn;
                this._schedule();
                return this;
            }
        },
        {
            /**
   * @param {number} duration
   * @param {number} delay
   */ key: "animate",
            value: function animate(duration, delay) {
                var next = new SVGAnimationRunner(this.el, duration, delay);
                this._next = next;
                next._root = this._root || this;
                return next;
            }
        },
        {
            key: "_schedule",
            value: function _schedule() {
                var root = this._root || this;
                if (!root._scheduled) {
                    root._scheduled = true;
                    queueMicrotask(function() {
                        return root._executeChain();
                    });
                }
            }
        },
        {
            key: "_executeChain",
            value: function _executeChain() {
                var chain = [];
                var r = this;
                while(r){
                    chain.push(r);
                    r = r._next;
                }
                var cumulativeDelay = 0;
                chain.forEach(function(runner) {
                    cumulativeDelay += runner.delay;
                    runner._execute(cumulativeDelay);
                    cumulativeDelay += runner.duration;
                });
            }
        },
        {
            /**
   * @param {number} startDelay
   */ key: "_execute",
            value: function _execute(startDelay) {
                var _this = this;
                var el = this.el;
                var duration = this.duration;
                if (duration <= 1) {
                    var apply = function apply() {
                        if (_this._attrTarget) el.attr(_this._attrTarget);
                        if (_this._plotTarget) el.plot(_this._plotSnap || _this._plotTarget);
                        if (_this._afterCb) _this._afterCb.call(el);
                    };
                    if (startDelay > 0) {
                        setTimeout(apply, startDelay);
                    } else {
                        apply();
                    }
                    return;
                }
                var run = function run() {
                    var fromAttrs = /** @type {Record<string, any>} */ {};
                    var fromColors = /** @type {Record<string, any>} */ {};
                    var toColors = /** @type {Record<string, any>} */ {};
                    if (_this._attrTarget) {
                        var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                        try {
                            for(var _iterator = Object.keys(_this._attrTarget)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                                var key = _step.value;
                                var fromVal = el.attr(key);
                                fromAttrs[key] = fromVal;
                                var fc = parseColor(fromVal);
                                var tc = parseColor(String(_this._attrTarget[key]));
                                if (fc && tc) {
                                    fromColors[key] = fc;
                                    toColors[key] = tc;
                                }
                            }
                        } catch (err) {
                            _didIteratorError = true;
                            _iteratorError = err;
                        } finally{
                            try {
                                if (!_iteratorNormalCompletion && _iterator.return != null) {
                                    _iterator.return();
                                }
                            } finally{
                                if (_didIteratorError) {
                                    throw _iteratorError;
                                }
                            }
                        }
                    }
                    var morphFn = null;
                    if (_this._plotTarget) {
                        var fromPath = el.attr("d") || "";
                        try {
                            morphFn = _this._plotAlgorithm === "polygons" ? morphPolygons(fromPath, _this._plotTarget) : morphPaths(fromPath, _this._plotTarget);
                        } catch (e) {
                            morphFn = null;
                        }
                    }
                    var start = performance.now();
                    var easing = _this._easing || _defaultEasing;
                    var tick = function tick1(now) {
                        var elapsed = now - start;
                        var rawPos = Math.min(elapsed / duration, 1);
                        var pos = easing(rawPos);
                        if (_this._attrTarget) {
                            if (rawPos >= 1) {
                                el.attr(_this._attrTarget);
                            } else {
                                var current = /** @type {Record<string, any>} */ {};
                                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                                try {
                                    for(var _iterator = Object.keys(_this._attrTarget)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                                        var key = _step.value;
                                        if (fromColors[key] && toColors[key]) {
                                            current[key] = interpolateColor(fromColors[key], toColors[key], pos);
                                        } else {
                                            var from = parseFloat(fromAttrs[key]);
                                            var to = parseFloat(_this._attrTarget[key]);
                                            if (!isNaN(from) && !isNaN(to)) {
                                                current[key] = from + (to - from) * pos;
                                            }
                                        }
                                    }
                                } catch (err) {
                                    _didIteratorError = true;
                                    _iteratorError = err;
                                } finally{
                                    try {
                                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                                            _iterator.return();
                                        }
                                    } finally{
                                        if (_didIteratorError) {
                                            throw _iteratorError;
                                        }
                                    }
                                }
                                el.attr(current);
                            }
                        }
                        if (morphFn && rawPos < 1) {
                            el.attr("d", /** @type {any} */ morphFn(pos));
                        }
                        if (_this._duringCb) _this._duringCb(pos);
                        if (rawPos < 1) {
                            BrowserAPIs.requestAnimationFrame(tick);
                        } else {
                            if (_this._plotTarget) {
                                el.attr("d", _this._plotSnap || _this._plotTarget);
                            }
                            if (_this._afterCb) _this._afterCb.call(el);
                        }
                    };
                    BrowserAPIs.requestAnimationFrame(tick);
                };
                if (startDelay > 0) {
                    setTimeout(run, startDelay);
                } else {
                    run();
                }
            }
        }
    ]);
    return SVGAnimationRunner;
}();
function installAnimationMethods(ElementClass) {
    ElementClass.prototype.animate = function(duration, delay) {
        return new SVGAnimationRunner(this, duration, delay);
    };
}
function easeInOutSine(t) {
    return -Math.cos(t * Math.PI) / 2 + 0.5;
}
function cubicBezier(x1, y1, x2, y2) {
    x1 = Math.min(Math.max(x1, 0), 1);
    x2 = Math.min(Math.max(x2, 0), 1);
    var cx = 3 * x1;
    var bx = 3 * (x2 - x1) - cx;
    var ax = 1 - cx - bx;
    var cy = 3 * y1;
    var by = 3 * (y2 - y1) - cy;
    var ay = 1 - cy - by;
    var sampleX = function sampleX(t) {
        return ((ax * t + bx) * t + cx) * t;
    };
    var sampleY = function sampleY(t) {
        return ((ay * t + by) * t + cy) * t;
    };
    var solveT = function solveT(x) {
        var lo = 0;
        var hi = 1;
        var t = x;
        if (t < lo) return lo;
        if (t > hi) return hi;
        while(lo < hi){
            var xt = sampleX(t);
            if (Math.abs(xt - x) < 1e-4) return t;
            if (x > xt) lo = t;
            else hi = t;
            t = (lo + hi) / 2;
        }
        return t;
    };
    return function(t) {
        return t <= 0 ? 0 : t >= 1 ? 1 : sampleY(solveT(t));
    };
}
var REGISTRY = /* @__PURE__ */ new Map();
var linear = function linear(t) {
    return t;
};
REGISTRY.set("linear", linear);
REGISTRY.set("easeInOutSine", easeInOutSine);
REGISTRY.set("easeInSine", function(t) {
    return 1 - Math.cos(t * Math.PI / 2);
});
REGISTRY.set("easeOutSine", function(t) {
    return Math.sin(t * Math.PI / 2);
});
REGISTRY.set("easeInQuad", function(t) {
    return t * t;
});
REGISTRY.set("easeOutQuad", function(t) {
    return 1 - (1 - t) * (1 - t);
});
REGISTRY.set("easeInOutQuad", function(t) {
    return t < 0.5 ? 2 * t * t : 1 - Math.pow(-2 * t + 2, 2) / 2;
});
REGISTRY.set("easeInCubic", function(t) {
    return t * t * t;
});
REGISTRY.set("easeOutCubic", function(t) {
    return 1 - Math.pow(1 - t, 3);
});
REGISTRY.set("easeInOutCubic", function(t) {
    return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2;
});
REGISTRY.set("easeOutBack", function(t) {
    var c1 = 1.70158;
    var c3 = c1 + 1;
    return 1 + c3 * Math.pow(t - 1, 3) + c1 * Math.pow(t - 1, 2);
});
REGISTRY.set("easeInOutBack", function(t) {
    var c1 = 1.70158;
    var c2 = c1 * 1.525;
    return t < 0.5 ? Math.pow(2 * t, 2) * ((c2 + 1) * 2 * t - c2) / 2 : (Math.pow(2 * t - 2, 2) * ((c2 + 1) * (t * 2 - 2) + c2) + 2) / 2;
});
function registerEasing(name2, fn) {
    if (typeof name2 === "string" && name2 && typeof fn === "function") {
        REGISTRY.set(name2, fn);
    }
}
function isBezierArray(v) {
    return Array.isArray(v) && v.length === 4 && v.every(function(n) {
        return typeof n === "number";
    });
}
function resolveEasing(value) {
    if (typeof value === "function") return guardEasing(value);
    if (isBezierArray(value)) return cubicBezier(value[0], value[1], value[2], value[3]);
    if (typeof value === "string" && REGISTRY.has(value)) {
        return guardEasing(/** @type {(t:number)=>number} */ REGISTRY.get(value));
    }
    return easeInOutSine;
}
function guardEasing(fn) {
    return function(t) {
        var y = fn(t);
        return typeof y === "number" && isFinite(y) ? y : t;
    };
}
var SVGNS$1 = "http://www.w3.org/2000/svg";
function easeOutCubic$1(t) {
    return 1 - Math.pow(1 - t, 3);
}
function easeOutBack$1(t) {
    var c1 = 1.70158;
    var c3 = c1 + 1;
    return 1 + c3 * Math.pow(t - 1, 3) + c1 * Math.pow(t - 1, 2);
}
var _reducedMotionMql = null;
function prefersReducedMotion() {
    if (!Environment.isBrowser()) return false;
    try {
        if (!_reducedMotionMql) {
            _reducedMotionMql = window.matchMedia("(prefers-reduced-motion: reduce)");
        }
        return !!_reducedMotionMql.matches;
    } catch (_) {
        return false;
    }
}
function applyProgressiveReveal(el, x, w) {
    if (!Environment.isBrowser()) return false;
    if (w.globals.dataChanged || w.globals.resized) return false;
    var animCfg = w.config.chart.animations;
    if (!animCfg || animCfg.enabled === false) return false;
    var chartType = w.config.chart.type;
    if (chartType !== "line" && chartType !== "area" && chartType !== "rangeArea") {
        return false;
    }
    if (!(w.layout.gridWidth > 0)) return false;
    var drawSpeed = (animCfg.speed || 800) * 2;
    var xRatio = Math.max(0, Math.min(1, x / w.layout.gridWidth));
    var easedT = 1 - Math.cbrt(1 - xRatio);
    var revealDelay = easedT * drawSpeed;
    var node = el.node;
    var style = node.style;
    style.opacity = "0";
    var startAnchor = null;
    var tick = function tick1(now) {
        if (startAnchor === null) startAnchor = now;
        if (now - startAnchor >= revealDelay) {
            style.opacity = "";
        } else {
            BrowserAPIs.requestAnimationFrame(tick);
        }
    };
    BrowserAPIs.requestAnimationFrame(tick);
    return true;
}
function applyAnimationPolicy(w) {
    var anim = w.config.chart.animations;
    if (!anim) return;
    if (anim.respectReducedMotion !== false && prefersReducedMotion()) {
        anim.enabled = false;
        if (anim.dynamicAnimation) anim.dynamicAnimation.enabled = false;
    }
    setDefaultEasing(resolveEasing(anim.easing));
}
function computeStagger(opts) {
    var style = opts.style;
    var index = opts.index || 0;
    var baseDelay = typeof opts.baseDelay === "number" ? opts.baseDelay : 40;
    var row = opts.row || 0;
    var col = opts.col || 0;
    var groupIndex = opts.groupIndex || 0;
    var perGroup = opts.perGroup || 1;
    var centerDistance = opts.centerDistance || 0;
    switch(style){
        case "none":
            return 0;
        case "diagonal":
            return (row + col) * baseDelay;
        case "group":
            return groupIndex * baseDelay + index % perGroup * (baseDelay / 4);
        case "centroid":
            return centerDistance * baseDelay * (index + 1);
        case "sequential":
        default:
            return index * baseDelay;
    }
}
var Animations = /*#__PURE__*/ function() {
    "use strict";
    function Animations(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Animations);
        this.w = w;
        this.ctx = ctx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Animations, [
        {
            /**
   * @param {any} el
   * @param {Record<string, any>} from
   * @param {Record<string, any>} to
   * @param {object} speed
   */ key: "animateLine",
            value: function animateLine(el, from, to, speed) {
                el.attr(from).animate(speed).attr(to);
            }
        },
        {
            /*
   ** Animate radius of a circle element
   * @param {any} el
   * @param {number} speed
   * @param {string} easing
   * @param {Function} cb
   */ /** @param {any} el @param {any} speed @param {any} easing @param {any} cb */ key: "animateMarker",
            value: function animateMarker(el, speed, easing, cb) {
                el.attr({
                    opacity: 0
                }).animate(speed).attr({
                    opacity: 1
                }).after(function() {
                    cb();
                });
            }
        },
        {
            /**
   * Scale-up "pop" effect for scatter/bubble markers. Animates
   * `transform: scale(0 → 1)` (with `easeOutBack` overshoot) plus opacity,
   * around the marker's own center via `transform-box: fill-box`.
   *
   * Falls back to instant render in SSR / when shouldAnimate is false.
   *
   * @param {any} el
   * @param {{ speed: number, delay?: number, onComplete?: () => void }} params
   */ key: "animatePop",
            value: function animatePop(el, param) {
                var speed = param.speed, _param_delay = param.delay, delay = _param_delay === void 0 ? 0 : _param_delay, onComplete = param.onComplete;
                var w = this.w;
                if (!Environment.isBrowser() || !w.globals.shouldAnimate || speed < 1) {
                    if (onComplete) onComplete();
                    return;
                }
                var node = el.node;
                var style = node.style;
                style.transformBox = "fill-box";
                style.transformOrigin = "center";
                style.transform = "scale(0)";
                style.opacity = "0";
                var startAt = performance.now() + delay;
                var step = function step1(now) {
                    if (w.globals.isDestroyed) return;
                    var t = Math.max(0, Math.min(1, (now - startAt) / speed));
                    style.transform = "scale(".concat(easeOutBack$1(t), ")");
                    style.opacity = String(Math.min(1, t * 2));
                    if (t < 1) {
                        BrowserAPIs.requestAnimationFrame(step);
                    } else {
                        style.transform = "";
                        style.transformOrigin = "";
                        style.transformBox = "";
                        style.opacity = "";
                        if (onComplete) onComplete();
                    }
                };
                BrowserAPIs.requestAnimationFrame(step);
            }
        },
        {
            /*
   ** Animate rect properties
   * @param {any} el
   * @param {any} from
   * @param {any} to
   * @param {number} speed
   * @param {Function} fn
   */ /** @param {any} el @param {any} from @param {any} to @param {any} speed @param {any} fn @param {number} [delay] */ key: "animateRect",
            value: function animateRect(el, from, to, speed, fn) {
                var delay = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : 0;
                el.attr(from).animate(speed, delay).attr(to).after(function() {
                    return fn();
                });
            }
        },
        {
            /**
   * @param {Record<string, any>} params
   */ key: "animatePathsGradually",
            value: function animatePathsGradually(params) {
                var el = params.el, realIndex = params.realIndex, j = params.j, fill = params.fill, pathFrom = params.pathFrom, pathTo = params.pathTo, pathToInterp = params.pathToInterp, speed = params.speed, delay = params.delay, scrollMorph = params.scrollMorph;
                var me = this;
                var w = this.w;
                var delayFactor = 0;
                if (w.config.chart.animations.animateGradually.enabled) {
                    delayFactor = w.config.chart.animations.animateGradually.delay;
                }
                if (w.config.chart.animations.dynamicAnimation.enabled && w.globals.dataChanged && w.config.chart.type !== "bar") {
                    delayFactor = 0;
                }
                me.morphSVG(el, realIndex, j, w.config.chart.type === "line" && !w.globals.comboCharts ? "stroke" : fill, pathFrom, pathTo, speed, delay * delayFactor, scrollMorph, pathToInterp);
            }
        },
        {
            /**
   * Opacity-fade reveal (see Graphics.renderPaths `revealViaFade`): hide this
   * path immediately, then reveal the whole series in a single CSS opacity fade
   * once the synchronous draw pass has finished. Used for two cases that both
   * want to avoid per-path morphs: the large-dataset bulk render (>
   * largeDatasetThreshold) and candlestick/boxPlot data-change updates (where an
   * index-based morph would slide candles around on zoom). Every faded path is
   * pushed to delayedElements, but only the first one schedules the reveal — a
   * lone requestAnimationFrame fires after all paths are in the DOM, so N paths
   * cost one frame callback instead of N morph timelines. The guard flag is
   * reset inside the callback (and in initGlobalVars) so subsequent renders
   * re-arm it.
   * @param {any} el
   */ key: "revealBulk",
            value: function revealBulk(el) {
                var _this = this;
                var w = this.w;
                el.node.classList.add("apexcharts-element-hidden");
                w.globals.delayedElements.push({
                    el: el.node
                });
                if (!Environment.isBrowser() || !w.globals.shouldAnimate) {
                    this.animationCompleted(el);
                    return;
                }
                if (!w.globals.bulkRevealScheduled) {
                    w.globals.bulkRevealScheduled = true;
                    BrowserAPIs.requestAnimationFrame(function() {
                        if (w.globals.isDestroyed) return;
                        w.globals.bulkRevealScheduled = false;
                        _this.animationCompleted(el);
                    });
                }
            }
        },
        {
            key: "showDelayedElements",
            value: function showDelayedElements() {
                var _this = this;
                this.w.globals.delayedElements.forEach(function(d) {
                    if (d.holdUntilComplete && !_this.w.globals.animationEnded) return;
                    var ele = d.el;
                    ele.classList.remove("apexcharts-element-hidden");
                    ele.classList.add("apexcharts-hidden-element-shown");
                });
            }
        },
        {
            /**
   * @param {any} el
   */ key: "animationCompleted",
            value: function animationCompleted(el) {
                var w = this.w;
                if (w.globals.animationEnded) return;
                w.globals.animationEnded = true;
                this.showDelayedElements();
                if (typeof w.config.chart.events.animationEnd === "function") {
                    w.config.chart.events.animationEnd(this.ctx, {
                        el: el,
                        w: w
                    });
                }
            }
        },
        {
            /**
   * Initial-mount "pen-stroke" draw effect for line / area / rangeArea paths.
   *
   * Stroke paths animate `stroke-dashoffset` from total length → 0.
   * Fill paths animate the width of a per-series SVG `<mask>` rect from 0 → gridWidth,
   * which coexists with the existing `clip-path: gridRectMask` (mask + clip-path
   * are independent SVG attributes).
   *
   * For radar / radial shapes, pass `mask: { type: 'radial', cx, cy, r }` to
   * use a circular mask that blooms from center outward instead of the default
   * left-to-right rect wipe.
   *
   * @param {any} el            - SVG.js path element
   * @param {{realIndex: number, j?: number, isFill: boolean, isLast: boolean, speed: number, delay: number, mask?: {type: 'rect'|'radial', cx?: number, cy?: number, r?: number}}} params
   */ key: "animateDraw",
            value: function animateDraw(el, param) {
                var realIndex = param.realIndex, j = param.j, isFill = param.isFill, isLast = param.isLast, speed = param.speed, delay = param.delay, maskShape = param.mask;
                var w = this.w;
                var me = this;
                var finalize = function finalize() {
                    if (isLast && w.globals.shouldAnimate) {
                        me.animationCompleted(el);
                    }
                    me.showDelayedElements();
                };
                if (!Environment.isBrowser() || !w.globals.shouldAnimate || speed < 1) {
                    finalize();
                    return;
                }
                var node = el.node;
                var runMaskReveal = function runMaskReveal() {
                    var pad = 4;
                    var isRadial = maskShape && maskShape.type === "radial";
                    var targetWidth = w.layout.gridWidth + pad * 2;
                    var radialCx = maskShape && maskShape.cx || 0;
                    var radialCy = maskShape && maskShape.cy || 0;
                    var targetRadius = (maskShape && maskShape.r || w.layout.gridWidth / 2) + pad;
                    var maskId = "apexDrawMask".concat(w.globals.cuid, "-").concat(realIndex, "-").concat(j != null ? j : 0, "-").concat(isFill ? "f" : "s");
                    var mask = BrowserAPIs.createElementNS(SVGNS$1, "mask");
                    mask.setAttribute("id", maskId);
                    mask.setAttribute("maskUnits", "userSpaceOnUse");
                    var revealEl;
                    if (isRadial) {
                        var region = targetRadius;
                        mask.setAttribute("x", String(radialCx - region));
                        mask.setAttribute("y", String(radialCy - region));
                        mask.setAttribute("width", String(region * 2));
                        mask.setAttribute("height", String(region * 2));
                        revealEl = BrowserAPIs.createElementNS(SVGNS$1, "circle");
                        revealEl.setAttribute("cx", String(radialCx));
                        revealEl.setAttribute("cy", String(radialCy));
                        revealEl.setAttribute("r", "0");
                        revealEl.setAttribute("fill", "#fff");
                    } else {
                        mask.setAttribute("x", String(-pad));
                        mask.setAttribute("y", String(-pad));
                        mask.setAttribute("width", String(targetWidth));
                        mask.setAttribute("height", String(w.layout.gridHeight + pad * 2));
                        revealEl = BrowserAPIs.createElementNS(SVGNS$1, "rect");
                        revealEl.setAttribute("x", String(-pad));
                        revealEl.setAttribute("y", String(-pad));
                        revealEl.setAttribute("width", "0");
                        revealEl.setAttribute("height", String(w.layout.gridHeight + pad * 2));
                        revealEl.setAttribute("fill", "#fff");
                    }
                    mask.appendChild(revealEl);
                    w.dom.elDefs.node.appendChild(mask);
                    node.setAttribute("mask", "url(#".concat(maskId, ")"));
                    var startAt = performance.now() + (delay || 0);
                    var step = function step1(now) {
                        if (w.globals.isDestroyed) return;
                        var t = Math.max(0, Math.min(1, (now - startAt) / speed));
                        var eased = easeOutCubic$1(t);
                        if (isRadial) {
                            revealEl.setAttribute("r", String(eased * targetRadius));
                        } else {
                            revealEl.setAttribute("width", String(eased * targetWidth));
                        }
                        if (t < 1) {
                            BrowserAPIs.requestAnimationFrame(step);
                        } else {
                            node.removeAttribute("mask");
                            if (mask.parentNode) mask.parentNode.removeChild(mask);
                            finalize();
                        }
                    };
                    BrowserAPIs.requestAnimationFrame(step);
                };
                var runStrokeDraw = function runStrokeDraw(len) {
                    node.setAttribute("stroke-dasharray", String(len));
                    node.setAttribute("stroke-dashoffset", String(len));
                    var startAt = performance.now() + (delay || 0);
                    var step = function step1(now) {
                        if (w.globals.isDestroyed) return;
                        var t = Math.max(0, Math.min(1, (now - startAt) / speed));
                        node.setAttribute("stroke-dashoffset", String(len * (1 - easeOutCubic$1(t))));
                        if (t < 1) {
                            BrowserAPIs.requestAnimationFrame(step);
                        } else {
                            node.removeAttribute("stroke-dasharray");
                            node.removeAttribute("stroke-dashoffset");
                            finalize();
                        }
                    };
                    BrowserAPIs.requestAnimationFrame(step);
                };
                BrowserAPIs.requestAnimationFrame(function() {
                    if (w.globals.isDestroyed) return;
                    if (isFill) {
                        runMaskReveal();
                        return;
                    }
                    var existingDash = node.getAttribute("stroke-dasharray");
                    var hasCustomDash = !!existingDash && existingDash !== "0" && existingDash !== "";
                    if (hasCustomDash) {
                        runMaskReveal();
                        return;
                    }
                    var len = 0;
                    try {
                        if (typeof node.getTotalLength === "function") {
                            len = node.getTotalLength();
                        }
                    } catch (_) {
                        len = 0;
                    }
                    if (!len) {
                        finalize();
                        return;
                    }
                    runStrokeDraw(len);
                });
            }
        },
        {
            // SVG.js animation for morphing one path to another
            /**
   * @param {any} el
   * @param {number} realIndex
   * @param {number} j
   * @param {string} fill
   * @param {string} pathFrom
   * @param {string} pathTo
   * @param {number} speed
   * @param {number} delay
   * @param {boolean} [scrollMorph] - this morph is a streaming scroll (StreamScroll)
   * @param {string} [pathToInterp] - interpolation target that differs from the
   *   final path. Reconciled length-change morphs tween toward a padded copy of
   *   pathTo (extra anchors sitting exactly on the final geometry) and snap to
   *   the clean pathTo at the end.
   */ key: "morphSVG",
            value: function morphSVG(el, realIndex, j, fill, pathFrom, pathTo, speed, delay, scrollMorph, pathToInterp) {
                var _this = this;
                var _a, _b;
                var w = this.w;
                if (!pathFrom) {
                    pathFrom = el.attr("pathFrom");
                }
                if (!pathTo) {
                    pathTo = el.attr("pathTo");
                }
                var disableAnimationForCorrupPath = function disableAnimationForCorrupPath() {
                    if (w.config.chart.type === "radar") {
                        speed = 1;
                    }
                    return "M 0 ".concat(w.layout.gridHeight);
                };
                if (!pathFrom || pathFrom.indexOf("undefined") > -1 || pathFrom.indexOf("NaN") > -1) {
                    pathFrom = disableAnimationForCorrupPath();
                }
                if (!pathTo.trim() || pathTo.indexOf("undefined") > -1 || pathTo.indexOf("NaN") > -1) {
                    pathTo = disableAnimationForCorrupPath();
                    pathToInterp = void 0;
                }
                if (pathToInterp && (!pathToInterp.trim() || pathToInterp.indexOf("undefined") > -1 || pathToInterp.indexOf("NaN") > -1)) {
                    pathToInterp = void 0;
                }
                if (!w.globals.shouldAnimate) {
                    speed = 1;
                }
                var crossTypeMorph = ((_b = (_a = this.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.isActive()) === true;
                var morphAlgo = crossTypeMorph ? "polygons" : "commands";
                var morphEase = null;
                if (w.globals.dataChanged) {
                    var dynEasing = w.config.chart.animations.dynamicAnimation.easing;
                    if (dynEasing != null) {
                        morphEase = resolveEasing(dynEasing);
                    } else if (scrollMorph) {
                        morphEase = resolveEasing("linear");
                    }
                }
                var runner = el.plot(pathFrom).animate(1, delay).plot(pathFrom).animate(speed, delay);
                if (morphEase) {
                    runner.ease(morphEase);
                }
                runner.plot(pathToInterp || pathTo, morphAlgo, pathToInterp ? pathTo : void 0).after(function() {
                    if (Utils$1.isNumber(j)) {
                        var maxSeries = w.seriesData.series[w.globals.maxValsInArrayIndex];
                        if (maxSeries && j === maxSeries.length - 2 && w.globals.shouldAnimate) {
                            _this.animationCompleted(el);
                        }
                    } else if (fill !== "none" && w.globals.shouldAnimate) {
                        if (!w.globals.comboCharts && realIndex === w.seriesData.series.length - 1 || w.globals.comboCharts) {
                            _this.animationCompleted(el);
                        }
                    }
                    _this.showDelayedElements();
                });
            }
        }
    ]);
    return Animations;
}();
var DEFAULT_INTENSITY = {
    lighten: 0.15,
    darken: 0.35
};
var Filters = /*#__PURE__*/ function() {
    "use strict";
    function Filters(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Filters);
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Filters, [
        {
            // create a re-usable filter which can be appended other filter effects and applied to multiple elements
            /**
   * @param {any} el
   * @param {number} i
   */ key: "getDefaultFilter",
            value: function getDefaultFilter(el, i) {
                var w = this.w;
                if (el.unfilter) {
                    el.unfilter(true);
                }
                if (w.config.chart.dropShadow.enabled) {
                    this.dropShadow(el, w.config.chart.dropShadow, i);
                }
            }
        },
        {
            /**
   * @param {any} el
   * @param {number} i
   * @param {string} filterType
   * @param {number} [intensity] Blend strength (0 to 1). Defaults per type.
   */ key: "applyFilter",
            value: function applyFilter(el, i, filterType, intensity) {
                var _this = this;
                var _a, _b, _c;
                var w = this.w;
                if (el.unfilter) {
                    el.unfilter(true);
                }
                if (filterType === "none") {
                    this.getDefaultFilter(el, i);
                    return;
                }
                var shadowAttr = w.config.chart.dropShadow;
                var fallback = filterType === "lighten" ? DEFAULT_INTENSITY.lighten : DEFAULT_INTENSITY.darken;
                var t = Math.max(0, Math.min(1, typeof intensity === "number" ? intensity : fallback));
                var diag = 1 - t;
                var offset = filterType === "lighten" ? t : 0;
                if (el.filterWith) {
                    el.filterWith(function(add) {
                        add.colorMatrix({
                            type: "matrix",
                            values: "\n            ".concat(diag, " 0 0 0 ").concat(offset, "\n            0 ").concat(diag, " 0 0 ").concat(offset, "\n            0 0 ").concat(diag, " 0 ").concat(offset, "\n            0 0 0 1 0\n          "),
                            in: "SourceGraphic",
                            result: "brightness"
                        });
                        if (shadowAttr.enabled) {
                            _this.addShadow(add, i, shadowAttr, "brightness");
                        }
                    });
                    if (!shadowAttr.noUserSpaceOnUse) {
                        (_b = (_a = el.filterer()) == null ? void 0 : _a.node) == null ? void 0 : _b.setAttribute("filterUnits", "userSpaceOnUse");
                    }
                    this._scaleFilterSize((_c = el.filterer()) == null ? void 0 : _c.node);
                }
            }
        },
        {
            // appends dropShadow to the filter object which can be chained with other filter effects
            /**
   * @param {any} add
   * @param {number} i
   * @param {Record<string, any>} attrs
   * @param {string} source
   */ key: "addShadow",
            value: function addShadow(add, i, attrs, source) {
                var _a;
                var w = this.w;
                var blur = attrs.blur, top = attrs.top, left = attrs.left, color = attrs.color, opacity = attrs.opacity;
                color = Array.isArray(color) ? color[i] : color;
                if (((_a = w.config.chart.dropShadow.enabledOnSeries) == null ? void 0 : _a.length) > 0) {
                    if (w.config.chart.dropShadow.enabledOnSeries.indexOf(i) === -1) {
                        return add;
                    }
                }
                add.offset({
                    in: source,
                    dx: left,
                    dy: top,
                    result: "offset"
                });
                add.gaussianBlur({
                    in: "offset",
                    stdDeviation: blur,
                    result: "blur"
                });
                add.flood({
                    "flood-color": color,
                    "flood-opacity": opacity,
                    result: "flood"
                });
                add.composite({
                    in: "flood",
                    in2: "blur",
                    operator: "in",
                    result: "shadow"
                });
                add.merge([
                    "shadow",
                    source
                ]);
            }
        },
        {
            // directly adds dropShadow to the element and returns the same element.
            /**
   * @param {any} el
   * @param {Record<string, any>} attrs
   */ key: "dropShadow",
            value: function dropShadow(el, attrs) {
                var _this = this;
                var i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
                var _a, _b, _c, _d, _e;
                var w = this.w;
                if (el.unfilter) {
                    el.unfilter(true);
                }
                if (Utils$1.isMsEdge() && w.config.chart.type === "radialBar") {
                    return el;
                }
                if (((_a = w.config.chart.dropShadow.enabledOnSeries) == null ? void 0 : _a.length) > 0) {
                    if (((_b = w.config.chart.dropShadow.enabledOnSeries) == null ? void 0 : _b.indexOf(i)) === -1) {
                        return el;
                    }
                }
                if (el.filterWith) {
                    el.filterWith(function(add) {
                        _this.addShadow(add, i, attrs, "SourceGraphic");
                    });
                    if (!attrs.noUserSpaceOnUse) {
                        (_d = (_c = el.filterer()) == null ? void 0 : _c.node) == null ? void 0 : _d.setAttribute("filterUnits", "userSpaceOnUse");
                    }
                    this._scaleFilterSize((_e = el.filterer()) == null ? void 0 : _e.node);
                }
                return el;
            }
        },
        {
            /**
   * @param {any} el
   * @param {number} realIndex
   * @param {number} dataPointIndex
   */ key: "setSelectionFilter",
            value: function setSelectionFilter(el, realIndex, dataPointIndex) {
                var w = this.w;
                if (typeof w.interact.selectedDataPoints[realIndex] !== "undefined") {
                    if (w.interact.selectedDataPoints[realIndex].indexOf(dataPointIndex) > -1) {
                        el.node.setAttribute("selected", true);
                        var activeFilter = w.config.states.active.filter;
                        if (activeFilter.type !== "none") {
                            this.applyFilter(el, realIndex, activeFilter.type, activeFilter.value);
                        }
                    }
                }
            }
        },
        {
            /**
   * @param {any} el
   */ key: "_scaleFilterSize",
            value: function _scaleFilterSize(el) {
                if (!el) return;
                var setAttributes = function setAttributes(attrs) {
                    for(var key in attrs){
                        if (Object.prototype.hasOwnProperty.call(attrs, key)) {
                            el.setAttribute(key, attrs[key]);
                        }
                    }
                };
                setAttributes({
                    width: "200%",
                    height: "200%",
                    x: "-50%",
                    y: "-50%"
                });
            }
        }
    ]);
    return Filters;
}();
var Graphics = /*#__PURE__*/ function() {
    "use strict";
    function Graphics(w) {
        var ctx = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Graphics);
        this.w = w;
        this.ctx = ctx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Graphics, [
        {
            /*****************************************************************************
   *                                                                            *
   *  SVG Path Rounding Function                                                *
   *  Copyright (C) 2014 Yona Appletree                                         *
   *                                                                            *
   *  Licensed under the Apache License, Version 2.0 (the "License");           *
   *  you may not use this file except in compliance with the License.          *
   *  You may obtain a copy of the License at                                   *
   *                                                                            *
   *      http://www.apache.org/licenses/LICENSE-2.0                            *
   *                                                                            *
   *  Unless required by applicable law or agreed to in writing, software       *
   *  distributed under the License is distributed on an "AS IS" BASIS,         *
   *  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  *
   *  See the License for the specific language governing permissions and       *
   *  limitations under the License.                                            *
   *                                                                            *
   *****************************************************************************/ /**
   * SVG Path rounding function. Takes an input path string and outputs a path
   * string where all line-line corners have been rounded. Only supports absolute
   * commands at the moment.
   *
   * @param pathString The SVG input path
   * @param radius The amount to round the corners, either a value in the SVG
   *               coordinate space, or, if useFractionalRadius is true, a value
   *               from 0 to 1.
   * @returns A new SVG path string with the rounding
   * @param {string} pathString
   * @param {number} radius
   */ key: "roundPathCorners",
            value: function roundPathCorners(pathString, radius) {
                if (pathString.indexOf("NaN") > -1) pathString = "";
                function moveTowardsLength(movingPoint, targetPoint, amount) {
                    var width = targetPoint.x - movingPoint.x;
                    var height = targetPoint.y - movingPoint.y;
                    var distance = Math.sqrt(width * width + height * height);
                    return moveTowardsFractional(movingPoint, targetPoint, Math.min(1, amount / distance));
                }
                function moveTowardsFractional(movingPoint, targetPoint, fraction) {
                    return {
                        x: movingPoint.x + (targetPoint.x - movingPoint.x) * fraction,
                        y: movingPoint.y + (targetPoint.y - movingPoint.y) * fraction
                    };
                }
                function adjustCommand(cmd, newPoint) {
                    if (cmd.length > 2) {
                        cmd[cmd.length - 2] = newPoint.x;
                        cmd[cmd.length - 1] = newPoint.y;
                    }
                }
                function pointForCommand(cmd) {
                    return {
                        x: parseFloat(cmd[cmd.length - 2]),
                        y: parseFloat(cmd[cmd.length - 1])
                    };
                }
                var pathParts = pathString.split(/[,\s]/).reduce(function(parts, part) {
                    var match = part.match(/^([a-zA-Z])(.+)/);
                    if (match) {
                        parts.push(match[1]);
                        parts.push(match[2]);
                    } else {
                        parts.push(part);
                    }
                    return parts;
                }, []);
                var commands = pathParts.reduce(function(commands2, part) {
                    if (parseFloat(part) == part && commands2.length) {
                        commands2[commands2.length - 1].push(part);
                    } else {
                        commands2.push([
                            part
                        ]);
                    }
                    return commands2;
                }, []);
                var resultCommands = [];
                if (commands.length > 1) {
                    var startPoint = pointForCommand(commands[0]);
                    var virtualCloseLine = null;
                    if (commands[commands.length - 1][0] == "Z" && commands[0].length > 2) {
                        virtualCloseLine = [
                            "L",
                            startPoint.x,
                            startPoint.y
                        ];
                        commands[commands.length - 1] = virtualCloseLine;
                    }
                    resultCommands.push(commands[0]);
                    for(var cmdIndex = 1; cmdIndex < commands.length; cmdIndex++){
                        var prevCmd = resultCommands[resultCommands.length - 1];
                        var curCmd = commands[cmdIndex];
                        var nextCmd = curCmd == virtualCloseLine ? commands[1] : commands[cmdIndex + 1];
                        if (nextCmd && prevCmd && prevCmd.length > 2 && curCmd[0] == "L" && nextCmd.length > 2 && nextCmd[0] == "L") {
                            var prevPoint = pointForCommand(prevCmd);
                            var curPoint = pointForCommand(curCmd);
                            var nextPoint = pointForCommand(nextCmd);
                            var curveStart, curveEnd;
                            curveStart = moveTowardsLength(curPoint, prevPoint, radius);
                            curveEnd = moveTowardsLength(curPoint, nextPoint, radius);
                            adjustCommand(curCmd, curveStart);
                            curCmd.origPoint = curPoint;
                            resultCommands.push(curCmd);
                            var startControl = moveTowardsFractional(curveStart, curPoint, 0.5);
                            var endControl = moveTowardsFractional(curPoint, curveEnd, 0.5);
                            var curveCmd = [
                                "C",
                                startControl.x,
                                startControl.y,
                                endControl.x,
                                endControl.y,
                                curveEnd.x,
                                curveEnd.y
                            ];
                            curveCmd.origPoint = curPoint;
                            resultCommands.push(curveCmd);
                        } else {
                            resultCommands.push(curCmd);
                        }
                    }
                    if (virtualCloseLine) {
                        var newStartPoint = pointForCommand(resultCommands[resultCommands.length - 1]);
                        resultCommands.push([
                            "Z"
                        ]);
                        adjustCommand(resultCommands[0], newStartPoint);
                    }
                } else {
                    resultCommands = commands;
                }
                return resultCommands.reduce(function(str, c) {
                    return str + c.join(" ") + " ";
                }, "");
            }
        },
        {
            /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   * @param {number | null} [strokeWidth]
   */ key: "drawLine",
            value: function drawLine(x1, y1, x2, y2) {
                var lineColor = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : "#a8a8a8", dashArray = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : 0, strokeWidth = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : null, strokeLineCap = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : "butt";
                var w = this.w;
                var line = w.dom.Paper.line().attr({
                    x1: x1,
                    y1: y1,
                    x2: x2,
                    y2: y2,
                    stroke: lineColor,
                    "stroke-dasharray": dashArray,
                    "stroke-width": strokeWidth,
                    "stroke-linecap": strokeLineCap
                });
                return line;
            }
        },
        {
            /**
   * @param {number | null} [strokeWidth]
   * @param {string | null} [strokeColor]
   */ key: "drawRect",
            value: function drawRect() {
                var x1 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, y1 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, x2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0, y2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0, radius = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 0, color = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : "#fefefe", opacity = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : 1, strokeWidth = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : null, strokeColor = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : null, strokeDashArray = arguments.length > 9 && arguments[9] !== void 0 ? arguments[9] : 0;
                var w = this.w;
                var rect = w.dom.Paper.rect();
                rect.attr({
                    x: x1,
                    y: y1,
                    width: x2 > 0 ? x2 : 0,
                    height: y2 > 0 ? y2 : 0,
                    rx: radius,
                    ry: radius,
                    opacity: opacity,
                    "stroke-width": strokeWidth !== null ? strokeWidth : 0,
                    stroke: strokeColor !== null ? strokeColor : "none",
                    "stroke-dasharray": strokeDashArray
                });
                rect.node.setAttribute("fill", color);
                return rect;
            }
        },
        {
            /**
   * @param {string} polygonString
   */ key: "drawPolygon",
            value: function drawPolygon(polygonString) {
                var stroke = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "#e1e1e1", strokeWidth = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1, fill = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "none";
                var w = this.w;
                var polygon = w.dom.Paper.polygon(polygonString).attr({
                    fill: fill,
                    stroke: stroke,
                    "stroke-width": strokeWidth
                });
                return polygon;
            }
        },
        {
            /**
   * @param {number} radius
   * @param {Record<string, any> | null} attrs
   */ key: "drawCircle",
            value: function drawCircle(radius) {
                var attrs = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
                var w = this.w;
                if (radius < 0) radius = 0;
                var c = w.dom.Paper.circle(radius * 2);
                if (attrs !== null) {
                    c.attr(attrs);
                }
                return c;
            }
        },
        {
            /** @param {{ d?: string, stroke?: string, strokeWidth?: number, fill: any, fillOpacity?: number, strokeOpacity?: number, classes?: any, strokeLinecap?: any, strokeDashArray?: number }} opts */ key: "drawPath",
            value: function drawPath(param) {
                var _param_d = param.d, d = _param_d === void 0 ? "" : _param_d, _param_stroke = param.stroke, stroke = _param_stroke === void 0 ? "#a8a8a8" : _param_stroke, _param_strokeWidth = param.strokeWidth, strokeWidth = _param_strokeWidth === void 0 ? 1 : _param_strokeWidth, fill = param.fill, _param_fillOpacity = param.fillOpacity, fillOpacity = _param_fillOpacity === void 0 ? 1 : _param_fillOpacity, _param_strokeOpacity = param.strokeOpacity, strokeOpacity = _param_strokeOpacity === void 0 ? 1 : _param_strokeOpacity, classes = param.classes, _param_strokeLinecap = param.strokeLinecap, strokeLinecap = _param_strokeLinecap === void 0 ? null : _param_strokeLinecap, _param_strokeDashArray = param.strokeDashArray, strokeDashArray = _param_strokeDashArray === void 0 ? 0 : _param_strokeDashArray;
                var w = this.w;
                if (strokeLinecap === null) {
                    strokeLinecap = w.config.stroke.lineCap;
                }
                if (d.indexOf("undefined") > -1 || d.indexOf("NaN") > -1) {
                    d = "M 0 ".concat(w.layout.gridHeight);
                }
                var p = w.dom.Paper.path(d).attr({
                    fill: fill,
                    "fill-opacity": fillOpacity,
                    stroke: stroke,
                    "stroke-opacity": strokeOpacity,
                    "stroke-linecap": strokeLinecap,
                    "stroke-width": strokeWidth,
                    "stroke-dasharray": strokeDashArray,
                    class: classes
                });
                return p;
            }
        },
        {
            /**
   * @param {Record<string, any> | null} attrs
   */ key: "group",
            value: function group() {
                var attrs = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
                var w = this.w;
                var g = w.dom.Paper.group();
                if (attrs !== null) {
                    g.attr(attrs);
                }
                return g;
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   */ key: "move",
            value: function move(x, y) {
                var move = [
                    "M",
                    x,
                    y
                ].join(" ");
                return move;
            }
        },
        {
            /**
   * @param {number | null} x
   * @param {number | null} y
   * @param {string | null} hORv
   * @returns {string}
   */ key: "line",
            value: function line(x, y) {
                var hORv = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
                if (hORv === "H") return [
                    " H",
                    x
                ].join(" ");
                if (hORv === "V") return [
                    " V",
                    y
                ].join(" ");
                return [
                    " L",
                    x,
                    y
                ].join(" ");
            }
        },
        {
            /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   * @param {number} x
   * @param {number} y
   */ key: "curve",
            value: function curve(x1, y1, x2, y2, x, y) {
                var curve = [
                    "C",
                    x1,
                    y1,
                    x2,
                    y2,
                    x,
                    y
                ].join(" ");
                return curve;
            }
        },
        {
            /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x
   * @param {number} y
   */ key: "quadraticCurve",
            value: function quadraticCurve(x1, y1, x, y) {
                var curve = [
                    "Q",
                    x1,
                    y1,
                    x,
                    y
                ].join(" ");
                return curve;
            }
        },
        {
            /**
   * @param {number} rx
   * @param {number} ry
   * @param {number} axisRotation
   * @param {number} largeArcFlag
   * @param {number} sweepFlag
   * @param {number} x
   * @param {number} y
   */ key: "arc",
            value: function arc(rx, ry, axisRotation, largeArcFlag, sweepFlag, x, y) {
                var relative = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : false;
                var coord = "A";
                if (relative) coord = "a";
                var arc = [
                    coord,
                    rx,
                    ry,
                    axisRotation,
                    largeArcFlag,
                    sweepFlag,
                    x,
                    y
                ].join(" ");
                return arc;
            }
        },
        {
            /**
   * @memberof Graphics
   * @param {Record<string, any>} opts
   *  i = series's index
   *  realIndex = realIndex is series's actual index when it was drawn time. After several redraws, the iterating "i" may change in loops, but realIndex doesn't
   *  pathFrom = existing pathFrom to animateTo
   *  pathTo = new Path to which d attr will be animated from pathFrom to pathTo
   *  stroke = line Color
   *  strokeWidth = width of path Line
   *  fill = it can be gradient, single color, pattern or image
   *  animationDelay = how much to delay when starting animation (in milliseconds)
   *  dataChangeSpeed = for dynamic animations, when data changes
   *  className = class attribute to add
   *  scrollMorph = this data-change morph is a streaming scroll (see StreamScroll);
   *                defaults the morph easing to linear so the slide is constant-velocity
   * @return {any} svg.js path object
   **/ key: "renderPaths",
            value: function renderPaths(param) {
                var j = param.j, realIndex = param.realIndex, pathFrom = param.pathFrom, pathTo = param.pathTo, pathToInterp = param.pathToInterp, stroke = param.stroke, strokeWidth = param.strokeWidth, strokeLinecap = param.strokeLinecap, fill = param.fill, animationDelay = param.animationDelay, initialSpeed = param.initialSpeed, dataChangeSpeed = param.dataChangeSpeed, className = param.className, chartType = param.chartType, _param_shouldClipToGrid = param.shouldClipToGrid, shouldClipToGrid = _param_shouldClipToGrid === void 0 ? true : _param_shouldClipToGrid, _param_bindEventsOnPaths = param.bindEventsOnPaths, bindEventsOnPaths = _param_bindEventsOnPaths === void 0 ? true : _param_bindEventsOnPaths, _param_drawShadow = param.drawShadow, drawShadow = _param_drawShadow === void 0 ? true : _param_drawShadow, _param_drawMask = param.drawMask, drawMask = _param_drawMask === void 0 ? null : _param_drawMask, _param_scrollMorph = param.scrollMorph, scrollMorph = _param_scrollMorph === void 0 ? false : _param_scrollMorph;
                var _a, _b;
                var w = this.w;
                var filters = new Filters(this.w);
                var anim = new Animations(this.w, (_a = this.ctx) != null ? _a : void 0);
                var initialAnim = this.w.config.chart.animations.enabled;
                var dynamicAnim = initialAnim && this.w.config.chart.animations.dynamicAnimation.enabled;
                if (pathFrom && pathFrom.startsWith("M 0 0 ") && pathTo) {
                    var moveCommand = pathTo.match(/^M\s+[\d.-]+\s+[\d.-]+/);
                    if (moveCommand) {
                        pathFrom = pathFrom.replace(/^M\s+0\s+0/, moveCommand[0]);
                    }
                }
                var d;
                var shouldAnimate = !!(initialAnim && !w.globals.resized || dynamicAnim && w.globals.dataChanged && w.globals.shouldAnimate);
                var isDrawableSeries = typeof className === "string" && (className.indexOf("apexcharts-line") > -1 || className.indexOf("apexcharts-area") > -1 || className.indexOf("apexcharts-rangeArea") > -1 || className.indexOf("apexcharts-radar") > -1);
                var useDrawMode = !!(initialAnim && !w.globals.resized && !w.globals.dataChanged && isDrawableSeries);
                var largeThreshold = (_b = w.config.chart.animations.largeDatasetThreshold) != null ? _b : 0;
                var bulkRender = !!(shouldAnimate && !useDrawMode && largeThreshold > 0 && w.globals.dataPoints > largeThreshold);
                var isCandleOrBox = chartType === "candlestick" || chartType === "boxPlot";
                var fadeOnDataChange = !!(isCandleOrBox && shouldAnimate && !useDrawMode && w.globals.dataChanged);
                var revealViaFade = bulkRender || fadeOnDataChange;
                if (shouldAnimate && !useDrawMode && !revealViaFade) {
                    d = pathFrom;
                } else {
                    d = pathTo;
                    if (!shouldAnimate) {
                        w.globals.animationEnded = true;
                    }
                }
                var strokeDashArrayOpt = w.config.stroke.dashArray;
                var strokeDashArray = 0;
                if (Array.isArray(strokeDashArrayOpt)) {
                    strokeDashArray = strokeDashArrayOpt[realIndex];
                } else {
                    strokeDashArray = w.config.stroke.dashArray;
                }
                var el = this.drawPath({
                    d: d,
                    stroke: stroke,
                    strokeWidth: strokeWidth,
                    fill: fill,
                    fillOpacity: 1,
                    classes: className,
                    strokeLinecap: strokeLinecap,
                    strokeDashArray: strokeDashArray
                });
                el.attr("index", realIndex);
                if (shouldClipToGrid) {
                    if (chartType === "bar" && !w.globals.isBarHorizontal || w.globals.comboCharts) {
                        el.attr({
                            "clip-path": "url(#gridRectBarMask".concat(w.globals.cuid, ")")
                        });
                    } else {
                        el.attr({
                            "clip-path": "url(#gridRectMask".concat(w.globals.cuid, ")")
                        });
                    }
                }
                if (w.config.chart.dropShadow.enabled && drawShadow) {
                    filters.dropShadow(el, w.config.chart.dropShadow, realIndex);
                }
                if (bindEventsOnPaths) {
                    el.node.addEventListener("mouseenter", this.pathMouseEnter.bind(this, el));
                    el.node.addEventListener("mouseleave", this.pathMouseLeave.bind(this, el));
                    el.node.addEventListener("mousedown", this.pathMouseDown.bind(this, el));
                }
                el.attr({
                    pathTo: pathTo,
                    pathFrom: pathFrom
                });
                var defaultAnimateOpts = {
                    el: el,
                    j: j,
                    realIndex: realIndex,
                    pathFrom: pathFrom,
                    pathTo: pathTo,
                    pathToInterp: pathToInterp,
                    fill: fill,
                    strokeWidth: strokeWidth,
                    delay: animationDelay,
                    scrollMorph: scrollMorph
                };
                if (initialAnim && !w.globals.resized && !w.globals.dataChanged) {
                    if (useDrawMode) {
                        var drawSpeed = initialSpeed * 2;
                        var isFill = stroke === "none" || strokeWidth === 0;
                        var seriesCount = w.seriesData.series.length;
                        var isLast = w.globals.comboCharts ? true : realIndex === seriesCount - 1;
                        anim.animateDraw(el, {
                            realIndex: realIndex,
                            j: j,
                            isFill: isFill,
                            isLast: isLast,
                            speed: drawSpeed,
                            delay: 0,
                            mask: drawMask
                        });
                    } else if (!revealViaFade) {
                        anim.animatePathsGradually(__spreadProps(__spreadValues({}, defaultAnimateOpts), {
                            speed: initialSpeed
                        }));
                    }
                } else {
                    if ((w.globals.resized || !w.globals.dataChanged) && !revealViaFade) {
                        anim.showDelayedElements();
                    }
                }
                if (w.globals.dataChanged && dynamicAnim && shouldAnimate && !revealViaFade) {
                    anim.animatePathsGradually(__spreadProps(__spreadValues({}, defaultAnimateOpts), {
                        speed: dataChangeSpeed
                    }));
                }
                if (revealViaFade) {
                    anim.revealBulk(el);
                }
                return el;
            }
        },
        {
            /**
   * @param {string} style
   * @param {number} width
   * @param {number} height
   */ key: "drawPattern",
            value: function drawPattern(style, width, height) {
                var stroke = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "#a8a8a8", strokeWidth = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 0;
                var w = this.w;
                var p = w.dom.Paper.pattern(width, height, function(add) {
                    if (style === "horizontalLines") {
                        add.line(0, 0, height, 0).stroke({
                            color: stroke,
                            width: strokeWidth + 1
                        });
                    } else if (style === "verticalLines") {
                        add.line(0, 0, 0, width).stroke({
                            color: stroke,
                            width: strokeWidth + 1
                        });
                    } else if (style === "slantedLines") {
                        add.line(0, 0, width, height).stroke({
                            color: stroke,
                            width: strokeWidth
                        });
                    } else if (style === "squares") {
                        add.rect(width, height).fill("none").stroke({
                            color: stroke,
                            width: strokeWidth
                        });
                    } else if (style === "circles") {
                        add.circle(width).fill("none").stroke({
                            color: stroke,
                            width: strokeWidth
                        });
                    }
                });
                return p;
            }
        },
        {
            /**
   * @param {string} style
   * @param {string} gfrom
   * @param {string} gto
   * @param {number} opacityFrom
   * @param {number} opacityTo
   * @param {number | null} [size]
   * @param {number[] | null} stops
   * @param {any[]} colorStops
   * @param {number} [i]
   * @param {boolean} [verticalUserSpace] anchor a vertical gradient to the plot
   *   area rather than to each path's own bounding box. Needed when one series
   *   is drawn as several path elements (nulls split it into segments): with
   *   the default objectBoundingBox each segment resolves the same offset
   *   against a different bbox, so the color transition lands on a different
   *   value in every segment.
   */ key: "drawGradient",
            value: function drawGradient(style, gfrom, gto, opacityFrom, opacityTo) {
                var size = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : null, stops = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : null, colorStops = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : [], i = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : 0, verticalUserSpace = arguments.length > 9 && arguments[9] !== void 0 ? arguments[9] : false;
                var w = this.w;
                var g;
                if (gfrom.length < 9 && gfrom.indexOf("#") === 0) {
                    gfrom = Utils$1.hexToRgba(gfrom, opacityFrom);
                }
                if (gto.length < 9 && gto.indexOf("#") === 0) {
                    gto = Utils$1.hexToRgba(gto, opacityTo);
                }
                var stop1 = 0;
                var stop2 = 1;
                var stop3 = 1;
                var stop4 = null;
                if (stops !== null) {
                    stop1 = typeof stops[0] !== "undefined" ? stops[0] / 100 : 0;
                    stop2 = typeof stops[1] !== "undefined" ? stops[1] / 100 : 1;
                    stop3 = typeof stops[2] !== "undefined" ? stops[2] / 100 : 1;
                    stop4 = typeof stops[3] !== "undefined" ? stops[3] / 100 : null;
                }
                var radial = !!(w.config.chart.type === "donut" || w.config.chart.type === "pie" || w.config.chart.type === "polarArea" || w.config.chart.type === "bubble");
                if (!colorStops || colorStops.length === 0) {
                    g = w.dom.Paper.gradient(radial ? "radial" : "linear", function(add) {
                        add.stop(stop1, gfrom, opacityFrom);
                        add.stop(stop2, gto, opacityTo);
                        add.stop(stop3, gto, opacityTo);
                        if (stop4 !== null) {
                            add.stop(stop4, gfrom, opacityFrom);
                        }
                    });
                } else {
                    g = w.dom.Paper.gradient(radial ? "radial" : "linear", function(add) {
                        var gradientStops = Array.isArray(colorStops[i]) ? colorStops[i] : Array.isArray(colorStops[0]) ? colorStops[0] || [] : colorStops;
                        gradientStops.forEach(function(s) {
                            add.stop(s.offset / 100, s.color, s.opacity);
                        });
                    });
                }
                if (!radial) {
                    if (style === "vertical") {
                        if (verticalUserSpace) {
                            g.attr({
                                gradientUnits: "userSpaceOnUse"
                            });
                            g.from(0, 0).to(0, w.layout.gridHeight);
                        } else {
                            g.from(0, 0).to(0, 1);
                        }
                    } else if (style === "diagonal") {
                        g.from(0, 0).to(1, 1);
                    } else if (style === "horizontal") {
                        g.from(0, 1).to(1, 1);
                    } else if (style === "diagonal2") {
                        g.from(1, 0).to(0, 1);
                    }
                } else {
                    var offx = w.layout.gridWidth / 2;
                    var offy = w.layout.gridHeight / 2;
                    if (w.config.chart.type !== "bubble") {
                        g.attr({
                            gradientUnits: "userSpaceOnUse",
                            cx: offx,
                            cy: offy,
                            r: size
                        });
                    } else {
                        g.attr({
                            cx: 0.5,
                            cy: 0.5,
                            r: 0.8,
                            fx: 0.2,
                            fy: 0.2
                        });
                    }
                }
                return g;
            }
        },
        {
            /** @param {{ text: any, maxWidth: any, fontSize: any, fontFamily?: any }} opts */ key: "getTextBasedOnMaxWidth",
            value: function getTextBasedOnMaxWidth(param) {
                var text = param.text, maxWidth = param.maxWidth, fontSize = param.fontSize, fontFamily = param.fontFamily;
                var tRects = this.getTextRects(text, fontSize, fontFamily, "");
                var wordWidth = tRects.width / text.length;
                var wordsBasedOnWidth = Math.floor(maxWidth / wordWidth);
                if (maxWidth < tRects.width) {
                    return text.slice(0, wordsBasedOnWidth - 3) + "...";
                }
                return text;
            }
        },
        {
            /**
   * @param {{ x: any, y: any, text: any, textAnchor?: any, fontSize?: any, fontFamily?: any, fontWeight?: any, foreColor?: any, opacity?: any, maxWidth?: any, cssClass?: string, isPlainText?: boolean, dominantBaseline?: string }} opts
   */ key: "drawText",
            value: function drawText(param) {
                var _this = this;
                var x = param.x, y = param.y, text = param.text, textAnchor = param.textAnchor, fontSize = param.fontSize, fontFamily = param.fontFamily, fontWeight = param.fontWeight, foreColor = param.foreColor, opacity = param.opacity, maxWidth = param.maxWidth, _param_cssClass = param.cssClass, cssClass = _param_cssClass === void 0 ? "" : _param_cssClass, _param_isPlainText = param.isPlainText, isPlainText = _param_isPlainText === void 0 ? true : _param_isPlainText, _param_dominantBaseline = param.dominantBaseline, dominantBaseline = _param_dominantBaseline === void 0 ? "auto" : _param_dominantBaseline;
                var w = this.w;
                if (typeof text === "undefined") text = "";
                var truncatedText = text;
                if (!textAnchor) {
                    textAnchor = "start";
                }
                if (!foreColor || !foreColor.length) {
                    foreColor = w.config.chart.foreColor;
                }
                fontFamily = fontFamily || w.config.chart.fontFamily;
                fontSize = fontSize || "11px";
                fontWeight = fontWeight || "regular";
                var commonProps = {
                    maxWidth: maxWidth,
                    fontSize: fontSize,
                    fontFamily: fontFamily
                };
                var elText;
                if (Array.isArray(text)) {
                    elText = w.dom.Paper.text(function(add) {
                        for(var i = 0; i < text.length; i++){
                            truncatedText = text[i];
                            if (maxWidth) {
                                truncatedText = _this.getTextBasedOnMaxWidth(__spreadValues({
                                    text: text[i]
                                }, commonProps));
                            }
                            i === 0 ? add.tspan(truncatedText) : add.tspan(truncatedText).newLine();
                        }
                    });
                } else {
                    if (maxWidth) {
                        truncatedText = this.getTextBasedOnMaxWidth(__spreadValues({
                            text: text
                        }, commonProps));
                    }
                    elText = isPlainText ? w.dom.Paper.plain(text) : /**
         * @param {any} add
         */ w.dom.Paper.text(function(add) {
                        return add.tspan(truncatedText);
                    });
                }
                elText.attr({
                    x: x,
                    y: y,
                    "text-anchor": textAnchor,
                    "dominant-baseline": dominantBaseline,
                    "font-size": fontSize,
                    "font-family": fontFamily,
                    "font-weight": fontWeight,
                    fill: foreColor,
                    class: "apexcharts-text " + cssClass
                });
                elText.node.style.fontFamily = fontFamily;
                elText.node.style.opacity = opacity;
                return elText;
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {string} type
   * @param {number} size
   */ key: "getMarkerPath",
            value: function getMarkerPath(x, y, type, size) {
                var CROSS_SHRINK = 1.4;
                var PLUS_SHRINK = 1.12;
                var STAR_GROW = 1.15;
                var SPARKLE_SHRINK = 1.1;
                var SQUARE_SHRINK = 1.125;
                var DIAMOND_GROW = 1.05;
                var LINE_SHRINK = 1.1;
                var CIRCLE_DIAMETER = 2;
                var d = "";
                switch(type){
                    case "cross":
                        size = size / CROSS_SHRINK;
                        d = "M ".concat(x - size, " ").concat(y - size, " L ").concat(x + size, " ").concat(y + size, "  M ").concat(x - size, " ").concat(y + size, " L ").concat(x + size, " ").concat(y - size);
                        break;
                    case "plus":
                        size = size / PLUS_SHRINK;
                        d = "M ".concat(x - size, " ").concat(y, " L ").concat(x + size, " ").concat(y, "  M ").concat(x, " ").concat(y - size, " L ").concat(x, " ").concat(y + size);
                        break;
                    case "star":
                    case "sparkle":
                        {
                            var points = 5;
                            size = size * STAR_GROW;
                            if (type === "sparkle") {
                                size = size / SPARKLE_SHRINK;
                                points = 4;
                            }
                            var step = Math.PI / points;
                            for(var i = 0; i <= 2 * points; i++){
                                var angle = i * step;
                                var radius = i % 2 === 0 ? size : size / 2;
                                var xPos = x + radius * Math.sin(angle);
                                var yPos = y - radius * Math.cos(angle);
                                d += (i === 0 ? "M" : "L") + xPos + "," + yPos;
                            }
                            d += "Z";
                            break;
                        }
                    case "triangle":
                        d = "M ".concat(x, " ").concat(y - size, " \n             L ").concat(x + size, " ").concat(y + size, " \n             L ").concat(x - size, " ").concat(y + size, " \n             Z");
                        break;
                    case "square":
                    case "rect":
                        size = size / SQUARE_SHRINK;
                        d = "M ".concat(x - size, " ").concat(y - size, " \n           L ").concat(x + size, " ").concat(y - size, " \n           L ").concat(x + size, " ").concat(y + size, " \n           L ").concat(x - size, " ").concat(y + size, " \n           Z");
                        break;
                    case "diamond":
                        size = size * DIAMOND_GROW;
                        d = "M ".concat(x, " ").concat(y - size, " \n             L ").concat(x + size, " ").concat(y, " \n             L ").concat(x, " ").concat(y + size, " \n             L ").concat(x - size, " ").concat(y, " \n            Z");
                        break;
                    case "line":
                        size = size / LINE_SHRINK;
                        d = "M ".concat(x - size, " ").concat(y, " \n           L ").concat(x + size, " ").concat(y);
                        break;
                    case "circle":
                    default:
                        size = size * CIRCLE_DIAMETER;
                        d = "M ".concat(x, ", ").concat(y, " \n           m -").concat(size / 2, ", 0 \n           a ").concat(size / 2, ",").concat(size / 2, " 0 1,0 ").concat(size, ",0 \n           a ").concat(size / 2, ",").concat(size / 2, " 0 1,0 -").concat(size, ",0");
                        break;
                }
                return d;
            }
        },
        {
            /**
   * @param {number} x - The x-coordinate of the marker
   * @param {number} y - The y-coordinate of the marker
   * @param {string} type - Marker shape type
   * @param {number} size - The size of the marker
   * @param {Record<string, any>} opts - The options for the marker
   * @returns {any} The created marker.
   */ key: "drawMarkerShape",
            value: function drawMarkerShape(x, y, type, size, opts) {
                var path = this.drawPath({
                    d: this.getMarkerPath(x, y, type, size),
                    stroke: opts.pointStrokeColor,
                    strokeDashArray: opts.pointStrokeDashArray,
                    strokeWidth: opts.pointStrokeWidth,
                    fill: opts.pointFillColor,
                    fillOpacity: opts.pointFillOpacity,
                    strokeOpacity: opts.pointStrokeOpacity
                });
                path.attr({
                    cx: x,
                    cy: y,
                    shape: opts.shape,
                    class: opts.class ? opts.class : ""
                });
                return path;
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {Record<string, any>} opts
   */ key: "drawMarker",
            value: function drawMarker(x, y, opts) {
                x = x || 0;
                var size = opts.pSize || 0;
                if (!Utils$1.isNumber(y)) {
                    size = 0;
                    y = 0;
                }
                return this.drawMarkerShape(x, y, opts == null ? void 0 : opts.shape, size, __spreadValues(__spreadValues({}, opts), opts.shape === "line" || opts.shape === "plus" || opts.shape === "cross" ? {
                    pointStrokeColor: opts.pointFillColor,
                    pointStrokeOpacity: opts.pointFillOpacity
                } : {}));
            }
        },
        {
            /**
   * @param {any} path
   * @param {Event | null} [e]
   */ key: "pathMouseEnter",
            value: function pathMouseEnter(path, e) {
                var _a, _b;
                var w = this.w;
                var filters = new Filters(this.w);
                var i = parseInt((_a = path.node.getAttribute("index")) != null ? _a : "", 10);
                var j = parseInt((_b = path.node.getAttribute("j")) != null ? _b : "", 10);
                if (isNaN(i) || isNaN(j)) return;
                if (typeof w.config.chart.events.dataPointMouseEnter === "function") {
                    w.config.chart.events.dataPointMouseEnter(e, this.ctx, {
                        seriesIndex: i,
                        dataPointIndex: j,
                        w: w
                    });
                }
                Graphics._fireEvent(w, "dataPointMouseEnter", [
                    e,
                    this.ctx,
                    {
                        seriesIndex: i,
                        dataPointIndex: j,
                        w: w
                    }
                ]);
                if (w.config.states.active.filter.type !== "none") {
                    if (path.node.getAttribute("selected") === "true") {
                        return;
                    }
                }
                if (w.config.states.hover.filter.type !== "none") {
                    if (!w.interact.isTouchDevice) {
                        var hoverFilter = w.config.states.hover.filter;
                        filters.applyFilter(path, i, hoverFilter.type, hoverFilter.value);
                    }
                }
            }
        },
        {
            /**
   * @param {any} path
   * @param {Event | null} [e]
   */ key: "pathMouseLeave",
            value: function pathMouseLeave(path, e) {
                var _a, _b;
                var w = this.w;
                var filters = new Filters(this.w);
                var i = parseInt((_a = path.node.getAttribute("index")) != null ? _a : "", 10);
                var j = parseInt((_b = path.node.getAttribute("j")) != null ? _b : "", 10);
                if (isNaN(i) || isNaN(j)) return;
                if (typeof w.config.chart.events.dataPointMouseLeave === "function") {
                    w.config.chart.events.dataPointMouseLeave(e, this.ctx, {
                        seriesIndex: i,
                        dataPointIndex: j,
                        w: w
                    });
                }
                Graphics._fireEvent(w, "dataPointMouseLeave", [
                    e,
                    this.ctx,
                    {
                        seriesIndex: i,
                        dataPointIndex: j,
                        w: w
                    }
                ]);
                if (w.config.states.active.filter.type !== "none") {
                    if (path.node.getAttribute("selected") === "true") {
                        return;
                    }
                }
                if (w.config.states.hover.filter.type !== "none") {
                    filters.getDefaultFilter(path, i);
                }
            }
        },
        {
            /**
   * Clear every selected data point (single-select mode): reset the selection
   * array and restore the default filter on all series path/circle/rect nodes.
   * NOTE: preserves the original behavior of passing the clicked series index
   * `i` to getDefaultFilter for every element (not each element's own index).
   * @param {any} filters @param {number} i
   */ key: "_clearAllDataPointSelections",
            value: function _clearAllDataPointSelections(filters, i) {
                var w = this.w;
                w.interact.selectedDataPoints = [];
                var elPaths = w.dom.Paper.find(".apexcharts-series path:not(.apexcharts-decoration-element)");
                var elCircles = w.dom.Paper.find(".apexcharts-series circle:not(.apexcharts-decoration-element), .apexcharts-series rect:not(.apexcharts-decoration-element)");
                var deSelect = function deSelect(els) {
                    Array.prototype.forEach.call(els, function(el) {
                        el.node.setAttribute("selected", "false");
                        filters.getDefaultFilter(el, i);
                    });
                };
                deSelect(elPaths);
                deSelect(elCircles);
            }
        },
        {
            /**
   * Toggle the selected state of a single data point. Mutates
   * `w.interact.selectedDataPoints` and the path's `selected` attribute,
   * honoring single- vs multi-select mode. Returns the new selected flag.
   * @param {any} path @param {any} filters @param {number} i @param {number} j
   * @returns {'true' | 'false'}
   */ key: "_togglePointSelection",
            value: function _togglePointSelection(path, filters, i, j) {
                var w = this.w;
                if (path.node.getAttribute("selected") === "true") {
                    path.node.setAttribute("selected", "false");
                    var index = w.interact.selectedDataPoints[i].indexOf(j);
                    if (index > -1) {
                        w.interact.selectedDataPoints[i].splice(index, 1);
                    }
                    return "false";
                }
                if (!w.config.states.active.allowMultipleDataPointsSelection && w.interact.selectedDataPoints.length > 0) {
                    this._clearAllDataPointSelections(filters, i);
                }
                path.node.setAttribute("selected", "true");
                if (typeof w.interact.selectedDataPoints[i] === "undefined") {
                    w.interact.selectedDataPoints[i] = [];
                }
                w.interact.selectedDataPoints[i].push(j);
                return "true";
            }
        },
        {
            /**
   * Apply the active / hover / default state filter after a selection toggle,
   * matching the original inline branching.
   * @param {any} path @param {any} filters @param {number} i
   * @param {'true' | 'false'} selected
   */ key: "_applyPointSelectionFilter",
            value: function _applyPointSelectionFilter(path, filters, i, selected) {
                var w = this.w;
                if (selected === "true") {
                    var activeFilter = w.config.states.active.filter;
                    if (activeFilter !== "none") {
                        filters.applyFilter(path, i, activeFilter.type, activeFilter.value);
                    } else {
                        if (w.config.states.hover.filter !== "none") {
                            if (!w.interact.isTouchDevice) {
                                var hoverFilter = w.config.states.hover.filter;
                                filters.applyFilter(path, i, hoverFilter.type, hoverFilter.value);
                            }
                        }
                    }
                } else {
                    if (w.config.states.active.filter.type !== "none") {
                        if (w.config.states.hover.filter.type !== "none" && !w.interact.isTouchDevice) {
                            var hoverFilter1 = w.config.states.hover.filter;
                            filters.applyFilter(path, i, hoverFilter1.type, hoverFilter1.value);
                        } else {
                            filters.getDefaultFilter(path, i);
                        }
                    }
                }
            }
        },
        {
            /**
   * @param {any} path
   * @param {Event | null} e
   */ key: "pathMouseDown",
            value: function pathMouseDown(path, e) {
                var _a, _b;
                var w = this.w;
                var filters = new Filters(this.w);
                var i = parseInt((_a = path.node.getAttribute("index")) != null ? _a : "", 10);
                var j = parseInt((_b = path.node.getAttribute("j")) != null ? _b : "", 10);
                if (isNaN(i) || isNaN(j)) return;
                var link = w.config.chart.link;
                var crossfilterClick = !!(link && (typeof link.dimension === "function" || link.enabled));
                if (!crossfilterClick) {
                    var selected = this._togglePointSelection(path, filters, i, j);
                    this._applyPointSelectionFilter(path, filters, i, selected);
                }
                if (typeof w.config.chart.events.dataPointSelection === "function") {
                    w.config.chart.events.dataPointSelection(e, this.ctx, {
                        selectedDataPoints: w.interact.selectedDataPoints,
                        seriesIndex: i,
                        dataPointIndex: j,
                        w: w
                    });
                }
                if (e) {
                    Graphics._fireEvent(w, "dataPointSelection", [
                        e,
                        this.ctx,
                        {
                            selectedDataPoints: w.interact.selectedDataPoints,
                            seriesIndex: i,
                            dataPointIndex: j,
                            w: w
                        }
                    ]);
                }
            }
        },
        {
            /**
   * @param {any} el
   * @returns {{ x: number, y: number }}
   */ key: "rotateAroundCenter",
            value: function rotateAroundCenter(el) {
                var coord = /** @type {any} */ {};
                if (el && typeof el.getBBox === "function") {
                    coord = el.getBBox();
                }
                var x = coord.x + coord.width / 2;
                var y = coord.y + coord.height / 2;
                return {
                    x: x,
                    y: y
                };
            }
        },
        {
            /**
   * Sets up event delegation on a parent group element.
   * Uses mouseover/mouseout (which bubble) to simulate mouseenter/mouseleave
   * on matching child elements, reducing per-element listener overhead.
   * @param {any} parentGroup
   * @param {string} targetSelector
   */ key: "setupEventDelegation",
            value: function setupEventDelegation(parentGroup, targetSelector) {
                var _this = this;
                var currentHovered = null;
                parentGroup.node.addEventListener("mouseover", function(e) {
                    var targetNode = Graphics._findDelegateTarget(e.target, parentGroup.node, targetSelector);
                    if (!targetNode || targetNode === currentHovered) return;
                    if (currentHovered && /** @type {any} */ currentHovered.instance) {
                        _this.pathMouseLeave(/** @type {any} */ currentHovered.instance, e);
                    }
                    currentHovered = targetNode;
                    if (targetNode.instance) {
                        _this.pathMouseEnter(targetNode.instance, e);
                    }
                });
                parentGroup.node.addEventListener("mouseout", function(e) {
                    if (!currentHovered) return;
                    var relatedNode = e.relatedTarget ? Graphics._findDelegateTarget(e.relatedTarget, parentGroup.node, targetSelector) : null;
                    if (relatedNode !== currentHovered) {
                        if (currentHovered && /** @type {any} */ currentHovered.instance) {
                            _this.pathMouseLeave(/** @type {any} */ currentHovered.instance, e);
                        }
                        currentHovered = null;
                    }
                });
                parentGroup.node.addEventListener("mousedown", function(e) {
                    var targetNode = Graphics._findDelegateTarget(e.target, parentGroup.node, targetSelector);
                    if (targetNode && targetNode.instance) {
                        _this.pathMouseDown(targetNode.instance, e);
                    }
                });
            }
        },
        {
            /**
   * @param {string} text
   * @param {string} fontSize
   * @param {string | null | undefined} [fontFamily]
   * @param {string} [transform]
   * @returns {{ width: number, height: number }}
   */ key: "getTextRects",
            value: function getTextRects(text, fontSize, fontFamily, transform) {
                var useBBox = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : true;
                var w = this.w;
                var cacheKey = [
                    text,
                    fontSize,
                    fontFamily,
                    transform,
                    useBBox
                ].join("\0");
                var cache = w.globals.textRectsCache;
                if (cache && cache.has(cacheKey)) {
                    return /** @type {{ width: number, height: number }} */ cache.get(cacheKey);
                }
                var virtualText = this.drawText({
                    x: -200,
                    y: -200,
                    text: text,
                    textAnchor: "start",
                    fontSize: fontSize,
                    fontFamily: fontFamily,
                    foreColor: "#fff",
                    opacity: 0
                });
                if (transform) {
                    virtualText.attr("transform", transform);
                }
                w.dom.Paper.add(virtualText);
                var rect = virtualText.bbox();
                var bboxY = rect.y;
                if (!useBBox) {
                    rect = virtualText.node.getBoundingClientRect();
                }
                virtualText.remove();
                var result = {
                    width: rect.width,
                    height: rect.height,
                    // Offset from the text element's `y` (alphabetic baseline) to the
                    // bbox vertical center. For most fonts this is NEGATIVE (bbox center
                    // sits above the baseline because the ascender is taller than the
                    // descender). Use as: `text_y = desired_visual_center_y - centerOffset`.
                    // Only populated when useBBox=true (the default).
                    centerOffset: useBBox ? bboxY + rect.height / 2 - -200 : 0
                };
                if (cache) {
                    cache.set(cacheKey, result);
                }
                return result;
            }
        },
        {
            /**
   * append ... to long text
   * http://stackoverflow.com/questions/9241315/trimming-text-to-a-given-pixel-width-in-svg
   * @memberof Graphics
   * @param {Record<string, any>} textObj
   * @param {string} textString
   * @param {number} width
   **/ key: "placeTextWithEllipsis",
            value: function placeTextWithEllipsis(textObj, textString, width) {
                if (typeof textObj.getComputedTextLength !== "function") return;
                textObj.textContent = textString;
                if (textString.length > 0) {
                    if (textObj.getComputedTextLength() >= width / 1.1) {
                        for(var x = textString.length - 3; x > 0; x -= 3){
                            if (textObj.getSubStringLength(0, x) <= width / 1.1) {
                                textObj.textContent = textString.substring(0, x) + "...";
                                return;
                            }
                        }
                        textObj.textContent = ".";
                    }
                }
            }
        }
    ], [
        {
            key: "_fireEvent",
            value: // Fire a named event from w.globals.events without requiring a ctx reference.
            // Mirrors Events.fireEvent() but reads the registry directly from w so that
            // pathMouseEnter/Leave/Down work even when this.ctx is null (Graphics instances
            // created without a ctx arg for drawing-only use cases).
            /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {string} name
   * @param {any[]} args
   */ function _fireEvent(w, name2, args) {
                var evs = w.globals.events;
                if (!evs || !Object.prototype.hasOwnProperty.call(evs, name2)) return;
                var handlers = /** @type {Record<string,any>} */ evs[name2];
                for(var i = 0; i < handlers.length; i++){
                    handlers[i].apply(null, args);
                }
            }
        },
        {
            key: "_findDelegateTarget",
            value: /**
   * @param {any} node
   * @param {Record<string, any>} boundary
   * @param {string} selector
   */ function _findDelegateTarget(node, boundary, selector) {
                while(node && node !== boundary && node !== document){
                    if (node.matches && node.matches(selector)) return node;
                    node = node.parentNode;
                }
                return null;
            }
        },
        {
            key: "setAttrs",
            value: /**
   * @param {any} el
   * @param {Record<string, any>} attrs
   */ function setAttrs(el, attrs) {
                for(var key in attrs){
                    if (Object.prototype.hasOwnProperty.call(attrs, key)) {
                        el.setAttribute(key, attrs[key]);
                    }
                }
            }
        }
    ]);
    return Graphics;
}();
var Fill = /*#__PURE__*/ function() {
    "use strict";
    function Fill(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Fill);
        this.w = w;
        this.opts = null;
        this.seriesIndex = 0;
        this.patternIDs = [];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Fill, [
        {
            /**
   * @param {Record<string, any>} params
   */ key: "clippedImgArea",
            value: function clippedImgArea(params) {
                var w = this.w;
                var cnf = w.config;
                var svgW = parseInt(String(w.layout.gridWidth), 10);
                var svgH = parseInt(String(w.layout.gridHeight), 10);
                var size = svgW > svgH ? svgW : svgH;
                var fillImg = params.image;
                var imgWidth = 0;
                var imgHeight = 0;
                if (typeof params.width === "undefined" && typeof params.height === "undefined") {
                    if (cnf.fill.image.width !== void 0 && cnf.fill.image.height !== void 0) {
                        imgWidth = cnf.fill.image.width + 1;
                        imgHeight = cnf.fill.image.height;
                    } else {
                        imgWidth = size + 1;
                        imgHeight = size;
                    }
                } else {
                    imgWidth = params.width;
                    imgHeight = params.height;
                }
                var elPattern = BrowserAPIs.createElementNS(SVGNS$2, "pattern");
                Graphics.setAttrs(elPattern, {
                    id: params.patternID,
                    patternUnits: params.patternUnits ? params.patternUnits : "userSpaceOnUse",
                    width: imgWidth + "px",
                    height: imgHeight + "px"
                });
                var elImage = BrowserAPIs.createElementNS(SVGNS$2, "image");
                elPattern.appendChild(elImage);
                var SVGLib = Environment.isBrowser() ? /** @type {any} */ window.SVG : /** @type {any} */ /*TURBOPACK member replacement*/ __turbopack_context__.g.SVG;
                elImage.setAttributeNS(SVGLib.xlink, "href", fillImg);
                Graphics.setAttrs(elImage, {
                    x: 0,
                    y: 0,
                    preserveAspectRatio: "none",
                    width: imgWidth + "px",
                    height: imgHeight + "px"
                });
                elImage.style.opacity = params.opacity;
                w.dom.elDefs.node.appendChild(elPattern);
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "getSeriesIndex",
            value: function getSeriesIndex(opts) {
                var w = this.w;
                var cType = w.config.chart.type;
                if ((cType === "bar" || cType === "rangeBar") && w.config.plotOptions.bar.distributed || cType === "heatmap" || cType === "treemap") {
                    this.seriesIndex = opts.seriesNumber;
                } else {
                    this.seriesIndex = opts.seriesNumber % w.seriesData.series.length;
                }
                return this.seriesIndex;
            }
        },
        {
            /**
   * The y-axis window a series is plotted against. This is the space the
   * threshold gradient is measured in, since that gradient spans the plot area.
   *
   * @param {number} realIndex
   */ key: "getSeriesAxisRange",
            value: function getSeriesAxisRange(realIndex) {
                var _a, _b, _c;
                var w = this.w;
                var yaxisIndex = (_b = (_a = w.globals.seriesYAxisReverseMap) == null ? void 0 : _a[realIndex]) != null ? _b : 0;
                return {
                    minY: Utils$1.isNumber(w.globals.minYArr[realIndex]) ? w.globals.minYArr[realIndex] : w.globals.minY,
                    maxY: Utils$1.isNumber(w.globals.maxYArr[realIndex]) ? w.globals.maxYArr[realIndex] : w.globals.maxY,
                    reversed: !!((_c = w.config.yaxis[yaxisIndex]) == null ? void 0 : _c.reversed)
                };
            }
        },
        {
            /**
   * Builds the two stops that split a series into an above- and a
   * below-threshold color.
   *
   * The stops are painted into a vertical gradient anchored to the plot area
   * (see Graphics.drawGradient's verticalUserSpace branch), so the boundary is
   * positioned over the *axis* range. Deriving it from the data range instead
   * puts the transition on the wrong value whenever the axis extends past the
   * data, which is the common case: an explicit yaxis.min/max, a nice scale, or
   * an axis shared with another series.
   *
   * @param {{ minY: number, maxY: number, reversed?: boolean }} axisRange
   * @param {Record<string, any>} multiColorConfig
   */ key: "computeColorStops",
            value: function computeColorStops(axisRange, multiColorConfig) {
                var w = this.w;
                var threshold = multiColorConfig.threshold, colorAboveThreshold = multiColorConfig.colorAboveThreshold, colorBelowThreshold = multiColorConfig.colorBelowThreshold;
                var minY = axisRange.minY, maxY = axisRange.maxY;
                var span = maxY - minY;
                var offset = span === 0 ? threshold > maxY ? 0 : 100 : (maxY - threshold) / span * 100;
                if (axisRange.reversed) {
                    offset = 100 - offset;
                }
                offset = Math.max(0, Math.min(offset, 100));
                var fillOpacity = Array.isArray(w.config.fill.opacity) ? w.config.fill.opacity[this.seriesIndex] : w.config.fill.opacity;
                var above = {
                    offset: offset,
                    color: colorAboveThreshold,
                    opacity: fillOpacity
                };
                var below = {
                    offset: offset,
                    color: colorBelowThreshold,
                    opacity: fillOpacity
                };
                return axisRange.reversed ? [
                    below,
                    above
                ] : [
                    above,
                    below
                ];
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "fillPath",
            value: function fillPath(opts) {
                var _a, _b, _c, _d;
                var w = this.w;
                this.opts = opts;
                var cnf = this.w.config;
                var pathFill;
                var patternFill, gradientFill;
                this.seriesIndex = this.getSeriesIndex(opts);
                var drawMultiColorLine = cnf.plotOptions.line.colors.colorAboveThreshold && cnf.plotOptions.line.colors.colorBelowThreshold;
                var fillColors = this.getFillColors();
                var fillColor = fillColors[this.seriesIndex];
                if (w.seriesData.seriesColors[this.seriesIndex] !== void 0) {
                    fillColor = w.seriesData.seriesColors[this.seriesIndex];
                }
                if (typeof fillColor === "function") {
                    fillColor = fillColor({
                        seriesIndex: this.seriesIndex,
                        dataPointIndex: opts.dataPointIndex,
                        value: opts.value,
                        w: w
                    });
                }
                var fillType = opts.fillType ? opts.fillType : this.getFillType(this.seriesIndex);
                var fillOpacity = Array.isArray(cnf.fill.opacity) ? cnf.fill.opacity[this.seriesIndex] : cnf.fill.opacity;
                var useGradient = fillType === "gradient" || drawMultiColorLine;
                if (opts.color) {
                    fillColor = opts.color;
                }
                var seriesItem = /** @type {Record<string,any>} */ w.config.series[this.seriesIndex];
                if ((_b = (_a = seriesItem == null ? void 0 : seriesItem.data) == null ? void 0 : _a[opts.dataPointIndex]) == null ? void 0 : _b.fillColor) {
                    fillColor = (_d = (_c = seriesItem == null ? void 0 : seriesItem.data) == null ? void 0 : _c[opts.dataPointIndex]) == null ? void 0 : _d.fillColor;
                }
                if (!fillColor) {
                    fillColor = "#fff";
                    console.warn("undefined color - ApexCharts");
                }
                if (opts.opacity !== void 0 && opts.opacity !== null) fillOpacity = opts.opacity;
                var defaultColor = fillColor;
                if (Utils$1.isCSSVariable(fillColor)) {
                    defaultColor = Utils$1.applyOpacityToColor(fillColor, fillOpacity);
                } else if (fillColor.indexOf("rgb") === -1) {
                    if (fillColor.indexOf("#") === -1) {
                        defaultColor = fillColor;
                    } else if (fillColor.length < 9) {
                        defaultColor = Utils$1.hexToRgba(fillColor, fillOpacity);
                    }
                } else {
                    if (fillColor.indexOf("rgba") > -1) {
                        fillOpacity = Utils$1.getOpacityFromRGBA(fillColor);
                    } else {
                        defaultColor = Utils$1.hexToRgba(Utils$1.rgb2hex(fillColor), fillOpacity);
                    }
                }
                var resolvedFillColor = Utils$1.isCSSVariable(fillColor) ? Utils$1.getThemeColor(fillColor) : fillColor;
                if (fillType === "pattern") {
                    patternFill = this.handlePatternFill({
                        fillConfig: opts.fillConfig,
                        patternFill: patternFill,
                        fillColor: resolvedFillColor,
                        defaultColor: defaultColor
                    });
                }
                if (useGradient) {
                    var colorStops = cnf.fill.gradient.colorStops ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(cnf.fill.gradient.colorStops) : [];
                    var type = cnf.fill.gradient.type;
                    if (drawMultiColorLine) {
                        colorStops[this.seriesIndex] = this.computeColorStops(this.getSeriesAxisRange(this.seriesIndex), cnf.plotOptions.line.colors);
                        type = "vertical";
                    }
                    gradientFill = this.handleGradientFill({
                        type: type,
                        fillConfig: opts.fillConfig,
                        fillColor: resolvedFillColor,
                        fillOpacity: fillOpacity,
                        colorStops: colorStops,
                        i: this.seriesIndex,
                        // Threshold stops are positioned over the axis range, so the gradient
                        // has to be anchored to the plot area to match. This also keeps every
                        // segment of a null-split series on one shared coordinate space.
                        verticalUserSpace: drawMultiColorLine
                    });
                }
                if (fillType === "image") {
                    var imgSrc = cnf.fill.image.src;
                    var patternID = opts.patternID ? opts.patternID : "";
                    var patternKey = "pattern".concat(w.globals.cuid).concat(opts.seriesNumber + 1).concat(patternID);
                    if (this.patternIDs.indexOf(patternKey) === -1) {
                        this.clippedImgArea({
                            opacity: fillOpacity,
                            image: Array.isArray(imgSrc) ? opts.seriesNumber < imgSrc.length ? imgSrc[opts.seriesNumber] : imgSrc[0] : imgSrc,
                            width: opts.width ? opts.width : void 0,
                            height: opts.height ? opts.height : void 0,
                            patternUnits: opts.patternUnits,
                            patternID: patternKey
                        });
                        this.patternIDs.push(patternKey);
                    }
                    pathFill = "url(#".concat(patternKey, ")");
                } else if (useGradient) {
                    pathFill = gradientFill;
                } else if (fillType === "pattern") {
                    pathFill = patternFill;
                } else {
                    pathFill = defaultColor;
                }
                if (opts.solid) {
                    pathFill = defaultColor;
                }
                return pathFill;
            }
        },
        {
            /**
   * @param {number} seriesIndex
   */ key: "getFillType",
            value: function getFillType(seriesIndex) {
                var w = this.w;
                if (Array.isArray(w.config.fill.type)) {
                    return w.config.fill.type[seriesIndex];
                } else {
                    return w.config.fill.type;
                }
            }
        },
        {
            key: "getFillColors",
            value: function getFillColors() {
                var w = this.w;
                var cnf = w.config;
                var opts = this.opts;
                var fillColors = [];
                if (w.globals.comboCharts) {
                    if (/** @type {Record<string,any>} */ w.config.series[this.seriesIndex].type === "line") {
                        if (Array.isArray(w.globals.stroke.colors)) {
                            fillColors = w.globals.stroke.colors;
                        } else {
                            fillColors.push(w.globals.stroke.colors);
                        }
                    } else {
                        if (Array.isArray(w.globals.fill.colors)) {
                            fillColors = w.globals.fill.colors;
                        } else {
                            fillColors.push(w.globals.fill.colors);
                        }
                    }
                } else {
                    if (cnf.chart.type === "line") {
                        if (Array.isArray(w.globals.stroke.colors)) {
                            fillColors = w.globals.stroke.colors;
                        } else {
                            fillColors.push(w.globals.stroke.colors);
                        }
                    } else {
                        if (Array.isArray(w.globals.fill.colors)) {
                            fillColors = w.globals.fill.colors;
                        } else {
                            fillColors.push(w.globals.fill.colors);
                        }
                    }
                }
                if (typeof opts.fillColors !== "undefined") {
                    fillColors = [];
                    if (Array.isArray(opts.fillColors)) {
                        fillColors = opts.fillColors.slice();
                    } else {
                        fillColors.push(opts.fillColors);
                    }
                }
                return fillColors;
            }
        },
        {
            /** @param {{fillConfig: any, patternFill: any, fillColor: any, defaultColor: any}} opts */ key: "handlePatternFill",
            value: function handlePatternFill(param) {
                var fillConfig = param.fillConfig, patternFill = param.patternFill, fillColor = param.fillColor, defaultColor = param.defaultColor;
                var fillCnf = this.w.config.fill;
                if (fillConfig) {
                    fillCnf = fillConfig;
                }
                var opts = this.opts;
                var graphics = new Graphics(this.w);
                var patternStrokeWidth = Array.isArray(fillCnf.pattern.strokeWidth) ? fillCnf.pattern.strokeWidth[this.seriesIndex] : fillCnf.pattern.strokeWidth;
                var patternLineColor = fillColor;
                if (Array.isArray(fillCnf.pattern.style)) {
                    if (typeof fillCnf.pattern.style[opts.seriesNumber] !== "undefined") {
                        var pf = graphics.drawPattern(fillCnf.pattern.style[opts.seriesNumber], fillCnf.pattern.width, fillCnf.pattern.height, patternLineColor, patternStrokeWidth);
                        patternFill = pf;
                    } else {
                        patternFill = defaultColor;
                    }
                } else {
                    patternFill = graphics.drawPattern(fillCnf.pattern.style, fillCnf.pattern.width, fillCnf.pattern.height, patternLineColor, patternStrokeWidth);
                }
                return patternFill;
            }
        },
        {
            key: "handleGradientFill",
            value: function handleGradientFill(param) {
                var type = param.type, fillColor = param.fillColor, fillOpacity = param.fillOpacity, fillConfig = param.fillConfig, colorStops = param.colorStops, i = param.i, _param_verticalUserSpace = param.verticalUserSpace, verticalUserSpace = _param_verticalUserSpace === void 0 ? false : _param_verticalUserSpace;
                var fillCnf = this.w.config.fill;
                if (fillConfig) {
                    fillCnf = __spreadValues(__spreadValues({}, fillCnf), fillConfig);
                }
                var opts = this.opts;
                var graphics = new Graphics(this.w);
                var utils = new Utils$1();
                type = type || fillCnf.gradient.type;
                var gradientFrom = fillColor;
                var gradientTo;
                var opacityFrom = fillCnf.gradient.opacityFrom === void 0 ? fillOpacity : Array.isArray(fillCnf.gradient.opacityFrom) ? fillCnf.gradient.opacityFrom[i] : fillCnf.gradient.opacityFrom;
                if (gradientFrom.indexOf("rgba") > -1) {
                    opacityFrom = Utils$1.getOpacityFromRGBA(gradientFrom);
                }
                var opacityTo = fillCnf.gradient.opacityTo === void 0 ? fillOpacity : Array.isArray(fillCnf.gradient.opacityTo) ? fillCnf.gradient.opacityTo[i] : fillCnf.gradient.opacityTo;
                if (fillCnf.gradient.gradientToColors === void 0 || fillCnf.gradient.gradientToColors.length === 0) {
                    if (fillCnf.gradient.shade === "dark") {
                        gradientTo = utils.shadeColor(parseFloat(fillCnf.gradient.shadeIntensity) * -1, fillColor.indexOf("rgb") > -1 ? Utils$1.rgb2hex(fillColor) : fillColor);
                    } else {
                        gradientTo = utils.shadeColor(parseFloat(fillCnf.gradient.shadeIntensity), fillColor.indexOf("rgb") > -1 ? Utils$1.rgb2hex(fillColor) : fillColor);
                    }
                } else {
                    if (fillCnf.gradient.gradientToColors[opts.seriesNumber]) {
                        var gToColor = fillCnf.gradient.gradientToColors[opts.seriesNumber];
                        gradientTo = gToColor;
                        if (gToColor.indexOf("rgba") > -1) {
                            opacityTo = Utils$1.getOpacityFromRGBA(gToColor);
                        }
                    } else {
                        gradientTo = fillColor;
                    }
                }
                if (fillCnf.gradient.gradientFrom) {
                    gradientFrom = fillCnf.gradient.gradientFrom;
                }
                if (fillCnf.gradient.gradientTo) {
                    gradientTo = fillCnf.gradient.gradientTo;
                }
                if (fillCnf.gradient.inverseColors) {
                    var t = gradientFrom;
                    gradientFrom = gradientTo;
                    gradientTo = t;
                }
                if (gradientFrom.indexOf("rgb") > -1) {
                    gradientFrom = Utils$1.rgb2hex(gradientFrom);
                }
                if (gradientTo.indexOf("rgb") > -1) {
                    gradientTo = Utils$1.rgb2hex(gradientTo);
                }
                return graphics.drawGradient(type, gradientFrom, gradientTo, opacityFrom, opacityTo, opts.size, fillCnf.gradient.stops, colorStops, i, verticalUserSpace);
            }
        }
    ]);
    return Fill;
}();
var OK_FILTER_TYPES = [
    "none",
    "lighten",
    "darken"
];
function seriesEmitter(ctx, graphics) {
    var r = ctx && ctx.renderer;
    return r && r.kind && r.kind !== "svg" ? r : graphics;
}
function computeMarkCount(w) {
    var series = w.config.series || [];
    var type = w.config.chart.type;
    var scatterish = type === "scatter" || type === "bubble";
    var markerSize = w.config.markers && w.config.markers.size;
    var markersOn = Array.isArray(markerSize) ? markerSize.some(function(s) {
        return s > 0;
    }) : (markerSize || 0) > 0;
    var labelsOn = !!(w.config.dataLabels && w.config.dataLabels.enabled);
    var isHeatmap = type === "heatmap";
    var total = 0;
    var maxLen = 0;
    series.forEach(function(s) {
        var n = Array.isArray(s.data) ? s.data.length : 0;
        if (n > maxLen) maxLen = n;
        if (scatterish || markersOn || isHeatmap) total += n;
        if (labelsOn) total += n;
    });
    var LARGE_D = 5e4;
    if (maxLen >= LARGE_D) total = Math.max(total, maxLen);
    return total;
}
function hasCanvasUnsupportedFeature(w) {
    var _a, _b;
    var fillType = w.config.fill && w.config.fill.type;
    var isUnsupportedFill = function isUnsupportedFill(t) {
        return t === "pattern" || t === "image" || t === "gradient";
    };
    if (Array.isArray(fillType) ? fillType.some(isUnsupportedFill) : isUnsupportedFill(fillType)) {
        return true;
    }
    var lineColors = (_b = (_a = w.config.plotOptions) == null ? void 0 : _a.line) == null ? void 0 : _b.colors;
    if (lineColors && lineColors.colorAboveThreshold && lineColors.colorBelowThreshold) {
        return true;
    }
    var states = w.config.states || {};
    var hoverFilter = states.hover && states.hover.filter && states.hover.filter.type;
    var activeFilter = states.active && states.active.filter && states.active.filter.type;
    if (hoverFilter && !OK_FILTER_TYPES.includes(hoverFilter)) return true;
    if (activeFilter && !OK_FILTER_TYPES.includes(activeFilter)) return true;
    return false;
}
var Markers = /*#__PURE__*/ function() {
    "use strict";
    function Markers(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Markers);
        this.w = w;
        this.ctx = ctx;
        this._filters = new Filters(this.w);
        this._graphics = new Graphics(this.w, this.ctx);
        this._seriesWrap = null;
        this._seriesWrapIndex = -1;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Markers, [
        {
            /**
   * Invalidate the cached per-series wrap group. Callers that drive
   * plotChartMarkers point-by-point (Line) must call this when a series'
   * element tree is (re)created, so a later render never appends markers to a
   * detached group from the previous pass.
   */ key: "resetSeriesWrapCache",
            value: function resetSeriesWrapCache() {
                this._seriesWrap = null;
                this._seriesWrapIndex = -1;
            }
        },
        {
            key: "setGlobalMarkerSize",
            value: function setGlobalMarkerSize() {
                var w = this.w;
                w.globals.markers.size = Array.isArray(w.config.markers.size) ? w.config.markers.size : [
                    w.config.markers.size
                ];
                if (w.globals.markers.size.length > 0) {
                    if (w.globals.markers.size.length < w.seriesData.series.length + 1) {
                        for(var i = 0; i <= w.seriesData.series.length; i++){
                            if (typeof w.globals.markers.size[i] === "undefined") {
                                w.globals.markers.size.push(w.globals.markers.size[0]);
                            }
                        }
                    }
                } else {
                    w.globals.markers.size = w.config.series.map(function() {
                        return /** @type {number} */ w.config.markers.size;
                    });
                }
            }
        },
        {
            /** @param {{ pointsPos?: any, seriesIndex?: any, j?: any, pSize?: any, alwaysDrawMarker?: boolean, isVirtualPoint?: boolean }} opts */ key: "plotChartMarkers",
            value: function plotChartMarkers(param) {
                var pointsPos = param.pointsPos, seriesIndex = param.seriesIndex, j = param.j, pSize = param.pSize, _param_alwaysDrawMarker = param.alwaysDrawMarker, alwaysDrawMarker = _param_alwaysDrawMarker === void 0 ? false : _param_alwaysDrawMarker, _param_isVirtualPoint = param.isVirtualPoint, isVirtualPoint = _param_isVirtualPoint === void 0 ? false : _param_isVirtualPoint;
                var w = this.w;
                var i = seriesIndex;
                var p = pointsPos;
                var elMarkersWrap = null;
                var graphics = new Graphics(this.w);
                var emit = seriesEmitter(this.ctx, graphics);
                var hasDiscreteMarkers = w.config.markers.discrete && w.config.markers.discrete.length;
                if (Array.isArray(p.x)) {
                    for(var q = 0; q < p.x.length; q++){
                        var markerElement = void 0;
                        var dataPointIndex = j;
                        var invalidMarker = !Utils$1.isNumber(p.y[q]);
                        if (w.globals.markers.largestSize === 0 && w.globals.hasNullValues && w.seriesData.series[i][j + 1] !== null && !isVirtualPoint) {
                            invalidMarker = true;
                        }
                        if (j === 1 && q === 0) dataPointIndex = 0;
                        if (j === 1 && q === 1) dataPointIndex = 1;
                        var markerClasses = "apexcharts-marker";
                        if ((w.config.chart.type === "line" || w.config.chart.type === "area") && !w.globals.comboCharts && !w.config.tooltip.intersect) {
                            markerClasses += " no-pointer-events";
                        }
                        var shouldMarkerDraw = Array.isArray(w.config.markers.size) ? w.globals.markers.size[seriesIndex] > 0 : w.config.markers.size > 0;
                        if (shouldMarkerDraw || alwaysDrawMarker || hasDiscreteMarkers) {
                            if (emit.kind === "canvas") {
                                if (typeof w.globals.pointsArray[seriesIndex] === "undefined") {
                                    w.globals.pointsArray[seriesIndex] = [];
                                }
                                w.globals.pointsArray[seriesIndex][dataPointIndex] = [
                                    p.x[q],
                                    p.y[q]
                                ];
                            }
                            if (!invalidMarker) {
                                markerClasses += " w".concat(Utils$1.randomId());
                            }
                            var opts = this.getMarkerConfig({
                                cssClass: markerClasses,
                                seriesIndex: seriesIndex,
                                dataPointIndex: dataPointIndex
                            });
                            var _si = /** @type {Record<string,any>} */ w.config.series[i];
                            if (_si.data[dataPointIndex]) {
                                if (_si.data[dataPointIndex].fillColor) {
                                    opts.pointFillColor = _si.data[dataPointIndex].fillColor;
                                }
                                if (_si.data[dataPointIndex].strokeColor) {
                                    opts.pointStrokeColor = _si.data[dataPointIndex].strokeColor;
                                }
                            }
                            if (typeof pSize !== "undefined") {
                                opts.pSize = pSize;
                            }
                            if (p.x[q] < -w.globals.markers.largestSize || p.x[q] > w.layout.gridWidth + w.globals.markers.largestSize || p.y[q] < -w.globals.markers.largestSize || p.y[q] > w.layout.gridHeight + w.globals.markers.largestSize) {
                                opts.pSize = 0;
                            }
                            if (!invalidMarker) {
                                var shouldCreateMarkerWrap = w.globals.markers.size[seriesIndex] > 0 || alwaysDrawMarker || hasDiscreteMarkers;
                                if (shouldCreateMarkerWrap && !elMarkersWrap) {
                                    var standardWrap = !alwaysDrawMarker && !hasDiscreteMarkers;
                                    if (standardWrap && this._seriesWrap && this._seriesWrapIndex === seriesIndex) {
                                        elMarkersWrap = this._seriesWrap;
                                    } else {
                                        elMarkersWrap = emit.group({
                                            class: standardWrap ? "apexcharts-series-markers" : ""
                                        });
                                        elMarkersWrap.attr("clip-path", "url(#gridRectMarkerMask".concat(w.globals.cuid, ")"));
                                        this.setupMarkerDelegation(elMarkersWrap);
                                        if (standardWrap) {
                                            this._seriesWrap = elMarkersWrap;
                                            this._seriesWrapIndex = seriesIndex;
                                        }
                                    }
                                }
                                markerElement = emit.drawMarker(p.x[q], p.y[q], opts);
                                markerElement.attr("rel", dataPointIndex);
                                markerElement.attr("j", dataPointIndex);
                                markerElement.attr("index", seriesIndex);
                                markerElement.node.setAttribute("default-marker-size", opts.pSize);
                                applyProgressiveReveal(markerElement, p.x[q], w);
                                this._filters.setSelectionFilter(markerElement, seriesIndex, dataPointIndex);
                                if (elMarkersWrap) {
                                    elMarkersWrap.add(markerElement);
                                }
                            }
                        } else {
                            if (typeof w.globals.pointsArray[seriesIndex] === "undefined") w.globals.pointsArray[seriesIndex] = [];
                            w.globals.pointsArray[seriesIndex].push([
                                p.x[q],
                                p.y[q]
                            ]);
                        }
                    }
                }
                return elMarkersWrap;
            }
        },
        {
            /** @param {{cssClass: any, seriesIndex: any, dataPointIndex?: any, radius?: any, size?: any, strokeWidth?: any}} opts */ key: "getMarkerConfig",
            value: function getMarkerConfig(param) {
                var cssClass = param.cssClass, seriesIndex = param.seriesIndex, _param_dataPointIndex = param.dataPointIndex, dataPointIndex = _param_dataPointIndex === void 0 ? null : _param_dataPointIndex, _param_radius = param.radius, radius = _param_radius === void 0 ? null : _param_radius, _param_size = param.size, size = _param_size === void 0 ? null : _param_size, _param_strokeWidth = param.strokeWidth, strokeWidth = _param_strokeWidth === void 0 ? null : _param_strokeWidth;
                var w = this.w;
                var pStyle = this.getMarkerStyle(seriesIndex);
                var pSize = size === null ? w.globals.markers.size[seriesIndex] : size;
                var m = w.config.markers;
                if (dataPointIndex !== null && m.discrete.length) {
                    m.discrete.map(function(marker) {
                        if (marker.seriesIndex === seriesIndex && marker.dataPointIndex === dataPointIndex) {
                            pStyle.pointStrokeColor = marker.strokeColor;
                            pStyle.pointFillColor = marker.fillColor;
                            pSize = marker.size;
                            pStyle.pointShape = marker.shape;
                        }
                    });
                }
                return {
                    pSize: radius === null ? pSize : radius,
                    pRadius: radius !== null ? radius : m.radius,
                    pointStrokeWidth: strokeWidth !== null ? strokeWidth : Array.isArray(m.strokeWidth) ? m.strokeWidth[seriesIndex] : m.strokeWidth,
                    pointStrokeColor: pStyle.pointStrokeColor,
                    pointFillColor: pStyle.pointFillColor,
                    shape: pStyle.pointShape || (Array.isArray(m.shape) ? m.shape[seriesIndex] : m.shape),
                    class: cssClass,
                    pointStrokeOpacity: Array.isArray(m.strokeOpacity) ? m.strokeOpacity[seriesIndex] : m.strokeOpacity,
                    pointStrokeDashArray: Array.isArray(m.strokeDashArray) ? m.strokeDashArray[seriesIndex] : m.strokeDashArray,
                    pointFillOpacity: Array.isArray(m.fillOpacity) ? m.fillOpacity[seriesIndex] : m.fillOpacity,
                    seriesIndex: seriesIndex
                };
            }
        },
        {
            /**
   * @param {any} parentGroup
   */ key: "setupMarkerDelegation",
            value: function setupMarkerDelegation(parentGroup) {
                var _this = this;
                var w = this.w;
                var selector = ".apexcharts-marker";
                this._graphics.setupEventDelegation(parentGroup, selector);
                parentGroup.node.addEventListener("click", function(e) {
                    if (w.config.markers.onClick) {
                        var targetNode = Graphics._findDelegateTarget(e.target, parentGroup.node, selector);
                        if (targetNode) w.config.markers.onClick(e);
                    }
                });
                parentGroup.node.addEventListener("dblclick", function(e) {
                    if (w.config.markers.onDblClick) {
                        var targetNode = Graphics._findDelegateTarget(e.target, parentGroup.node, selector);
                        if (targetNode) w.config.markers.onDblClick(e);
                    }
                });
                parentGroup.node.addEventListener("touchstart", function(e) {
                    var targetNode = Graphics._findDelegateTarget(e.target, parentGroup.node, selector);
                    if (targetNode && targetNode.instance) {
                        _this._graphics.pathMouseDown(targetNode.instance, e);
                    }
                }, {
                    passive: true
                });
            }
        },
        {
            /**
   * @returns {any}
   * @param {number} seriesIndex
   */ key: "getMarkerStyle",
            value: function getMarkerStyle(seriesIndex) {
                var w = this.w;
                var colors = w.globals.markers.colors;
                var strokeColors = w.config.markers.strokeColor || w.config.markers.strokeColors;
                var pointStrokeColor = Array.isArray(strokeColors) ? strokeColors[seriesIndex] : strokeColors;
                var pointFillColor = Array.isArray(colors) ? colors[seriesIndex] : colors;
                return {
                    pointStrokeColor: pointStrokeColor,
                    pointFillColor: pointFillColor
                };
            }
        }
    ]);
    return Markers;
}();
var Scatter = /*#__PURE__*/ function() {
    "use strict";
    function Scatter(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Scatter);
        this.ctx = ctx;
        this.w = w;
        this.initialAnim = this.w.config.chart.animations.enabled;
        this.anim = new Animations(this.w);
        this.filters = new Filters(this.w);
        this.fill = new Fill(this.w);
        this.markers = new Markers(this.w, this.ctx);
        this.graphics = new Graphics(this.w);
        this._elPointsWrap = null;
        this._elPointsWrapParent = null;
        this._perSeries = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Scatter, [
        {
            /**
   * @param {Element} elSeries
   * @param {number} j
   * @param {Record<string, any>} opts
   */ key: "draw",
            value: function draw(elSeries, j, opts) {
                var w = this.w;
                var graphics = this.graphics;
                var emit = seriesEmitter(this.ctx, graphics);
                var realIndex = opts.realIndex;
                var pointsPos = opts.pointsPos;
                var zRatio = opts.zRatio;
                var elPointsMain = opts.elParent;
                var elPointsWrap = this._elPointsWrap;
                if (!elPointsWrap || this._elPointsWrapParent !== elPointsMain) {
                    elPointsWrap = emit.group({
                        class: "apexcharts-series-markers apexcharts-series-".concat(w.config.chart.type)
                    });
                    elPointsWrap.attr("clip-path", "url(#gridRectMarkerMask".concat(w.globals.cuid, ")"));
                    this.markers.setupMarkerDelegation(elPointsWrap);
                    elPointsMain.add(elPointsWrap);
                    this._elPointsWrap = elPointsWrap;
                    this._elPointsWrapParent = elPointsMain;
                    this._perSeries = this._buildPerSeriesCache(realIndex, emit);
                }
                if (Array.isArray(pointsPos.x)) {
                    for(var q = 0; q < pointsPos.x.length; q++){
                        var dataPointIndex = j + 1;
                        var shouldDraw = true;
                        if (j === 0 && q === 0) dataPointIndex = 0;
                        if (j === 0 && q === 1) dataPointIndex = 1;
                        var radius = w.globals.markers.size[realIndex];
                        if (zRatio !== Infinity) {
                            var bubble = w.config.plotOptions.bubble;
                            radius = w.seriesData.seriesZ[realIndex][dataPointIndex];
                            if (bubble.zScaling) {
                                radius /= zRatio;
                            }
                            if (bubble.minBubbleRadius && radius < bubble.minBubbleRadius) {
                                radius = bubble.minBubbleRadius;
                            }
                            if (bubble.maxBubbleRadius && radius > bubble.maxBubbleRadius) {
                                radius = bubble.maxBubbleRadius;
                            }
                        }
                        var x = pointsPos.x[q];
                        var y = pointsPos.y[q];
                        radius = radius || 0;
                        if (y === null || typeof w.seriesData.series[realIndex][dataPointIndex] === "undefined") {
                            shouldDraw = false;
                        }
                        if (shouldDraw) {
                            var point = this.drawPoint(x, y, radius, realIndex, dataPointIndex, j);
                            elPointsWrap.add(point);
                            if (emit.kind === "canvas") {
                                if (typeof w.globals.pointsArray[realIndex] === "undefined") {
                                    w.globals.pointsArray[realIndex] = [];
                                }
                                w.globals.pointsArray[realIndex][dataPointIndex] = [
                                    x,
                                    y
                                ];
                            }
                        }
                    }
                }
            }
        },
        {
            /**
   * Per-series constants for drawPoint's hot path. Everything here is uniform
   * across the points of one series; computing or allocating it per point is
   * measurable overhead at 20k-50k points.
   * @param {number} realIndex
   * @param {any} emit
   */ key: "_buildPerSeriesCache",
            value: function _buildPerSeriesCache(realIndex, emit) {
                var _a;
                var w = this.w;
                return {
                    realIndex: realIndex,
                    emit: emit,
                    isBubble: w.config.chart.type === "bubble" || w.globals.comboCharts && w.config.series[realIndex] && /** @type {Record<string,any>} */ w.config.series[realIndex].type === "bubble",
                    // discrete markers vary per point; they disable both caches below
                    canCacheConfig: !w.config.markers.discrete.length,
                    /** @type {any} lazily built shared marker config (first point) */ markerConfig: null,
                    /** @type {boolean|undefined} lazily resolved on the first fillPath call */ fillCacheable: void 0,
                    /** @type {any} cached fillPath result (undefined = not cached yet) */ fillCircle: void 0,
                    dropShadowEnabled: w.config.chart.dropShadow.enabled,
                    doInitialAnim: this.initialAnim && !w.globals.dataChanged && !w.globals.resized,
                    jitter: (_a = w.config.plotOptions.scatter) == null ? void 0 : _a.jitter,
                    /** @type {any} lazily built pop-animation constants */ anim: null
                };
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {number} radius
   * @param {number} realIndex
   * @param {number} dataPointIndex
   * @param {number} j
   */ key: "drawPoint",
            value: function drawPoint(x, y, radius, realIndex, dataPointIndex, j) {
                var _a;
                var w = this.w;
                var i = realIndex;
                var anim = this.anim;
                var filters = this.filters;
                var fill = this.fill;
                var markers = this.markers;
                var ps = this._perSeries;
                if (!ps || ps.realIndex !== realIndex) {
                    ps = this._perSeries = this._buildPerSeriesCache(realIndex, seriesEmitter(this.ctx, this.graphics));
                }
                var emit = ps.emit;
                var markerConfig;
                if (ps.canCacheConfig) {
                    if (!ps.markerConfig) {
                        ps.markerConfig = markers.getMarkerConfig({
                            cssClass: "apexcharts-marker",
                            seriesIndex: i,
                            dataPointIndex: dataPointIndex,
                            radius: ps.isBubble ? radius : null
                        });
                    }
                    markerConfig = ps.markerConfig;
                    if (ps.isBubble) {
                        markerConfig.pSize = radius;
                        markerConfig.pRadius = radius;
                    }
                } else {
                    markerConfig = markers.getMarkerConfig({
                        cssClass: "apexcharts-marker",
                        seriesIndex: i,
                        dataPointIndex: dataPointIndex,
                        radius: ps.isBubble ? radius : null
                    });
                }
                var _si = /** @type {Record<string,any>} */ w.config.series[i];
                var dataItem = _si.data[dataPointIndex];
                var pathFillCircle;
                if (ps.fillCircle !== void 0) {
                    pathFillCircle = ps.fillCircle;
                } else {
                    pathFillCircle = fill.fillPath({
                        seriesNumber: realIndex,
                        dataPointIndex: dataPointIndex,
                        color: markerConfig.pointFillColor,
                        patternUnits: "objectBoundingBox",
                        value: w.seriesData.series[realIndex][j]
                    });
                    if (ps.fillCacheable === void 0) {
                        ps.fillCacheable = ps.canCacheConfig && fill.getFillType(realIndex) === "solid" && typeof markerConfig.pointFillColor === "string" && !!markerConfig.pointFillColor;
                    }
                    if (ps.fillCacheable && !(dataItem == null ? void 0 : dataItem.fillColor)) {
                        ps.fillCircle = pathFillCircle;
                    }
                }
                var el = emit.drawMarker(x, y, markerConfig);
                if (dataItem == null ? void 0 : dataItem.fillColor) {
                    pathFillCircle = dataItem.fillColor;
                }
                var jt = ps.jitter;
                if ((jt == null ? void 0 : jt.enabled) && jt.distributed && w.globals.colors.length) {
                    var bandIdx = Math.round((_a = w.seriesData.seriesX[realIndex]) == null ? void 0 : _a[dataPointIndex]);
                    if (!isNaN(bandIdx)) {
                        pathFillCircle = w.globals.colors[bandIdx % w.globals.colors.length];
                    }
                }
                el.attr({
                    fill: pathFillCircle
                });
                if (ps.dropShadowEnabled) {
                    var dropShadow = w.config.chart.dropShadow;
                    filters.dropShadow(el, dropShadow, realIndex);
                }
                if (ps.doInitialAnim) {
                    if (!ps.anim) {
                        var animCfg = w.config.chart.animations;
                        var totalPoints = w.globals.dataPoints || 1;
                        var gradCfg = animCfg.animateGradually;
                        var gradEnabled = gradCfg && gradCfg.enabled !== false;
                        ps.anim = {
                            popSpeed: animCfg.speed,
                            baseDelay: gradEnabled ? Math.min(20, animCfg.speed * 0.5 / Math.max(1, totalPoints)) : 0
                        };
                    }
                    var delay = computeStagger({
                        style: ps.anim.baseDelay > 0 ? "sequential" : "none",
                        index: dataPointIndex,
                        baseDelay: ps.anim.baseDelay
                    });
                    anim.animatePop(el, {
                        speed: ps.anim.popSpeed,
                        delay: delay,
                        onComplete: function onComplete() {
                            return anim.animationCompleted(el);
                        }
                    });
                } else {
                    w.globals.animationEnded = true;
                }
                el.attr({
                    rel: dataPointIndex,
                    j: dataPointIndex,
                    index: realIndex,
                    "default-marker-size": markerConfig.pSize
                });
                filters.setSelectionFilter(el, realIndex, dataPointIndex);
                return el;
            }
        },
        {
            /**
   * @param {number} y
   */ key: "centerTextInBubble",
            value: function centerTextInBubble(y) {
                var w = this.w;
                y = y + parseInt(w.config.dataLabels.style.fontSize, 10) / 4;
                return {
                    y: y
                };
            }
        }
    ]);
    return Scatter;
}();
var resolveDataLabelOffset = function resolveDataLabelOffset(value, w, seriesIndex, dataPointIndex) {
    if (typeof value !== "function") return value;
    var resolved = value({
        series: w.seriesData.series,
        seriesIndex: seriesIndex,
        dataPointIndex: dataPointIndex,
        w: w
    });
    return Number.isFinite(resolved) ? resolved : 0;
};
var DataLabels = /*#__PURE__*/ function() {
    "use strict";
    function DataLabels(w) {
        var ctx = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, DataLabels);
        this.w = w;
        this.ctx = ctx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(DataLabels, [
        {
            // When there are many datalabels to be printed, and some of them overlaps each other in the same series, this method will take care of that
            // Also, when datalabels exceeds the drawable area and get clipped off, we need to adjust and move some pixels to make them visible again
            /**
   * @param {number} x
   * @param {number} y
   * @param {any} val
   * @param {number} i
   * @param {number} dataPointIndex
   * @param {boolean} alwaysDrawDataLabel
   * @param {string} fontSize
   */ key: "dataLabelsCorrection",
            value: function dataLabelsCorrection(x, y, val, i, dataPointIndex, alwaysDrawDataLabel, fontSize) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var drawnextLabel = false;
                var textRects = /** @type {any} */ graphics.getTextRects(val, fontSize);
                var width = textRects.width;
                var height = textRects.height;
                if (y < 0) y = 0;
                if (y > w.layout.gridHeight + height) y = w.layout.gridHeight + height / 2;
                if (typeof /** @type {any} */ w.globals.dataLabelsRects[i] === "undefined") {
                    w.globals.dataLabelsRects[i] = [];
                }
                w.globals.dataLabelsRects[i].push({
                    x: x,
                    y: y,
                    width: width,
                    height: height
                });
                var len = /** @type {any} */ w.globals.dataLabelsRects[i].length - 2;
                var lastDrawnIndex = typeof w.globals.lastDrawnDataLabelsIndexes[i] !== "undefined" ? w.globals.lastDrawnDataLabelsIndexes[i][w.globals.lastDrawnDataLabelsIndexes[i].length - 1] : 0;
                if (typeof /** @type {any} */ w.globals.dataLabelsRects[i][len] !== "undefined") {
                    var lastDataLabelRect = /** @type {any} */ w.globals.dataLabelsRects[i][lastDrawnIndex];
                    if (// next label forward and x not intersecting
                    x > lastDataLabelRect.x + lastDataLabelRect.width || y > lastDataLabelRect.y + lastDataLabelRect.height || y + height < lastDataLabelRect.y || x + width < lastDataLabelRect.x) {
                        drawnextLabel = true;
                    }
                }
                if (dataPointIndex === 0 || alwaysDrawDataLabel) {
                    drawnextLabel = true;
                }
                return {
                    x: x,
                    y: y,
                    textRects: textRects,
                    drawnextLabel: drawnextLabel
                };
            }
        },
        {
            /** @param {{type: any, pos: any, i: any, j: any, isRangeStart: any, strokeWidth?: any}} opts */ key: "drawDataLabel",
            value: function drawDataLabel(param) {
                var type = param.type, pos = param.pos, i = param.i, j = param.j, isRangeStart = param.isRangeStart, _param_strokeWidth = param.strokeWidth, strokeWidth = _param_strokeWidth === void 0 ? 2 : _param_strokeWidth;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var dataLabelsConfig = w.config.dataLabels;
                var x = 0;
                var y = 0;
                var dataPointIndex = j;
                var elDataLabelsWrap = null;
                var seriesCollapsed = w.globals.collapsedSeriesIndices.indexOf(i) !== -1;
                if (seriesCollapsed || !dataLabelsConfig.enabled || !Array.isArray(pos.x)) {
                    return elDataLabelsWrap;
                }
                elDataLabelsWrap = graphics.group({
                    class: "apexcharts-data-labels"
                });
                for(var q = 0; q < pos.x.length; q++){
                    if (j === 1 && q === 0) dataPointIndex = 0;
                    if (j === 1 && q === 1) dataPointIndex = 1;
                    x = pos.x[q] + resolveDataLabelOffset(dataLabelsConfig.offsetX, w, i, dataPointIndex);
                    y = pos.y[q] + resolveDataLabelOffset(dataLabelsConfig.offsetY, w, i, dataPointIndex) + strokeWidth;
                    if (!isNaN(x)) {
                        var val = w.seriesData.series[i][dataPointIndex];
                        if (type === "rangeArea") {
                            if (isRangeStart) {
                                val = w.rangeData.seriesRangeStart[i][dataPointIndex];
                            } else {
                                val = w.rangeData.seriesRangeEnd[i][dataPointIndex];
                            }
                        }
                        var text = "";
                        var getText = function getText(v) {
                            return w.config.dataLabels.formatter(v, {
                                seriesIndex: i,
                                dataPointIndex: dataPointIndex,
                                w: w
                            });
                        };
                        if (w.config.chart.type === "bubble") {
                            val = w.seriesData.seriesZ[i][dataPointIndex];
                            text = getText(val);
                            y = pos.y[q];
                            var scatter = new Scatter(this.w, /** @type {import('../types/internal').ChartContext} */ this.ctx);
                            var centerTextInBubbleCoords = scatter.centerTextInBubble(y);
                            y = centerTextInBubbleCoords.y;
                        } else {
                            if (typeof val !== "undefined") {
                                text = getText(val);
                            }
                        }
                        var textAnchor = w.config.dataLabels.textAnchor;
                        if (w.globals.isSlopeChart) {
                            if (dataPointIndex === 0) {
                                textAnchor = "end";
                            } else if (dataPointIndex === /** @type {Record<string,any>} */ w.config.series[i].data.length - 1) {
                                textAnchor = "start";
                            } else {
                                textAnchor = "middle";
                            }
                        }
                        this.plotDataLabelsText({
                            x: x,
                            y: y,
                            text: text,
                            i: i,
                            j: dataPointIndex,
                            parent: elDataLabelsWrap,
                            offsetCorrection: true,
                            dataLabelsConfig: w.config.dataLabels,
                            textAnchor: textAnchor
                        });
                    }
                }
                return elDataLabelsWrap;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "plotDataLabelsText",
            value: function plotDataLabelsText(opts) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var x = opts.x, y = opts.y, i = opts.i, j = opts.j, text = opts.text, textAnchor = opts.textAnchor, fontSize = opts.fontSize, parent = opts.parent, dataLabelsConfig = opts.dataLabelsConfig, color = opts.color, alwaysDrawDataLabel = opts.alwaysDrawDataLabel, offsetCorrection = opts.offsetCorrection, className = opts.className, _opts_seriesIndex = opts.// some callers (radar) reuse `j` for something other than the data point
                // index, so per-point offsets take these explicit indices when supplied
                seriesIndex, seriesIndex = _opts_seriesIndex === void 0 ? i : _opts_seriesIndex, _opts_dataPointIndex = opts.dataPointIndex, dataPointIndex = _opts_dataPointIndex === void 0 ? j : _opts_dataPointIndex;
                var dataLabelText = null;
                if (Array.isArray(w.config.dataLabels.enabledOnSeries)) {
                    if (w.config.dataLabels.enabledOnSeries.indexOf(i) < 0) {
                        return dataLabelText;
                    }
                }
                var correctedLabels = {
                    x: x,
                    y: y,
                    drawnextLabel: true,
                    textRects: null
                };
                if (offsetCorrection) {
                    correctedLabels = this.dataLabelsCorrection(x, y, text, i, j, alwaysDrawDataLabel, parseInt(/** @type {any} */ dataLabelsConfig.style.fontSize, 10).toString());
                }
                if (!w.interact.zoomed) {
                    x = correctedLabels.x;
                    y = correctedLabels.y;
                }
                if (correctedLabels.textRects) {
                    var barPad = w.globals.barPadForNumericAxis || 0;
                    if (x < -(barPad + 20) - /** @type {any} */ correctedLabels.textRects.width || x > w.layout.gridWidth + /** @type {any} */ correctedLabels.textRects.width + barPad + 30) {
                        text = "";
                    }
                }
                var dataLabelColor = w.globals.dataLabels.style.colors[i];
                if ((w.config.chart.type === "bar" || w.config.chart.type === "rangeBar") && w.config.plotOptions.bar.distributed || w.config.dataLabels.distributed) {
                    dataLabelColor = w.globals.dataLabels.style.colors[j];
                }
                if (typeof dataLabelColor === "function") {
                    dataLabelColor = /** @type {any} */ dataLabelColor({
                        series: w.seriesData.series,
                        seriesIndex: i,
                        dataPointIndex: j,
                        w: w
                    });
                }
                if (color) {
                    dataLabelColor = color;
                }
                var offsetsHandledElsewhere = w.config.chart.type === "bar" || w.config.chart.type === "rangeBar";
                var resolvedOffX = offsetsHandledElsewhere ? 0 : resolveDataLabelOffset(dataLabelsConfig.offsetX, w, seriesIndex, dataPointIndex);
                var resolvedOffY = offsetsHandledElsewhere ? 0 : resolveDataLabelOffset(dataLabelsConfig.offsetY, w, seriesIndex, dataPointIndex);
                var offX = resolvedOffX;
                var offY = resolvedOffY;
                if (w.globals.isSlopeChart) {
                    if (j !== 0) {
                        offX = resolvedOffX * -2 + 5;
                    }
                    if (j !== 0 && j !== /** @type {Record<string,any>} */ w.config.series[i].data.length - 1) {
                        offX = 0;
                    }
                }
                if (correctedLabels.drawnextLabel) {
                    if (textAnchor === "middle") {
                        if (x === w.layout.gridWidth) {
                            textAnchor = "end";
                        }
                    }
                    dataLabelText = graphics.drawText({
                        x: x + offX,
                        y: y + offY,
                        foreColor: dataLabelColor,
                        textAnchor: textAnchor || dataLabelsConfig.textAnchor,
                        text: text,
                        fontSize: fontSize || dataLabelsConfig.style.fontSize,
                        fontFamily: dataLabelsConfig.style.fontFamily,
                        fontWeight: dataLabelsConfig.style.fontWeight || "normal"
                    });
                    dataLabelText.attr({
                        class: className || "apexcharts-datalabel",
                        cx: x,
                        cy: y
                    });
                    if (dataLabelsConfig.dropShadow.enabled) {
                        var textShadow = dataLabelsConfig.dropShadow;
                        var filters = new Filters(this.w);
                        filters.dropShadow(dataLabelText, textShadow);
                    }
                    parent.add(dataLabelText);
                    applyProgressiveReveal(dataLabelText, x, w);
                    if (typeof w.globals.lastDrawnDataLabelsIndexes[i] === "undefined") {
                        w.globals.lastDrawnDataLabelsIndexes[i] = [];
                    }
                    w.globals.lastDrawnDataLabelsIndexes[i].push(j);
                }
                return dataLabelText;
            }
        },
        {
            /**
   * @param {Element} el
   * @param {{x: number, y: number, width: number, height: number}} coords
   */ key: "addBackgroundToDataLabel",
            value: function addBackgroundToDataLabel(el, coords) {
                var w = this.w;
                var bCnf = w.config.dataLabels.background;
                var paddingH = bCnf.padding;
                var paddingV = bCnf.padding / 2;
                var width = coords.width;
                var height = coords.height;
                var graphics = new Graphics(this.w);
                var elRect = graphics.drawRect(coords.x - paddingH, coords.y - paddingV / 2, width + paddingH * 2, height + paddingV, bCnf.borderRadius, w.config.chart.background === "transparent" || !w.config.chart.background ? "#fff" : w.config.chart.background, bCnf.opacity, bCnf.borderWidth, bCnf.borderColor);
                if (bCnf.dropShadow.enabled) {
                    var filters = new Filters(this.w);
                    filters.dropShadow(elRect, bCnf.dropShadow);
                }
                return elRect;
            }
        },
        {
            key: "dataLabelsBackground",
            value: function dataLabelsBackground() {
                var _a;
                var w = this.w;
                if (w.config.chart.type === "bubble") return;
                var elDataLabels = w.dom.baseEl.querySelectorAll(".apexcharts-datalabels text");
                for(var i = 0; i < elDataLabels.length; i++){
                    var el = elDataLabels[i];
                    var coords = /** @type {SVGGraphicsElement} */ el.getBBox();
                    var elRect = null;
                    if (coords.width && coords.height) {
                        elRect = this.addBackgroundToDataLabel(el, coords);
                    }
                    if (elRect) {
                        (_a = el.parentNode) == null ? void 0 : _a.insertBefore(elRect.node, el);
                        var background = w.config.dataLabels.background.backgroundColor || el.getAttribute("fill");
                        var shouldAnim = w.config.chart.animations.enabled && !w.globals.resized && !w.globals.dataChanged;
                        if (shouldAnim) {
                            elRect.animate().attr({
                                fill: background
                            });
                        } else {
                            elRect.attr({
                                fill: background
                            });
                        }
                        el.setAttribute("fill", w.config.dataLabels.background.foreColor);
                        var cxAttr = el.getAttribute("cx");
                        if (cxAttr !== null) {
                            applyProgressiveReveal(elRect, parseFloat(cxAttr), w);
                        }
                    }
                }
            }
        },
        {
            key: "bringForward",
            value: function bringForward() {
                var w = this.w;
                var elDataLabelsNodes = w.dom.baseEl.querySelectorAll(".apexcharts-datalabels");
                var elSeries = w.dom.baseEl.querySelector(".apexcharts-plot-series:last-child");
                for(var i = 0; i < elDataLabelsNodes.length; i++){
                    if (elSeries) {
                        elSeries.insertBefore(elDataLabelsNodes[i], elSeries.nextSibling);
                    }
                }
            }
        }
    ]);
    return DataLabels;
}();
var PerformanceCache = /*#__PURE__*/ function() {
    "use strict";
    function PerformanceCache() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, PerformanceCache);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(PerformanceCache, null, [
        {
            key: "invalidateAll",
            value: /**
   * Invalidate all caches
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   */ function invalidateAll(w) {
                if (!w || !w.globals) return;
                if (w.globals.cachedSelectors) {
                    w.globals.cachedSelectors = {};
                }
                if (w.globals.domCache) {
                    w.globals.domCache.clear();
                }
                w.globals.dimensionCache = {};
            }
        },
        {
            key: "invalidateDimensions",
            value: /**
   * Invalidate dimension cache only
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   */ function invalidateDimensions(w) {
                if (!w || !w.globals) return;
                w.globals.dimensionCache = {};
            }
        },
        {
            key: "invalidateSelectors",
            value: /**
   * Invalidate selector cache only
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   */ function invalidateSelectors(w) {
                if (!w || !w.globals) return;
                if (w.globals.cachedSelectors) {
                    w.globals.cachedSelectors = {};
                }
            }
        },
        {
            key: "getCachedSelector",
            value: /**
   * Get cached selector result or compute and cache it
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @param {Function} queryFn - Function to execute if not cached
   * @returns {*} Cached or newly computed result
   */ function getCachedSelector(w, key, queryFn) {
                if (!w || !w.globals) return queryFn();
                if (!w.globals.cachedSelectors) {
                    w.globals.cachedSelectors = {};
                }
                if (!w.globals.cachedSelectors[key]) {
                    w.globals.cachedSelectors[key] = queryFn();
                }
                return w.globals.cachedSelectors[key];
            }
        },
        {
            key: "getCachedDimension",
            value: /**
   * Get cached dimension or compute and cache it
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @param {Function} computeFn - Function to compute dimensions
   * @param {number} maxAge - Maximum cache age in milliseconds (default: 1000ms)
   * @returns {*} Cached or newly computed dimensions
   */ function getCachedDimension(w, key, computeFn) {
                var maxAge = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 1e3;
                if (!w || !w.globals) return computeFn();
                if (!w.globals.dimensionCache) {
                    w.globals.dimensionCache = {};
                }
                var cache = w.globals.dimensionCache[key];
                var now = Date.now();
                if (cache && cache.lastUpdate && now - cache.lastUpdate < maxAge) {
                    return cache.value;
                }
                var value = computeFn();
                w.globals.dimensionCache[key] = {
                    value: value,
                    lastUpdate: now
                };
                return value;
            }
        },
        {
            key: "cacheDOMElement",
            value: /**
   * Cache a DOM element reference
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @param {Element} element - DOM element to cache
   */ function cacheDOMElement(w, key, element) {
                if (!w || !w.globals) return;
                if (!w.globals.domCache) {
                    w.globals.domCache = /* @__PURE__ */ new Map();
                }
                w.globals.domCache.set(key, element);
            }
        },
        {
            key: "getCachedDOMElement",
            value: /**
   * Get cached DOM element
   * @param {import('../types/internal').ChartStateW} w - ApexCharts state object
   * @param {string} key - Cache key
   * @returns {Element|null} Cached element or null
   */ function getCachedDOMElement(w, key) {
                if (!w || !w.globals || !w.globals.domCache) return null;
                return w.globals.domCache.get(key) || null;
            }
        }
    ]);
    return PerformanceCache;
}();
var AxesUtils = /*#__PURE__*/ function() {
    "use strict";
    function AxesUtils(w) {
        var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref_theme = _ref.theme, theme = _ref_theme === void 0 ? null : _ref_theme, _ref_timeScale = _ref.timeScale, timeScale = _ref_timeScale === void 0 ? null : _ref_timeScale;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, AxesUtils);
        this.w = w;
        this.theme = theme;
        this.timeScale = timeScale;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(AxesUtils, [
        {
            // Based on the formatter function, get the label text and position
            /**
   * @param {any[]} labels
   * @param {Array<Record<string, any>>} timescaleLabels
   * @param {number} x
   * @param {number} i
   * @param {any[]} drawnLabels
   */ key: "getLabel",
            value: function getLabel(labels, timescaleLabels, x, i) {
                var drawnLabels = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : [], fontSize = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : "12px", isLeafGroup = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : true;
                var w = this.w;
                var rawLabel = typeof labels[i] === "undefined" ? "" : labels[i];
                var label = rawLabel;
                var xlbFormatter = w.formatters.xLabelFormatter;
                var customFormatter = w.config.xaxis.labels.formatter;
                var xFormat = new Formatters(this.w);
                var timestamp = rawLabel;
                if (isLeafGroup) {
                    label = /** @type {any} */ xFormat.xLabelFormat(xlbFormatter, rawLabel, timestamp, {
                        i: i,
                        dateFormatter: new DateTime(this.w).formatDate,
                        w: w
                    });
                    if (customFormatter !== void 0) {
                        label = customFormatter(rawLabel, labels[i], {
                            i: i,
                            dateFormatter: new DateTime(this.w).formatDate,
                            w: w
                        });
                    }
                }
                if (timescaleLabels.length > 0) {
                    x = timescaleLabels[i].position;
                    label = timescaleLabels[i].value;
                } else {
                    if (w.config.xaxis.type === "datetime" && customFormatter === void 0) {
                        label = "";
                    }
                }
                if (typeof label === "undefined") label = "";
                label = Array.isArray(label) ? label : label.toString();
                var graphics = new Graphics(this.w);
                var textRect = {};
                if (w.layout.rotateXLabels && isLeafGroup) {
                    textRect = graphics.getTextRects(label, parseInt(fontSize, 10).toString(), null, "rotate(".concat(w.config.xaxis.labels.rotate, " 0 0)"), false);
                } else {
                    textRect = graphics.getTextRects(label, parseInt(fontSize, 10).toString());
                }
                var allowDuplicatesInTimeScale = !w.config.xaxis.labels.showDuplicates && this.timeScale;
                if (!Array.isArray(label) && (String(label) === "NaN" || drawnLabels.indexOf(label) >= 0 && allowDuplicatesInTimeScale)) {
                    label = "";
                }
                return {
                    x: x,
                    text: label,
                    textRect: textRect
                };
            }
        },
        {
            /**
   * @param {number} i
   * @param {any} label
   * @param {number} labelsLen
   */ key: "checkLabelBasedOnTickamount",
            value: function checkLabelBasedOnTickamount(i, label, labelsLen) {
                var w = this.w;
                var ticks = w.config.xaxis.tickAmount;
                if (ticks === "dataPoints") ticks = Math.round(w.layout.gridWidth / 120);
                if (ticks > labelsLen) return label;
                var tickMultiple = Math.round(labelsLen / (ticks + 1));
                if (i % tickMultiple === 0) {
                    return label;
                } else {
                    label.text = "";
                }
                return label;
            }
        },
        {
            /**
   * @param {number} i
   * @param {any} label
   * @param {number} labelsLen
   * @param {any[]} drawnLabels
   * @param {Array<Record<string, any>>} drawnLabelsRects
   */ key: "checkForOverflowingLabels",
            value: function checkForOverflowingLabels(i, label, labelsLen, drawnLabels, drawnLabelsRects) {
                var w = this.w;
                if (i === 0) {
                    if (w.globals.skipFirstTimelinelabel) {
                        label.text = "";
                    }
                }
                if (i === labelsLen - 1) {
                    if (w.globals.skipLastTimelinelabel) {
                        label.text = "";
                    }
                }
                if (w.config.xaxis.labels.hideOverlappingLabels && drawnLabels.length > 0) {
                    var prev = drawnLabelsRects[drawnLabelsRects.length - 1];
                    if (w.config.xaxis.labels.trim && w.config.xaxis.type !== "datetime") {
                        return label;
                    }
                    if (/** @type {any} */ label.x < prev.textRect.width / (w.layout.rotateXLabels ? // Floor the rotation at 1deg: rotateAlways:true forces
                    // rotateXLabels even with rotate:0, and a 0 divisor here makes the
                    // spread Infinity, blanking every label after the first.
                    Math.max(Math.abs(w.config.xaxis.labels.rotate), 1) / 12 : 1.01) + prev.x) {
                        label.text = "";
                    }
                }
                return label;
            }
        },
        {
            /**
   * @param {number} i
   * @param {any[]} labels
   */ key: "checkForReversedLabels",
            value: function checkForReversedLabels(i, labels) {
                var w = this.w;
                if (w.config.yaxis[i] && w.config.yaxis[i].reversed) {
                    labels.reverse();
                }
                return labels;
            }
        },
        {
            /**
   * @param {number} index
   */ key: "yAxisAllSeriesCollapsed",
            value: function yAxisAllSeriesCollapsed(index) {
                var gl = this.w.globals;
                return !gl.seriesYAxisMap[index].some(function(si) {
                    return gl.collapsedSeriesIndices.indexOf(si) === -1;
                });
            }
        },
        {
            // Method to translate annotation.yAxisIndex values from
            // seriesName-as-a-string values to seriesName-as-an-array values (old style
            // series mapping to new style).
            /**
   * @param {number} index
   */ key: "translateYAxisIndex",
            value: function translateYAxisIndex(index) {
                var w = this.w;
                var gl = w.globals;
                var yaxis = w.config.yaxis;
                var newStyle = w.seriesData.series.length > yaxis.length || /**
     * @param {Record<string, any>} a
     */ yaxis.some(function(a) {
                    return Array.isArray(a.seriesName);
                });
                if (newStyle) {
                    return index;
                } else {
                    return gl.seriesYAxisReverseMap[index];
                }
            }
        },
        {
            /**
   * @param {number} index
   */ key: "isYAxisHidden",
            value: function isYAxisHidden(index) {
                var w = this.w;
                var yaxis = w.config.yaxis[index];
                if (!yaxis.show || this.yAxisAllSeriesCollapsed(index)) {
                    return true;
                }
                if (!yaxis.showForNullSeries) {
                    var seriesIndices = w.globals.seriesYAxisMap[index];
                    var coreUtils = new CoreUtils(this.w);
                    return seriesIndices.every(function(si) {
                        return coreUtils.isSeriesNull(si);
                    });
                }
                return false;
            }
        },
        {
            // get the label color for y-axis
            // realIndex is the actual series index, while i is the tick Index
            /**
   * @param {string[]} yColors
   * @param {number} realIndex
   */ key: "getYAxisForeColor",
            value: function getYAxisForeColor(yColors, realIndex) {
                var _a;
                var w = this.w;
                if (Array.isArray(yColors) && w.globals.yAxisScale[realIndex]) {
                    (_a = this.theme) == null ? void 0 : _a.pushExtraColors(yColors, w.globals.yAxisScale[realIndex].result.length, false);
                }
                return yColors;
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} tickAmount
   * @param {Record<string, any>} axisBorder
   * @param {Record<string, any>} axisTicks
   * @param {number} realIndex
   * @param {any} labelsDivider
   * @param {any} elYaxis
   */ key: "drawYAxisTicks",
            value: function drawYAxisTicks(x, tickAmount, axisBorder, axisTicks, realIndex, labelsDivider, elYaxis) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var tY = w.layout.translateY + w.config.yaxis[realIndex].labels.offsetY;
                if (w.globals.isBarHorizontal) {
                    tY = 0;
                } else if (w.config.chart.type === "heatmap") {
                    tY += labelsDivider / 2;
                }
                if (axisTicks.show && tickAmount > 0) {
                    if (w.config.yaxis[realIndex].opposite === true) x = x + axisTicks.width;
                    for(var i = tickAmount; i >= 0; i--){
                        var elTick = graphics.drawLine(x + axisBorder.offsetX - axisTicks.width + axisTicks.offsetX, tY + axisTicks.offsetY, x + axisBorder.offsetX + axisTicks.offsetX, tY + axisTicks.offsetY, axisTicks.color);
                        elYaxis.add(elTick);
                        tY += labelsDivider;
                    }
                }
            }
        }
    ]);
    return AxesUtils;
}();
var XAxis = /*#__PURE__*/ function() {
    "use strict";
    function XAxis(w, ctx, elgrid) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, XAxis);
        this.w = w;
        this.ctx = ctx;
        this.elgrid = elgrid;
        this.axesUtils = new AxesUtils(w, {
            theme: ctx.theme,
            timeScale: ctx.timeScale
        });
        this.xaxisLabels = w.labelData.labels.slice();
        if (w.labelData.timescaleLabels.length > 0 && !w.globals.isBarHorizontal) {
            this.xaxisLabels = w.labelData.timescaleLabels.slice();
        }
        if (w.config.xaxis.overwriteCategories) {
            this.xaxisLabels = w.config.xaxis.overwriteCategories;
        }
        this.drawnLabels = [];
        this.drawnLabelsRects = [];
        if (w.config.xaxis.position === "top") {
            this.offY = 0;
        } else {
            this.offY = w.layout.gridHeight;
        }
        this.offY = this.offY + w.config.xaxis.axisBorder.offsetY;
        this.isCategoryBarHorizontal = w.config.chart.type === "bar" && w.config.plotOptions.bar.horizontal;
        this.xaxisFontSize = w.config.xaxis.labels.style.fontSize;
        this.xaxisFontFamily = w.config.xaxis.labels.style.fontFamily;
        this.xaxisForeColors = w.config.xaxis.labels.style.colors;
        this.xaxisBorderWidth = w.config.xaxis.axisBorder.width;
        if (this.isCategoryBarHorizontal) {
            this.xaxisBorderWidth = w.config.yaxis[0].axisBorder.width.toString();
        }
        if (String(this.xaxisBorderWidth).indexOf("%") > -1) {
            this.xaxisBorderWidth = w.layout.gridWidth * parseInt(this.xaxisBorderWidth, 10) / 100;
        } else {
            this.xaxisBorderWidth = parseInt(this.xaxisBorderWidth, 10);
        }
        this.xaxisBorderHeight = w.config.xaxis.axisBorder.height;
        this.yaxis = w.config.yaxis[0];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(XAxis, [
        {
            key: "drawXaxis",
            value: function drawXaxis() {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var elXaxis = graphics.group({
                    class: "apexcharts-xaxis",
                    transform: "translate(".concat(w.config.xaxis.offsetX, ", ").concat(w.config.xaxis.offsetY, ")")
                });
                var elXaxisTexts = graphics.group({
                    class: "apexcharts-xaxis-texts-g",
                    transform: "translate(".concat(w.layout.translateXAxisX, ", ").concat(w.layout.translateXAxisY, ")")
                });
                elXaxis.add(elXaxisTexts);
                var labels = [];
                for(var i = 0; i < this.xaxisLabels.length; i++){
                    labels.push(this.xaxisLabels[i]);
                }
                this.drawXAxisLabelAndGroup(true, graphics, elXaxisTexts, labels, w.axisFlags.isXNumeric, function(i, colWidth) {
                    return colWidth;
                });
                if (w.labelData.hasXaxisGroups) {
                    var labelsGroup = w.labelData.groups;
                    labels = [];
                    for(var i1 = 0; i1 < labelsGroup.length; i1++){
                        labels.push(labelsGroup[i1].title);
                    }
                    var overwriteStyles = /** @type {any} */ {};
                    if (w.config.xaxis.group.style) {
                        overwriteStyles.xaxisFontSize = w.config.xaxis.group.style.fontSize;
                        overwriteStyles.xaxisFontFamily = w.config.xaxis.group.style.fontFamily;
                        overwriteStyles.xaxisForeColors = w.config.xaxis.group.style.colors;
                        overwriteStyles.fontWeight = w.config.xaxis.group.style.fontWeight;
                        overwriteStyles.cssClass = w.config.xaxis.group.style.cssClass;
                    }
                    this.drawXAxisLabelAndGroup(false, graphics, elXaxisTexts, labels, false, function(i, colWidth) {
                        return labelsGroup[i].cols * colWidth;
                    }, overwriteStyles);
                }
                if (w.config.xaxis.title.text !== void 0) {
                    var elXaxisTitle = graphics.group({
                        class: "apexcharts-xaxis-title"
                    });
                    var elXAxisTitleText = graphics.drawText({
                        x: w.layout.gridWidth / 2 + w.config.xaxis.title.offsetX,
                        y: this.offY + parseFloat(this.xaxisFontSize) + (w.config.xaxis.position === "bottom" ? w.layout.xAxisLabelsHeight : -w.layout.xAxisLabelsHeight - 10) + w.config.xaxis.title.offsetY,
                        text: w.config.xaxis.title.text,
                        textAnchor: "middle",
                        fontSize: w.config.xaxis.title.style.fontSize,
                        fontFamily: w.config.xaxis.title.style.fontFamily,
                        fontWeight: w.config.xaxis.title.style.fontWeight,
                        foreColor: w.config.xaxis.title.style.color,
                        cssClass: "apexcharts-xaxis-title-text " + w.config.xaxis.title.style.cssClass
                    });
                    elXaxisTitle.add(elXAxisTitleText);
                    elXaxis.add(elXaxisTitle);
                }
                if (w.config.xaxis.axisBorder.show) {
                    var offX = w.globals.barPadForNumericAxis;
                    var elHorzLine = graphics.drawLine(w.globals.padHorizontal + w.config.xaxis.axisBorder.offsetX - offX, this.offY, this.xaxisBorderWidth + offX, this.offY, w.config.xaxis.axisBorder.color, 0, this.xaxisBorderHeight);
                    if (this.elgrid && this.elgrid.elGridBorders && w.config.grid.show) {
                        this.elgrid.elGridBorders.add(elHorzLine);
                    } else {
                        elXaxis.add(elHorzLine);
                    }
                }
                return elXaxis;
            }
        },
        {
            /**
   * @param {Record<string, any>} [overwriteStyles]
   * @param {boolean} isLeafGroup
   * @param {import('../Graphics').default} graphics
   * @param {any} elXaxisTexts
   * @param {any[]} labels
   * @param {boolean} isXNumeric
   * @param {any} colWidthCb
   */ key: "drawXAxisLabelAndGroup",
            value: function drawXAxisLabelAndGroup(isLeafGroup, graphics, elXaxisTexts, labels, isXNumeric, colWidthCb) {
                var _this, _loop = function(i) {
                    var x = xPos - colWidthCb(i, colWidth) / 2 + w.config.xaxis.labels.offsetX;
                    if (i === 0 && labelsLen === 1 && colWidth / 2 === xPos && dataPoints === 1) {
                        x = w.layout.gridWidth / 2;
                    }
                    var label = _this.axesUtils.getLabel(labels, w.labelData.timescaleLabels, x, i, drawnLabels, xaxisFontSize, isLeafGroup);
                    var offsetYCorrection = 28;
                    if (w.layout.rotateXLabels && isLeafGroup) {
                        offsetYCorrection = 22;
                    }
                    if (w.config.xaxis.title.text && w.config.xaxis.position === "top") {
                        offsetYCorrection += parseFloat(w.config.xaxis.title.style.fontSize) + 2;
                    }
                    if (!isLeafGroup) {
                        offsetYCorrection = offsetYCorrection + parseFloat(xaxisFontSize) + (w.layout.xAxisLabelsHeight - w.layout.xAxisGroupLabelsHeight) + (w.layout.rotateXLabels ? 10 : 0);
                    }
                    var isCategoryTickAmounts = typeof w.config.xaxis.tickAmount !== "undefined" && w.config.xaxis.tickAmount !== "dataPoints" && w.config.xaxis.type !== "datetime";
                    if (isCategoryTickAmounts) {
                        label = _this.axesUtils.checkLabelBasedOnTickamount(i, label, labelsLen);
                    } else {
                        label = _this.axesUtils.checkForOverflowingLabels(i, label, labelsLen, drawnLabels, drawnLabelsRects);
                    }
                    var getCatForeColor = function getCatForeColor() {
                        return isLeafGroup && w.config.xaxis.convertedCatToNumeric ? xaxisForeColors[w.globals.minX + i - 1] : xaxisForeColors[i];
                    };
                    var labelRectWidth = /** @type {any} */ (_b = (_a = label.textRect) == null ? void 0 : _a.width) != null ? _b : 0;
                    var halfWidth = labelRectWidth / 2;
                    var fullyOutsideGrid = label.x + halfWidth < 0;
                    if (w.config.xaxis.labels.show && !fullyOutsideGrid) {
                        var elText = graphics.drawText({
                            x: label.x,
                            y: _this.offY + w.config.xaxis.labels.offsetY + offsetYCorrection - (w.config.xaxis.position === "top" ? w.layout.xAxisHeight + w.config.xaxis.axisTicks.height - 2 : 0),
                            text: label.text,
                            textAnchor: "middle",
                            fontWeight: fontWeight,
                            fontSize: xaxisFontSize,
                            fontFamily: xaxisFontFamily,
                            foreColor: Array.isArray(xaxisForeColors) ? getCatForeColor() : xaxisForeColors,
                            isPlainText: false,
                            cssClass: (isLeafGroup ? "apexcharts-xaxis-label " : "apexcharts-xaxis-group-label ") + cssClass
                        });
                        elXaxisTexts.add(elText);
                        elText.on("click", function(e) {
                            if (typeof w.config.chart.events.xAxisLabelClick === "function") {
                                var opts = Object.assign({}, w, {
                                    labelIndex: i
                                });
                                w.config.chart.events.xAxisLabelClick(e, _this1.ctx, opts);
                            }
                        });
                        if (isLeafGroup) {
                            var elTooltipTitle = BrowserAPIs.createElementNS(SVGNS$2, "title");
                            elTooltipTitle.textContent = Array.isArray(label.text) ? label.text.join(" ") : label.text;
                            elText.node.appendChild(elTooltipTitle);
                            if (label.text !== "") {
                                drawnLabels.push(label.text);
                                drawnLabelsRects.push(label);
                            }
                        }
                    }
                    if (i < labelsLen - 1) {
                        xPos = xPos + colWidthCb(i + 1, colWidth);
                    }
                };
                var _this1 = this;
                var overwriteStyles = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : {};
                var _a, _b;
                var drawnLabels = [];
                var drawnLabelsRects = [];
                var w = this.w;
                var xaxisFontSize = overwriteStyles.xaxisFontSize || this.xaxisFontSize;
                var xaxisFontFamily = overwriteStyles.xaxisFontFamily || this.xaxisFontFamily;
                var xaxisForeColors = overwriteStyles.xaxisForeColors || this.xaxisForeColors;
                var fontWeight = overwriteStyles.fontWeight || w.config.xaxis.labels.style.fontWeight;
                var cssClass = overwriteStyles.cssClass || w.config.xaxis.labels.style.cssClass;
                var colWidth;
                var xPos = w.globals.padHorizontal;
                var labelsLen = labels.length;
                var dataPoints = w.config.xaxis.type === "category" ? w.globals.dataPoints : labelsLen;
                if (dataPoints === 0 && labelsLen > dataPoints) dataPoints = labelsLen;
                if (isXNumeric) {
                    var len = Math.max(Number(w.config.xaxis.tickAmount) || 1, dataPoints > 1 ? dataPoints - 1 : dataPoints);
                    colWidth = w.layout.gridWidth / Math.min(len, labelsLen - 1);
                    xPos = xPos + colWidthCb(0, colWidth) / 2 + w.config.xaxis.labels.offsetX;
                } else {
                    colWidth = w.layout.gridWidth / dataPoints;
                    xPos = xPos + colWidthCb(0, colWidth) + w.config.xaxis.labels.offsetX;
                }
                for(var i = 0; i <= labelsLen - 1; i++)_this = this, _loop(i);
            }
        },
        {
            // this actually becomes the vertical axis (for bar charts)
            /**
   * @param {number} realIndex
   */ key: "drawXaxisInversed",
            value: function drawXaxisInversed(realIndex) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var translateYAxisX = w.config.yaxis[0].opposite ? w.globals.translateYAxisX[realIndex] : 0;
                var elYaxis = graphics.group({
                    class: "apexcharts-yaxis apexcharts-xaxis-inversed",
                    rel: realIndex
                });
                var elYaxisTexts = graphics.group({
                    class: "apexcharts-yaxis-texts-g apexcharts-xaxis-inversed-texts-g",
                    transform: "translate(" + translateYAxisX + ", 0)"
                });
                elYaxis.add(elYaxisTexts);
                var labels = [];
                if (w.config.yaxis[realIndex].show) {
                    for(var i = 0; i < this.xaxisLabels.length; i++){
                        labels.push(this.xaxisLabels[i]);
                    }
                }
                var colHeight = w.layout.gridHeight / labels.length;
                var yPos = -(colHeight / 2.2);
                var lbFormatter = w.formatters.yLabelFormatters[0];
                var ylabels = w.config.yaxis[0].labels;
                if (ylabels.show) {
                    var _this1, _loop = function(i1) {
                        var label = typeof labels[i1] === "undefined" ? "" : labels[i1];
                        label = lbFormatter(label, {
                            seriesIndex: realIndex,
                            dataPointIndex: i1,
                            w: w
                        });
                        var yColors = _this1.axesUtils.getYAxisForeColor(ylabels.style.colors, realIndex);
                        var getForeColor = function getForeColor() {
                            return Array.isArray(yColors) ? yColors[i1] : yColors;
                        };
                        var multiY = 0;
                        if (Array.isArray(label)) {
                            multiY = label.length / 2 * parseInt(ylabels.style.fontSize, 10);
                        }
                        var offsetX = ylabels.offsetX - 15;
                        var textAnchor = "end";
                        if (_this1.yaxis.opposite) {
                            textAnchor = "start";
                        }
                        if (w.config.yaxis[0].labels.align === "left") {
                            offsetX = ylabels.offsetX;
                            textAnchor = "start";
                        } else if (w.config.yaxis[0].labels.align === "center") {
                            offsetX = ylabels.offsetX;
                            textAnchor = "middle";
                        } else if (w.config.yaxis[0].labels.align === "right") {
                            textAnchor = "end";
                        }
                        var elLabel = graphics.drawText({
                            x: offsetX,
                            y: yPos + colHeight + ylabels.offsetY - multiY,
                            text: label,
                            textAnchor: textAnchor,
                            foreColor: getForeColor(),
                            fontSize: ylabels.style.fontSize,
                            fontFamily: ylabels.style.fontFamily,
                            fontWeight: ylabels.style.fontWeight,
                            isPlainText: false,
                            cssClass: "apexcharts-yaxis-label " + ylabels.style.cssClass,
                            maxWidth: ylabels.maxWidth
                        });
                        elYaxisTexts.add(elLabel);
                        elLabel.on("click", function(e) {
                            if (typeof w.config.chart.events.xAxisLabelClick === "function") {
                                var opts = Object.assign({}, w, {
                                    labelIndex: i1
                                });
                                w.config.chart.events.xAxisLabelClick(e, _this.ctx, opts);
                            }
                        });
                        var elTooltipTitle = BrowserAPIs.createElementNS(SVGNS$2, "title");
                        elTooltipTitle.textContent = Array.isArray(label) ? label.join(" ") : label;
                        elLabel.node.appendChild(elTooltipTitle);
                        if (w.config.yaxis[realIndex].labels.rotate !== 0) {
                            var labelRotatingCenter = graphics.rotateAroundCenter(elLabel.node);
                            elLabel.node.setAttribute("transform", "rotate(".concat(w.config.yaxis[realIndex].labels.rotate, " 0 ").concat(labelRotatingCenter.y, ")"));
                        }
                        yPos = yPos + colHeight;
                    };
                    for(var i1 = 0; i1 <= labels.length - 1; i1++)_this1 = this, _loop(i1);
                }
                if (w.config.yaxis[0].title.text !== void 0) {
                    var elXaxisTitle = graphics.group({
                        class: "apexcharts-yaxis-title apexcharts-xaxis-title-inversed",
                        transform: "translate(" + translateYAxisX + ", 0)"
                    });
                    var elXAxisTitleText = graphics.drawText({
                        x: w.config.yaxis[0].title.offsetX,
                        y: w.layout.gridHeight / 2 + w.config.yaxis[0].title.offsetY,
                        text: w.config.yaxis[0].title.text,
                        textAnchor: "middle",
                        foreColor: w.config.yaxis[0].title.style.color,
                        fontSize: w.config.yaxis[0].title.style.fontSize,
                        fontWeight: w.config.yaxis[0].title.style.fontWeight,
                        fontFamily: w.config.yaxis[0].title.style.fontFamily,
                        cssClass: "apexcharts-yaxis-title-text " + w.config.yaxis[0].title.style.cssClass
                    });
                    elXaxisTitle.add(elXAxisTitleText);
                    elYaxis.add(elXaxisTitle);
                }
                var offX = 0;
                if (this.isCategoryBarHorizontal && w.config.yaxis[0].opposite) {
                    offX = w.layout.gridWidth;
                }
                var axisBorder = w.config.xaxis.axisBorder;
                if (axisBorder.show) {
                    var elVerticalLine = graphics.drawLine(w.globals.padHorizontal + axisBorder.offsetX + offX, 1 + axisBorder.offsetY, w.globals.padHorizontal + axisBorder.offsetX + offX, w.layout.gridHeight + axisBorder.offsetY, axisBorder.color, 0);
                    if (this.elgrid && this.elgrid.elGridBorders && w.config.grid.show) {
                        this.elgrid.elGridBorders.add(elVerticalLine);
                    } else {
                        elYaxis.add(elVerticalLine);
                    }
                }
                if (w.config.yaxis[0].axisTicks.show) {
                    this.axesUtils.drawYAxisTicks(offX, labels.length, w.config.yaxis[0].axisBorder, w.config.yaxis[0].axisTicks, 0, colHeight, elYaxis);
                }
                return elYaxis;
            }
        },
        {
            /**
   * @param {number} x1
   * @param {number} y2
   * @param {any} appendToElement
   */ key: "drawXaxisTicks",
            value: function drawXaxisTicks(x1, y2, appendToElement) {
                var w = this.w;
                var x2 = x1;
                if (x1 < 0 || x1 - 2 > w.layout.gridWidth) return;
                var y1 = this.offY + w.config.xaxis.axisTicks.offsetY;
                y2 = y2 + y1 + w.config.xaxis.axisTicks.height;
                if (w.config.xaxis.position === "top") {
                    y2 = y1 - w.config.xaxis.axisTicks.height;
                }
                if (w.config.xaxis.axisTicks.show) {
                    var graphics = new Graphics(this.w);
                    var line = graphics.drawLine(x1 + w.config.xaxis.axisTicks.offsetX, y1 + w.config.xaxis.offsetY, x2 + w.config.xaxis.axisTicks.offsetX, y2 + w.config.xaxis.offsetY, w.config.xaxis.axisTicks.color);
                    appendToElement.add(line);
                    line.node.classList.add("apexcharts-xaxis-tick");
                }
            }
        },
        {
            key: "getXAxisTicksPositions",
            value: function getXAxisTicksPositions() {
                var w = this.w;
                var xAxisTicksPositions = [];
                var xCount = this.xaxisLabels.length;
                var x1 = w.globals.padHorizontal;
                if (w.labelData.timescaleLabels.length > 0) {
                    for(var i = 0; i < xCount; i++){
                        x1 = this.xaxisLabels[i].position;
                        xAxisTicksPositions.push(x1);
                    }
                } else {
                    var xCountForCategoryCharts = xCount;
                    for(var i1 = 0; i1 < xCountForCategoryCharts; i1++){
                        var x1Count = xCountForCategoryCharts;
                        if (w.axisFlags.isXNumeric && w.config.chart.type !== "bar") {
                            x1Count -= 1;
                        }
                        x1 = x1 + w.layout.gridWidth / x1Count;
                        xAxisTicksPositions.push(x1);
                    }
                }
                return xAxisTicksPositions;
            }
        },
        {
            // to rotate x-axis labels or to put ... for longer text in xaxis
            key: "xAxisLabelCorrections",
            value: function xAxisLabelCorrections() {
                var _a, _b, _c;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var xAxis = w.dom.baseEl.querySelector(".apexcharts-xaxis-texts-g");
                var xAxisTexts = w.dom.baseEl.querySelectorAll(".apexcharts-xaxis-texts-g text:not(.apexcharts-xaxis-group-label)");
                var yAxisTextsInversed = w.dom.baseEl.querySelectorAll(".apexcharts-yaxis-inversed text");
                var xAxisTextsInversed = w.dom.baseEl.querySelectorAll(".apexcharts-xaxis-inversed-texts-g text tspan");
                if (w.layout.rotateXLabels || w.config.xaxis.labels.rotateAlways) {
                    for(var xat = 0; xat < xAxisTexts.length; xat++){
                        var textRotatingCenter = graphics.rotateAroundCenter(xAxisTexts[xat]);
                        textRotatingCenter.y = textRotatingCenter.y - 1;
                        textRotatingCenter.x = textRotatingCenter.x + 1;
                        xAxisTexts[xat].setAttribute("transform", "rotate(".concat(w.config.xaxis.labels.rotate, " ").concat(textRotatingCenter.x, " ").concat(textRotatingCenter.y, ")"));
                        xAxisTexts[xat].setAttribute("text-anchor", "end");
                        xAxis == null ? void 0 : xAxis.setAttribute("transform", "translate(0, ".concat(-10, ")"));
                        var tSpan = xAxisTexts[xat].childNodes;
                        if (w.config.xaxis.labels.trim) {
                            Array.prototype.forEach.call(tSpan, function(ts) {
                                graphics.placeTextWithEllipsis(ts, ts.textContent, w.layout.xAxisLabelsHeight - (w.config.legend.position === "bottom" ? 20 : 10));
                            });
                        }
                    }
                } else {
                    var width = w.layout.gridWidth / (w.labelData.labels.length + 1);
                    for(var xat1 = 0; xat1 < xAxisTexts.length; xat1++){
                        var tSpan1 = xAxisTexts[xat1].childNodes;
                        if (w.config.xaxis.labels.trim && w.config.xaxis.type !== "datetime") {
                            Array.prototype.forEach.call(tSpan1, function(ts) {
                                graphics.placeTextWithEllipsis(ts, ts.textContent, width);
                            });
                        }
                    }
                }
                if (yAxisTextsInversed.length > 0) {
                    var firstLabelPosX = /** @type {SVGGraphicsElement} */ yAxisTextsInversed[yAxisTextsInversed.length - 1].getBBox();
                    var lastLabelPosX = /** @type {SVGGraphicsElement} */ yAxisTextsInversed[0].getBBox();
                    if (firstLabelPosX.x < -20) {
                        (_a = yAxisTextsInversed[yAxisTextsInversed.length - 1].parentNode) == null ? void 0 : _a.removeChild(yAxisTextsInversed[yAxisTextsInversed.length - 1]);
                    }
                    if (lastLabelPosX.x + lastLabelPosX.width > w.layout.gridWidth && !w.globals.isBarHorizontal) {
                        (_b = yAxisTextsInversed[0].parentNode) == null ? void 0 : _b.removeChild(yAxisTextsInversed[0]);
                    }
                    for(var xat2 = 0; xat2 < xAxisTextsInversed.length; xat2++){
                        graphics.placeTextWithEllipsis(xAxisTextsInversed[xat2], (_c = xAxisTextsInversed[xat2].textContent) != null ? _c : "", w.config.yaxis[0].labels.maxWidth - (w.config.yaxis[0].title.text ? parseFloat(w.config.yaxis[0].title.style.fontSize) * 2 : 0) - 15);
                    }
                }
            }
        }
    ]);
    return XAxis;
}();
var Grid = /*#__PURE__*/ function() {
    "use strict";
    function Grid(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Grid);
        this.w = w;
        this.ctx = ctx;
        this.xaxisLabels = w.labelData.labels.slice();
        this.axesUtils = new AxesUtils(ctx.w, {
            theme: ctx.theme,
            timeScale: ctx.timeScale
        });
        this.isRangeBar = w.rangeData.seriesRange.length && w.globals.isBarHorizontal;
        if (w.labelData.timescaleLabels.length > 0) {
            this.xaxisLabels = w.labelData.timescaleLabels.slice();
        }
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Grid, [
        {
            /**
   * @param {any} elGrid
   */ key: "drawGridArea",
            value: function drawGridArea() {
                var elGrid = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
                var w = this.w;
                var graphics = new Graphics(this.w);
                if (!elGrid) {
                    elGrid = graphics.group({
                        class: "apexcharts-grid"
                    });
                }
                var elVerticalLine = graphics.drawLine(w.globals.padHorizontal, 1, w.globals.padHorizontal, w.layout.gridHeight, "transparent");
                var elHorzLine = graphics.drawLine(w.globals.padHorizontal, w.layout.gridHeight, w.layout.gridWidth, w.layout.gridHeight, "transparent");
                elGrid.add(elHorzLine);
                elGrid.add(elVerticalLine);
                return elGrid;
            }
        },
        {
            key: "drawGrid",
            value: function drawGrid() {
                var gl = this.w.globals;
                if (gl.axisCharts) {
                    var elgrid = this.renderGrid();
                    this.drawGridArea(elgrid.el);
                    return elgrid;
                }
                return null;
            }
        },
        {
            key: "createGridMask",
            value: function createGridMask() {
                var _Math;
                var w = this.w;
                var gl = w.globals;
                var graphics = new Graphics(this.w);
                var strokeSize = Array.isArray(w.config.stroke.width) ? (_Math = Math).max.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.config.stroke.width)) : w.config.stroke.width;
                var createClipPath = function createClipPath(id) {
                    var clipPath = BrowserAPIs.createElementNS(SVGNS$2, "clipPath");
                    clipPath.setAttribute("id", id);
                    return clipPath;
                };
                w.dom.elGridRectMask = createClipPath("gridRectMask".concat(gl.cuid));
                w.dom.elGridRectBarMask = createClipPath("gridRectBarMask".concat(gl.cuid));
                w.dom.elGridRectMarkerMask = createClipPath("gridRectMarkerMask".concat(gl.cuid));
                w.dom.elForecastMask = createClipPath("forecastMask".concat(gl.cuid));
                w.dom.elNonForecastMask = createClipPath("nonForecastMask".concat(gl.cuid));
                var hasBar = [
                    "bar",
                    "rangeBar",
                    "candlestick",
                    "boxPlot",
                    "violin"
                ].includes(w.config.chart.type) || w.globals.comboBarCount > 0;
                var barWidthLeft = 0;
                var barWidthRight = 0;
                if (hasBar && w.axisFlags.isXNumeric && !w.globals.isBarHorizontal) {
                    barWidthLeft = Math.max(w.layout.gridPad.left, gl.barPadForNumericAxis);
                    barWidthRight = Math.max(w.layout.gridPad.right, gl.barPadForNumericAxis);
                }
                w.dom.elGridRect = graphics.drawRect(-strokeSize / 2 - 2, -strokeSize / 2 - 2, w.layout.gridWidth + strokeSize + 4, w.layout.gridHeight + strokeSize + 4, 0, "#fff");
                w.dom.elGridRectBar = graphics.drawRect(-strokeSize / 2 - barWidthLeft - 2, -strokeSize / 2 - 2, w.layout.gridWidth + strokeSize + barWidthRight + barWidthLeft + 4, w.layout.gridHeight + strokeSize + 4, 0, "#fff");
                var markerSize = w.globals.markers.largestSize;
                w.dom.elGridRectMarker = graphics.drawRect(Math.min(-strokeSize / 2 - barWidthLeft - 2, -markerSize), -markerSize, w.layout.gridWidth + Math.max(strokeSize + barWidthRight + barWidthLeft + 4, markerSize * 2), w.layout.gridHeight + markerSize * 2, 0, "#fff");
                w.dom.elGridRectMask.appendChild(w.dom.elGridRect.node);
                w.dom.elGridRectBarMask.appendChild(w.dom.elGridRectBar.node);
                w.dom.elGridRectMarkerMask.appendChild(w.dom.elGridRectMarker.node);
                var defs = w.dom.elDefs.node;
                defs.appendChild(w.dom.elGridRectMask);
                defs.appendChild(w.dom.elGridRectBarMask);
                defs.appendChild(w.dom.elGridRectMarkerMask);
                defs.appendChild(w.dom.elForecastMask);
                defs.appendChild(w.dom.elNonForecastMask);
            }
        },
        {
            /** @param {{i: any, x1: any, y1: any, x2: any, y2: any, xCount: any, parent: any}} opts */ key: "_drawGridLines",
            value: function _drawGridLines(param) {
                var i = param.i, x1 = param.x1, y1 = param.y1, x2 = param.x2, y2 = param.y2, xCount = param.xCount, parent = param.parent;
                var w = this.w;
                var shouldDraw = function shouldDraw() {
                    if (i === 0 && w.globals.skipFirstTimelinelabel) return false;
                    if (i === xCount - 1 && w.globals.skipLastTimelinelabel && !w.config.xaxis.labels.formatter) return false;
                    if (w.config.chart.type === "radar") return false;
                    return true;
                };
                if (shouldDraw()) {
                    if (w.config.grid.xaxis.lines.show) {
                        this._drawGridLine({
                            i: i,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2,
                            xCount: xCount,
                            parent: parent
                        });
                    }
                    var y_2 = 0;
                    if (w.labelData.hasXaxisGroups && w.config.xaxis.tickPlacement === "between") {
                        var groups = w.labelData.groups;
                        if (groups) {
                            var gacc = 0;
                            for(var gi = 0; gacc < i && gi < groups.length; gi++){
                                gacc += groups[gi].cols;
                            }
                            if (gacc === i) {
                                y_2 = w.layout.xAxisLabelsHeight * 0.6;
                            }
                        }
                    }
                    var xAxis = new XAxis(this.w, this.ctx);
                    xAxis.drawXaxisTicks(x1, y_2, w.dom.elGraphical);
                }
            }
        },
        {
            /** @param {{i: any, x1: any, y1: any, x2: any, y2: any, xCount: any, parent: any}} opts */ key: "_drawGridLine",
            value: function _drawGridLine(param) {
                var i = param.i, x1 = param.x1, y1 = param.y1, x2 = param.x2, y2 = param.y2, xCount = param.xCount, parent = param.parent;
                var w = this.w;
                var isHorzLine = parent.node.classList.contains("apexcharts-gridlines-horizontal");
                var offX = w.globals.barPadForNumericAxis;
                var excludeBorders = y1 === 0 && y2 === 0 || x1 === 0 && x2 === 0 || y1 === w.layout.gridHeight && y2 === w.layout.gridHeight || w.globals.isBarHorizontal && (i === 0 || i === xCount - 1);
                var graphics = new Graphics(this.w);
                var line = graphics.drawLine(x1 - (isHorzLine ? offX : 0), y1, x2 + (isHorzLine ? offX : 0), y2, w.config.grid.borderColor, w.config.grid.strokeDashArray);
                line.node.classList.add("apexcharts-gridline");
                if (excludeBorders && w.config.grid.show) {
                    this.elGridBorders.add(line);
                } else {
                    parent.add(line);
                }
            }
        },
        {
            /** @param {{c: any, x1: any, y1: any, x2: any, y2: any, type: any}} opts */ key: "_drawGridBandRect",
            value: function _drawGridBandRect(param) {
                var c = param.c, x1 = param.x1, y1 = param.y1, x2 = param.x2, y2 = param.y2, type = param.type;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var offX = w.globals.barPadForNumericAxis;
                var color = w.config.grid[type].colors[c];
                var rect = graphics.drawRect(x1 - (type === "row" ? offX : 0), y1, x2 + (type === "row" ? offX * 2 : 0), y2, 0, color, w.config.grid[type].opacity);
                this.elg.add(rect);
                rect.attr("clip-path", "url(#gridRectMask".concat(w.globals.cuid, ")"));
                rect.node.classList.add("apexcharts-grid-".concat(type));
            }
        },
        {
            /** @param {{xCount: any, tickAmount: any}} opts */ key: "_drawXYLines",
            value: function _drawXYLines(param) {
                var _this = this;
                var xCount = param.xCount, tickAmount = param.tickAmount;
                var _a;
                var w = this.w;
                var datetimeLines = function datetimeLines(param) {
                    var xC = param.xC, x1 = param.x1, y1 = param.y1, x2 = param.x2, y2 = param.y2;
                    for(var i = 0; i < xC; i++){
                        x1 = /** @type {any} */ _this.xaxisLabels[i].position;
                        x2 = /** @type {any} */ _this.xaxisLabels[i].position;
                        _this._drawGridLines({
                            i: i,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2,
                            xCount: xCount,
                            parent: _this.elgridLinesV
                        });
                    }
                };
                var categoryLines = function categoryLines(param) {
                    var xC = param.xC, x1 = param.x1, y1 = param.y1, x2 = param.x2, y2 = param.y2;
                    for(var i = 0; i < xC + (w.axisFlags.isXNumeric ? 0 : 1); i++){
                        if (i === 0 && xC === 1 && w.globals.dataPoints === 1) {
                            x1 = w.layout.gridWidth / 2;
                            x2 = x1;
                        }
                        _this._drawGridLines({
                            i: i,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2,
                            xCount: xCount,
                            parent: _this.elgridLinesV
                        });
                        x1 += w.layout.gridWidth / (w.axisFlags.isXNumeric ? xC - 1 : xC);
                        x2 = x1;
                    }
                };
                if (w.config.grid.xaxis.lines.show || w.config.xaxis.axisTicks.show) {
                    var x1 = w.globals.padHorizontal;
                    var y1 = 0;
                    var x2;
                    var y2 = w.layout.gridHeight;
                    if (w.labelData.timescaleLabels.length) {
                        datetimeLines({
                            xC: xCount,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2
                        });
                    } else {
                        if (w.axisFlags.isXNumeric) {
                            xCount = (_a = w.globals.xAxisScale) == null ? void 0 : _a.result.length;
                        }
                        categoryLines({
                            xC: xCount,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2
                        });
                    }
                }
                if (w.config.grid.yaxis.lines.show) {
                    var x11 = 0;
                    var y11 = 0;
                    var y21 = 0;
                    var x21 = w.layout.gridWidth;
                    var tA = tickAmount + 1;
                    if (this.isRangeBar) {
                        tA = w.labelData.labels.length;
                    }
                    for(var i = 0; i < tA + (this.isRangeBar ? 1 : 0); i++){
                        this._drawGridLine({
                            i: i,
                            xCount: tA + (this.isRangeBar ? 1 : 0),
                            x1: x11,
                            y1: y11,
                            x2: x21,
                            y2: y21,
                            parent: this.elgridLinesH
                        });
                        y11 += w.layout.gridHeight / (this.isRangeBar ? tA : tickAmount);
                        y21 = y11;
                    }
                }
            }
        },
        {
            /** @param {{ xCount?: any, tickAmount?: any }} opts */ key: "_drawInvertedXYLines",
            value: function _drawInvertedXYLines(param) {
                var xCount = param.xCount;
                var w = this.w;
                if (w.config.grid.xaxis.lines.show || w.config.xaxis.axisTicks.show) {
                    var x1 = w.globals.padHorizontal;
                    var y1 = 0;
                    var x2;
                    var y2 = w.layout.gridHeight;
                    for(var i = 0; i < xCount + 1; i++){
                        if (w.config.grid.xaxis.lines.show) {
                            this._drawGridLine({
                                i: i,
                                xCount: xCount + 1,
                                x1: x1,
                                y1: y1,
                                x2: x2,
                                y2: y2,
                                parent: this.elgridLinesV
                            });
                        }
                        var xAxis = new XAxis(this.w, this.ctx);
                        xAxis.drawXaxisTicks(x1, 0, w.dom.elGraphical);
                        x1 += w.layout.gridWidth / xCount;
                        x2 = x1;
                    }
                }
                if (w.config.grid.yaxis.lines.show) {
                    var x11 = 0;
                    var y11 = 0;
                    var y21 = 0;
                    var x21 = w.layout.gridWidth;
                    for(var i1 = 0; i1 < w.globals.dataPoints + 1; i1++){
                        this._drawGridLine({
                            i: i1,
                            xCount: w.globals.dataPoints + 1,
                            x1: x11,
                            y1: y11,
                            x2: x21,
                            y2: y21,
                            parent: this.elgridLinesH
                        });
                        y11 += w.layout.gridHeight / w.globals.dataPoints;
                        y21 = y11;
                    }
                }
            }
        },
        {
            key: "renderGrid",
            value: function renderGrid() {
                var _a, _b, _c;
                var w = this.w;
                var gl = w.globals;
                var graphics = new Graphics(this.w);
                this.elg = graphics.group({
                    class: "apexcharts-grid"
                });
                this.elgridLinesH = graphics.group({
                    class: "apexcharts-gridlines-horizontal"
                });
                this.elgridLinesV = graphics.group({
                    class: "apexcharts-gridlines-vertical"
                });
                this.elGridBorders = graphics.group({
                    class: "apexcharts-grid-borders"
                });
                this.elg.add(this.elgridLinesH);
                this.elg.add(this.elgridLinesV);
                if (!w.config.grid.show) {
                    this.elgridLinesV.hide();
                    this.elgridLinesH.hide();
                    this.elGridBorders.hide();
                }
                var gridAxisIndex = 0;
                while(gridAxisIndex < gl.seriesYAxisMap.length && gl.ignoreYAxisIndexes.includes(gridAxisIndex)){
                    gridAxisIndex++;
                }
                if (gridAxisIndex === gl.seriesYAxisMap.length) {
                    gridAxisIndex = 0;
                }
                var yTickAmount = gl.yAxisScale[gridAxisIndex].result.length - 1;
                var xCount;
                if (!gl.isBarHorizontal || this.isRangeBar) {
                    xCount = this.xaxisLabels.length;
                    if (this.isRangeBar) {
                        yTickAmount = w.labelData.labels.length;
                        if (w.config.xaxis.tickAmount && w.config.xaxis.labels.formatter) {
                            xCount = w.config.xaxis.tickAmount;
                        }
                        if (((_c = (_b = (_a = gl.yAxisScale) == null ? void 0 : _a[gridAxisIndex]) == null ? void 0 : _b.result) == null ? void 0 : _c.length) > 0 && w.config.xaxis.type !== "datetime") {
                            xCount = gl.yAxisScale[gridAxisIndex].result.length - 1;
                        }
                    }
                    this._drawXYLines({
                        xCount: xCount,
                        tickAmount: yTickAmount
                    });
                } else {
                    xCount = yTickAmount;
                    yTickAmount = gl.xTickAmount;
                    this._drawInvertedXYLines({
                        xCount: xCount,
                        tickAmount: yTickAmount
                    });
                }
                this.drawGridBands(xCount, yTickAmount);
                return {
                    el: this.elg,
                    elGridBorders: this.elGridBorders,
                    xAxisTickWidth: w.layout.gridWidth / xCount
                };
            }
        },
        {
            /**
   * @param {number} xCount
   * @param {number} tickAmount
   */ key: "drawGridBands",
            value: function drawGridBands(xCount, tickAmount) {
                var _this = this;
                var _a, _b, _c, _d, _e;
                var w = this.w;
                var drawBands = function drawBands(type, count, x1, y1, x2, y2) {
                    for(var i = 0, c = 0; i < count; i++, c++){
                        if (c >= w.config.grid[type].colors.length) {
                            c = 0;
                        }
                        _this._drawGridBandRect({
                            c: c,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2,
                            type: type
                        });
                        y1 += w.layout.gridHeight / tickAmount;
                    }
                };
                if (((_a = w.config.grid.row.colors) == null ? void 0 : _a.length) > 0) {
                    drawBands("row", tickAmount, 0, 0, w.layout.gridWidth, w.layout.gridHeight / tickAmount);
                }
                if (((_b = w.config.grid.column.colors) == null ? void 0 : _b.length) > 0) {
                    var xc = !w.globals.isBarHorizontal && w.config.xaxis.tickPlacement === "on" && (w.config.xaxis.type === "category" || w.config.xaxis.convertedCatToNumeric) ? xCount - 1 : xCount;
                    if (w.axisFlags.isXNumeric) {
                        xc = ((_d = (_c = w.globals.xAxisScale) == null ? void 0 : _c.result.length) != null ? _d : 1) - 1;
                    }
                    var x1 = w.globals.padHorizontal;
                    var y1 = 0;
                    var x2 = w.globals.padHorizontal + w.layout.gridWidth / xc;
                    var y2 = w.layout.gridHeight;
                    for(var i = 0, c = 0; i < xCount; i++, c++){
                        if (c >= w.config.grid.column.colors.length) {
                            c = 0;
                        }
                        if (w.config.xaxis.type === "datetime") {
                            x1 = /** @type {any} */ this.xaxisLabels[i].position;
                            x2 = /** @type {any} */ (((_e = this.xaxisLabels[i + 1]) == null ? void 0 : _e.position) || w.layout.gridWidth) - /** @type {any} */ this.xaxisLabels[i].position;
                        }
                        this._drawGridBandRect({
                            c: c,
                            x1: x1,
                            y1: y1,
                            x2: x2,
                            y2: y2,
                            type: "column"
                        });
                        x1 += w.layout.gridWidth / xc;
                    }
                }
            }
        }
    ]);
    return Grid;
}();
var Scales = /*#__PURE__*/ function() {
    "use strict";
    function Scales(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Scales);
        this.w = w;
        this.coreUtils = new CoreUtils(this.w);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Scales, [
        {
            // http://stackoverflow.com/questions/326679/choosing-an-attractive-linear-scale-for-a-graphs-y-axis
            // This routine creates the Y axis values for a graph.
            /**
   * @param {number} yMin
   * @param {number} yMax
   */ key: "niceScale",
            value: function niceScale(yMin, yMax) {
                var index = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
                var jsPrecision = 1e-11;
                var w = this.w;
                var gl = w.globals;
                var axisCnf;
                var maxTicks;
                var gotMin;
                var gotMax;
                if (gl.isBarHorizontal) {
                    axisCnf = w.config.xaxis;
                    maxTicks = Math.max((gl.svgWidth - 100) / 25, 2);
                } else {
                    axisCnf = w.config.yaxis[index];
                    maxTicks = Math.max((gl.svgHeight - 100) / 15, 2);
                }
                if (!Utils$1.isNumber(maxTicks)) {
                    maxTicks = 10;
                }
                gotMin = axisCnf.min !== void 0 && axisCnf.min !== null;
                gotMax = axisCnf.max !== void 0 && axisCnf.max !== null;
                var gotStepSize = axisCnf.stepSize !== void 0 && axisCnf.stepSize !== null;
                var gotTickAmount = axisCnf.tickAmount !== void 0 && axisCnf.tickAmount !== null;
                var ticks = gotTickAmount ? axisCnf.tickAmount : NICE_SCALE_DEFAULT_TICKS[Math.min(Math.round(maxTicks / 2), NICE_SCALE_DEFAULT_TICKS.length - 1)];
                if (gl.isMultipleYAxis && !gotTickAmount && gl.multiAxisTickAmount > 0) {
                    ticks = gl.multiAxisTickAmount;
                    gotTickAmount = true;
                }
                if (ticks === "dataPoints") {
                    ticks = gl.dataPoints - 1;
                } else {
                    ticks = Math.abs(Math.round(ticks));
                }
                if (yMin === Number.MIN_VALUE && yMax === 0 || !Utils$1.isNumber(yMin) && !Utils$1.isNumber(yMax) || yMin === Number.MIN_VALUE && yMax === -Number.MAX_VALUE) {
                    yMin = Utils$1.isNumber(axisCnf.min) ? axisCnf.min : 0;
                    yMax = Utils$1.isNumber(axisCnf.max) ? axisCnf.max : yMin + ticks;
                    gl.allSeriesCollapsed = false;
                }
                if (yMin > yMax) {
                    console.warn("axis.min cannot be greater than axis.max: swapping min and max");
                    var temp = yMax;
                    yMax = yMin;
                    yMin = temp;
                } else if (yMin === yMax) {
                    yMin = yMin === 0 ? 0 : yMin - 1;
                    yMax = yMax === 0 ? 2 : yMax + 1;
                }
                var result = [];
                if (ticks < 1) {
                    ticks = 1;
                }
                var tiks = ticks;
                var range = Math.abs(yMax - yMin);
                var proximityRatio = 0.15;
                if (!gotMin && yMin > 0 && yMin / range < proximityRatio) {
                    yMin = 0;
                    gotMin = true;
                }
                if (!gotMax && yMax < 0 && -yMax / range < proximityRatio) {
                    yMax = 0;
                    gotMax = true;
                }
                range = Math.abs(yMax - yMin);
                var stepSize = range / tiks;
                var niceStep = stepSize;
                var mag = Math.floor(Math.log10(niceStep));
                var magPow = Math.pow(10, mag);
                var magMsd = Math.ceil(niceStep / magPow);
                magMsd = NICE_SCALE_ALLOWED_MAG_MSD[gl.yValueDecimal === 0 ? 0 : 1][magMsd];
                niceStep = magMsd * magPow;
                stepSize = niceStep;
                if (gl.isBarHorizontal && axisCnf.stepSize && axisCnf.type !== "datetime") {
                    stepSize = axisCnf.stepSize;
                    gotStepSize = true;
                } else if (gotStepSize) {
                    stepSize = axisCnf.stepSize;
                }
                if (gotStepSize) {
                    if (axisCnf.forceNiceScale) {
                        var stepMag = Math.floor(Math.log10(stepSize));
                        stepSize *= Math.pow(10, mag - stepMag);
                    }
                }
                if (gotMin && gotMax) {
                    var crudeStep = range / tiks;
                    if (gotTickAmount) {
                        if (gotStepSize) {
                            if (Utils$1.mod(range, stepSize) != 0) {
                                var gcdStep = Utils$1.getGCD(stepSize, crudeStep);
                                if (crudeStep / gcdStep < 10) {
                                    stepSize = gcdStep;
                                } else {
                                    stepSize = crudeStep;
                                }
                            } else {
                                if (Utils$1.mod(stepSize, crudeStep) == 0) {
                                    stepSize = crudeStep;
                                } else {
                                    crudeStep = stepSize;
                                    gotTickAmount = false;
                                }
                            }
                        } else {
                            stepSize = crudeStep;
                        }
                    } else {
                        if (gotStepSize) {
                            if (Utils$1.mod(range, stepSize) == 0) {
                                crudeStep = stepSize;
                            } else {
                                stepSize = crudeStep;
                            }
                        } else {
                            if (Utils$1.mod(range, stepSize) == 0) {
                                crudeStep = stepSize;
                            } else {
                                tiks = Math.ceil(range / stepSize);
                                crudeStep = range / tiks;
                                var gcdStep1 = Utils$1.getGCD(range, stepSize);
                                if (range / gcdStep1 < maxTicks) {
                                    crudeStep = gcdStep1;
                                }
                                stepSize = crudeStep;
                            }
                        }
                    }
                    tiks = Math.round(range / stepSize);
                } else {
                    if (!gotMin && !gotMax) {
                        if (gl.isMultipleYAxis && gotTickAmount) {
                            var tMin = stepSize * Math.floor(yMin / stepSize);
                            var tMax = tMin + stepSize * tiks;
                            if (tMax < yMax) {
                                stepSize *= 2;
                            }
                            yMin = tMin;
                            tMax = yMax;
                            yMax = yMin + stepSize * tiks;
                            range = Math.abs(yMax - yMin);
                            if (yMin > 0 && yMin < Math.abs(tMax - yMax)) {
                                yMin = 0;
                                yMax = stepSize * tiks;
                            }
                            if (yMax < 0 && -yMax < Math.abs(tMin - yMin)) {
                                yMax = 0;
                                yMin = -stepSize * tiks;
                            }
                        } else {
                            yMin = stepSize * Math.floor(yMin / stepSize);
                            yMax = stepSize * Math.ceil(yMax / stepSize);
                        }
                    } else if (gotMax) {
                        if (gotTickAmount) {
                            yMin = yMax - stepSize * tiks;
                        } else {
                            var yMinPrev = yMin;
                            yMin = stepSize * Math.floor(yMin / stepSize);
                            if (Math.abs(yMax - yMin) / Utils$1.getGCD(range, stepSize) > maxTicks) {
                                yMin = yMax - stepSize * ticks;
                                yMin += stepSize * Math.floor((yMinPrev - yMin) / stepSize);
                            }
                        }
                    } else if (gotMin) {
                        if (gotTickAmount) {
                            yMax = yMin + stepSize * tiks;
                        } else {
                            var yMaxPrev = yMax;
                            yMax = stepSize * Math.ceil(yMax / stepSize);
                            if (Math.abs(yMax - yMin) / Utils$1.getGCD(range, stepSize) > maxTicks) {
                                yMax = yMin + stepSize * ticks;
                                yMax += stepSize * Math.ceil((yMaxPrev - yMax) / stepSize);
                            }
                        }
                    }
                    range = Math.abs(yMax - yMin);
                    stepSize = Utils$1.getGCD(range, stepSize);
                    tiks = Math.round(range / stepSize);
                }
                if (!gotTickAmount && !(gotMin || gotMax)) {
                    tiks = Math.ceil((range - jsPrecision) / (stepSize + jsPrecision));
                    if (tiks > 16 && Utils$1.getPrimeFactors(tiks).length < 2) {
                        tiks++;
                    }
                }
                if (!gotTickAmount && axisCnf.forceNiceScale && gl.yValueDecimal === 0 && tiks > range) {
                    tiks = range;
                    stepSize = Math.round(range / tiks);
                }
                if (tiks > maxTicks && (!(gotTickAmount || gotStepSize) || axisCnf.forceNiceScale)) {
                    var pf = Utils$1.getPrimeFactors(tiks);
                    var last = pf.length - 1;
                    var tt = tiks;
                    reduceLoop: for(var xFactors = 0; xFactors < last; xFactors++){
                        for(var lowest = 0; lowest <= last - xFactors; lowest++){
                            var stop = Math.min(lowest + xFactors, last);
                            var t = tt;
                            var div = 1;
                            for(var next = lowest; next <= stop; next++){
                                div *= pf[next];
                            }
                            t /= div;
                            if (t < maxTicks) {
                                tt = t;
                                break reduceLoop;
                            }
                        }
                    }
                    if (tt === tiks) {
                        stepSize = range;
                    } else {
                        stepSize = range / tt;
                    }
                    tiks = Math.round(range / stepSize);
                }
                if (gl.isMultipleYAxis && gl.multiAxisTickAmount == 0 && gl.ignoreYAxisIndexes.indexOf(index) < 0) {
                    gl.multiAxisTickAmount = tiks;
                }
                var val = yMin - stepSize;
                var err = stepSize * jsPrecision;
                do {
                    val += stepSize;
                    result.push(Utils$1.stripNumber(val, 7));
                }while (yMax - val > err)
                return {
                    result: result,
                    niceMin: result[0],
                    niceMax: result[result.length - 1]
                };
            }
        },
        {
            /** @param {number} yMin @param {number} yMax @param {number|string} ticks @param {number} index @param {number|undefined} step */ key: "linearScale",
            value: function linearScale(yMin, yMax) {
                var ticks = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 10, index = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0, step = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : void 0;
                var range = Math.abs(yMax - yMin);
                var result = [];
                if (yMin === yMax) {
                    result = [
                        yMin
                    ];
                    return {
                        result: result,
                        niceMin: result[0],
                        niceMax: result[result.length - 1]
                    };
                }
                ticks = this._adjustTicksForSmallRange(ticks, index, range);
                if (/** @type {any} */ ticks === "dataPoints") {
                    ticks = this.w.globals.dataPoints - 1;
                }
                var ticksNum = /** @type {number} */ ticks;
                if (!step) {
                    step = range / ticksNum;
                }
                var MIN_PRECISION = 2;
                if (step !== 0 && isFinite(step)) {
                    var magnitude = Math.floor(Math.log10(Math.abs(step)));
                    var precision = Math.max(MIN_PRECISION, -magnitude + MIN_PRECISION);
                    var multiplier = Math.pow(10, precision);
                    step = Math.round((step + Number.EPSILON) * multiplier) / multiplier;
                }
                var tickCount = ticks === Number.MAX_VALUE ? 5 : ticksNum;
                if (ticks === Number.MAX_VALUE) {
                    step = 1;
                }
                var v = yMin;
                while(tickCount >= 0){
                    result.push(v);
                    v = Utils$1.preciseAddition(v, step);
                    tickCount -= 1;
                }
                return {
                    result: result,
                    niceMin: result[0],
                    niceMax: result[result.length - 1]
                };
            }
        },
        {
            /**
   * @param {number} yMin
   * @param {number} yMax
   * @param {number} base
   */ key: "logarithmicScaleNice",
            value: function logarithmicScaleNice(yMin, yMax, base) {
                if (yMax <= 0) yMax = Math.max(yMin, base);
                if (yMin <= 0) yMin = Math.min(yMax, base);
                var logs = [];
                var logMax = Math.ceil(Math.log(yMax) / Math.log(base) + 1);
                var logMin = Math.floor(Math.log(yMin) / Math.log(base));
                for(var i = logMin; i < logMax; i++){
                    logs.push(Math.pow(base, i));
                }
                return {
                    result: logs,
                    niceMin: logs[0],
                    niceMax: logs[logs.length - 1]
                };
            }
        },
        {
            /**
   * @param {number} yMin
   * @param {number} yMax
   * @param {number} base
   */ key: "logarithmicScale",
            value: function logarithmicScale(yMin, yMax, base) {
                if (yMax <= 0) yMax = Math.max(yMin, base);
                if (yMin <= 0) yMin = Math.min(yMax, base);
                var logs = [];
                var logMax = Math.log(yMax) / Math.log(base);
                var logMin = Math.log(yMin) / Math.log(base);
                var logRange = logMax - logMin;
                var ticks = Math.round(logRange);
                var logTickSpacing = logRange / ticks;
                for(var i = 0, logTick = logMin; i < ticks; i++, logTick += logTickSpacing){
                    logs.push(Math.pow(base, logTick));
                }
                logs.push(Math.pow(base, logMax));
                return {
                    result: logs,
                    niceMin: yMin,
                    niceMax: yMax
                };
            }
        },
        {
            /**
   * @param {number | string} ticks
   * @param {number} index
   * @param {number} range
   */ key: "_adjustTicksForSmallRange",
            value: function _adjustTicksForSmallRange(ticks, index, range) {
                var newTicks = ticks;
                if (typeof index !== "undefined" && this.w.config.yaxis[index].labels.formatter && this.w.config.yaxis[index].tickAmount === void 0) {
                    var formattedVal = Number(this.w.config.yaxis[index].labels.formatter(1));
                    if (Utils$1.isNumber(formattedVal) && this.w.globals.yValueDecimal === 0) {
                        newTicks = Math.ceil(range);
                    }
                }
                return newTicks < ticks ? newTicks : ticks;
            }
        },
        {
            /**
   * @param {number} index
   * @param {number} minY
   * @param {number} maxY
   */ key: "setYScaleForIndex",
            value: function setYScaleForIndex(index, minY, maxY) {
                var gl = this.w.globals;
                var cnf = this.w.config;
                var y = gl.isBarHorizontal ? cnf.xaxis : cnf.yaxis[index];
                if (typeof gl.yAxisScale[index] === "undefined") {
                    gl.yAxisScale[index] = [];
                }
                var range = Math.abs(maxY - minY);
                if (y.logarithmic && range <= 5) {
                    gl.invalidLogScale = true;
                }
                if (y.logarithmic && range > 5) {
                    gl.allSeriesCollapsed = false;
                    gl.yAxisScale[index] = y.forceNiceScale ? this.logarithmicScaleNice(minY, maxY, y.logBase) : this.logarithmicScale(minY, maxY, y.logBase);
                } else {
                    if (maxY === -Number.MAX_VALUE || !Utils$1.isNumber(maxY) || minY === Number.MAX_VALUE || !Utils$1.isNumber(minY)) {
                        gl.yAxisScale[index] = this.niceScale(Number.MIN_VALUE, 0, index);
                    } else {
                        gl.allSeriesCollapsed = false;
                        gl.yAxisScale[index] = this.niceScale(minY, maxY, index);
                    }
                }
            }
        },
        {
            /**
   * @param {number} minX
   * @param {number} maxX
   */ key: "setXScale",
            value: function setXScale(minX, maxX) {
                var w = this.w;
                var gl = w.globals;
                if (maxX === -Number.MAX_VALUE || !Utils$1.isNumber(maxX)) {
                    gl.xAxisScale = this.linearScale(0, 10, 10);
                } else {
                    var ticks = gl.xTickAmount;
                    gl.xAxisScale = this.linearScale(minX, maxX, ticks, 0, w.config.xaxis.max === void 0 ? w.config.xaxis.stepSize : void 0);
                }
                return gl.xAxisScale;
            }
        },
        {
            key: "scaleMultipleYAxes",
            value: function scaleMultipleYAxes() {
                var _this = this;
                var cnf = this.w.config;
                var gl = this.w.globals;
                this.coreUtils.setSeriesYAxisMappings();
                var axisSeriesMap = gl.seriesYAxisMap;
                var minYArr = gl.minYArr;
                var maxYArr = gl.maxYArr;
                var alignZeroParticipants = [];
                var canAlignZero = !gl.isBarHorizontal;
                gl.allSeriesCollapsed = true;
                gl.barGroups = [];
                axisSeriesMap.forEach(function(axisSeries, ai) {
                    var groupNames = [];
                    axisSeries.forEach(function(as) {
                        var _a;
                        var group = /** @type {Record<string,any>} */ (_a = cnf.series[as]) == null ? void 0 : _a.group;
                        if (groupNames.indexOf(group) < 0) {
                            groupNames.push(group);
                        }
                    });
                    if (axisSeries.length > 0) {
                        var minY = Number.MAX_VALUE;
                        var maxY = -Number.MAX_VALUE;
                        var lowestY = minY;
                        var highestY = maxY;
                        var seriesType;
                        var seriesGroupName;
                        if (cnf.chart.stacked) {
                            var _loop = function(i) {
                                if (!seriesType && /** @type {Record<string,any>} */ cnf.series[axisSeries[i]].type) {
                                    seriesType = /** @type {Record<string,any>} */ cnf.series[axisSeries[i]].type;
                                }
                                var si = axisSeries[i];
                                if (/** @type {Record<string,any>} */ cnf.series[si].group) {
                                    seriesGroupName = /** @type {Record<string,any>} */ cnf.series[si].group;
                                } else {
                                    seriesGroupName = "axis-".concat(ai.toString());
                                }
                                var collapsed = !(gl.collapsedSeriesIndices.indexOf(si) < 0 && gl.ancillaryCollapsedSeriesIndices.indexOf(si) < 0);
                                if (!collapsed) {
                                    gl.allSeriesCollapsed = false;
                                    groupNames.forEach(function(gn, gni) {
                                        if (/** @type {Record<string,any>} */ cnf.series[si].group === gn) {
                                            for(var j = 0; j < _this.w.seriesData.series[si].length; j++){
                                                var val = _this.w.seriesData.series[si][j];
                                                if (val >= 0) {
                                                    posSeries[gni][j] += val;
                                                } else {
                                                    negSeries[gni][j] += val;
                                                }
                                                sumSeries[gni][j] += val;
                                                lowestY = Math.min(lowestY, val);
                                                highestY = Math.max(highestY, val);
                                            }
                                        }
                                    });
                                }
                                if (seriesType === "bar" || seriesType === "column") {
                                    gl.barGroups.push(seriesGroupName);
                                }
                            };
                            var mapSeries = new Array(gl.dataPoints).fill(0);
                            var sumSeries = [];
                            var posSeries = [];
                            var negSeries = [];
                            groupNames.forEach(function() {
                                sumSeries.push(mapSeries.map(function() {
                                    return Number.MIN_VALUE;
                                }));
                                posSeries.push(mapSeries.map(function() {
                                    return Number.MIN_VALUE;
                                }));
                                negSeries.push(mapSeries.map(function() {
                                    return Number.MIN_VALUE;
                                }));
                            });
                            for(var i = 0; i < axisSeries.length; i++)_loop(i);
                            if (!seriesType) {
                                seriesType = cnf.chart.type;
                            }
                            if (seriesType === "bar" || seriesType === "column") {
                                groupNames.forEach(function(gn, gni) {
                                    minY = Math.min(minY, Math.min.apply(null, negSeries[gni]));
                                    maxY = Math.max(maxY, Math.max.apply(null, posSeries[gni]));
                                });
                            } else {
                                groupNames.forEach(function(gn, gni) {
                                    lowestY = Math.min(lowestY, Math.min.apply(null, sumSeries[gni]));
                                    highestY = Math.max(highestY, Math.max.apply(null, sumSeries[gni]));
                                });
                                minY = lowestY;
                                maxY = highestY;
                            }
                            if (minY === Number.MIN_VALUE && maxY === Number.MIN_VALUE) {
                                maxY = -Number.MAX_VALUE;
                            }
                        } else {
                            for(var i1 = 0; i1 < axisSeries.length; i1++){
                                var si = axisSeries[i1];
                                minY = Math.min(minY, minYArr[si]);
                                maxY = Math.max(maxY, maxYArr[si]);
                                var collapsed = !(gl.collapsedSeriesIndices.indexOf(si) < 0 && gl.ancillaryCollapsedSeriesIndices.indexOf(si) < 0);
                                if (!collapsed) {
                                    gl.allSeriesCollapsed = false;
                                }
                            }
                        }
                        if (cnf.yaxis[ai].min !== void 0) {
                            if (typeof cnf.yaxis[ai].min === "function") {
                                minY = cnf.yaxis[ai].min(minY);
                            } else {
                                minY = cnf.yaxis[ai].min;
                            }
                        }
                        if (cnf.yaxis[ai].max !== void 0) {
                            if (typeof cnf.yaxis[ai].max === "function") {
                                maxY = cnf.yaxis[ai].max(maxY);
                            } else {
                                maxY = cnf.yaxis[ai].max;
                            }
                        }
                        gl.barGroups = gl.barGroups.filter(function(v, i, a) {
                            return a.indexOf(v) === i;
                        });
                        var yaxe = cnf.yaxis[ai];
                        var participates = canAlignZero && yaxe.alignZero === true && !yaxe.logarithmic && yaxe.min === void 0 && yaxe.max === void 0 && gl.ignoreYAxisIndexes.indexOf(ai) < 0 && Utils$1.isNumber(minY) && Utils$1.isNumber(maxY);
                        if (participates) {
                            alignZeroParticipants.push({
                                ai: ai,
                                minY: minY,
                                maxY: maxY
                            });
                        } else {
                            _this.setYScaleForIndex(ai, minY, maxY);
                            axisSeries.forEach(function(si) {
                                minYArr[si] = gl.yAxisScale[ai].niceMin;
                                maxYArr[si] = gl.yAxisScale[ai].niceMax;
                            });
                        }
                    } else {
                        _this.setYScaleForIndex(ai, 0, -Number.MAX_VALUE);
                    }
                });
                if (alignZeroParticipants.length >= 2) {
                    alignZeroParticipants.forEach(function(p) {
                        _this.setYScaleForIndex(p.ai, p.minY, p.maxY);
                        axisSeriesMap[p.ai].forEach(function(si) {
                            minYArr[si] = gl.yAxisScale[p.ai].niceMin;
                            maxYArr[si] = gl.yAxisScale[p.ai].niceMax;
                        });
                    });
                    var targetRatio = 0;
                    alignZeroParticipants.forEach(function(p) {
                        var scale = gl.yAxisScale[p.ai];
                        var range = scale.niceMax - scale.niceMin;
                        if (range > 0) {
                            var r = -scale.niceMin / range;
                            if (r > targetRatio) targetRatio = r;
                        }
                    });
                    if (targetRatio > 1) targetRatio = 1;
                    if (targetRatio < 0) targetRatio = 0;
                    var niceRoundUp = function niceRoundUp(raw) {
                        if (raw <= 0) return 1;
                        var mag = Math.floor(Math.log10(raw));
                        var magPow = Math.pow(10, mag);
                        var msd = raw / magPow;
                        var mult;
                        if (msd <= 1 + 1e-9) mult = 1;
                        else if (msd <= 2 + 1e-9) mult = 2;
                        else if (msd <= 2.5 + 1e-9) mult = 2.5;
                        else if (msd <= 5 + 1e-9) mult = 5;
                        else mult = 10;
                        return mult * magPow;
                    };
                    alignZeroParticipants.forEach(function(p) {
                        var scale = gl.yAxisScale[p.ai];
                        if (!scale.result || scale.result.length < 2) return;
                        var range = scale.niceMax - scale.niceMin;
                        if (range <= 0) return;
                        var r = -scale.niceMin / range;
                        if (Math.abs(r - targetRatio) <= 1e-9) return;
                        var extendMin = r < targetRatio && targetRatio < 1 - 1e-9;
                        var extendMaxOnly = !extendMin && r > targetRatio && targetRatio > 1e-9;
                        if (!extendMin && !extendMaxOnly) return;
                        var targetNiceMin = extendMin ? -targetRatio * scale.niceMax / (1 - targetRatio) : scale.niceMin;
                        var targetNiceMax = extendMaxOnly ? -scale.niceMin * (1 - targetRatio) / targetRatio : scale.niceMax;
                        var newRange = targetNiceMax - targetNiceMin;
                        if (newRange <= 0) return;
                        var desiredTicks = Math.max(scale.result.length, 5);
                        var newStep = niceRoundUp(newRange / Math.max(desiredTicks - 1, 1));
                        if (newStep <= 0) return;
                        var newNiceMin;
                        var newNiceMax;
                        if (extendMin) {
                            newNiceMin = Math.floor(targetNiceMin / newStep + 1e-9) * newStep;
                            var requiredMax = targetRatio > 1e-9 ? newNiceMin * (targetRatio - 1) / targetRatio : scale.niceMax;
                            var maxNeeded = Math.max(requiredMax, scale.niceMax);
                            newNiceMax = Math.ceil(maxNeeded / newStep - 1e-9) * newStep;
                        } else {
                            newNiceMax = Math.ceil(targetNiceMax / newStep - 1e-9) * newStep;
                            var requiredMin = targetRatio < 1 - 1e-9 ? -targetRatio * newNiceMax / (1 - targetRatio) : scale.niceMin;
                            var minNeeded = Math.min(requiredMin, scale.niceMin);
                            newNiceMin = Math.floor(minNeeded / newStep + 1e-9) * newStep;
                        }
                        scale.result = [];
                        for(var v = newNiceMin; v <= newNiceMax + newStep * 1e-9; v = Utils$1.preciseAddition(v, newStep)){
                            scale.result.push(Utils$1.stripNumber(v, 7));
                        }
                        scale.niceMin = newNiceMin;
                        scale.niceMax = newNiceMax;
                        axisSeriesMap[p.ai].forEach(function(si) {
                            minYArr[si] = scale.niceMin;
                            maxYArr[si] = scale.niceMax;
                        });
                    });
                } else if (alignZeroParticipants.length === 1) {
                    var p = alignZeroParticipants[0];
                    this.setYScaleForIndex(p.ai, p.minY, p.maxY);
                    axisSeriesMap[p.ai].forEach(function(si) {
                        minYArr[si] = gl.yAxisScale[p.ai].niceMin;
                        maxYArr[si] = gl.yAxisScale[p.ai].niceMax;
                    });
                }
            }
        }
    ]);
    return Scales;
}();
var Range = /*#__PURE__*/ function() {
    "use strict";
    function Range(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Range);
        this.w = w;
        this.scales = new Scales(this.w);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Range, [
        {
            key: "init",
            value: function init() {
                this.setYRange();
                this.setXRange();
                this.setZRange();
            }
        },
        {
            /**
   * One rendered x-pixel expressed in data units, used to widen the
   * autoScaleYaxis window so a boundary point that is still drawn inside the
   * plot survives a sub-pixel rounding error in `xaxis.min`/`xaxis.max`.
   *
   * The window is taken from the configured bounds (that is what maps onto the
   * grid) and falls back to the series' own x-extent for whichever bound is
   * unset. Returns 0 when there is nothing to scale against: `gridWidth` is
   * still 0 before the first `plotCoords()` run, and a first render has no
   * pixel round-trip to compensate for anyway.
   *
   * @param {number[] | undefined} seriesX
   * @returns {number}
   */ key: "_xPixelTolerance",
            value: function _xPixelTolerance(seriesX) {
                var gridWidth = this.w.layout.gridWidth;
                if (!gridWidth || !seriesX || !seriesX.length) return 0;
                var cnfX = this.w.config.xaxis;
                var lo = typeof cnfX.min === "number" ? cnfX.min : seriesX[0];
                var hi = typeof cnfX.max === "number" ? cnfX.max : seriesX[seriesX.length - 1];
                if (!(hi > lo)) return 0;
                return (hi - lo) / gridWidth;
            }
        },
        {
            /**
   * @param {number} startingSeriesIndex
   * @param {number | null} [endingSeriesIndex]
   */ key: "getMinYMaxY",
            value: function getMinYMaxY(startingSeriesIndex) {
                var lowestY = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Number.MAX_VALUE, highestY = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : -Number.MAX_VALUE, endingSeriesIndex = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : null;
                var _a, _b, _c, _d, _e, _f, _g, _h, _i;
                var cnf = this.w.config;
                var gl = this.w.globals;
                var maxY = -Number.MAX_VALUE;
                var minY = Number.MIN_VALUE;
                if (endingSeriesIndex === null) {
                    endingSeriesIndex = startingSeriesIndex + 1;
                }
                var series = this.w.seriesData.series;
                var seriesMin = series;
                var seriesMax = series;
                if (cnf.chart.type === "candlestick") {
                    seriesMin = this.w.candleData.seriesCandleL;
                    seriesMax = this.w.candleData.seriesCandleH;
                } else if (cnf.chart.type === "boxPlot") {
                    seriesMin = this.w.candleData.seriesCandleO;
                    seriesMax = this.w.candleData.seriesCandleC;
                } else if (cnf.chart.type === "violin") {
                    seriesMin = this.w.violinData.seriesViolinMin;
                    seriesMax = this.w.violinData.seriesViolinMax;
                } else if (this.w.axisFlags.isRangeData) {
                    seriesMin = this.w.rangeData.seriesRangeStart;
                    seriesMax = this.w.rangeData.seriesRangeEnd;
                }
                var autoScaleYaxis = false;
                if (this.w.seriesData.seriesX.length >= endingSeriesIndex) {
                    var brush = /** @type {any} */ (_a = gl.brushSource) == null ? void 0 : _a.w.config.chart.brush;
                    if (cnf.chart.zoom.enabled && cnf.chart.zoom.autoScaleYaxis || (brush == null ? void 0 : brush.enabled) && (brush == null ? void 0 : brush.autoScaleYaxis)) {
                        autoScaleYaxis = true;
                    }
                }
                for(var i = startingSeriesIndex; i < endingSeriesIndex; i++){
                    gl.dataPoints = Math.max(gl.dataPoints, series[i].length);
                    var seriesType = /** @type {Record<string,any>} */ cnf.series[i].type;
                    if (this.w.labelData.categoryLabels.length) {
                        gl.dataPoints = this.w.labelData.categoryLabels.filter(function(label) {
                            return typeof label !== "undefined";
                        }).length;
                    }
                    if (this.w.labelData.labels.length && cnf.xaxis.type !== "datetime" && /**
       * @param {number} a
       * @param {number[]} c
       */ this.w.seriesData.series.reduce(function(a, c) {
                        return a + c.length;
                    }, 0) !== 0) {
                        gl.dataPoints = Math.max(gl.dataPoints, this.w.labelData.labels.length);
                    }
                    var firstXIndex = 0;
                    var lastXIndex = series[i].length - 1;
                    if (autoScaleYaxis) {
                        var xTolerance = this._xPixelTolerance(this.w.seriesData.seriesX[i]);
                        if (cnf.xaxis.min) {
                            var lowerBound = cnf.xaxis.min - xTolerance;
                            for(; firstXIndex < lastXIndex && this.w.seriesData.seriesX[i][firstXIndex] < lowerBound; firstXIndex++){}
                        }
                        if (cnf.xaxis.max) {
                            var upperBound = cnf.xaxis.max + xTolerance;
                            for(; lastXIndex > firstXIndex && this.w.seriesData.seriesX[i][lastXIndex] > upperBound; lastXIndex--){}
                        }
                    }
                    var plainNumeric = seriesMin === series && seriesMax === series && cnf.chart.type !== "boxPlot" && seriesType !== "candlestick" && seriesType !== "boxPlot" && seriesType !== "violin" && seriesType !== "rangeArea" && seriesType !== "rangeBar" && !(this.w.seriesData.seriesGoals[i] && this.w.seriesData.seriesGoals[i].length);
                    if (plainNumeric) {
                        var arr = series[i];
                        var jEnd = Math.min(lastXIndex, arr.length - 1);
                        var pe = (_b = this.w.seriesData._parsedExtrema) == null ? void 0 : _b[i];
                        if (pe && pe.ref === arr && pe.len === arr.length && firstXIndex === 0 && jEnd === arr.length - 1) {
                            if (pe.maxY > maxY) maxY = pe.maxY;
                            if (pe.lowestY < lowestY) lowestY = pe.lowestY;
                            if (pe.negMinY < 0 && pe.negMinY < minY) minY = pe.negMinY;
                            if (pe.yDec > gl.yValueDecimal) gl.yValueDecimal = pe.yDec;
                            if (pe.hasNulls) gl.hasNullValues = true;
                        } else {
                            var yDec = gl.yValueDecimal;
                            var hasNulls = false;
                            for(var j = firstXIndex; j <= jEnd; j++){
                                var val = arr[j];
                                if (val !== null && typeof val === "number" && val === val && val !== Infinity && val !== -Infinity) {
                                    if (val > maxY) maxY = val;
                                    if (val < lowestY) lowestY = val;
                                    if (minY > val && val < 0) minY = val;
                                    if (!Number.isInteger(val)) {
                                        var av = val < 0 ? -val : val;
                                        if (av >= 1e-6 && av < 1e21) {
                                            var str = "" + val;
                                            var dot = str.indexOf(".");
                                            var dec = dot === -1 ? 0 : str.length - dot - 1;
                                            if (dec > yDec) yDec = dec;
                                        } else {
                                            var nv = Utils$1.noExponents(val);
                                            if (Utils$1.isFloat(nv)) {
                                                yDec = Math.max(yDec, nv.toString().split(".")[1].length);
                                            }
                                        }
                                    }
                                } else {
                                    hasNulls = true;
                                }
                            }
                            gl.yValueDecimal = yDec;
                            if (hasNulls) gl.hasNullValues = true;
                        }
                        highestY = maxY;
                        if (seriesType === "bar" || seriesType === "column") {
                            if (minY < 0 && maxY < 0) {
                                maxY = 0;
                                highestY = Math.max(highestY, 0);
                            }
                            if (minY === Number.MIN_VALUE) {
                                minY = 0;
                                lowestY = Math.min(lowestY, 0);
                            }
                        }
                        continue;
                    }
                    for(var j1 = firstXIndex; j1 <= lastXIndex && j1 < this.w.seriesData.series[i].length; j1++){
                        var val1 = series[i][j1];
                        if (val1 !== null && Utils$1.isNumber(val1)) {
                            if (typeof ((_c = seriesMax[i]) == null ? void 0 : _c[j1]) !== "undefined") {
                                maxY = Math.max(maxY, seriesMax[i][j1]);
                                lowestY = Math.min(lowestY, seriesMax[i][j1]);
                            }
                            if (typeof ((_d = seriesMin[i]) == null ? void 0 : _d[j1]) !== "undefined") {
                                lowestY = Math.min(lowestY, seriesMin[i][j1]);
                                highestY = Math.max(highestY, seriesMin[i][j1]);
                            }
                            switch(seriesType){
                                case "candlestick":
                                    {
                                        if (typeof this.w.candleData.seriesCandleC[i][j1] !== "undefined") {
                                            maxY = Math.max(maxY, this.w.candleData.seriesCandleH[i][j1]);
                                            lowestY = Math.min(lowestY, this.w.candleData.seriesCandleL[i][j1]);
                                        }
                                    }
                                    break;
                                case "boxPlot":
                                    {
                                        if (typeof this.w.candleData.seriesCandleC[i][j1] !== "undefined") {
                                            maxY = Math.max(maxY, this.w.candleData.seriesCandleC[i][j1]);
                                            lowestY = Math.min(lowestY, this.w.candleData.seriesCandleO[i][j1]);
                                        }
                                    }
                                    break;
                                case "violin":
                                    {
                                        if (typeof ((_e = this.w.violinData.seriesViolinMax[i]) == null ? void 0 : _e[j1]) !== "undefined") {
                                            maxY = Math.max(maxY, this.w.violinData.seriesViolinMax[i][j1]);
                                            lowestY = Math.min(lowestY, this.w.violinData.seriesViolinMin[i][j1]);
                                        }
                                    }
                                    break;
                            }
                            if (seriesType && seriesType !== "candlestick" && seriesType !== "boxPlot" && seriesType !== "violin" && seriesType !== "rangeArea" && seriesType !== "rangeBar") {
                                maxY = Math.max(maxY, this.w.seriesData.series[i][j1]);
                                lowestY = Math.min(lowestY, this.w.seriesData.series[i][j1]);
                            }
                            if (this.w.seriesData.seriesGoals[i] && this.w.seriesData.seriesGoals[i][j1] && Array.isArray(this.w.seriesData.seriesGoals[i][j1])) {
                                this.w.seriesData.seriesGoals[i][j1].forEach(function(g) {
                                    maxY = Math.max(maxY, g.value);
                                    lowestY = Math.min(lowestY, g.value);
                                });
                            }
                            if (this.w.config.chart.type === "boxPlot" || seriesType === "boxPlot") {
                                var boxPts = (_g = (_f = this.w.candleData.seriesBoxPoints) == null ? void 0 : _f[i]) == null ? void 0 : _g[j1];
                                if (boxPts) {
                                    for(var p = 0; p < boxPts.length; p++){
                                        var pv = boxPts[p];
                                        if (typeof pv === "number") {
                                            maxY = Math.max(maxY, pv);
                                            lowestY = Math.min(lowestY, pv);
                                        }
                                    }
                                }
                            }
                            highestY = maxY;
                            val1 = Utils$1.noExponents(val1);
                            if (Utils$1.isFloat(val1)) {
                                gl.yValueDecimal = Math.max(gl.yValueDecimal, val1.toString().split(".")[1].length);
                            }
                            if (minY > ((_h = seriesMin[i]) == null ? void 0 : _h[j1]) && ((_i = seriesMin[i]) == null ? void 0 : _i[j1]) < 0) {
                                minY = seriesMin[i][j1];
                            }
                        } else {
                            gl.hasNullValues = true;
                        }
                    }
                    if (seriesType === "bar" || seriesType === "column") {
                        if (minY < 0 && maxY < 0) {
                            maxY = 0;
                            highestY = Math.max(highestY, 0);
                        }
                        if (minY === Number.MIN_VALUE) {
                            minY = 0;
                            lowestY = Math.min(lowestY, 0);
                        }
                    }
                }
                if (cnf.chart.type === "rangeBar" && this.w.rangeData.seriesRangeStart.length && gl.isBarHorizontal) {
                    minY = lowestY;
                }
                if (cnf.chart.type === "bar") {
                    if (minY < 0 && maxY < 0) {
                        maxY = 0;
                    }
                    if (minY === Number.MIN_VALUE) {
                        minY = 0;
                    }
                }
                return {
                    minY: minY,
                    maxY: maxY,
                    lowestY: lowestY,
                    highestY: highestY
                };
            }
        },
        {
            key: "setYRange",
            value: function setYRange() {
                var gl = this.w.globals;
                var cnf = this.w.config;
                gl.maxY = -Number.MAX_VALUE;
                gl.minY = Number.MIN_VALUE;
                var lowestYInAllSeries = Number.MAX_VALUE;
                var minYMaxY;
                if (gl.isMultipleYAxis) {
                    lowestYInAllSeries = Number.MAX_VALUE;
                    for(var i = 0; i < this.w.seriesData.series.length; i++){
                        minYMaxY = this.getMinYMaxY(i);
                        gl.minYArr[i] = minYMaxY.lowestY;
                        gl.maxYArr[i] = minYMaxY.highestY;
                        lowestYInAllSeries = Math.min(lowestYInAllSeries, minYMaxY.lowestY);
                    }
                }
                minYMaxY = this.getMinYMaxY(0, lowestYInAllSeries, void 0, this.w.seriesData.series.length);
                if (cnf.chart.type === "bar") {
                    gl.minY = minYMaxY.minY;
                    gl.maxY = minYMaxY.maxY;
                } else {
                    gl.minY = minYMaxY.lowestY;
                    gl.maxY = minYMaxY.highestY;
                }
                lowestYInAllSeries = minYMaxY.lowestY;
                if (cnf.chart.stacked) {
                    this._setStackedMinMax();
                }
                if (cnf.chart.type === "line" || cnf.chart.type === "area" || cnf.chart.type === "scatter" || cnf.chart.type === "candlestick" || cnf.chart.type === "boxPlot" || cnf.chart.type === "violin" || cnf.chart.type === "rangeBar" && !gl.isBarHorizontal) {
                    if (gl.minY === Number.MIN_VALUE && lowestYInAllSeries !== -Number.MAX_VALUE && lowestYInAllSeries !== gl.maxY) {
                        gl.minY = lowestYInAllSeries;
                    }
                } else {
                    gl.minY = gl.minY !== Number.MIN_VALUE ? Math.min(minYMaxY.minY, gl.minY) : minYMaxY.minY;
                }
                cnf.yaxis.forEach(function(yaxe, index) {
                    if (yaxe.max !== void 0) {
                        if (typeof yaxe.max === "number") {
                            gl.maxYArr[index] = yaxe.max;
                        } else if (typeof yaxe.max === "function") {
                            gl.maxYArr[index] = yaxe.max(gl.isMultipleYAxis ? gl.maxYArr[index] : gl.maxY);
                        }
                        gl.maxY = gl.maxYArr[index];
                    }
                    if (yaxe.min !== void 0) {
                        if (typeof yaxe.min === "number") {
                            gl.minYArr[index] = yaxe.min;
                        } else if (typeof yaxe.min === "function") {
                            gl.minYArr[index] = yaxe.min(gl.isMultipleYAxis ? gl.minYArr[index] === Number.MIN_VALUE ? 0 : gl.minYArr[index] : gl.minY);
                        }
                        gl.minY = gl.minYArr[index];
                    }
                });
                if (gl.isBarHorizontal) {
                    var minmax = [
                        "min",
                        "max"
                    ];
                    minmax.forEach(function(m) {
                        if (cnf.xaxis[m] !== void 0 && typeof cnf.xaxis[m] === "number") {
                            m === "min" ? gl.minY = cnf.xaxis[m] : gl.maxY = cnf.xaxis[m];
                        }
                    });
                }
                if (gl.isMultipleYAxis) {
                    this.scales.scaleMultipleYAxes();
                    gl.minY = lowestYInAllSeries;
                } else {
                    this.scales.setYScaleForIndex(0, gl.minY, gl.maxY);
                    gl.minY = gl.yAxisScale[0].niceMin;
                    gl.maxY = gl.yAxisScale[0].niceMax;
                    gl.minYArr[0] = gl.minY;
                    gl.maxYArr[0] = gl.maxY;
                }
                gl.barGroups = [];
                gl.lineGroups = [];
                gl.areaGroups = [];
                cnf.series.forEach(function(s) {
                    var _s = /** @type {any} */ s;
                    var type = _s.type || cnf.chart.type;
                    switch(type){
                        case "bar":
                        case "column":
                            gl.barGroups.push(_s.group);
                            break;
                        case "line":
                            gl.lineGroups.push(_s.group);
                            break;
                        case "area":
                            gl.areaGroups.push(_s.group);
                            break;
                    }
                });
                gl.barGroups = gl.barGroups.filter(function(v, i, a) {
                    return a.indexOf(v) === i;
                });
                gl.lineGroups = gl.lineGroups.filter(function(v, i, a) {
                    return a.indexOf(v) === i;
                });
                gl.areaGroups = gl.areaGroups.filter(function(v, i, a) {
                    return a.indexOf(v) === i;
                });
                return {
                    minY: gl.minY,
                    maxY: gl.maxY,
                    minYArr: gl.minYArr,
                    maxYArr: gl.maxYArr,
                    yAxisScale: gl.yAxisScale
                };
            }
        },
        {
            key: "setXRange",
            value: function setXRange() {
                var _this = this;
                var gl = this.w.globals;
                var cnf = this.w.config;
                var isXNumeric = cnf.xaxis.type === "numeric" || cnf.xaxis.type === "datetime" || cnf.xaxis.type === "category" && !this.w.axisFlags.noLabelsProvided || this.w.axisFlags.noLabelsProvided || this.w.axisFlags.isXNumeric;
                var getInitialMinXMaxX = function getInitialMinXMaxX() {
                    var _a;
                    var minX = gl.minX;
                    var maxX = gl.maxX;
                    for(var i = 0; i < _this.w.seriesData.series.length; i++){
                        var lbls = /** @type {any} */ _this.w.labelData.labels[i];
                        if (!lbls) continue;
                        var pe = (_a = _this.w.seriesData._parsedExtrema) == null ? void 0 : _a[i];
                        if (pe && pe.xNumeric && pe.xref === lbls && pe.len === lbls.length) {
                            if (pe.maxX > maxX) maxX = pe.maxX;
                            if (pe.minX < minX) minX = pe.minX;
                            continue;
                        }
                        for(var j = 0; j < lbls.length; j++){
                            var v = lbls[j];
                            if (v !== null && typeof v === "number" && v === v) {
                                if (v > maxX) maxX = v;
                                if (v < minX) minX = v;
                            }
                        }
                    }
                    gl.maxX = maxX;
                    gl.initialMaxX = maxX;
                    gl.minX = minX;
                    gl.initialMinX = minX;
                };
                if (this.w.axisFlags.isXNumeric) {
                    getInitialMinXMaxX();
                }
                if (this.w.axisFlags.noLabelsProvided) {
                    if (cnf.xaxis.categories.length === 0) {
                        gl.maxX = /** @type {any} */ this.w.labelData.labels[this.w.labelData.labels.length - 1];
                        gl.initialMaxX = /** @type {any} */ this.w.labelData.labels[this.w.labelData.labels.length - 1];
                        gl.minX = 1;
                        gl.initialMinX = 1;
                    }
                }
                if (this.w.axisFlags.isXNumeric || this.w.axisFlags.noLabelsProvided || this.w.axisFlags.dataFormatXNumeric) {
                    var ticks = 10;
                    if (cnf.xaxis.tickAmount === void 0) {
                        ticks = Math.round(gl.svgWidth / 150);
                        if (cnf.xaxis.type === "numeric" && gl.dataPoints < 30) {
                            ticks = gl.dataPoints - 1;
                        }
                        if (ticks > gl.dataPoints && gl.dataPoints !== 0) {
                            ticks = gl.dataPoints - 1;
                        }
                    } else if (cnf.xaxis.tickAmount === "dataPoints") {
                        if (this.w.seriesData.series.length > 1) {
                            ticks = this.w.seriesData.series[gl.maxValsInArrayIndex].length - 1;
                        }
                        if (this.w.axisFlags.isXNumeric) {
                            var diff = Math.round(gl.maxX - gl.minX);
                            if (diff < 30) {
                                ticks = diff;
                            }
                        }
                    } else {
                        ticks = cnf.xaxis.tickAmount;
                    }
                    gl.xTickAmount = ticks;
                    if (cnf.xaxis.max !== void 0 && typeof cnf.xaxis.max === "number") {
                        gl.maxX = cnf.xaxis.max;
                    }
                    if (cnf.xaxis.min !== void 0 && typeof cnf.xaxis.min === "number") {
                        gl.minX = cnf.xaxis.min;
                    }
                    if (cnf.xaxis.range !== void 0) {
                        gl.minX = gl.maxX - cnf.xaxis.range;
                    }
                    if (gl.minX !== Number.MAX_VALUE && gl.maxX !== -Number.MAX_VALUE) {
                        if (cnf.xaxis.convertedCatToNumeric && !this.w.axisFlags.dataFormatXNumeric) {
                            var catScale = [];
                            for(var i = gl.minX - 1; i < gl.maxX; i++){
                                catScale.push(i + 1);
                            }
                            gl.xAxisScale = {
                                result: catScale,
                                niceMin: catScale[0],
                                niceMax: catScale[catScale.length - 1]
                            };
                        } else {
                            gl.xAxisScale = this.scales.setXScale(gl.minX, gl.maxX);
                        }
                    } else {
                        gl.xAxisScale = this.scales.linearScale(0, ticks, ticks, 0, cnf.xaxis.stepSize);
                        if (this.w.axisFlags.noLabelsProvided && this.w.labelData.labels.length > 0) {
                            gl.xAxisScale = this.scales.linearScale(1, this.w.labelData.labels.length, ticks - 1, 0, cnf.xaxis.stepSize);
                            this.w.seriesData.seriesX = /** @type {any} */ this.w.labelData.labels.slice();
                        }
                    }
                    if (isXNumeric) {
                        this.w.labelData.labels = /** @type {any} */ gl.xAxisScale.result.slice();
                    }
                }
                if (gl.isBarHorizontal && this.w.labelData.labels.length) {
                    gl.xTickAmount = this.w.labelData.labels.length;
                }
                this._handleSingleDataPoint();
                this._getMinXDiff();
                return {
                    minX: gl.minX,
                    maxX: gl.maxX
                };
            }
        },
        {
            key: "setZRange",
            value: function setZRange() {
                var gl = this.w.globals;
                if (!this.w.axisFlags.isDataXYZ) return;
                for(var i = 0; i < this.w.seriesData.series.length; i++){
                    if (typeof this.w.seriesData.seriesZ[i] !== "undefined") {
                        for(var j = 0; j < this.w.seriesData.seriesZ[i].length; j++){
                            if (this.w.seriesData.seriesZ[i][j] !== null && Utils$1.isNumber(this.w.seriesData.seriesZ[i][j])) {
                                gl.maxZ = Math.max(gl.maxZ, this.w.seriesData.seriesZ[i][j]);
                                gl.minZ = Math.min(gl.minZ, this.w.seriesData.seriesZ[i][j]);
                            }
                        }
                    }
                }
            }
        },
        {
            key: "_handleSingleDataPoint",
            value: function _handleSingleDataPoint() {
                var gl = this.w.globals;
                var cnf = this.w.config;
                if (gl.minX === gl.maxX) {
                    var datetimeObj = new DateTime(this.w);
                    if (cnf.xaxis.type === "datetime") {
                        var newMinX = datetimeObj.getDate(gl.minX);
                        if (cnf.xaxis.labels.datetimeUTC) {
                            newMinX.setUTCDate(newMinX.getUTCDate() - 2);
                        } else {
                            newMinX.setDate(newMinX.getDate() - 2);
                        }
                        gl.minX = new Date(newMinX).getTime();
                        var newMaxX = datetimeObj.getDate(gl.maxX);
                        if (cnf.xaxis.labels.datetimeUTC) {
                            newMaxX.setUTCDate(newMaxX.getUTCDate() + 2);
                        } else {
                            newMaxX.setDate(newMaxX.getDate() + 2);
                        }
                        gl.maxX = new Date(newMaxX).getTime();
                    } else if (cnf.xaxis.type === "numeric" || cnf.xaxis.type === "category" && !this.w.axisFlags.noLabelsProvided) {
                        gl.minX = gl.minX - 2;
                        gl.initialMinX = gl.minX;
                        gl.maxX = gl.maxX + 2;
                        gl.initialMaxX = gl.maxX;
                    }
                }
            }
        },
        {
            key: "_getMinXDiff",
            value: function _getMinXDiff() {
                var _this = this;
                var gl = this.w.globals;
                if (this.w.axisFlags.isXNumeric) {
                    this.w.seriesData.seriesX.forEach(function(sX, si) {
                        var _a;
                        if (sX.length) {
                            if (sX.length === 1) {
                                sX.push(_this.w.seriesData.seriesX[gl.maxValsInArrayIndex][_this.w.seriesData.seriesX[gl.maxValsInArrayIndex].length - 1]);
                            }
                            var pe = (_a = _this.w.seriesData._parsedExtrema) == null ? void 0 : _a[si];
                            if (pe && pe.xNumeric && pe.xSorted && pe.xref === sX && pe.len === sX.length) {
                                if (pe.minXDiff < gl.minXDiff) gl.minXDiff = pe.minXDiff;
                                if (gl.dataPoints === 1 || gl.minXDiff === Number.MAX_VALUE) {
                                    gl.minXDiff = 0.5;
                                }
                                return;
                            }
                            var presorted = true;
                            var minDiff = gl.minXDiff;
                            for(var j = 1; j < sX.length; j++){
                                var d = sX[j] - sX[j - 1];
                                if (d > 0) {
                                    if (d < minDiff) minDiff = d;
                                } else if (d < 0) {
                                    presorted = false;
                                    break;
                                }
                            }
                            if (presorted) {
                                gl.minXDiff = minDiff;
                                if (gl.dataPoints === 1 || gl.minXDiff === Number.MAX_VALUE) {
                                    gl.minXDiff = 0.5;
                                }
                                return;
                            }
                            var seriesX = sX.slice();
                            seriesX.sort(function(a, b) {
                                return a - b;
                            });
                            seriesX.forEach(function(s, j) {
                                if (j > 0) {
                                    var xDiff = s - seriesX[j - 1];
                                    if (xDiff > 0) {
                                        gl.minXDiff = Math.min(xDiff, gl.minXDiff);
                                    }
                                }
                            });
                            if (gl.dataPoints === 1 || gl.minXDiff === Number.MAX_VALUE) {
                                gl.minXDiff = 0.5;
                            }
                        }
                    });
                }
            }
        },
        {
            key: "_setStackedMinMax",
            value: function _setStackedMinMax() {
                var _this = this;
                var gl = this.w.globals;
                if (!this.w.seriesData.series.length) return;
                var seriesGroups = this.w.labelData.seriesGroups;
                if (!seriesGroups.length) {
                    seriesGroups = [
                        this.w.seriesData.seriesNames.map(function(name2) {
                            return name2;
                        })
                    ];
                }
                var stackedPoss = {};
                var stackedNegs = {};
                seriesGroups.forEach(function(group) {
                    stackedPoss[group] = [];
                    stackedNegs[group] = [];
                    var indicesOfSeriesInGroup = _this.w.config.series.map(function(serie, si) {
                        return group.indexOf(_this.w.seriesData.seriesNames[si]) > -1 ? si : null;
                    }).filter(function(f) {
                        return f !== null;
                    });
                    indicesOfSeriesInGroup.forEach(function(i) {
                        var _a, _b, _c, _d;
                        for(var j = 0; j < _this.w.seriesData.series[gl.maxValsInArrayIndex].length; j++){
                            if (typeof stackedPoss[group][j] === "undefined") {
                                stackedPoss[group][j] = 0;
                                stackedNegs[group][j] = 0;
                            }
                            var stackSeries = _this.w.config.chart.stacked && !gl.comboCharts || _this.w.config.chart.stacked && gl.comboCharts && (!_this.w.config.chart.stackOnlyBar || /** @type {Record<string,any>} */ ((_b = (_a = _this.w.config.series) == null ? void 0 : _a[i]) == null ? void 0 : _b.type) === "bar" || /** @type {Record<string,any>} */ ((_d = (_c = _this.w.config.series) == null ? void 0 : _c[i]) == null ? void 0 : _d.type) === "column");
                            if (stackSeries) {
                                if (_this.w.seriesData.series[i][j] !== null && Utils$1.isNumber(_this.w.seriesData.series[i][j])) {
                                    _this.w.seriesData.series[i][j] > 0 ? stackedPoss[group][j] += parseFloat(String(_this.w.seriesData.series[i][j])) + 1e-4 : stackedNegs[group][j] += parseFloat(String(_this.w.seriesData.series[i][j]));
                                }
                            }
                        }
                    });
                });
                Object.entries(stackedPoss).forEach(function(param) {
                    var _param = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(param, 1), key = _param[0];
                    stackedPoss[key].forEach(function(_, stgi) {
                        gl.maxY = Math.max(gl.maxY, stackedPoss[key][stgi]);
                        gl.minY = Math.min(gl.minY, stackedNegs[key][stgi]);
                    });
                });
            }
        }
    ]);
    return Range;
}();
function getThemePalettes() {
    return {
        // All colours pass WCAG 1.4.11 non-text contrast (≥ 3:1) against the
        // default light (#fff) and dark (#293450) theme backgrounds.
        palette1: [
            "#008FFB",
            "#00A86F",
            "#CA8501",
            "#FF4560",
            "#846DD5"
        ],
        palette2: [
            "#6978CB",
            "#039DE2",
            "#49A84D",
            "#B39105",
            "#D68000"
        ],
        palette3: [
            "#209FCC",
            "#648291",
            "#D4526E",
            "#0FA783",
            "#A19285"
        ],
        palette4: [
            "#2FA59D",
            "#73A20B",
            "#099DE1",
            "#FD5D5D",
            "#648291"
        ],
        palette5: [
            "#2B908F",
            "#F56566",
            "#2EAB16",
            "#FA4443",
            "#1EA2BD"
        ],
        palette6: [
            "#449DD1",
            "#F86624",
            "#EA3A4A",
            "#9C63D1",
            "#899E2A"
        ],
        palette7: [
            "#DF475B",
            "#1B998B",
            "#7E75B7",
            "#F46036",
            "#B1911B"
        ],
        palette8: [
            "#9C63D1",
            "#F86624",
            "#B38F04",
            "#EA3A4A",
            "#2FA2B3"
        ],
        palette9: [
            "#98776F",
            "#A19285",
            "#A8705E",
            "#BA6560",
            "#A0927F"
        ],
        palette10: [
            "#C91EFF",
            "#A94BFD",
            "#6C6AFE",
            "#2983FF",
            "#009ED8"
        ],
        // CVD-safe palettes (Wong 2011 / IBM design)
        cvdDeuteranopia: [
            "#0072B2",
            "#E69F00",
            "#56B4E9",
            "#009E73",
            "#F0E442",
            "#D55E00",
            "#CC79A7"
        ],
        cvdProtanopia: [
            "#0077BB",
            "#EE7733",
            "#009988",
            "#EE3377",
            "#BBBBBB",
            "#33BBEE",
            "#CC3311"
        ],
        cvdTritanopia: [
            "#CC3311",
            "#009988",
            "#EE7733",
            "#0077BB",
            "#EE3377",
            "#BBBBBB",
            "#33BBEE"
        ],
        highContrast: [
            "#005A9C",
            "#C00000",
            "#007A33",
            "#6C3483",
            "#7B3F00",
            "#0097A7",
            "#4A235A"
        ]
    };
}
var YAxis = /*#__PURE__*/ function() {
    "use strict";
    function YAxis(w) {
        var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref_theme = _ref.theme, theme = _ref_theme === void 0 ? null : _ref_theme, _ref_timeScale = _ref.timeScale, timeScale = _ref_timeScale === void 0 ? null : _ref_timeScale, elgrid = arguments.length > 2 ? arguments[2] : void 0;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, YAxis);
        this.w = w;
        this.elgrid = elgrid;
        this.xaxisFontSize = w.config.xaxis.labels.style.fontSize;
        this.axisFontFamily = w.config.xaxis.labels.style.fontFamily;
        this.xaxisForeColors = w.config.xaxis.labels.style.colors;
        this.isCategoryBarHorizontal = w.config.chart.type === "bar" && w.config.plotOptions.bar.horizontal;
        this.xAxisoffX = w.config.xaxis.position === "bottom" ? w.layout.gridHeight : 0;
        this.drawnLabels = [];
        this.axesUtils = new AxesUtils(w, {
            theme: theme,
            timeScale: timeScale
        });
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(YAxis, [
        {
            /**
   * @param {number} realIndex
   */ key: "drawYaxis",
            value: function drawYaxis(realIndex) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var yaxisStyle = w.config.yaxis[realIndex].labels.style;
                var yaxisFontSize = yaxisStyle.fontSize, yaxisFontFamily = yaxisStyle.fontFamily, yaxisFontWeight = yaxisStyle.fontWeight;
                var elYaxis = graphics.group({
                    class: "apexcharts-yaxis",
                    rel: realIndex,
                    transform: "translate(".concat(w.globals.translateYAxisX[realIndex], ", 0)")
                });
                if (this.axesUtils.isYAxisHidden(realIndex)) return elYaxis;
                var elYaxisTexts = graphics.group({
                    class: "apexcharts-yaxis-texts-g"
                });
                elYaxis.add(elYaxisTexts);
                var tickAmount = w.globals.yAxisScale[realIndex].result.length - 1;
                var labelsDivider = w.layout.gridHeight / tickAmount;
                var lbFormatter = w.formatters.yLabelFormatters[realIndex];
                var labels = this.axesUtils.checkForReversedLabels(realIndex, w.globals.yAxisScale[realIndex].result.slice());
                var labelStep = 1;
                if (w.config.chart.type === "heatmap" && !w.config.yaxis[realIndex].labels.formatter) {
                    var fs = parseInt(yaxisFontSize, 10) || 11;
                    var maxLabels = Math.max(1, Math.floor(w.layout.gridHeight / (fs * 1.4)));
                    var count = tickAmount + 1;
                    if (count > maxLabels) labelStep = Math.ceil(count / maxLabels);
                }
                if (w.config.yaxis[realIndex].labels.show) {
                    var lY = w.layout.translateY + w.config.yaxis[realIndex].labels.offsetY;
                    if (w.globals.isBarHorizontal) lY = 0;
                    else if (w.config.chart.type === "heatmap") lY -= labelsDivider / 2;
                    lY += parseInt(yaxisFontSize, 10) / 3;
                    var firstLabel = null;
                    for(var i = tickAmount; i >= 0; i--){
                        var thinned = labelStep > 1 && i % labelStep !== 0;
                        var val = thinned ? "" : lbFormatter(labels[i], i, w);
                        var xPad = w.config.yaxis[realIndex].labels.padding;
                        if (w.config.yaxis[realIndex].opposite && w.config.yaxis.length !== 0) xPad *= -1;
                        var textAnchor = this.getTextAnchor(w.config.yaxis[realIndex].labels.align, w.config.yaxis[realIndex].opposite);
                        var yColors = this.axesUtils.getYAxisForeColor(yaxisStyle.colors, realIndex);
                        var foreColor = Array.isArray(yColors) ? yColors[i] : yColors;
                        var existingYLabels = Array.from(w.dom.baseEl.querySelectorAll(".apexcharts-yaxis[rel='".concat(realIndex, "'] .apexcharts-yaxis-label tspan"))).map(function(label2) {
                            return label2.textContent;
                        });
                        var label = graphics.drawText({
                            x: xPad,
                            y: lY,
                            text: existingYLabels.includes(val) && !w.config.yaxis[realIndex].labels.showDuplicates ? "" : val,
                            textAnchor: textAnchor,
                            fontSize: yaxisFontSize,
                            fontFamily: yaxisFontFamily,
                            fontWeight: yaxisFontWeight,
                            maxWidth: w.config.yaxis[realIndex].labels.maxWidth,
                            foreColor: foreColor,
                            isPlainText: false,
                            cssClass: "apexcharts-yaxis-label ".concat(yaxisStyle.cssClass)
                        });
                        elYaxisTexts.add(label);
                        this.addTooltip(label, val);
                        if (firstLabel === null) {
                            firstLabel = label;
                        }
                        if (w.config.yaxis[realIndex].labels.rotate !== 0) {
                            this.rotateLabel(graphics, label, firstLabel, w.config.yaxis[realIndex].labels.rotate);
                        }
                        lY += labelsDivider;
                    }
                }
                this.addYAxisTitle(graphics, elYaxis, realIndex);
                this.addAxisBorder(graphics, elYaxis, realIndex, tickAmount, labelsDivider);
                return elYaxis;
            }
        },
        {
            /**
   * @param {string} align
   * @param {boolean} opposite
   */ key: "getTextAnchor",
            value: function getTextAnchor(align, opposite) {
                if (align === "left") return "start";
                if (align === "center") return "middle";
                if (align === "right") return "end";
                return opposite ? "start" : "end";
            }
        },
        {
            /**
   * @param {any} label
   * @param {any} val
   */ key: "addTooltip",
            value: function addTooltip(label, val) {
                var elTooltipTitle = BrowserAPIs.createElementNS(SVGNS$2, "title");
                elTooltipTitle.textContent = Array.isArray(val) ? val.join(" ") : val;
                label.node.appendChild(elTooltipTitle);
            }
        },
        {
            /**
   * @param {import('../Graphics').default} graphics
   * @param {any} label
   * @param {any} firstLabel
   * @param {number} rotate
   */ key: "rotateLabel",
            value: function rotateLabel(graphics, label, firstLabel, rotate) {
                var firstLabelCenter = graphics.rotateAroundCenter(firstLabel.node);
                var labelCenter = graphics.rotateAroundCenter(label.node);
                label.node.setAttribute("transform", "rotate(".concat(rotate, " ").concat(firstLabelCenter.x, " ").concat(labelCenter.y, ")"));
            }
        },
        {
            /**
   * @param {import('../Graphics').default} graphics
   * @param {any} elYaxis
   * @param {number} realIndex
   */ key: "addYAxisTitle",
            value: function addYAxisTitle(graphics, elYaxis, realIndex) {
                var w = this.w;
                if (w.config.yaxis[realIndex].title.text !== void 0) {
                    var elYaxisTitle = graphics.group({
                        class: "apexcharts-yaxis-title"
                    });
                    var x = w.config.yaxis[realIndex].opposite ? w.globals.translateYAxisX[realIndex] : 0;
                    var elYAxisTitleText = graphics.drawText({
                        x: x,
                        y: w.layout.gridHeight / 2 + w.layout.translateY + w.config.yaxis[realIndex].title.offsetY,
                        text: w.config.yaxis[realIndex].title.text,
                        textAnchor: "end",
                        foreColor: w.config.yaxis[realIndex].title.style.color,
                        fontSize: w.config.yaxis[realIndex].title.style.fontSize,
                        fontWeight: w.config.yaxis[realIndex].title.style.fontWeight,
                        fontFamily: w.config.yaxis[realIndex].title.style.fontFamily,
                        cssClass: "apexcharts-yaxis-title-text ".concat(w.config.yaxis[realIndex].title.style.cssClass)
                    });
                    elYaxisTitle.add(elYAxisTitleText);
                    elYaxis.add(elYaxisTitle);
                }
            }
        },
        {
            /**
   * @param {import('../Graphics').default} graphics
   * @param {any} elYaxis
   * @param {number} realIndex
   * @param {number} tickAmount
   * @param {number} labelsDivider
   */ key: "addAxisBorder",
            value: function addAxisBorder(graphics, elYaxis, realIndex, tickAmount, labelsDivider) {
                var w = this.w;
                var axisBorder = w.config.yaxis[realIndex].axisBorder;
                var x = 31 + axisBorder.offsetX;
                if (w.config.yaxis[realIndex].opposite) x = -31 - axisBorder.offsetX;
                if (axisBorder.show) {
                    var elVerticalLine = graphics.drawLine(x, w.layout.translateY + axisBorder.offsetY - 2, x, w.layout.gridHeight + w.layout.translateY + axisBorder.offsetY + 2, axisBorder.color, 0, axisBorder.width);
                    elYaxis.add(elVerticalLine);
                }
                if (w.config.yaxis[realIndex].axisTicks.show) {
                    this.axesUtils.drawYAxisTicks(x, tickAmount, axisBorder, w.config.yaxis[realIndex].axisTicks, realIndex, labelsDivider, elYaxis);
                }
            }
        },
        {
            /**
   * @param {number} realIndex
   */ key: "drawYaxisInversed",
            value: function drawYaxisInversed(realIndex) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var elXaxis = graphics.group({
                    class: "apexcharts-xaxis apexcharts-yaxis-inversed"
                });
                var elXaxisTexts = graphics.group({
                    class: "apexcharts-xaxis-texts-g",
                    transform: "translate(".concat(w.layout.translateXAxisX, ", ").concat(w.layout.translateXAxisY, ")")
                });
                elXaxis.add(elXaxisTexts);
                var tickAmount = w.globals.yAxisScale[realIndex].result.length - 1;
                var labelsDivider = w.layout.gridWidth / tickAmount + 0.1;
                var l = labelsDivider + w.config.xaxis.labels.offsetX;
                var lbFormatter = w.formatters.xLabelFormatter;
                var labels = this.axesUtils.checkForReversedLabels(realIndex, w.globals.yAxisScale[realIndex].result.slice());
                var timescaleLabels = w.labelData.timescaleLabels;
                if (timescaleLabels.length > 0) {
                    this.xaxisLabels = timescaleLabels.slice();
                    labels = timescaleLabels.slice();
                    tickAmount = labels.length;
                }
                if (w.config.xaxis.labels.show) {
                    for(var i = timescaleLabels.length ? 0 : tickAmount; timescaleLabels.length ? i < timescaleLabels.length : i >= 0; timescaleLabels.length ? i++ : i--){
                        var val = lbFormatter == null ? void 0 : lbFormatter(labels[i], i, w);
                        var x = w.layout.gridWidth + w.globals.padHorizontal - (l - labelsDivider + w.config.xaxis.labels.offsetX);
                        if (timescaleLabels.length) {
                            var label = this.axesUtils.getLabel(labels, timescaleLabels, x, i, this.drawnLabels, this.xaxisFontSize);
                            x = label.x;
                            val = label.text;
                            this.drawnLabels.push(label.text);
                            if (i === 0 && w.globals.skipFirstTimelinelabel) val = "";
                            if (i === labels.length - 1 && w.globals.skipLastTimelinelabel) val = "";
                        }
                        var elTick = graphics.drawText({
                            x: x,
                            y: this.xAxisoffX + w.config.xaxis.labels.offsetY + 30 - (w.config.xaxis.position === "top" ? w.layout.xAxisHeight + w.config.xaxis.axisTicks.height - 2 : 0),
                            text: val,
                            textAnchor: "middle",
                            foreColor: Array.isArray(this.xaxisForeColors) ? this.xaxisForeColors[realIndex] : this.xaxisForeColors,
                            fontSize: this.xaxisFontSize,
                            fontFamily: this.axisFontFamily,
                            fontWeight: w.config.xaxis.labels.style.fontWeight,
                            isPlainText: false,
                            cssClass: "apexcharts-xaxis-label ".concat(w.config.xaxis.labels.style.cssClass)
                        });
                        elXaxisTexts.add(elTick);
                        this.addTooltip(elTick, val);
                        l += labelsDivider;
                    }
                }
                this.inversedYAxisTitleText(elXaxis);
                this.inversedYAxisBorder(elXaxis);
                return elXaxis;
            }
        },
        {
            /**
   * @param {any} parent
   */ key: "inversedYAxisBorder",
            value: function inversedYAxisBorder(parent) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var axisBorder = w.config.xaxis.axisBorder;
                if (axisBorder.show) {
                    var lineCorrection = 0;
                    if (w.config.chart.type === "bar" && w.axisFlags.isXNumeric) lineCorrection -= 15;
                    var elHorzLine = graphics.drawLine(w.globals.padHorizontal + lineCorrection + axisBorder.offsetX, this.xAxisoffX, w.layout.gridWidth, this.xAxisoffX, axisBorder.color, 0, axisBorder.height);
                    if (this.elgrid && this.elgrid.elGridBorders && w.config.grid.show) {
                        this.elgrid.elGridBorders.add(elHorzLine);
                    } else {
                        parent.add(elHorzLine);
                    }
                }
            }
        },
        {
            /**
   * @param {any} parent
   */ key: "inversedYAxisTitleText",
            value: function inversedYAxisTitleText(parent) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                if (w.config.xaxis.title.text !== void 0) {
                    var elYaxisTitle = graphics.group({
                        class: "apexcharts-xaxis-title apexcharts-yaxis-title-inversed"
                    });
                    var elYAxisTitleText = graphics.drawText({
                        x: w.layout.gridWidth / 2 + w.config.xaxis.title.offsetX,
                        y: this.xAxisoffX + parseFloat(this.xaxisFontSize) + parseFloat(w.config.xaxis.title.style.fontSize) + w.config.xaxis.title.offsetY + 20,
                        text: w.config.xaxis.title.text,
                        textAnchor: "middle",
                        fontSize: w.config.xaxis.title.style.fontSize,
                        fontFamily: w.config.xaxis.title.style.fontFamily,
                        fontWeight: w.config.xaxis.title.style.fontWeight,
                        foreColor: w.config.xaxis.title.style.color,
                        cssClass: "apexcharts-xaxis-title-text ".concat(w.config.xaxis.title.style.cssClass)
                    });
                    elYaxisTitle.add(elYAxisTitleText);
                    parent.add(elYaxisTitle);
                }
            }
        },
        {
            /**
   * @param {number} realIndex
   * @param {boolean} yAxisOpposite
   */ key: "yAxisTitleRotate",
            value: function yAxisTitleRotate(realIndex, yAxisOpposite) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var elYAxisLabelsWrap = w.dom.baseEl.querySelector(".apexcharts-yaxis[rel='".concat(realIndex, "'] .apexcharts-yaxis-texts-g"));
                var yAxisLabelsCoord = elYAxisLabelsWrap ? elYAxisLabelsWrap.getBoundingClientRect() : {
                    width: 0,
                    height: 0
                };
                var yAxisTitle = w.dom.baseEl.querySelector(".apexcharts-yaxis[rel='".concat(realIndex, "'] .apexcharts-yaxis-title text"));
                var yAxisTitleCoord = yAxisTitle ? yAxisTitle.getBoundingClientRect() : {
                    width: 0,
                    height: 0
                };
                if (yAxisTitle) {
                    var x = this.xPaddingForYAxisTitle(realIndex, yAxisLabelsCoord, yAxisTitleCoord, yAxisOpposite);
                    yAxisTitle.setAttribute("x", String(x.xPos - (yAxisOpposite ? 10 : 0)));
                    var titleRotatingCenter = graphics.rotateAroundCenter(yAxisTitle);
                    yAxisTitle.setAttribute("transform", "rotate(".concat(yAxisOpposite ? w.config.yaxis[realIndex].title.rotate * -1 : w.config.yaxis[realIndex].title.rotate, " ").concat(titleRotatingCenter.x, " ").concat(titleRotatingCenter.y, ")"));
                }
            }
        },
        {
            /**
   * @param {number} realIndex
   * @param {{width: number, height: number}} yAxisLabelsCoord
   * @param {{width: number, height: number}} yAxisTitleCoord
   * @param {boolean} yAxisOpposite
   */ key: "xPaddingForYAxisTitle",
            value: function xPaddingForYAxisTitle(realIndex, yAxisLabelsCoord, yAxisTitleCoord, yAxisOpposite) {
                var w = this.w;
                var x = 0;
                var padd = 10;
                if (w.config.yaxis[realIndex].title.text === void 0 || realIndex < 0) {
                    return {
                        xPos: x,
                        padd: 0
                    };
                }
                if (yAxisOpposite) {
                    x = yAxisLabelsCoord.width + w.config.yaxis[realIndex].title.offsetX + yAxisTitleCoord.width / 2 + padd / 2;
                } else {
                    x = yAxisLabelsCoord.width * -1 + w.config.yaxis[realIndex].title.offsetX + padd / 2 + yAxisTitleCoord.width / 2;
                    if (w.globals.isBarHorizontal) {
                        padd = 25;
                        x = yAxisLabelsCoord.width * -1 - w.config.yaxis[realIndex].title.offsetX - padd;
                    }
                }
                return {
                    xPos: x,
                    padd: padd
                };
            }
        },
        {
            /**
   * @param {Array<{width: number, height: number}>} yaxisLabelCoords
   * @param {Array<{width: number, height: number}>} yTitleCoords
   */ key: "setYAxisXPosition",
            value: function setYAxisXPosition(yaxisLabelCoords, yTitleCoords) {
                var w = this.w;
                var xLeft = 0;
                var xRight = 0;
                var leftOffsetX = 18;
                var rightOffsetX = 1;
                if (w.config.yaxis.length > 1) this.multipleYs = true;
                w.config.yaxis.forEach(function(yaxe, index) {
                    var shouldNotDrawAxis = w.globals.ignoreYAxisIndexes.includes(index) || !yaxe.show || yaxe.floating || yaxisLabelCoords[index].width === 0;
                    var axisWidth = yaxisLabelCoords[index].width + yTitleCoords[index].width;
                    if (!yaxe.opposite) {
                        xLeft = w.layout.translateX - leftOffsetX;
                        if (!shouldNotDrawAxis) leftOffsetX += axisWidth + 20;
                        w.globals.translateYAxisX[index] = xLeft + yaxe.labels.offsetX;
                    } else {
                        if (w.globals.isBarHorizontal) {
                            xRight = w.layout.gridWidth + w.layout.translateX - 1;
                            w.globals.translateYAxisX[index] = xRight - yaxe.labels.offsetX;
                        } else {
                            xRight = w.layout.gridWidth + w.layout.translateX + rightOffsetX;
                            if (!shouldNotDrawAxis) rightOffsetX += axisWidth + 20;
                            w.globals.translateYAxisX[index] = xRight - yaxe.labels.offsetX + 20;
                        }
                    }
                });
            }
        },
        {
            key: "setYAxisTextAlignments",
            value: function setYAxisTextAlignments() {
                var w = this.w;
                var yaxis = Array.from(w.dom.baseEl.getElementsByClassName("apexcharts-yaxis"));
                yaxis.forEach(function(y, index) {
                    var yaxe = w.config.yaxis[index];
                    if (yaxe && !yaxe.floating && yaxe.labels.align !== void 0) {
                        var yAxisInner = w.dom.baseEl.querySelector(".apexcharts-yaxis[rel='".concat(index, "'] .apexcharts-yaxis-texts-g"));
                        var yAxisTexts = Array.from(w.dom.baseEl.querySelectorAll(".apexcharts-yaxis[rel='".concat(index, "'] .apexcharts-yaxis-label")));
                        var rect = /** @type {Element} */ yAxisInner.getBoundingClientRect();
                        yAxisTexts.forEach(function(label) {
                            label.setAttribute("text-anchor", yaxe.labels.align);
                        });
                        if (yaxe.labels.align === "left" && !yaxe.opposite) {
                            yAxisInner.setAttribute("transform", "translate(-".concat(rect.width, ", 0)"));
                        } else if (yaxe.labels.align === "center") {
                            yAxisInner.setAttribute("transform", "translate(".concat(rect.width / 2 * (!yaxe.opposite ? -1 : 1), ", 0)"));
                        } else if (yaxe.labels.align === "right" && yaxe.opposite) {
                            yAxisInner.setAttribute("transform", "translate(".concat(rect.width, ", 0)"));
                        }
                    }
                });
            }
        }
    ]);
    return YAxis;
}();
var Events = /*#__PURE__*/ function() {
    "use strict";
    function Events(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Events);
        this.w = w;
        this.ctx = ctx;
        this.documentEvent = this.documentEvent.bind(this);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Events, [
        {
            /**
   * @param {string} name
   * @param {Function} handler
   */ key: "addEventListener",
            value: function addEventListener(name2, handler) {
                var w = this.w;
                if (Object.prototype.hasOwnProperty.call(w.globals.events, name2)) {
                    w.globals.events[name2].push(handler);
                } else {
                    w.globals.events[name2] = [
                        handler
                    ];
                }
            }
        },
        {
            /**
   * @param {string} name
   * @param {Function} handler
   */ key: "removeEventListener",
            value: function removeEventListener(name2, handler) {
                var w = this.w;
                if (!Object.prototype.hasOwnProperty.call(w.globals.events, name2)) {
                    return;
                }
                var index = /** @type {Record<string,any>} */ w.globals.events[name2].indexOf(handler);
                if (index !== -1) {
                    w.globals.events[name2].splice(index, 1);
                }
            }
        },
        {
            /**
   * @param {string} name
   * @param {any[]} args
   */ key: "fireEvent",
            value: function fireEvent(name2, args) {
                var w = this.w;
                if (!Object.prototype.hasOwnProperty.call(w.globals.events, name2)) {
                    return;
                }
                if (!args || !args.length) {
                    args = [];
                }
                var evs = /** @type {Record<string,any>} */ w.globals.events[name2];
                var l = evs.length;
                for(var i = 0; i < l; i++){
                    evs[i].apply(null, args);
                }
            }
        },
        {
            key: "setupEventHandlers",
            value: function setupEventHandlers() {
                var _this = this;
                var w = this.w;
                var me = this.ctx;
                var clickableArea = w.dom.baseEl.querySelector(w.globals.chartClass);
                this.ctx.eventList.forEach(function(event) {
                    clickableArea == null ? void 0 : clickableArea.addEventListener(event, function(e) {
                        var capturedSeriesIndex = e.target.getAttribute("i") === null && w.interact.capturedSeriesIndex !== -1 ? w.interact.capturedSeriesIndex : e.target.getAttribute("i");
                        var capturedDataPointIndex = e.target.getAttribute("j") === null && w.interact.capturedDataPointIndex !== -1 ? w.interact.capturedDataPointIndex : e.target.getAttribute("j");
                        var opts = Object.assign({}, w, {
                            seriesIndex: w.globals.axisCharts ? capturedSeriesIndex : 0,
                            dataPointIndex: capturedDataPointIndex
                        });
                        if (e.type === "keydown") {
                            if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.keyboard.enabled) {
                                if (me.ctx.keyboardNavigation) {
                                    me.ctx.keyboardNavigation.handleKey(e);
                                }
                                if (typeof w.config.chart.events.keyDown === "function") {
                                    w.config.chart.events.keyDown(e, me, opts);
                                }
                                me.ctx.events.fireEvent("keydown", [
                                    e,
                                    me,
                                    opts
                                ]);
                            }
                        } else if (e.type === "keyup") {
                            if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.keyboard.enabled) {
                                if (typeof w.config.chart.events.keyUp === "function") {
                                    w.config.chart.events.keyUp(e, me, opts);
                                }
                                me.ctx.events.fireEvent("keyup", [
                                    e,
                                    me,
                                    opts
                                ]);
                            }
                        } else if (e.type === "mousemove" || e.type === "touchmove") {
                            if (typeof w.config.chart.events.mouseMove === "function") {
                                w.config.chart.events.mouseMove(e, me, opts);
                            }
                        } else if (e.type === "mouseleave" || e.type === "touchleave") {
                            if (typeof w.config.chart.events.mouseLeave === "function") {
                                w.config.chart.events.mouseLeave(e, me, opts);
                            }
                        } else if (e.type === "mouseup" && e.which === 1 || e.type === "touchend") {
                            if (typeof w.config.chart.events.click === "function") {
                                w.config.chart.events.click(e, me, opts);
                            }
                            me.ctx.events.fireEvent("click", [
                                e,
                                me,
                                opts
                            ]);
                        }
                    }, {
                        capture: false,
                        passive: true
                    });
                });
                this.ctx.eventList.forEach(function(event) {
                    w.dom.baseEl.addEventListener(event, _this.documentEvent, {
                        passive: true
                    });
                });
                this.ctx.core.setupBrushHandler();
            }
        },
        {
            /**
   * @param {any} e
   */ key: "documentEvent",
            value: function documentEvent(e) {
                var w = this.w;
                var target = e.target.className;
                if (e.type === "click") {
                    var elMenu = w.dom.baseEl.querySelector(".apexcharts-menu");
                    if (elMenu && elMenu.classList.contains("apexcharts-menu-open") && target !== "apexcharts-menu-icon") {
                        elMenu.classList.remove("apexcharts-menu-open");
                    }
                }
                w.interact.clientX = e.type === "touchmove" ? e.touches[0].clientX : e.clientX;
                w.interact.clientY = e.type === "touchmove" ? e.touches[0].clientY : e.clientY;
            }
        }
    ]);
    return Events;
}();
var Localization = /*#__PURE__*/ function() {
    "use strict";
    function Localization(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Localization);
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Localization, [
        {
            /**
   * @param {string} localeName
   */ key: "setCurrentLocaleValues",
            value: function setCurrentLocaleValues(localeName) {
                var locales = this.w.config.chart.locales;
                var globalApex = Environment.getApex();
                if (globalApex.chart && globalApex.chart.locales && globalApex.chart.locales.length > 0) {
                    locales = this.w.config.chart.locales.concat(globalApex.chart.locales);
                }
                var selectedLocale = locales.filter(function(c) {
                    return c.name === localeName;
                })[0];
                if (selectedLocale) {
                    var ret = Utils$1.extend(en, selectedLocale);
                    this.w.globals.locale = ret.options;
                } else {
                    throw new Error("Wrong locale name provided. Please make sure you set the correct locale name in options");
                }
            }
        }
    ]);
    return Localization;
}();
var Axes = /*#__PURE__*/ function() {
    "use strict";
    function Axes(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Axes);
        this.w = w;
        this.ctx = ctx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Axes, [
        {
            /**
   * @param {string} type
   * @param {any} elgrid
   */ key: "drawAxis",
            value: function drawAxis(type, elgrid) {
                var _this = this;
                var gl = this.w.globals;
                var cnf = this.w.config;
                var xAxis = new XAxis(this.w, this.ctx, elgrid);
                var yAxis = new YAxis(this.w, {
                    theme: this.ctx.theme,
                    timeScale: this.ctx.timeScale
                }, elgrid);
                if (gl.axisCharts && type !== "radar") {
                    var elXaxis, elYaxis;
                    if (gl.isBarHorizontal) {
                        elYaxis = yAxis.drawYaxisInversed(0);
                        elXaxis = xAxis.drawXaxisInversed(0);
                        this.w.dom.elGraphical.add(elXaxis);
                        this.w.dom.elGraphical.add(elYaxis);
                    } else {
                        elXaxis = xAxis.drawXaxis();
                        this.w.dom.elGraphical.add(elXaxis);
                        cnf.yaxis.map(function(yaxe, index) {
                            if (gl.ignoreYAxisIndexes.indexOf(index) === -1) {
                                elYaxis = yAxis.drawYaxis(index);
                                _this.w.dom.Paper.add(elYaxis);
                                if (_this.w.config.grid.position === "back") {
                                    var inner = _this.w.dom.Paper.children()[1];
                                    if (inner) {
                                        inner.remove();
                                        _this.w.dom.Paper.add(inner);
                                    }
                                }
                            }
                        });
                    }
                }
            }
        }
    ]);
    return Axes;
}();
var Crosshairs = /*#__PURE__*/ function() {
    "use strict";
    function Crosshairs(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Crosshairs);
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Crosshairs, [
        {
            key: "drawXCrosshairs",
            value: function drawXCrosshairs() {
                var w = this.w;
                w.dom.elGraphical.node.querySelectorAll(":scope > .apexcharts-xcrosshairs").forEach(function(el) {
                    return el.remove();
                });
                var graphics = new Graphics(this.w);
                var filters = new Filters(this.w);
                var crosshairGradient = w.config.xaxis.crosshairs.fill.gradient;
                var crosshairShadow = w.config.xaxis.crosshairs.dropShadow;
                var fillType = w.config.xaxis.crosshairs.fill.type;
                var gradientFrom = crosshairGradient.colorFrom;
                var gradientTo = crosshairGradient.colorTo;
                var opacityFrom = crosshairGradient.opacityFrom;
                var opacityTo = crosshairGradient.opacityTo;
                var stops = crosshairGradient.stops;
                var shadow = "none";
                var dropShadow = crosshairShadow.enabled;
                var shadowLeft = crosshairShadow.left;
                var shadowTop = crosshairShadow.top;
                var shadowBlur = crosshairShadow.blur;
                var shadowColor = crosshairShadow.color;
                var shadowOpacity = crosshairShadow.opacity;
                var xcrosshairsFill = w.config.xaxis.crosshairs.fill.color;
                if (w.config.xaxis.crosshairs.show) {
                    if (fillType === "gradient") {
                        xcrosshairsFill = graphics.drawGradient("vertical", gradientFrom, gradientTo, opacityFrom, opacityTo, null, stops, []);
                    }
                    var xcrosshairs = w.config.xaxis.crosshairs.width === 1 ? graphics.drawLine(0, 0, 0, 0) : graphics.drawRect();
                    var gridHeight = w.layout.gridHeight;
                    if (!Utils$1.isNumber(gridHeight) || gridHeight < 0) {
                        gridHeight = 0;
                    }
                    var crosshairsWidth = w.config.xaxis.crosshairs.width;
                    if (!Utils$1.isNumber(crosshairsWidth) || Number(crosshairsWidth) < 0) {
                        crosshairsWidth = 0;
                    }
                    xcrosshairs.attr({
                        class: "apexcharts-xcrosshairs",
                        x: 0,
                        y: 0,
                        y2: gridHeight,
                        width: crosshairsWidth,
                        height: gridHeight,
                        fill: xcrosshairsFill,
                        filter: shadow,
                        "fill-opacity": w.config.xaxis.crosshairs.opacity,
                        stroke: w.config.xaxis.crosshairs.stroke.color,
                        "stroke-width": w.config.xaxis.crosshairs.stroke.width,
                        "stroke-dasharray": w.config.xaxis.crosshairs.stroke.dashArray
                    });
                    if (dropShadow) {
                        xcrosshairs = filters.dropShadow(xcrosshairs, {
                            left: shadowLeft,
                            top: shadowTop,
                            blur: shadowBlur,
                            color: shadowColor,
                            opacity: shadowOpacity
                        });
                    }
                    w.dom.elGraphical.add(xcrosshairs);
                }
            }
        },
        {
            key: "drawYCrosshairs",
            value: function drawYCrosshairs() {
                var w = this.w;
                w.dom.elGraphical.node.querySelectorAll(":scope > .apexcharts-ycrosshairs, :scope > .apexcharts-ycrosshairs-hidden").forEach(function(el) {
                    return el.remove();
                });
                var graphics = new Graphics(this.w);
                var crosshair = /** @type {any[]} */ w.config.yaxis[0].crosshairs;
                var offX = w.globals.barPadForNumericAxis;
                if (/** @type {any[]} */ w.config.yaxis[0].crosshairs.show) {
                    var ycrosshairs = graphics.drawLine(-offX, 0, w.layout.gridWidth + offX, 0, crosshair.stroke.color, crosshair.stroke.dashArray, crosshair.stroke.width);
                    ycrosshairs.attr({
                        class: "apexcharts-ycrosshairs"
                    });
                    w.dom.elGraphical.add(ycrosshairs);
                }
                var ycrosshairsHidden = graphics.drawLine(-offX, 0, w.layout.gridWidth + offX, 0, crosshair.stroke.color, 0, 0);
                ycrosshairsHidden.attr({
                    class: "apexcharts-ycrosshairs-hidden"
                });
                w.dom.elGraphical.add(ycrosshairsHidden);
            }
        }
    ]);
    return Crosshairs;
}();
function mergeYaxisOverride(base, override) {
    if (!Utils$1.isObject(base) || !Utils$1.isObject(override)) {
        return override !== void 0 ? override : base;
    }
    var out = __spreadValues({}, base);
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    try {
        for(var _iterator = Object.keys(override)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var key = _step.value;
            var v = override[key];
            if (v === void 0) continue;
            if (Utils$1.isObject(v) && Utils$1.isObject(base[key])) {
                out[key] = mergeYaxisOverride(base[key], v);
            } else {
                out[key] = v;
            }
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return out;
}
var Responsive = /*#__PURE__*/ function() {
    "use strict";
    function Responsive(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Responsive);
        this.w = w;
        this._activeBreakpoint = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Responsive, [
        {
            // the opts parameter if not null has to be set overriding everything
            // as the opts is set by user externally
            /**
   * @param {object} opts
   */ key: "checkResponsiveConfig",
            value: function checkResponsiveConfig(opts) {
                var _this = this;
                var w = this.w;
                var cnf = w.config;
                if (cnf.responsive.length === 0) return;
                var res = cnf.responsive.slice();
                res.sort(function(a, b) {
                    return a.breakpoint > b.breakpoint ? 1 : b.breakpoint > a.breakpoint ? -1 : 0;
                }).reverse();
                var config = new Config({});
                var iterateResponsiveOptions = function iterateResponsiveOptions() {
                    var newOptions = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                    var _a;
                    var largestBreakpoint = res[0].breakpoint;
                    var width = Environment.isBrowser() ? window.innerWidth > 0 ? window.innerWidth : screen.width : 0;
                    if (width > largestBreakpoint) {
                        if (_this._activeBreakpoint !== null) {
                            if (!w.globals.initialConfig) return;
                            var initialConfig = Utils$1.clone(w.globals.initialConfig);
                            initialConfig.series = Utils$1.clone(w.config.series);
                            var options2 = CoreUtils.extendArrayProps(config, initialConfig, w);
                            newOptions = Utils$1.extend(options2, newOptions);
                            _this.overrideResponsiveOptions(newOptions);
                            _this._activeBreakpoint = null;
                        }
                    } else {
                        var _loop = function(i) {
                            if (width < res[i].breakpoint) {
                                var originalUserYaxis = ((_a = res[i].options) == null ? void 0 : _a.yaxis) ? Utils$1.clone(res[i].options.yaxis) : null;
                                newOptions = CoreUtils.extendArrayProps(config, res[i].options, w);
                                newOptions = Utils$1.extend(w.config, newOptions);
                                if (Array.isArray(w.config.yaxis) && originalUserYaxis) {
                                    var userYaxis = Array.isArray(originalUserYaxis) ? originalUserYaxis : [
                                        originalUserYaxis
                                    ];
                                    newOptions = __spreadProps(__spreadValues({}, newOptions), {
                                        yaxis: w.config.yaxis.map(function(baseAxis, idx) {
                                            return mergeYaxisOverride(baseAxis, userYaxis[idx]);
                                        })
                                    });
                                }
                                _this.overrideResponsiveOptions(newOptions);
                                _this._activeBreakpoint = res[i].breakpoint;
                            }
                        };
                        for(var i = 0; i < res.length; i++)_loop(i);
                    }
                };
                if (opts) {
                    var options2 = CoreUtils.extendArrayProps(config, opts, w);
                    options2 = Utils$1.extend(w.config, options2);
                    options2 = Utils$1.extend(options2, opts);
                    iterateResponsiveOptions(options2);
                } else {
                    iterateResponsiveOptions({});
                }
            }
        },
        {
            /**
   * @param {Record<string, any>} newOptions
   */ key: "overrideResponsiveOptions",
            value: function overrideResponsiveOptions(newOptions) {
                var newConfig = new Config(newOptions).init({
                    responsiveOverride: true
                });
                this.w.config = /** @type {any} */ newConfig;
            }
        }
    ]);
    return Responsive;
}();
function captureStreamFrame(w) {
    var _a, _b, _c;
    var gl = w.globals;
    if (!gl.axisCharts || !w.seriesData || !Array.isArray(w.seriesData.series) || w.seriesData.series.length === 0) {
        gl.prevStreamFrame = null;
        return;
    }
    var rPixels = [];
    if ((_b = (_a = w.dom) == null ? void 0 : _a.baseEl) == null ? void 0 : _b.querySelectorAll) {
        w.dom.baseEl.querySelectorAll(".apexcharts-marker").forEach(function(node) {
            var _a2, _b2, _c2, _d, _e;
            var ri = parseInt((_a2 = node.getAttribute("index")) != null ? _a2 : "", 10);
            var j = parseInt((_c2 = (_b2 = node.getAttribute("j")) != null ? _b2 : node.getAttribute("rel")) != null ? _c2 : "", 10);
            var r = parseFloat((_e = (_d = node.getAttribute("r")) != null ? _d : node.getAttribute("default-marker-size")) != null ? _e : "");
            if (isFinite(ri) && isFinite(j) && isFinite(r)) {
                (rPixels[ri] = rPixels[ri] || [])[j] = r;
            }
        });
    }
    gl.prevStreamFrame = {
        seriesX: (w.seriesData.seriesX || []).slice(),
        seriesY: w.seriesData.series.slice(),
        xPixels: (gl.seriesXvalues || []).slice(),
        yPixels: (gl.seriesYvalues || []).slice(),
        rPixels: rPixels,
        labels: (gl.labels || []).slice(),
        isXNumeric: !!((_c = w.axisFlags) == null ? void 0 : _c.isXNumeric)
    };
}
function detectStreamScroll(w, realIndex, newXPixels, newYPixels) {
    var _a;
    var gl = w.globals;
    var frame = gl.prevStreamFrame;
    if (!frame || !gl.dataChanged || !((_a = w.axisFlags) == null ? void 0 : _a.isXNumeric)) return null;
    var oldX = frame.seriesX[realIndex];
    var oldY = frame.seriesY[realIndex];
    var newX = w.seriesData.seriesX[realIndex];
    var newY = w.seriesData.series[realIndex];
    if (!oldX || !oldY || !newX || !newY) return null;
    if (oldX.length < 3 || newX.length < 3) return null;
    var k = -1;
    for(var i = 0; i < oldX.length; i++){
        if (oldX[i] === newX[0]) {
            k = i;
            break;
        }
    }
    if (k === -1) return null;
    var overlap = Math.min(oldX.length - k, newX.length);
    if (overlap < 2) return null;
    var appended = newX.length - overlap;
    if (k === 0 && appended === 0) return null;
    for(var i1 = 0; i1 < overlap; i1++){
        if (oldX[k + i1] !== newX[i1]) return null;
        var oy = oldY[k + i1];
        var ny = newY[i1];
        if (oy !== ny && !(oy == null && ny == null)) return null;
    }
    var oldXP = frame.xPixels[realIndex];
    var oldYP = frame.yPixels[realIndex];
    if (!oldXP || !oldYP) return null;
    var a = -1;
    var b = -1;
    for(var i2 = 0; i2 < overlap; i2++){
        if (oldXP[k + i2] == null || oldYP[k + i2] == null || newXPixels[i2] == null || newYPixels[i2] == null) {
            continue;
        }
        if (a === -1) a = i2;
        b = i2;
    }
    if (a === -1 || b <= a) return null;
    var nxA = /** @type {number} */ newXPixels[a];
    var nxB = /** @type {number} */ newXPixels[b];
    var oxA = /** @type {number} */ oldXP[k + a];
    var oxB = /** @type {number} */ oldXP[k + b];
    if (Math.abs(nxB - nxA) < 1e-6) return null;
    var ax = (oxB - oxA) / (nxB - nxA);
    var bx = oxA - ax * nxA;
    if (!isFinite(ax) || !isFinite(bx)) return null;
    if (Math.abs(ax - 1) > 0.02) return null;
    if (Math.abs(bx) < 0.5) return null;
    var yLo = a;
    var yHi = a;
    for(var i3 = a; i3 <= b; i3++){
        var ny1 = newYPixels[i3];
        if (ny1 == null || oldYP[k + i3] == null) continue;
        if (ny1 < /** @type {number} */ newYPixels[yLo]) yLo = i3;
        if (ny1 > /** @type {number} */ newYPixels[yHi]) yHi = i3;
    }
    var ay = 1;
    var by = 0;
    var nyLo = /** @type {number} */ newYPixels[yLo];
    var nyHi = /** @type {number} */ newYPixels[yHi];
    if (Math.abs(nyHi - nyLo) > 1e-6) {
        ay = /** @type {number} */ (oldYP[k + yHi] - /** @type {number} */ oldYP[k + yLo]) / (nyHi - nyLo);
        by = /** @type {number} */ oldYP[k + yLo] - ay * nyLo;
    } else {
        by = /** @type {number} */ oldYP[k + yLo] - nyLo;
    }
    if (!isFinite(ay) || !isFinite(by) || ay < 0.2 || ay > 5) return null;
    var m = Math.floor((a + b) / 2);
    if (m !== a && m !== b && newXPixels[m] != null && oldXP[k + m] != null) {
        var predX = ax * /** @type {number} */ newXPixels[m] + bx;
        if (Math.abs(predX - /** @type {number} */ oldXP[k + m]) > 1.5) {
            return null;
        }
        if (newYPixels[m] != null && oldYP[k + m] != null) {
            var predY = ay * /** @type {number} */ newYPixels[m] + by;
            if (Math.abs(predY - /** @type {number} */ oldYP[k + m]) > 1.5) {
                return null;
            }
        }
    }
    return {
        ax: ax,
        bx: bx,
        ay: ay,
        by: by
    };
}
var NUM_RE = /[+-]?(?:\d+\.?\d*|\.\d+)(?:e[+-]?\d+)?/gi;
function projectPathToPrevFrame(d, t) {
    var ax = t.ax, bx = t.bx, ay = t.ay, by = t.by;
    var out = [];
    var re = /([MmLlHhVvCcSsQqTtAaZz])([^MmLlHhVvCcSsQqTtAaZz]*)/g;
    var match;
    while((match = re.exec(d)) !== null){
        var cmd = match[1].toUpperCase();
        var nums = (match[2].match(NUM_RE) || []).map(parseFloat);
        if (cmd === "Z") {
            out.push("z");
            continue;
        }
        if (cmd === "H") {
            var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
            try {
                for(var _iterator = nums[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                    var x = _step.value;
                    out.push("H ".concat(ax * x + bx));
                }
            } catch (err) {
                _didIteratorError = true;
                _iteratorError = err;
            } finally{
                try {
                    if (!_iteratorNormalCompletion && _iterator.return != null) {
                        _iterator.return();
                    }
                } finally{
                    if (_didIteratorError) {
                        throw _iteratorError;
                    }
                }
            }
            continue;
        }
        if (cmd === "V") {
            var _iteratorNormalCompletion1 = true, _didIteratorError1 = false, _iteratorError1 = undefined;
            try {
                for(var _iterator1 = nums[Symbol.iterator](), _step1; !(_iteratorNormalCompletion1 = (_step1 = _iterator1.next()).done); _iteratorNormalCompletion1 = true){
                    var y = _step1.value;
                    out.push("V ".concat(ay * y + by));
                }
            } catch (err) {
                _didIteratorError1 = true;
                _iteratorError1 = err;
            } finally{
                try {
                    if (!_iteratorNormalCompletion1 && _iterator1.return != null) {
                        _iterator1.return();
                    }
                } finally{
                    if (_didIteratorError1) {
                        throw _iteratorError1;
                    }
                }
            }
            continue;
        }
        if (cmd === "A") {
            for(var i = 0; i + 6 < nums.length; i += 7){
                out.push("A ".concat(nums[i], " ").concat(nums[i + 1], " ").concat(nums[i + 2], " ").concat(nums[i + 3], " ").concat(nums[i + 4], " ").concat(ax * nums[i + 5] + bx, " ").concat(ay * nums[i + 6] + by));
            }
            continue;
        }
        var coords = [];
        for(var i1 = 0; i1 + 1 < nums.length; i1 += 2){
            coords.push("".concat(ax * nums[i1] + bx, " ").concat(ay * nums[i1 + 1] + by));
        }
        if (coords.length) out.push("".concat(cmd, " ").concat(coords.join(" ")));
    }
    return out.join(" ");
}
function trimStreamingSeries(newSeries, w) {
    var cfg = w.config.chart.streaming;
    if (!cfg || !cfg.enabled) return;
    var maxPoints = cfg.maxPoints;
    var range = w.config.xaxis.range;
    var xOf = function xOf(p) {
        if (p == null) return null;
        if (Array.isArray(p)) return typeof p[0] === "number" ? p[0] : null;
        if ((typeof p === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(p)) === "object") return typeof p.x === "number" ? p.x : null;
        return null;
    };
    newSeries.forEach(function(s) {
        var _a;
        var data = s == null ? void 0 : s.data;
        if (!Array.isArray(data) || data.length < 2) return;
        if (typeof maxPoints === "number" && maxPoints > 0) {
            if (data.length > maxPoints) s.data = data.slice(data.length - maxPoints);
            return;
        }
        if (!range) return;
        var lastX = xOf(data[data.length - 1]);
        var firstX = xOf(data[0]);
        if (lastX == null || firstX == null || lastX <= firstX) return;
        var avgSpacing = (lastX - firstX) / (data.length - 1);
        var cutoff = lastX - range - 2 * avgSpacing;
        var idx = 0;
        while(idx < data.length && ((_a = xOf(data[idx])) != null ? _a : cutoff) < cutoff)idx++;
        if (idx > 0) s.data = data.slice(idx);
    });
}
function buildUnionEntries(join, oldN) {
    var exitSet = new Set(join.exits);
    var entries = [];
    var oi = 0;
    for(var nj = 0; nj < join.toOld.length; nj++){
        var oj = join.toOld[nj];
        if (oj !== -1) {
            while(oi < oj){
                if (exitSet.has(oi)) entries.push({
                    oldJ: oi,
                    newJ: -1
                });
                oi++;
            }
            entries.push({
                oldJ: oj,
                newJ: nj
            });
            oi = oj + 1;
        } else {
            entries.push({
                oldJ: -1,
                newJ: nj
            });
        }
    }
    while(oi < oldN){
        if (exitSet.has(oi)) entries.push({
            oldJ: oi,
            newJ: -1
        });
        oi++;
    }
    return entries;
}
function analyzeSeriesPath(d, expectedAnchors, isArea) {
    if (!d || typeof d !== "string") return null;
    var cmds = parsePath(d);
    if (!cmds.length || cmds[0][0] !== "M") return null;
    for(var i = 1; i < cmds.length; i++){
        if (cmds[i][0] === "M") return null;
    }
    var body = cmds;
    var closing = null;
    if (isArea) {
        if (cmds.length < 5) return null;
        closing = cmds.slice(-3);
        body = cmds.slice(0, -3);
        if (closing[2][0] !== "Z") return null;
        if (closing[1][0] !== "L") return null;
        if (closing[0][0] !== "L" && closing[0][0] !== "C") return null;
    } else if (cmds[cmds.length - 1][0] === "Z") {
        return null;
    }
    if (body.length !== expectedAnchors || body.length < 2) return null;
    var segType = body[1][0];
    if (segType !== "L" && segType !== "C") return null;
    for(var i1 = 2; i1 < body.length; i1++){
        if (body[i1][0] !== segType) return null;
    }
    var anchors = body.map(function(c) {
        return c[0] === "C" ? [
            Number(c[5]),
            Number(c[6])
        ] : [
            Number(c[1]),
            Number(c[2])
        ];
    });
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    try {
        for(var _iterator = anchors[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var a = _step.value;
            if (!isFinite(a[0]) || !isFinite(a[1])) return null;
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return {
        body: body,
        closing: closing,
        segType: segType,
        anchors: anchors
    };
}
function lerpPt(a, b, t) {
    return [
        a[0] + (b[0] - a[0]) * t,
        a[1] + (b[1] - a[1]) * t
    ];
}
function splitCubic(s, cmd, t) {
    var p0 = s;
    var p1 = [
        cmd[1],
        cmd[2]
    ];
    var p2 = [
        cmd[3],
        cmd[4]
    ];
    var p3 = [
        cmd[5],
        cmd[6]
    ];
    var p01 = lerpPt(p0, p1, t);
    var p12 = lerpPt(p1, p2, t);
    var p23 = lerpPt(p2, p3, t);
    var p012 = lerpPt(p01, p12, t);
    var p123 = lerpPt(p12, p23, t);
    var mid = lerpPt(p012, p123, t);
    return {
        first: [
            "C",
            p01[0],
            p01[1],
            p012[0],
            p012[1],
            mid[0],
            mid[1]
        ],
        second: [
            "C",
            p123[0],
            p123[1],
            p23[0],
            p23[1],
            p3[0],
            p3[1]
        ],
        mid: mid
    };
}
function splitLine(s, cmd, t) {
    var e = [
        cmd[1],
        cmd[2]
    ];
    var mid = lerpPt(s, e, t);
    return {
        first: [
            "L",
            mid[0],
            mid[1]
        ],
        second: [
            "L",
            e[0],
            e[1]
        ],
        mid: mid
    };
}
function expandPath(analysis, ownIdx) {
    var body = analysis.body, closing = analysis.closing, segType = analysis.segType, anchors = analysis.anchors;
    var n = anchors.length;
    var m = ownIdx.length;
    var degen = function degen(p) {
        return segType === "C" ? [
            "C",
            p[0],
            p[1],
            p[0],
            p[1],
            p[0],
            p[1]
        ] : [
            "L",
            p[0],
            p[1]
        ];
    };
    var out = [
        [
            "M",
            anchors[0][0],
            anchors[0][1]
        ]
    ];
    var firstOwn = 0;
    while(firstOwn < m && ownIdx[firstOwn] !== 0)firstOwn++;
    for(var q = 1; q <= firstOwn; q++)out.push(degen(anchors[0]));
    var entry = firstOwn + 1;
    for(var s = 0; s < n - 1; s++){
        var interior = 0;
        while(entry + interior < m && ownIdx[entry + interior] === -1)interior++;
        var cmd = body[s + 1];
        if (!interior) {
            out.push(cmd.slice());
        } else {
            var totalParts = interior + 1;
            var start = anchors[s];
            var rest = cmd;
            for(var q1 = 0; q1 < interior; q1++){
                var t = 1 / (totalParts - q1);
                var sp = segType === "C" ? splitCubic(start, rest, t) : splitLine(start, rest, t);
                out.push(sp.first);
                start = sp.mid;
                rest = sp.second;
            }
            out.push(rest);
        }
        entry += interior + 1;
    }
    while(entry < m){
        out.push(degen(anchors[n - 1]));
        entry++;
    }
    if (closing) closing.forEach(function(c) {
        return out.push(c.slice());
    });
    return out;
}
function reconcilePathPair(fromD, toD, entries, oldN, newN, isArea) {
    var fromAnalysis = analyzeSeriesPath(fromD, oldN, isArea);
    if (!fromAnalysis) return null;
    var toAnalysis = analyzeSeriesPath(toD, newN, isArea);
    if (!toAnalysis) return null;
    if (fromAnalysis.segType !== toAnalysis.segType) return null;
    if ((fromAnalysis.closing || toAnalysis.closing) && (!fromAnalysis.closing || !toAnalysis.closing || fromAnalysis.closing[0][0] !== toAnalysis.closing[0][0])) {
        return null;
    }
    var fromOwn = entries.map(function(e) {
        return e.oldJ;
    });
    var toOwn = entries.map(function(e) {
        return e.newJ;
    });
    return {
        from: arrayToPath(expandPath(fromAnalysis, fromOwn)),
        toInterp: arrayToPath(expandPath(toAnalysis, toOwn))
    };
}
function lengthTransitionEnabled(w) {
    var _a;
    var anim = w.config.chart.animations;
    if (!anim || anim.enabled === false) return false;
    if (!anim.dynamicAnimation || anim.dynamicAnimation.enabled === false) {
        return false;
    }
    var largeThreshold = (_a = anim.largeDatasetThreshold) != null ? _a : 0;
    if (largeThreshold > 0 && w.globals.dataPoints > largeThreshold) return false;
    return !!(Environment.isBrowser() && w.globals.dataChanged && w.globals.shouldAnimate);
}
function datumKey(w, realIndex, j) {
    var _a, _b, _c, _d;
    if ((_a = w.axisFlags) == null ? void 0 : _a.isXNumeric) {
        var sx = (_c = (_b = w.seriesData) == null ? void 0 : _b.seriesX) == null ? void 0 : _c[realIndex];
        if (sx && sx.length && sx[j] != null) return "x:" + sx[j];
    }
    var lbl = (_d = w.globals.labels) == null ? void 0 : _d[j];
    if (lbl != null && String(lbl) !== "") {
        return "c:" + (Array.isArray(lbl) ? lbl.join(" ") : String(lbl));
    }
    return "j:" + j;
}
function frameDatumKey(frame, realIndex, j) {
    var _a, _b;
    if (frame.isXNumeric) {
        var sx = (_a = frame.seriesX) == null ? void 0 : _a[realIndex];
        if (sx && sx.length && sx[j] != null) return "x:" + sx[j];
    }
    var lbl = (_b = frame.labels) == null ? void 0 : _b[j];
    if (lbl != null && String(lbl) !== "") {
        return "c:" + (Array.isArray(lbl) ? lbl.join(" ") : String(lbl));
    }
    return "j:" + j;
}
function joinKeys(oldKeys, newKeys) {
    var oldIndex = /* @__PURE__ */ new Map();
    oldKeys.forEach(function(k, i) {
        if (!oldIndex.has(k)) oldIndex.set(k, i);
    });
    var toOld = new Array(newKeys.length);
    var usedOld = /* @__PURE__ */ new Set();
    var prev = -1;
    var ordered = true;
    var identity = oldKeys.length === newKeys.length;
    newKeys.forEach(function(k, i) {
        var oi = oldIndex.has(k) && !usedOld.has(oldIndex.get(k)) ? oldIndex.get(k) : -1;
        toOld[i] = oi;
        if (oi !== -1) {
            usedOld.add(oi);
            if (oi < prev) ordered = false;
            prev = oi;
        }
        if (oi !== i) identity = false;
    });
    var exits = [];
    for(var i = 0; i < oldKeys.length; i++){
        if (!usedOld.has(i)) exits.push(i);
    }
    return {
        toOld: toOld,
        exits: exits,
        ordered: ordered,
        changed: !identity
    };
}
function uniquifyKeys(keys) {
    var seen = /* @__PURE__ */ new Map();
    return keys.map(function(k) {
        var count = seen.get(k) || 0;
        seen.set(k, count + 1);
        return count === 0 ? k : "".concat(k, "#").concat(count);
    });
}
function seriesJoin(w, realIndex) {
    var includeIdentity = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false, allowReorder = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
    var _a, _b;
    if (!lengthTransitionEnabled(w)) return null;
    var frame = w.globals.prevStreamFrame;
    if (!frame) return null;
    var oldY = (_a = frame.seriesY) == null ? void 0 : _a[realIndex];
    var newY = (_b = w.seriesData.series) == null ? void 0 : _b[realIndex];
    if (!Array.isArray(oldY) || !Array.isArray(newY)) return null;
    if (!oldY.length || !newY.length) return null;
    var oldKeys = uniquifyKeys(oldY.map(function(_, j) {
        return frameDatumKey(frame, realIndex, j);
    }));
    var newKeys = uniquifyKeys(newY.map(function(_, j) {
        return datumKey(w, realIndex, j);
    }));
    var join = joinKeys(oldKeys, newKeys);
    if (!join.ordered && !allowReorder) return null;
    if (!join.changed && !includeIdentity) return null;
    return {
        join: join,
        oldKeys: oldKeys,
        newKeys: newKeys
    };
}
function morphEasing(w) {
    var _a, _b;
    var anim = w.config.chart.animations;
    return resolveEasing((_b = (_a = anim.dynamicAnimation) == null ? void 0 : _a.easing) != null ? _b : anim.easing);
}
function rafTween(w, duration, ease, onFrame, onDone) {
    var startAt = performance.now();
    var step = function step1(now) {
        if (w.globals.isDestroyed) return;
        var raw = Math.max(0, Math.min(1, (now - startAt) / duration));
        onFrame(ease(raw), raw);
        if (raw < 1) {
            BrowserAPIs.requestAnimationFrame(step);
        } else if (onDone) {
            onDone();
        }
    };
    BrowserAPIs.requestAnimationFrame(step);
}
function tweenSeriesMarkers(w, param) {
    var elPointsMain = param.elPointsMain, realIndex = param.realIndex, speed = param.speed;
    var _a, _b, _c, _d;
    if (!(elPointsMain == null ? void 0 : elPointsMain.node)) return false;
    var sj = seriesJoin(w, realIndex, true);
    if (!sj) return false;
    var frame = w.globals.prevStreamFrame;
    if (!frame) return false;
    var oldXP = (_a = frame.xPixels) == null ? void 0 : _a[realIndex];
    var oldYP = (_b = frame.yPixels) == null ? void 0 : _b[realIndex];
    if (!oldXP || !oldYP) return false;
    var markers = elPointsMain.node.querySelectorAll(".apexcharts-marker");
    if (!markers.length) return false;
    var newXP = ((_c = w.globals.seriesXvalues) == null ? void 0 : _c[realIndex]) || [];
    var newYP = ((_d = w.globals.seriesYvalues) == null ? void 0 : _d[realIndex]) || [];
    var ease = morphEasing(w);
    var duration = Math.max(1, speed || 1);
    elPointsMain.node.classList.remove("apexcharts-element-hidden");
    markers.forEach(function(node) {
        var _a2, _b2, _c2, _d2, _e, _f, _g, _h, _i;
        var j = parseInt((_b2 = (_a2 = node.getAttribute("j")) != null ? _a2 : node.getAttribute("rel")) != null ? _b2 : "", 10);
        if (!isFinite(j) || j < 0 || j >= sj.join.toOld.length) return;
        var oldJ = sj.join.toOld[j];
        var to = newXP[j] != null && newYP[j] != null ? [
            newXP[j],
            newYP[j]
        ] : null;
        if (oldJ === -1 || !to) {
            var style = /** @type {any} */ node.style;
            style.opacity = "0";
            rafTween(w, duration, ease, function(eased) {
                style.opacity = String(eased);
            }, function() {
                style.opacity = "";
            });
            return;
        }
        var dx = ((_c2 = oldXP[oldJ]) != null ? _c2 : NaN) - to[0];
        var dy = ((_d2 = oldYP[oldJ]) != null ? _d2 : NaN) - to[1];
        if (!isFinite(dx) || !isFinite(dy)) return;
        var rFrom = (_g = (_f = (_e = frame.rPixels) == null ? void 0 : _e[realIndex]) == null ? void 0 : _f[oldJ]) != null ? _g : NaN;
        var rTo = parseFloat((_i = (_h = node.getAttribute("r")) != null ? _h : node.getAttribute("default-marker-size")) != null ? _i : "");
        var scales = isFinite(rFrom) && isFinite(rTo) && rTo > 0 && Math.abs(rFrom - rTo) > 0.25;
        var moves = Math.abs(dx) >= 0.5 || Math.abs(dy) >= 0.5;
        if (!moves && !scales) return;
        var apply = function apply(eased) {
            var offX = dx * (1 - eased);
            var offY = dy * (1 - eased);
            if (scales) {
                var s = (rFrom + (rTo - rFrom) * eased) / rTo;
                node.setAttribute("transform", "translate(".concat(offX + to[0] * (1 - s), ", ").concat(offY + to[1] * (1 - s), ") scale(").concat(s, ")"));
            } else {
                node.setAttribute("transform", "translate(".concat(offX, ", ").concat(offY, ")"));
            }
        };
        apply(0);
        rafTween(w, duration, ease, apply, function() {
            node.removeAttribute("transform");
        });
    });
    return true;
}
function reconcileSeriesPaths(w, param) {
    var type = param.type, realIndex = param.realIndex, pathFromLine = param.pathFromLine, pathFromArea = param.pathFromArea, linePaths = param.linePaths, areaPaths = param.areaPaths;
    var _a, _b;
    var sj = seriesJoin(w, realIndex);
    if (!sj) return null;
    var join = sj.join, oldKeys = sj.oldKeys, newKeys = sj.newKeys;
    var frame = w.globals.prevStreamFrame;
    if (!frame) return null;
    var oldY = (_a = frame.seriesY) == null ? void 0 : _a[realIndex];
    var newY = (_b = w.seriesData.series) == null ? void 0 : _b[realIndex];
    if (oldY.length < 2 || newY.length < 2) return null;
    if (oldY.some(function(v) {
        return v === null;
    }) || newY.some(function(v) {
        return v === null;
    })) return null;
    var entries = buildUnionEntries(join, oldKeys.length);
    var out = {};
    if (Array.isArray(linePaths) && linePaths.length === 1 && pathFromLine) {
        out.line = reconcilePathPair(pathFromLine, linePaths[0], entries, oldKeys.length, newKeys.length, false);
    }
    if (type === "area" && Array.isArray(areaPaths) && areaPaths.length === 1 && pathFromArea) {
        out.area = reconcilePathPair(pathFromArea, areaPaths[0], entries, oldKeys.length, newKeys.length, true);
    }
    if (!out.line && !out.area) return null;
    return out;
}
function firstMove(d) {
    var m = /^M\s*([+-]?[\d.eE]+)[\s,]+([+-]?[\d.eE]+)/.exec(d || "");
    if (!m) return null;
    var x = parseFloat(m[1]);
    var y = parseFloat(m[2]);
    return isFinite(x) && isFinite(y) ? {
        x: x,
        y: y
    } : null;
}
function renderBarExitGhosts(param) {
    var w = param.w, elSeries = param.elSeries, record = param.record, newKeys = param.newKeys, isHorizontal = param.isHorizontal, speed = param.speed;
    var _a;
    if (!lengthTransitionEnabled(w)) return;
    if (!record || !Array.isArray(record.paths) || !(elSeries == null ? void 0 : elSeries.node)) return;
    var newKeySet = new Set(newKeys);
    var exits = record.paths.filter(function(p) {
        return p && p.d && p.key != null && !newKeySet.has(p.key);
    });
    if (!exits.length) return;
    var graphics = new Graphics(w);
    var fallbackFill = (_a = w.globals.colors) == null ? void 0 : _a[parseInt(String(record.realIndex), 10)];
    exits.forEach(function(p) {
        var fill = p.fill || fallbackFill || "#c8c8c8";
        if (String(fill).indexOf("url(") === 0) fill = fallbackFill || "#c8c8c8";
        var ghost = graphics.drawPath({
            d: p.d,
            stroke: "none",
            strokeWidth: 0,
            fill: fill,
            fillOpacity: 1,
            classes: "apexcharts-bar-ghost"
        });
        var node = ghost.node;
        node.setAttribute("pointer-events", "none");
        ghost.attr("clip-path", "url(#gridRectBarMask".concat(w.globals.cuid, ")"));
        elSeries.node.insertBefore(node, elSeries.node.firstChild);
        var start = firstMove(p.d);
        var origin = isHorizontal ? "left center" : "center bottom";
        try {
            var bb = node.getBBox();
            if (start && bb) {
                if (isHorizontal) {
                    origin = Math.abs(start.x - bb.x) <= Math.abs(start.x - (bb.x + bb.width)) ? "left center" : "right center";
                } else {
                    origin = Math.abs(start.y - (bb.y + bb.height)) <= Math.abs(start.y - bb.y) ? "center bottom" : "center top";
                }
            }
        } catch (_) {}
        var style = node.style;
        style.transformBox = "fill-box";
        style.transformOrigin = origin;
        var duration = Math.max(1, speed || 1);
        var startAt = performance.now();
        var step = function step1(now) {
            if (w.globals.isDestroyed || !node.parentNode) return;
            var t = Math.max(0, Math.min(1, (now - startAt) / duration));
            var eased = 1 - Math.pow(1 - t, 3);
            var scale = 1 - eased;
            style.transform = isHorizontal ? "scaleX(".concat(scale, ")") : "scaleY(".concat(scale, ")");
            style.opacity = String(1 - eased);
            if (t < 1) {
                BrowserAPIs.requestAnimationFrame(step);
            } else {
                node.parentNode.removeChild(node);
            }
        };
        BrowserAPIs.requestAnimationFrame(step);
    });
}
function grabLabels(root, sel, posAttr) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(root.querySelectorAll(sel)).map(function(el) {
        var _a, _b, _c;
        return {
            // `text` is the matching KEY (textContent, which doubles tspan + title
            // but does so consistently on both sides); `display` is the visible
            // string, used when rendering an exit ghost.
            text: el.textContent || "",
            display: (_c = (_b = (_a = el.querySelector("tspan")) == null ? void 0 : _a.textContent) != null ? _b : el.textContent) != null ? _c : "",
            pos: parseFloat(el.getAttribute(posAttr) || ""),
            transform: el.getAttribute("transform")
        };
    });
}
function grabLines(root, sel, posAttr) {
    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(root.querySelectorAll(sel)).map(function(el) {
        return parseFloat(el.getAttribute(posAttr) || "");
    });
}
var NO_GHOST = ":not(.apexcharts-tick-ghost)";
var X_LABELS_SEL = ".apexcharts-xaxis-texts-g text:not(.apexcharts-xaxis-group-label)".concat(NO_GHOST);
var Y_LABELS_SEL = ".apexcharts-yaxis-texts-g text".concat(NO_GHOST);
var V_GRID_SEL = ".apexcharts-gridlines-vertical line".concat(NO_GHOST);
var H_GRID_SEL = ".apexcharts-gridlines-horizontal line".concat(NO_GHOST);
function currentXScale(w) {
    var _a, _b;
    var gl = w.globals;
    if (!((_a = w.axisFlags) == null ? void 0 : _a.isXNumeric) || gl.isBarHorizontal) return null;
    if ((_b = w.config.xaxis) == null ? void 0 : _b.reversed) return null;
    var min = gl.minX;
    var max = gl.maxX;
    var width = w.layout.gridWidth;
    if (!isFinite(min) || !isFinite(max) || !(max > min) || !(width > 0)) {
        return null;
    }
    return {
        min: min,
        max: max,
        width: width
    };
}
function currentYAnchors(w, labels) {
    var _Math, _Math1;
    var _a;
    var gl = w.globals;
    if (gl.isBarHorizontal) return null;
    if (!Array.isArray(w.config.yaxis) || w.config.yaxis.length !== 1) return null;
    if ((_a = w.config.yaxis[0]) == null ? void 0 : _a.logarithmic) return null;
    var min = gl.minY;
    var max = gl.maxY;
    if (!isFinite(min) || !isFinite(max) || !(max > min)) return null;
    var ps = labels.map(function(l) {
        return l.pos;
    }).filter(function(p) {
        return isFinite(p);
    });
    if (ps.length < 2) return null;
    return {
        min: min,
        max: max,
        pLo: (_Math = Math).max.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ps)),
        pHi: (_Math1 = Math).min.apply(_Math1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ps))
    };
}
function composeXMap(o, n) {
    if (!o || !n) return null;
    var os = o.max - o.min;
    var ns = n.max - n.min;
    if (!(os > 0) || !(ns > 0) || !(o.width > 0) || !(n.width > 0)) return null;
    return {
        toNew: function toNew(p) {
            return (o.min + p / o.width * os - n.min) / ns * n.width;
        },
        toOld: function toOld(p) {
            return (n.min + p / n.width * ns - o.min) / os * o.width;
        }
    };
}
function composeYMap(o, n) {
    if (!o || !n) return null;
    var oSpanP = o.pHi - o.pLo;
    var nSpanP = n.pHi - n.pLo;
    var oSpanV = o.max - o.min;
    var nSpanV = n.max - n.min;
    if (!oSpanP || !nSpanP || !(oSpanV > 0) || !(nSpanV > 0)) return null;
    var oldVal = function oldVal(p) {
        return o.min + (p - o.pLo) / oSpanP * oSpanV;
    };
    var newVal = function newVal(p) {
        return n.min + (p - n.pLo) / nSpanP * nSpanV;
    };
    return {
        toNew: function toNew(p) {
            return n.pLo + (oldVal(p) - n.min) / nSpanV * nSpanP;
        },
        toOld: function toOld(p) {
            return o.pLo + (newVal(p) - o.min) / oSpanV * oSpanP;
        }
    };
}
function captureAxisChrome(w) {
    var gl = w.globals;
    gl.prevChromeFrame = null;
    if (!gl.axisCharts || !Environment.isBrowser()) return;
    var root = w.dom.baseEl;
    if (!Utils$1.elementExists(root)) return;
    try {
        var yLabels = grabLabels(root, Y_LABELS_SEL, "y");
        gl.prevChromeFrame = {
            xLabels: grabLabels(root, X_LABELS_SEL, "x"),
            yLabels: yLabels,
            vGrid: grabLines(root, V_GRID_SEL, "x1"),
            hGrid: grabLines(root, H_GRID_SEL, "y1"),
            // Value scales of the outgoing render, so ticks whose TEXT has no
            // counterpart (e.g. a zoom across datetime granularities) can still be
            // re-projected: new ticks slide in from where their value sat, old
            // ticks ghost out to where their value lands.
            xScale: currentXScale(w),
            yAnchors: currentYAnchors(w, yLabels)
        };
    } catch (_) {
        gl.prevChromeFrame = null;
    }
}
function fadeIn$1(w, node, duration, ease) {
    var style = /** @type {any} */ node.style;
    style.opacity = "0";
    rafTween(w, duration, ease, function(eased) {
        style.opacity = String(eased);
    }, function() {
        style.opacity = "";
    });
}
function tweenPos(w, node, attrs, from, to, duration, ease) {
    attrs.forEach(function(a) {
        return node.setAttribute(a, String(from));
    });
    rafTween(w, duration, ease, function(eased) {
        var v = String(from + (to - from) * eased);
        attrs.forEach(function(a) {
            return node.setAttribute(a, v);
        });
    }, function() {
        attrs.forEach(function(a) {
            return node.setAttribute(a, String(to));
        });
    });
}
function spawnGhost(w, param) {
    var template = param.template, display = param.display, attrs = param.attrs, from = param.from, to = param.to, duration = param.duration, ease = param.ease;
    var parent = template.parentNode;
    if (!parent) return;
    var ghost = /** @type {Element} */ template.cloneNode(true);
    ghost.classList.add("apexcharts-tick-ghost");
    ghost.setAttribute("pointer-events", "none");
    ghost.removeAttribute("id");
    if (display !== void 0) {
        var tspan = ghost.querySelector("tspan");
        if (tspan) tspan.textContent = display;
        else ghost.textContent = display;
        var title = ghost.querySelector("title");
        if (title) title.textContent = display;
    }
    attrs.forEach(function(a) {
        return ghost.setAttribute(a, String(from));
    });
    var style = /** @type {any} */ ghost.style;
    style.opacity = "1";
    parent.appendChild(ghost);
    rafTween(w, duration, ease, function(eased) {
        var v = String(from + (to - from) * eased);
        attrs.forEach(function(a) {
            return ghost.setAttribute(a, v);
        });
        style.opacity = String(1 - eased);
    }, function() {
        if (ghost.parentNode) ghost.parentNode.removeChild(ghost);
    });
}
var MAX_GHOSTS = 20;
function transitionAxis(w, param) {
    var newLabels = param.newLabels, oldLabels = param.oldLabels, posAttr = param.posAttr, newLines = param.newLines, oldLines = param.oldLines, lineAttrs = param.lineAttrs, duration = param.duration, ease = param.ease, project = param.project;
    var _Math, _Math1;
    var oldByText = /* @__PURE__ */ new Map();
    oldLabels.forEach(function(l, i) {
        if (!oldByText.has(l.text)) oldByText.set(l.text, __spreadProps(__spreadValues({}, l), {
            i: i
        }));
    });
    var matchedOld = /* @__PURE__ */ new Set();
    var newLinesAligned = newLines.length === newLabels.length;
    var oldLinesAligned = oldLines.length === oldLabels.length;
    var spanPs = oldLabels.map(function(l) {
        return l.pos;
    }).concat(newLabels.map(function(l) {
        return parseFloat(l.getAttribute(posAttr) || "");
    })).filter(function(p) {
        return isFinite(p);
    });
    var spanLo = (_Math = Math).min.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(spanPs));
    var spanHi = (_Math1 = Math).max.apply(_Math1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(spanPs));
    var margin = Math.max(40, (spanHi - spanLo) * 0.25);
    var clamp = function clamp(p) {
        return Math.max(spanLo - margin, Math.min(spanHi + margin, p));
    };
    var tweenPairedLine = function tweenPairedLine(line, old) {
        if (!line) return;
        var lineTo = parseFloat(line.getAttribute(lineAttrs[0]) || "");
        var lineFrom = oldLines[old.i];
        if (isFinite(lineTo) && isFinite(lineFrom)) {
            tweenPos(w, line, lineAttrs, lineFrom, lineTo, duration, ease);
        }
    };
    newLabels.forEach(function(label, i) {
        var to = parseFloat(label.getAttribute(posAttr) || "");
        var old = oldByText.get(label.textContent || "");
        var line = newLinesAligned ? newLines[i] : null;
        if (old) matchedOld.add(old.i);
        var labelTransform = label.getAttribute("transform");
        if (!old || !isFinite(old.pos)) {
            if (!old) {
                if (project && isFinite(to) && !labelTransform) {
                    var from = isFinite(project.toOld(to)) ? clamp(project.toOld(to)) : NaN;
                    if (isFinite(from) && Math.abs(from - to) > 0.5) {
                        tweenPos(w, label, [
                            posAttr
                        ], from, to, duration, ease);
                        if (line) tweenPos(w, line, lineAttrs, from, to, duration, ease);
                    }
                }
                fadeIn$1(w, label, duration, ease);
                if (line) fadeIn$1(w, line, duration, ease);
            }
            return;
        }
        if (!isFinite(to) || Math.abs(old.pos - to) < 0.5) return;
        if (labelTransform || old.transform) {
            var delta = old.pos - to;
            if (isFinite(delta)) {
                var base = labelTransform || "";
                rafTween(w, duration, ease, function(eased) {
                    var v = delta * (1 - eased);
                    var t = posAttr === "x" ? "translate(".concat(v, " 0)") : "translate(0 ".concat(v, ")");
                    label.setAttribute("transform", "".concat(t, " ").concat(base).trim());
                }, function() {
                    if (base) label.setAttribute("transform", base);
                    else label.removeAttribute("transform");
                });
            }
            tweenPairedLine(line, old);
            return;
        }
        tweenPos(w, label, [
            posAttr
        ], old.pos, to, duration, ease);
        tweenPairedLine(line, old);
    });
    if (!project || !newLabels.length) return;
    var ghosts = 0;
    oldLabels.forEach(function(old, i) {
        if (matchedOld.has(i)) return;
        if (!isFinite(old.pos) || old.transform) return;
        if (ghosts >= MAX_GHOSTS) return;
        var rawTo = project.toNew(old.pos);
        if (!isFinite(rawTo) || Math.abs(rawTo - old.pos) < 0.5) return;
        ghosts++;
        spawnGhost(w, {
            template: newLabels[0],
            display: old.display,
            attrs: [
                posAttr
            ],
            from: old.pos,
            to: clamp(rawTo),
            duration: duration,
            ease: ease
        });
        if (oldLinesAligned && newLines.length && isFinite(oldLines[i])) {
            spawnGhost(w, {
                template: newLines[0],
                attrs: lineAttrs,
                from: oldLines[i],
                to: clamp(project.toNew(oldLines[i])),
                duration: duration,
                ease: ease
            });
        }
    });
}
function applyAxisTransition(w) {
    var gl = w.globals;
    var chrome = gl.prevChromeFrame;
    gl.prevChromeFrame = null;
    if (!chrome || !gl.axisCharts || !Environment.isBrowser()) return;
    if (!lengthTransitionEnabled(w)) return;
    var anyMotion = (w.seriesData.series || []).some(function(_, i) {
        return seriesJoin(w, i, true, true) !== null;
    });
    if (!anyMotion) return;
    var root = w.dom.baseEl;
    if (!Utils$1.elementExists(root)) return;
    var duration = Math.max(1, w.config.chart.animations.dynamicAnimation.speed || 1);
    var ease = morphEasing(w);
    try {
        var newYLabels = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(root.querySelectorAll(Y_LABELS_SEL));
        var projX = composeXMap(chrome.xScale, currentXScale(w));
        var projY = composeYMap(chrome.yAnchors, currentYAnchors(w, newYLabels.map(function(el) {
            return {
                pos: parseFloat(el.getAttribute("y") || "")
            };
        })));
        transitionAxis(w, {
            newLabels: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(root.querySelectorAll(X_LABELS_SEL)),
            oldLabels: chrome.xLabels,
            posAttr: "x",
            newLines: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(root.querySelectorAll(V_GRID_SEL)),
            oldLines: chrome.vGrid,
            lineAttrs: [
                "x1",
                "x2"
            ],
            duration: duration,
            ease: ease,
            project: projX
        });
        transitionAxis(w, {
            newLabels: newYLabels,
            oldLabels: chrome.yLabels,
            posAttr: "y",
            newLines: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(root.querySelectorAll(H_GRID_SEL)),
            oldLines: chrome.hGrid,
            lineAttrs: [
                "y1",
                "y2"
            ],
            duration: duration,
            ease: ease,
            project: projY
        });
    } catch (_) {}
}
var DL_GROUP_SEL = ".apexcharts-data-labels[data\\:dlKey]";
var DL_TEXT_SEL = ".apexcharts-datalabel";
function dataLabelMotionEnabled(w) {
    var _a, _b;
    var dl = w.config.dataLabels;
    return !!(((_a = dl == null ? void 0 : dl.animate) == null ? void 0 : _a.enabled) || ((_b = dl == null ? void 0 : dl.countUp) == null ? void 0 : _b.enabled));
}
function decimalsOf(n) {
    if (!isFinite(n)) return 0;
    var s = String(Math.abs(n));
    var e = s.indexOf("e");
    if (e !== -1) {
        var mantissa = s.slice(0, e);
        var exp = parseInt(s.slice(e + 1), 10);
        var dot2 = mantissa.indexOf(".");
        var mantissaDec = dot2 === -1 ? 0 : mantissa.length - dot2 - 1;
        return Math.min(6, Math.max(0, mantissaDec - exp));
    }
    var dot = s.indexOf(".");
    return dot === -1 ? 0 : Math.min(6, s.length - dot - 1);
}
function writeLabel(textEl, s) {
    var tspan = textEl.querySelector("tspan");
    if (tspan) tspan.textContent = s;
    else textEl.textContent = s;
}
function captureDataLabels(w) {
    var gl = w.globals;
    gl.prevDataLabels = null;
    if (!gl.axisCharts || !Environment.isBrowser()) return;
    if (!dataLabelMotionEnabled(w)) return;
    var root = w.dom.baseEl;
    if (!Utils$1.elementExists(root)) return;
    try {
        var map = /* @__PURE__ */ new Map();
        root.querySelectorAll(DL_GROUP_SEL).forEach(function(group) {
            var key = group.getAttribute("data:dlKey");
            if (!key) return;
            var textEl = group.querySelector(DL_TEXT_SEL);
            if (!textEl) return;
            map.set(key, {
                cx: parseFloat(textEl.getAttribute("cx") || ""),
                cy: parseFloat(textEl.getAttribute("cy") || ""),
                val: parseFloat(group.getAttribute("data:dlVal") || "")
            });
        });
        gl.prevDataLabels = map.size ? map : null;
    } catch (_) {
        gl.prevDataLabels = null;
    }
}
function fadeIn(w, node, duration, ease) {
    var style = /** @type {any} */ node.style;
    style.opacity = "0";
    rafTween(w, duration, ease, function(eased) {
        style.opacity = String(eased);
    }, function() {
        style.opacity = "";
    });
}
function applyDataLabelTransition(w) {
    var _a, _b;
    var gl = w.globals;
    var prev = gl.prevDataLabels;
    gl.prevDataLabels = null;
    if (!prev || !gl.axisCharts || !Environment.isBrowser()) return;
    if (!dataLabelMotionEnabled(w)) return;
    if (!lengthTransitionEnabled(w)) return;
    var root = w.dom.baseEl;
    if (!Utils$1.elementExists(root)) return;
    var dl = w.config.dataLabels;
    var ride = !!((_a = dl.animate) == null ? void 0 : _a.enabled);
    var countUp = !!((_b = dl.countUp) == null ? void 0 : _b.enabled);
    var formatter = dl.formatter;
    var duration = Math.max(1, w.config.chart.animations.dynamicAnimation.speed || 1);
    var ease = morphEasing(w);
    try {
        root.querySelectorAll(DL_GROUP_SEL).forEach(function(group) {
            var key = group.getAttribute("data:dlKey");
            if (!key) return;
            var textEl = group.querySelector(DL_TEXT_SEL);
            if (!textEl) return;
            var old = prev.get(key);
            if (ride) {
                if (old && isFinite(old.cx) && isFinite(old.cy)) {
                    var newCx = parseFloat(textEl.getAttribute("cx") || "");
                    var newCy = parseFloat(textEl.getAttribute("cy") || "");
                    var dx = old.cx - newCx;
                    var dy = old.cy - newCy;
                    if (isFinite(dx) && isFinite(dy) && Math.abs(dx) + Math.abs(dy) > 0.5) {
                        var base = group.getAttribute("transform") || "";
                        rafTween(w, duration, ease, function(eased) {
                            var t = 1 - eased;
                            group.setAttribute("transform", "translate(".concat(dx * t, " ").concat(dy * t, ") ").concat(base).trim());
                        }, function() {
                            group.setAttribute("transform", base);
                        });
                    }
                } else if (!old) {
                    fadeIn(w, group, duration, ease);
                }
            }
            if (countUp && old && isFinite(old.val)) {
                var newVal = parseFloat(group.getAttribute("data:dlVal") || "");
                if (isFinite(newVal) && Math.abs(newVal - old.val) > 1e-9) {
                    var from = old.val;
                    var dec = Math.max(decimalsOf(from), decimalsOf(newVal));
                    var realIndex = parseInt(key, 10);
                    var j = parseInt(group.getAttribute("data:dlJ") || "", 10);
                    var fmtOpts = __spreadProps(__spreadValues({}, w), {
                        seriesIndex: realIndex,
                        dataPointIndex: isFinite(j) ? j : 0,
                        w: w
                    });
                    var format = function format(v) {
                        var rounded = Number(v.toFixed(dec));
                        var out = rounded;
                        if (typeof formatter === "function") {
                            try {
                                out = formatter(rounded, fmtOpts);
                            } catch (_) {
                                out = rounded;
                            }
                        }
                        return String(out);
                    };
                    rafTween(w, duration, ease, function(eased) {
                        writeLabel(textEl, format(from + (newVal - from) * eased));
                    }, function() {
                        writeLabel(textEl, format(newVal));
                    });
                }
            }
        });
    } catch (_) {}
}
var Series = /*#__PURE__*/ function() {
    "use strict";
    function Series(w) {
        var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref_toggleDataSeries = _ref.toggleDataSeries, toggleDataSeries = _ref_toggleDataSeries === void 0 ? void 0 : _ref_toggleDataSeries, _ref_revertDefaultAxisMinMax = _ref.revertDefaultAxisMinMax, revertDefaultAxisMinMax = _ref_revertDefaultAxisMinMax === void 0 ? void 0 : _ref_revertDefaultAxisMinMax, _ref_updateSeries = _ref.updateSeries, updateSeries = _ref_updateSeries === void 0 ? void 0 : _ref_updateSeries;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Series);
        this.w = w;
        this._toggleDataSeries = toggleDataSeries || null;
        this._revertDefaultAxisMinMax = revertDefaultAxisMinMax || null;
        this._updateSeries = updateSeries || null;
        this.legendInactiveClass = "legend-mouseover-inactive";
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Series, [
        {
            key: "clearSeriesCache",
            value: function clearSeriesCache() {
                var w = this.w;
                if (w.globals.cachedSelectors) {
                    delete w.globals.cachedSelectors.allSeriesEls;
                    delete w.globals.cachedSelectors.highlightSeriesEls;
                }
            }
        },
        {
            key: "getAllSeriesEls",
            value: function getAllSeriesEls() {
                var w = this.w;
                var cacheKey = "allSeriesEls";
                if (!w.globals.cachedSelectors[cacheKey]) {
                    w.globals.cachedSelectors[cacheKey] = /** @type {any} */ w.dom.baseEl.getElementsByClassName("apexcharts-series");
                }
                return w.globals.cachedSelectors[cacheKey];
            }
        },
        {
            /**
   * @param {string} seriesName
   */ key: "getSeriesByName",
            value: function getSeriesByName(seriesName) {
                return this.w.dom.baseEl.querySelector(".apexcharts-inner .apexcharts-series[seriesName='".concat(Utils$1.escapeString(seriesName), "']"));
            }
        },
        {
            /**
   * @param {string} seriesName
   */ key: "isSeriesHidden",
            value: function isSeriesHidden(seriesName) {
                var _a;
                var targetElement = this.getSeriesByName(seriesName);
                var el = /** @type {Element} */ targetElement;
                var realIndex = parseInt((_a = el.getAttribute("data:realIndex")) != null ? _a : "0", 10);
                var isHidden = el.classList.contains("apexcharts-series-collapsed");
                return {
                    isHidden: isHidden,
                    realIndex: realIndex
                };
            }
        },
        {
            /**
   * @param {any} elSeries
   * @param {number} index
   */ key: "addCollapsedClassToSeries",
            value: function addCollapsedClassToSeries(elSeries, index) {
                Series.addCollapsedClassToSeries(this.w, elSeries, index);
            }
        },
        {
            /**
   * @param {string} seriesName
   */ key: "toggleSeries",
            value: function toggleSeries(seriesName) {
                var _a;
                var isSeriesHidden = this.isSeriesHidden(seriesName);
                (_a = this._toggleDataSeries) == null ? void 0 : _a.call(this, isSeriesHidden.realIndex, isSeriesHidden.isHidden);
                return isSeriesHidden.isHidden;
            }
        },
        {
            /**
   * @param {string} seriesName
   */ key: "showSeries",
            value: function showSeries(seriesName) {
                var _a;
                var isSeriesHidden = this.isSeriesHidden(seriesName);
                if (isSeriesHidden.isHidden) {
                    (_a = this._toggleDataSeries) == null ? void 0 : _a.call(this, isSeriesHidden.realIndex, true);
                }
            }
        },
        {
            /**
   * @param {string} seriesName
   */ key: "hideSeries",
            value: function hideSeries(seriesName) {
                var _a;
                var isSeriesHidden = this.isSeriesHidden(seriesName);
                if (!isSeriesHidden.isHidden) {
                    (_a = this._toggleDataSeries) == null ? void 0 : _a.call(this, isSeriesHidden.realIndex, false);
                }
            }
        },
        {
            /**
   * Cheap pre-update reset for the data-replacement paths (updateSeries /
   * appendSeries). Clears the same bookkeeping resetSeries() clears (series
   * cache, previous paths, collapsed-series state) WITHOUT restoring
   * config.series from the initialSeries snapshot: the caller is about to
   * replace the series anyway (parseData assigns config.series = newSeries),
   * so materializing and cloning the snapshot per update is pure waste (two
   * O(n) deep clones per streaming tick at 50k points).
   */ key: "prepareDataUpdate",
            value: function prepareDataUpdate() {
                var w = this.w;
                this.clearSeriesCache();
                w.globals.previousPaths = [];
                w.globals.collapsedSeries = [];
                w.globals.ancillaryCollapsedSeries = [];
                w.globals.collapsedSeriesIndices = [];
                w.globals.ancillaryCollapsedSeriesIndices = [];
            }
        },
        {
            key: "resetSeries",
            value: function resetSeries() {
                var shouldUpdateChart = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true, shouldResetZoom = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true, shouldResetCollapsed = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
                var _a, _b;
                var w = this.w;
                this.clearSeriesCache();
                var series = Utils$1.clone(w.globals.initialSeries);
                w.globals.previousPaths = [];
                if (shouldResetCollapsed) {
                    w.globals.collapsedSeries = [];
                    w.globals.ancillaryCollapsedSeries = [];
                    w.globals.collapsedSeriesIndices = [];
                    w.globals.ancillaryCollapsedSeriesIndices = [];
                } else {
                    series = this.emptyCollapsedSeries(series);
                }
                w.config.series = series;
                if (shouldUpdateChart) {
                    if (shouldResetZoom) {
                        w.interact.zoomed = false;
                        (_a = this._revertDefaultAxisMinMax) == null ? void 0 : _a.call(this);
                    }
                    (_b = this._updateSeries) == null ? void 0 : _b.call(this, series, w.config.chart.animations.dynamicAnimation.enabled);
                }
            }
        },
        {
            /**
   * @param {any[]} series
   */ key: "emptyCollapsedSeries",
            value: function emptyCollapsedSeries(series) {
                var w = this.w;
                for(var i = 0; i < series.length; i++){
                    if (w.globals.collapsedSeriesIndices.indexOf(i) > -1) {
                        if (series[i] && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(series[i]) === "object") {
                            series[i].data = [];
                        } else {
                            series[i] = 0;
                        }
                    }
                }
                return series;
            }
        },
        {
            /**
   * Series display names for the CURRENT `w.config` (post-merge), derived the
   * same way the parser does: an object series' own `name`, else the matching
   * `labels` entry (non-axis / pie / unit), else a generated `series-N`.
   * @returns {string[]}
   */ key: "_deriveSeriesNames",
            value: function _deriveSeriesNames() {
                var w = this.w;
                var series = w.config.series || [];
                var labels = w.config.labels || [];
                return series.map(function(s, i) {
                    if (s && (typeof s === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(s)) === "object" && s.name != null) return String(s.name);
                    return labels[i] != null ? String(labels[i]) : "series-".concat(i + 1);
                });
            }
        },
        {
            /**
   * Re-apply legend-hidden (collapsed) series to a freshly-updated
   * `w.config.series`, matching BY CATEGORY NAME rather than index. This keeps a
   * hide alive across a data update that reorders or regroups categories - most
   * visibly a storyboard beat that supplies new series each scroll:
   *   - a category still present stays hidden (at its possibly-new index);
   *   - a category the update dropped/regrouped away is un-hidden (it no longer
   *     exists, so it must reappear as part of the new grouping).
   * Records without a stored name (older collapses) fall back to their index.
   */ key: "reconcileCollapsedByName",
            value: function reconcileCollapsedByName() {
                var w = this.w;
                var gl = w.globals;
                var newNames = this._deriveSeriesNames();
                var reconcile = function reconcile(records) {
                    var nextRecords = [];
                    var nextIndices = [];
                    records.forEach(function(rec) {
                        var j = rec && rec.name != null ? newNames.indexOf(rec.name) : rec.index;
                        if (j == null || j < 0 || j >= w.config.series.length) return;
                        var s = /** @type {any} */ w.config.series[j];
                        rec.index = j;
                        rec.data = gl.axisCharts ? s && s.data ? s.data.slice() : [] : s;
                        nextRecords.push(rec);
                        nextIndices.push(j);
                    });
                    return {
                        records: nextRecords,
                        indices: nextIndices
                    };
                };
                var main = reconcile(gl.collapsedSeries);
                gl.collapsedSeries = main.records;
                gl.collapsedSeriesIndices = main.indices;
                var anc = reconcile(gl.ancillaryCollapsedSeries);
                gl.ancillaryCollapsedSeries = anc.records;
                gl.ancillaryCollapsedSeriesIndices = anc.indices;
                gl.allSeriesCollapsed = gl.collapsedSeries.length + gl.ancillaryCollapsedSeries.length === w.config.series.length;
                this.emptyCollapsedSeries(w.config.series);
            }
        },
        {
            /**
   * @param {string} seriesName
   */ /**
   * Bridge SVG series-dim state to the canvas renderer: SVG opacity classes
   * (legend-mouseover-inactive) don't touch the painted canvas series layer, so
   * repaint it with a matching per-series opacity. No-op unless the canvas
   * renderer is active. The renderer is mirrored on globals by RendererController
   * (Series has no ctx handle).
   * @param {{active:number, opacity:number}|null} dim
   */ key: "canvasRestyle",
            value: function canvasRestyle(dim) {
                var r = this.w.globals.activeRenderer;
                if (r && r.kind === "canvas" && typeof r.restyle === "function") {
                    r.restyle(dim);
                }
            }
        },
        {
            /**
   * @param {string} seriesName
   */ key: "highlightSeries",
            value: function highlightSeries(seriesName) {
                var _a;
                var w = this.w;
                var targetElement = this.getSeriesByName(seriesName);
                var realIndex = parseInt((_a = targetElement == null ? void 0 : targetElement.getAttribute("data:realIndex")) != null ? _a : "", 10);
                var cacheKey = "highlightSeriesEls";
                var allSeriesEls = w.globals.cachedSelectors[cacheKey];
                if (!allSeriesEls) {
                    allSeriesEls = w.dom.baseEl.querySelectorAll(".apexcharts-series, .apexcharts-datalabels, .apexcharts-yaxis");
                    w.globals.cachedSelectors[cacheKey] = allSeriesEls;
                }
                var seriesEl = null;
                var dataLabelEl = null;
                var yaxisEl = null;
                if (w.globals.axisCharts || w.config.chart.type === "radialBar") {
                    if (w.globals.axisCharts) {
                        seriesEl = w.dom.baseEl.querySelector(".apexcharts-series[data\\:realIndex='".concat(realIndex, "']"));
                        dataLabelEl = w.dom.baseEl.querySelector(".apexcharts-datalabels[data\\:realIndex='".concat(realIndex, "']"));
                        var yaxisIndex = w.globals.seriesYAxisReverseMap[realIndex];
                        yaxisEl = w.dom.baseEl.querySelector(".apexcharts-yaxis[rel='".concat(yaxisIndex, "']"));
                    } else {
                        seriesEl = w.dom.baseEl.querySelector(".apexcharts-series[rel='".concat(realIndex + 1, "']"));
                    }
                } else {
                    seriesEl = w.dom.baseEl.querySelector(".apexcharts-series[rel='".concat(realIndex + 1, "'] path"));
                }
                for(var se = 0; se < allSeriesEls.length; se++){
                    var serEl = /** @type {Element} */ allSeriesEls[se];
                    serEl.classList.add(this.legendInactiveClass);
                }
                if (seriesEl) {
                    if (!w.globals.axisCharts) {
                        var parentEl = /** @type {Element} */ seriesEl.parentNode;
                        parentEl == null ? void 0 : parentEl.classList.remove(this.legendInactiveClass);
                    }
                    seriesEl.classList.remove(this.legendInactiveClass);
                    if (dataLabelEl !== null) {
                        dataLabelEl.classList.remove(this.legendInactiveClass);
                    }
                    if (yaxisEl !== null) {
                        yaxisEl.classList.remove(this.legendInactiveClass);
                    }
                } else {
                    for(var se1 = 0; se1 < allSeriesEls.length; se1++){
                        var serEl1 = /** @type {Element} */ allSeriesEls[se1];
                        serEl1.classList.remove(this.legendInactiveClass);
                    }
                }
                this.canvasRestyle(seriesEl && !Number.isNaN(realIndex) ? {
                    active: realIndex,
                    opacity: 0.2
                } : null);
            }
        },
        {
            /**
   * @param {Event} e
   * @param {any} targetElement
   */ key: "toggleSeriesOnHover",
            value: function toggleSeriesOnHover(e, targetElement) {
                var w = this.w;
                if (!targetElement) targetElement = e.target;
                var allSeriesEls = w.dom.baseEl.querySelectorAll(".apexcharts-series, .apexcharts-datalabels, .apexcharts-yaxis");
                if (e.type === "mousemove") {
                    var realIndex = parseInt(targetElement.getAttribute("rel"), 10) - 1;
                    this.highlightSeries(w.seriesData.seriesNames[realIndex]);
                } else if (e.type === "mouseout") {
                    for(var se = 0; se < allSeriesEls.length; se++){
                        allSeriesEls[se].classList.remove(this.legendInactiveClass);
                    }
                    this.canvasRestyle(null);
                }
            }
        },
        {
            /**
   * Dim every heatmap cell except those whose value falls inside the color
   * range at `rangeIndex`. Shared by the categorical legend (hover a legend
   * item) and the gradient legend (hover a band) — both supply an index into
   * `colorScale.ranges` plus an action, decoupling the highlight from any
   * particular DOM element / event shape.
   *
   * @param {number} rangeIndex index into `colorScale.ranges`
   * @param {'highlight'|'reset'} action
   */ key: "highlightRangeInSeries",
            value: function highlightRangeInSeries(rangeIndex, action) {
                var _this = this;
                var w = this.w;
                var allHeatMapElements = w.dom.baseEl.getElementsByClassName("apexcharts-heatmap-rect");
                var toggleAllInactive = function toggleAllInactive(op) {
                    for(var i = 0; i < allHeatMapElements.length; i++){
                        var classList = /** @type {any} */ allHeatMapElements[i].classList;
                        if (typeof classList[op] === "function") {
                            classList[op](_this.legendInactiveClass);
                        }
                    }
                };
                if (action === "reset") {
                    toggleAllInactive("remove");
                    return;
                }
                var ranges = w.config.plotOptions.heatmap.colorScale.ranges;
                var range = ranges && ranges[rangeIndex];
                if (!range) return;
                toggleAllInactive("add");
                for(var i = 0; i < allHeatMapElements.length; i++){
                    var val = Number(allHeatMapElements[i].getAttribute("val"));
                    if (val >= range.from && val <= range.to) {
                        allHeatMapElements[i].classList.remove(this.legendInactiveClass);
                    }
                }
            }
        },
        {
            /**
   * @param {string[]} chartTypes
   */ key: "getActiveConfigSeriesIndex",
            value: function getActiveConfigSeriesIndex() {
                var order = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "asc", chartTypes = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
                var w = this.w;
                var activeIndex = 0;
                if (w.config.series.length > 1) {
                    var activeSeriesIndex = w.config.series.map(function(s, index) {
                        var checkChartType = function checkChartType() {
                            if (w.globals.comboCharts) {
                                return chartTypes.length === 0 || chartTypes.length && chartTypes.indexOf(/** @type {Record<string,any>} */ w.config.series[index].type) > -1;
                            }
                            return true;
                        };
                        var hasData = /** @type {any} */ s.data && /** @type {any} */ s.data.length > 0 && w.globals.collapsedSeriesIndices.indexOf(index) === -1;
                        return hasData && checkChartType() ? index : -1;
                    });
                    for(var a = order === "asc" ? 0 : activeSeriesIndex.length - 1; order === "asc" ? a < activeSeriesIndex.length : a >= 0; order === "asc" ? a++ : a--){
                        if (activeSeriesIndex[a] !== -1) {
                            activeIndex = activeSeriesIndex[a];
                            break;
                        }
                    }
                }
                return activeIndex;
            }
        },
        {
            key: "getBarSeriesIndices",
            value: function getBarSeriesIndices() {
                var w = this.w;
                if (w.globals.comboCharts) {
                    return this.w.config.series.map(function(s, i) {
                        return s.type === "bar" || s.type === "column" ? i : -1;
                    }).filter(function(i) {
                        return i !== -1;
                    });
                }
                return this.w.config.series.map(function(s, i) {
                    return i;
                });
            }
        },
        {
            key: "getPreviousPaths",
            value: function getPreviousPaths() {
                var _a, _b, _c, _d;
                var w = this.w;
                captureStreamFrame(w);
                captureAxisChrome(w);
                captureDataLabels(w);
                if (!w.globals.axisCharts) {
                    w.globals.previousPaths = w.seriesData.series;
                    return;
                }
                if (!Utils$1.elementExists(w.dom.baseEl)) {
                    w.globals.previousPaths = [];
                    return;
                }
                w.globals.previousPaths = [];
                function pushPaths(seriesEls, i, type) {
                    var paths = seriesEls[i].childNodes;
                    var dArr = {
                        type: type,
                        paths: /** @type {any[]} */ [],
                        realIndex: seriesEls[i].getAttribute("data:realIndex")
                    };
                    for(var j = 0; j < paths.length; j++){
                        if (paths[j].hasAttribute("pathTo")) {
                            var d = paths[j].getAttribute("pathTo");
                            dArr.paths.push({
                                d: d,
                                key: paths[j].getAttribute("data:pathKey"),
                                fill: paths[j].getAttribute("fill")
                            });
                        }
                    }
                    w.globals.previousPaths.push(dArr);
                }
                var getPaths = function getPaths(chartType) {
                    return w.dom.baseEl.querySelectorAll(".apexcharts-".concat(chartType, "-series .apexcharts-series"));
                };
                var chartTypes = [
                    "line",
                    "area",
                    "bar",
                    "rangebar",
                    "rangeArea",
                    "candlestick",
                    "radar"
                ];
                chartTypes.forEach(function(type) {
                    var paths = getPaths(type);
                    for(var p = 0; p < paths.length; p++){
                        pushPaths(paths, p, type);
                    }
                });
                var heatTreeSeries = w.dom.baseEl.querySelectorAll(".apexcharts-".concat(w.config.chart.type, " .apexcharts-series"));
                if (heatTreeSeries.length > 0) {
                    var _loop = function(h) {
                        var _loop = function(i) {
                            var getAttr = function getAttr(x) {
                                return /** @type {Element} */ seriesEls[i].getAttribute(x);
                            };
                            var rect = {
                                x: parseFloat((_a = getAttr("x")) != null ? _a : "0"),
                                y: parseFloat((_b = getAttr("y")) != null ? _b : "0"),
                                width: parseFloat((_c = getAttr("width")) != null ? _c : "0"),
                                height: parseFloat((_d = getAttr("height")) != null ? _d : "0")
                            };
                            dArr.push({
                                rect: rect,
                                color: seriesEls[i].getAttribute("color")
                            });
                        };
                        var seriesEls = w.dom.baseEl.querySelectorAll(".apexcharts-".concat(w.config.chart.type, " .apexcharts-series[data\\:realIndex='").concat(h, "'] rect"));
                        var dArr = [];
                        for(var i = 0; i < seriesEls.length; i++)_loop(i);
                        w.globals.previousPaths.push(dArr);
                    };
                    for(var h = 0; h < heatTreeSeries.length; h++)_loop(h);
                }
            }
        },
        {
            key: "clearPreviousPaths",
            value: function clearPreviousPaths() {
                var w = this.w;
                w.globals.previousPaths = [];
                w.globals.allSeriesCollapsed = false;
            }
        },
        {
            key: "handleNoData",
            value: function handleNoData() {
                var w = this.w;
                var me = this;
                var noDataOpts = w.config.noData;
                var graphics = new Graphics(me.w);
                var x = w.globals.svgWidth / 2;
                var y = w.globals.svgHeight / 2;
                var textAnchor = "middle";
                w.globals.noData = true;
                w.globals.animationEnded = true;
                if (noDataOpts.align === "left") {
                    x = 10;
                    textAnchor = "start";
                } else if (noDataOpts.align === "right") {
                    x = w.globals.svgWidth - 10;
                    textAnchor = "end";
                }
                if (noDataOpts.verticalAlign === "top") {
                    y = 50;
                } else if (noDataOpts.verticalAlign === "bottom") {
                    y = w.globals.svgHeight - 50;
                }
                x = x + noDataOpts.offsetX;
                y = y + parseInt(noDataOpts.style.fontSize, 10) + 2 + noDataOpts.offsetY;
                if (noDataOpts.text !== void 0 && noDataOpts.text !== "") {
                    var titleText = graphics.drawText({
                        x: x,
                        y: y,
                        text: noDataOpts.text,
                        textAnchor: textAnchor,
                        fontSize: noDataOpts.style.fontSize,
                        fontFamily: noDataOpts.style.fontFamily,
                        foreColor: noDataOpts.style.color,
                        opacity: 1,
                        cssClass: "apexcharts-text-nodata"
                    });
                    w.dom.Paper.add(titleText);
                }
            }
        },
        {
            // When user clicks on legends, the collapsed series is filled with [0,0,0,...,0]
            // This is because we don't want to alter the series' length as it is used at many places
            /**
   * @param {any[]} series
   */ key: "setNullSeriesToZeroValues",
            value: function setNullSeriesToZeroValues(series) {
                var w = this.w;
                for(var sl = 0; sl < series.length; sl++){
                    if (series[sl].length === 0) {
                        for(var j = 0; j < series[w.globals.maxValsInArrayIndex].length; j++){
                            series[sl].push(0);
                        }
                    }
                }
                return series;
            }
        },
        {
            key: "hasAllSeriesEqualX",
            value: function hasAllSeriesEqualX() {
                var equalLen = true;
                var w = this.w;
                var filteredSerX = this.filteredSeriesX();
                for(var i = 0; i < filteredSerX.length - 1; i++){
                    if (filteredSerX[i][0] !== filteredSerX[i + 1][0]) {
                        equalLen = false;
                        break;
                    }
                }
                w.globals.allSeriesHasEqualX = equalLen;
                return equalLen;
            }
        },
        {
            key: "filteredSeriesX",
            value: function filteredSeriesX() {
                var w = this.w;
                var filteredSeriesX = w.seriesData.seriesX.map(function(ser) {
                    return ser.length > 0 ? ser : [];
                });
                return filteredSeriesX;
            }
        }
    ], [
        {
            key: "addCollapsedClassToSeries",
            value: /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {any} elSeries
   * @param {number} index
   */ function addCollapsedClassToSeries(w, elSeries, index) {
                function iterateOnAllCollapsedSeries(series) {
                    for(var cs = 0; cs < series.length; cs++){
                        if (series[cs].index === index) {
                            elSeries.node.classList.add("apexcharts-series-collapsed");
                        }
                    }
                }
                iterateOnAllCollapsedSeries(w.globals.collapsedSeries);
                iterateOnAllCollapsedSeries(w.globals.ancillaryCollapsedSeries);
            }
        }
    ]);
    return Series;
}();
var TOKEN_MAP = {
    accent: "--apx-accent",
    fore: "--apx-fore",
    grid: "--apx-grid",
    surface: "--apx-surface"
};
var MAX_SERIES_TOKENS = 24;
function readTokens(w) {
    if (!Environment.isBrowser()) return {};
    var el = w.dom && (w.dom.elWrap || w.dom.baseEl) || null;
    if (!el) return {};
    var cs = BrowserAPIs.getComputedStyle(el);
    if (!cs || typeof /** @type {any} */ cs.getPropertyValue !== "function") {
        return {};
    }
    var read = function read(name2) {
        var v = /** @type {any} */ cs.getPropertyValue(name2);
        return v ? String(v).trim() : "";
    };
    var out = {};
    for(var key in TOKEN_MAP){
        var v = read(/** @type {any} */ TOKEN_MAP[key]);
        if (v) out[key] = v;
    }
    var series = [];
    for(var i = 1; i <= MAX_SERIES_TOKENS; i++){
        var v1 = read("--apx-series-".concat(i));
        if (!v1) break;
        series.push(v1);
    }
    if (series.length) out.series = series;
    return out;
}
var THEME_KEY = "__apexcharts_themes__";
if (!/** @type {any} */ globalThis[THEME_KEY]) {
    globalThis[THEME_KEY] = {};
}
function getThemes() {
    return /** @type {any} */ globalThis[THEME_KEY];
}
function registerTheme(name2, def) {
    if (!name2 || typeof name2 !== "string") {
        console.warn("ApexCharts: registerTheme requires a non-empty name.");
        return;
    }
    if (def != null && ((typeof def === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(def)) !== "object" || Array.isArray(def))) {
        console.warn('ApexCharts: registerTheme("'.concat(name2, '") expects an object like { mode, palette, tokens, monochrome, accessibility }.'));
        return;
    }
    getThemes()[name2] = def || {};
}
function getTheme(name2) {
    if (!name2) return null;
    return getThemes()[name2] || null;
}
function unregisterTheme(name2) {
    if (!name2) return;
    delete getThemes()[name2];
}
var DEFAULT_FORECOLOR_LIGHT = "#373d3f";
var DEFAULT_FORECOLOR_DARK = "#f6f7f8";
var DEFAULT_AXIS_GRID = "#e0e0e0";
var Theme = /*#__PURE__*/ function() {
    "use strict";
    function Theme(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Theme);
        this.w = w;
        this.colors = [];
        this.isColorFn = false;
        this.isHeatmapDistributed = this.checkHeatmapDistributed();
        this.isBarDistributed = this.checkBarDistributed();
        this._tokens = {};
        this._namedTheme = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Theme, [
        {
            key: "checkHeatmapDistributed",
            value: function checkHeatmapDistributed() {
                var _this_w_config = this.w.config, chart = _this_w_config.chart, plotOptions = _this_w_config.plotOptions;
                return chart.type === "treemap" && plotOptions.treemap && plotOptions.treemap.distributed || chart.type === "heatmap" && plotOptions.heatmap && plotOptions.heatmap.distributed;
            }
        },
        {
            key: "checkBarDistributed",
            value: function checkBarDistributed() {
                var _this_w_config = this.w.config, chart = _this_w_config.chart, plotOptions = _this_w_config.plotOptions;
                return plotOptions.bar && plotOptions.bar.distributed && (chart.type === "bar" || chart.type === "rangeBar");
            }
        },
        {
            key: "init",
            value: function init() {
                this.setDefaultColors();
            }
        },
        {
            key: "setDefaultColors",
            value: function setDefaultColors() {
                var _a;
                var w = this.w;
                var utils = new Utils$1();
                this._namedTheme = getTheme(w.config.theme.name);
                this._applyNamedThemeMode();
                w.dom.elWrap.classList.add("apexcharts-theme-".concat(w.config.theme.mode || "light"));
                this._applyModeDefaults();
                this._tokens = this._resolveTokens();
                this.applyTokenChrome(this._tokens);
                var colorBlindMode = (_a = w.config.theme.accessibility) == null ? void 0 : _a.colorBlindMode;
                if (colorBlindMode) {
                    w.globals.colors = this.getColorBlindColors(colorBlindMode);
                    this.applySeriesColors(w.seriesData.seriesColors, w.globals.colors);
                    var defaultColors2 = w.globals.colors.slice();
                    this.pushExtraColors(w.globals.colors);
                    this.applyColorTypes([
                        "fill",
                        "stroke"
                    ], defaultColors2);
                    this.applyDataLabelsColors(defaultColors2);
                    this.applyRadarPolygonsColors();
                    this.applyMarkersColors(defaultColors2);
                    if (colorBlindMode === "highContrast") {
                        w.dom.elWrap.classList.add("apexcharts-high-contrast");
                    }
                    return;
                }
                var configColors = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.config.colors || w.config.fill.colors || []);
                w.globals.colors = this.getColors(configColors);
                this.applySeriesColors(w.seriesData.seriesColors, w.globals.colors);
                if (w.config.theme.monochrome.enabled) {
                    w.globals.colors = this.getMonochromeColors(w.config.theme.monochrome, w.seriesData.series, utils);
                }
                var defaultColors = w.globals.colors.slice();
                this.pushExtraColors(w.globals.colors);
                this.applyColorTypes([
                    "fill",
                    "stroke"
                ], defaultColors);
                this.applyDataLabelsColors(defaultColors);
                this.applyRadarPolygonsColors();
                this.applyMarkersColors(defaultColors);
            }
        },
        {
            /**
   * Facet (#13): normalize the mode's concrete defaults (foreColor + palette +
   * tooltip theme) at render time. Only overwrites a value still at its
   * opposite-mode default sentinel, so an explicit user value or a value
   * already normalized by checkForDarkTheme/updateThemeOptions is untouched.
   */ key: "_applyModeDefaults",
            value: function _applyModeDefaults() {
                var w = this.w;
                var mode = w.config.theme.mode;
                if (mode === "dark") {
                    if (w.config.chart.foreColor === DEFAULT_FORECOLOR_LIGHT) {
                        w.config.chart.foreColor = DEFAULT_FORECOLOR_DARK;
                    }
                    if (w.config.theme.palette === "palette1") {
                        w.config.theme.palette = "palette4";
                    }
                    if (w.config.tooltip && w.config.tooltip.theme !== "light") {
                        w.config.tooltip.theme = "dark";
                    }
                } else if (mode === "light") {
                    if (w.config.chart.foreColor === DEFAULT_FORECOLOR_DARK) {
                        w.config.chart.foreColor = DEFAULT_FORECOLOR_LIGHT;
                    }
                    if (w.config.theme.palette === "palette4") {
                        w.config.theme.palette = "palette1";
                    }
                }
            }
        },
        {
            /**
   * Facet (#13): apply a registered named theme's mode / accessibility /
   * monochrome, each only when the user (or the OS watcher) has not set it, so
   * explicit config and `follow:'os'` both win over the named theme.
   */ key: "_applyNamedThemeMode",
            value: function _applyNamedThemeMode() {
                var named = this._namedTheme;
                if (!named) return;
                var theme = this.w.config.theme;
                if (named.mode && !theme.mode) {
                    theme.mode = named.mode;
                }
                if (named.accessibility && named.accessibility.colorBlindMode && !(theme.accessibility && theme.accessibility.colorBlindMode)) {
                    theme.accessibility = theme.accessibility || {};
                    theme.accessibility.colorBlindMode = named.accessibility.colorBlindMode;
                }
                if (named.monochrome && named.monochrome.enabled && !theme.monochrome.enabled) {
                    theme.monochrome = __spreadValues(__spreadValues({}, theme.monochrome), named.monochrome);
                }
            }
        },
        {
            /**
   * Facet (#13): the effective token set. CSS `--apx-*` tokens (when enabled)
   * layer over the named theme's `tokens`, so a page-level token overrides a
   * registered brand default.
   * @returns {{accent?:string, fore?:string, grid?:string, surface?:string, series?:string[]}}
   */ key: "_resolveTokens",
            value: function _resolveTokens() {
                var named = this._namedTheme && this._namedTheme.tokens || {};
                var css = this._shouldUseTokens() ? readTokens(this.w) : {};
                return __spreadValues(__spreadValues({}, named), css);
            }
        },
        {
            /**
   * Facet (#13): tokens are on unless explicitly disabled (`theme.tokens:false`).
   * `true` is the default (the legacy `'auto'` value is accepted and means the
   * same); `readTokens` returns only the tokens actually present, so absence
   * is a no-op.
   * @returns {boolean}
   */ key: "_shouldUseTokens",
            value: function _shouldUseTokens() {
                return this.w.config.theme.tokens !== false;
            }
        },
        {
            /**
   * Facet (#13): overwrite chrome defaults with `--apx-*` tokens, but only where
   * the value still equals its built-in default (so explicit config wins).
   * @param {{fore?:string, grid?:string, surface?:string}} tokens
   */ key: "applyTokenChrome",
            value: function applyTokenChrome(tokens) {
                if (!tokens) return;
                var w = this.w;
                if (tokens.fore && (w.config.chart.foreColor === DEFAULT_FORECOLOR_LIGHT || w.config.chart.foreColor === DEFAULT_FORECOLOR_DARK)) {
                    w.config.chart.foreColor = tokens.fore;
                }
                if (tokens.grid) {
                    if (w.config.grid.borderColor === DEFAULT_AXIS_GRID) {
                        w.config.grid.borderColor = tokens.grid;
                    }
                    var applyAxis = function applyAxis(axis) {
                        if (!axis) return;
                        if (axis.axisBorder && axis.axisBorder.color === DEFAULT_AXIS_GRID) {
                            axis.axisBorder.color = tokens.grid;
                        }
                        if (axis.axisTicks && axis.axisTicks.color === DEFAULT_AXIS_GRID) {
                            axis.axisTicks.color = tokens.grid;
                        }
                    };
                    applyAxis(w.config.xaxis);
                    if (Array.isArray(w.config.yaxis)) {
                        w.config.yaxis.forEach(applyAxis);
                    } else {
                        applyAxis(w.config.yaxis);
                    }
                }
                if (tokens.surface && !w.config.chart.background) {
                    w.config.chart.background = tokens.surface;
                    var paperNode = w.dom.Paper && w.dom.Paper.node;
                    if (paperNode && paperNode.style) {
                        paperNode.style.background = tokens.surface;
                    }
                }
            }
        },
        {
            /**
   * @param {any[]} configColors
   */ key: "getColors",
            value: function getColors(configColors) {
                var _this = this;
                var w = this.w;
                if (!configColors || configColors.length === 0) {
                    return this.predefined();
                }
                if (Array.isArray(configColors) && configColors.length > 0 && typeof configColors[0] === "function") {
                    this.isColorFn = true;
                    return w.config.series.map(function(s, i) {
                        var c = configColors[i] || configColors[0];
                        return typeof c === "function" ? c({
                            value: w.globals.axisCharts ? w.seriesData.series[i][0] || 0 : w.seriesData.series[i],
                            seriesIndex: i,
                            dataPointIndex: i,
                            w: _this.w
                        }) : c;
                    });
                }
                return configColors;
            }
        },
        {
            /**
   * @param {any[]} seriesColors
   * @param {any[]} globalsColors
   */ key: "applySeriesColors",
            value: function applySeriesColors(seriesColors, globalsColors) {
                seriesColors.forEach(function(c, i) {
                    if (c) {
                        globalsColors[i] = c;
                    }
                });
            }
        },
        {
            /**
   * @param {Record<string, any>} monochrome
   * @param {any[]} series
   * @param {any} utils
   */ key: "getMonochromeColors",
            value: function getMonochromeColors(monochrome, series, utils) {
                var color = monochrome.color, shadeIntensity = monochrome.shadeIntensity, shadeTo = monochrome.shadeTo;
                var glsCnt = this.isBarDistributed || this.isHeatmapDistributed ? series[0].length * series.length : series.length;
                var part = 1 / (glsCnt / shadeIntensity);
                var percent = 0;
                return Array.from({
                    length: glsCnt
                }, function() {
                    var newColor = shadeTo === "dark" ? utils.shadeColor(percent * -1, color) : utils.shadeColor(percent, color);
                    percent += part;
                    return newColor;
                });
            }
        },
        {
            /**
   * @param {string[]} colorTypes
   * @param {string[]} defaultColors
   */ key: "applyColorTypes",
            value: function applyColorTypes(colorTypes, defaultColors) {
                var _this = this;
                var w = this.w;
                colorTypes.forEach(function(c) {
                    w.globals[c].colors = w.config[c].colors === void 0 ? _this.isColorFn ? w.config.colors : defaultColors : w.config[c].colors.slice();
                    _this.pushExtraColors(/** @type {Record<string,any>} */ w.globals[c].colors);
                });
            }
        },
        {
            /**
   * @param {string[]} defaultColors
   */ key: "applyDataLabelsColors",
            value: function applyDataLabelsColors(defaultColors) {
                var w = this.w;
                w.globals.dataLabels.style.colors = w.config.dataLabels.style.colors === void 0 ? defaultColors : w.config.dataLabels.style.colors.slice();
                this.pushExtraColors(w.globals.dataLabels.style.colors, 50);
            }
        },
        {
            key: "applyRadarPolygonsColors",
            value: function applyRadarPolygonsColors() {
                var w = this.w;
                w.globals.radarPolygons.fill.colors = w.config.plotOptions.radar.polygons.fill.colors === void 0 ? [
                    w.config.theme.mode === "dark" ? "#343A3F" : "none"
                ] : w.config.plotOptions.radar.polygons.fill.colors.slice();
                this.pushExtraColors(w.globals.radarPolygons.fill.colors, 20);
            }
        },
        {
            /**
   * @param {string[]} defaultColors
   */ key: "applyMarkersColors",
            value: function applyMarkersColors(defaultColors) {
                var w = this.w;
                w.globals.markers.colors = w.config.markers.colors === void 0 ? defaultColors : w.config.markers.colors.slice();
                this.pushExtraColors(w.globals.markers.colors);
            }
        },
        {
            /**
   * @param {any} colorSeries
   * @param {number} [length]
   * @param {boolean | null} [distributed]
   */ key: "pushExtraColors",
            value: function pushExtraColors(colorSeries, length) {
                var distributed = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
                var w = this.w;
                var len = length || w.seriesData.series.length;
                if (distributed === null) {
                    distributed = this.isBarDistributed || this.isHeatmapDistributed || w.config.chart.type === "heatmap" && w.config.plotOptions.heatmap && w.config.plotOptions.heatmap.colorScale.inverse;
                }
                if (distributed && w.seriesData.series.length) {
                    len = w.seriesData.series[w.globals.maxValsInArrayIndex].length * w.seriesData.series.length;
                }
                if (colorSeries.length < len) {
                    var diff = len - colorSeries.length;
                    for(var i = 0; i < diff; i++){
                        colorSeries.push(colorSeries[i]);
                    }
                }
            }
        },
        {
            /**
   * @param {'light' | 'dark'} mode
   */ key: "getColorBlindColors",
            value: function getColorBlindColors(mode) {
                var palettes = getThemePalettes();
                var map = {
                    deuteranopia: palettes.cvdDeuteranopia,
                    protanopia: palettes.cvdProtanopia,
                    tritanopia: palettes.cvdTritanopia,
                    highContrast: palettes.highContrast
                };
                return /** @type {Record<string,any>} */ /** @type {any} */ (map[mode] || palettes.palette1).slice();
            }
        },
        {
            /**
   * @param {Record<string, any>} options
   */ key: "updateThemeOptions",
            value: function updateThemeOptions(options2) {
                options2.chart = options2.chart || {};
                options2.tooltip = options2.tooltip || {};
                var mode = options2.theme.mode;
                var palette = mode === "dark" ? "palette4" : mode === "light" ? "palette1" : options2.theme.palette || "palette1";
                var foreColor = mode === "dark" ? "#f6f7f8" : mode === "light" ? "#373d3f" : options2.chart.foreColor || "#373d3f";
                options2.tooltip.theme = mode || "light";
                options2.chart.foreColor = foreColor;
                options2.theme.palette = palette;
                return options2;
            }
        },
        {
            key: "predefined",
            value: function predefined() {
                var palette = this.w.config.theme.palette;
                var palettes = getThemePalettes();
                var builtin = /** @type {Record<string,any>} */ palettes[palette] || palettes.palette1;
                var tokens = this._tokens || {};
                if (Array.isArray(tokens.series) && tokens.series.length) {
                    return tokens.series.slice();
                }
                if (tokens.accent) {
                    return [
                        tokens.accent
                    ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(builtin));
                }
                var named = this._namedTheme;
                if (named && Array.isArray(named.palette) && named.palette.length) {
                    return named.palette.slice();
                }
                return builtin;
            }
        }
    ]);
    return Theme;
}();
var TitleSubtitle = /*#__PURE__*/ function() {
    "use strict";
    function TitleSubtitle(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, TitleSubtitle);
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(TitleSubtitle, [
        {
            key: "draw",
            value: function draw() {
                this.drawTitleSubtitle("title");
                this.drawTitleSubtitle("subtitle");
            }
        },
        {
            /**
   * @param {'title' | 'subtitle'} type
   */ key: "drawTitleSubtitle",
            value: function drawTitleSubtitle(type) {
                var w = this.w;
                var tsConfig = type === "title" ? w.config.title : w.config.subtitle;
                var x = w.globals.svgWidth / 2;
                var y = tsConfig.offsetY;
                var textAnchor = "middle";
                if (tsConfig.align === "left") {
                    x = 10;
                    textAnchor = "start";
                } else if (tsConfig.align === "right") {
                    x = w.globals.svgWidth - 10;
                    textAnchor = "end";
                }
                x = x + tsConfig.offsetX;
                y = y + parseInt(tsConfig.style.fontSize, 10) + tsConfig.margin / 2;
                if (tsConfig.text !== void 0) {
                    var graphics = new Graphics(this.w);
                    var titleText = graphics.drawText({
                        x: x,
                        y: y,
                        text: tsConfig.text,
                        textAnchor: textAnchor,
                        fontSize: tsConfig.style.fontSize,
                        fontFamily: tsConfig.style.fontFamily,
                        fontWeight: tsConfig.style.fontWeight,
                        foreColor: tsConfig.style.color,
                        opacity: 1
                    });
                    titleText.node.setAttribute("class", "apexcharts-".concat(type, "-text"));
                    w.dom.Paper.add(titleText);
                }
            }
        }
    ]);
    return TitleSubtitle;
}();
var Helpers$4 = /*#__PURE__*/ function() {
    "use strict";
    function Helpers(dCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Helpers);
        this.w = dCtx.w;
        this.dCtx = dCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Helpers, [
        {
            /**
   * Get Chart Title/Subtitle Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   * @param {string} type
   **/ key: "getTitleSubtitleCoords",
            value: function getTitleSubtitleCoords(type) {
                var w = this.w;
                var width = 0;
                var height = 0;
                var floating = type === "title" ? w.config.title.floating : w.config.subtitle.floating;
                var el = w.dom.baseEl.querySelector(".apexcharts-".concat(type, "-text"));
                if (el !== null && !floating) {
                    var coord = el.getBoundingClientRect();
                    width = coord.width;
                    height = w.globals.axisCharts ? coord.height + 5 : coord.height;
                }
                return {
                    width: width,
                    height: height
                };
            }
        },
        {
            key: "getLegendsRect",
            value: function getLegendsRect() {
                var w = this.w;
                var elLegendWrap = w.dom.elLegendWrap;
                if (!w.config.legend.height && (w.config.legend.position === "top" || w.config.legend.position === "bottom")) {
                    if (elLegendWrap) elLegendWrap.style.maxHeight = w.globals.svgHeight / 2 + "px";
                }
                var lgRect = /** @type {any} */ Object.assign({}, Utils$1.getBoundingClientRect(elLegendWrap));
                if (elLegendWrap !== null && !w.config.legend.floating && w.config.legend.show) {
                    this.dCtx.lgRect = {
                        x: lgRect.x,
                        y: lgRect.y,
                        height: lgRect.height,
                        width: lgRect.height === 0 ? 0 : lgRect.width
                    };
                } else {
                    this.dCtx.lgRect = {
                        x: 0,
                        y: 0,
                        height: 0,
                        width: 0
                    };
                }
                if (w.config.legend.position === "left" || w.config.legend.position === "right") {
                    if (this.dCtx.lgRect.width * 1.5 > w.globals.svgWidth) {
                        this.dCtx.lgRect.width = w.globals.svgWidth / 1.5;
                    }
                }
                return this.dCtx.lgRect;
            }
        },
        {
            /**
   * Get Y Axis Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   **/ key: "getDatalabelsRect",
            value: function getDatalabelsRect() {
                var w = this.w;
                var allLabels = [];
                w.config.series.forEach(function(serie, seriesIndex) {
                    serie.data.forEach(function(datum, dataPointIndex) {
                        var getText = function getText(v) {
                            return w.config.dataLabels.formatter(v, {
                                seriesIndex: seriesIndex,
                                dataPointIndex: dataPointIndex,
                                w: w
                            });
                        };
                        var labelText = getText(w.seriesData.series[seriesIndex][dataPointIndex]);
                        allLabels.push(labelText);
                    });
                });
                var val = Utils$1.getLargestStringFromArr(allLabels);
                var graphics = new Graphics(this.w);
                var dataLabelsStyle = w.config.dataLabels.style;
                var labelrect = graphics.getTextRects(val, parseInt(dataLabelsStyle.fontSize).toString(), dataLabelsStyle.fontFamily);
                return {
                    width: labelrect.width * 1.05,
                    height: labelrect.height
                };
            }
        },
        {
            /**
   * @param {any} val
   * @param {any[]} arr
   */ key: "getLargestStringFromMultiArr",
            value: function getLargestStringFromMultiArr(val, arr) {
                var w = this.w;
                var valArr = val;
                if (w.axisFlags.isMultiLineX) {
                    var _Math;
                    var maxArrs = arr.map(function(xl) {
                        return Array.isArray(xl) ? xl.length : 1;
                    });
                    var maxArrLen = (_Math = Math).max.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(maxArrs));
                    var maxArrIndex = maxArrs.indexOf(maxArrLen);
                    valArr = arr[maxArrIndex];
                }
                return valArr;
            }
        },
        {
            /**
   * Vertical space a sparkline has to keep free inside its SVG so the series
   * stroke isn't clipped at the top / bottom edge.
   *
   * A stroke is centred on its path, so half of it hangs outside the plot
   * wherever the path runs along an edge. Reserving that half unconditionally
   * lifts the whole plot away from the SVG edges even when nothing is drawn
   * there, which shows up as a strip of empty space under an area fill or a
   * bar base (#5137). So reserve only what the ink can't absorb itself: where
   * the stroke traces the data points, the distance between the extreme datum
   * and the axis extreme already swallows part or all of the overhang. Fills
   * need nothing: area fills are drawn unstroked (see Line.js renderPaths).
   *
   * @returns {{ top: number, bottom: number }}
   **/ key: "getSparklineStrokeInset",
            value: function getSparklineStrokeInset() {
                var _Math;
                var w = this.w;
                var maxStrokeWidth = Array.isArray(w.config.stroke.width) ? (_Math = Math).max.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.config.stroke.width)) : w.config.stroke.width;
                var half = maxStrokeWidth / 2;
                if (!w.config.stroke.show || !(half > 0)) {
                    return {
                        top: 0,
                        bottom: 0
                    };
                }
                var yRange = w.globals.maxY - w.globals.minY;
                var extremes = this._getSeriesYExtremes();
                if (!this._strokeTracesDataPoints() || !(yRange > 0) || !extremes) {
                    return {
                        top: half,
                        bottom: half
                    };
                }
                var plotHeight = Math.max(w.globals.svgHeight - maxStrokeWidth, 0);
                var roomAbove = plotHeight * (w.globals.maxY - extremes.max) / yRange;
                var roomBelow = plotHeight * (extremes.min - w.globals.minY) / yRange;
                return {
                    top: Math.min(Math.max(half - roomAbove, 0), half),
                    bottom: Math.min(Math.max(half - roomBelow, 0), half)
                };
            }
        },
        {
            /**
   * Whether every drawn series is one whose stroke follows the data points, so
   * the extreme datum marks the outermost ink. False for anything that strokes
   * to the baseline or fills the plot (bar, heatmap, ...) and for stacked
   * charts, where the ink is the cumulative total rather than the raw values.
   * @returns {boolean}
   **/ key: "_strokeTracesDataPoints",
            value: function _strokeTracesDataPoints() {
                var w = this.w;
                if (!w.globals.axisCharts || w.config.chart.stacked) return false;
                var tracesPoints = [
                    "line",
                    "area",
                    "scatter"
                ];
                return /** @type {any[]} */ w.config.series.every(function(s) {
                    return tracesPoints.includes(s.type || w.config.chart.type);
                });
            }
        },
        {
            /**
   * Min / max across every plotted y value, or null when nothing is plottable.
   * @returns {{ min: number, max: number } | null}
   **/ key: "_getSeriesYExtremes",
            value: function _getSeriesYExtremes() {
                var min = Infinity;
                var max = -Infinity;
                this.w.seriesData.series.forEach(function(data) {
                    if (!Array.isArray(data)) return;
                    data.forEach(function(val) {
                        if (!Number.isFinite(val)) return;
                        if (val < min) min = val;
                        if (val > max) max = val;
                    });
                });
                return min === Infinity ? null : {
                    min: min,
                    max: max
                };
            }
        }
    ]);
    return Helpers;
}();
var DimXAxis = /*#__PURE__*/ function() {
    "use strict";
    function DimXAxis(dCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, DimXAxis);
        this.w = dCtx.w;
        this.dCtx = dCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(DimXAxis, [
        {
            /**
   * Get X Axis Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   **/ key: "getxAxisLabelsCoords",
            value: function getxAxisLabelsCoords() {
                var w = this.w;
                var xaxisLabels = w.labelData.labels.slice();
                if (w.config.xaxis.convertedCatToNumeric && xaxisLabels.length === 0) {
                    xaxisLabels = w.labelData.categoryLabels;
                }
                var rect;
                if (w.labelData.timescaleLabels.length > 0) {
                    var coords = this.getxAxisTimeScaleLabelsCoords();
                    rect = {
                        width: coords.width,
                        height: coords.height
                    };
                    w.layout.rotateXLabels = false;
                } else {
                    this.dCtx.lgWidthForSideLegends = (w.config.legend.position === "left" || w.config.legend.position === "right") && !w.config.legend.floating ? this.dCtx.lgRect.width : 0;
                    var xlbFormatter = w.formatters.xLabelFormatter;
                    var val = Utils$1.getLargestStringFromArr(xaxisLabels);
                    var valArr = this.dCtx.dimHelpers.getLargestStringFromMultiArr(val, xaxisLabels);
                    if (w.globals.isBarHorizontal) {
                        val = w.globals.yAxisScale[0].result.reduce(function(a, b) {
                            return a.length > b.length ? a : b;
                        }, 0);
                        valArr = val;
                    }
                    var xFormat = new Formatters(this.w);
                    var timestamp = val;
                    val = xFormat.xLabelFormat(/** @type {Function} */ xlbFormatter, val, timestamp, {
                        i: void 0,
                        dateFormatter: new DateTime(this.w).formatDate,
                        w: w
                    });
                    valArr = xFormat.xLabelFormat(/** @type {Function} */ xlbFormatter, valArr, timestamp, {
                        i: void 0,
                        dateFormatter: new DateTime(this.w).formatDate,
                        w: w
                    });
                    if (w.config.xaxis.convertedCatToNumeric && typeof val === "undefined" || String(val).trim() === "") {
                        val = "1";
                        valArr = val;
                    }
                    var graphics = new Graphics(this.w);
                    var xLabelrect = graphics.getTextRects(val, w.config.xaxis.labels.style.fontSize);
                    var xArrLabelrect = xLabelrect;
                    if (val !== valArr) {
                        xArrLabelrect = graphics.getTextRects(valArr, w.config.xaxis.labels.style.fontSize);
                    }
                    rect = {
                        width: xLabelrect.width >= xArrLabelrect.width ? xLabelrect.width : xArrLabelrect.width,
                        height: xLabelrect.height >= xArrLabelrect.height ? xLabelrect.height : xArrLabelrect.height
                    };
                    if (rect.width * xaxisLabels.length > w.globals.svgWidth - this.dCtx.lgWidthForSideLegends - this.dCtx.yAxisWidth - this.dCtx.gridPad.left - this.dCtx.gridPad.right && w.config.xaxis.labels.rotate !== 0 || w.config.xaxis.labels.rotateAlways) {
                        if (!w.globals.isBarHorizontal) {
                            w.layout.rotateXLabels = true;
                            var getRotatedTextRects = function getRotatedTextRects(text) {
                                return graphics.getTextRects(text, w.config.xaxis.labels.style.fontSize, w.config.xaxis.labels.style.fontFamily, "rotate(".concat(w.config.xaxis.labels.rotate, " 0 0)"), false);
                            };
                            xLabelrect = getRotatedTextRects(val);
                            if (val !== valArr) {
                                xArrLabelrect = getRotatedTextRects(valArr);
                            }
                            rect.height = (xLabelrect.height > xArrLabelrect.height ? xLabelrect.height : xArrLabelrect.height) / 1.5;
                            rect.width = xLabelrect.width > xArrLabelrect.width ? xLabelrect.width : xArrLabelrect.width;
                        }
                    } else {
                        w.layout.rotateXLabels = false;
                    }
                }
                if (!w.config.xaxis.labels.show) {
                    rect = {
                        width: 0,
                        height: 0
                    };
                }
                return {
                    width: rect.width,
                    height: rect.height
                };
            }
        },
        {
            /**
   * Get X Axis Label Group height
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   */ key: "getxAxisGroupLabelsCoords",
            value: function getxAxisGroupLabelsCoords() {
                var _a;
                var w = this.w;
                if (!w.labelData.hasXaxisGroups) {
                    return {
                        width: 0,
                        height: 0
                    };
                }
                var fontSize = ((_a = w.config.xaxis.group.style) == null ? void 0 : _a.fontSize) || w.config.xaxis.labels.style.fontSize;
                var xaxisLabels = w.labelData.groups.map(function(g) {
                    return g.title;
                });
                var rect;
                var val = Utils$1.getLargestStringFromArr(xaxisLabels);
                var valArr = this.dCtx.dimHelpers.getLargestStringFromMultiArr(val, xaxisLabels);
                var graphics = new Graphics(this.w);
                var xLabelrect = graphics.getTextRects(val, fontSize);
                var xArrLabelrect = xLabelrect;
                if (val !== valArr) {
                    xArrLabelrect = graphics.getTextRects(valArr, fontSize);
                }
                rect = {
                    width: xLabelrect.width >= xArrLabelrect.width ? xLabelrect.width : xArrLabelrect.width,
                    height: xLabelrect.height >= xArrLabelrect.height ? xLabelrect.height : xArrLabelrect.height
                };
                if (!w.config.xaxis.labels.show) {
                    rect = {
                        width: 0,
                        height: 0
                    };
                }
                return {
                    width: rect.width,
                    height: rect.height
                };
            }
        },
        {
            /**
   * Get X Axis Title Dimensions
   * @memberof Dimensions
   * @return {{width: number, height: number}}
   **/ key: "getxAxisTitleCoords",
            value: function getxAxisTitleCoords() {
                var w = this.w;
                var width = 0;
                var height = 0;
                if (w.config.xaxis.title.text !== void 0) {
                    var graphics = new Graphics(this.w);
                    var rect = graphics.getTextRects(w.config.xaxis.title.text, w.config.xaxis.title.style.fontSize);
                    width = rect.width;
                    height = rect.height;
                }
                return {
                    width: width,
                    height: height
                };
            }
        },
        {
            key: "getxAxisTimeScaleLabelsCoords",
            value: function getxAxisTimeScaleLabelsCoords() {
                var w = this.w;
                this.dCtx.timescaleLabels = w.labelData.timescaleLabels.slice();
                var labels = this.dCtx.timescaleLabels.map(function(label) {
                    return label.value;
                });
                var val = labels.reduce(function(a, b) {
                    if (typeof a === "undefined") {
                        console.error("You have possibly supplied invalid Date format. Please supply a valid JavaScript Date");
                        return 0;
                    } else {
                        return a.length > b.length ? a : b;
                    }
                }, 0);
                var graphics = new Graphics(this.w);
                var rect = graphics.getTextRects(val, w.config.xaxis.labels.style.fontSize);
                var totalWidthRotated = rect.width * 1.05 * labels.length;
                if (totalWidthRotated > w.layout.gridWidth && w.config.xaxis.labels.rotate !== 0) {
                    w.globals.overlappingXLabels = true;
                }
                return rect;
            }
        },
        {
            // In certain cases, the last labels gets cropped in xaxis.
            // Hence, we add some additional padding based on the label length to avoid the last label being cropped or we don't draw it at all
            /**
   * @param {Record<string, any>} xaxisLabelCoords
   */ key: "additionalPaddingXLabels",
            value: function additionalPaddingXLabels(xaxisLabelCoords) {
                var _this = this;
                var w = this.w;
                var gl = w.globals;
                var cnf = w.config;
                var xtype = cnf.xaxis.type;
                var lbWidth = xaxisLabelCoords.width;
                gl.skipLastTimelinelabel = false;
                gl.skipFirstTimelinelabel = false;
                var isBarOpposite = w.config.yaxis[0].opposite && w.globals.isBarHorizontal;
                var isCollapsed = function isCollapsed(i) {
                    return gl.collapsedSeriesIndices.indexOf(i) !== -1;
                };
                var rightPad = function rightPad(yaxe) {
                    if (_this.dCtx.timescaleLabels && _this.dCtx.timescaleLabels.length) {
                        var firstimescaleLabel = _this.dCtx.timescaleLabels[0];
                        var lastTimescaleLabel = _this.dCtx.timescaleLabels[_this.dCtx.timescaleLabels.length - 1];
                        var lastLabelPosition = lastTimescaleLabel.position + lbWidth / 1.75 - _this.dCtx.yAxisWidthRight;
                        var firstLabelPosition = firstimescaleLabel.position - lbWidth / 1.75 + _this.dCtx.yAxisWidthLeft;
                        var lgRightRectWidth = w.config.legend.position === "right" && _this.dCtx.lgRect.width > 0 ? _this.dCtx.lgRect.width : 0;
                        if (lastLabelPosition > gl.svgWidth - w.layout.translateX - lgRightRectWidth) {
                            gl.skipLastTimelinelabel = true;
                        }
                        if (firstLabelPosition < -((!yaxe.show || yaxe.floating) && (cnf.chart.type === "bar" || cnf.chart.type === "candlestick" || cnf.chart.type === "rangeBar" || cnf.chart.type === "boxPlot" || cnf.chart.type === "violin") ? lbWidth / 1.75 : 10)) {
                            gl.skipFirstTimelinelabel = true;
                        }
                    } else if (xtype === "datetime") {
                        if (_this.dCtx.gridPad.right < lbWidth && !w.layout.rotateXLabels) {
                            gl.skipLastTimelinelabel = true;
                        }
                    } else if (xtype !== "datetime") {
                        if (_this.dCtx.gridPad.right < lbWidth / 2 - _this.dCtx.yAxisWidthRight && !w.layout.rotateXLabels && !w.config.xaxis.labels.trim) {
                            _this.dCtx.xPadRight = lbWidth / 2 + 1;
                        }
                    }
                };
                var padYAxe = function padYAxe(yaxe, i) {
                    if (cnf.yaxis.length > 1 && isCollapsed(i)) return;
                    rightPad(yaxe);
                };
                cnf.yaxis.forEach(function(yaxe, i) {
                    if (isBarOpposite) {
                        if (_this.dCtx.gridPad.left < lbWidth) {
                            _this.dCtx.xPadLeft = lbWidth / 2 + 1;
                        }
                        _this.dCtx.xPadRight = lbWidth / 2 + 1;
                    } else {
                        padYAxe(yaxe, i);
                    }
                });
            }
        }
    ]);
    return DimXAxis;
}();
var DimYAxis = /*#__PURE__*/ function() {
    "use strict";
    function DimYAxis(dCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, DimYAxis);
        this.w = dCtx.w;
        this.dCtx = dCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(DimYAxis, [
        {
            /**
   * Get Y Axis Dimensions
   * @memberof Dimensions
   * @returns {Array<{width: number, height: number}>}
   **/ key: "getyAxisLabelsCoords",
            value: function getyAxisLabelsCoords() {
                var _this = this;
                var w = this.w;
                var width = 0;
                var height = 0;
                var ret = [];
                var labelPad = 10;
                var axesUtils = new AxesUtils(this.w, {
                    theme: this.dCtx.theme,
                    timeScale: this.dCtx.timeScale
                });
                w.config.yaxis.map(function(yaxe, index) {
                    var formatterArgs = {
                        seriesIndex: index,
                        dataPointIndex: -1,
                        w: w
                    };
                    var yS = w.globals.yAxisScale[index];
                    var yAxisMinWidth = 0;
                    if (!axesUtils.isYAxisHidden(index) && yaxe.labels.show && yaxe.labels.minWidth !== void 0) yAxisMinWidth = yaxe.labels.minWidth;
                    if (!axesUtils.isYAxisHidden(index) && yaxe.labels.show && yS.result.length) {
                        var lbFormatter = w.formatters.yLabelFormatters[index];
                        var minV = yS.niceMin === Number.MIN_VALUE ? 0 : yS.niceMin;
                        var val = yS.result.reduce(function(acc, curr) {
                            var _a, _b;
                            return ((_a = String(lbFormatter(acc, formatterArgs))) == null ? void 0 : _a.length) > ((_b = String(lbFormatter(curr, formatterArgs))) == null ? void 0 : _b.length) ? acc : curr;
                        }, minV);
                        val = lbFormatter(val, formatterArgs);
                        var valArr = val;
                        if (typeof val === "undefined" || val.length === 0) {
                            val = yS.niceMax;
                        }
                        if (String(val).length === 1) {
                            val = val + ".0";
                            valArr = val;
                        }
                        if (w.globals.isBarHorizontal) {
                            labelPad = 0;
                            var barYaxisLabels = w.labelData.labels.slice();
                            val = Utils$1.getLargestStringFromArr(barYaxisLabels);
                            val = lbFormatter(val, {
                                seriesIndex: index,
                                dataPointIndex: -1,
                                w: w
                            });
                            valArr = _this.dCtx.dimHelpers.getLargestStringFromMultiArr(val, barYaxisLabels);
                        }
                        var graphics = new Graphics(_this.w);
                        var rotateStr = "rotate(".concat(yaxe.labels.rotate, " 0 0)");
                        var rect = graphics.getTextRects(val, yaxe.labels.style.fontSize, yaxe.labels.style.fontFamily, rotateStr, false);
                        var arrLabelrect = rect;
                        if (val !== valArr) {
                            arrLabelrect = graphics.getTextRects(valArr, yaxe.labels.style.fontSize, yaxe.labels.style.fontFamily, rotateStr, false);
                        }
                        ret.push({
                            width: (yAxisMinWidth > arrLabelrect.width || yAxisMinWidth > rect.width ? yAxisMinWidth : arrLabelrect.width > rect.width ? arrLabelrect.width : rect.width) + labelPad,
                            height: arrLabelrect.height > rect.height ? arrLabelrect.height : rect.height
                        });
                    } else {
                        ret.push({
                            width: width,
                            height: height
                        });
                    }
                });
                return ret;
            }
        },
        {
            /**
   * Get Y Axis Dimensions
   * @memberof Dimensions
   * @returns {Array<{width: number, height: number}>}
   **/ key: "getyAxisTitleCoords",
            value: function getyAxisTitleCoords() {
                var _this = this;
                var w = this.w;
                var ret = [];
                w.config.yaxis.map(function(yaxe) {
                    if (yaxe.show && yaxe.title.text !== void 0) {
                        var graphics = new Graphics(_this.w);
                        var rotateStr = "rotate(".concat(yaxe.title.rotate, " 0 0)");
                        var rect = graphics.getTextRects(yaxe.title.text, yaxe.title.style.fontSize, yaxe.title.style.fontFamily, rotateStr, false);
                        ret.push({
                            width: rect.width,
                            height: rect.height
                        });
                    } else {
                        ret.push({
                            width: 0,
                            height: 0
                        });
                    }
                });
                return ret;
            }
        },
        {
            key: "getTotalYAxisWidth",
            value: function getTotalYAxisWidth() {
                var w = this.w;
                var yAxisWidth = 0;
                var yAxisWidthLeft = 0;
                var yAxisWidthRight = 0;
                var padding = w.globals.yAxisScale.length > 1 ? 10 : 0;
                var axesUtils = new AxesUtils(this.w, {
                    theme: this.dCtx.theme,
                    timeScale: this.dCtx.timeScale
                });
                var isHiddenYAxis = function isHiddenYAxis(index) {
                    return w.globals.ignoreYAxisIndexes.indexOf(index) > -1;
                };
                var padForLabelTitle = function padForLabelTitle(coord, index) {
                    var floating = w.config.yaxis[index].floating;
                    var width = 0;
                    if (coord.width > 0 && !floating) {
                        width = coord.width + padding;
                        if (isHiddenYAxis(index)) {
                            width = width - coord.width - padding;
                        }
                    } else {
                        width = floating || axesUtils.isYAxisHidden(index) ? 0 : 5;
                    }
                    w.config.yaxis[index].opposite ? yAxisWidthRight = yAxisWidthRight + width : yAxisWidthLeft = yAxisWidthLeft + width;
                    yAxisWidth = yAxisWidth + width;
                };
                w.layout.yLabelsCoords.map(function(yLabelCoord, index) {
                    padForLabelTitle(yLabelCoord, index);
                });
                w.layout.yTitleCoords.map(function(yTitleCoord, index) {
                    padForLabelTitle(yTitleCoord, index);
                });
                if (w.globals.isBarHorizontal && !w.config.yaxis[0].floating) {
                    yAxisWidth = w.layout.yLabelsCoords[0].width + w.layout.yTitleCoords[0].width + 15;
                }
                this.dCtx.yAxisWidthLeft = yAxisWidthLeft;
                this.dCtx.yAxisWidthRight = yAxisWidthRight;
                return yAxisWidth;
            }
        }
    ]);
    return DimYAxis;
}();
var DimGrid = /*#__PURE__*/ function() {
    "use strict";
    function DimGrid(dCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, DimGrid);
        this.w = dCtx.w;
        this.dCtx = dCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(DimGrid, [
        {
            /**
   * @param {number} gridWidth
   */ key: "gridPadForColumnsInNumericAxis",
            value: function gridPadForColumnsInNumericAxis(gridWidth) {
                var w = this.w;
                var cnf = w.config, gl = w.globals;
                if (gl.noData || gl.collapsedSeries.length + gl.ancillaryCollapsedSeries.length === cnf.series.length) {
                    return 0;
                }
                var hasBar = function hasBar(type2) {
                    return [
                        "bar",
                        "rangeBar",
                        "candlestick",
                        "boxPlot",
                        "violin"
                    ].includes(type2);
                };
                var type = cnf.chart.type;
                var barWidth = 0;
                var seriesLen = hasBar(type) ? cnf.series.length : 1;
                if (gl.comboBarCount > 0) {
                    seriesLen = gl.comboBarCount;
                }
                gl.collapsedSeries.forEach(function(c) {
                    if (hasBar(c.type)) {
                        seriesLen -= 1;
                    }
                });
                if (cnf.chart.stacked) {
                    seriesLen = 1;
                }
                var barsPresent = hasBar(type) || gl.comboBarCount > 0;
                var xRange = Math.abs(gl.initialMaxX - gl.initialMinX);
                if (barsPresent && w.axisFlags.isXNumeric && !gl.isBarHorizontal && seriesLen > 0 && xRange !== 0) {
                    if (xRange <= 3) {
                        xRange = gl.dataPoints;
                    }
                    var xRatio = xRange / gridWidth;
                    var xDivision = gl.minXDiff && gl.minXDiff / xRatio > 0 ? gl.minXDiff / xRatio : 0;
                    if (xDivision > gridWidth / 2) {
                        xDivision /= 2;
                    }
                    barWidth = xDivision * parseInt(cnf.plotOptions.bar.columnWidth, 10) / 100;
                    if (barWidth < 1) {
                        barWidth = 1;
                    }
                    gl.barPadForNumericAxis = barWidth;
                }
                return barWidth;
            }
        },
        {
            key: "gridPadFortitleSubtitle",
            value: function gridPadFortitleSubtitle() {
                var _this = this;
                var w = this.w;
                var gl = w.globals;
                var gridShrinkOffset = this.dCtx.isSparkline || !gl.axisCharts ? 0 : 10;
                var titleSubtitle = [
                    "title",
                    "subtitle"
                ];
                titleSubtitle.forEach(function(t) {
                    if (w.config[t].text !== void 0) {
                        gridShrinkOffset += w.config[t].margin;
                    } else {
                        gridShrinkOffset += _this.dCtx.isSparkline || !gl.axisCharts ? 0 : 5;
                    }
                });
                if (w.config.legend.show && w.config.legend.position === "bottom" && !w.config.legend.floating && !gl.axisCharts) {
                    gridShrinkOffset += 10;
                }
                var titleCoords = this.dCtx.dimHelpers.getTitleSubtitleCoords("title");
                var subtitleCoords = this.dCtx.dimHelpers.getTitleSubtitleCoords("subtitle");
                w.layout.gridHeight -= titleCoords.height + subtitleCoords.height + gridShrinkOffset;
                w.layout.translateY += titleCoords.height + subtitleCoords.height + gridShrinkOffset;
            }
        },
        {
            /**
   * @param {{width: number, height: number}[]} yTitleCoords
   * @param {{width: number, height: number}[]} yaxisLabelCoords
   */ key: "setGridXPosForDualYAxis",
            value: function setGridXPosForDualYAxis(yTitleCoords, yaxisLabelCoords) {
                var w = this.w;
                var axesUtils = new AxesUtils(this.w, {
                    theme: this.dCtx.theme,
                    timeScale: this.dCtx.timeScale
                });
                w.config.yaxis.forEach(function(yaxe, index) {
                    if (w.globals.ignoreYAxisIndexes.indexOf(index) === -1 && !yaxe.floating && !axesUtils.isYAxisHidden(index)) {
                        if (yaxe.opposite) {
                            w.layout.translateX -= yaxisLabelCoords[index].width + yTitleCoords[index].width + parseInt(yaxe.labels.style.fontSize, 10) / 1.2 + 12;
                        }
                        if (w.layout.translateX < 2) {
                            w.layout.translateX = 2;
                        }
                    }
                });
            }
        }
    ]);
    return DimGrid;
}();
var Dimensions = /*#__PURE__*/ function() {
    "use strict";
    function Dimensions(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Dimensions);
        this.w = w;
        this.ctx = ctx;
        this.theme = ctx.theme;
        this.timeScale = ctx.timeScale;
        this.lgRect = /** @type {any} */ {};
        this.yAxisWidth = 0;
        this.yAxisWidthLeft = 0;
        this.yAxisWidthRight = 0;
        this.xAxisHeight = 0;
        this.isSparkline = this.w.config.chart.sparkline.enabled;
        this.dimHelpers = new Helpers$4(this);
        this.dimYAxis = new DimYAxis(this);
        this.dimXAxis = new DimXAxis(this);
        this.dimGrid = new DimGrid(this);
        this.lgWidthForSideLegends = 0;
        this.gridPad = __spreadValues({}, this.w.config.grid.padding);
        this.xPadRight = 0;
        this.xPadLeft = 0;
        this.datalabelsCoords = {
            width: 0,
            height: 0
        };
        this.xAxisWidth = 0;
        this.timescaleLabels = [];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Dimensions, [
        {
            /**
   * @memberof Dimensions
   **/ key: "plotCoords",
            value: function plotCoords() {
                var _this = this;
                var w = this.w;
                var gl = w.globals;
                this.lgRect = this.dimHelpers.getLegendsRect();
                this.datalabelsCoords = {
                    width: 0,
                    height: 0
                };
                if (this.isSparkline) {
                    if (this.w.globals.markers.largestSize > 0) {
                        Object.entries(this.gridPad).forEach(function(param) {
                            var _param = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(param, 2), k = _param[0], v = _param[1];
                            _this.gridPad[k] = Math.max(v, _this.w.globals.markers.largestSize / 1.5);
                        });
                    }
                    var strokeInset = this.dimHelpers.getSparklineStrokeInset();
                    this.gridPad.top = Math.max(strokeInset.top, this.gridPad.top);
                    this.gridPad.bottom = Math.max(strokeInset.bottom, this.gridPad.bottom);
                }
                if (gl.axisCharts) {
                    this.setDimensionsForAxisCharts();
                } else {
                    this.setDimensionsForNonAxisCharts();
                }
                this.dimGrid.gridPadFortitleSubtitle();
                w.layout.gridHeight = w.layout.gridHeight - this.gridPad.top - this.gridPad.bottom;
                w.layout.gridWidth = w.layout.gridWidth - this.gridPad.left - this.gridPad.right - this.xPadRight - this.xPadLeft;
                var barWidth = this.dimGrid.gridPadForColumnsInNumericAxis(w.layout.gridWidth);
                w.layout.gridWidth = w.layout.gridWidth - barWidth * 2;
                w.layout.translateX = w.layout.translateX + this.gridPad.left + this.xPadLeft + (barWidth > 0 ? barWidth : 0);
                w.layout.translateY = w.layout.translateY + this.gridPad.top;
                return {
                    // w.layout (future slice)
                    layout: {
                        gridHeight: w.layout.gridHeight,
                        gridWidth: w.layout.gridWidth,
                        translateX: w.layout.translateX,
                        translateY: w.layout.translateY,
                        translateXAxisX: w.layout.translateXAxisX,
                        translateXAxisY: w.layout.translateXAxisY,
                        rotateXLabels: w.layout.rotateXLabels,
                        xAxisHeight: w.layout.xAxisHeight,
                        xAxisLabelsHeight: w.layout.xAxisLabelsHeight,
                        xAxisGroupLabelsHeight: w.layout.xAxisGroupLabelsHeight,
                        xAxisLabelsWidth: w.layout.xAxisLabelsWidth,
                        yLabelsCoords: w.layout.yLabelsCoords,
                        yTitleCoords: w.layout.yTitleCoords,
                        gridPad: __spreadValues({}, this.gridPad)
                    }
                };
            }
        },
        {
            key: "setDimensionsForAxisCharts",
            value: function setDimensionsForAxisCharts() {
                var _this = this;
                var w = this.w;
                var gl = w.globals;
                var yaxisLabelCoords = this.dimYAxis.getyAxisLabelsCoords();
                var yTitleCoords = this.dimYAxis.getyAxisTitleCoords();
                if (gl.isSlopeChart) {
                    this.datalabelsCoords = this.dimHelpers.getDatalabelsRect();
                }
                w.layout.yLabelsCoords = [];
                w.layout.yTitleCoords = [];
                w.config.yaxis.map(function(yaxe, index) {
                    w.layout.yLabelsCoords.push({
                        width: yaxisLabelCoords[index].width,
                        index: index
                    });
                    w.layout.yTitleCoords.push(/** @type {any} */ {
                        width: yTitleCoords[index].width,
                        index: index
                    });
                });
                this.yAxisWidth = this.dimYAxis.getTotalYAxisWidth();
                var xaxisLabelCoords = this.dimXAxis.getxAxisLabelsCoords();
                var xaxisGroupLabelCoords = this.dimXAxis.getxAxisGroupLabelsCoords();
                var xtitleCoords = this.dimXAxis.getxAxisTitleCoords();
                this.conditionalChecksForAxisCoords(xaxisLabelCoords, xtitleCoords, xaxisGroupLabelCoords);
                w.layout.translateXAxisY = w.layout.rotateXLabels ? this.xAxisHeight / 8 : -4;
                w.layout.translateXAxisX = w.layout.rotateXLabels && w.axisFlags.isXNumeric && w.config.xaxis.labels.rotate <= -45 ? -this.xAxisWidth / 4 : 0;
                if (w.globals.isBarHorizontal) {
                    w.layout.rotateXLabels = false;
                    w.layout.translateXAxisY = -1 * (parseInt(w.config.xaxis.labels.style.fontSize, 10) / 1.5);
                }
                w.layout.translateXAxisY = w.layout.translateXAxisY + w.config.xaxis.labels.offsetY;
                w.layout.translateXAxisX = w.layout.translateXAxisX + w.config.xaxis.labels.offsetX;
                var yAxisWidth = this.yAxisWidth;
                var xAxisHeight = this.xAxisHeight;
                w.layout.xAxisLabelsHeight = this.xAxisHeight - xtitleCoords.height;
                w.layout.xAxisGroupLabelsHeight = w.layout.xAxisLabelsHeight - xaxisLabelCoords.height;
                w.layout.xAxisLabelsWidth = this.xAxisWidth;
                w.layout.xAxisHeight = this.xAxisHeight;
                var translateY = 10;
                if (w.config.chart.type === "radar" || this.isSparkline) {
                    yAxisWidth = 0;
                    xAxisHeight = 0;
                }
                if (this.isSparkline) {
                    this.lgRect = {
                        height: 0,
                        width: 0
                    };
                }
                if (this.isSparkline || w.config.chart.type === "treemap") {
                    yAxisWidth = 0;
                    xAxisHeight = 0;
                    translateY = 0;
                }
                if (!this.isSparkline && w.config.chart.type !== "treemap") {
                    this.dimXAxis.additionalPaddingXLabels(xaxisLabelCoords);
                }
                var legendTopBottom = function legendTopBottom() {
                    w.layout.translateX = yAxisWidth + _this.datalabelsCoords.width;
                    w.layout.gridHeight = gl.svgHeight - _this.lgRect.height - xAxisHeight - (!_this.isSparkline && w.config.chart.type !== "treemap" ? w.layout.rotateXLabels ? 10 : 15 : 0);
                    w.layout.gridWidth = gl.svgWidth - yAxisWidth - _this.datalabelsCoords.width * 2;
                };
                if (w.config.xaxis.position === "top") translateY = w.layout.xAxisHeight - w.config.xaxis.axisTicks.height - 5;
                switch(w.config.legend.position){
                    case "bottom":
                        w.layout.translateY = translateY;
                        legendTopBottom();
                        break;
                    case "top":
                        w.layout.translateY = this.lgRect.height + translateY;
                        legendTopBottom();
                        break;
                    case "left":
                        w.layout.translateY = translateY;
                        w.layout.translateX = this.lgRect.width + yAxisWidth + this.datalabelsCoords.width;
                        w.layout.gridHeight = gl.svgHeight - xAxisHeight - 12;
                        w.layout.gridWidth = gl.svgWidth - this.lgRect.width - yAxisWidth - this.datalabelsCoords.width * 2;
                        break;
                    case "right":
                        w.layout.translateY = translateY;
                        w.layout.translateX = yAxisWidth + this.datalabelsCoords.width;
                        w.layout.gridHeight = gl.svgHeight - xAxisHeight - 12;
                        w.layout.gridWidth = gl.svgWidth - this.lgRect.width - yAxisWidth - this.datalabelsCoords.width * 2 - 5;
                        break;
                    default:
                        throw new Error("Legend position not supported");
                }
                this.dimGrid.setGridXPosForDualYAxis(yTitleCoords, yaxisLabelCoords);
                var objyAxis = new YAxis(this.w, {
                    theme: this.theme,
                    timeScale: this.timeScale
                });
                objyAxis.setYAxisXPosition(yaxisLabelCoords, yTitleCoords);
            }
        },
        {
            key: "setDimensionsForNonAxisCharts",
            value: function setDimensionsForNonAxisCharts() {
                var w = this.w;
                var gl = w.globals;
                var cnf = w.config;
                var xPad = 0;
                if (w.config.legend.show && !w.config.legend.floating) {
                    xPad = 20;
                }
                if (cnf.chart.type === "unit") {
                    var legendVisible = cnf.legend.show && !cnf.legend.floating;
                    var pos = cnf.legend.position;
                    var top = 0;
                    var side = 0;
                    if (legendVisible) {
                        if (pos === "bottom" || pos === "top") {
                            top = this.lgRect.height;
                        } else {
                            side = this.lgRect.width + xPad;
                        }
                    }
                    w.layout.gridWidth = gl.svgWidth - side;
                    w.layout.gridHeight = gl.svgHeight - top;
                    w.layout.translateX = pos === "left" ? side : 0;
                    w.layout.translateY = pos === "top" ? top : 0;
                    return;
                }
                var type = cnf.chart.type === "sunburst" ? "sunburst" : cnf.chart.type === "pie" || cnf.chart.type === "polarArea" || cnf.chart.type === "donut" ? "pie" : "radialBar";
                var offY = cnf.plotOptions[type].offsetY;
                var offX = cnf.plotOptions[type].offsetX;
                if (!cnf.legend.show || cnf.legend.floating) {
                    w.layout.gridHeight = gl.svgHeight;
                    var maxWidth = w.dom.elWrap.getBoundingClientRect().width;
                    w.layout.gridWidth = Math.min(maxWidth, w.layout.gridHeight);
                    w.layout.translateY = offY;
                    w.layout.translateX = offX + (gl.svgWidth - w.layout.gridWidth) / 2;
                    return;
                }
                switch(cnf.legend.position){
                    case "bottom":
                        w.layout.gridHeight = gl.svgHeight - this.lgRect.height;
                        w.layout.gridWidth = gl.svgWidth;
                        w.layout.translateY = offY - 10;
                        w.layout.translateX = offX + (gl.svgWidth - w.layout.gridWidth) / 2;
                        break;
                    case "top":
                        w.layout.gridHeight = gl.svgHeight - this.lgRect.height;
                        w.layout.gridWidth = gl.svgWidth;
                        w.layout.translateY = this.lgRect.height + offY + 10;
                        w.layout.translateX = offX + (gl.svgWidth - w.layout.gridWidth) / 2;
                        break;
                    case "left":
                        w.layout.gridWidth = gl.svgWidth - this.lgRect.width - xPad;
                        w.layout.gridHeight = cnf.chart.height !== "auto" ? gl.svgHeight : w.layout.gridWidth;
                        w.layout.translateY = offY;
                        w.layout.translateX = offX + this.lgRect.width + xPad;
                        break;
                    case "right":
                        w.layout.gridWidth = gl.svgWidth - this.lgRect.width - xPad - 5;
                        w.layout.gridHeight = cnf.chart.height !== "auto" ? gl.svgHeight : w.layout.gridWidth;
                        w.layout.translateY = offY;
                        w.layout.translateX = offX + 10;
                        break;
                    default:
                        throw new Error("Legend position not supported");
                }
            }
        },
        {
            /**
   * @param {any} xaxisLabelCoords
   * @param {any} xtitleCoords
   * @param {any} xaxisGroupLabelCoords
   */ key: "conditionalChecksForAxisCoords",
            value: function conditionalChecksForAxisCoords(xaxisLabelCoords, xtitleCoords, xaxisGroupLabelCoords) {
                var w = this.w;
                var xAxisNum = w.labelData.hasXaxisGroups ? 2 : 1;
                var baseXAxisHeight = xaxisGroupLabelCoords.height + xaxisLabelCoords.height + xtitleCoords.height;
                var xAxisHeightMultiplicate = w.axisFlags.isMultiLineX ? 1.2 : LINE_HEIGHT_RATIO;
                var rotatedXAxisOffset = w.layout.rotateXLabels ? 22 : 10;
                var rotatedXAxisLegendOffset = w.layout.rotateXLabels && w.config.legend.position === "bottom";
                var additionalOffset = rotatedXAxisLegendOffset ? 10 : 0;
                this.xAxisHeight = baseXAxisHeight * xAxisHeightMultiplicate + xAxisNum * rotatedXAxisOffset + additionalOffset;
                this.xAxisWidth = xaxisLabelCoords.width;
                if (this.xAxisHeight - xtitleCoords.height > w.config.xaxis.labels.maxHeight) {
                    this.xAxisHeight = w.config.xaxis.labels.maxHeight;
                }
                if (w.config.xaxis.labels.minHeight && this.xAxisHeight < w.config.xaxis.labels.minHeight) {
                    this.xAxisHeight = w.config.xaxis.labels.minHeight;
                }
                if (w.config.xaxis.floating) {
                    this.xAxisHeight = 0;
                }
                var minYAxisWidth = 0;
                var maxYAxisWidth = 0;
                w.config.yaxis.forEach(function(y) {
                    minYAxisWidth += y.labels.minWidth;
                    maxYAxisWidth += y.labels.maxWidth;
                });
                if (this.yAxisWidth < minYAxisWidth) {
                    this.yAxisWidth = minYAxisWidth;
                }
                if (this.yAxisWidth > maxYAxisWidth) {
                    this.yAxisWidth = maxYAxisWidth;
                }
            }
        }
    ]);
    return Dimensions;
}();
var SECOND = 1e3;
var MINUTE = 60 * SECOND;
var HOUR = 60 * MINUTE;
var DAY = 24 * HOUR;
var WEEK = 7 * DAY;
var APPROX_MONTH = 30 * DAY;
var APPROX_YEAR = 365 * DAY;
var MIN_ZOOM_DAYS = 10 / (24 * 60 * 60);
var TICK_LADDER = [
    {
        unit: "second",
        step: 1,
        approxMs: SECOND
    },
    {
        unit: "second",
        step: 5,
        approxMs: 5 * SECOND
    },
    {
        unit: "second",
        step: 15,
        approxMs: 15 * SECOND
    },
    {
        unit: "second",
        step: 30,
        approxMs: 30 * SECOND
    },
    {
        unit: "minute",
        step: 1,
        approxMs: MINUTE
    },
    {
        unit: "minute",
        step: 5,
        approxMs: 5 * MINUTE
    },
    {
        unit: "minute",
        step: 15,
        approxMs: 15 * MINUTE
    },
    {
        unit: "minute",
        step: 30,
        approxMs: 30 * MINUTE
    },
    {
        unit: "hour",
        step: 1,
        approxMs: HOUR
    },
    {
        unit: "hour",
        step: 3,
        approxMs: 3 * HOUR
    },
    {
        unit: "hour",
        step: 6,
        approxMs: 6 * HOUR
    },
    {
        unit: "hour",
        step: 12,
        approxMs: 12 * HOUR
    },
    {
        unit: "day",
        step: 1,
        approxMs: DAY
    },
    {
        unit: "day",
        step: 2,
        approxMs: 2 * DAY
    },
    {
        unit: "week",
        step: 1,
        approxMs: WEEK
    },
    {
        unit: "week",
        step: 2,
        approxMs: 2 * WEEK
    },
    {
        unit: "month",
        step: 1,
        approxMs: APPROX_MONTH
    },
    {
        unit: "month",
        step: 3,
        approxMs: 3 * APPROX_MONTH
    },
    {
        unit: "month",
        step: 6,
        approxMs: 6 * APPROX_MONTH
    },
    {
        unit: "year",
        step: 1,
        approxMs: APPROX_YEAR
    },
    {
        unit: "year",
        step: 2,
        approxMs: 2 * APPROX_YEAR
    },
    {
        unit: "year",
        step: 5,
        approxMs: 5 * APPROX_YEAR
    },
    {
        unit: "year",
        step: 10,
        approxMs: 10 * APPROX_YEAR
    },
    {
        unit: "year",
        step: 25,
        approxMs: 25 * APPROX_YEAR
    },
    {
        unit: "year",
        step: 50,
        approxMs: 50 * APPROX_YEAR
    },
    {
        unit: "year",
        step: 100,
        approxMs: 100 * APPROX_YEAR
    }
];
var DEFAULT_TICK_COUNT = 10;
var TimeScale = /*#__PURE__*/ function() {
    "use strict";
    function TimeScale(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, TimeScale);
        this.w = w;
        this.ctx = ctx;
        this.tickInterval = null;
        this.timeScaleArray = [];
        this.utc = w.config.xaxis.labels.datetimeUTC;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(TimeScale, [
        {
            /**
   * Compute raw ticks for a datetime axis spanning [minX, maxX]. Single
   * uniform stride — one unit, no promotion, no calendar-boundary overlay.
   * Context (year, day) is folded into each label's format string at
   * `formatDates` time, not split across promoted ticks.
   *
   * @param {number} minX
   * @param {number} maxX
   * @returns {Array<any>} raw ticks (pre-formatting). Pass to recalcDimensionsBasedOnFormat for the final timescaleLabels.
   */ key: "calculateTimeScaleTicks",
            value: function calculateTimeScaleTicks(minX, maxX) {
                var w = this.w;
                if (w.globals.allSeriesCollapsed) {
                    w.labelData.labels = [];
                    w.labelData.timescaleLabels = [];
                    this.timeScaleArray = [];
                    return [];
                }
                var span = maxX - minX;
                var daysDiff = span / DAY;
                w.interact.disableZoomIn = false;
                w.interact.disableZoomOut = false;
                if (daysDiff < MIN_ZOOM_DAYS) {
                    w.interact.disableZoomIn = true;
                } else if (daysDiff > 5e4) {
                    w.interact.disableZoomOut = true;
                }
                var targetCount = Number.isFinite(w.config.xaxis.tickAmount) ? /** @type {number} */ w.config.xaxis.tickAmount : DEFAULT_TICK_COUNT;
                this.tickInterval = pickInterval(span, targetCount);
                var ticks = this.generateBaseTicks(minX, maxX, this.tickInterval);
                this.timeScaleArray = ticks;
                return ticks;
            }
        },
        {
            /**
   * Walk the interval stride from `minX` to `maxX`. Every tick carries
   * `unit: interval.unit` — no promotion, no snapping. Uniform spacing is
   * guaranteed.
   *
   * @param {number} minX
   * @param {number} maxX
   * @param {{ unit: string, step: number }} interval
   * @returns {Array<any>}
   */ key: "generateBaseTicks",
            value: function generateBaseTicks(minX, maxX, interval) {
                var w = this.w;
                var dt = new DateTime(w);
                var isUTC = this.utc;
                var gridWidth = w.layout.gridWidth;
                var span = maxX - minX;
                var ticks = [];
                var t = dt.ceilToBoundary(minX, /** @type {any} */ interval.unit, interval.step, isUTC);
                var iter = 0;
                var MAX_ITER = 5e3;
                while(t <= maxX && iter < MAX_ITER){
                    var f = dt.getDateFields(t, isUTC);
                    var position = span > 0 ? (t - minX) / span * gridWidth : 0;
                    ticks.push({
                        timestamp: t,
                        position: position,
                        unit: interval.unit,
                        year: f.year,
                        month: f.month + 1,
                        day: f.date,
                        hour: f.hour,
                        minute: f.minute,
                        second: f.second,
                        value: t
                    });
                    t = dt.addInterval(t, /** @type {any} */ interval.unit, interval.step, isUTC);
                    iter++;
                }
                return ticks;
            }
        },
        {
            /**
   * Public entry called from Core.js after calculateTimeScaleTicks. Formats
   * the raw ticks into display labels, removes overlapping entries, writes
   * the result to `w.labelData.timescaleLabels`, and re-runs Dimensions to
   * lay out the grid based on the final label widths.
   *
   * @param {Array<any>} rawTicks
   */ key: "recalcDimensionsBasedOnFormat",
            value: function recalcDimensionsBasedOnFormat(rawTicks) {
                var w = this.w;
                var formatted = this.formatDates(rawTicks);
                var filtered = this.removeOverlappingTS(formatted);
                w.labelData.timescaleLabels = filtered.slice();
                var dimensions = new Dimensions(this.w, this.ctx);
                var layoutState = dimensions.plotCoords();
                this.ctx._writeLayoutCoords(layoutState.layout);
            }
        },
        {
            /**
   * Format each raw tick into a display label. All ticks share one
   * effective format computed once from the interval unit and the data
   * range — when the range spans coarser units, the base format from
   * `datetimeFormatter[unit]` is automatically extended with the higher-
   * unit context (e.g. month-scale spanning years → `MMM yyyy`, hour-scale
   * spanning days → `dd MMM HH:mm`). A user-supplied `xaxis.labels.format`
   * overrides everything.
   *
   * @param {Array<any>} rawTicks
   * @returns {Array<any>}
   */ key: "formatDates",
            value: function formatDates(rawTicks) {
                var w = this.w;
                var dt = new DateTime(w);
                var userFormat = w.config.xaxis.labels.format;
                var dtFmt = w.config.xaxis.labels.datetimeFormatter;
                var isUTC = this.utc;
                var pad = function pad(n) {
                    var len = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 2;
                    return String(n).padStart(len, "0");
                };
                var effectiveFormat = userFormat ? userFormat : this._effectiveFormat(rawTicks, dtFmt);
                return rawTicks.map(function(tick) {
                    var date = dt.getDate(tick.timestamp);
                    var value = dt.formatDate(date, effectiveFormat);
                    var ds = "".concat(tick.year, "-").concat(pad(tick.month), "-").concat(pad(tick.day), "T").concat(pad(tick.hour), ":").concat(pad(tick.minute), ":").concat(pad(tick.second), ".000").concat(isUTC ? "Z" : "");
                    return {
                        dateString: ds,
                        position: tick.position,
                        value: value,
                        unit: tick.unit,
                        year: tick.year,
                        month: tick.month
                    };
                });
            }
        },
        {
            /**
   * Pick the format string used for every tick this render. Folds coarser
   * context into the base `datetimeFormatter[unit]` when the data range
   * spans it. Skipped when the base format already references the higher
   * unit's tokens (so user customizations aren't doubled).
   *
   * @param {Array<any>} rawTicks
   * @param {Record<string, string>} dtFmt
   * @returns {string}
   */ key: "_effectiveFormat",
            value: function _effectiveFormat(rawTicks, dtFmt) {
                if (rawTicks.length === 0) return dtFmt.day || "dd MMM";
                var unit = this.tickInterval && this.tickInterval.unit || rawTicks[0].unit;
                var base = dtFmt[unit === "week" ? "day" : unit] || dtFmt.day || "dd MMM";
                var first = rawTicks[0];
                var last = rawTicks[rawTicks.length - 1];
                var spansYears = first.year !== last.year;
                var spansMonths = spansYears || first.month !== last.month;
                var spansDays = spansMonths || first.day !== last.day;
                var hasYearToken = /y/i.test(base);
                var hasMonthToken = /M/.test(base);
                var hasDayToken = /d/i.test(base);
                if (unit === "month" || unit === "week") {
                    if (spansYears && !hasYearToken) return base + " yyyy";
                    return base;
                }
                if (unit === "day") {
                    if (spansYears && !hasYearToken) return base + " yyyy";
                    return base;
                }
                if (unit === "hour" || unit === "minute" || unit === "second") {
                    if (spansDays && !hasDayToken && !hasMonthToken) {
                        var prefix = spansYears ? "dd MMM yyyy" : "dd MMM";
                        return prefix + " " + base;
                    }
                    return base;
                }
                return base;
            }
        },
        {
            /**
   * Drop labels that would overlap their predecessor (when
   * `xaxis.labels.hideOverlappingLabels` is true). The first label is always
   * kept. Width is measured per-label unless all labels have the same string
   * length, in which case one measurement is reused.
   *
   * @param {Array<any>} arr
   * @returns {Array<any>}
   */ key: "removeOverlappingTS",
            value: function removeOverlappingTS(arr) {
                if (arr.length === 0) return [];
                var w = this.w;
                var graphics = new Graphics(w);
                var equalLabelLengthFlag = false;
                var constantLabelWidth;
                if (arr[0].value && arr.every(function(lb) {
                    return lb.value.length === arr[0].value.length;
                })) {
                    equalLabelLengthFlag = true;
                    constantLabelWidth = graphics.getTextRects(arr[0].value, w.config.xaxis.labels.style.fontSize).width;
                }
                var lastDrawnIndex = 0;
                var filtered = arr.map(function(item, index) {
                    if (index === 0) return item;
                    if (!w.config.xaxis.labels.hideOverlappingLabels) return item;
                    var prevLabelWidth = equalLabelLengthFlag ? /** @type {number} */ constantLabelWidth : graphics.getTextRects(arr[lastDrawnIndex].value, w.config.xaxis.labels.style.fontSize).width;
                    var prevPos = arr[lastDrawnIndex].position;
                    var pos = item.position;
                    if (pos > prevPos + prevLabelWidth + 10) {
                        lastDrawnIndex = index;
                        return item;
                    }
                    return null;
                }).filter(function(f) {
                    return f !== null;
                });
                return filtered;
            }
        }
    ]);
    return TimeScale;
}();
function pickInterval(span, targetCount) {
    if (!Number.isFinite(targetCount) || targetCount <= 0) {
        targetCount = DEFAULT_TICK_COUNT;
    }
    if (span <= 0) return TICK_LADDER[0];
    var targetMs = span / targetCount;
    var best = TICK_LADDER[0];
    var bestDist = Infinity;
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    try {
        for(var _iterator = TICK_LADDER[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var interval = _step.value;
            var dist = Math.abs(Math.log(interval.approxMs / targetMs));
            if (dist < bestDist) {
                bestDist = dist;
                best = interval;
            }
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return best;
}
var REGISTRY_KEY$2 = "__apexcharts_registry__";
var CUSTOM_KEY = "__apexcharts_custom_types__";
if (!/** @type {any} */ globalThis[REGISTRY_KEY$2]) {
    globalThis[REGISTRY_KEY$2] = {};
}
if (!/** @type {any} */ globalThis[CUSTOM_KEY]) {
    globalThis[CUSTOM_KEY] = /* @__PURE__ */ new Set();
}
function getRegistry$1() {
    return /** @type {any} */ globalThis[REGISTRY_KEY$2];
}
function getCustomTypes() {
    return /** @type {any} */ globalThis[CUSTOM_KEY];
}
function markCustom(name2) {
    getCustomTypes().add(name2);
}
function isCustom(name2) {
    return getCustomTypes().has(name2);
}
function hasChartClass(type) {
    return !!getRegistry$1()[type];
}
function unregister(name2) {
    delete getRegistry$1()[name2];
    getCustomTypes().delete(name2);
}
function register(typeMap) {
    Object.assign(getRegistry$1(), typeMap);
}
function getChartClass(type) {
    var Cls = getRegistry$1()[type];
    if (!Cls) {
        throw new Error('ApexCharts: chart type "'.concat(type, '" is not registered. Import it via ApexCharts.use() or use the full apexcharts bundle.'));
    }
    return Cls;
}
var Core = /*#__PURE__*/ function() {
    "use strict";
    function Core(el, w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Core);
        this.w = w;
        this.ctx = ctx;
        this.el = el;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Core, [
        {
            key: "setupElements",
            value: function setupElements() {
                var _this_w = this.w, gl = _this_w.globals, cnf = _this_w.config;
                var ct = cnf.chart.type;
                var xyChartsArrTypes = [
                    "line",
                    "area",
                    "bar",
                    "rangeBar",
                    "rangeArea",
                    "candlestick",
                    "boxPlot",
                    "violin",
                    "scatter",
                    "bubble"
                ];
                var axisChartsArrTypes = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(xyChartsArrTypes).concat([
                    "radar",
                    "heatmap",
                    "treemap"
                ]);
                var isCustomType = !axisChartsArrTypes.includes(ct) && ![
                    "pie",
                    "donut",
                    "polarArea",
                    "radialBar"
                ].includes(ct) && isCustom(ct);
                gl.axisCharts = axisChartsArrTypes.includes(ct) || isCustomType;
                gl.xyCharts = xyChartsArrTypes.includes(ct) || isCustomType;
                gl.isBarHorizontal = [
                    "bar",
                    "rangeBar",
                    "boxPlot",
                    "violin"
                ].includes(ct) && cnf.plotOptions.bar.horizontal;
                gl.chartClass = ".apexcharts".concat(gl.chartID);
                this.w.dom.baseEl = this.el;
                this.w.dom.elWrap = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                Graphics.setAttrs(this.w.dom.elWrap, {
                    id: gl.chartClass.substring(1),
                    class: "apexcharts-canvas ".concat(gl.chartClass.substring(1))
                });
                this.el.appendChild(this.w.dom.elWrap);
                var SVG2 = /** @type {any} */ globalThis.SVG;
                this.w.dom.Paper = SVG2().addTo(this.w.dom.elWrap);
                this.w.dom.Paper.attr({
                    class: "apexcharts-svg",
                    "xmlns:data": "ApexChartsNS",
                    transform: "translate(".concat(cnf.chart.offsetX, ", ").concat(cnf.chart.offsetY, ")")
                });
                this.w.dom.Paper.node.style.background = cnf.theme.mode === "dark" && !cnf.chart.background ? "#343A3F" : cnf.theme.mode === "light" && !cnf.chart.background ? "#fff" : cnf.chart.background;
                this.setSVGDimensions();
                this.w.dom.elLegendForeign = BrowserAPIs.createElementNS(SVGNS$2, "foreignObject");
                Graphics.setAttrs(this.w.dom.elLegendForeign, {
                    x: 0,
                    y: 0,
                    width: gl.svgWidth,
                    height: gl.svgHeight
                });
                this.w.dom.elLegendWrap = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                this.w.dom.elLegendWrap.classList.add("apexcharts-legend");
                this.w.dom.elWrap.appendChild(this.w.dom.elLegendWrap);
                this.w.dom.Paper.node.appendChild(this.w.dom.elLegendForeign);
                if (cnf.chart.accessibility.enabled && cnf.chart.accessibility.announcements.enabled) {
                    var srStatus = BrowserAPIs.createElement("div");
                    srStatus.className = "apexcharts-sr-status";
                    srStatus.setAttribute("role", "status");
                    srStatus.setAttribute("aria-live", "polite");
                    srStatus.setAttribute("aria-atomic", "true");
                    this.w.dom.elWrap.appendChild(srStatus);
                }
                if (cnf.chart.accessibility.enabled) {
                    var ariaLabel = this.getAccessibleChartLabel();
                    var svgRole = cnf.chart.accessibility.keyboard.enabled && cnf.chart.accessibility.keyboard.navigation.enabled ? "application" : "img";
                    this.w.dom.Paper.attr({
                        role: svgRole,
                        "aria-label": ariaLabel
                    });
                    if (cnf.chart.accessibility.description) {
                        var descEl = BrowserAPIs.createElementNS(SVGNS$2, "desc");
                        descEl.textContent = cnf.chart.accessibility.description;
                        this.w.dom.Paper.node.insertBefore(descEl, this.w.dom.elLegendForeign.nextSibling);
                    }
                }
                this.w.dom.elGraphical = this.w.dom.Paper.group().attr({
                    class: "apexcharts-inner apexcharts-graphical"
                });
                this.w.dom.elDefs = this.w.dom.Paper.defs();
                this.w.dom.Paper.add(this.w.dom.elGraphical);
                this.w.dom.elGraphical.add(this.w.dom.elDefs);
            }
        },
        {
            /**
   * Classify each series by its resolved chart type into per-type render
   * buckets (`{ series, i }`, plus `seriesRangeEnd` for rangeArea). Side
   * effects mirror the original inline code: sets `w.globals.columnSeries`,
   * folds `w.globals.comboCharts`, and warns on unsupported combinations (a
   * non-combo type mixed in, or horizontal bars in a combo). Extracted from
   * plotChartType to shrink it (audit C2).
   * @param {any[]} ser
   * @returns {{ seriesTypes: Record<string, any>, customBuckets: Record<string, {series: any[], i: number[]}> }}
   */ key: "_classifySeriesByType",
            value: function _classifySeriesByType(ser) {
                var _this = this;
                var w = this.w;
                var cnf = w.config, gl = w.globals;
                var seriesTypes = {
                    line: {
                        series: [],
                        i: []
                    },
                    area: {
                        series: [],
                        i: []
                    },
                    scatter: {
                        series: [],
                        i: []
                    },
                    bubble: {
                        series: [],
                        i: []
                    },
                    bar: {
                        series: [],
                        i: []
                    },
                    candlestick: {
                        series: [],
                        i: []
                    },
                    boxPlot: {
                        series: [],
                        i: []
                    },
                    violin: {
                        series: [],
                        i: []
                    },
                    rangeBar: {
                        series: [],
                        i: []
                    },
                    rangeArea: {
                        series: [],
                        seriesRangeEnd: [],
                        i: []
                    }
                };
                var customBuckets = {};
                var chartType = cnf.chart.type || "line";
                var nonComboType = null;
                var comboCount = 0;
                this.w.seriesData.series.forEach(function(serie, st) {
                    var _a, _b;
                    var seriesType = ((_a = ser[st]) == null ? void 0 : _a.type) === "column" ? "bar" : ((_b = ser[st]) == null ? void 0 : _b.type) || (chartType === "column" ? "bar" : chartType);
                    var st_ = /** @type {Record<string,any>} */ seriesTypes;
                    if (st_[seriesType]) {
                        if (seriesType === "rangeArea") {
                            st_[seriesType].series.push(_this.w.rangeData.seriesRangeStart[st]);
                            st_[seriesType].seriesRangeEnd.push(_this.w.rangeData.seriesRangeEnd[st]);
                        } else {
                            st_[seriesType].series.push(serie);
                        }
                        st_[seriesType].i.push(st);
                        if (seriesType === "bar") w.globals.columnSeries = seriesTypes.bar;
                    } else if ([
                        "heatmap",
                        "treemap",
                        "pie",
                        "donut",
                        "polarArea",
                        "radialBar",
                        "radar",
                        "unit",
                        "sunburst"
                    ].includes(seriesType)) {
                        nonComboType = seriesType;
                    } else if (isCustom(seriesType)) {
                        if (!customBuckets[seriesType]) {
                            customBuckets[seriesType] = {
                                series: [],
                                i: []
                            };
                        }
                        customBuckets[seriesType].series.push(serie);
                        customBuckets[seriesType].i.push(st);
                    } else {
                        console.warn("You have specified an unrecognized series type (".concat(seriesType, ")."));
                    }
                    if (chartType !== seriesType && seriesType !== "scatter") comboCount++;
                });
                if (comboCount > 0) {
                    if (nonComboType) {
                        console.warn("Chart or series type ".concat(nonComboType, " cannot appear with other chart or series types."));
                    }
                    if (seriesTypes.bar.series.length > 0 && cnf.plotOptions.bar.horizontal) {
                        comboCount -= seriesTypes.bar.series.length;
                        seriesTypes.bar = {
                            series: [],
                            i: []
                        };
                        w.globals.columnSeries = {
                            series: [],
                            i: []
                        };
                        console.warn("Horizontal bars are not supported in a mixed/combo chart. Please turn off `plotOptions.bar.horizontal`");
                    }
                }
                gl.comboCharts || (gl.comboCharts = comboCount > 0);
                return {
                    seriesTypes: seriesTypes,
                    customBuckets: customBuckets
                };
            }
        },
        {
            /**
   * @param {any[]} ser
   * @param {import('../types/internal').XYRatios} xyRatios
   */ key: "plotChartType",
            value: function plotChartType(ser, xyRatios) {
                var _this = this, w = _this.w, ctx = _this.ctx;
                var canvasMode = ctx.renderer && ctx.renderer.kind === "canvas";
                if (canvasMode) ctx.renderer.beginSeries();
                var _this__classifySeriesByType = this._classifySeriesByType(ser), seriesTypes = _this__classifySeriesByType.seriesTypes, customBuckets = _this__classifySeriesByType.customBuckets;
                var renderers = this._instantiateSeriesRenderers(seriesTypes, xyRatios);
                var elGraph = this._assembleSeriesGraphics(seriesTypes, customBuckets, renderers, xyRatios);
                if (canvasMode) {
                    var rr = /** @type {any} */ ctx.renderer;
                    if (rr && rr._repaintHostInPlace) {
                        rr._repaintHostInPlace = false;
                        var wrapNode = this.w.dom.elGraphical.node.querySelector(".apexcharts-canvas-series-wrap");
                        if (wrapNode && rr.canRepaintInPlace && rr.canRepaintInPlace()) {
                            rr.repaintInPlace();
                            var groups = Array.isArray(elGraph) ? elGraph : [
                                elGraph
                            ];
                            groups.forEach(function(g) {
                                if (g && g.node) wrapNode.appendChild(g.node);
                            });
                            return [];
                        }
                    }
                    var host = ctx.renderer.present();
                    if (host) {
                        var wrap = new Graphics(w).group({
                            class: "apexcharts-canvas-series-wrap"
                        });
                        wrap.add(host);
                        var groups1 = Array.isArray(elGraph) ? elGraph : [
                            elGraph
                        ];
                        groups1.forEach(function(g) {
                            if (g) wrap.add(g);
                        });
                        return wrap;
                    }
                }
                return elGraph;
            }
        },
        {
            /**
   * Lazily instantiate the shared series renderers the orchestration below
   * needs. getChartClass() is only called for types actually present, so a page
   * that registers just 'line' never triggers a lookup for 'candlestick' etc.
   * (tree-shaking). Returns the line/candlestick/violin renderers and sets
   * ctx.pie / ctx.rangeBar as a side effect (both may be null). Extracted from
   * plotChartType (audit C2).
   * @param {Record<string, any>} seriesTypes
   * @param {import('../types/internal').XYRatios} xyRatios
   * @returns {{ line: any, boxCandlestick: any, violin: any }}
   */ key: "_instantiateSeriesRenderers",
            value: function _instantiateSeriesRenderers(seriesTypes, xyRatios) {
                var _this = this, w = _this.w, ctx = _this.ctx;
                var cnf = w.config, gl = w.globals;
                var needsLine = seriesTypes.line.series.length > 0 || seriesTypes.area.series.length > 0 || seriesTypes.scatter.series.length > 0 || seriesTypes.bubble.series.length > 0 || seriesTypes.rangeArea.series.length > 0 || !gl.comboCharts && [
                    "line",
                    "area",
                    "scatter",
                    "bubble",
                    "rangeArea"
                ].includes(cnf.chart.type);
                var line = needsLine ? new (getChartClass("line"))(ctx.w, ctx, xyRatios) : null;
                var needsCandlestick = seriesTypes.candlestick.series.length > 0 || seriesTypes.boxPlot.series.length > 0 || !gl.comboCharts && [
                    "candlestick",
                    "boxPlot"
                ].includes(cnf.chart.type);
                var boxCandlestick = needsCandlestick ? new (getChartClass("candlestick"))(ctx.w, ctx, xyRatios) : null;
                var needsViolin = seriesTypes.violin.series.length > 0 || !gl.comboCharts && cnf.chart.type === "violin";
                var violin = needsViolin ? new (getChartClass("violin"))(ctx.w, ctx, xyRatios) : null;
                var needsPie = !gl.comboCharts && [
                    "pie",
                    "donut",
                    "polarArea"
                ].includes(cnf.chart.type);
                ctx.pie = needsPie ? new (getChartClass("pie"))(ctx.w, ctx) : null;
                var needsRangeBar = seriesTypes.rangeBar.series.length > 0 || !gl.comboCharts && cnf.chart.type === "rangeBar";
                ctx.rangeBar = needsRangeBar ? new (getChartClass("rangeBar"))(ctx.w, ctx, xyRatios) : null;
                return {
                    line: line,
                    boxCandlestick: boxCandlestick,
                    violin: violin
                };
            }
        },
        {
            /**
   * Build the ordered list of series graphic groups (elGraph) from the
   * classified buckets. Combo charts layer per-type draws in z-order; a
   * single-type chart dispatches on cnf.chart.type. Sets ctx.bar for the
   * non-stacked bar paths (side effect, preserved). Extracted from
   * plotChartType (audit C2).
   * @param {Record<string, any>} seriesTypes
   * @param {Record<string, {series: any[], i: number[]}>} customBuckets
   * @param {{ line: any, boxCandlestick: any, violin: any }} renderers
   * @param {import('../types/internal').XYRatios} xyRatios
   * @returns {any}
   */ key: "_assembleSeriesGraphics",
            value: function _assembleSeriesGraphics(seriesTypes, customBuckets, renderers, xyRatios) {
                var _this = this, w = _this.w, ctx = _this.ctx;
                var cnf = w.config, gl = w.globals;
                var line = renderers.line, boxCandlestick = renderers.boxCandlestick, violin = renderers.violin;
                var elGraph = [];
                if (gl.comboCharts) {
                    var coreUtils = new CoreUtils(this.w);
                    if (seriesTypes.area.series.length > 0) {
                        var _elGraph;
                        (_elGraph = elGraph).push.apply(_elGraph, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(coreUtils.drawSeriesByGroup(seriesTypes.area, gl.areaGroups, "area", line)));
                    }
                    if (seriesTypes.bar.series.length > 0) {
                        if (cnf.chart.stacked) {
                            var barStacked = new (getChartClass("barStacked"))(ctx.w, ctx, xyRatios);
                            elGraph.push(barStacked.draw(seriesTypes.bar.series, seriesTypes.bar.i));
                        } else {
                            ctx.bar = new (getChartClass("bar"))(ctx.w, ctx, xyRatios);
                            elGraph.push(ctx.bar.draw(seriesTypes.bar.series, seriesTypes.bar.i));
                        }
                    }
                    if (seriesTypes.rangeArea.series.length > 0) {
                        elGraph.push(line.draw(seriesTypes.rangeArea.series, "rangeArea", seriesTypes.rangeArea.i, seriesTypes.rangeArea.seriesRangeEnd));
                    }
                    if (seriesTypes.line.series.length > 0) {
                        var _elGraph1;
                        (_elGraph1 = elGraph).push.apply(_elGraph1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(coreUtils.drawSeriesByGroup(seriesTypes.line, gl.lineGroups, "line", line)));
                    }
                    if (seriesTypes.candlestick.series.length > 0) {
                        elGraph.push(boxCandlestick.draw(seriesTypes.candlestick.series, "candlestick", seriesTypes.candlestick.i));
                    }
                    if (seriesTypes.boxPlot.series.length > 0) {
                        elGraph.push(boxCandlestick.draw(seriesTypes.boxPlot.series, "boxPlot", seriesTypes.boxPlot.i));
                    }
                    if (seriesTypes.violin.series.length > 0) {
                        elGraph.push(violin.draw(seriesTypes.violin.series, "violin", seriesTypes.violin.i));
                    }
                    if (seriesTypes.rangeBar.series.length > 0) {
                        elGraph.push(ctx.rangeBar.draw(seriesTypes.rangeBar.series, seriesTypes.rangeBar.i));
                    }
                    if (seriesTypes.scatter.series.length > 0) {
                        var scatterLine = new (getChartClass("line"))(ctx.w, ctx, xyRatios, true);
                        elGraph.push(scatterLine.draw(seriesTypes.scatter.series, "scatter", seriesTypes.scatter.i));
                    }
                    if (seriesTypes.bubble.series.length > 0) {
                        var bubbleLine = new (getChartClass("line"))(ctx.w, ctx, xyRatios, true);
                        elGraph.push(bubbleLine.draw(seriesTypes.bubble.series, "bubble", seriesTypes.bubble.i));
                    }
                    Object.keys(customBuckets).forEach(function(cname) {
                        var bucket = customBuckets[cname];
                        if (bucket.series.length > 0) {
                            var cs = new (getChartClass(cname))(ctx.w, ctx, xyRatios);
                            elGraph.push(cs.draw(bucket.series, cname, bucket.i));
                        }
                    });
                } else {
                    var type = cnf.chart.type;
                    switch(type){
                        case "line":
                            elGraph = line.draw(this.w.seriesData.series, "line");
                            break;
                        case "area":
                            elGraph = line.draw(this.w.seriesData.series, "area");
                            break;
                        case "bar":
                            if (cnf.chart.stacked) {
                                var barStacked1 = new (getChartClass("barStacked"))(ctx.w, ctx, xyRatios);
                                elGraph = barStacked1.draw(this.w.seriesData.series);
                            } else {
                                ctx.bar = new (getChartClass("bar"))(ctx.w, ctx, xyRatios);
                                elGraph = ctx.bar.draw(this.w.seriesData.series);
                            }
                            break;
                        case "candlestick":
                            elGraph = boxCandlestick.draw(this.w.seriesData.series, "candlestick");
                            break;
                        case "boxPlot":
                            elGraph = boxCandlestick.draw(this.w.seriesData.series, type);
                            break;
                        case "violin":
                            elGraph = violin.draw(this.w.seriesData.series, "violin");
                            break;
                        case "rangeBar":
                            elGraph = ctx.rangeBar.draw(this.w.seriesData.series);
                            break;
                        case "rangeArea":
                            elGraph = line.draw(this.w.rangeData.seriesRangeStart, "rangeArea", void 0, this.w.rangeData.seriesRangeEnd);
                            break;
                        case "heatmap":
                            {
                                var heatmap = new (getChartClass("heatmap"))(ctx.w, ctx, xyRatios);
                                elGraph = heatmap.draw(this.w.seriesData.series);
                                break;
                            }
                        case "treemap":
                            {
                                var treemap = new (getChartClass("treemap"))(ctx.w, ctx);
                                elGraph = treemap.draw(this.w.seriesData.series);
                                break;
                            }
                        case "unit":
                            {
                                var unit = new (getChartClass("unit"))(ctx.w, ctx);
                                elGraph = unit.draw(this.w.seriesData.series);
                                break;
                            }
                        case "sunburst":
                            {
                                var sunburst = new (getChartClass("sunburst"))(ctx.w, ctx);
                                elGraph = sunburst.draw(this.w.seriesData.series);
                                break;
                            }
                        case "pie":
                        case "donut":
                        case "polarArea":
                            elGraph = ctx.pie.draw(this.w.seriesData.series);
                            break;
                        case "radialBar":
                            {
                                var radialBar = new (getChartClass("radialBar"))(ctx.w, ctx);
                                elGraph = radialBar.draw(this.w.seriesData.series);
                                break;
                            }
                        case "radar":
                            {
                                var radar = new (getChartClass("radar"))(ctx.w, ctx);
                                elGraph = radar.draw(this.w.seriesData.series);
                                break;
                            }
                        default:
                            if (isCustom(type)) {
                                var cs = new (getChartClass(type))(ctx.w, ctx, xyRatios);
                                elGraph = cs.draw(this.w.seriesData.series, type);
                            } else {
                                elGraph = line.draw(this.w.seriesData.series);
                            }
                    }
                }
                return elGraph;
            }
        },
        {
            /**
   * Extract the CSS unit suffix from a chart width/height config value
   * (`'100%'` -> `'%'`, `'300px'` -> `'px'`, `300`/`'300'` -> `''`). Kept as the
   * original digit-split idiom so results are identical for every input the
   * call sites already handle ('%', 'px', '', 'auto').
   * @param {string|number} value
   * @returns {string|undefined}
   */ key: "_extractDimensionUnit",
            value: function _extractDimensionUnit(value) {
                return String(value).split(/[0-9]+/g).pop();
            }
        },
        {
            key: "setSVGDimensions",
            value: function setSVGDimensions() {
                var _a;
                var _this_w = this.w, gl = _this_w.globals, cnf = _this_w.config;
                cnf.chart.width = cnf.chart.width || "100%";
                cnf.chart.height = cnf.chart.height || "auto";
                var rawWidth = cnf.chart.width;
                var rawHeight = cnf.chart.height;
                gl.svgWidth = NaN;
                gl.svgHeight = NaN;
                var elDim = Utils$1.getDimensions(this.el);
                var widthUnit = this._extractDimensionUnit(rawWidth);
                if (widthUnit === "%") {
                    if (Utils$1.isNumber(elDim[0])) {
                        if (elDim[0].width === 0) {
                            elDim = Utils$1.getDimensions(this.el.parentNode);
                        }
                        gl.svgWidth = elDim[0] * parseInt(rawWidth, 10) / 100;
                    }
                } else if (widthUnit === "px" || widthUnit === "") {
                    gl.svgWidth = parseInt(rawWidth, 10);
                }
                var heightUnit = this._extractDimensionUnit(rawHeight);
                if (rawHeight !== "auto" && rawHeight !== "") {
                    if (heightUnit === "%") {
                        var elParentDim = Utils$1.getDimensions(this.el.parentNode);
                        gl.svgHeight = elParentDim[1] * parseInt(rawHeight, 10) / 100;
                    } else {
                        gl.svgHeight = parseInt(rawHeight, 10);
                    }
                } else {
                    gl.svgHeight = gl.axisCharts ? gl.svgWidth / 1.61 : gl.svgWidth / 1.2;
                }
                gl.svgWidth = Math.max(gl.svgWidth, 0);
                gl.svgHeight = Math.max(gl.svgHeight, 0);
                Graphics.setAttrs(this.w.dom.Paper.node, {
                    width: gl.svgWidth,
                    height: gl.svgHeight
                });
                if (heightUnit !== "%" && Environment.isBrowser()) {
                    var needsAxisPadding = gl.axisCharts && (cnf.grid.show || cnf.dataLabels.enabled || cnf.xaxis.labels.show || cnf.xaxis.axisBorder.show || cnf.xaxis.axisTicks.show || cnf.yaxis.some(function(y) {
                        return y.show && (y.labels.show || y.axisBorder.show || y.axisTicks.show);
                    }));
                    var offsetY = cnf.chart.sparkline.enabled || !needsAxisPadding ? 0 : cnf.chart.parentHeightOffset;
                    var paperNode = this.w.dom.Paper.node;
                    if ((_a = paperNode.parentNode) == null ? void 0 : _a.parentNode) {
                        paperNode.parentNode.parentNode.style.minHeight = "".concat(gl.svgHeight + offsetY, "px");
                    }
                }
                this.w.dom.elWrap.style.width = "".concat(gl.svgWidth, "px");
                this.w.dom.elWrap.style.height = "".concat(gl.svgHeight, "px");
                gl.lastResizeSignature = this.getResizeSignature();
            }
        },
        {
            /**
   * A stable fingerprint of the container measurements that actually feed the
   * chart's rendered dimensions, given the width/height config. Only inputs that
   * a resize can change are included: the element width when the width is a
   * percentage, and the parent height when the height is a percentage. A fully
   * pixel-sized chart therefore has a constant signature, so a resize never
   * forces a redraw; a percentage-sized chart's signature tracks the container.
   * The window-resize handler compares this against the last render to skip
   * redraws (and the entrance-animation teardown they cause) that aren't needed.
   * @returns {{ w: number, h: number }}
   */ key: "getResizeSignature",
            value: function getResizeSignature() {
                var _this_w = this.w, cnf = _this_w.config;
                var rawWidth = (cnf.chart.width || "100%").toString().trim();
                var rawHeight = (cnf.chart.height || "auto").toString().trim();
                var w = 0;
                var h = 0;
                if (rawWidth.endsWith("%")) {
                    var elDim = Utils$1.getDimensions(this.el);
                    if (!elDim[0]) elDim = Utils$1.getDimensions(this.el.parentNode);
                    w = elDim[0] || 0;
                }
                if (rawHeight.endsWith("%")) {
                    h = Utils$1.getDimensions(this.el.parentNode)[1] || 0;
                }
                var sig = {
                    w: Math.round(w),
                    h: Math.round(h)
                };
                if (cnf.responsive && cnf.responsive.length && Environment.isBrowser()) {
                    sig.iw = window.innerWidth;
                }
                return sig;
            }
        },
        {
            key: "shiftGraphPosition",
            value: function shiftGraphPosition() {
                var _this_w = this.w, gl = _this_w.globals;
                var tY = gl.translateY, tX = gl.translateX;
                Graphics.setAttrs(this.w.dom.elGraphical.node, {
                    transform: "translate(".concat(tX, ", ").concat(tY, ")")
                });
            }
        },
        {
            key: "resizeNonAxisCharts",
            value: function resizeNonAxisCharts() {
                var _a, _b, _c, _d, _e, _f, _g, _h, _i;
                var w = this.w;
                var heightStr = w.config.chart.height ? String(w.config.chart.height) : "";
                var userSetFixedHeight = heightStr !== "" && heightStr !== "auto";
                var isPercentHeight = heightStr.includes("%");
                var legendHeight = 0;
                var offY = w.config.chart.sparkline.enabled ? 1 : 15;
                offY += w.layout.gridPad.bottom;
                if ([
                    "top",
                    "bottom"
                ].includes(w.config.legend.position) && w.config.legend.show && !w.config.legend.floating) {
                    legendHeight = ((_b = (_a = this.ctx.legend) == null ? void 0 : _a.legendHelpers.getLegendDimensions().clwh) != null ? _b : 0) + 7;
                }
                var el = w.dom.baseEl.querySelector(".apexcharts-radialbar, .apexcharts-pie, .apexcharts-sunburst");
                var externalLabelMarginY = w.globals.pieExternalLabelMarginY || 0;
                var chartInnerDimensions = externalLabelMarginY > 0 ? w.globals.radialSize * 2 + externalLabelMarginY * 2 : w.globals.radialSize * 2.05;
                var angleType = w.config.chart.type === "sunburst" ? "sunburst" : w.config.chart.type === "pie" || w.config.chart.type === "donut" || w.config.chart.type === "polarArea" ? "pie" : "radialBar";
                var radialAngleSpan = Math.abs(w.config.plotOptions[angleType].endAngle - w.config.plotOptions[angleType].startAngle);
                if (el && !w.config.chart.sparkline.enabled && radialAngleSpan < 360) {
                    var svgRect = Utils$1.getBoundingClientRect(this.w.dom.Paper.node);
                    var arcTopFromSVGTop = Infinity;
                    var arcBottomFromSVGTop = -Infinity;
                    var accumulate = function accumulate1(node) {
                        var _a2, _b2, _c2, _d2;
                        if ((_a2 = node.classList) == null ? void 0 : _a2.contains("apexcharts-radialbar-hollow")) {
                            return;
                        }
                        var tag = (_c2 = (_b2 = node.tagName) == null ? void 0 : _b2.toLowerCase) == null ? void 0 : _c2.call(_b2);
                        if (tag === "text" || tag === "tspan") return;
                        var children = Array.from((_d2 = node.children) != null ? _d2 : []);
                        if (children.length > 0) {
                            children.forEach(function(c) {
                                return accumulate(/** @type {Element} */ c);
                            });
                            return;
                        }
                        var r = Utils$1.getBoundingClientRect(node);
                        var height = r.bottom - r.top;
                        if (height > 0) {
                            var top = r.top - svgRect.top;
                            var bottom = r.bottom - svgRect.top;
                            if (top < arcTopFromSVGTop) arcTopFromSVGTop = top;
                            if (bottom > arcBottomFromSVGTop) arcBottomFromSVGTop = bottom;
                        }
                    };
                    Array.from((_c = el.children) != null ? _c : []).forEach(function(c) {
                        return accumulate(/** @type {Element} */ c);
                    });
                    if (!Number.isFinite(arcTopFromSVGTop)) arcTopFromSVGTop = 0;
                    if (!Number.isFinite(arcBottomFromSVGTop)) {
                        var elRect = Utils$1.getBoundingClientRect(el);
                        arcBottomFromSVGTop = elRect.bottom - svgRect.top;
                    }
                    var padding = Math.max(offY, w.globals.radialSize * 0.2);
                    var verticalShift = Math.max(padding - arcTopFromSVGTop, 0);
                    if (verticalShift !== 0) {
                        w.layout.translateY = ((_d = w.layout.translateY) != null ? _d : 0) + verticalShift;
                        Graphics.setAttrs(this.w.dom.elGraphical.node, {
                            transform: "translate(".concat((_e = w.layout.translateX) != null ? _e : 0, ", ").concat(w.layout.translateY, ")")
                        });
                        arcBottomFromSVGTop += verticalShift;
                    }
                    chartInnerDimensions = arcBottomFromSVGTop > 0 ? arcBottomFromSVGTop : w.globals.radialSize * 2.05;
                    var bottomPadding = Math.max(padding, arcTopFromSVGTop);
                    var svgHeight = Math.ceil(chartInnerDimensions + legendHeight + bottomPadding);
                    var chartOffsetY = (_f = w.config.chart.offsetY) != null ? _f : 0;
                    var elWrapHeight = svgHeight + Math.max(chartOffsetY, 0);
                    if (!isPercentHeight) {
                        if (this.w.dom.elLegendForeign) {
                            this.w.dom.elLegendForeign.setAttribute("height", String(elWrapHeight));
                        }
                        this.w.dom.elWrap.style.height = "".concat(elWrapHeight, "px");
                        Graphics.setAttrs(this.w.dom.Paper.node, {
                            height: svgHeight
                        });
                        if (Environment.isBrowser()) {
                            var grandparent = (_g = this.w.dom.Paper.node.parentNode) == null ? void 0 : _g.parentNode;
                            if (grandparent) {
                                grandparent.style.minHeight = "".concat(elWrapHeight, "px");
                            }
                        }
                        w.globals.svgHeight = svgHeight;
                        if (w.config.legend.position === "bottom" && w.config.legend.show && !w.config.legend.floating) {
                            (_h = this.ctx.legend) == null ? void 0 : _h.setLegendWrapXY(20, 0);
                        }
                    }
                    return;
                }
                var newHeight = Math.ceil(chartInnerDimensions + this.w.layout.translateY + legendHeight + offY);
                if (userSetFixedHeight) return;
                if (this.w.dom.elLegendForeign) {
                    this.w.dom.elLegendForeign.setAttribute("height", String(newHeight));
                }
                this.w.dom.elWrap.style.height = "".concat(newHeight, "px");
                Graphics.setAttrs(this.w.dom.Paper.node, {
                    height: newHeight
                });
                if (Environment.isBrowser()) {
                    var grandparent1 = (_i = this.w.dom.Paper.node.parentNode) == null ? void 0 : _i.parentNode;
                    if (grandparent1) {
                        grandparent1.style.minHeight = "".concat(newHeight, "px");
                    }
                }
            }
        },
        {
            key: "coreCalculations",
            value: function coreCalculations() {
                new Range(this.w).init();
            }
        },
        {
            key: "resetGlobals",
            value: function resetGlobals() {
                var _this = this;
                var resetxyValues = function resetxyValues() {
                    return _this.w.config.series.map(function() {
                        return [];
                    });
                };
                var globalObj = new Globals();
                var _this_w = this.w, gl = _this_w.globals;
                var parsingFlags = {
                    dataWasParsed: this.w.axisFlags.dataWasParsed,
                    originalSeries: gl.originalSeries
                };
                globalObj.initGlobalVars(gl);
                gl.seriesXvalues = resetxyValues();
                gl.seriesYvalues = resetxyValues();
                if (parsingFlags.dataWasParsed) {
                    this.w.axisFlags.dataWasParsed = parsingFlags.dataWasParsed;
                    gl.originalSeries = parsingFlags.originalSeries;
                }
            }
        },
        {
            key: "isMultipleY",
            value: function isMultipleY() {
                if (Array.isArray(this.w.config.yaxis) && this.w.config.yaxis.length > 1) {
                    this.w.globals.isMultipleYAxis = true;
                    return true;
                }
                return false;
            }
        },
        {
            key: "xySettings",
            value: function xySettings() {
                var w = this.w;
                var xyRatios = null;
                if (w.globals.axisCharts) {
                    if (w.config.xaxis.crosshairs.position === "back") {
                        new Crosshairs(this.w).drawXCrosshairs();
                    }
                    if (w.config.yaxis[0].crosshairs.position === "back") {
                        new Crosshairs(this.w).drawYCrosshairs();
                    }
                    if (w.config.xaxis.type === "datetime" && w.config.xaxis.labels.formatter === void 0) {
                        this.ctx.timeScale = new TimeScale(this.w, this.ctx);
                        var formattedTimeScale = [];
                        if (isFinite(w.globals.minX) && isFinite(w.globals.maxX) && !w.globals.isBarHorizontal) {
                            formattedTimeScale = this.ctx.timeScale.calculateTimeScaleTicks(w.globals.minX, w.globals.maxX);
                        } else if (w.globals.isBarHorizontal) {
                            formattedTimeScale = this.ctx.timeScale.calculateTimeScaleTicks(w.globals.minY, w.globals.maxY);
                        }
                        this.ctx.timeScale.recalcDimensionsBasedOnFormat(formattedTimeScale);
                    }
                    var coreUtils = new CoreUtils(this.w);
                    xyRatios = coreUtils.getCalculatedRatios();
                }
                return xyRatios;
            }
        },
        {
            /**
   * @param {any} targetChart
   */ key: "updateSourceChart",
            value: function updateSourceChart(targetChart) {
                this.ctx.w.interact.selection = void 0;
                this.ctx.updateHelpers._updateOptions({
                    chart: {
                        selection: {
                            xaxis: {
                                min: targetChart.w.globals.minX,
                                max: targetChart.w.globals.maxX
                            }
                        }
                    }
                }, false, false);
            }
        },
        {
            key: "setupBrushHandler",
            value: function setupBrushHandler() {
                var _this = this;
                var _this1 = this, ctx = _this1.ctx, w = _this1.w;
                if (!w.config.chart.brush.enabled) return;
                if (typeof w.config.chart.events.selection !== "function") {
                    var targets = Array.isArray(w.config.chart.brush.targets) ? w.config.chart.brush.targets : [
                        w.config.chart.brush.target
                    ];
                    targets.forEach(function(target) {
                        var targetChart = /** @type {any} */ ctx.constructor.getChartByID(target);
                        if (!targetChart) {
                            console.warn('ApexCharts: brush target "'.concat(target, '" was not found. Ensure the target chart is rendered (and its chart.id matches) before the brush chart.'));
                            return;
                        }
                        targetChart.w.globals.brushSource = _this.ctx;
                        if (typeof targetChart.w.config.chart.events.zoomed !== "function") {
                            targetChart.w.config.chart.events.zoomed = function() {
                                return _this.updateSourceChart(targetChart);
                            };
                        }
                        if (typeof targetChart.w.config.chart.events.scrolled !== "function") {
                            targetChart.w.config.chart.events.scrolled = function() {
                                return(/**
             * @param {any} chart
             * @param {Event} e
             */ _this.updateSourceChart(targetChart));
                            };
                        }
                    });
                    w.config.chart.events.selection = function(chart, e) {
                        targets.forEach(function(target) {
                            var targetChart = /** @type {any} */ ctx.constructor.getChartByID(target);
                            if (!targetChart) return;
                            targetChart.ctx.updateHelpers._updateOptions({
                                xaxis: {
                                    min: e.xaxis.min,
                                    max: e.xaxis.max
                                }
                            }, false, false, false, false);
                        });
                    };
                }
            }
        },
        {
            key: "getAccessibleChartLabel",
            value: function getAccessibleChartLabel() {
                var w = this.w;
                var cnf = w.config;
                if (cnf.chart.accessibility && cnf.chart.accessibility.description) {
                    return cnf.chart.accessibility.description;
                }
                var chartType = cnf.chart.type;
                var parts = [];
                if (cnf.title.text) {
                    parts.push("".concat(cnf.title.text, ". ").concat(chartType, " chart"));
                    if (cnf.subtitle.text) parts.push(cnf.subtitle.text);
                } else {
                    var namedSeries = function() {
                        if (Array.isArray(w.seriesData.seriesNames) && w.seriesData.seriesNames.length) {
                            return w.seriesData.seriesNames.filter(Boolean);
                        }
                        if (Array.isArray(cnf.series)) {
                            return cnf.series.map(function(s) {
                                return (typeof s === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(s)) === "object" && s !== null ? s.name : null;
                            }).filter(Boolean);
                        }
                        return [];
                    }();
                    var seriesCount = w.seriesData.series.length || (cnf.series ? cnf.series.length : 0);
                    if (namedSeries.length) {
                        parts.push("".concat(chartType, " chart with ").concat(seriesCount, " data series: ").concat(namedSeries.join(", ")));
                    } else {
                        parts.push("".concat(chartType, " chart with ").concat(seriesCount, " data series"));
                    }
                }
                return parts.join(". ");
            }
        }
    ]);
    return Core;
}();
var Data = /*#__PURE__*/ function() {
    "use strict";
    function Data(w) {
        var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref_resetGlobals = _ref.resetGlobals, resetGlobals = _ref_resetGlobals === void 0 ? function() {} : _ref_resetGlobals, _ref_isMultipleY = _ref.isMultipleY, isMultipleY = _ref_isMultipleY === void 0 ? function() {} : _ref_isMultipleY;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Data);
        this.w = w;
        this.resetGlobals = resetGlobals;
        this.isMultipleY = isMultipleY;
        this.twoDSeries = [];
        this.threeDSeries = [];
        this.twoDSeriesX = [];
        this.seriesGoals = [];
        this.coreUtils = new CoreUtils(this.w);
        this.activeSeriesIndex = 0;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Data, [
        {
            // Helper to get the first valid data point from the active series
            key: "getFirstDataPoint",
            value: function getFirstDataPoint() {
                var series = this.w.config.series;
                var sr = new Series(this.w);
                this.activeSeriesIndex = sr.getActiveConfigSeriesIndex();
                var activeItem = /** @type {any} */ series[this.activeSeriesIndex];
                if (activeItem && activeItem.data && activeItem.data.length > 0 && activeItem.data[0] !== null && typeof activeItem.data[0] !== "undefined") {
                    return activeItem.data[0];
                }
                return null;
            }
        },
        {
            key: "isMultiFormat",
            value: function isMultiFormat() {
                return this.isFormatXY() || this.isFormat2DArray();
            }
        },
        {
            // given format is [{x, y}, {x, y}]
            key: "isFormatXY",
            value: function isFormatXY() {
                var _a;
                var firstDataPoint = this.getFirstDataPoint();
                if (!firstDataPoint || typeof firstDataPoint.x === "undefined") return false;
                var data = /** @type {any} */ (_a = this.w.config.series[this.activeSeriesIndex]) == null ? void 0 : _a.data;
                if (data) {
                    var isXY = function isXY(pt) {
                        return pt && typeof pt.x !== "undefined";
                    };
                    for(var k = 1; k < Math.min(3, data.length); k++){
                        if (isXY(data[k]) !== true) {
                            console.warn("ApexCharts: series data has mixed formats starting at index ".concat(k));
                            break;
                        }
                    }
                }
                return true;
            }
        },
        {
            // given format is [[x, y], [x, y]]
            key: "isFormat2DArray",
            value: function isFormat2DArray() {
                var firstDataPoint = this.getFirstDataPoint();
                return firstDataPoint && Array.isArray(firstDataPoint);
            }
        },
        {
            /**
   * Typed single pass for the dominant [[x, y], ...] shape: scalar numeric or
   * null y, no z, no OHLC tuples. One monomorphic loop fills preallocated
   * x/y arrays and fuses the y-extrema scan that Range.getMinYMaxY would
   * otherwise repeat over every value (the extrema entry is ref+length
   * guarded, so any later reshaping of the series array simply falls back to
   * the scan). Returns false untouched on any non-conforming point so the
   * general loop below handles mixed/exotic data with unchanged output.
   * @param {any[]} data
   * @param {number} i
   * @returns {boolean}
   */ key: "_fast2DArrayParse",
            value: function _fast2DArrayParse(data, i) {
                var _a, _b;
                var n = data.length;
                if (n === 0) return false;
                var ys = new Array(n);
                var xs = new Array(n);
                var maxY = -Number.MAX_VALUE;
                var lowestY = Number.MAX_VALUE;
                var negMinY = Infinity;
                var hasNulls = false;
                var yDec = 0;
                var xNumeric = true;
                var minX = Infinity;
                var maxX = -Infinity;
                var xSorted = true;
                var minXDiff = Infinity;
                var prevX = NaN;
                for(var j = 0; j < n; j++){
                    var point = data[j];
                    if (!Array.isArray(point) || point.length > 2) return false;
                    var x = point[0];
                    var y = point[1];
                    if (xNumeric) {
                        if (typeof x === "number") {
                            if (x === x) {
                                if (x < minX) minX = x;
                                if (x > maxX) maxX = x;
                            }
                            var d = x - prevX;
                            if (d > 0) {
                                if (d < minXDiff) minXDiff = d;
                            } else if (d < 0) {
                                xSorted = false;
                            }
                            prevX = x;
                        } else {
                            xNumeric = false;
                        }
                    }
                    if (typeof y === "number") {
                        if (y === y && y !== Infinity && y !== -Infinity) {
                            if (y > maxY) maxY = y;
                            if (y < lowestY) lowestY = y;
                            if (y < 0 && y < negMinY) negMinY = y;
                            if (!Number.isInteger(y)) {
                                var av = y < 0 ? -y : y;
                                if (av >= 1e-6 && av < 1e21) {
                                    var str = "" + y;
                                    var dot = str.indexOf(".");
                                    var dec = dot === -1 ? 0 : str.length - dot - 1;
                                    if (dec > yDec) yDec = dec;
                                } else {
                                    var nv = Utils$1.noExponents(y);
                                    if (Utils$1.isFloat(nv)) {
                                        yDec = Math.max(yDec, nv.toString().split(".")[1].length);
                                    }
                                }
                            }
                        } else {
                            hasNulls = true;
                        }
                    } else if (y === null) {
                        hasNulls = true;
                    } else {
                        return false;
                    }
                    ys[j] = y;
                    xs[j] = x;
                }
                this.twoDSeries = ys;
                this.twoDSeriesX = xs;
                this.w.axisFlags.dataFormatXNumeric = true;
                var extrema = (_b = (_a = this.w.seriesData)._parsedExtrema) != null ? _b : _a._parsedExtrema = [];
                extrema[i] = {
                    ref: ys,
                    len: n,
                    maxY: maxY,
                    lowestY: lowestY,
                    negMinY: negMinY,
                    hasNulls: hasNulls,
                    yDec: yDec,
                    xref: xs,
                    xNumeric: xNumeric,
                    minX: minX,
                    maxX: maxX,
                    xSorted: xSorted,
                    minXDiff: minXDiff
                };
                return true;
            }
        },
        {
            /**
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleFormat2DArray",
            value: function handleFormat2DArray(ser, i) {
                var cnf = this.w.config;
                var data = ser[i].data;
                var isBoxPlot = cnf.chart.type === "boxPlot" || /** @type {any} */ cnf.series[i].type === "boxPlot";
                if (!isBoxPlot && cnf.xaxis.type !== "datetime" && this._fast2DArrayParse(data, i)) {
                    return;
                }
                for(var j = 0; j < data.length; j++){
                    var point = data[j];
                    var x = point[0];
                    var y = point[1];
                    var z = point[2];
                    if (typeof y !== "undefined") {
                        if (Array.isArray(y) && y.length === 4 && !isBoxPlot) {
                            this.twoDSeries.push(Utils$1.parseNumber(y[3]));
                        } else if (point.length >= 5) {
                            this.twoDSeries.push(Utils$1.parseNumber(point[4]));
                        } else {
                            this.twoDSeries.push(Utils$1.parseNumber(y));
                        }
                        this.w.axisFlags.dataFormatXNumeric = true;
                    }
                    if (cnf.xaxis.type === "datetime") {
                        var ts = new Date(x).getTime();
                        this.twoDSeriesX.push(ts);
                    } else {
                        this.twoDSeriesX.push(x);
                    }
                    if (typeof z !== "undefined") {
                        this.threeDSeries.push(z);
                        this.w.axisFlags.isDataXYZ = true;
                    }
                }
            }
        },
        {
            /**
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleFormatXY",
            value: function handleFormatXY(ser, i) {
                var cnf = this.w.config;
                var gl = this.w.globals;
                var dt = new DateTime(this.w);
                var data = ser[i].data;
                var activeI = i;
                if (gl.collapsedSeriesIndices.indexOf(i) > -1) {
                    activeI = this.activeSeriesIndex;
                }
                var activeData = ser[activeI].data;
                for(var j = 0; j < data.length; j++){
                    var point = data[j];
                    if (typeof point.y !== "undefined") {
                        var val = Array.isArray(point.y) ? Utils$1.parseNumber(point.y[point.y.length - 1]) : Utils$1.parseNumber(point.y);
                        this.twoDSeries.push(val);
                    }
                    if (typeof this.seriesGoals[i] === "undefined") {
                        this.seriesGoals[i] = [];
                    }
                    if (typeof point.goals !== "undefined" && Array.isArray(point.goals)) {
                        this.seriesGoals[i].push(point.goals);
                    } else {
                        this.seriesGoals[i].push(null);
                    }
                    if (typeof point.z !== "undefined") {
                        this.threeDSeries.push(point.z);
                        this.w.axisFlags.isDataXYZ = true;
                    }
                }
                for(var j1 = 0; j1 < activeData.length; j1++){
                    var point1 = activeData[j1];
                    var x = point1.x;
                    var isXString = typeof x === "string";
                    var isXArr = Array.isArray(x);
                    var isXDate = !isXArr && !!dt.isValidDate(x);
                    if (isXString || isXDate) {
                        if (isXString || cnf.xaxis.convertedCatToNumeric) {
                            var isRangeColumn = gl.isBarHorizontal && this.w.axisFlags.isRangeData;
                            if (cnf.xaxis.type === "datetime" && !isRangeColumn) {
                                this.twoDSeriesX.push(dt.parseDate(x));
                            } else {
                                this.fallbackToCategory = true;
                                this.twoDSeriesX.push(x);
                                if (!isNaN(x) && this.w.config.xaxis.type !== "category" && typeof x !== "string") {
                                    this.w.axisFlags.isXNumeric = true;
                                }
                            }
                        } else {
                            if (cnf.xaxis.type === "datetime") {
                                this.twoDSeriesX.push(dt.parseDate(x.toString()));
                            } else {
                                this.w.axisFlags.dataFormatXNumeric = true;
                                this.w.axisFlags.isXNumeric = true;
                                this.twoDSeriesX.push(parseFloat(x));
                            }
                        }
                    } else if (isXArr) {
                        this.fallbackToCategory = true;
                        this.twoDSeriesX.push(x);
                    } else {
                        this.w.axisFlags.isXNumeric = true;
                        this.w.axisFlags.dataFormatXNumeric = true;
                        this.twoDSeriesX.push(x);
                    }
                }
            }
        },
        {
            /**
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleRangeData",
            value: function handleRangeData(ser, i) {
                var range = {
                    start: [],
                    end: [],
                    rangeUniques: []
                };
                if (this.isFormat2DArray()) {
                    range = this.handleRangeDataFormat("array", ser, i);
                } else if (this.isFormatXY()) {
                    range = this.handleRangeDataFormat("xy", ser, i);
                }
                this.w.rangeData.seriesRangeStart[i] = range.start === void 0 ? [] : range.start;
                this.w.rangeData.seriesRangeEnd[i] = range.end === void 0 ? [] : range.end;
                this.w.rangeData.seriesRange[i] = range.rangeUniques;
                this.w.rangeData.seriesRange.forEach(function(sr) {
                    if (!sr) return;
                    sr.forEach(function(sarr) {
                        var yItems = /** @type {any} */ sarr.y;
                        var len = /** @type {any[]} */ yItems.length;
                        if (len <= 1) return;
                        for(var arri = 0; arri < len; arri++){
                            var arr = /** @type {any} */ yItems[arri];
                            var range1y1 = arr.y1;
                            var range1y2 = arr.y2;
                            for(var sri = arri + 1; sri < len; sri++){
                                var range2 = /** @type {any} */ yItems[sri];
                                var range2y1 = range2.y1;
                                var range2y2 = range2.y2;
                                if (range1y1 <= range2y2 && range2y1 <= range1y2) {
                                    var sarrAny = /** @type {any} */ sarr;
                                    sarrAny.overlaps.add(arr.rangeName);
                                    sarrAny.overlaps.add(range2.rangeName);
                                }
                            }
                        }
                    });
                });
                return range;
            }
        },
        {
            /**
   * Marks (#11) P3: fold a custom series' per-datum y-extent into the
   * range-data slice so both bounds drive the y-axis scale. When `yExtent` is
   * given it supplies the values a datum occupies (scalar or array => min/max
   * across them); otherwise the datum's `y` is used (array => first/last,
   * scalar => itself). The datum still carries a representative scalar `y`
   * (folded by handleFormatXY into seriesData.series) that gates Range.
   * @param {any[]} ser @param {number} i @param {Function|null} yExtent
   */ key: "handleCustomRangeData",
            value: function handleCustomRangeData(ser, i, yExtent) {
                var data = ser[i].data || [];
                var start = [];
                var end = [];
                for(var j = 0; j < data.length; j++){
                    var datum = data[j];
                    var lo = void 0;
                    var hi = void 0;
                    if (typeof yExtent === "function") {
                        var _Math, _Math1;
                        var ext = yExtent(datum, j);
                        if (!Array.isArray(ext)) ext = [
                            ext
                        ];
                        var nums = ext.map(function(v) {
                            return Utils$1.parseNumber(v);
                        }).filter(function(v) {
                            return v !== null && !isNaN(v);
                        });
                        lo = nums.length ? (_Math = Math).min.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(nums)) : null;
                        hi = nums.length ? (_Math1 = Math).max.apply(_Math1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(nums)) : null;
                    } else {
                        var y = datum == null ? null : datum.y;
                        if (Array.isArray(y)) {
                            lo = Utils$1.parseNumber(y[0]);
                            hi = Utils$1.parseNumber(y[y.length - 1]);
                        } else {
                            lo = hi = Utils$1.parseNumber(y);
                        }
                    }
                    start.push(lo);
                    end.push(hi);
                }
                this.w.rangeData.seriesRangeStart[i] = start;
                this.w.rangeData.seriesRangeEnd[i] = end;
            }
        },
        {
            /**
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleCandleStickBoxData",
            value: function handleCandleStickBoxData(ser, i) {
                var ohlc = {
                    o: [],
                    h: [],
                    m: [],
                    l: [],
                    c: []
                };
                if (this.isFormat2DArray()) {
                    ohlc = this.handleCandleStickBoxDataFormat("array", ser, i);
                } else if (this.isFormatXY()) {
                    ohlc = this.handleCandleStickBoxDataFormat("xy", ser, i);
                }
                this.w.candleData.seriesCandleO[i] = ohlc.o;
                this.w.candleData.seriesCandleH[i] = ohlc.h;
                this.w.candleData.seriesCandleM[i] = ohlc.m;
                this.w.candleData.seriesCandleL[i] = ohlc.l;
                this.w.candleData.seriesCandleC[i] = ohlc.c;
                this.w.candleData.seriesBoxPoints[i] = ohlc.points || [];
                return ohlc;
            }
        },
        {
            /**
   * Parse a violin series. Each data point carries a precomputed density
   * profile (the violin shape) and an array of raw observations (the jitter):
   *
   *   { x, y: { density: [[value, weight], ...], points: [v1, v2, ...] } }
   *
   * Array fallback form: [x, densityPairs, pointsArray].
   *
   * Density `weight` need not be normalized — Violin.js scales each violin by
   * its own maxWeight. The representative scalar pushed into the main series
   * (so generic code paths see a non-null y) is the density mode — the value
   * carrying the greatest weight.
   *
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleViolinData",
            value: function handleViolinData(ser, i) {
                var _a, _b, _c, _d, _e, _f;
                var w = this.w;
                var data = ser[i].data;
                var densityArr = [];
                var pointsArr = [];
                var minArr = [];
                var maxArr = [];
                var placeholders = [];
                for(var j = 0; j < data.length; j++){
                    var d = data[j];
                    var dens = (_c = (_b = (_a = d == null ? void 0 : d.y) == null ? void 0 : _a.density) != null ? _b : d == null ? void 0 : d[1]) != null ? _c : [];
                    var pts = (_f = (_e = (_d = d == null ? void 0 : d.y) == null ? void 0 : _d.points) != null ? _e : d == null ? void 0 : d[2]) != null ? _f : [];
                    var values = [];
                    var weights = [];
                    var maxWeight = 0;
                    var modeValue = null;
                    var minVal = Infinity;
                    var maxVal = -Infinity;
                    for(var k = 0; k < dens.length; k++){
                        var v = Utils$1.parseNumber(dens[k][0]);
                        var wt = Utils$1.parseNumber(dens[k][1]);
                        if (v === null || wt === null) continue;
                        values.push(v);
                        weights.push(wt);
                        if (wt > maxWeight) {
                            maxWeight = wt;
                            modeValue = v;
                        }
                        if (v < minVal) minVal = v;
                        if (v > maxVal) maxVal = v;
                    }
                    var cleanPts = [];
                    for(var k1 = 0; k1 < pts.length; k1++){
                        var p = Utils$1.parseNumber(pts[k1]);
                        if (p === null) continue;
                        cleanPts.push(p);
                        if (p < minVal) minVal = p;
                        if (p > maxVal) maxVal = p;
                    }
                    densityArr.push({
                        values: values,
                        weights: weights,
                        maxWeight: maxWeight
                    });
                    pointsArr.push(cleanPts);
                    minArr.push(minVal === Infinity ? 0 : minVal);
                    maxArr.push(maxVal === -Infinity ? 0 : maxVal);
                    placeholders.push(modeValue !== null ? modeValue : cleanPts.length ? cleanPts[Math.floor(cleanPts.length / 2)] : 0);
                }
                w.violinData.seriesViolinDensity[i] = densityArr;
                w.violinData.seriesViolinPoints[i] = pointsArr;
                w.violinData.seriesViolinMin[i] = minArr;
                w.violinData.seriesViolinMax[i] = maxArr;
                this.twoDSeries = placeholders;
            }
        },
        {
            /**
   * @param {string} format
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleRangeDataFormat",
            value: function handleRangeDataFormat(format, ser, i) {
                var rangeStart = [];
                var rangeEnd = [];
                var uniqueKeysMap = /* @__PURE__ */ new Map();
                var uniqueKeys = [];
                ser[i].data.forEach(function(item) {
                    if (!uniqueKeysMap.has(item.x)) {
                        var keyObj = {
                            x: item.x,
                            overlaps: /* @__PURE__ */ new Set(),
                            y: []
                        };
                        uniqueKeysMap.set(item.x, keyObj);
                        uniqueKeys.push(keyObj);
                    }
                });
                if (format === "array") {
                    for(var j = 0; j < ser[i].data.length; j++){
                        if (Array.isArray(ser[i].data[j])) {
                            rangeStart.push(ser[i].data[j][1][0]);
                            rangeEnd.push(ser[i].data[j][1][1]);
                        } else {
                            rangeStart.push(ser[i].data[j]);
                            rangeEnd.push(ser[i].data[j]);
                        }
                    }
                } else if (format === "xy") {
                    for(var j1 = 0; j1 < ser[i].data.length; j1++){
                        var isDataPoint2D = Array.isArray(ser[i].data[j1].y);
                        var id = Utils$1.randomId();
                        var x = ser[i].data[j1].x;
                        var y = {
                            y1: isDataPoint2D ? ser[i].data[j1].y[0] : ser[i].data[j1].y,
                            y2: isDataPoint2D ? ser[i].data[j1].y[1] : ser[i].data[j1].y,
                            rangeName: id
                        };
                        var gl = this.w.globals;
                        if (!gl.seriesRangeName) gl.seriesRangeName = {};
                        if (!gl.seriesRangeName[i]) gl.seriesRangeName[i] = {};
                        gl.seriesRangeName[i][j1] = id;
                        var keyObj = uniqueKeysMap.get(x);
                        if (keyObj) {
                            keyObj.y.push(y);
                        }
                        rangeStart.push(y.y1);
                        rangeEnd.push(y.y2);
                    }
                }
                return {
                    start: rangeStart,
                    end: rangeEnd,
                    rangeUniques: uniqueKeys
                };
            }
        },
        {
            /**
   * @param {string} format
   * @param {any[]} ser
   * @param {number} i
   */ key: "handleCandleStickBoxDataFormat",
            value: function handleCandleStickBoxDataFormat(format, ser, i) {
                var w = this.w;
                var isBoxPlot = w.config.chart.type === "boxPlot" || /** @type {Record<string,any>} */ w.config.series[i].type === "boxPlot";
                var serO = [];
                var serH = [];
                var serM = [];
                var serL = [];
                var serC = [];
                var serPoints = [];
                var data = ser[i].data;
                var getVals;
                if (format === "array") {
                    var first = data[0];
                    var isFlat = isBoxPlot && first && first.length === 6 || !isBoxPlot && first && first.length === 5;
                    if (isFlat) {
                        getVals = function getVals(d) {
                            return d.slice(1);
                        };
                    } else {
                        getVals = function getVals(d) {
                            return Array.isArray(d[1]) ? d[1] : [];
                        };
                    }
                } else {
                    getVals = function getVals(d) {
                        return Array.isArray(d.y) ? d.y : [];
                    };
                }
                for(var j = 0; j < data.length; j++){
                    var vals = getVals(data[j]);
                    if (vals && vals.length >= 2) {
                        serO.push(vals[0]);
                        serH.push(vals[1]);
                        if (isBoxPlot) {
                            serM.push(vals[2]);
                            serL.push(vals[3]);
                            serC.push(vals[4]);
                        } else {
                            serL.push(vals[2]);
                            serC.push(vals[3]);
                        }
                    }
                    var pts = data[j] && /** @type {any} */ data[j].points;
                    serPoints.push(Array.isArray(pts) ? pts : []);
                }
                return {
                    o: serO,
                    h: serH,
                    m: serM,
                    l: serL,
                    c: serC,
                    points: serPoints
                };
            }
        },
        {
            /**
   * @param {any[]} ser
   */ key: "parseDataAxisCharts",
            value: function parseDataAxisCharts(ser) {
                var _this = this;
                var _a, _b, _c, _d, _e, _f;
                var cnf = this.w.config;
                var gl = this.w.globals;
                var dt = new DateTime(this.w);
                this.w.seriesData._parsedExtrema = [];
                var xlabels = cnf.labels.length > 0 ? cnf.labels.slice() : cnf.xaxis.categories.slice();
                this.w.axisFlags.isRangeBar = cnf.chart.type === "rangeBar" && gl.isBarHorizontal;
                this.w.labelData.hasXaxisGroups = cnf.xaxis.type === "category" && cnf.xaxis.group.groups.length > 0;
                if (this.w.labelData.hasXaxisGroups) {
                    this.w.labelData.groups = cnf.xaxis.group.groups;
                }
                ser.forEach(function(s, i) {
                    if (s.name !== void 0) {
                        _this.w.seriesData.seriesNames.push(s.name);
                    } else {
                        _this.w.seriesData.seriesNames.push("series-" + parseInt(String(i + 1), 10));
                    }
                });
                this.coreUtils.setSeriesYAxisMappings();
                var buckets = [];
                var groups = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(new Set(cnf.series.map(function(s) {
                    return s.group;
                })));
                cnf.series.forEach(function(s, i) {
                    var index = groups.indexOf(s.group);
                    if (!buckets[index]) buckets[index] = [];
                    buckets[index].push(_this.w.seriesData.seriesNames[i]);
                });
                this.w.labelData.seriesGroups = buckets;
                var handleDates = function handleDates() {
                    for(var j = 0; j < xlabels.length; j++){
                        if (typeof xlabels[j] === "string") {
                            var isDate = dt.isValidDate(xlabels[j]);
                            if (isDate) {
                                _this.twoDSeriesX.push(dt.parseDate(xlabels[j]));
                            } else {
                                throw new Error("You have provided invalid Date format. Please provide a valid JavaScript Date");
                            }
                        } else {
                            _this.twoDSeriesX.push(xlabels[j]);
                        }
                    }
                };
                for(var i = 0; i < ser.length; i++){
                    this.twoDSeries = [];
                    this.twoDSeriesX = [];
                    this.threeDSeries = [];
                    if (typeof ser[i].data === "undefined") {
                        console.error("It is a possibility that you may have not included 'data' property in series.");
                        ser[i] = __spreadProps(__spreadValues({}, ser[i]), {
                            data: []
                        });
                    }
                    var dr = cnf.chart.dataReducer;
                    var rawStash = (_b = (_a = gl.dataReducerRawSeries) == null ? void 0 : _a[i]) == null ? void 0 : _b.data;
                    if ((dr == null ? void 0 : dr.enabled) && this.isMultiFormat() && Array.isArray(rawStash) && rawStash.length > ((_c = dr.threshold) != null ? _c : 500)) {
                        var targetPoints = (_d = dr.targetPoints) != null ? _d : 250;
                        var xmin = cnf.xaxis.min;
                        var xmax = cnf.xaxis.max;
                        var windowed = xmin == null && xmax == null ? rawStash : Data.sliceByXRange(rawStash, xmin, xmax);
                        var reduced = windowed;
                        if (windowed.length > targetPoints) {
                            var sampleY = !Array.isArray(windowed[0]) ? (_e = windowed[0]) == null ? void 0 : _e.y : (_f = windowed[0]) == null ? void 0 : _f[1];
                            if (Array.isArray(sampleY)) {
                                if (sampleY.length === 4) {
                                    reduced = Data.ohlcAggregate(windowed, targetPoints);
                                } else if (sampleY.length === 2) {
                                    reduced = Data.rangeAggregate(windowed, targetPoints);
                                }
                            } else {
                                reduced = Data.lttbDownsample(windowed, targetPoints);
                            }
                        }
                        ser[i] = __spreadProps(__spreadValues({}, ser[i]), {
                            data: reduced
                        });
                    }
                    if (cnf.chart.type === "rangeBar" || cnf.chart.type === "rangeArea" || ser[i].type === "rangeBar" || ser[i].type === "rangeArea") {
                        this.w.axisFlags.isRangeData = true;
                        this.handleRangeData(ser, i);
                    }
                    var customType = ser[i].type || cnf.chart.type;
                    if (isCustom(customType)) {
                        var cls = /** @type {any} */ getChartClass(customType);
                        var yExtent = cls && cls.yExtent;
                        if (cls && cls.dataType === "rangeXY" || typeof yExtent === "function") {
                            this.w.axisFlags.isRangeData = true;
                            this.handleCustomRangeData(ser, i, yExtent);
                        }
                    }
                    if (this.isMultiFormat()) {
                        if (this.isFormat2DArray()) {
                            this.handleFormat2DArray(ser, i);
                        } else if (this.isFormatXY()) {
                            this.handleFormatXY(ser, i);
                        }
                        if (cnf.chart.type === "candlestick" || ser[i].type === "candlestick" || cnf.chart.type === "boxPlot" || ser[i].type === "boxPlot") {
                            this.handleCandleStickBoxData(ser, i);
                        }
                        if (cnf.chart.type === "violin" || ser[i].type === "violin") {
                            this.handleViolinData(ser, i);
                        }
                        this.w.seriesData.series.push(this.twoDSeries);
                        this.w.labelData.labels.push(this.twoDSeriesX);
                        this.w.seriesData.seriesX.push(this.twoDSeriesX);
                        this.w.seriesData.seriesGoals = this.seriesGoals;
                        if (i === this.activeSeriesIndex && !this.fallbackToCategory) {
                            this.w.axisFlags.isXNumeric = true;
                        }
                    } else {
                        if (cnf.xaxis.type === "datetime") {
                            this.w.axisFlags.isXNumeric = true;
                            handleDates();
                            this.w.seriesData.seriesX.push(this.twoDSeriesX);
                        } else if (cnf.xaxis.type === "numeric") {
                            this.w.axisFlags.isXNumeric = true;
                            if (xlabels.length > 0) {
                                this.twoDSeriesX = xlabels;
                                this.w.seriesData.seriesX.push(this.twoDSeriesX);
                            }
                        }
                        this.w.labelData.labels.push(this.twoDSeriesX);
                        var singleArray = ser[i].data.map(function(d) {
                            return Utils$1.parseNumber(d);
                        });
                        this.w.seriesData.series.push(singleArray);
                    }
                    this.w.seriesData.seriesZ.push(this.threeDSeries);
                    if (ser[i].color !== void 0) {
                        this.w.seriesData.seriesColors.push(ser[i].color);
                    } else {
                        this.w.seriesData.seriesColors.push(/** @type {any} */ void 0);
                    }
                }
                return this.w;
            }
        },
        {
            /**
   * @param {any[]} ser
   */ key: "parseDataNonAxisCharts",
            value: function parseDataNonAxisCharts(ser) {
                var cnf = this.w.config;
                this.w.seriesData.unitData = [];
                var hasOldFormat = Array.isArray(ser) && ser.every(function(s) {
                    return typeof s === "number";
                }) && cnf.labels.length > 0;
                var hasNewFormat = Array.isArray(ser) && ser.some(function(s) {
                    return s && (typeof s === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(s)) === "object" && s.data || s && (typeof s === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(s)) === "object" && s.parsing;
                });
                if (cnf.chart.type === "unit" && hasNewFormat && !hasOldFormat) {
                    return this.parseUnitSeries(ser);
                }
                if (hasOldFormat && hasNewFormat) {
                    console.warn("ApexCharts: Both old format (numeric series + labels) and new format (series objects with data/parsing) detected. Using old format for backward compatibility.");
                }
                if (hasOldFormat) {
                    this.w.seriesData.series = /** @type {any} */ ser.slice();
                    this.w.seriesData.seriesNames = cnf.labels.slice();
                    for(var i = 0; i < this.w.seriesData.series.length; i++){
                        if (this.w.seriesData.seriesNames[i] === void 0) {
                            this.w.seriesData.seriesNames.push("series-" + (i + 1));
                        }
                    }
                    return this.w;
                }
                if (Array.isArray(ser) && ser.every(function(s) {
                    return typeof s === "number";
                })) {
                    this.w.seriesData.series = /** @type {any} */ ser.slice();
                    this.w.seriesData.seriesNames = [];
                    for(var i1 = 0; i1 < this.w.seriesData.series.length; i1++){
                        this.w.seriesData.seriesNames.push(cnf.labels[i1] || "series-".concat(i1 + 1));
                    }
                    return this.w;
                }
                var processedData = this.extractPieDataFromSeries(ser);
                this.w.seriesData.series = processedData.values;
                this.w.seriesData.seriesNames = processedData.labels;
                if (cnf.chart.type === "radialBar") {
                    this.w.seriesData.series = this.w.seriesData.series.map(function(val) {
                        var numVal = Utils$1.parseNumber(val);
                        if (numVal > 100) {
                            console.warn("ApexCharts: RadialBar value ".concat(numVal, " > 100, consider using percentage values (0-100)"));
                        }
                        return numVal;
                    });
                }
                for(var i2 = 0; i2 < this.w.seriesData.series.length; i2++){
                    if (this.w.seriesData.seriesNames[i2] === void 0) {
                        this.w.seriesData.seriesNames.push("series-" + (i2 + 1));
                    }
                }
                return this.w;
            }
        },
        {
            /**
   * Parse the unit chart's per-unit object form:
   *   series: [{ name, data: [datum, datum, ...] }, ...]
   * Each category's dot count is `data.length` (one dot per datum), and the
   * per-unit data is kept on `w.seriesData.unitData` so the renderer can colour
   * dots individually and the tooltip can show each unit's own info.
   * @param {any[]} ser
   * @returns {any} w
   */ key: "parseUnitSeries",
            value: function parseUnitSeries(ser) {
                var cnf = this.w.config;
                var series = [];
                var seriesNames = [];
                var unitData = [];
                ser.forEach(function(s, i) {
                    var _a;
                    var data = s && Array.isArray(s.data) ? s.data : [];
                    series.push(data.length);
                    var name2 = s && s.name !== void 0 && s.name !== null ? s.name : void 0;
                    seriesNames.push((_a = name2 != null ? name2 : cnf.labels[i]) != null ? _a : "series-".concat(i + 1));
                    unitData.push(data.slice());
                });
                this.w.seriesData.series = /** @type {any} */ series;
                this.w.seriesData.seriesNames = seriesNames;
                this.w.seriesData.unitData = unitData;
                return this.w;
            }
        },
        {
            /**
   * Reset parsing flags to allow re-parsing of data during updates
   */ key: "resetParsingFlags",
            value: function resetParsingFlags() {
                var w = this.w;
                w.axisFlags.dataWasParsed = false;
                w.globals.originalSeries = null;
                if (w.config.series) {
                    w.config.series.forEach(function(serie) {
                        if (/** @type {any} */ serie.__apexParsed) {
                            delete /** @type {any} */ serie.__apexParsed;
                        }
                    });
                }
            }
        },
        {
            /**
   * @param {any[]} ser
   */ key: "extractPieDataFromSeries",
            value: function extractPieDataFromSeries(ser) {
                var values = [];
                var labels = [];
                if (!Array.isArray(ser)) {
                    console.warn("ApexCharts: Expected array for series data");
                    return {
                        values: [],
                        labels: []
                    };
                }
                if (ser.length === 0) {
                    console.warn("ApexCharts: Empty series array");
                    return {
                        values: [],
                        labels: []
                    };
                }
                var firstItem = ser[0];
                if ((typeof firstItem === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(firstItem)) === "object" && firstItem !== null && firstItem.data) {
                    this.extractPieDataFromSeriesObjects(ser, values, labels);
                } else {
                    console.warn("ApexCharts: Unsupported series format for pie/donut/radialBar. Expected series objects with data property.");
                    return {
                        values: [],
                        labels: []
                    };
                }
                return {
                    values: values,
                    labels: labels
                };
            }
        },
        {
            // Extract data from series objects: [{ data: [...], parsing: {...} }]
            /**
   * @param {any[]} seriesArray
   * @param {any[]} values
   * @param {any[]} labels
   */ key: "extractPieDataFromSeriesObjects",
            value: function extractPieDataFromSeriesObjects(seriesArray, values, labels) {
                seriesArray.forEach(function(serie, serieIndex) {
                    if (!serie.data || !Array.isArray(serie.data)) {
                        console.warn("ApexCharts: Series ".concat(serieIndex, " has no valid data array"));
                        return;
                    }
                    serie.data.forEach(function(dataPoint) {
                        if ((typeof dataPoint === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dataPoint)) === "object" && dataPoint !== null) {
                            if (dataPoint.x !== void 0 && dataPoint.y !== void 0) {
                                labels.push(String(dataPoint.x));
                                values.push(Utils$1.parseNumber(dataPoint.y));
                            } else {
                                console.warn("ApexCharts: Invalid data point format for pie chart. Expected {x, y} format:", dataPoint);
                            }
                        } else {
                            console.warn("ApexCharts: Expected object data point, got:", typeof dataPoint === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dataPoint));
                        }
                    });
                });
            }
        },
        {
            /** User possibly set string categories in xaxis.categories or labels prop
   * Or didn't set xaxis labels at all - in which case we manually do it.
   * If user passed series data as [[3, 2], [4, 5]] or [{ x: 3, y: 55 }],
   * this shouldn't be called
   * @param {any[]} ser - the series which user passed to the config
   */ key: "handleExternalLabelsData",
            value: function handleExternalLabelsData(ser) {
                var _this = this;
                var cnf = this.w.config;
                if (cnf.xaxis.categories.length > 0) {
                    this.w.labelData.labels = cnf.xaxis.categories;
                } else if (cnf.labels.length > 0) {
                    this.w.labelData.labels = cnf.labels.slice();
                } else if (this.fallbackToCategory) {
                    this.w.labelData.labels = /** @type {string[]} */ /** @type {unknown} */ this.w.labelData.labels[0];
                    if (this.w.rangeData.seriesRange.length) {
                        this.w.rangeData.seriesRange.map(function(srt) {
                            srt.forEach(function(sr) {
                                if (_this.w.labelData.labels.indexOf(sr.x) < 0 && sr.x) {
                                    _this.w.labelData.labels.push(sr.x);
                                }
                            });
                        });
                        var _labels = this.w.labelData.labels;
                        if (_labels.length > 0 && (typeof _labels[0] === "number" || typeof _labels[0] === "string")) {
                            this.w.labelData.labels = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(new Set(_labels));
                        } else {
                            var _seen = /* @__PURE__ */ new Map();
                            var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                            try {
                                for(var _iterator = _labels[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                                    var _label = _step.value;
                                    var _key = JSON.stringify(_label);
                                    if (!_seen.has(_key)) _seen.set(_key, _label);
                                }
                            } catch (err) {
                                _didIteratorError = true;
                                _iteratorError = err;
                            } finally{
                                try {
                                    if (!_iteratorNormalCompletion && _iterator.return != null) {
                                        _iterator.return();
                                    }
                                } finally{
                                    if (_didIteratorError) {
                                        throw _iteratorError;
                                    }
                                }
                            }
                            this.w.labelData.labels = Array.from(_seen.values());
                        }
                    }
                    if (cnf.xaxis.convertedCatToNumeric) {
                        var defaults = new Defaults(cnf);
                        defaults.convertCatToNumericXaxis(cnf, this.w.seriesData.seriesX[0]);
                        this._generateExternalLabels(ser);
                    }
                } else {
                    this._generateExternalLabels(ser);
                }
            }
        },
        {
            /**
   * @param {any[]} ser
   */ key: "_generateExternalLabels",
            value: function _generateExternalLabels(ser) {
                var gl = this.w.globals;
                var cnf = this.w.config;
                var labelArr = [];
                if (gl.axisCharts) {
                    if (this.w.seriesData.series.length > 0) {
                        if (this.isFormatXY()) {
                            var seriesDataFiltered = cnf.series.map(function(serie) {
                                var seen = /* @__PURE__ */ new Map();
                                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                                try {
                                    for(var _iterator = serie.data[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                                        var point = _step.value;
                                        if (!seen.has(point.x)) seen.set(point.x, point);
                                    }
                                } catch (err) {
                                    _didIteratorError = true;
                                    _iteratorError = err;
                                } finally{
                                    try {
                                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                                            _iterator.return();
                                        }
                                    } finally{
                                        if (_didIteratorError) {
                                            throw _iteratorError;
                                        }
                                    }
                                }
                                return Array.from(seen.values());
                            });
                            var len = seriesDataFiltered.reduce(function(p, c, i, a) {
                                return a[p].length > c.length ? p : i;
                            }, 0);
                            for(var i = 0; i < seriesDataFiltered[len].length; i++){
                                labelArr.push(i + 1);
                            }
                        } else {
                            for(var i1 = 0; i1 < this.w.seriesData.series[gl.maxValsInArrayIndex].length; i1++){
                                labelArr.push(i1 + 1);
                            }
                        }
                    }
                    this.w.seriesData.seriesX = [];
                    for(var i2 = 0; i2 < ser.length; i2++){
                        this.w.seriesData.seriesX.push(labelArr);
                    }
                    if (!this.w.globals.isBarHorizontal) {
                        this.w.axisFlags.isXNumeric = true;
                    }
                }
                if (labelArr.length === 0) {
                    labelArr = gl.axisCharts ? [] : /**
         * @param {Record<string, any>} gls
         * @param {number} glsi
         */ this.w.seriesData.series.map(function(gls, glsi) {
                        return glsi + 1;
                    });
                    for(var i3 = 0; i3 < ser.length; i3++){
                        this.w.seriesData.seriesX.push(labelArr);
                    }
                }
                this.w.labelData.labels = /** @type {string[]} */ /** @type {unknown} */ labelArr;
                if (cnf.xaxis.convertedCatToNumeric) {
                    this.w.labelData.categoryLabels = labelArr.map(function(l) {
                        return cnf.xaxis.labels.formatter(l);
                    });
                }
                this.w.axisFlags.noLabelsProvided = true;
            }
        },
        {
            /**
   * @param {any[]} series
   */ key: "parseRawDataIfNeeded",
            value: function parseRawDataIfNeeded(series) {
                var _this = this;
                var cnf = this.w.config;
                var gl = this.w.globals;
                var globalParsing = cnf.parsing;
                if (this.w.axisFlags.dataWasParsed) {
                    return series;
                }
                var hasGlobalParsing = !!(globalParsing && (globalParsing.x || globalParsing.y || globalParsing.z));
                var hasSeriesParsing = series.some(function(s) {
                    return s.parsing && (s.parsing.x || s.parsing.y || s.parsing.z);
                });
                if (!hasGlobalParsing && !hasSeriesParsing) {
                    return series;
                }
                var processedSeries = series.map(function(serie, index) {
                    var _a, _b, _c, _d, _e;
                    if (!serie.data || !Array.isArray(serie.data) || serie.data.length === 0) {
                        return serie;
                    }
                    var effectiveParsing = {
                        x: ((_a = serie.parsing) == null ? void 0 : _a.x) || (globalParsing == null ? void 0 : globalParsing.x),
                        y: ((_b = serie.parsing) == null ? void 0 : _b.y) || (globalParsing == null ? void 0 : globalParsing.y),
                        z: ((_c = serie.parsing) == null ? void 0 : _c.z) || (globalParsing == null ? void 0 : globalParsing.z)
                    };
                    if (!effectiveParsing.x && !effectiveParsing.y) {
                        return serie;
                    }
                    var firstDataPoint = serie.data[0];
                    if ((typeof firstDataPoint === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(firstDataPoint)) === "object" && firstDataPoint !== null && (Object.prototype.hasOwnProperty.call(firstDataPoint, "x") || Object.prototype.hasOwnProperty.call(firstDataPoint, "y")) || Array.isArray(firstDataPoint)) {
                        return serie;
                    }
                    if (!effectiveParsing.x || !effectiveParsing.y || Array.isArray(effectiveParsing.y) && effectiveParsing.y.length === 0) {
                        var missing = [];
                        if (!effectiveParsing.x) missing.push("x");
                        if (!effectiveParsing.y || Array.isArray(effectiveParsing.y) && effectiveParsing.y.length === 0) {
                            missing.push("y");
                        }
                        var seriesName = (_d = serie.name) != null ? _d : "series[".concat(index, "]");
                        console.warn("ApexCharts [".concat(_this.w.globals.chartID, ']: "').concat(seriesName, "\" has a parseData config but is missing the '").concat(missing.join("', '"), "' field specification."), {
                            parsing: (_e = serie.parsing) != null ? _e : globalParsing
                        });
                        return serie;
                    }
                    var transformedData = serie.data.map(function(item, itemIndex) {
                        if ((typeof item === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(item)) !== "object" || item === null) {
                            console.warn("ApexCharts: Series ".concat(index, ", data point ").concat(itemIndex, " is not an object, skipping parsing"));
                            return item;
                        }
                        var x = _this.getNestedValue(item, effectiveParsing.x);
                        var y;
                        var z = void 0;
                        if (Array.isArray(effectiveParsing.y)) {
                            var yValues = effectiveParsing.y.map(function(fieldName) {
                                return _this.getNestedValue(item, fieldName);
                            });
                            if (_this.w.config.chart.type === "bubble") {
                                if (yValues.length < 2) {
                                    console.warn("ApexCharts: series[".concat(index, "] bubble chart requires parseData.y to have at least 2 fields (y and z). Got: ").concat(JSON.stringify(effectiveParsing.y)));
                                }
                                y = yValues[0];
                            } else {
                                y = yValues;
                            }
                        } else {
                            y = _this.getNestedValue(item, effectiveParsing.y);
                        }
                        if (effectiveParsing.z) {
                            z = _this.getNestedValue(item, effectiveParsing.z);
                        }
                        if (x === void 0) {
                            console.warn("ApexCharts: Series ".concat(index, ", data point ").concat(itemIndex, " missing field '").concat(effectiveParsing.x, "'"));
                        }
                        if (y === void 0) {
                            console.warn("ApexCharts: Series ".concat(index, ", data point ").concat(itemIndex, " missing field '").concat(effectiveParsing.y, "'"));
                        }
                        var result = {
                            x: x,
                            y: y,
                            z: void 0
                        };
                        if (_this.w.config.chart.type === "bubble" && Array.isArray(effectiveParsing.y) && effectiveParsing.y.length === 2) {
                            var zValue = _this.getNestedValue(item, effectiveParsing.y[1]);
                            if (zValue !== void 0) {
                                result.z = zValue;
                            }
                        }
                        if (z !== void 0) {
                            result.z = z;
                        }
                        return result;
                    });
                    return __spreadProps(__spreadValues({}, serie), {
                        data: transformedData,
                        __apexParsed: true
                    });
                });
                this.w.axisFlags.dataWasParsed = true;
                if (!gl.originalSeries) {
                    gl.originalSeries = Utils$1.clone(series);
                }
                return processedSeries;
            }
        },
        {
            /**
   * Get nested object value using dot notation path
   * @param {Object} obj - The object to search in
   * @param {string} path - Dot notation path (e.g., 'user.profile.name')
   * @returns {*} The value at the path, or undefined if not found
   */ key: "getNestedValue",
            value: function getNestedValue(obj, path) {
                if (!obj || (typeof obj === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(obj)) !== "object" || !path) {
                    return void 0;
                }
                if (path.indexOf(".") === -1) {
                    return /** @type {any} */ obj[path];
                }
                var keys = path.split(".");
                var current = obj;
                for(var i = 0; i < keys.length; i++){
                    if (current === null || current === void 0 || (typeof current === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(current)) !== "object") {
                        return void 0;
                    }
                    current = /** @type {any} */ current[keys[i]];
                }
                return current;
            }
        },
        {
            /**
   * Scatter strip-plot support. When `plotOptions.scatter.jitter.enabled` and a
   * series carries compact `{ x: 'Category', y: [v1, v2, ...] }` data, expand
   * each observation into its own `{ x: bandIndex, y }` point (so every dot is a
   * first-class, hoverable marker) and frame the x-axis as evenly-spaced,
   * labelled bands. The reference per-point form (numeric x + `xaxis.categories`)
   * is reframed too, without expansion. Returns `ser` unchanged for non-scatter
   * charts and for plain numeric/datetime data (continuous overplotting jitter
   * is offset at render time instead).
   *
   * @param {any[]} ser
   * @returns {any[]}
   */ key: "expandScatterJitterData",
            value: function expandScatterJitterData(ser) {
                var _a, _b;
                var cnf = this.w.config;
                var isScatter = cnf.chart.type === "scatter" || cnf.chart.type === "bubble";
                var jt = (_b = (_a = cnf.plotOptions) == null ? void 0 : _a.scatter) == null ? void 0 : _b.jitter;
                if (!isScatter || !jt || !jt.enabled || !Array.isArray(ser)) return ser;
                var hasArrayY = ser.some(function(s) {
                    return Array.isArray(s == null ? void 0 : s.data) && s.data.some(function(d) {
                        return d && !Array.isArray(d) && Array.isArray(d.y);
                    });
                });
                if (!hasArrayY) {
                    if (cnf.xaxis.type !== "datetime") {
                        if (Array.isArray(cnf.xaxis.categories) && cnf.xaxis.categories.length) {
                            this._applyBandAxis(cnf.xaxis.categories.slice());
                        } else if (Array.isArray(cnf.xaxis._scatterBandLabels) && cnf.xaxis._scatterBandLabels.length) {
                            this._applyBandAxis(cnf.xaxis._scatterBandLabels);
                        }
                    }
                    return ser;
                }
                var bandLabels = [];
                var bandIndex = /* @__PURE__ */ new Map();
                ser.forEach(function(s) {
                    if (!Array.isArray(s == null ? void 0 : s.data)) return;
                    s.data.forEach(function(d) {
                        if (d && Array.isArray(d.y)) {
                            var key = String(d.x);
                            if (!bandIndex.has(key)) {
                                bandIndex.set(key, bandLabels.length);
                                bandLabels.push(d.x);
                            }
                        }
                    });
                });
                var maxPoints = jt.maxPoints || 5e3;
                var expanded = ser.map(function(s) {
                    if (!Array.isArray(s == null ? void 0 : s.data)) return s;
                    var out = [];
                    s.data.forEach(function(d) {
                        if (d && Array.isArray(d.y)) {
                            var bi = bandIndex.get(String(d.x));
                            var ys = d.y;
                            var stride = ys.length > maxPoints ? Math.ceil(ys.length / maxPoints) : 1;
                            for(var k = 0; k < ys.length; k += stride){
                                var yv = Utils$1.parseNumber(ys[k]);
                                if (yv === null) continue;
                                out.push({
                                    x: bi,
                                    y: yv
                                });
                            }
                        } else if (d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object" && !Array.isArray(d)) {
                            var key = String(d.x);
                            out.push({
                                x: bandIndex.has(key) ? bandIndex.get(key) : d.x,
                                y: d.y
                            });
                        } else {
                            out.push(d);
                        }
                    });
                    return __spreadProps(__spreadValues({}, s), {
                        data: out
                    });
                });
                this._applyBandAxis(bandLabels);
                return expanded;
            }
        },
        {
            /**
   * Frame the x-axis as N evenly-spaced bands (one per category) on a numeric
   * scale. Bands sit at integer positions 0..N-1; the range is padded by a full
   * band on each side (min -1, max N) so jittered dots never clip. Crucially the
   * range bounds and tick count are integers, so the ticks land exactly on the
   * band centers regardless of how the numeric scale "nices" the step (e.g. the
   * small-range reduction in Scales._adjustTicksForSmallRange triggered by a
   * y-axis formatter). Only fills in options the user hasn't set, so explicit
   * min/max/tickAmount/formatter still win. The exception is an interactive
   * zoom/pan window (w.interact.zoomed): its fractional bounds are snapped to
   * whole bands so tick labels stay on band centers and edge bands are never
   * half-cropped.
   *
   * @param {any[]} bandLabels
   */ key: "_applyBandAxis",
            value: function _applyBandAxis(bandLabels) {
                var _a;
                var xa = this.w.config.xaxis;
                var n = bandLabels.length;
                if (!n) return;
                var owned = /** @type {Record<string, boolean>} */ xa._scatterBand = xa._scatterBand || {};
                xa._scatterBandLabels = bandLabels.slice();
                xa.type = "numeric";
                if (((_a = this.w.interact) == null ? void 0 : _a.zoomed) && typeof xa.min === "number" && typeof xa.max === "number" && isFinite(xa.min) && isFinite(xa.max)) {
                    var clampBand = function clampBand(b) {
                        return Math.max(0, Math.min(n - 1, b));
                    };
                    var first = clampBand(Math.round(xa.min + 0.49));
                    var last = clampBand(Math.round(xa.max - 0.49));
                    if (last < first) {
                        first = last = clampBand(Math.round((xa.min + xa.max) / 2));
                    }
                    xa.min = first - 1;
                    xa.max = last + 1;
                    xa.tickAmount = last - first + 2;
                    owned.min = true;
                    owned.max = true;
                    owned.tick = true;
                } else {
                    if (xa.min == null || owned.min) {
                        xa.min = -1;
                        owned.min = true;
                    }
                    if (xa.max == null || owned.max) {
                        xa.max = n;
                        owned.max = true;
                    }
                    if (xa.tickAmount == null || xa.tickAmount === "dataPoints" || owned.tick) {
                        xa.tickAmount = n + 1;
                        owned.tick = true;
                    }
                }
                xa.labels = xa.labels || {};
                var existing = /** @type {any} */ xa.labels.formatter;
                if (typeof existing !== "function" || existing._scatterBand) {
                    var fmt = function(val) {
                        var r = Math.round(val);
                        return Math.abs(val - r) < 1e-6 && bandLabels[r] !== void 0 ? bandLabels[r] : "";
                    };
                    fmt._scatterBand = true;
                    xa.labels.formatter = fmt;
                }
            }
        },
        {
            // Segregate user provided data into appropriate vars
            /**
   * @param {any[]} ser
   */ key: "parseData",
            value: function parseData(ser) {
                var _this = this;
                var _a, _b, _c, _d, _e, _f, _g;
                var w = this.w;
                var cnf = w.config;
                var gl = w.globals;
                ser = this.parseRawDataIfNeeded(ser);
                ser = this.expandScatterJitterData(ser);
                if (((_a = cnf.chart.dataReducer) == null ? void 0 : _a.enabled) && gl.axisCharts && !gl.dataReducerRawSeries) {
                    gl.dataReducerRawSeries = ser.map(function(s) {
                        return {
                            data: Array.isArray(s == null ? void 0 : s.data) ? s.data.slice() : s == null ? void 0 : s.data
                        };
                    });
                    var rawMinX = Infinity;
                    var rawMaxX = -Infinity;
                    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                    try {
                        for(var _iterator = ser[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                            var s = _step.value;
                            var d = s == null ? void 0 : s.data;
                            if (!Array.isArray(d) || d.length === 0) continue;
                            var isXY = !Array.isArray(d[0]);
                            var firstX = isXY ? (_b = d[0]) == null ? void 0 : _b.x : (_c = d[0]) == null ? void 0 : _c[0];
                            var lastX = isXY ? (_d = d[d.length - 1]) == null ? void 0 : _d.x : (_e = d[d.length - 1]) == null ? void 0 : _e[0];
                            if (typeof firstX === "number") rawMinX = Math.min(rawMinX, firstX);
                            if (typeof lastX === "number") rawMaxX = Math.max(rawMaxX, lastX);
                        }
                    } catch (err) {
                        _didIteratorError = true;
                        _iteratorError = err;
                    } finally{
                        try {
                            if (!_iteratorNormalCompletion && _iterator.return != null) {
                                _iterator.return();
                            }
                        } finally{
                            if (_didIteratorError) {
                                throw _iteratorError;
                            }
                        }
                    }
                    if (rawMinX !== Infinity) {
                        gl.dataReducerRawMinX = rawMinX;
                        gl.dataReducerRawMaxX = rawMaxX;
                    }
                }
                if (gl.dataReducerRawSeries && ((_f = cnf.chart.dataReducer) == null ? void 0 : _f.enabled)) {
                    ser = ser.map(function(s) {
                        return __spreadValues({}, s);
                    });
                }
                cnf.series = ser;
                if (gl.dataReducerRawSeries && ((_g = cnf.chart.dataReducer) == null ? void 0 : _g.enabled)) {
                    var stash = gl.dataReducerRawSeries;
                    gl.initialSeries = ser.map(function(s, i) {
                        var _a2, _b2, _c2;
                        return __spreadProps(__spreadValues({}, s), {
                            data: (_c2 = (_b2 = (_a2 = stash[i]) == null ? void 0 : _a2.data) == null ? void 0 : _b2.slice()) != null ? _c2 : s.data
                        });
                    });
                } else {
                    gl.initialSeries = ser;
                }
                this.excludeCollapsedSeriesInYAxis();
                this.fallbackToCategory = false;
                this.resetGlobals();
                this.isMultipleY();
                if (gl.axisCharts) {
                    this.parseDataAxisCharts(ser);
                    this.coreUtils.getLargestSeries();
                } else {
                    this.parseDataNonAxisCharts(ser);
                }
                if (cnf.chart.stacked) {
                    var series = new Series(this.w);
                    this.w.seriesData.series = series.setNullSeriesToZeroValues(this.w.seriesData.series);
                }
                this.coreUtils.getSeriesTotals();
                if (gl.axisCharts) {
                    Data._defineLazyResult(this.w.seriesData, "stackedSeriesTotals", function() {
                        return _this.coreUtils.getStackedSeriesTotals();
                    });
                    Data._defineLazyResult(this.w.seriesData, "stackedSeriesTotalsByGroups", function() {
                        return _this.coreUtils.getStackedSeriesTotalsByGroups();
                    });
                    Data._defineLazyResult(gl, "seriesPercent", function() {
                        _this.coreUtils.getPercentSeries();
                        return gl.seriesPercent;
                    });
                } else {
                    this.coreUtils.getPercentSeries();
                }
                if (!this.w.axisFlags.dataFormatXNumeric && (!this.w.axisFlags.isXNumeric || cnf.xaxis.type === "numeric" && cnf.labels.length === 0 && cnf.xaxis.categories.length === 0)) {
                    this.handleExternalLabelsData(ser);
                }
                var catLabels = this.coreUtils.getCategoryLabels(this.w.labelData.labels);
                for(var l = 0; l < catLabels.length; l++){
                    if (Array.isArray(catLabels[l])) {
                        this.w.axisFlags.isMultiLineX = true;
                        break;
                    }
                }
                return {
                    // w.seriesData (future slice)
                    // initialSeries/originalSeries and the stacked totals are deliberately
                    // ABSENT: they already live as lazy accessors on gl / w.seriesData, so
                    // a snapshot field would either force their materialization (a deep
                    // clone plus three O(n) passes per parse that most charts never need)
                    // or, as a delegating getter, recurse into itself when a writer copies
                    // it back onto the object it delegates to.
                    seriesData: {
                        series: this.w.seriesData.series,
                        seriesNames: this.w.seriesData.seriesNames,
                        seriesX: this.w.seriesData.seriesX,
                        seriesZ: this.w.seriesData.seriesZ,
                        seriesColors: this.w.seriesData.seriesColors,
                        seriesGoals: this.w.seriesData.seriesGoals,
                        unitData: this.w.seriesData.unitData,
                        noLabelsProvided: this.w.axisFlags.noLabelsProvided
                    },
                    // w.rangeData (future slice)
                    rangeData: {
                        seriesRangeStart: this.w.rangeData.seriesRangeStart,
                        seriesRangeEnd: this.w.rangeData.seriesRangeEnd,
                        seriesRange: this.w.rangeData.seriesRange
                    },
                    // w.candleData (future slice)
                    candleData: {
                        seriesCandleO: this.w.candleData.seriesCandleO,
                        seriesCandleH: this.w.candleData.seriesCandleH,
                        seriesCandleM: this.w.candleData.seriesCandleM,
                        seriesCandleL: this.w.candleData.seriesCandleL,
                        seriesCandleC: this.w.candleData.seriesCandleC,
                        seriesBoxPoints: this.w.candleData.seriesBoxPoints
                    },
                    // w.labelData (future slice)
                    labelData: {
                        labels: this.w.labelData.labels,
                        categoryLabels: this.w.labelData.categoryLabels
                    },
                    // w.axisFlags (future slice)
                    axisFlags: {
                        isXNumeric: this.w.axisFlags.isXNumeric,
                        dataFormatXNumeric: this.w.axisFlags.dataFormatXNumeric,
                        isDataXYZ: this.w.axisFlags.isDataXYZ,
                        isRangeData: this.w.axisFlags.isRangeData,
                        isRangeBar: this.w.axisFlags.isRangeBar,
                        isMultiLineX: this.w.axisFlags.isMultiLineX,
                        dataWasParsed: this.w.axisFlags.dataWasParsed,
                        hasXaxisGroups: this.w.labelData.hasXaxisGroups,
                        groups: this.w.labelData.groups,
                        seriesGroups: this.w.labelData.seriesGroups
                    }
                };
            }
        },
        {
            key: "excludeCollapsedSeriesInYAxis",
            value: function excludeCollapsedSeriesInYAxis() {
                var w = this.w;
                var yAxisIndexes = [];
                w.globals.seriesYAxisMap.forEach(function(yAxisArr, yi) {
                    var collapsedCount = 0;
                    yAxisArr.forEach(function(seriesIndex) {
                        if (w.globals.collapsedSeriesIndices.indexOf(seriesIndex) !== -1) {
                            collapsedCount++;
                        }
                    });
                    if (collapsedCount > 0 && collapsedCount == yAxisArr.length) {
                        yAxisIndexes.push(yi);
                    }
                });
                w.globals.ignoreYAxisIndexes = yAxisIndexes.map(function(x) {
                    return x;
                });
            }
        }
    ], [
        {
            key: "_defineLazyResult",
            value: /**
   * Slice a sorted-by-x series to a [xmin, xmax] window using binary search.
   *
   * Pads with one extra point on each side so lines extend cleanly to the
   * chart edges. Either bound may be null/undefined to disable that side.
   *
   * @param {any[]} data - Series data in [{x,y}] or [[x,y]] format, sorted by x.
   * @param {number|null|undefined} xmin
   * @param {number|null|undefined} xmax
   * @returns {any[]} Sliced array (new array, never the input reference).
   */ /**
   * Define `key` on `obj` as a lazily computed property: `compute` runs on
   * first read after this call and its result is cached; assigning to the
   * property stores the assigned value directly (so code that writes the
   * field, like getPercentSeries, keeps working). Re-calling resets the cache
   * (used once per parse).
   * @param {any} obj
   * @param {string} key
   * @param {() => any} compute
   */ function _defineLazyResult(obj, key, compute) {
                var has = false;
                var value;
                Object.defineProperty(obj, key, {
                    configurable: true,
                    enumerable: true,
                    get: function get() {
                        if (!has) {
                            has = true;
                            value = compute();
                        }
                        return value;
                    },
                    set: function set(v) {
                        has = true;
                        value = v;
                    }
                });
            }
        },
        {
            key: "sliceByXRange",
            value: /**
   * @param {any[]} data
   * @param {any} xmin
   * @param {any} xmax
   */ function sliceByXRange(data, xmin, xmax) {
                var len = data.length;
                if (len === 0) return data;
                var isXY = !Array.isArray(data[0]);
                var getX = isXY ? function(p) {
                    return p.x;
                } : function(p) {
                    return p[0];
                };
                var lo = 0;
                if (xmin != null) {
                    var l = 0;
                    var r = len - 1;
                    while(l <= r){
                        var m = l + r >> 1;
                        if (getX(data[m]) < xmin) l = m + 1;
                        else r = m - 1;
                    }
                    lo = Math.max(0, l - 1);
                }
                var hi = len;
                if (xmax != null) {
                    var l1 = 0;
                    var r1 = len - 1;
                    while(l1 <= r1){
                        var m1 = l1 + r1 >> 1;
                        if (getX(data[m1]) > xmax) r1 = m1 - 1;
                        else l1 = m1 + 1;
                    }
                    hi = Math.min(len, l1 + 1);
                }
                return lo === 0 && hi === len ? data.slice() : data.slice(lo, hi);
            }
        },
        {
            key: "lttbDownsample",
            value: /**
   * Largest-Triangle-Three-Bucket (LTTB) downsampling.
   *
   * Reduces `data` to `targetPoints` points while preserving the visual shape
   * of the series as perceived by the human eye.
   *
   * @param {any[]} data   - Raw series data in [{x,y}] or [[x,y]] format.
   * @param {number} targetPoints - Desired output length (>= 3).
   * @returns {any[]} Downsampled array in the same format as the input.
   */ function lttbDownsample(data, targetPoints) {
                var len = data.length;
                if (targetPoints >= len || targetPoints < 3) return data;
                var isXY = !Array.isArray(data[0]);
                var getX = isXY ? function(p) {
                    return p.x;
                } : function(p) {
                    return p[0];
                };
                var getY = isXY ? function(p) {
                    return p.y;
                } : function(p) {
                    return p[1];
                };
                var sampled = [];
                sampled.push(data[0]);
                var bucketSize = (len - 2) / (targetPoints - 2);
                var a = 0;
                for(var i = 0; i < targetPoints - 2; i++){
                    var avgRangeStart = Math.floor((i + 1) * bucketSize) + 1;
                    var avgRangeEnd = Math.min(Math.floor((i + 2) * bucketSize) + 1, len);
                    var avgX = 0;
                    var avgY = 0;
                    var avgRangeLen = avgRangeEnd - avgRangeStart;
                    for(var j = avgRangeStart; j < avgRangeEnd; j++){
                        avgX += getX(data[j]);
                        avgY += getY(data[j]);
                    }
                    avgX /= avgRangeLen;
                    avgY /= avgRangeLen;
                    var rangeStart = Math.floor(i * bucketSize) + 1;
                    var rangeEnd = Math.min(Math.floor((i + 1) * bucketSize) + 1, len);
                    var pointAX = getX(data[a]);
                    var pointAY = getY(data[a]);
                    var maxArea = -1;
                    var maxAreaIdx = rangeStart;
                    for(var j1 = rangeStart; j1 < rangeEnd; j1++){
                        var area = Math.abs((pointAX - avgX) * (getY(data[j1]) - pointAY) - (pointAX - getX(data[j1])) * (avgY - pointAY)) * 0.5;
                        if (area > maxArea) {
                            maxArea = area;
                            maxAreaIdx = j1;
                        }
                    }
                    sampled.push(data[maxAreaIdx]);
                    a = maxAreaIdx;
                }
                sampled.push(data[len - 1]);
                return sampled;
            }
        },
        {
            key: "ohlcAggregate",
            value: /**
   * OHLC-aware bucket aggregation for candlestick / OHLC series.
   *
   * Each point's y is a 4-tuple `[open, high, low, close]`. LTTB is unusable
   * here — it treats y as a scalar, so the triangle-area math degenerates and
   * silently discards the high/low extremes that *define* a candle. Instead we
   * split the series into `targetPoints` contiguous buckets and roll each up
   * into a single candle: open = first bucket open, close = last bucket close,
   * high = max of highs, low = min of lows. The x is the first point's x in the
   * bucket. Output keeps the input's format ([{x,y}] or [[x,y]]).
   *
   * @param {any[]} data         - Raw OHLC series in [{x,y:[o,h,l,c]}] or [[x,[o,h,l,c]]] format.
   * @param {number} targetPoints - Desired output length (>= 1).
   * @returns {any[]} Aggregated array in the same format as the input.
   */ function ohlcAggregate(data, targetPoints) {
                var len = data.length;
                if (targetPoints >= len || targetPoints < 1) return data;
                var isXY = !Array.isArray(data[0]);
                var getX = isXY ? function(p) {
                    return p.x;
                } : function(p) {
                    return p[0];
                };
                var getY = isXY ? function(p) {
                    return p.y;
                } : function(p) {
                    return p[1];
                };
                var make = isXY ? function(x, y) {
                    return {
                        x: x,
                        y: y
                    };
                } : function(x, y) {
                    return [
                        x,
                        y
                    ];
                };
                var out = [];
                var bucketSize = len / targetPoints;
                for(var i = 0; i < targetPoints; i++){
                    var start = Math.floor(i * bucketSize);
                    var end = i === targetPoints - 1 ? len : Math.floor((i + 1) * bucketSize);
                    if (end <= start) continue;
                    var firstY = getY(data[start]);
                    var open = firstY[0];
                    var high = firstY[1];
                    var low = firstY[2];
                    var close = firstY[3];
                    for(var j = start + 1; j < end; j++){
                        var y = getY(data[j]);
                        if (y[1] > high) high = y[1];
                        if (y[2] < low) low = y[2];
                        close = y[3];
                    }
                    out.push(make(getX(data[start]), [
                        open,
                        high,
                        low,
                        close
                    ]));
                }
                return out;
            }
        },
        {
            key: "rangeAggregate",
            value: /**
   * Bucket-aggregate 2-tuple range data (`y: [low, high]`, rangeArea/rangeBar)
   * into `targetPoints` points, the range analog of {@link ohlcAggregate}. Each
   * bucket emits `[min low, max high]` so the band's vertical extent is never
   * understated by downsampling (LTTB, built for scalar y, would drop these
   * extremes). Order-agnostic: the min/max scan both tuple slots, so it is
   * correct whether a point is stored `[low, high]` or `[high, low]`.
   *
   * Null bounds (e.g. an indicator's warm-up period) are ignored, not treated
   * as 0 — `Math.min(null, x)` would coerce to 0 and pin the band to the
   * baseline. A bucket with no finite bounds emits `[null, null]` so it renders
   * as a gap, matching the un-downsampled series.
   * @param {any[]} data
   * @param {number} targetPoints
   * @returns {any[]}
   */ function rangeAggregate(data, targetPoints) {
                var len = data.length;
                if (targetPoints >= len || targetPoints < 1) return data;
                var isXY = !Array.isArray(data[0]);
                var getX = isXY ? function(p) {
                    return p.x;
                } : function(p) {
                    return p[0];
                };
                var getY = isXY ? function(p) {
                    return p.y;
                } : function(p) {
                    return p[1];
                };
                var make = isXY ? function(x, y) {
                    return {
                        x: x,
                        y: y
                    };
                } : function(x, y) {
                    return [
                        x,
                        y
                    ];
                };
                var out = [];
                var bucketSize = len / targetPoints;
                for(var i = 0; i < targetPoints; i++){
                    var start = Math.floor(i * bucketSize);
                    var end = i === targetPoints - 1 ? len : Math.floor((i + 1) * bucketSize);
                    if (end <= start) continue;
                    var low = Infinity;
                    var high = -Infinity;
                    for(var j = start; j < end; j++){
                        var y = getY(data[j]);
                        if (y == null) continue;
                        for(var k = 0; k < 2; k++){
                            var v = y[k];
                            if (v == null || !isFinite(v)) continue;
                            if (v < low) low = v;
                            if (v > high) high = v;
                        }
                    }
                    out.push(make(getX(data[start]), low === Infinity ? [
                        null,
                        null
                    ] : [
                        low,
                        high
                    ]));
                }
                return out;
            }
        }
    ]);
    return Data;
}();
var UpdateHelpers = /*#__PURE__*/ function() {
    "use strict";
    function UpdateHelpers(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, UpdateHelpers);
        this.w = w;
        this.ctx = ctx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(UpdateHelpers, [
        {
            /**
   * private method to update Options.
   *
   * @param {Record<string, any>} options - A new config object can be passed which will be merged with the existing config object
   * @param {boolean} redraw - should redraw from beginning or should use existing paths and redraw from there
   * @param {boolean} animate - should animate or not on updating Options
   * @param {boolean} overwriteInitialConfig - should update the initial config or not
   */ key: "_updateOptions",
            value: function _updateOptions(options2) {
                var _this = this;
                var redraw = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false, animate = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true, updateSyncedCharts = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : true, overwriteInitialConfig = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
                return new Promise(function(resolve, reject) {
                    var charts = [
                        _this.ctx
                    ];
                    if (updateSyncedCharts) {
                        charts = _this.ctx.getSyncedCharts();
                    }
                    if (_this.w.globals.isExecCalled) {
                        charts = [
                            _this.ctx
                        ];
                        _this.w.globals.isExecCalled = false;
                    }
                    charts.forEach(function(ch, chartIndex) {
                        var _a, _b;
                        var w = ch.w;
                        w.globals.shouldAnimate = animate;
                        if (!redraw) {
                            w.globals.resized = true;
                            w.globals.dataChanged = true;
                            if (animate && w.config.chart.animations.enabled) {
                                ch.series.getPreviousPaths();
                            }
                        }
                        if (animate && options2 && (typeof options2 === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(options2)) === "object") {
                            var newType = (_a = options2 == null ? void 0 : options2.chart) == null ? void 0 : _a.type;
                            var fromType = w.config.chart.requestedType || w.config.chart.type;
                            if (newType && newType !== fromType) {
                                (_b = ch.morphTypeChange) == null ? void 0 : _b.captureBeforeDestroy({
                                    fromType: fromType,
                                    toType: newType,
                                    newSeries: options2.series || w.config.series
                                });
                            }
                        }
                        if (options2 && (typeof options2 === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(options2)) === "object") {
                            ch.config = new Config(options2);
                            var incomingType = options2.chart && options2.chart.type;
                            var isAliasRequest = incomingType === "funnel" || incomingType === "pyramid" || incomingType === "gauge";
                            var wasAlias = !!w.config.chart.requestedType;
                            if (incomingType && !isAliasRequest && wasAlias) {
                                options2.chart = options2.chart || {};
                                options2.chart.requestedType = incomingType;
                                var prev = w.config.chart.requestedType;
                                if (prev === "funnel" || prev === "pyramid") {
                                    options2.plotOptions = options2.plotOptions || {};
                                    options2.plotOptions.bar = options2.plotOptions.bar || {};
                                    if (options2.plotOptions.bar.isFunnel === void 0) {
                                        options2.plotOptions.bar.isFunnel = false;
                                    }
                                    if (options2.plotOptions.bar.isPyramid === void 0) {
                                        options2.plotOptions.bar.isPyramid = false;
                                    }
                                }
                            }
                            ch.config.normalizeAliasedChartType(options2);
                            options2 = CoreUtils.extendArrayProps(ch.config, options2, w);
                            if (ch.w.globals.chartID !== _this.w.globals.chartID) {
                                delete options2.series;
                                delete options2.yaxis;
                            }
                            w.config = Utils$1.extend(w.config, options2);
                            if (overwriteInitialConfig) {
                                w.globals.lastXAxis = options2.xaxis ? Utils$1.clone(options2.xaxis) : [];
                                w.globals.lastYAxis = options2.yaxis ? Utils$1.clone(options2.yaxis) : [];
                                w.globals.initialConfig = Utils$1.extend({}, w.config);
                                w.globals.initialSeries = w.config.series;
                            }
                            if (options2.series && (w.globals.collapsedSeriesIndices.length > 0 || w.globals.ancillaryCollapsedSeriesIndices.length > 0)) {
                                ch.series.reconcileCollapsedByName();
                            }
                        }
                        return ch.update(options2).then(function() {
                            if (chartIndex === charts.length - 1) {
                                resolve(ch);
                            }
                        }).catch(reject);
                    });
                });
            }
        },
        {
            /**
   * Private method to update Series.
   *
   * @param {any[]} newSeries - New series which will override the existing
   * @param {boolean} animate
   */ key: "_updateSeries",
            value: function _updateSeries(newSeries, animate) {
                var _this = this;
                var overwriteInitialSeries = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
                return new Promise(function(resolve, reject) {
                    var w = _this.w;
                    w.globals.shouldAnimate = animate;
                    w.globals.dataChanged = true;
                    var prevAxisScaleSig = JSON.stringify({
                        y: (w.globals.yAxisScale || []).map(function(s) {
                            return s ? s.result : null;
                        }),
                        xMin: w.globals.minX,
                        xMax: w.globals.maxX
                    });
                    PerformanceCache.invalidateSelectors(w);
                    if (animate && w.config.chart.animations.enabled) {
                        _this.ctx.series.getPreviousPaths();
                    }
                    var prevSeriesCount = w.config.series.length;
                    var prevDataLengths = w.config.series.map(function(s) {
                        var _a, _b;
                        return (_b = (_a = s == null ? void 0 : s.data) == null ? void 0 : _a.length) != null ? _b : 0;
                    });
                    w.globals.dataReducerRawSeries = null;
                    _this.ctx.data.resetParsingFlags();
                    var parsedState = _this.ctx.data.parseData(newSeries);
                    _this.ctx._writeParsedSeriesData(parsedState.seriesData);
                    _this.ctx._writeParsedRangeData(parsedState.rangeData);
                    _this.ctx._writeParsedCandleData(parsedState.candleData);
                    _this.ctx._writeParsedLabelData(parsedState.labelData);
                    _this.ctx._writeParsedAxisFlags(parsedState.axisFlags);
                    if (overwriteInitialSeries) {
                        if (w.globals.initialConfig) {
                            w.globals.initialConfig.series = w.config.series;
                        }
                        w.globals.initialSeries = w.config.series;
                    }
                    if (_this._canUseFastPath(newSeries, prevSeriesCount, prevDataLengths, w)) {
                        return _this.ctx.fastUpdate(animate, prevAxisScaleSig).then(function() {
                            resolve(_this.ctx);
                        }).catch(reject);
                    }
                    if (_this.ctx._updateStats) _this.ctx._updateStats.full++;
                    return _this.ctx.update().then(function() {
                        resolve(_this.ctx);
                    }).catch(reject);
                });
            }
        },
        {
            /**
   * Returns true if the data-only fast path can be used for this update.
   * Fast path skips rebuilding grid, axes, legend, annotations, and tooltip DOM.
   *
   * Requirements:
   * - Chart has been fully rendered (DOM exists)
   * - Axis chart (non-axis charts like pie always need full rebuild due to radial layout)
   * - Series count unchanged (grid column/row counts depend on it)
   * - Per-series data lengths unchanged (the fast path preserves the axis DOM,
   *   and a changed point count re-slots categories/ticks: with explicit
   *   categories the axis-scale signature can still match, leaving a stale
   *   ruler under the re-slotted marks; length changes also want the full
   *   render so enter/exit and axis transitions run)
   * - No series currently collapsing (collapsed series changes visible data range)
   * - Not a combo chart (combo charts mix types and need coordinated axis recalc)
   * - Not currently zoomed (zoomed charts have altered x-labels that need recalculation)
   * @param {any[]} newSeries
   * @param {number} prevSeriesCount
   * @param {number[]} prevDataLengths
   * @param {import('../../types/internal').ChartStateW} w
   */ key: "_canUseFastPath",
            value: function _canUseFastPath(newSeries, prevSeriesCount, prevDataLengths, w) {
                if (!w.dom.elGraphical) return false;
                if (!w.globals.axisCharts) return false;
                if (newSeries.length !== prevSeriesCount) return false;
                if (newSeries.some(function(s, i) {
                    var _a, _b;
                    return ((_b = (_a = s == null ? void 0 : s.data) == null ? void 0 : _a.length) != null ? _b : 0) !== prevDataLengths[i];
                })) {
                    return false;
                }
                if (w.globals.collapsedSeries.length > 0) return false;
                if (w.globals.ancillaryCollapsedSeries.length > 0) return false;
                if (w.globals.risingSeries.length > 0) return false;
                if (w.globals.comboCharts) return false;
                if (w.interact.zoomed) return false;
                return true;
            }
        },
        {
            /**
   * @param {any} s
   * @param {number} i
   */ key: "_extendSeries",
            value: function _extendSeries(s, i) {
                var w = this.w;
                var ser = w.config.series[i];
                return __spreadProps(__spreadValues({}, /** @type {Record<string,any>} */ w.config.series[i]), {
                    name: s.name ? s.name : /** @type {any} */ ser == null ? void 0 : ser.name,
                    color: s.color ? s.color : /** @type {any} */ ser == null ? void 0 : ser.color,
                    type: s.type ? s.type : /** @type {any} */ ser == null ? void 0 : ser.type,
                    group: s.group ? s.group : /** @type {any} */ ser == null ? void 0 : ser.group,
                    hidden: typeof s.hidden !== "undefined" ? s.hidden : /** @type {any} */ ser == null ? void 0 : ser.hidden,
                    data: s.data ? s.data : /** @type {any} */ ser == null ? void 0 : ser.data,
                    zIndex: typeof s.zIndex !== "undefined" ? s.zIndex : i
                });
            }
        },
        {
            /**
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   */ key: "toggleDataPointSelection",
            value: function toggleDataPointSelection(seriesIndex, dataPointIndex) {
                var w = this.w;
                var elPath = null;
                var parent = ".apexcharts-series[data\\:realIndex='".concat(seriesIndex, "']");
                if (w.globals.axisCharts) {
                    elPath = w.dom.Paper.findOne("".concat(parent, " path[j='").concat(dataPointIndex, "'], ").concat(parent, " circle[j='").concat(dataPointIndex, "'], ").concat(parent, " rect[j='").concat(dataPointIndex, "']"));
                } else {
                    if (typeof dataPointIndex === "undefined") {
                        elPath = w.dom.Paper.findOne("".concat(parent, " path[j='").concat(seriesIndex, "']"));
                        if (w.config.chart.type === "pie" || w.config.chart.type === "polarArea" || w.config.chart.type === "donut") {
                            this.ctx.pie.pieClicked(seriesIndex);
                        }
                    }
                }
                if (elPath) {
                    var graphics = new Graphics(this.w);
                    graphics.pathMouseDown(elPath, /** @type {any} */ null);
                } else {
                    console.warn("toggleDataPointSelection: Element not found");
                    return null;
                }
                return elPath.node ? elPath.node : null;
            }
        },
        {
            /**
   * @param {Record<string, any>} options
   */ key: "forceXAxisUpdate",
            value: function forceXAxisUpdate(options2) {
                var w = this.w;
                var minmax = [
                    "min",
                    "max"
                ];
                minmax.forEach(function(a) {
                    if (typeof options2.xaxis[a] !== "undefined") {
                        w.config.xaxis[a] = options2.xaxis[a];
                        w.globals.lastXAxis[a] = options2.xaxis[a];
                    }
                });
                if (options2.xaxis.categories && options2.xaxis.categories.length) {
                    w.config.xaxis.categories = options2.xaxis.categories;
                }
                if (w.config.xaxis.convertedCatToNumeric) {
                    var defaults = new Defaults(options2);
                    options2 = defaults.convertCatToNumericXaxis(options2, this.ctx);
                }
                return options2;
            }
        },
        {
            /**
   * @param {Record<string, any>} options
   */ key: "forceYAxisUpdate",
            value: function forceYAxisUpdate(options2) {
                if (options2.chart && options2.chart.stacked && options2.chart.stackType === "100%") {
                    if (Array.isArray(options2.yaxis)) {
                        options2.yaxis.forEach(function(yaxe, index) {
                            options2.yaxis[index].min = 0;
                            options2.yaxis[index].max = 100;
                        });
                    } else {
                        options2.yaxis.min = 0;
                        options2.yaxis.max = 100;
                    }
                }
                return options2;
            }
        },
        {
            /**
   * This function reverts the yaxis and xaxis min/max values to what it was when the chart was defined.
   * This function fixes an important bug where a user might load a new series after zooming in/out of previous series which resulted in wrong min/max
   * Also, this should never be called internally on zoom/pan - the reset should only happen when user calls the updateSeries() function externally
   * The function also accepts an object {xaxis, yaxis} which when present is set as the new xaxis/yaxis
   * @param {Record<string, any>} opts
   */ key: "revertDefaultAxisMinMax",
            value: function revertDefaultAxisMinMax(opts) {
                var _this = this;
                var w = this.w;
                var xaxis = w.globals.lastXAxis;
                var yaxis = w.globals.lastYAxis;
                if (opts && opts.xaxis) {
                    xaxis = opts.xaxis;
                }
                if (opts && opts.yaxis) {
                    yaxis = opts.yaxis;
                }
                var _xaxis = /** @type {any} */ xaxis;
                w.config.xaxis.min = _xaxis.min;
                w.config.xaxis.max = _xaxis.max;
                var getLastYAxis = function getLastYAxis(index) {
                    if (typeof yaxis[index] !== "undefined") {
                        var _y = /** @type {any} */ yaxis[index];
                        w.config.yaxis[index].min = _y.min;
                        w.config.yaxis[index].max = _y.max;
                    }
                };
                w.config.yaxis.map(function(yaxe, index) {
                    if (w.interact.zoomed) {
                        getLastYAxis(index);
                    } else {
                        if (typeof yaxis[index] !== "undefined") {
                            getLastYAxis(index);
                        } else {
                            if (typeof _this.ctx.opts.yaxis[index] !== "undefined") {
                                yaxe.min = _this.ctx.opts.yaxis[index].min;
                                yaxe.max = _this.ctx.opts.yaxis[index].max;
                            }
                        }
                    }
                });
            }
        }
    ]);
    return UpdateHelpers;
}();
var Utils2 = /*#__PURE__*/ function() {
    "use strict";
    function Utils2(tooltipContext) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Utils2);
        this.w = tooltipContext.w;
        this.ttCtx = tooltipContext;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Utils2, [
        {
            /**
   ** When hovering over series, you need to capture which series is being hovered on.
   ** This function will return both capturedseries index as well as inner index of that series
   * @memberof Utils
   * @param {{ hoverArea: any, elGrid: any, clientX: any, clientY: any, context?: any }} opts
   */ key: "getNearestValues",
            value: function getNearestValues(param) {
                var hoverArea = param.hoverArea, elGrid = param.elGrid, clientX = param.clientX, clientY = param.clientY;
                var _a, _b;
                var w = this.w;
                var seriesBound = elGrid.getBoundingClientRect();
                var hoverWidth = seriesBound.width;
                var hoverHeight = seriesBound.height;
                var xDivisor = hoverWidth / (w.globals.dataPoints - 1);
                var yDivisor = hoverHeight / w.globals.dataPoints;
                var hasBars = this.hasBars();
                if ((w.globals.comboCharts || hasBars) && !w.config.xaxis.convertedCatToNumeric) {
                    xDivisor = hoverWidth / w.globals.dataPoints;
                }
                var hoverX = clientX - seriesBound.left - w.globals.barPadForNumericAxis;
                var hoverY = clientY - seriesBound.top;
                var notInRect = hoverX < 0 || hoverY < 0 || hoverX > hoverWidth || hoverY > hoverHeight;
                if (notInRect) {
                    hoverArea.classList.remove("hovering-zoom");
                    hoverArea.classList.remove("hovering-pan");
                } else {
                    if (w.interact.zoomEnabled) {
                        hoverArea.classList.remove("hovering-pan");
                        hoverArea.classList.add("hovering-zoom");
                    } else if (w.interact.panEnabled) {
                        hoverArea.classList.remove("hovering-zoom");
                        hoverArea.classList.add("hovering-pan");
                    }
                }
                var j = Math.round(hoverX / xDivisor);
                var jHorz = Math.floor(hoverY / yDivisor);
                if (hasBars && !w.config.xaxis.convertedCatToNumeric) {
                    j = Math.ceil(hoverX / xDivisor);
                    j = j - 1;
                }
                var capturedSeries = null;
                var closest = null;
                var seriesXValArr = w.globals.seriesXvalues.map(function(seriesXVal) {
                    return seriesXVal.filter(function(s) {
                        return Utils$1.isNumber(s);
                    });
                });
                var seriesYValArr = w.globals.seriesYvalues.map(function(seriesYVal) {
                    return seriesYVal.filter(function(s) {
                        return Utils$1.isNumber(s);
                    });
                });
                if (w.axisFlags.isXNumeric) {
                    var chartGridEl = this.ttCtx.getElGrid();
                    if (!chartGridEl) return {
                        hoverX: hoverX,
                        hoverY: hoverY
                    };
                    var chartGridElBoundingRect = chartGridEl.getBoundingClientRect();
                    var transformedHoverX = hoverX * (chartGridElBoundingRect.width / hoverWidth);
                    var transformedHoverY = hoverY * (chartGridElBoundingRect.height / hoverHeight);
                    closest = this.closestInMultiArray(transformedHoverX, transformedHoverY, seriesXValArr, seriesYValArr);
                    capturedSeries = closest.index;
                    j = (_a = closest.j) != null ? _a : 0;
                    if (capturedSeries !== null && w.globals.hasNullValues) {
                        seriesXValArr = w.globals.seriesXvalues[capturedSeries];
                        closest = this.closestInArray(transformedHoverX, seriesXValArr);
                        j = (_b = closest.j) != null ? _b : 0;
                    }
                }
                w.interact.capturedSeriesIndex = capturedSeries === null ? -1 : capturedSeries;
                if (!j || j < 1) j = 0;
                if (w.globals.isBarHorizontal) {
                    w.interact.capturedDataPointIndex = jHorz;
                } else {
                    w.interact.capturedDataPointIndex = j;
                }
                return {
                    capturedSeries: capturedSeries,
                    j: w.globals.isBarHorizontal ? jHorz : j,
                    hoverX: hoverX,
                    hoverY: hoverY
                };
            }
        },
        {
            /**
   * @param {any[]} Xarrays
   */ key: "getFirstActiveXArray",
            value: function getFirstActiveXArray(Xarrays) {
                var w = this.w;
                var activeIndex = 0;
                var firstActiveSeriesIndex = Xarrays.map(function(xarr, index) {
                    return xarr.length > 0 ? index : -1;
                });
                for(var a = 0; a < firstActiveSeriesIndex.length; a++){
                    if (firstActiveSeriesIndex[a] !== -1 && w.globals.collapsedSeriesIndices.indexOf(a) === -1 && w.globals.ancillaryCollapsedSeriesIndices.indexOf(a) === -1) {
                        activeIndex = firstActiveSeriesIndex[a];
                        break;
                    }
                }
                return activeIndex;
            }
        },
        {
            /**
   * @param {number} hoverX
   * @param {number} hoverY
   * @param {any[]} Xarrays
   * @param {any[]} Yarrays
   */ key: "closestInMultiArray",
            value: function closestInMultiArray(hoverX, hoverY, Xarrays, Yarrays) {
                var w = this.w;
                var isActiveSeries = function isActiveSeries(seriesIndex) {
                    return w.globals.collapsedSeriesIndices.indexOf(seriesIndex) === -1 && w.globals.ancillaryCollapsedSeriesIndices.indexOf(seriesIndex) === -1;
                };
                var chartType = w.config.chart.type;
                var isLineArea = !w.globals.comboCharts && (chartType === "line" || chartType === "area");
                var closestDist = Infinity;
                var closestSeriesIndex = null;
                var closestPointIndex = null;
                if (w.globals.allSeriesHasEqualX) {
                    var bucketDistX = Infinity;
                    for(var i = 0; i < Xarrays.length; i++){
                        if (!isActiveSeries(i)) continue;
                        var xArr = Xarrays[i];
                        var yArr = Yarrays[i];
                        var len = Math.min(xArr.length, yArr.length);
                        for(var j = 0; j < len; j++){
                            var distX = Math.abs(hoverX - xArr[j]);
                            if (distX < bucketDistX) {
                                bucketDistX = distX;
                                closestPointIndex = j;
                            }
                        }
                    }
                    if (closestPointIndex !== null) {
                        if (isLineArea) {
                            var bestSegDist = Infinity;
                            for(var i1 = 0; i1 < Xarrays.length; i1++){
                                if (!isActiveSeries(i1)) continue;
                                var xArr1 = Xarrays[i1];
                                var yArr1 = Yarrays[i1];
                                var len1 = Math.min(xArr1.length, yArr1.length);
                                if (len1 < 2) {
                                    var yVal = yArr1[closestPointIndex];
                                    if (typeof yVal !== "number") continue;
                                    var d = Math.abs(hoverY - yVal);
                                    if (d < bestSegDist) {
                                        bestSegDist = d;
                                        closestSeriesIndex = i1;
                                    }
                                    continue;
                                }
                                for(var j1 = 0; j1 < len1 - 1; j1++){
                                    var seg = this._distanceToSegment(hoverX, hoverY, xArr1[j1], yArr1[j1], xArr1[j1 + 1], yArr1[j1 + 1]);
                                    if (seg.dist < bestSegDist) {
                                        bestSegDist = seg.dist;
                                        closestSeriesIndex = i1;
                                    }
                                }
                            }
                        } else {
                            var bestY = Infinity;
                            for(var i2 = 0; i2 < Xarrays.length; i2++){
                                if (!isActiveSeries(i2)) continue;
                                var yVal1 = Yarrays[i2][closestPointIndex];
                                if (typeof yVal1 !== "number") continue;
                                var distY = Math.abs(hoverY - yVal1);
                                if (distY < bestY) {
                                    bestY = distY;
                                    closestSeriesIndex = i2;
                                }
                            }
                        }
                    }
                    return {
                        index: closestSeriesIndex,
                        j: closestPointIndex
                    };
                }
                for(var i3 = 0; i3 < Xarrays.length; i3++){
                    if (!isActiveSeries(i3)) {
                        continue;
                    }
                    var xArr2 = Xarrays[i3];
                    var yArr2 = Yarrays[i3];
                    var len2 = Math.min(xArr2.length, yArr2.length);
                    if (isLineArea && len2 >= 2) {
                        for(var j2 = 0; j2 < len2 - 1; j2++){
                            var seg1 = this._distanceToSegment(hoverX, hoverY, xArr2[j2], yArr2[j2], xArr2[j2 + 1], yArr2[j2 + 1]);
                            if (seg1.dist < closestDist) {
                                closestDist = seg1.dist;
                                closestSeriesIndex = i3;
                                closestPointIndex = seg1.t < 0.5 ? j2 : j2 + 1;
                            }
                        }
                        continue;
                    }
                    for(var j3 = 0; j3 < len2; j3++){
                        var xVal = xArr2[j3];
                        var distX1 = hoverX - xVal;
                        var yVal2 = yArr2[j3];
                        var distY1 = hoverY - yVal2;
                        var dist = Math.sqrt(distX1 * distX1 + distY1 * distY1);
                        if (dist < closestDist) {
                            closestDist = dist;
                            closestSeriesIndex = i3;
                            closestPointIndex = j3;
                        }
                    }
                }
                return {
                    index: closestSeriesIndex,
                    j: closestPointIndex
                };
            }
        },
        {
            /**
   * Perpendicular distance from point (px, py) to the line segment
   * (ax, ay) → (bx, by). Returns the distance plus the projection
   * parameter t (0 = at A, 1 = at B, clamped) so callers know which
   * endpoint the projection landed nearest.
   * @param {number} px
   * @param {number} py
   * @param {number} ax
   * @param {number} ay
   * @param {number} bx
   * @param {number} by
   * @returns {{ dist: number, t: number }}
   */ key: "_distanceToSegment",
            value: function _distanceToSegment(px, py, ax, ay, bx, by) {
                var dx = bx - ax;
                var dy = by - ay;
                var lenSq = dx * dx + dy * dy;
                var t = lenSq === 0 ? 0 : ((px - ax) * dx + (py - ay) * dy) / lenSq;
                if (t < 0) t = 0;
                else if (t > 1) t = 1;
                var cx = ax + t * dx;
                var cy = ay + t * dy;
                var ex = px - cx;
                var ey = py - cy;
                return {
                    dist: Math.sqrt(ex * ex + ey * ey),
                    t: t
                };
            }
        },
        {
            /**
   * @param {number} val
   * @param {any[]} arr
   */ key: "closestInArray",
            value: function closestInArray(val, arr) {
                var curr = arr[0];
                var currIndex = null;
                var diff = Math.abs(val - curr);
                for(var i = 0; i < arr.length; i++){
                    var newdiff = Math.abs(val - arr[i]);
                    if (newdiff < diff) {
                        diff = newdiff;
                        currIndex = i;
                    }
                }
                return {
                    j: currIndex
                };
            }
        },
        {
            /**
   * When there are multiple series, it is possible to have different x values for each series.
   * But it may be possible in those multiple series, that there is same x value for 2 or more
   * series.
   * @memberof Utils
   * @param {number} j - the inner index of series (series[i][j])
   * @return {boolean}
   */ key: "isXoverlap",
            value: function isXoverlap(j) {
                var w = this.w;
                var xSameForAllSeriesJArr = [];
                var seriesX = w.seriesData.seriesX.filter(function(s) {
                    return typeof s[0] !== "undefined";
                });
                if (seriesX.length > 0) {
                    for(var i = 0; i < seriesX.length - 1; i++){
                        if (typeof seriesX[i][j] !== "undefined" && typeof seriesX[i + 1][j] !== "undefined") {
                            if (seriesX[i][j] !== seriesX[i + 1][j]) {
                                xSameForAllSeriesJArr.push("unEqual");
                            }
                        }
                    }
                }
                if (xSameForAllSeriesJArr.length === 0) {
                    return true;
                }
                return false;
            }
        },
        {
            key: "isInitialSeriesSameLen",
            value: function isInitialSeriesSameLen() {
                var _this = this;
                var _a, _b, _c, _d;
                var sameLen = true;
                var initialSeries = /** @type {any[]} */ ((_b = (_a = this.w.globals._initialSeriesPeek) != null ? _a : this.w.globals.initialSeries) == null ? void 0 : _b.filter(/**
         * @param {Record<string, any>} s
         * @param {number} i
         */ function(s, i) {
                    var _a2;
                    return !((_a2 = _this.w.globals.collapsedSeriesIndices) == null ? void 0 : _a2.includes(i));
                })) || [];
                for(var i = 0; i < initialSeries.length - 1; i++){
                    if (!((_c = initialSeries[i]) == null ? void 0 : _c.data) || !((_d = initialSeries[i + 1]) == null ? void 0 : _d.data)) return true;
                    if (initialSeries[i].data.length !== initialSeries[i + 1].data.length) {
                        sameLen = false;
                        break;
                    }
                }
                return sameLen;
            }
        },
        {
            /**
   * @param {any[]} allbars
   */ key: "getBarsHeight",
            value: function getBarsHeight(allbars) {
                var bars = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(allbars);
                var totalHeight = bars.reduce(function(acc, bar) {
                    return acc + bar.getBBox().height;
                }, 0);
                return totalHeight;
            }
        },
        {
            /**
   * @param {number} capturedSeries
   */ key: "getElMarkers",
            value: function getElMarkers(capturedSeries) {
                if (typeof capturedSeries == "number") {
                    return this.w.dom.baseEl.querySelectorAll(".apexcharts-series[data\\:realIndex='".concat(capturedSeries, "'] .apexcharts-series-markers-wrap > *"));
                }
                return this.w.dom.baseEl.querySelectorAll(".apexcharts-series-markers-wrap > *");
            }
        },
        {
            key: "getAllMarkers",
            value: function getAllMarkers() {
                var _this = this;
                var filterCollapsed = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
                var markersWraps = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this.w.dom.baseEl.querySelectorAll(".apexcharts-series-markers-wrap"));
                if (filterCollapsed) {
                    markersWraps = markersWraps.filter(function(m) {
                        var realIndex = Number(m.getAttribute("data:realIndex"));
                        return _this.w.globals.collapsedSeriesIndices.indexOf(realIndex) === -1;
                    });
                }
                markersWraps.sort(function(a, b) {
                    var indexA = Number(a.getAttribute("data:realIndex"));
                    var indexB = Number(b.getAttribute("data:realIndex"));
                    return indexB < indexA ? 1 : indexB > indexA ? -1 : 0;
                });
                var markers = [];
                markersWraps.forEach(function(m) {
                    markers.push(m.querySelector(".apexcharts-marker"));
                });
                return markers;
            }
        },
        {
            /**
   * @param {number} capturedSeries
   */ key: "hasMarkers",
            value: function hasMarkers(capturedSeries) {
                var markers = this.getElMarkers(capturedSeries);
                return markers.length > 0;
            }
        },
        {
            /**
   * @param {any} point
   * @param {number} size
   */ key: "getPathFromPoint",
            value: function getPathFromPoint(point, size) {
                var cx = Number(point.getAttribute("cx"));
                var cy = Number(point.getAttribute("cy"));
                var shape = point.getAttribute("shape");
                return new Graphics(this.w).getMarkerPath(cx, cy, shape, size);
            }
        },
        {
            key: "getElBars",
            value: function getElBars() {
                return this.w.dom.baseEl.querySelectorAll(".apexcharts-bar-series,  .apexcharts-candlestick-series, .apexcharts-boxPlot-series, .apexcharts-violin-series, .apexcharts-rangebar-series");
            }
        },
        {
            key: "hasBars",
            value: function hasBars() {
                var bars = this.getElBars();
                return bars.length > 0;
            }
        },
        {
            /**
   * @param {number} index
   */ key: "getHoverMarkerSize",
            value: function getHoverMarkerSize(index) {
                var w = this.w;
                var hoverSize = w.config.markers.hover.size;
                if (hoverSize === void 0) {
                    hoverSize = w.globals.markers.size[index] + w.config.markers.hover.sizeOffset;
                }
                return hoverSize;
            }
        },
        {
            /**
   * @param {string} state
   */ key: "toggleAllTooltipSeriesGroups",
            value: function toggleAllTooltipSeriesGroups(state) {
                var w = this.w;
                var ttCtx = this.ttCtx;
                if (ttCtx.allTooltipSeriesGroups.length === 0) {
                    ttCtx.allTooltipSeriesGroups = w.dom.baseEl.querySelectorAll(".apexcharts-tooltip-series-group");
                }
                var allTooltipSeriesGroups = ttCtx.allTooltipSeriesGroups;
                for(var i = 0; i < allTooltipSeriesGroups.length; i++){
                    if (state === "enable") {
                        allTooltipSeriesGroups[i].classList.add("apexcharts-active");
                        allTooltipSeriesGroups[i].style.display = w.config.tooltip.items.display;
                    } else {
                        allTooltipSeriesGroups[i].classList.remove("apexcharts-active");
                        allTooltipSeriesGroups[i].style.display = "none";
                    }
                }
            }
        }
    ]);
    return Utils2;
}();
var Labels = /*#__PURE__*/ function() {
    "use strict";
    function Labels(tooltipContext) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Labels);
        this.w = tooltipContext.w;
        this.ttCtx = tooltipContext;
        this.tooltipUtil = new Utils2(tooltipContext);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Labels, [
        {
            /** @param {{ shared?: boolean, ttItems?: any, i?: number, j?: any, y1?: any, y2?: any, e?: any }} opts */ key: "drawSeriesTexts",
            value: function drawSeriesTexts(param) {
                var _param_shared = param.shared, shared = _param_shared === void 0 ? true : _param_shared, ttItems = param.ttItems, _param_i = param.i, i = _param_i === void 0 ? 0 : _param_i, _param_j = param.j, j = _param_j === void 0 ? null : _param_j, y1 = param.y1, y2 = param.y2, e = param.e;
                var w = this.w;
                if (w.config.tooltip.custom !== void 0) {
                    this.handleCustomTooltip({
                        i: i,
                        j: j,
                        y1: y1,
                        y2: y2,
                        w: w
                    });
                } else {
                    this.toggleActiveInactiveSeries(shared, i);
                }
                var values = this.getValuesToPrint({
                    i: i,
                    j: j
                });
                this.printLabels({
                    i: i,
                    j: j,
                    values: values,
                    ttItems: ttItems,
                    shared: shared,
                    e: e
                });
                var tooltipEl = this.ttCtx.getElTooltip();
                if (tooltipEl) {
                    this.ttCtx.tooltipRect.ttWidth = tooltipEl.getBoundingClientRect().width;
                    this.ttCtx.tooltipRect.ttHeight = tooltipEl.getBoundingClientRect().height;
                }
            }
        },
        {
            /** @param {{i: any, j: any, values: any, ttItems: any, shared: any, e: any}} opts */ key: "printLabels",
            value: function printLabels(param) {
                var i = param.i, j = param.j, values = param.values, ttItems = param.ttItems, shared = param.shared, e = param.e;
                var w = this.w;
                var xVal = values.xVal, zVal = values.zVal, xAxisTTVal = values.xAxisTTVal;
                var seriesLen = w.seriesData.series.length;
                var basePColor = j !== null && w.config.plotOptions.bar.distributed ? w.globals.colors[j] : w.globals.colors[i];
                for(var t = 0; t < seriesLen; t++){
                    var tIndex = w.config.tooltip.inverseOrder ? seriesLen - 1 - t : t;
                    var row = this.computeSeriesRow({
                        i: i,
                        j: j,
                        t: t,
                        tIndex: tIndex,
                        shared: shared,
                        e: e,
                        basePColor: basePColor
                    });
                    this.DOMHandling({
                        i: i,
                        t: tIndex,
                        j: j,
                        ttItems: ttItems,
                        values: {
                            val: row.val,
                            goalVals: row.goalVals,
                            xVal: xVal,
                            xAxisTTVal: xAxisTTVal,
                            zVal: zVal
                        },
                        seriesName: row.seriesName,
                        shared: shared,
                        pColor: row.pColor
                    });
                }
            }
        },
        {
            /**
   * Compute the per-series row values (seriesName, val, goalVals, pColor)
   * for one iteration of the tooltip's series loop. Extracted from
   * printLabels() to keep the outer loop scannable.
   * @param {{i: number, j: any, t: number, tIndex: number, shared: boolean, e: any, basePColor: string}} opts
   */ key: "computeSeriesRow",
            value: function computeSeriesRow(param) {
                var i = param.i, j = param.j, tIndex = param.tIndex, shared = param.shared, e = param.e, basePColor = param.basePColor;
                var w = this.w;
                var f = this.getFormatters(i);
                var pColor = basePColor;
                var val;
                var goalVals = /** @type {any[]} */ [];
                var seriesName = w.config.chart.type === "treemap" ? f.yLbTitleFormatter(String(/** @type {any} */ w.config.series[i].data[j].x), {
                    series: w.seriesData.series,
                    seriesIndex: i,
                    dataPointIndex: j,
                    w: w
                }) : this.getSeriesName({
                    fn: f.yLbTitleFormatter,
                    index: i,
                    seriesIndex: i,
                    j: j
                });
                if (w.globals.axisCharts) {
                    if (shared) {
                        f = this.getFormatters(tIndex);
                        seriesName = this.getSeriesName({
                            fn: f.yLbTitleFormatter,
                            index: tIndex,
                            seriesIndex: i,
                            j: j
                        });
                        pColor = w.globals.colors[tIndex];
                        val = this.formatYValue(f, tIndex, j);
                        goalVals = this.formatGoalVals(f, tIndex, j);
                    } else {
                        pColor = this.resolvePatternColor(e, pColor);
                        val = this.formatYValue(f, i, j);
                        goalVals = this.formatGoalVals(f, i, j);
                    }
                }
                if (j === null) {
                    val = f.yLbFormatter(w.seriesData.series[i], __spreadProps(__spreadValues({}, w), {
                        seriesIndex: i,
                        dataPointIndex: i
                    }));
                }
                return {
                    seriesName: seriesName,
                    val: val,
                    goalVals: goalVals,
                    pColor: pColor
                };
            }
        },
        {
            /**
   * Run the y-value formatter for a given (seriesIndex, dataPointIndex),
   * handling the range-data case (start - end concatenation).
   * @param {{yLbFormatter: Function}} f
   * @param {number} index
   * @param {any} j
   */ key: "formatYValue",
            value: function formatYValue(f, index, j) {
                var _a, _b, _c, _d;
                var w = this.w;
                if (w.axisFlags.isRangeData) {
                    return f.yLbFormatter((_b = (_a = w.rangeData.seriesRangeStart) == null ? void 0 : _a[index]) == null ? void 0 : _b[j], {
                        series: w.rangeData.seriesRangeStart,
                        seriesIndex: index,
                        dataPointIndex: j,
                        w: w
                    }) + " - " + f.yLbFormatter((_d = (_c = w.rangeData.seriesRangeEnd) == null ? void 0 : _c[index]) == null ? void 0 : _d[j], {
                        series: w.rangeData.seriesRangeEnd,
                        seriesIndex: index,
                        dataPointIndex: j,
                        w: w
                    });
                }
                return f.yLbFormatter(w.seriesData.series[index][j], {
                    series: w.seriesData.series,
                    seriesIndex: index,
                    dataPointIndex: j,
                    w: w
                });
            }
        },
        {
            /**
   * Format the goal-line values attached to a given (seriesIndex, dataPointIndex).
   * Returns an empty array when no goals exist.
   * @param {{yLbFormatter: Function}} f
   * @param {number} index
   * @param {any} j
   */ key: "formatGoalVals",
            value: function formatGoalVals(f, index, j) {
                var _a;
                var w = this.w;
                var goals = (_a = w.seriesData.seriesGoals[index]) == null ? void 0 : _a[j];
                if (!Array.isArray(goals)) return [];
                return goals.map(function(goal) {
                    return {
                        attrs: goal,
                        val: f.yLbFormatter(goal.value, {
                            seriesIndex: index,
                            dataPointIndex: j,
                            w: w
                        })
                    };
                });
            }
        },
        {
            /**
   * When the hovered element has a pattern fill (url(#…Pattern…)), reach
   * into the pattern's first child to pull a stroke color. Otherwise
   * return the raw fill attribute or the fallback.
   * @param {any} e
   * @param {string} fallback
   */ key: "resolvePatternColor",
            value: function resolvePatternColor(e, fallback) {
                var _a, _b, _c;
                var w = this.w;
                var targetFill = (_a = e == null ? void 0 : e.target) == null ? void 0 : _a.getAttribute("fill");
                if (!targetFill) return fallback;
                if (targetFill.indexOf("url") === -1) return targetFill;
                if (targetFill.indexOf("Pattern") === -1) return fallback;
                var patternEl = w.dom.baseEl.querySelector(targetFill.substr(4).slice(0, -1));
                return (_c = (_b = patternEl == null ? void 0 : patternEl.childNodes[0]) == null ? void 0 : _b.getAttribute("stroke")) != null ? _c : fallback;
            }
        },
        {
            /**
   * @param {number} i
   */ key: "getFormatters",
            value: function getFormatters(i) {
                var w = this.w;
                var yLbFormatter = w.formatters.yLabelFormatters[i];
                var yLbTitleFormatter;
                if (w.formatters.ttVal !== void 0) {
                    if (Array.isArray(w.formatters.ttVal)) {
                        yLbFormatter = /** @type {any} */ w.formatters.ttVal[i] && /** @type {any} */ w.formatters.ttVal[i].formatter;
                        yLbTitleFormatter = /** @type {any} */ w.formatters.ttVal[i] && /** @type {any} */ w.formatters.ttVal[i].title && /** @type {any} */ w.formatters.ttVal[i].title.formatter;
                    } else {
                        yLbFormatter = /** @type {any} */ w.formatters.ttVal.formatter;
                        if (typeof /** @type {any} */ w.formatters.ttVal.title.formatter === "function") {
                            yLbTitleFormatter = /** @type {any} */ w.formatters.ttVal.title.formatter;
                        }
                    }
                } else {
                    yLbTitleFormatter = w.config.tooltip.y.title.formatter;
                }
                if (typeof yLbFormatter !== "function") {
                    if (w.formatters.yLabelFormatters[0]) {
                        yLbFormatter = w.formatters.yLabelFormatters[0];
                    } else {
                        yLbFormatter = function yLbFormatter(label) {
                            return label;
                        };
                    }
                }
                if (typeof yLbTitleFormatter !== "function") {
                    yLbTitleFormatter = function yLbTitleFormatter(label) {
                        return label ? label + ": " : "";
                    };
                }
                return {
                    yLbFormatter: yLbFormatter,
                    yLbTitleFormatter: yLbTitleFormatter
                };
            }
        },
        {
            /** @param {{fn: any, index: any, seriesIndex: any, j: any}} opts */ key: "getSeriesName",
            value: function getSeriesName(param) {
                var fn = param.fn, index = param.index, seriesIndex = param.seriesIndex, j = param.j;
                var w = this.w;
                return fn(String(w.seriesData.seriesNames[index]), {
                    series: w.seriesData.series,
                    seriesIndex: seriesIndex,
                    dataPointIndex: j,
                    w: w
                });
            }
        },
        {
            /** @param {{ t?: any, j?: any, i?: any, ttItems?: any, values?: any, seriesName?: any, shared?: any, pColor?: any }} opts */ key: "DOMHandling",
            value: function DOMHandling(param) {
                var t = param.t, j = param.j, ttItems = param.ttItems, values = param.values, seriesName = param.seriesName, shared = param.shared, pColor = param.pColor;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var val = values.val, goalVals = values.goalVals, xVal = values.xVal, xAxisTTVal = values.xAxisTTVal, zVal = values.zVal;
                if (!ttItems || !ttItems[t]) return;
                var ttItemsChildren = null;
                ttItemsChildren = ttItems[t].children;
                if (w.config.tooltip.fillSeriesColor) {
                    ttItems[t].style.backgroundColor = pColor;
                    ttItemsChildren[0].style.display = "none";
                }
                if (ttCtx.showTooltipTitle) {
                    if (ttCtx.tooltipTitle === null) {
                        ttCtx.tooltipTitle = w.dom.baseEl.querySelector(".apexcharts-tooltip-title");
                    }
                    if (ttCtx.tooltipTitle) {
                        ttCtx.tooltipTitle.innerHTML = xVal;
                    }
                }
                if (ttCtx.isXAxisTooltipEnabled) {
                    if (ttCtx.xaxisTooltipText) {
                        ttCtx.xaxisTooltipText.innerHTML = xAxisTTVal !== "" ? xAxisTTVal : xVal;
                    }
                }
                var ttYLabel = ttItems[t].querySelector(".apexcharts-tooltip-text-y-label");
                if (ttYLabel) {
                    ttYLabel.innerHTML = seriesName ? seriesName : "";
                }
                var ttYVal = ttItems[t].querySelector(".apexcharts-tooltip-text-y-value");
                if (ttYVal) {
                    ttYVal.innerHTML = typeof val !== "undefined" ? val : "";
                }
                if (ttItemsChildren[0] && ttItemsChildren[0].classList.contains("apexcharts-tooltip-marker")) {
                    if (w.config.tooltip.marker.fillColors && Array.isArray(w.config.tooltip.marker.fillColors)) {
                        pColor = w.config.tooltip.marker.fillColors[t];
                    }
                    if (w.config.tooltip.fillSeriesColor) {
                        ttItemsChildren[0].style.backgroundColor = pColor;
                    } else {
                        ttItemsChildren[0].style.color = pColor;
                    }
                }
                if (!w.config.tooltip.marker.show) {
                    ttItemsChildren[0].style.display = "none";
                }
                var ttGLabel = ttItems[t].querySelector(".apexcharts-tooltip-text-goals-label");
                var ttGVal = ttItems[t].querySelector(".apexcharts-tooltip-text-goals-value");
                if (goalVals.length && w.seriesData.seriesGoals[t]) {
                    var createGoalsHtml = function createGoalsHtml() {
                        var gLabels = "<div>";
                        var gVals = "<div>";
                        goalVals.forEach(function(goal) {
                            gLabels += ' <div style="display: flex"><span class="apexcharts-tooltip-marker" style="background-color: '.concat(goal.attrs.strokeColor, '; height: 3px; border-radius: 0; top: 5px;"></span> ').concat(goal.attrs.name, "</div>");
                            gVals += "<div>".concat(goal.val, "</div>");
                        });
                        ttGLabel.innerHTML = gLabels + "</div>";
                        ttGVal.innerHTML = gVals + "</div>";
                    };
                    if (shared) {
                        if (w.seriesData.seriesGoals[t][j] && Array.isArray(w.seriesData.seriesGoals[t][j])) {
                            createGoalsHtml();
                        } else {
                            ttGLabel.innerHTML = "";
                            ttGVal.innerHTML = "";
                        }
                    } else {
                        createGoalsHtml();
                    }
                } else {
                    ttGLabel.innerHTML = "";
                    ttGVal.innerHTML = "";
                }
                if (zVal !== null) {
                    var ttZLabel = ttItems[t].querySelector(".apexcharts-tooltip-text-z-label");
                    ttZLabel.innerHTML = w.config.tooltip.z.title;
                    var ttZVal = ttItems[t].querySelector(".apexcharts-tooltip-text-z-value");
                    ttZVal.innerHTML = typeof zVal !== "undefined" ? zVal : "";
                }
                if (shared && ttItemsChildren[0]) {
                    if (w.config.tooltip.hideEmptySeries) {
                        var ttItemMarker = ttItems[t].querySelector(".apexcharts-tooltip-marker");
                        var ttItemText = ttItems[t].querySelector(".apexcharts-tooltip-text");
                        if (parseFloat(val) == 0) {
                            ttItemMarker.style.display = "none";
                            ttItemText.style.display = "none";
                        } else {
                            ttItemMarker.style.display = "block";
                            ttItemText.style.display = "block";
                        }
                    }
                    if (typeof val === "undefined" || val === null || w.globals.ancillaryCollapsedSeriesIndices.indexOf(t) > -1 || w.globals.collapsedSeriesIndices.indexOf(t) > -1 || Array.isArray(ttCtx.tConfig.enabledOnSeries) && ttCtx.tConfig.enabledOnSeries.indexOf(t) === -1) {
                        ttItemsChildren[0].parentNode.style.display = "none";
                    } else {
                        ttItemsChildren[0].parentNode.style.display = w.config.tooltip.items.display;
                    }
                } else {
                    if (Array.isArray(ttCtx.tConfig.enabledOnSeries) && ttCtx.tConfig.enabledOnSeries.indexOf(t) === -1) {
                        ttItemsChildren[0].parentNode.style.display = "none";
                    }
                }
            }
        },
        {
            /**
   * @param {boolean} shared
   * @param {number} i
   */ key: "toggleActiveInactiveSeries",
            value: function toggleActiveInactiveSeries(shared, i) {
                var w = this.w;
                if (shared) {
                    this.tooltipUtil.toggleAllTooltipSeriesGroups("enable");
                } else {
                    this.tooltipUtil.toggleAllTooltipSeriesGroups("disable");
                    var firstTooltipSeriesGroup = w.dom.baseEl.querySelector(".apexcharts-tooltip-series-group-".concat(i));
                    if (firstTooltipSeriesGroup) {
                        var ftsGroup = /** @type {HTMLElement} */ firstTooltipSeriesGroup;
                        ftsGroup.classList.add("apexcharts-active");
                        ftsGroup.style.display = w.config.tooltip.items.display;
                    }
                }
            }
        },
        {
            /** @param {{i: any, j: any}} opts */ key: "getValuesToPrint",
            value: function getValuesToPrint(param) {
                var i = param.i, j = param.j;
                var _a, _b, _c, _d, _e, _f, _g, _h;
                var w = this.w;
                var filteredSeriesX = w.seriesData.seriesX.map(function(ser) {
                    return ser.length > 0 ? ser : [];
                });
                var xVal = "";
                var xAxisTTVal = "";
                var zVal = null;
                var val = null;
                var customFormatterOpts = {
                    series: w.seriesData.series,
                    seriesIndex: i,
                    dataPointIndex: j,
                    w: w
                };
                var zFormatter = w.formatters.ttZFormatter;
                if (j === null) {
                    val = w.seriesData.series[i];
                } else {
                    if (w.axisFlags.isXNumeric && w.config.chart.type !== "treemap") {
                        xVal = filteredSeriesX[i][j];
                        if (filteredSeriesX[i].length === 0) {
                            var firstActiveSeriesIndex = this.tooltipUtil.getFirstActiveXArray(filteredSeriesX);
                            xVal = filteredSeriesX[firstActiveSeriesIndex][j];
                        }
                    } else {
                        var dataFormat = new Data(this.w);
                        if (dataFormat.isFormatXY()) {
                            xVal = typeof /** @type {any} */ w.config.series[i].data[j] !== "undefined" ? /** @type {any} */ w.config.series[i].data[j].x : "";
                        } else {
                            xVal = typeof w.labelData.labels[j] !== "undefined" ? w.labelData.labels[j] : "";
                        }
                    }
                }
                var bufferXVal = xVal;
                if (w.axisFlags.isXNumeric && w.config.xaxis.type === "datetime") {
                    var xFormat = new Formatters(this.w);
                    xVal = xFormat.xLabelFormat(/** @type {Function} */ w.formatters.ttKeyFormatter, bufferXVal, bufferXVal, {
                        i: void 0,
                        dateFormatter: new DateTime(this.w).formatDate,
                        w: this.w
                    });
                } else {
                    if (w.globals.isBarHorizontal) {
                        xVal = w.formatters.yLabelFormatters[0](bufferXVal, customFormatterOpts);
                    } else {
                        xVal = (_c = (_b = (_a = w.formatters).xLabelFormatter) == null ? void 0 : _b.call(_a, bufferXVal, customFormatterOpts)) != null ? _c : bufferXVal;
                    }
                }
                if (w.config.tooltip.x.formatter !== void 0) {
                    xVal = (_f = (_e = (_d = w.formatters).ttKeyFormatter) == null ? void 0 : _e.call(_d, bufferXVal, customFormatterOpts)) != null ? _f : bufferXVal;
                }
                if (w.seriesData.seriesZ.length > 0 && w.seriesData.seriesZ[i].length > 0) {
                    zVal = zFormatter == null ? void 0 : zFormatter(w.seriesData.seriesZ[i][j], w);
                }
                if (typeof w.config.xaxis.tooltip.formatter === "function") {
                    xAxisTTVal = (_h = (_g = w.formatters).xaxisTooltipFormatter) == null ? void 0 : _h.call(_g, bufferXVal, customFormatterOpts);
                } else {
                    xAxisTTVal = xVal;
                }
                return {
                    val: Array.isArray(val) ? val.join(" ") : val,
                    xVal: Array.isArray(xVal) ? xVal.join(" ") : xVal,
                    xAxisTTVal: Array.isArray(xAxisTTVal) ? xAxisTTVal.join(" ") : xAxisTTVal,
                    zVal: zVal
                };
            }
        },
        {
            /** @param {{i: any, j: any, y1: any, y2: any, w: any}} opts */ key: "handleCustomTooltip",
            value: function handleCustomTooltip(param) {
                var i = param.i, j = param.j, y1 = param.y1, y2 = param.y2, w = param.w;
                var tooltipEl = this.ttCtx.getElTooltip();
                var fn = w.config.tooltip.custom;
                if (Array.isArray(fn)) {
                    fn = fn[i];
                }
                if (typeof fn !== "function") return;
                var customTooltip = fn({
                    series: w.seriesData.series,
                    seriesIndex: i,
                    dataPointIndex: j,
                    y1: y1,
                    y2: y2,
                    w: w
                });
                if (tooltipEl) {
                    var arrowEl = tooltipEl.querySelector(".apexcharts-tooltip-arrow");
                    if (typeof customTooltip === "string" || typeof customTooltip === "number") {
                        tooltipEl.innerHTML = String(customTooltip);
                    } else if (customTooltip != null && ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(customTooltip, Element) || typeof customTooltip.nodeName === "string")) {
                        tooltipEl.innerHTML = "";
                        tooltipEl.appendChild(customTooltip.cloneNode(true));
                    }
                    if (arrowEl) tooltipEl.appendChild(arrowEl);
                }
            }
        }
    ]);
    return Labels;
}();
var ARROW_TIP_OVERHANG = 7;
var Position = /*#__PURE__*/ function() {
    "use strict";
    function Position(tooltipContext) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Position);
        this.ttCtx = tooltipContext;
        this.w = tooltipContext.w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Position, [
        {
            /**
   * This will move the crosshair (the vertical/horz line that moves along with mouse)
   * Along with this, this function also calls the xaxisMove function
   * @memberof Position
   * @param {number} cx - point's x position, wherever point's x is, you need to move crosshair
   * @param {number | null} [j]
   */ key: "moveXCrosshairs",
            value: function moveXCrosshairs(cx) {
                var j = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
                var ttCtx = this.ttCtx;
                var w = this.w;
                var xcrosshairs = ttCtx.getElXCrosshairs();
                var x = cx - ttCtx.xcrosshairsWidth / 2;
                var tickAmount = w.labelData.labels.slice().length;
                if (j !== null) {
                    x = w.layout.gridWidth / tickAmount * j;
                }
                if (xcrosshairs !== null && !w.globals.isBarHorizontal) {
                    xcrosshairs.setAttribute("x", String(x));
                    xcrosshairs.setAttribute("x1", String(x));
                    xcrosshairs.setAttribute("x2", String(x));
                    xcrosshairs.setAttribute("y2", String(w.layout.gridHeight));
                    xcrosshairs.classList.add("apexcharts-active");
                }
                if (x < 0) {
                    x = 0;
                }
                if (x > w.layout.gridWidth) {
                    x = w.layout.gridWidth;
                }
                if (ttCtx.isXAxisTooltipEnabled) {
                    var tx = x;
                    if (w.config.xaxis.crosshairs.width === "tickWidth" || w.config.xaxis.crosshairs.width === "barWidth") {
                        tx = x + ttCtx.xcrosshairsWidth / 2;
                    }
                    this.moveXAxisTooltip(tx);
                }
            }
        },
        {
            /**
   * This will move the crosshair (the vertical/horz line that moves along with mouse)
   * Along with this, this function also calls the xaxisMove function
   * @memberof Position
   * @param {number} cy - point's y position, wherever point's y is, you need to move crosshair
   */ key: "moveYCrosshairs",
            value: function moveYCrosshairs(cy) {
                var ttCtx = this.ttCtx;
                if (ttCtx.ycrosshairs !== null) {
                    Graphics.setAttrs(ttCtx.ycrosshairs, {
                        y1: cy,
                        y2: cy
                    });
                }
                if (ttCtx.ycrosshairsHidden !== null) {
                    Graphics.setAttrs(ttCtx.ycrosshairsHidden, {
                        y1: cy,
                        y2: cy
                    });
                }
            }
        },
        {
            /**
   ** AxisTooltip is the small rectangle which appears on x axis with x value, when user moves
   * @memberof Position
   * @param {number} cx - point's x position, wherever point's x is, you need to move
   */ key: "moveXAxisTooltip",
            value: function moveXAxisTooltip(cx) {
                var _a, _b;
                var w = this.w;
                var ttCtx = this.ttCtx;
                if (ttCtx.xaxisTooltip !== null && ttCtx.xcrosshairsWidth !== 0) {
                    ttCtx.xaxisTooltip.classList.add("apexcharts-active");
                    var cy = ttCtx.xaxisOffY + w.config.xaxis.tooltip.offsetY + w.layout.translateY + 5 + w.config.xaxis.offsetY;
                    var xaxisTTText = ttCtx.xaxisTooltip.getBoundingClientRect();
                    var xaxisTTTextWidth = xaxisTTText.width;
                    cx = cx - xaxisTTTextWidth / 2;
                    if (!isNaN(cx)) {
                        cx = cx + w.layout.translateX;
                        var graphics = new Graphics(this.w);
                        var textRect = graphics.getTextRects((_b = (_a = ttCtx.xaxisTooltipText) == null ? void 0 : _a.innerHTML) != null ? _b : "", w.config.xaxis.labels.style.fontSize);
                        if (ttCtx.xaxisTooltipText) {
                            ttCtx.xaxisTooltipText.style.minWidth = textRect.width + "px";
                        }
                        ttCtx.xaxisTooltip.style.left = cx + "px";
                        ttCtx.xaxisTooltip.style.top = cy + "px";
                    }
                }
            }
        },
        {
            /**
   * @param {number} index
   */ key: "moveYAxisTooltip",
            value: function moveYAxisTooltip(index) {
                var _a, _b;
                var w = this.w;
                var ttCtx = this.ttCtx;
                if (ttCtx.yaxisTTEls === null) {
                    ttCtx.yaxisTTEls = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.dom.baseEl.querySelectorAll(".apexcharts-yaxistooltip"));
                }
                var ycrosshairsHiddenRectY1 = parseInt((_b = (_a = ttCtx.ycrosshairsHidden) == null ? void 0 : _a.getAttribute("y1")) != null ? _b : "0", 10);
                var cy = w.layout.translateY + ycrosshairsHiddenRectY1;
                if (ttCtx.yaxisTTEls) {
                    var yAxisTTRect = ttCtx.yaxisTTEls[index].getBoundingClientRect();
                    var yAxisTTHeight = yAxisTTRect.height;
                    var cx;
                    var labelsGroup = /** @type {SVGGElement | null} */ w.dom.baseEl.querySelector(".apexcharts-yaxis[rel='".concat(index, "'] .apexcharts-yaxis-texts-g"));
                    var elWrapRect = w.dom.elWrap.getBoundingClientRect();
                    if (labelsGroup) {
                        var lr = labelsGroup.getBoundingClientRect();
                        if (lr.width > 0) {
                            var labelsCenterInElWrap = lr.left + lr.width / 2 - elWrapRect.left;
                            cx = labelsCenterInElWrap - yAxisTTRect.width / 2;
                        }
                    }
                    if (cx == null) {
                        var GAP = 4;
                        cx = w.config.yaxis[index].opposite ? w.globals.translateYAxisX[index] + GAP : w.globals.translateYAxisX[index] - yAxisTTRect.width - GAP;
                    }
                    cy = cy - yAxisTTHeight / 2;
                    if (w.globals.ignoreYAxisIndexes.indexOf(index) === -1 && cy > 0 && cy < w.layout.gridHeight) {
                        ttCtx.yaxisTTEls[index].classList.add("apexcharts-active");
                        ttCtx.yaxisTTEls[index].style.top = cy + "px";
                        ttCtx.yaxisTTEls[index].style.left = cx + w.config.yaxis[index].tooltip.offsetX + "px";
                    } else {
                        ttCtx.yaxisTTEls[index].classList.remove("apexcharts-active");
                    }
                }
            }
        },
        {
            /**
   ** moves the whole tooltip by changing x, y attrs
   * @memberof Position
   * @param {number} cx - point's x position, wherever point's x is, you need to move tooltip
   * @param {number} cy - point's y position, wherever point's y is, you need to move tooltip
   * @param {number | null} [markerSize] - point's size
   */ key: "moveTooltip",
            value: function moveTooltip(cx, cy) {
                var markerSize = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
                var ttCtx = this.ttCtx;
                var tooltipEl = ttCtx.getElTooltip();
                if (!tooltipEl) return;
                var pos = this.computeTooltipPosition(cx, cy, markerSize);
                if (pos === null) return;
                this.applyTooltipPosition(tooltipEl, pos);
            }
        },
        {
            /**
   * Pure-ish (reads from `this.ttCtx` + `this.w` but performs no DOM writes)
   * computation of the tooltip box position, edge placement (for arrow),
   * and arrow vertical offset. Returns null when inputs are not numeric.
   *
   * @param {number} cx
   * @param {number} cy
   * @param {number | null} [markerSize]
   * @returns {{ x: number, y: number, placement: 'left'|'right', arrowY: number|null } | null}
   */ key: "computeTooltipPosition",
            value: function computeTooltipPosition(cx, cy) {
                var markerSize = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
                var _a, _b, _c, _d, _e, _f, _g;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var tooltipRect = ttCtx.tooltipRect;
                var arrowEnabled = !!w.config.tooltip.arrow;
                var pointSize = markerSize !== null ? parseFloat(String(markerSize)) : 1;
                var ttH = tooltipRect.ttHeight || 0;
                var ttW = tooltipRect.ttWidth || 0;
                var cxNum = parseFloat(String(cx));
                var cyNum = parseFloat(String(cy));
                if (isNaN(cxNum) || isNaN(cyNum)) return null;
                var x = cxNum + pointSize + 5;
                var pointY = cyNum + w.layout.translateY;
                var y = arrowEnabled ? pointY - ttH / 2 + pointSize / 2 : cyNum + pointSize / 2;
                var placement = "right";
                if (x > w.layout.gridWidth / 2) {
                    x = x - ttW - pointSize - 10;
                    placement = "left";
                }
                if (x > w.layout.gridWidth - ttW - 10) {
                    x = w.layout.gridWidth - ttW;
                }
                if (x < -20) {
                    x = -20;
                }
                if (w.config.tooltip.followCursor) {
                    var elGrid = ttCtx.getElGrid();
                    if (!elGrid) return null;
                    var seriesBound = elGrid.getBoundingClientRect();
                    x = ttCtx.e.clientX - seriesBound.left;
                    if (x > w.layout.gridWidth / 2) {
                        x = x - ttW;
                        placement = "left";
                    } else {
                        placement = "right";
                    }
                    y = ttCtx.e.clientY + w.layout.translateY - seriesBound.top;
                    if (y > w.layout.gridHeight / 2) {
                        y = y - ttH;
                    }
                } else {
                    if (!w.globals.isBarHorizontal) {
                        if (arrowEnabled) {
                            var gridTop = w.layout.translateY;
                            var gridBottom = w.layout.translateY + w.layout.gridHeight;
                            if (y + ttH > gridBottom) {
                                y = gridBottom - ttH;
                            }
                            if (y < gridTop) {
                                y = gridTop;
                            }
                        } else {
                            if (ttH / 2 + y > w.layout.gridHeight) {
                                y = w.layout.gridHeight - ttH + w.layout.translateY;
                            }
                        }
                    }
                }
                if (isNaN(x)) return null;
                x = x + w.layout.translateX;
                var a11y = (_b = (_a = w.config) == null ? void 0 : _a.chart) == null ? void 0 : _b.accessibility;
                if ((a11y == null ? void 0 : a11y.enabled) && ((_d = (_c = a11y == null ? void 0 : a11y.keyboard) == null ? void 0 : _c.navigation) == null ? void 0 : _d.enabled) && ((_g = (_f = (_e = w.dom) == null ? void 0 : _e.baseEl) == null ? void 0 : _f.querySelector) == null ? void 0 : _g.call(_f, ".apexcharts-keyboard-focused"))) {
                    var refPointY = arrowEnabled ? pointY : cyNum;
                    var margin = (pointSize || 1) + 12;
                    var tooltipTop = y;
                    var tooltipBottom = y + ttH;
                    if (!isNaN(refPointY) && ttH > 0 && tooltipTop < refPointY + margin && tooltipBottom > refPointY - margin) {
                        y = refPointY - ttH - margin;
                        if (y < 0) {
                            y = refPointY + margin;
                        }
                    }
                }
                var arrowY = null;
                if (arrowEnabled && ttH > 0) {
                    var localY = pointY - y;
                    var minArrowY = 10;
                    var maxArrowY = ttH - 10;
                    arrowY = Math.max(minArrowY, Math.min(maxArrowY, localY));
                }
                return {
                    x: x,
                    y: y,
                    placement: placement,
                    arrowY: arrowY
                };
            }
        },
        {
            /**
   * Single DOM-writer used by every positioning path on the main tooltip.
   * Replaces the duplicated `style.left/top` writes that previously lived
   * in Position.moveTooltip, Tooltip.drawFixedTooltipRect, and Intersect.
   *
   * @param {HTMLElement} tooltipEl
   * @param {{
   *   x: number,
   *   y: number,
   *   placement?: 'left'|'right'|'top'|'bottom',
   *   arrowY?: number|null,
   *   arrowX?: number|null,
   * }} pos
   */ key: "applyTooltipPosition",
            value: function applyTooltipPosition(tooltipEl, pos) {
                if (!tooltipEl) return;
                var firstPaint = tooltipEl.dataset.positioned !== "true";
                if (firstPaint) {
                    tooltipEl.style.transitionProperty = "none";
                }
                tooltipEl.style.left = pos.x + "px";
                tooltipEl.style.top = pos.y + "px";
                if (pos.placement) {
                    tooltipEl.dataset.placement = pos.placement;
                }
                if (pos.arrowY != null) {
                    tooltipEl.style.setProperty("--apx-tt-arrow-y", pos.arrowY + "px");
                }
                if (pos.arrowX != null) {
                    tooltipEl.style.setProperty("--apx-tt-arrow-x", pos.arrowX + "px");
                }
                if (firstPaint) {
                    void tooltipEl.offsetWidth;
                    tooltipEl.dataset.positioned = "true";
                    requestAnimationFrame(function() {
                        tooltipEl.style.transitionProperty = "";
                    });
                }
            }
        },
        {
            /**
   * @param {number} i
   * @param {number} j
   */ key: "moveMarkers",
            value: function moveMarkers(i, j) {
                var _a;
                var w = this.w;
                var ttCtx = this.ttCtx;
                if (w.globals.markers.size[i] > 0) {
                    var allPoints = w.dom.baseEl.querySelectorAll(" .apexcharts-series[data\\:realIndex='".concat(i, "'] .apexcharts-marker"));
                    for(var p = 0; p < allPoints.length; p++){
                        if (parseInt((_a = allPoints[p].getAttribute("rel")) != null ? _a : "0", 10) === j) {
                            ttCtx.marker.resetPointsSize();
                            ttCtx.marker.enlargeCurrentPoint(j, allPoints[p]);
                        }
                    }
                } else {
                    ttCtx.marker.resetPointsSize();
                    this.moveDynamicPointOnHover(j, i);
                }
            }
        },
        {
            // This function is used when you need to show markers/points only on hover -
            // DIFFERENT X VALUES in multiple series
            /**
   * @param {number} j
   * @param {number} capturedSeries
   */ key: "moveDynamicPointOnHover",
            value: function moveDynamicPointOnHover(j, capturedSeries) {
                var _a, _b, _c, _d, _e;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var cx = 0;
                var cy = 0;
                var graphics = new Graphics(this.w);
                var pointsArr = w.globals.pointsArray;
                var hoverSize = ttCtx.tooltipUtil.getHoverMarkerSize(capturedSeries);
                var serType = /** @type {any} */ w.config.series[capturedSeries].type;
                if (serType && (serType === "column" || serType === "candlestick" || serType === "boxPlot" || serType === "violin")) {
                    return;
                }
                cx = (_b = (_a = pointsArr[capturedSeries]) == null ? void 0 : _a[j]) == null ? void 0 : _b[0];
                cy = ((_d = (_c = pointsArr[capturedSeries]) == null ? void 0 : _c[j]) == null ? void 0 : _d[1]) || 0;
                var point = w.dom.baseEl.querySelector(".apexcharts-series[data\\:realIndex='".concat(capturedSeries, "'] .apexcharts-series-markers path"));
                if (point && cy < w.layout.gridHeight && cy > 0) {
                    var shape = (_e = point.getAttribute("shape")) != null ? _e : "circle";
                    var path = graphics.getMarkerPath(cx, cy, shape, hoverSize * 1.5);
                    point.setAttribute("d", path);
                }
                this.moveXCrosshairs(cx);
                if (!ttCtx.fixedTooltip) {
                    this.moveTooltip(cx, cy, hoverSize);
                }
            }
        },
        {
            // This function is used when you need to show markers/points only on hover -
            // SAME X VALUES in multiple series
            /**
   * @param {number} j
   */ key: "moveDynamicPointsOnHover",
            value: function moveDynamicPointsOnHover(j) {
                var _a, _b;
                var ttCtx = this.ttCtx;
                var w = ttCtx.w;
                var cx = 0;
                var cy = 0;
                var activeSeries = 0;
                var pointsArr = w.globals.pointsArray;
                var series = new Series(this.w);
                var graphics = new Graphics(this.w);
                activeSeries = series.getActiveConfigSeriesIndex("asc", [
                    "line",
                    "area",
                    "scatter",
                    "bubble"
                ]);
                var hoverSize = ttCtx.tooltipUtil.getHoverMarkerSize(activeSeries);
                if ((_a = pointsArr[activeSeries]) == null ? void 0 : _a[j]) {
                    cx = pointsArr[activeSeries][j][0];
                    cy = pointsArr[activeSeries][j][1];
                }
                if (isNaN(cx)) {
                    return;
                }
                var points = ttCtx.tooltipUtil.getAllMarkers();
                if (points.length) {
                    for(var p = 0; p < w.seriesData.series.length; p++){
                        var pointArr = pointsArr[p];
                        if (w.globals.comboCharts) {
                            if (typeof pointArr === "undefined") {
                                points.splice(p, 0, null);
                            }
                        }
                        if (points[p] && pointArr && pointArr.length) {
                            var pcy = pointsArr[p][j][1];
                            var pcy2 = void 0;
                            points[p].setAttribute("cx", cx);
                            var shape = (_b = points[p].getAttribute("shape")) != null ? _b : "circle";
                            if (w.config.chart.type === "rangeArea" && !w.globals.comboCharts) {
                                var rangeStartIndex = j + w.seriesData.series[p].length;
                                pcy2 = pointsArr[p][rangeStartIndex][1];
                                var pcyDiff = Math.abs(pcy - pcy2) / 2;
                                pcy = pcy - pcyDiff;
                            }
                            if (pcy !== null && !isNaN(pcy) && pcy < w.layout.gridHeight + hoverSize && pcy + hoverSize > 0) {
                                var path = graphics.getMarkerPath(cx, pcy, shape, hoverSize);
                                points[p].setAttribute("d", path);
                            } else {
                                points[p].setAttribute("d", "");
                            }
                        }
                    }
                }
                this.moveXCrosshairs(cx);
                if (!ttCtx.fixedTooltip) {
                    this.moveTooltip(cx, cy || w.layout.gridHeight, hoverSize);
                }
            }
        },
        {
            /**
   * @param {number} j
   * @param {number} capturedSeries
   */ key: "moveStickyTooltipOverBars",
            value: function moveStickyTooltipOverBars(j, capturedSeries) {
                var _a, _b, _c, _d, _e;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var barLen = w.globals.columnSeries ? /** @type {any} */ w.globals.columnSeries.length : w.seriesData.series.length;
                if (w.config.chart.stacked) {
                    barLen = w.globals.barGroups.length;
                }
                var i = barLen >= 2 && barLen % 2 === 0 ? Math.floor(barLen / 2) : Math.floor(barLen / 2) + 1;
                if (w.globals.isBarHorizontal) {
                    var series = new Series(this.w);
                    i = series.getActiveConfigSeriesIndex("desc") + 1;
                }
                var jBar = w.dom.baseEl.querySelector(".apexcharts-bar-series .apexcharts-series[rel='".concat(i, "'] path[j='").concat(j, "'], .apexcharts-candlestick-series .apexcharts-series[rel='").concat(i, "'] path[j='").concat(j, "'], .apexcharts-boxPlot-series .apexcharts-series[rel='").concat(i, "'] path[j='").concat(j, "'], .apexcharts-violin-series .apexcharts-series[rel='").concat(i, "'] path[j='").concat(j, "'], .apexcharts-rangebar-series .apexcharts-series[rel='").concat(i, "'] path[j='").concat(j, "']"));
                if (!jBar && typeof capturedSeries === "number") {
                    jBar = w.dom.baseEl.querySelector(".apexcharts-bar-series .apexcharts-series[data\\:realIndex='".concat(capturedSeries, "'] path[j='").concat(j, "'],\n        .apexcharts-candlestick-series .apexcharts-series[data\\:realIndex='").concat(capturedSeries, "'] path[j='").concat(j, "'],\n        .apexcharts-boxPlot-series .apexcharts-series[data\\:realIndex='").concat(capturedSeries, "'] path[j='").concat(j, "'],\n        .apexcharts-violin-series .apexcharts-series[data\\:realIndex='").concat(capturedSeries, "'] path[j='").concat(j, "'],\n        .apexcharts-rangebar-series .apexcharts-series[data\\:realIndex='").concat(capturedSeries, "'] path[j='").concat(j, "']"));
                }
                var bc = null;
                var bcc = /** @type {any} */ w.globals.barCanvasCoords;
                if (!jBar && bcc) {
                    bc = typeof capturedSeries === "number" && ((_a = bcc[capturedSeries]) == null ? void 0 : _a[j]) || null;
                    if (!bc) {
                        for(var key in bcc){
                            if ((_b = bcc[key]) == null ? void 0 : _b[j]) {
                                bc = bcc[key][j];
                                break;
                            }
                        }
                    }
                }
                var bcx = jBar ? parseFloat((_c = jBar.getAttribute("cx")) != null ? _c : "0") : bc ? bc.cx : 0;
                var bcy = jBar ? parseFloat((_d = jBar.getAttribute("cy")) != null ? _d : "0") : bc ? bc.cy : 0;
                var bw = jBar ? parseFloat((_e = jBar.getAttribute("barWidth")) != null ? _e : "0") : bc ? bc.barWidth : 0;
                var elGrid = ttCtx.getElGrid();
                if (!elGrid) return;
                var seriesBound = elGrid.getBoundingClientRect();
                var isBoxOrCandle = jBar && (jBar.classList.contains("apexcharts-candlestick-area") || jBar.classList.contains("apexcharts-boxPlot-area"));
                if (w.axisFlags.isXNumeric) {
                    if (jBar && !isBoxOrCandle) {
                        var center = this._datapointCenterXFromBars(j, seriesBound);
                        if (center != null) {
                            bcx = center;
                        } else {
                            bcx = bcx - (barLen % 2 !== 0 ? bw / 2 : 0);
                        }
                    }
                    if (jBar && // fixes apexcharts.js#2354
                    isBoxOrCandle) {
                        bcx = bcx - bw / 2;
                    }
                } else {
                    if (!w.globals.isBarHorizontal && !bc) {
                        bcx = ttCtx.xAxisTicksPositions[j - 1] + ttCtx.dataPointsDividedWidth / 2;
                        if (isNaN(bcx)) {
                            bcx = ttCtx.xAxisTicksPositions[j] - ttCtx.dataPointsDividedWidth / 2;
                        }
                    }
                }
                if (!w.globals.isBarHorizontal) {
                    if (w.config.tooltip.followCursor) {
                        bcy = ttCtx.e.clientY - seriesBound.top - ttCtx.tooltipRect.ttHeight / 2;
                    } else {
                        if (bcy + ttCtx.tooltipRect.ttHeight + 15 > w.layout.gridHeight) {
                            bcy = w.layout.gridHeight;
                        }
                    }
                } else {
                    bcy = bcy - ttCtx.tooltipRect.ttHeight;
                }
                if (!w.globals.isBarHorizontal) {
                    this.moveXCrosshairs(bcx);
                }
                if (!ttCtx.fixedTooltip) {
                    if (w.globals.isBarHorizontal && !w.config.tooltip.followCursor) {
                        var placed = this.placeHorizontalSharedTooltip(j);
                        if (placed) return;
                    }
                    this.moveTooltip(bcx, bcy || w.layout.gridHeight);
                }
            }
        },
        {
            /**
   * Place tooltip above (or flipped: below) the union rect of all bars at
   * dataPointIndex `j` for horizontal-bar-likes. Returns true when a
   * Compute the true horizontal center of dataPointIndex `j` in grid-local
   * coords from the union of every visible bar's `getBoundingClientRect()`.
   * Used as a replacement for the (buggy on numeric/datetime xaxis) `cx`
   * attribute math in `moveStickyTooltipOverBars`. Returns null when no
   * usable bars are found.
   * @param {number} j
   * @param {DOMRect} gridRect
   * @returns {number | null}
   */ key: "_datapointCenterXFromBars",
            value: function _datapointCenterXFromBars(j, gridRect) {
                var _a, _b;
                var w = this.w;
                var bars = w.dom.baseEl.querySelectorAll(".apexcharts-bar-series path[j='".concat(j, "'],.apexcharts-rangebar-series path[j='").concat(j, "']"));
                if (!bars.length) return null;
                var unionLeft = Infinity;
                var unionRight = -Infinity;
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = bars[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var bar = _step.value;
                        var parent = /** @type {Element|null} */ bar.parentNode;
                        if ((_b = (_a = parent == null ? void 0 : parent.classList) == null ? void 0 : _a.contains) == null ? void 0 : _b.call(_a, "apexcharts-series-collapsed")) continue;
                        var r = /** @type {Element} */ bar.getBoundingClientRect();
                        if (r.width === 0 && r.height === 0) continue;
                        if (r.left < unionLeft) unionLeft = r.left;
                        if (r.right > unionRight) unionRight = r.right;
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                if (!isFinite(unionLeft)) return null;
                return (unionLeft + unionRight) / 2 - gridRect.left - (w.globals.barPadForNumericAxis || 0);
            }
        },
        {
            /**
   * Place tooltip above (or flipped: below) the union rect of all bars at
   * dataPointIndex `j` for horizontal-bar-likes. Returns true when a
   * placement was applied; false when no bars found (caller falls back).
   * @param {number} j
   * @returns {boolean}
   */ key: "placeHorizontalSharedTooltip",
            value: function placeHorizontalSharedTooltip(j) {
                var _a, _b;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var tooltipEl = ttCtx.getElTooltip();
                if (!tooltipEl) return false;
                var elGrid = ttCtx.getElGrid();
                if (!elGrid) return false;
                var gridRect = elGrid.getBoundingClientRect();
                var bars = w.dom.baseEl.querySelectorAll(".apexcharts-bar-series path[j='".concat(j, "'],.apexcharts-rangebar-series path[j='").concat(j, "'],.apexcharts-boxPlot-series path[j='").concat(j, "']"));
                if (!bars.length) return false;
                var unionLeft = Infinity;
                var unionRight = -Infinity;
                var unionTop = Infinity;
                var unionBottom = -Infinity;
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = bars[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var bar = _step.value;
                        var parent = /** @type {Element|null} */ bar.parentNode;
                        if ((_b = (_a = parent == null ? void 0 : parent.classList) == null ? void 0 : _a.contains) == null ? void 0 : _b.call(_a, "apexcharts-series-collapsed")) continue;
                        var r = /** @type {Element} */ bar.getBoundingClientRect();
                        if (r.width === 0 && r.height === 0) continue;
                        if (r.left < unionLeft) unionLeft = r.left;
                        if (r.right > unionRight) unionRight = r.right;
                        if (r.top < unionTop) unionTop = r.top;
                        if (r.bottom > unionBottom) unionBottom = r.bottom;
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                if (!isFinite(unionLeft)) return false;
                var ttW = ttCtx.tooltipRect.ttWidth || 0;
                var ttH = ttCtx.tooltipRect.ttHeight || 0;
                var rowCenterX = (unionLeft + unionRight) / 2 - gridRect.left + w.layout.translateX;
                var rowTopElWrap = unionTop - gridRect.top + w.layout.translateY;
                var rowBottomElWrap = unionBottom - gridRect.top + w.layout.translateY;
                var gridTop = w.layout.translateY;
                var gridBottom = w.layout.translateY + w.layout.gridHeight;
                var gridLeft = w.layout.translateX;
                var gridRight = w.layout.translateX + w.layout.gridWidth;
                var placement = "top";
                var finalY = rowTopElWrap - ttH - ARROW_TIP_OVERHANG;
                if (finalY < gridTop) {
                    var belowTop = rowBottomElWrap + ARROW_TIP_OVERHANG;
                    if (belowTop + ttH <= gridBottom) {
                        placement = "bottom";
                        finalY = belowTop;
                    }
                }
                var finalX = rowCenterX - ttW / 2;
                if (finalX < gridLeft) finalX = gridLeft;
                if (finalX + ttW > gridRight) finalX = gridRight - ttW;
                var arrowX = Math.max(10, Math.min(ttW - 10, rowCenterX - finalX));
                this.applyTooltipPosition(tooltipEl, {
                    x: finalX,
                    y: finalY,
                    placement: placement,
                    arrowY: null,
                    arrowX: arrowX
                });
                return true;
            }
        }
    ]);
    return Position;
}();
function renderMarkerSVG(shape) {
    var svg = function svg(body) {
        return '<svg viewBox="0 0 12 12" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">'.concat(body, "</svg>");
    };
    switch(shape){
        case "square":
        case "rect":
            return svg('<rect x="1" y="1" width="10" height="10" rx="1" fill="currentColor"/>');
        case "line":
            return svg('<rect x="0" y="5" width="12" height="2" rx="1" fill="currentColor"/>');
        case "diamond":
            return svg('<path d="M6 0.5 L11.5 6 L6 11.5 L0.5 6 Z" fill="currentColor"/>');
        case "triangle":
            return svg('<path d="M6 1 L11.2 10.5 L0.8 10.5 Z" fill="currentColor"/>');
        case "cross":
            return svg('<path d="M2 2 L10 10 M10 2 L2 10" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>');
        case "plus":
            return svg('<path d="M6 1 L6 11 M1 6 L11 6" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>');
        case "star":
            return svg('<path d="M6 0.5 L7.5 4.4 L11.5 4.7 L8.4 7.2 L9.5 11.1 L6 8.9 L2.5 11.1 L3.6 7.2 L0.5 4.7 L4.5 4.4 Z" fill="currentColor"/>');
        case "sparkle":
            return svg('<path d="M6 0.5 L7 5 L11.5 6 L7 7 L6 11.5 L5 7 L0.5 6 L5 5 Z" fill="currentColor"/>');
        case "circle":
        default:
            return svg('<circle cx="6" cy="6" r="5" fill="currentColor"/>');
    }
}
var Marker = /*#__PURE__*/ function() {
    "use strict";
    function Marker(tooltipContext) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Marker);
        this.w = tooltipContext.w;
        this.ttCtx = tooltipContext;
        this.ctx = tooltipContext.ctx;
        this.tooltipPosition = new Position(tooltipContext);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Marker, [
        {
            key: "drawDynamicPoints",
            value: function drawDynamicPoints() {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var marker = new Markers(this.w, this.ctx);
                var elsSeries = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.dom.baseEl.querySelectorAll(".apexcharts-series"));
                if (w.config.chart.stacked) {
                    elsSeries.sort(function(a, b) {
                        return parseFloat(a.getAttribute("data:realIndex")) - parseFloat(b.getAttribute("data:realIndex"));
                    });
                }
                for(var i = 0; i < elsSeries.length; i++){
                    var pointsMain = elsSeries[i].querySelector(".apexcharts-series-markers-wrap");
                    if (pointsMain !== null) {
                        var PointClasses = "apexcharts-marker w".concat((Math.random() + 1).toString(36).substring(4));
                        if ((w.config.chart.type === "line" || w.config.chart.type === "area") && !w.globals.comboCharts && !w.config.tooltip.intersect) {
                            PointClasses += " no-pointer-events";
                        }
                        var elPointOptions = marker.getMarkerConfig({
                            cssClass: PointClasses,
                            seriesIndex: Number(pointsMain.getAttribute("data:realIndex"))
                        });
                        var point = graphics.drawMarker(0, 0, elPointOptions);
                        point.node.setAttribute("default-marker-size", 0);
                        var elPointsG = BrowserAPIs.createElementNS(SVGNS$2, "g");
                        elPointsG.classList.add("apexcharts-series-markers");
                        elPointsG.appendChild(point.node);
                        pointsMain.appendChild(elPointsG);
                    }
                }
            }
        },
        {
            /**
   * @param {any} rel
   * @param {any} point
   * @param {number | null} [x]
   * @param {number | null} [y]
   */ key: "enlargeCurrentPoint",
            value: function enlargeCurrentPoint(rel, point) {
                var x = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null, y = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : null;
                var w = this.w;
                if (w.config.chart.type !== "bubble") {
                    this.newPointSize(rel, point);
                }
                var cx = point.getAttribute("cx");
                var cy = point.getAttribute("cy");
                if (x !== null && y !== null) {
                    cx = x;
                    cy = y;
                }
                this.tooltipPosition.moveXCrosshairs(cx);
                if (!this.fixedTooltip) {
                    if (w.config.chart.type === "radar") {
                        var elGrid = this.ttCtx.getElGrid();
                        if (!elGrid) return;
                        var seriesBound = elGrid.getBoundingClientRect();
                        cx = this.ttCtx.e.clientX - seriesBound.left;
                    }
                    this.tooltipPosition.moveTooltip(cx, cy, w.config.markers.hover.size);
                }
            }
        },
        {
            /**
   * @param {number} j
   */ key: "enlargePoints",
            value: function enlargePoints(j) {
                var _a, _b;
                var w = this.w;
                var me = this;
                var ttCtx = this.ttCtx;
                var col = j;
                var points = w.dom.baseEl.querySelectorAll(".apexcharts-series:not(.apexcharts-series-collapsed) .apexcharts-marker");
                var newSize = w.config.markers.hover.size;
                for(var p = 0; p < points.length; p++){
                    var rel = points[p].getAttribute("rel");
                    var index = points[p].getAttribute("index");
                    if (newSize === void 0) {
                        newSize = w.globals.markers.size[/** @type {any} */ index] + w.config.markers.hover.sizeOffset;
                    }
                    if (col === parseInt(rel != null ? rel : "0", 10)) {
                        me.newPointSize(col, points[p]);
                        var cx = (_a = points[p].getAttribute("cx")) != null ? _a : "0";
                        var cy = (_b = points[p].getAttribute("cy")) != null ? _b : "0";
                        me.tooltipPosition.moveXCrosshairs(parseFloat(cx));
                        if (!ttCtx.fixedTooltip) {
                            me.tooltipPosition.moveTooltip(parseFloat(cx), parseFloat(cy), newSize);
                        }
                    } else {
                        me.oldPointSize(points[p]);
                    }
                }
            }
        },
        {
            /**
   * @param {any} rel
   * @param {any} point
   */ key: "newPointSize",
            value: function newPointSize(rel, point) {
                var w = this.w;
                var newSize = w.config.markers.hover.size;
                var elPoint = rel === 0 ? point.parentNode.firstChild : point.parentNode.lastChild;
                if (elPoint.getAttribute("default-marker-size") !== "0") {
                    var index = parseInt(elPoint.getAttribute("index"), 10);
                    if (newSize === void 0) {
                        newSize = w.globals.markers.size[index] + w.config.markers.hover.sizeOffset;
                    }
                    if (newSize < 0) {
                        newSize = 0;
                    }
                    var path = this.ttCtx.tooltipUtil.getPathFromPoint(point, newSize);
                    point.setAttribute("d", path);
                }
            }
        },
        {
            /**
   * @param {any} point
   */ key: "oldPointSize",
            value: function oldPointSize(point) {
                var size = parseFloat(point.getAttribute("default-marker-size"));
                var path = this.ttCtx.tooltipUtil.getPathFromPoint(point, size);
                point.setAttribute("d", path);
            }
        },
        {
            key: "resetPointsSize",
            value: function resetPointsSize() {
                var _a;
                var w = this.w;
                var points = w.dom.baseEl.querySelectorAll(".apexcharts-series:not(.apexcharts-series-collapsed) .apexcharts-marker");
                for(var p = 0; p < points.length; p++){
                    var size = parseFloat((_a = points[p].getAttribute("default-marker-size")) != null ? _a : "0");
                    if (Utils$1.isNumber(size) && size > 0) {
                        var path = this.ttCtx.tooltipUtil.getPathFromPoint(points[p], size);
                        points[p].setAttribute("d", path);
                    } else {
                        points[p].setAttribute("d", "M0,0");
                    }
                }
            }
        }
    ]);
    return Marker;
}();
var Intersect = /*#__PURE__*/ function() {
    "use strict";
    function Intersect(tooltipContext) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Intersect);
        this.w = tooltipContext.w;
        var w = this.w;
        this.ttCtx = tooltipContext;
        this.isVerticalGroupedRangeBar = !w.globals.isBarHorizontal && w.config.chart.type === "rangeBar" && w.config.plotOptions.bar.rangeBarGroupRows;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Intersect, [
        {
            // a helper function to get an element's attribute value
            /**
   * @param {Event} e
   * @param {string} attr
   */ key: "getAttr",
            value: function getAttr(e, attr) {
                var _a;
                return parseFloat(/** @type {Element} */ (_a = e.target.getAttribute(attr)) != null ? _a : "");
            }
        },
        {
            // handle tooltip for heatmaps and treemaps
            /** @param {{e: any, opt: any, x: any, y: any, type: any}} opts */ key: "handleHeatTreeTooltip",
            value: function handleHeatTreeTooltip(param) {
                var e = param.e, opt = param.opt, x = param.x, y = param.y, type = param.type;
                var _a, _b;
                var ttCtx = this.ttCtx;
                var w = this.w;
                var renderer = w.globals.activeRenderer;
                var canvasCells = type === "heatmap" && renderer && renderer.kind === "canvas" && typeof renderer.hitTest === "function";
                var i, j, cx, cy, width, height;
                if (canvasCells) {
                    var seriesBound = opt.elGrid.getBoundingClientRect();
                    var clientX = e.type === "touchmove" ? e.touches[0].clientX : e.clientX;
                    var clientY = e.type === "touchmove" ? e.touches[0].clientY : e.clientY;
                    var hit = renderer.hitTest(clientX - seriesBound.left, clientY - seriesBound.top);
                    if (!hit) {
                        return {
                            x: x,
                            y: y,
                            noHit: true
                        };
                    }
                    i = hit.seriesIndex;
                    j = hit.dataPointIndex;
                    cx = hit.x;
                    cy = hit.y;
                    width = hit.width;
                    height = hit.height;
                } else if (e.target.classList.contains("apexcharts-".concat(type, "-rect"))) {
                    i = this.getAttr(e, "i");
                    j = this.getAttr(e, "j");
                    cx = this.getAttr(e, "cx");
                    cy = this.getAttr(e, "cy");
                    width = this.getAttr(e, "width");
                    height = this.getAttr(e, "height");
                } else {
                    return {
                        x: x,
                        y: y
                    };
                }
                ttCtx.tooltipLabels.drawSeriesTexts({
                    ttItems: opt.ttItems,
                    i: i,
                    j: j,
                    shared: false,
                    e: e
                });
                w.interact.capturedSeriesIndex = i;
                w.interact.capturedDataPointIndex = j;
                ttCtx.tooltipPosition.moveXCrosshairs(cx + width / 2);
                var tooltipEl = ttCtx.getElTooltip();
                if (type === "heatmap" && w.config.tooltip.arrow && !w.config.tooltip.followCursor && tooltipEl) {
                    var elGridRect = opt.elGrid.getBoundingClientRect();
                    var elWrapRect = w.dom.elWrap.getBoundingClientRect();
                    var gridOffsetXInElWrap = elGridRect.left - elWrapRect.left;
                    var clLeft, clTop, clRight, clBottom;
                    if (canvasCells) {
                        clLeft = cx;
                        clTop = cy;
                        clRight = cx + width;
                        clBottom = cy + height;
                    } else {
                        var r = e.target.getBoundingClientRect();
                        clLeft = r.left - elGridRect.left;
                        clTop = r.top - elGridRect.top;
                        clRight = r.right - elGridRect.left;
                        clBottom = r.bottom - elGridRect.top;
                    }
                    var ttW = ttCtx.tooltipRect.ttWidth || 0;
                    var ttH = ttCtx.tooltipRect.ttHeight || 0;
                    var cellCenterXInElWrap = (clLeft + clRight) / 2 + gridOffsetXInElWrap;
                    var cellTopInElWrap = clTop + w.layout.translateY;
                    var cellBottomInElWrap = clBottom + w.layout.translateY;
                    var gridTop = w.layout.translateY;
                    var gridBottom = w.layout.translateY + w.layout.gridHeight;
                    var gridLeft = gridOffsetXInElWrap;
                    var gridRight = gridOffsetXInElWrap + w.layout.gridWidth;
                    var placement = "top";
                    var finalY = cellTopInElWrap - ttH - ARROW_TIP_OVERHANG;
                    if (finalY < gridTop) {
                        var belowTop = cellBottomInElWrap + ARROW_TIP_OVERHANG;
                        if (belowTop + ttH <= gridBottom) {
                            placement = "bottom";
                            finalY = belowTop;
                        } else {
                            finalY = gridTop;
                        }
                    }
                    var finalX = cellCenterXInElWrap - ttW / 2;
                    if (finalX < gridLeft) finalX = gridLeft;
                    if (finalX + ttW > gridRight) finalX = gridRight - ttW;
                    var arrowX = Math.max(10, Math.min(ttW - 10, cellCenterXInElWrap - finalX));
                    ttCtx.tooltipPosition.applyTooltipPosition(tooltipEl, {
                        x: finalX,
                        y: finalY,
                        placement: placement,
                        arrowY: null,
                        arrowX: arrowX
                    });
                    return {
                        x: finalX,
                        y: finalY,
                        positioned: true
                    };
                }
                x = cx + ttCtx.tooltipRect.ttWidth / 2 + width;
                y = cy + ttCtx.tooltipRect.ttHeight / 2 - height / 2;
                if (x > w.layout.gridWidth / 2) {
                    x = cx - ttCtx.tooltipRect.ttWidth / 2 + width;
                }
                if (ttCtx.w.config.tooltip.followCursor) {
                    var seriesBound1 = w.dom.elWrap.getBoundingClientRect();
                    x = ((_a = w.interact.clientX) != null ? _a : 0) - seriesBound1.left - (x > w.layout.gridWidth / 2 ? ttCtx.tooltipRect.ttWidth : 0);
                    y = ((_b = w.interact.clientY) != null ? _b : 0) - seriesBound1.top - (y > w.layout.gridHeight / 2 ? ttCtx.tooltipRect.ttHeight : 0);
                }
                return {
                    x: x,
                    y: y
                };
            }
        },
        {
            /**
   * handle tooltips for line/area/scatter charts where tooltip.intersect is true
   * when user hovers over the marker directly, this function is executed
   */ /** @param {{e: any, opt: any, x: any, y: any}} opts */ key: "handleMarkerTooltip",
            value: function handleMarkerTooltip(param) {
                var e = param.e, opt = param.opt, x = param.x, y = param.y;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var i;
                var j;
                if (e.target.classList.contains("apexcharts-marker")) {
                    var cx = parseInt(opt.paths.getAttribute("cx"), 10);
                    var cy = parseInt(opt.paths.getAttribute("cy"), 10);
                    var val = parseFloat(opt.paths.getAttribute("val"));
                    j = parseInt(opt.paths.getAttribute("rel"), 10);
                    i = parseInt(opt.paths.parentNode.parentNode.parentNode.getAttribute("rel"), 10) - 1;
                    if (ttCtx.intersect) {
                        var el = Utils$1.findAncestor(opt.paths, "apexcharts-series");
                        if (el) {
                            i = parseInt(el.getAttribute("data:realIndex"), 10);
                        }
                    }
                    ttCtx.tooltipLabels.drawSeriesTexts({
                        ttItems: opt.ttItems,
                        i: i,
                        j: j,
                        shared: ttCtx.showOnIntersect ? false : w.config.tooltip.shared,
                        e: e
                    });
                    if (e.type === "mouseup") {
                        ttCtx.markerClick(e, i, j);
                    }
                    w.interact.capturedSeriesIndex = i;
                    w.interact.capturedDataPointIndex = j;
                    var arrowEnabled = !!w.config.tooltip.arrow;
                    x = cx;
                    if (arrowEnabled) {
                        y = cy;
                    } else {
                        y = cy + w.layout.translateY - ttCtx.tooltipRect.ttHeight * 1.4;
                        if (val < 0) {
                            y = cy;
                        }
                    }
                    if (ttCtx.w.config.tooltip.followCursor) {
                        var elGrid = ttCtx.getElGrid();
                        if (!elGrid) return {
                            x: x,
                            y: y
                        };
                        var seriesBound = elGrid.getBoundingClientRect();
                        y = ttCtx.e.clientY + w.layout.translateY - seriesBound.top;
                    }
                    ttCtx.marker.enlargeCurrentPoint(j, opt.paths, x, y);
                }
                return {
                    x: x,
                    y: y
                };
            }
        },
        {
            /**
   * handle tooltips for bar/column charts
   */ /** @param {{e: any, opt: any}} opts */ key: "handleBarTooltip",
            value: function handleBarTooltip(param) {
                var e = param.e, opt = param.opt;
                var _a, _b, _c;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var tooltipEl = ttCtx.getElTooltip();
                var bx = 0;
                var x = 0;
                var y = 0;
                var i = 0;
                var strokeWidth;
                var barXY = this.getBarTooltipXY({
                    e: e,
                    opt: opt
                });
                if (barXY.j === null && barXY.barHeight === 0 && barXY.barWidth === 0) {
                    return;
                }
                i = barXY.i;
                var j = barXY.j;
                w.interact.capturedSeriesIndex = i;
                w.interact.capturedDataPointIndex = j !== null ? j : w.interact.capturedDataPointIndex;
                if (w.globals.isBarHorizontal && ttCtx.tooltipUtil.hasBars() || !w.config.tooltip.shared) {
                    x = barXY.x;
                    y = barXY.y;
                    strokeWidth = Array.isArray(w.config.stroke.width) ? w.config.stroke.width[i] : w.config.stroke.width;
                    bx = x;
                } else {
                    if (!w.globals.comboCharts && !w.config.tooltip.shared) {
                        bx = bx / 2;
                    }
                }
                if (isNaN(y)) {
                    y = w.globals.svgHeight - ttCtx.tooltipRect.ttHeight;
                }
                if (x + ttCtx.tooltipRect.ttWidth > w.layout.gridWidth) {
                    x = x - ttCtx.tooltipRect.ttWidth;
                } else if (x < 0) {
                    x = 0;
                }
                if (ttCtx.w.config.tooltip.followCursor) {
                    var elGrid = ttCtx.getElGrid();
                    if (!elGrid) return;
                }
                if (ttCtx.tooltip === null) {
                    ttCtx.tooltip = w.dom.baseEl.querySelector(".apexcharts-tooltip:not(.apexcharts-annotation-tooltip)");
                }
                if (!w.config.tooltip.shared) {
                    if (w.globals.comboBarCount > 0) {
                        ttCtx.tooltipPosition.moveXCrosshairs(bx + strokeWidth / 2);
                    } else {
                        ttCtx.tooltipPosition.moveXCrosshairs(bx);
                    }
                }
                if (!ttCtx.fixedTooltip && (!w.config.tooltip.shared || w.globals.isBarHorizontal && ttCtx.tooltipUtil.hasBars())) {
                    y = y + w.layout.translateY - ttCtx.tooltipRect.ttHeight / 2;
                    if (tooltipEl) {
                        var ttW = ttCtx.tooltipRect.ttWidth || 0;
                        var ttH = ttCtx.tooltipRect.ttHeight || 0;
                        var arrowEnabled = !!w.config.tooltip.arrow;
                        var barAnchorXInGrid = barXY.barAnchorXInGrid, barAnchorYInGrid = barXY.barAnchorYInGrid, barRectInGrid = barXY.barRectInGrid;
                        var elGridRect = (_a = ttCtx.getElGrid()) == null ? void 0 : _a.getBoundingClientRect();
                        var elWrapRect = w.dom.elWrap.getBoundingClientRect();
                        var gridOffsetXInElWrap = elGridRect ? elGridRect.left - elWrapRect.left : w.layout.translateX;
                        var placement;
                        var arrowY = null;
                        var arrowX = null;
                        var finalX = x + gridOffsetXInElWrap;
                        var finalY = y;
                        if (arrowEnabled && w.globals.isBarHorizontal && barRectInGrid != null) {
                            var gridTop = w.layout.translateY;
                            var gridBottom = w.layout.translateY + w.layout.gridHeight;
                            var gridLeft = gridOffsetXInElWrap;
                            var gridRight = gridOffsetXInElWrap + w.layout.gridWidth;
                            var barCenterXInElWrap = (barRectInGrid.left + barRectInGrid.right) / 2 + gridOffsetXInElWrap;
                            var barTopInElWrap = barRectInGrid.top + w.layout.translateY;
                            var barBottomInElWrap = barRectInGrid.bottom + w.layout.translateY;
                            var proposedTop = barTopInElWrap - ttH - ARROW_TIP_OVERHANG;
                            placement = "top";
                            if (proposedTop < gridTop) {
                                var belowTop = barBottomInElWrap + ARROW_TIP_OVERHANG;
                                if (belowTop + ttH <= gridBottom) {
                                    placement = "bottom";
                                    proposedTop = belowTop;
                                }
                            }
                            finalY = proposedTop;
                            finalX = barCenterXInElWrap - ttW / 2;
                            if (finalX < gridLeft) finalX = gridLeft;
                            if (finalX + ttW > gridRight) finalX = gridRight - ttW;
                            arrowX = Math.max(10, Math.min(ttW - 10, barCenterXInElWrap - finalX));
                        } else if (arrowEnabled && barAnchorXInGrid != null && barAnchorYInGrid != null) {
                            var barCenterXInElWrap1 = barAnchorXInGrid + gridOffsetXInElWrap;
                            var gridCenterXInElWrap = gridOffsetXInElWrap + w.layout.gridWidth / 2;
                            var barLeftInElWrap = ((_b = barRectInGrid == null ? void 0 : barRectInGrid.left) != null ? _b : barAnchorXInGrid) + gridOffsetXInElWrap;
                            var barRightInElWrap = ((_c = barRectInGrid == null ? void 0 : barRectInGrid.right) != null ? _c : barAnchorXInGrid) + gridOffsetXInElWrap;
                            if (barCenterXInElWrap1 < gridCenterXInElWrap) {
                                placement = "right";
                                finalX = barRightInElWrap + ARROW_TIP_OVERHANG;
                            } else {
                                placement = "left";
                                finalX = barLeftInElWrap - ttW - ARROW_TIP_OVERHANG;
                            }
                            if (barRectInGrid) {
                                var barCenterYInElWrap = (barRectInGrid.top + barRectInGrid.bottom) / 2 + w.layout.translateY;
                                finalY = barCenterYInElWrap - ttH / 2;
                                var gridTop1 = w.layout.translateY;
                                var gridBottom1 = w.layout.translateY + w.layout.gridHeight;
                                if (finalY < gridTop1) finalY = gridTop1;
                                if (finalY + ttH > gridBottom1) finalY = gridBottom1 - ttH;
                            }
                            if (ttH > 0 && barRectInGrid) {
                                var barCenterYInElWrap1 = (barRectInGrid.top + barRectInGrid.bottom) / 2 + w.layout.translateY;
                                arrowY = Math.max(10, Math.min(ttH - 10, barCenterYInElWrap1 - finalY));
                            }
                        }
                        ttCtx.tooltipPosition.applyTooltipPosition(tooltipEl, {
                            x: finalX,
                            y: finalY,
                            placement: placement,
                            arrowY: arrowY,
                            arrowX: arrowX
                        });
                    }
                }
            }
        },
        {
            /** @param {{e: any, opt: any}} opts */ key: "getBarTooltipXY",
            value: function getBarTooltipXY(param) {
                var _this = this;
                var e = param.e, opt = param.opt;
                var w = this.w;
                var j = null;
                var ttCtx = this.ttCtx;
                var i = 0;
                var x = 0;
                var y = 0;
                var barWidth = 0;
                var barHeight = 0;
                var barCx = null;
                var barCy = null;
                var barAnchorXInGrid = null;
                var barAnchorYInGrid = null;
                var barRectInGrid = null;
                var cl = e.target.classList;
                if (cl.contains("apexcharts-bar-area") || cl.contains("apexcharts-candlestick-area") || cl.contains("apexcharts-boxPlot-area") || cl.contains("apexcharts-rangebar-area")) {
                    var bar = e.target;
                    var barRect = bar.getBoundingClientRect();
                    var seriesBound = opt.elGrid.getBoundingClientRect();
                    var bh = barRect.height;
                    barHeight = barRect.height;
                    var bw = barRect.width;
                    var cx = parseInt(bar.getAttribute("cx"), 10);
                    var cy = parseInt(bar.getAttribute("cy"), 10);
                    barCx = cx;
                    barCy = cy;
                    barWidth = parseFloat(bar.getAttribute("barWidth"));
                    var rectLeftInGrid = barRect.left - seriesBound.left;
                    var rectTopInGrid = barRect.top - seriesBound.top;
                    var rectCenterXInGrid = rectLeftInGrid + bw / 2;
                    var rectCenterYInGrid = rectTopInGrid + bh / 2;
                    barAnchorXInGrid = rectCenterXInGrid;
                    barAnchorYInGrid = w.globals.isBarHorizontal ? rectCenterYInGrid : rectTopInGrid;
                    barRectInGrid = {
                        left: rectLeftInGrid,
                        top: rectTopInGrid,
                        right: rectLeftInGrid + bw,
                        bottom: rectTopInGrid + bh
                    };
                    var clientX = e.type === "touchmove" ? e.touches[0].clientX : e.clientX;
                    j = parseInt(bar.getAttribute("j"), 10);
                    i = parseInt(bar.parentNode.getAttribute("rel"), 10) - 1;
                    var y1 = bar.getAttribute("data-range-y1");
                    var y2 = bar.getAttribute("data-range-y2");
                    if (w.globals.comboCharts) {
                        i = parseInt(bar.parentNode.getAttribute("data:realIndex"), 10);
                    }
                    var handleXForColumns = function handleXForColumns(x2) {
                        if (w.axisFlags.isXNumeric) {
                            x2 = cx - bw / 2;
                        } else {
                            if (_this.isVerticalGroupedRangeBar) {
                                x2 = cx + bw / 2;
                            } else {
                                x2 = cx - ttCtx.dataPointsDividedWidth + bw / 2;
                            }
                        }
                        return x2;
                    };
                    var handleYForBars = function handleYForBars() {
                        return cy - ttCtx.dataPointsDividedHeight + bh / 2 - ttCtx.tooltipRect.ttHeight / 2;
                    };
                    ttCtx.tooltipLabels.drawSeriesTexts({
                        ttItems: opt.ttItems,
                        i: i,
                        j: j,
                        y1: y1 ? parseInt(y1, 10) : null,
                        y2: y2 ? parseInt(y2, 10) : null,
                        shared: ttCtx.showOnIntersect ? false : w.config.tooltip.shared,
                        e: e
                    });
                    if (w.config.tooltip.followCursor) {
                        if (w.globals.isBarHorizontal) {
                            x = clientX - seriesBound.left + 15;
                            y = handleYForBars();
                        } else {
                            x = handleXForColumns(x);
                            y = e.clientY - seriesBound.top - ttCtx.tooltipRect.ttHeight / 2 - 15;
                        }
                    } else {
                        if (w.globals.isBarHorizontal) {
                            x = cx;
                            if (ttCtx.xyRatios && x < ttCtx.xyRatios.baseLineInvertedY) {
                                x = cx - ttCtx.tooltipRect.ttWidth;
                            }
                            y = handleYForBars();
                        } else {
                            x = handleXForColumns(x);
                            y = cy;
                        }
                    }
                }
                return {
                    x: x,
                    y: y,
                    barHeight: barHeight,
                    barWidth: barWidth,
                    i: i,
                    j: j,
                    // SVG attribute values — left for any caller that still wants them.
                    barCx: barCx,
                    barCy: barCy,
                    // Arrow anchor in grid-local coords (rect-derived; column→top,
                    // horizontal→center). Used by handleBarTooltip to place the arrow
                    // exactly on the bar's data point.
                    barAnchorXInGrid: barAnchorXInGrid,
                    barAnchorYInGrid: barAnchorYInGrid,
                    // Full rendered bar rect (grid-local). Used for top/bottom
                    // placement and flip-on-overflow detection.
                    barRectInGrid: barRectInGrid
                };
            }
        }
    ]);
    return Intersect;
}();
var AxesTooltip = /*#__PURE__*/ function() {
    "use strict";
    function AxesTooltip(tooltipContext) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, AxesTooltip);
        this.w = tooltipContext.w;
        this.ttCtx = tooltipContext;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(AxesTooltip, [
        {
            /**
   * This method adds the secondary tooltip which appears below x axis
   * @memberof Tooltip
   **/ key: "drawXaxisTooltip",
            value: function drawXaxisTooltip() {
                var w = this.w;
                var ttCtx = this.ttCtx;
                var isBottom = w.config.xaxis.position === "bottom";
                ttCtx.xaxisOffY = isBottom ? w.layout.gridHeight + 1 : -w.layout.xAxisHeight - w.config.xaxis.axisTicks.height + 3;
                var tooltipCssClass = isBottom ? "apexcharts-xaxistooltip apexcharts-xaxistooltip-bottom" : "apexcharts-xaxistooltip apexcharts-xaxistooltip-top";
                var renderTo = w.dom.elWrap;
                if (ttCtx.isXAxisTooltipEnabled) {
                    var xaxisTooltip = w.dom.baseEl.querySelector(".apexcharts-xaxistooltip");
                    if (xaxisTooltip === null) {
                        ttCtx.xaxisTooltip = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                        ttCtx.xaxisTooltip.setAttribute("class", tooltipCssClass + " apexcharts-theme-" + w.config.tooltip.theme);
                        renderTo.appendChild(ttCtx.xaxisTooltip);
                        ttCtx.xaxisTooltipText = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                        ttCtx.xaxisTooltipText.classList.add("apexcharts-xaxistooltip-text");
                        ttCtx.xaxisTooltipText.style.fontFamily = w.config.xaxis.tooltip.style.fontFamily || w.config.chart.fontFamily;
                        ttCtx.xaxisTooltipText.style.fontSize = w.config.xaxis.tooltip.style.fontSize;
                        ttCtx.xaxisTooltip.appendChild(ttCtx.xaxisTooltipText);
                    }
                }
            }
        },
        {
            /**
   * This method adds the secondary tooltip which appears below x axis
   * @memberof Tooltip
   **/ key: "drawYaxisTooltip",
            value: function drawYaxisTooltip() {
                var w = this.w;
                var ttCtx = this.ttCtx;
                for(var i = 0; i < w.config.yaxis.length; i++){
                    var isRight = w.config.yaxis[i].opposite || w.config.yaxis[i].crosshairs.opposite;
                    ttCtx.yaxisOffX = isRight ? w.layout.gridWidth + 1 : 1;
                    var tooltipCssClass = isRight ? "apexcharts-yaxistooltip apexcharts-yaxistooltip-".concat(i, " apexcharts-yaxistooltip-right") : "apexcharts-yaxistooltip apexcharts-yaxistooltip-".concat(i, " apexcharts-yaxistooltip-left");
                    var renderTo = w.dom.elWrap;
                    var yaxisTooltip = w.dom.baseEl.querySelector(".apexcharts-yaxistooltip.apexcharts-yaxistooltip-".concat(i));
                    if (yaxisTooltip === null) {
                        ttCtx.yaxisTooltip = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                        ttCtx.yaxisTooltip.setAttribute("class", tooltipCssClass + " apexcharts-theme-" + w.config.tooltip.theme);
                        renderTo.appendChild(ttCtx.yaxisTooltip);
                        if (i === 0) ttCtx.yaxisTooltipText = [];
                        ttCtx.yaxisTooltipText[i] = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                        ttCtx.yaxisTooltipText[i].classList.add("apexcharts-yaxistooltip-text");
                        ttCtx.yaxisTooltip.appendChild(/** @type {any} */ ttCtx.yaxisTooltipText[i]);
                    }
                }
            }
        },
        {
            /**
   * @memberof Tooltip
   **/ key: "setXCrosshairWidth",
            value: function setXCrosshairWidth() {
                var _a, _b;
                var w = this.w;
                var ttCtx = this.ttCtx;
                var xcrosshairs = ttCtx.getElXCrosshairs();
                ttCtx.xcrosshairsWidth = parseInt(w.config.xaxis.crosshairs.width, 10);
                if (!w.globals.comboCharts) {
                    if (w.config.xaxis.crosshairs.width === "tickWidth") {
                        var count = w.labelData.labels.length;
                        ttCtx.xcrosshairsWidth = w.layout.gridWidth / count;
                    } else if (w.config.xaxis.crosshairs.width === "barWidth") {
                        var bar = w.dom.baseEl.querySelector(".apexcharts-bar-area");
                        if (bar !== null) {
                            var barWidth = parseFloat((_a = bar.getAttribute("barWidth")) != null ? _a : "0");
                            ttCtx.xcrosshairsWidth = barWidth;
                        } else {
                            ttCtx.xcrosshairsWidth = 1;
                        }
                    }
                } else {
                    var bar1 = w.dom.baseEl.querySelector(".apexcharts-bar-area");
                    if (bar1 !== null && w.config.xaxis.crosshairs.width === "barWidth") {
                        var barWidth1 = parseFloat((_b = bar1.getAttribute("barWidth")) != null ? _b : "0");
                        ttCtx.xcrosshairsWidth = barWidth1;
                    } else {
                        if (w.config.xaxis.crosshairs.width === "tickWidth") {
                            var count1 = w.labelData.labels.length;
                            ttCtx.xcrosshairsWidth = w.layout.gridWidth / count1;
                        }
                    }
                }
                if (w.globals.isBarHorizontal) {
                    ttCtx.xcrosshairsWidth = 0;
                }
                if (xcrosshairs !== null && ttCtx.xcrosshairsWidth > 0) {
                    xcrosshairs.setAttribute("width", String(ttCtx.xcrosshairsWidth));
                }
            }
        },
        {
            key: "handleYCrosshair",
            value: function handleYCrosshair() {
                var w = this.w;
                var ttCtx = this.ttCtx;
                ttCtx.ycrosshairs = w.dom.baseEl.querySelector(".apexcharts-ycrosshairs");
                ttCtx.ycrosshairsHidden = w.dom.baseEl.querySelector(".apexcharts-ycrosshairs-hidden");
            }
        },
        {
            /**
   * @param {number} index
   * @param {number} clientY
   * @param {import('../../types/internal').XYRatios} xyRatios
   */ key: "drawYaxisTooltipText",
            value: function drawYaxisTooltipText(index, clientY, xyRatios) {
                var ttCtx = this.ttCtx;
                var w = this.w;
                var gl = w.globals;
                var yAxisSeriesArr = gl.seriesYAxisMap[index];
                if (ttCtx.yaxisTooltips[index] && yAxisSeriesArr.length > 0) {
                    var lbFormatter = w.formatters.yLabelFormatters[index];
                    var elGrid = ttCtx.getElGrid();
                    if (!elGrid) return;
                    var seriesBound = elGrid.getBoundingClientRect();
                    var seriesIndex = yAxisSeriesArr[0];
                    var translationsIndex = 0;
                    if (xyRatios.yRatio.length > 1) {
                        translationsIndex = seriesIndex;
                    }
                    var hoverY = (clientY - seriesBound.top) * xyRatios.yRatio[translationsIndex];
                    var height = gl.maxYArr[seriesIndex] - gl.minYArr[seriesIndex];
                    var val = gl.minYArr[seriesIndex] + (height - hoverY);
                    if (w.config.yaxis[index].reversed) {
                        val = gl.maxYArr[seriesIndex] - (height - hoverY);
                    }
                    ttCtx.tooltipPosition.moveYCrosshairs(clientY - seriesBound.top);
                    ttCtx.yaxisTooltipText[index].innerHTML = lbFormatter(val);
                    ttCtx.tooltipPosition.moveYAxisTooltip(index);
                }
            }
        }
    ]);
    return AxesTooltip;
}();
var Tooltip = /*#__PURE__*/ function() {
    "use strict";
    function Tooltip(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Tooltip);
        this.w = w;
        this.ctx = ctx;
        this.tConfig = w.config.tooltip;
        this.tooltipUtil = new Utils2(this);
        this.tooltipLabels = new Labels(this);
        this.tooltipPosition = new Position(this);
        this.marker = new Marker(this);
        this.intersect = new Intersect(this);
        this.axesTooltip = new AxesTooltip(this);
        this.showOnIntersect = this.tConfig.intersect;
        this.showTooltipTitle = this.tConfig.x.show;
        this.fixedTooltip = this.tConfig.fixed.enabled;
        this.xaxisTooltip = null;
        this.xaxisTooltipText = null;
        this.yaxisTooltip = null;
        this.yaxisTooltipText = null;
        this.yaxisTTEls = null;
        this.xaxisOffY = 0;
        this.yaxisOffX = 0;
        this.xcrosshairsWidth = 0;
        this.ycrosshairs = null;
        this.ycrosshairsHidden = null;
        this.tooltip = null;
        this.e = null;
        this.isBarShared = !w.globals.isBarHorizontal && this.tConfig.shared;
        this.lastHoverTime = Date.now();
        this.dimensionUpdateScheduled = false;
        this.xyRatios = null;
        this.isXAxisTooltipEnabled = false;
        this.yaxisTooltips = [];
        this.allTooltipSeriesGroups = [];
        this.xAxisTicksPositions = null;
        this.dataPointsDividedHeight = 0;
        this.dataPointsDividedWidth = 0;
        this.tooltipTitle = null;
        this.legendLabels = null;
        this.ttItems = null;
        this.seriesBound = null;
        this.seriesHoverTimeout = void 0;
        this.clientX = 0;
        this.clientY = 0;
        this.barSeriesHeight = 0;
        this.tooltipRect = {
            x: 0,
            y: 0,
            ttWidth: 0,
            ttHeight: 0
        };
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Tooltip, [
        {
            key: "setupDimensionCache",
            value: function setupDimensionCache() {
                var _this = this;
                var w = this.w;
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return;
                this.updateDimensionCache();
                if (typeof ResizeObserver !== "undefined" && !w.globals.resizeObserver) {
                    w.globals.resizeObserver = new ResizeObserver(function() {
                        if (!_this.dimensionUpdateScheduled) {
                            _this.dimensionUpdateScheduled = true;
                            requestAnimationFrame(function() {
                                _this.updateDimensionCache();
                                _this.dimensionUpdateScheduled = false;
                            });
                        }
                    });
                    w.globals.resizeObserver.observe(tooltipEl);
                }
            }
        },
        {
            key: "updateDimensionCache",
            value: function updateDimensionCache() {
                var w = this.w;
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return;
                var rect = tooltipEl.getBoundingClientRect();
                w.globals.dimensionCache.tooltip = /** @type {any} */ {
                    width: rect.width,
                    height: rect.height,
                    lastUpdate: Date.now()
                };
            }
        },
        {
            key: "getCachedDimensions",
            value: function getCachedDimensions() {
                var w = this.w;
                if (w.globals.dimensionCache.tooltip) {
                    var cache2 = /** @type {Record<string,any>} */ w.globals.dimensionCache.tooltip;
                    var age = Date.now() - cache2.lastUpdate;
                    if (age < 1e3) {
                        return {
                            ttWidth: cache2.width,
                            ttHeight: cache2.height
                        };
                    }
                }
                this.updateDimensionCache();
                var cache = /** @type {Record<string,any>} */ w.globals.dimensionCache.tooltip;
                return cache ? {
                    ttWidth: cache.width,
                    ttHeight: cache.height
                } : {
                    ttWidth: 0,
                    ttHeight: 0
                };
            }
        },
        {
            /**
   * @param {{ w: import('../../types/internal').ChartStateW }} [ctx]
   * @returns {HTMLElement | null}
   */ key: "getElTooltip",
            value: function getElTooltip(ctx) {
                if (!ctx) ctx = this;
                if (!ctx.w.dom.baseEl) return null;
                return /** @type {HTMLElement | null} */ ctx.w.dom.baseEl.querySelector(".apexcharts-tooltip:not(.apexcharts-annotation-tooltip)");
            }
        },
        {
            key: "getElXCrosshairs",
            value: function getElXCrosshairs() {
                return this.w.dom.baseEl.querySelector(".apexcharts-xcrosshairs");
            }
        },
        {
            key: "getElGrid",
            value: function getElGrid() {
                return this.w.dom.baseEl.querySelector(".apexcharts-grid");
            }
        },
        {
            /**
   * @param {import('../../types/internal').XYRatios} xyRatios
   */ key: "drawTooltip",
            value: function drawTooltip(xyRatios) {
                var w = this.w;
                this.xyRatios = xyRatios;
                this.isXAxisTooltipEnabled = w.config.xaxis.tooltip.enabled && w.globals.axisCharts;
                this.yaxisTooltips = w.config.yaxis.map(function(y) {
                    return y.show && y.tooltip.enabled && w.globals.axisCharts ? true : false;
                });
                this.allTooltipSeriesGroups = [];
                if (!w.globals.axisCharts) {
                    this.showTooltipTitle = false;
                }
                var existingTooltip = this.getElTooltip();
                if (existingTooltip == null ? void 0 : existingTooltip.parentNode) {
                    existingTooltip.parentNode.removeChild(existingTooltip);
                }
                this.tooltipTitle = null;
                var tooltipEl = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                tooltipEl.classList.add("apexcharts-tooltip");
                if (w.config.tooltip.cssClass) {
                    tooltipEl.classList.add(w.config.tooltip.cssClass);
                }
                tooltipEl.classList.add("apexcharts-theme-".concat(this.tConfig.theme || "light"));
                if (this.tConfig.fillSeriesColor) {
                    tooltipEl.classList.add("apexcharts-tooltip-fill-series");
                }
                if (this.tConfig.style && this.tConfig.style.background) {
                    tooltipEl.style.setProperty("--apx-tt-bg", this.tConfig.style.background);
                }
                var isSharedMulti = this.tConfig.shared && w.config.series.length > 1 && !w.globals.isBarHorizontal && w.config.chart.type !== "heatmap";
                var shouldDrawArrow = this.tConfig.arrow && !this.tConfig.followCursor && !this.tConfig.fixed.enabled && !isSharedMulti && !this.tConfig.fillSeriesColor && w.globals.axisCharts;
                if (shouldDrawArrow) {
                    var arrowEl = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                    arrowEl.classList.add("apexcharts-tooltip-arrow");
                    tooltipEl.appendChild(arrowEl);
                }
                if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.announcements.enabled) {
                    tooltipEl.setAttribute("role", "tooltip");
                    tooltipEl.setAttribute("aria-live", "polite");
                    tooltipEl.setAttribute("aria-atomic", "true");
                    tooltipEl.setAttribute("aria-hidden", "true");
                }
                w.dom.elWrap.appendChild(tooltipEl);
                if (w.globals.axisCharts) {
                    this.axesTooltip.drawXaxisTooltip();
                    this.axesTooltip.drawYaxisTooltip();
                    this.axesTooltip.setXCrosshairWidth();
                    this.axesTooltip.handleYCrosshair();
                    var xAxis = new XAxis(this.w, this.ctx, void 0);
                    this.xAxisTicksPositions = xAxis.getXAxisTicksPositions();
                }
                if ((w.globals.comboCharts || this.tConfig.intersect || w.config.chart.type === "rangeBar") && !this.tConfig.shared) {
                    this.showOnIntersect = true;
                }
                if (w.config.markers.size === 0 || w.globals.markers.largestSize === 0) {
                    this.marker.drawDynamicPoints();
                }
                if (w.globals.collapsedSeries.length === w.seriesData.series.length) return;
                this.dataPointsDividedHeight = w.layout.gridHeight / w.globals.dataPoints;
                this.dataPointsDividedWidth = w.layout.gridWidth / w.globals.dataPoints;
                if (this.showTooltipTitle) {
                    this.tooltipTitle = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                    this.tooltipTitle.classList.add("apexcharts-tooltip-title");
                    this.tooltipTitle.style.fontFamily = this.tConfig.style.fontFamily || w.config.chart.fontFamily;
                    this.tooltipTitle.style.fontSize = this.tConfig.style.fontSize;
                    tooltipEl.appendChild(this.tooltipTitle);
                }
                var ttItemsCnt = w.seriesData.series.length;
                if ((w.globals.xyCharts || w.globals.comboCharts) && this.tConfig.shared) {
                    if (!this.showOnIntersect) {
                        ttItemsCnt = w.seriesData.series.length;
                    } else {
                        ttItemsCnt = 1;
                    }
                }
                this.legendLabels = w.dom.baseEl.querySelectorAll(".apexcharts-legend-text");
                this.ttItems = this.createTTElements(ttItemsCnt);
                this.addSVGEvents();
                this.setupDimensionCache();
            }
        },
        {
            /**
   * @param {number} ttItemsCnt
   */ key: "createTTElements",
            value: function createTTElements(ttItemsCnt) {
                var _this, _loop = function(i) {
                    var gTxt = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                    gTxt.classList.add("apexcharts-tooltip-series-group", "apexcharts-tooltip-series-group-".concat(i));
                    gTxt.style.order = String(w.config.tooltip.inverseOrder ? ttItemsCnt - i : i + 1);
                    var point = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "span");
                    point.classList.add("apexcharts-tooltip-marker");
                    if (w.config.tooltip.fillSeriesColor) {
                        point.style.backgroundColor = w.globals.colors[i];
                    } else {
                        point.style.color = w.globals.colors[i];
                    }
                    var mShape = w.config.markers.shape;
                    var shape = mShape;
                    if (Array.isArray(mShape)) {
                        shape = mShape[i];
                    }
                    point.setAttribute("shape", shape);
                    point.innerHTML = renderMarkerSVG(shape);
                    gTxt.appendChild(point);
                    var gYZ = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                    gYZ.classList.add("apexcharts-tooltip-text");
                    gYZ.style.fontFamily = _this.tConfig.style.fontFamily || w.config.chart.fontFamily;
                    gYZ.style.fontSize = _this.tConfig.style.fontSize;
                    [
                        "y",
                        "goals",
                        "z"
                    ].forEach(function(g) {
                        var gValText = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                        gValText.classList.add("apexcharts-tooltip-".concat(g, "-group"));
                        var txtLabel = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "span");
                        txtLabel.classList.add("apexcharts-tooltip-text-".concat(g, "-label"));
                        gValText.appendChild(txtLabel);
                        var txtValue = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "span");
                        txtValue.classList.add("apexcharts-tooltip-text-".concat(g, "-value"));
                        gValText.appendChild(txtValue);
                        gYZ.appendChild(gValText);
                    });
                    gTxt.appendChild(gYZ);
                    tooltipEl.appendChild(gTxt);
                    ttItems.push(gTxt);
                };
                var w = this.w;
                var ttItems = [];
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return ttItems;
                for(var i = 0; i < ttItemsCnt; i++)_this = this, _loop(i);
                return ttItems;
            }
        },
        {
            key: "addSVGEvents",
            value: function addSVGEvents() {
                var w = this.w;
                var type = w.config.chart.type;
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return;
                var commonBar = !!(type === "bar" || type === "candlestick" || type === "boxPlot" || type === "violin" || type === "rangeBar");
                var chartWithmarkers = type === "area" || type === "line" || type === "scatter" || type === "bubble" || type === "radar";
                var hoverArea = w.dom.Paper.node;
                var elGrid = this.getElGrid();
                if (elGrid) {
                    this.seriesBound = elGrid.getBoundingClientRect();
                }
                var tooltipY = [];
                var tooltipX = [];
                var seriesHoverParams = {
                    hoverArea: hoverArea,
                    elGrid: elGrid,
                    tooltipEl: tooltipEl,
                    tooltipY: tooltipY,
                    tooltipX: tooltipX,
                    ttItems: this.ttItems
                };
                var points;
                if (w.globals.axisCharts) {
                    if (chartWithmarkers) {
                        points = w.dom.baseEl.querySelectorAll(".apexcharts-series[data\\:longestSeries='true'] .apexcharts-marker");
                    } else if (commonBar) {
                        points = w.dom.baseEl.querySelectorAll(".apexcharts-series .apexcharts-bar-area, .apexcharts-series .apexcharts-candlestick-area, .apexcharts-series .apexcharts-boxPlot-area, .apexcharts-series .apexcharts-violin-area, .apexcharts-series .apexcharts-rangebar-area");
                    } else if (type === "heatmap" || type === "treemap") {
                        points = w.dom.baseEl.querySelectorAll(".apexcharts-series .apexcharts-heatmap, .apexcharts-series .apexcharts-treemap");
                    }
                    if (points && points.length) {
                        for(var p = 0; p < points.length; p++){
                            tooltipY.push(points[p].getAttribute("cy"));
                            tooltipX.push(points[p].getAttribute("cx"));
                        }
                    }
                }
                var validSharedChartTypes = w.globals.xyCharts && !this.showOnIntersect || w.globals.comboCharts && !this.showOnIntersect || commonBar && this.tooltipUtil.hasBars() && this.tConfig.shared;
                if (validSharedChartTypes) {
                    this.addPathsEventListeners([
                        hoverArea
                    ], seriesHoverParams);
                } else if (commonBar && !w.globals.comboCharts || chartWithmarkers && this.showOnIntersect) {
                    this.addDatapointEventsListeners(seriesHoverParams);
                } else if (type === "heatmap" && w.globals.activeRenderer && w.globals.activeRenderer.kind === "canvas") {
                    this.addPathsEventListeners([
                        hoverArea
                    ], seriesHoverParams);
                } else if (!w.globals.axisCharts || type === "heatmap" || type === "treemap") {
                    var seriesAll = w.dom.baseEl.querySelectorAll(".apexcharts-series");
                    this.addPathsEventListeners(seriesAll, seriesHoverParams);
                }
                if (this.showOnIntersect) {
                    var lineAreaPoints = w.dom.baseEl.querySelectorAll(".apexcharts-line-series .apexcharts-marker, .apexcharts-area-series .apexcharts-marker");
                    if (lineAreaPoints.length > 0) {
                        this.addPathsEventListeners(lineAreaPoints, seriesHoverParams);
                    }
                    if (this.tooltipUtil.hasBars() && !this.tConfig.shared) {
                        this.addDatapointEventsListeners(seriesHoverParams);
                    }
                }
            }
        },
        {
            key: "drawFixedTooltipRect",
            value: function drawFixedTooltipRect() {
                var w = this.w;
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return {
                    x: 0,
                    y: 0,
                    ttWidth: 0,
                    ttHeight: 0
                };
                var tooltipRect = tooltipEl.getBoundingClientRect();
                var ttWidth = tooltipRect.width + 10;
                var ttHeight = tooltipRect.height + 10;
                var x = this.tConfig.fixed.offsetX;
                var y = this.tConfig.fixed.offsetY;
                var fixed = this.tConfig.fixed.position.toLowerCase();
                if (fixed.indexOf("right") > -1) {
                    x = x + w.globals.svgWidth - ttWidth + 10;
                }
                if (fixed.indexOf("bottom") > -1) {
                    y = y + w.globals.svgHeight - ttHeight - 10;
                }
                this.tooltipPosition.applyTooltipPosition(tooltipEl, {
                    x: x,
                    y: y
                });
                return {
                    x: x,
                    y: y,
                    ttWidth: ttWidth,
                    ttHeight: ttHeight
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} seriesHoverParams
   */ key: "addDatapointEventsListeners",
            value: function addDatapointEventsListeners(seriesHoverParams) {
                var w = this.w;
                var points = w.dom.baseEl.querySelectorAll(".apexcharts-series-markers .apexcharts-marker, .apexcharts-bar-area, .apexcharts-candlestick-area, .apexcharts-boxPlot-area, .apexcharts-rangebar-area");
                this.addPathsEventListeners(points, seriesHoverParams);
            }
        },
        {
            /**
   * @param {any} paths
   * @param {Record<string, any>} opts
   */ key: "addPathsEventListeners",
            value: function addPathsEventListeners(paths, opts) {
                var _loop = function(p) {
                    var extendedOpts = {
                        paths: paths[p],
                        tooltipEl: opts.tooltipEl,
                        tooltipY: opts.tooltipY,
                        tooltipX: opts.tooltipX,
                        elGrid: opts.elGrid,
                        hoverArea: opts.hoverArea,
                        ttItems: opts.ttItems
                    };
                    var events = [
                        "mousemove",
                        "mouseup",
                        "touchmove",
                        "mouseout",
                        "touchend"
                    ];
                    events.map(function(ev) {
                        return paths[p].addEventListener(ev, self.onSeriesHover.bind(self, extendedOpts), {
                            capture: false,
                            passive: true
                        });
                    });
                };
                var self = this;
                for(var p = 0; p < paths.length; p++)_loop(p);
            }
        },
        {
            /*
   ** Check to see if the tooltips should be updated based on a mouse / touch event
   * @param {Record<string, any>} opt
   * @param {Event} e
   */ /** @param {Record<string, any>} opt @param {any} e */ key: "onSeriesHover",
            value: function onSeriesHover(opt, e) {
                var _this = this;
                var targetDelay = 20;
                var timeSinceLastUpdate = Date.now() - this.lastHoverTime;
                if (timeSinceLastUpdate >= targetDelay) {
                    this.seriesHover(opt, e);
                } else {
                    clearTimeout(this.seriesHoverTimeout);
                    this.seriesHoverTimeout = setTimeout(function() {
                        _this.seriesHover(opt, e);
                    }, targetDelay - timeSinceLastUpdate);
                }
            }
        },
        {
            /*
   ** The actual series hover function
   * @param {Record<string, any>} opt
   * @param {Event} e
   */ /** @param {Record<string, any>} opt @param {any} e */ key: "seriesHover",
            value: function seriesHover(opt, e) {
                var _this = this;
                if (this.w.globals.isDestroyed) return;
                this.lastHoverTime = Date.now();
                var chartGroups = [];
                var w = this.w;
                var isCfMember = function isCfMember(chart) {
                    var _a, _b, _c;
                    var link = (_c = (_b = (_a = chart == null ? void 0 : chart.w) == null ? void 0 : _a.config) == null ? void 0 : _b.chart) == null ? void 0 : _c.link;
                    return !!(link && typeof link.dimension === "function");
                };
                if (w.config.chart.group && !isCfMember(this.ctx)) {
                    chartGroups = this.ctx.getSyncedCharts().filter(function(ch) {
                        return !isCfMember(ch);
                    });
                }
                if (w.globals.axisCharts && (w.globals.minX === -Infinity && w.globals.maxX === Infinity || w.globals.dataPoints === 0)) {
                    return;
                }
                if (chartGroups.length) {
                    chartGroups.forEach(function(ch) {
                        var tooltipEl = _this.getElTooltip(ch);
                        var newOpts = {
                            paths: opt.paths,
                            tooltipEl: tooltipEl,
                            tooltipY: opt.tooltipY,
                            tooltipX: opt.tooltipX,
                            elGrid: opt.elGrid,
                            hoverArea: opt.hoverArea,
                            ttItems: ch.w.globals.tooltip.ttItems
                        };
                        if (ch.w.globals.minX === _this.w.globals.minX && ch.w.globals.maxX === _this.w.globals.maxX) {
                            ch.w.globals.tooltip.seriesHoverByContext({
                                chartCtx: ch,
                                ttCtx: ch.w.globals.tooltip,
                                opt: newOpts,
                                e: e
                            });
                        }
                    });
                } else {
                    this.seriesHoverByContext({
                        chartCtx: this.ctx,
                        ttCtx: this.w.globals.tooltip,
                        opt: opt,
                        e: e
                    });
                }
            }
        },
        {
            /** @param {{chartCtx: any, ttCtx: any, opt: any, e: any}} opts */ key: "seriesHoverByContext",
            value: function seriesHoverByContext(param) {
                var chartCtx = param.chartCtx, ttCtx = param.ttCtx, opt = param.opt, e = param.e;
                var w = chartCtx.w;
                var tooltipEl = this.getElTooltip(chartCtx);
                if (!tooltipEl) return;
                var cachedDims = ttCtx.getCachedDimensions();
                ttCtx.tooltipRect = {
                    x: 0,
                    y: 0,
                    ttWidth: cachedDims.ttWidth,
                    ttHeight: cachedDims.ttHeight
                };
                ttCtx.e = e;
                if (ttCtx.tooltipUtil.hasBars() && !w.globals.comboCharts && !ttCtx.isBarShared) {
                    if (this.tConfig.onDatasetHover.highlightDataSeries) {
                        var series = new Series(chartCtx.w);
                        series.toggleSeriesOnHover(e, e.target.parentNode);
                    }
                }
                if (w.globals.axisCharts) {
                    ttCtx.axisChartsTooltips({
                        e: e,
                        opt: opt,
                        tooltipRect: ttCtx.tooltipRect
                    });
                } else {
                    ttCtx.nonAxisChartsTooltips({
                        e: e,
                        opt: opt,
                        tooltipRect: ttCtx.tooltipRect
                    });
                }
                if (ttCtx.fixedTooltip) {
                    ttCtx.drawFixedTooltipRect();
                }
            }
        },
        {
            // tooltip handling for line/area/bar/columns/scatter
            /** @param {{e: any, opt: any}} opts */ key: "axisChartsTooltips",
            value: function axisChartsTooltips(param) {
                var e = param.e, opt = param.opt;
                var _a;
                var w = this.w;
                var x, y;
                var seriesBound = opt.elGrid.getBoundingClientRect();
                var clientX = e.type === "touchmove" ? e.touches[0].clientX : e.clientX;
                var clientY = e.type === "touchmove" ? e.touches[0].clientY : e.clientY;
                this.clientY = clientY;
                this.clientX = clientX;
                w.interact.capturedSeriesIndex = -1;
                w.interact.capturedDataPointIndex = -1;
                if (clientY < seriesBound.top || clientY > seriesBound.top + seriesBound.height) {
                    this.handleMouseOut(opt);
                    return;
                }
                if (w.dom.elWrap.querySelector(".apexcharts-annotation-tooltip.apexcharts-active")) {
                    this.handleMouseOut(opt);
                    return;
                }
                if (Array.isArray(this.tConfig.enabledOnSeries) && !w.config.tooltip.shared) {
                    var index = parseInt(opt.paths.getAttribute("index"), 10);
                    if (this.tConfig.enabledOnSeries.indexOf(index) < 0) {
                        this.handleMouseOut(opt);
                        return;
                    }
                }
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return;
                var xcrosshairs = this.getElXCrosshairs();
                var syncedCharts = [];
                if (w.config.chart.group) {
                    syncedCharts = this.ctx.getSyncedCharts();
                }
                var isStickyTooltip = w.globals.xyCharts || w.config.chart.type === "bar" && !w.globals.isBarHorizontal && this.tooltipUtil.hasBars() && this.tConfig.shared || w.globals.comboCharts && this.tooltipUtil.hasBars();
                if (e.type === "mousemove" || e.type === "touchmove" || e.type === "mouseup") {
                    if (w.globals.collapsedSeries.length + w.globals.ancillaryCollapsedSeries.length === w.seriesData.series.length) {
                        return;
                    }
                    if (xcrosshairs !== null) {
                        xcrosshairs.classList.add("apexcharts-active");
                    }
                    var hasYAxisTooltip = (_a = this.yaxisTooltips) == null ? void 0 : _a.filter(function(b) {
                        return b === true;
                    });
                    var _yc = /** @type {any} */ this.ycrosshairs;
                    if (_yc !== null && (hasYAxisTooltip == null ? void 0 : hasYAxisTooltip.length)) {
                        _yc.classList.add("apexcharts-active");
                    }
                    if (isStickyTooltip && !this.showOnIntersect || syncedCharts.length > 1) {
                        this.handleStickyTooltip(e, clientX, clientY, opt);
                    } else {
                        if (w.config.chart.type === "heatmap" || w.config.chart.type === "treemap") {
                            var markerXY = this.intersect.handleHeatTreeTooltip({
                                e: e,
                                opt: opt,
                                x: x,
                                y: y,
                                type: w.config.chart.type
                            });
                            if (markerXY.noHit) {
                                this.handleMouseOut(opt);
                                return;
                            }
                            x = markerXY.x;
                            y = markerXY.y;
                            if (!markerXY.positioned) {
                                tooltipEl.style.left = x + "px";
                                tooltipEl.style.top = y + "px";
                            }
                        } else {
                            if (this.tooltipUtil.hasBars()) {
                                this.intersect.handleBarTooltip({
                                    e: e,
                                    opt: opt
                                });
                            }
                            if (this.tooltipUtil.hasMarkers(0)) {
                                this.intersect.handleMarkerTooltip({
                                    e: e,
                                    opt: opt,
                                    x: x,
                                    y: y
                                });
                            }
                        }
                    }
                    if (this.yaxisTooltips && this.yaxisTooltips.length) {
                        for(var yt = 0; yt < w.config.yaxis.length; yt++){
                            this.axesTooltip.drawYaxisTooltipText(yt, clientY, /** @type {import('../../types/internal').XYRatios} */ this.xyRatios);
                        }
                    }
                    w.dom.baseEl.classList.add("apexcharts-tooltip-active");
                    opt.tooltipEl.classList.add("apexcharts-active");
                    if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.announcements.enabled) {
                        opt.tooltipEl.removeAttribute("aria-hidden");
                    }
                } else if (e.type === "mouseout" || e.type === "touchend") {
                    this.handleMouseOut(opt);
                }
            }
        },
        {
            // tooltip handling for pie/donuts
            /** @param {{e: any, opt: any, tooltipRect: any}} opts */ key: "nonAxisChartsTooltips",
            value: function nonAxisChartsTooltips(param) {
                var e = param.e, opt = param.opt, tooltipRect = param.tooltipRect;
                var _a, _b, _c, _d;
                var w = this.w;
                var rel = opt.paths.getAttribute("rel");
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return;
                var seriesBound = w.dom.elWrap.getBoundingClientRect();
                if (e.type === "mousemove" || e.type === "touchmove") {
                    w.dom.baseEl.classList.add("apexcharts-tooltip-active");
                    tooltipEl.classList.add("apexcharts-active");
                    if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.announcements.enabled) {
                        tooltipEl.removeAttribute("aria-hidden");
                    }
                    if (w.config.chart.type === "unit") {
                        var unitDot = e.target && typeof e.target.closest === "function" ? e.target.closest(".apexcharts-unit-area") : null;
                        if (!unitDot) return;
                        this.renderUnitTooltip(unitDot);
                    } else {
                        this.tooltipLabels.drawSeriesTexts({
                            ttItems: opt.ttItems,
                            i: parseInt(rel, 10) - 1,
                            shared: false
                        });
                    }
                    var x, y;
                    var arcPath = opt.paths.querySelector("path[data\\:cx]") || opt.paths;
                    if (w.config.tooltip.intersect && arcPath.hasAttribute("data:cx") && arcPath.hasAttribute("data:cy")) {
                        var svgBound = w.dom.Paper.node.getBoundingClientRect();
                        x = svgBound.left - seriesBound.left + parseFloat(arcPath.getAttribute("data:cx")) - tooltipRect.ttWidth / 2;
                        y = svgBound.top - seriesBound.top + parseFloat(arcPath.getAttribute("data:cy")) - tooltipRect.ttHeight - 10;
                    } else {
                        x = ((_a = w.interact.clientX) != null ? _a : 0) - seriesBound.left - tooltipRect.ttWidth / 2;
                        y = ((_b = w.interact.clientY) != null ? _b : 0) - seriesBound.top - tooltipRect.ttHeight - 10;
                    }
                    tooltipEl.style.left = x + "px";
                    tooltipEl.style.top = y + "px";
                    if (w.config.legend.tooltipHoverFormatter) {
                        var legendFormatter = w.config.legend.tooltipHoverFormatter;
                        var i = rel - 1;
                        var legendEl = /** @type {HTMLElement | undefined} */ (_c = this.legendLabels) == null ? void 0 : _c[i];
                        if (!legendEl) return;
                        var legendName = legendEl.getAttribute("data:default-text");
                        var text = legendFormatter(legendName, {
                            seriesIndex: i,
                            dataPointIndex: i,
                            w: w
                        });
                        legendEl.innerHTML = text;
                    }
                } else if (e.type === "mouseout" || e.type === "touchend") {
                    tooltipEl.classList.remove("apexcharts-active");
                    w.dom.baseEl.classList.remove("apexcharts-tooltip-active");
                    if (w.config.legend.tooltipHoverFormatter) {
                        (_d = this.legendLabels) == null ? void 0 : _d.forEach(function(l) {
                            var defaultText = l.getAttribute("data:default-text");
                            l.innerHTML = decodeURIComponent(defaultText != null ? defaultText : "");
                        });
                    }
                }
            }
        },
        {
            /**
   * Fill the tooltip for one hovered unit-chart dot. Each dot carries `i` (its
   * category / series index) and `j` (its index within that category). The
   * default body reads "#<j+1> of <count>"; `plotOptions.unit.tooltip.formatter`
   * overrides just the body text (it is handed i/j so it can look up per-unit
   * data), and the global `tooltip.custom` still overrides the whole markup.
   * @param {Element} dotEl the hovered `.apexcharts-unit-area` node
   */ key: "renderUnitTooltip",
            value: function renderUnitTooltip(dotEl) {
                var _a, _b, _c, _d, _e;
                var w = this.w;
                var tooltipEl = this.getElTooltip();
                if (!tooltipEl) return;
                var i = parseInt(dotEl.getAttribute("i") || "0", 10);
                var j = parseInt(dotEl.getAttribute("j") || "0", 10);
                if (typeof w.config.tooltip.custom === "function") {
                    this.tooltipLabels.handleCustomTooltip({
                        i: i,
                        j: j,
                        y1: null,
                        y2: null,
                        w: w
                    });
                    return;
                }
                var seriesName = w.seriesData.seriesNames[i] || "series-".concat(i + 1);
                var value = Math.round(Number(w.seriesData.series[i]) || 0);
                var group = dotEl.parentNode;
                var count = group && group.querySelectorAll ? group.querySelectorAll(".apexcharts-unit-area").length : value;
                var unitOpts = w.config.plotOptions.unit || {};
                var unitValue = unitOpts.unitValue > 0 ? unitOpts.unitValue : 1;
                var catData = w.seriesData.unitData && w.seriesData.unitData[i];
                var datum = catData ? catData[j] : void 0;
                var datumObj = datum && (typeof datum === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(datum)) === "object" ? datum : null;
                var color = datumObj && datumObj.fillColor || w.globals.colors && w.globals.colors[i] || "#008FFB";
                var body;
                var fmt = unitOpts.tooltip && unitOpts.tooltip.formatter;
                if (typeof fmt === "function") {
                    body = fmt({
                        seriesName: seriesName,
                        seriesIndex: i,
                        dataPointIndex: j,
                        count: count,
                        value: value,
                        unitValue: unitValue,
                        datum: datum,
                        color: color,
                        w: w
                    });
                } else if (datum !== void 0 && datum !== null) {
                    var label = datumObj ? (_c = (_b = (_a = datumObj.name) != null ? _a : datumObj.label) != null ? _b : datumObj.x) != null ? _c : null : null;
                    var dVal = datumObj ? (_e = (_d = datumObj.value) != null ? _d : datumObj.y) != null ? _e : null : datum;
                    body = label != null && dVal != null ? "".concat(label, ": ").concat(dVal) : label != null ? String(label) : dVal != null ? String(dVal) : "#".concat((j + 1).toLocaleString(), " of ").concat(count.toLocaleString());
                } else {
                    body = "#".concat((j + 1).toLocaleString(), " of ").concat(count.toLocaleString());
                    if (unitValue !== 1) {
                        body += " &middot; ".concat(unitValue.toLocaleString(), " per dot");
                    }
                }
                var fontFamily = w.config.chart.fontFamily || "inherit";
                var fontSize = w.config.tooltip.style && w.config.tooltip.style.fontSize || "12px";
                var arrowEl = tooltipEl.querySelector(".apexcharts-tooltip-arrow");
                tooltipEl.innerHTML = '<div class="apexcharts-tooltip-title" style="font-family: '.concat(fontFamily, "; font-size: ").concat(fontSize, ';">').concat(seriesName, '</div><div class="apexcharts-tooltip-series-group apexcharts-active" style="display: flex;"><span class="apexcharts-tooltip-marker" style="background-color: ').concat(color, ';"></span><div class="apexcharts-tooltip-text" style="font-family: ').concat(fontFamily, "; font-size: ").concat(fontSize, ';"><div class="apexcharts-tooltip-y-group"><span class="apexcharts-tooltip-text-y-value">').concat(body, "</span></div></div></div>");
                if (arrowEl) tooltipEl.appendChild(arrowEl);
                var rect = tooltipEl.getBoundingClientRect();
                this.tooltipRect.ttWidth = rect.width;
                this.tooltipRect.ttHeight = rect.height;
            }
        },
        {
            /**
   * @param {Event} e
   * @param {number} clientX
   * @param {number} clientY
   * @param {Record<string, any>} opt
   */ key: "handleStickyTooltip",
            value: function handleStickyTooltip(e, clientX, clientY, opt) {
                var w = this.w;
                var capj = this.tooltipUtil.getNearestValues({
                    context: this,
                    hoverArea: opt.hoverArea,
                    elGrid: opt.elGrid,
                    clientX: clientX,
                    clientY: clientY
                });
                var j = capj.j;
                var capturedSeries = capj.capturedSeries;
                if (capturedSeries !== null && w.globals.collapsedSeriesIndices.includes(capturedSeries != null ? capturedSeries : -1)) capturedSeries = null;
                var bounds = opt.elGrid.getBoundingClientRect();
                if (capj.hoverX < 0 || capj.hoverX > bounds.width) {
                    this.handleMouseOut(opt);
                    return;
                }
                if (capturedSeries !== null) {
                    this.handleStickyCapturedSeries(e, capturedSeries != null ? capturedSeries : -1, opt, j != null ? j : 0);
                } else {
                    if (this.tooltipUtil.isXoverlap(j != null ? j : 0) || w.globals.isBarHorizontal) {
                        var firstVisibleSeries = w.seriesData.series.findIndex(/**
           * @param {any} s
           * @param {number} i
           */ function(s, i) {
                            return !w.globals.collapsedSeriesIndices.includes(i);
                        });
                        this.create(e, this, firstVisibleSeries, j != null ? j : 0, opt.ttItems);
                    }
                }
            }
        },
        {
            /**
   * @param {Event} e
   * @param {number} capturedSeries
   * @param {Record<string, any>} opt
   * @param {number} j
   */ key: "handleStickyCapturedSeries",
            value: function handleStickyCapturedSeries(e, capturedSeries, opt, j) {
                var w = this.w;
                if (!this.tConfig.shared) {
                    var ignoreNull = w.seriesData.series[capturedSeries][j] === null;
                    if (ignoreNull) {
                        this.handleMouseOut(opt);
                        return;
                    }
                }
                if (typeof w.seriesData.series[capturedSeries][j] !== "undefined") {
                    if (this.tConfig.shared && this.tooltipUtil.isXoverlap(j) && this.tooltipUtil.isInitialSeriesSameLen()) {
                        this.create(e, this, capturedSeries, j, opt.ttItems);
                    } else {
                        this.create(e, this, capturedSeries, j, opt.ttItems, false);
                    }
                } else {
                    if (this.tooltipUtil.isXoverlap(j)) {
                        var firstVisibleSeries = w.seriesData.series.findIndex(/**
           * @param {any} s
           * @param {number} i
           */ function(s, i) {
                            return !w.globals.collapsedSeriesIndices.includes(i);
                        });
                        this.create(e, this, firstVisibleSeries, j, opt.ttItems);
                    }
                }
            }
        },
        {
            key: "deactivateHoverFilter",
            value: function deactivateHoverFilter() {
                var w = this.w;
                var graphics = new Graphics(this.w, this.ctx);
                var allPaths = w.dom.Paper.find(".apexcharts-bar-area");
                for(var b = 0; b < allPaths.length; b++){
                    graphics.pathMouseLeave(/** @type {any} */ allPaths[b], /** @type {any} */ void 0);
                }
            }
        },
        {
            /**
   * @param {Record<string, any>} opt
   */ key: "handleMouseOut",
            value: function handleMouseOut(opt) {
                var _a, _b;
                var w = this.w;
                var xcrosshairs = this.getElXCrosshairs();
                w.dom.baseEl.classList.remove("apexcharts-tooltip-active");
                opt.tooltipEl.classList.remove("apexcharts-active");
                delete opt.tooltipEl.dataset.positioned;
                if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.announcements.enabled) {
                    opt.tooltipEl.setAttribute("aria-hidden", "true");
                }
                this.deactivateHoverFilter();
                if (w.config.chart.type !== "bubble") {
                    this.marker.resetPointsSize();
                }
                if (xcrosshairs !== null) {
                    xcrosshairs.classList.remove("apexcharts-active");
                }
                var _yc2 = /** @type {any} */ this.ycrosshairs;
                if (_yc2 !== null) {
                    _yc2.classList.remove("apexcharts-active");
                }
                if (this.isXAxisTooltipEnabled) {
                    (_a = this.xaxisTooltip) == null ? void 0 : _a.classList.remove("apexcharts-active");
                }
                if (this.yaxisTooltips && this.yaxisTooltips.length) {
                    if (this.yaxisTTEls === null) {
                        this.yaxisTTEls = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.dom.baseEl.querySelectorAll(".apexcharts-yaxistooltip"));
                    }
                    for(var i = 0; i < this.yaxisTTEls.length; i++){
                        this.yaxisTTEls[i].classList.remove("apexcharts-active");
                    }
                }
                if (w.config.legend.tooltipHoverFormatter) {
                    (_b = this.legendLabels) == null ? void 0 : _b.forEach(function(l) {
                        var defaultText = l.getAttribute("data:default-text");
                        l.innerHTML = decodeURIComponent(defaultText != null ? defaultText : "");
                    });
                }
            }
        },
        {
            /**
   * @param {Event} e
   * @param {number} seriesIndex
   * @param {number} dataPointIndex
   */ key: "markerClick",
            value: function markerClick(e, seriesIndex, dataPointIndex) {
                var w = this.w;
                if (typeof w.config.chart.events.markerClick === "function") {
                    w.config.chart.events.markerClick(e, this.ctx, {
                        seriesIndex: seriesIndex,
                        dataPointIndex: dataPointIndex,
                        w: w
                    });
                }
                this.ctx.events.fireEvent("markerClick", [
                    e,
                    this.ctx,
                    {
                        seriesIndex: seriesIndex,
                        dataPointIndex: dataPointIndex,
                        w: w
                    }
                ]);
            }
        },
        {
            /**
   * Marks (#11): whether the chart's type (or any series' type) is a registered
   * custom series, whose marks live outside the built-in marker DOM.
   * @returns {boolean}
   */ key: "_hasCustomSeries",
            value: function _hasCustomSeries() {
                var w = this.w;
                if (isCustom(w.config.chart.type)) return true;
                var series = w.config.series || [];
                return series.some(function(s) {
                    return s && s.type && isCustom(s.type);
                });
            }
        },
        {
            /**
   * @param {Event} e
   * @param {any} context
   * @param {number} capturedSeries
   * @param {number} j
   * @param {any} ttItems
   * @param {boolean | null} shared
   */ key: "create",
            value: function create(e, context, capturedSeries, j, ttItems) {
                var shared = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : null;
                var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u;
                var w = this.w;
                var ttCtx = context;
                if (e.type === "mouseup") {
                    this.markerClick(e, capturedSeries, j);
                }
                if (shared === null) shared = this.tConfig.shared;
                var hasMarkers = this.tooltipUtil.hasMarkers(capturedSeries);
                var canvasMode = ((_b = (_a = this.ctx) == null ? void 0 : _a.renderer) == null ? void 0 : _b.kind) === "canvas";
                var canvasNonBar = canvasMode && !this.tooltipUtil.hasBars();
                var marksMode = !canvasMode && !hasMarkers && !this.tooltipUtil.hasBars() && this._hasCustomSeries();
                var dynamicPoints = canvasNonBar || marksMode;
                var bars = this.tooltipUtil.getElBars();
                var handlePoints = function handlePoints() {
                    if (w.globals.markers.largestSize > 0 && !canvasMode) {
                        ttCtx.marker.enlargePoints(j);
                    } else {
                        ttCtx.tooltipPosition.moveDynamicPointsOnHover(j);
                    }
                };
                if (w.config.legend.tooltipHoverFormatter) {
                    var legendFormatter = w.config.legend.tooltipHoverFormatter;
                    var els = /** @type {HTMLElement[]} */ Array.from((_c = this.legendLabels) != null ? _c : []);
                    els.forEach(function(l) {
                        var legendName = l.getAttribute("data:default-text");
                        l.innerHTML = decodeURIComponent(legendName != null ? legendName : "");
                    });
                    for(var i = 0; i < els.length; i++){
                        var l = els[i];
                        var lsIndex = parseInt((_d = l.getAttribute("i")) != null ? _d : "", 10);
                        var legendName = decodeURIComponent((_e = l.getAttribute("data:default-text")) != null ? _e : "");
                        var text = legendFormatter(legendName, {
                            seriesIndex: shared ? lsIndex : capturedSeries,
                            dataPointIndex: j,
                            w: w
                        });
                        if (!shared) {
                            l.innerHTML = lsIndex === capturedSeries ? text : legendName;
                            if (capturedSeries === lsIndex) {
                                break;
                            }
                        } else {
                            l.innerHTML = w.globals.collapsedSeriesIndices.indexOf(lsIndex) < 0 ? text : legendName;
                        }
                    }
                }
                var _rangeData = /** @type {any} */ w.rangeData;
                var commonSeriesTextsParams = __spreadValues(__spreadValues({
                    ttItems: ttItems,
                    i: capturedSeries,
                    j: j
                }, ((_i = (_h = (_g = (_f = _rangeData.seriesRange) == null ? void 0 : _f[capturedSeries]) == null ? void 0 : _g[j]) == null ? void 0 : _h.y[0]) == null ? void 0 : _i.y1) !== void 0 && {
                    y1: (_m = (_l = (_k = (_j = _rangeData.seriesRange) == null ? void 0 : _j[capturedSeries]) == null ? void 0 : _k[j]) == null ? void 0 : _l.y[0]) == null ? void 0 : _m.y1
                }), ((_q = (_p = (_o = (_n = _rangeData.seriesRange) == null ? void 0 : _n[capturedSeries]) == null ? void 0 : _o[j]) == null ? void 0 : _p.y[0]) == null ? void 0 : _q.y2) !== void 0 && {
                    y2: (_u = (_t = (_s = (_r = _rangeData.seriesRange) == null ? void 0 : _r[capturedSeries]) == null ? void 0 : _s[j]) == null ? void 0 : _t.y[0]) == null ? void 0 : _u.y2
                });
                if (shared) {
                    ttCtx.tooltipLabels.drawSeriesTexts(__spreadProps(__spreadValues({}, commonSeriesTextsParams), {
                        shared: this.showOnIntersect ? false : this.tConfig.shared
                    }));
                    if (hasMarkers || dynamicPoints) {
                        handlePoints();
                    } else if (this.tooltipUtil.hasBars()) {
                        if (canvasMode) {
                            ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, capturedSeries);
                        }
                        this.barSeriesHeight = this.tooltipUtil.getBarsHeight((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(bars));
                        if (this.barSeriesHeight > 0) {
                            var graphics = new Graphics(this.w, this.ctx);
                            var paths = w.dom.Paper.find(".apexcharts-bar-area[j='".concat(j, "']"));
                            this.deactivateHoverFilter();
                            var points = ttCtx.tooltipUtil.getAllMarkers(true);
                            if (points.length && !this.barSeriesHeight) {
                                handlePoints();
                            }
                            ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, capturedSeries);
                            for(var b = 0; b < paths.length; b++){
                                graphics.pathMouseEnter(/** @type {any} */ paths[b], /** @type {any} */ void 0);
                            }
                        }
                    }
                } else {
                    ttCtx.tooltipLabels.drawSeriesTexts(__spreadValues({
                        shared: false
                    }, commonSeriesTextsParams));
                    if (this.tooltipUtil.hasBars()) {
                        ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, capturedSeries);
                    }
                    if (hasMarkers) {
                        ttCtx.tooltipPosition.moveMarkers(capturedSeries, j);
                    } else if (dynamicPoints) {
                        ttCtx.tooltipPosition.moveDynamicPointOnHover(j, capturedSeries);
                    }
                }
            }
        }
    ]);
    return Tooltip;
}();
var SvgRenderer = /*#__PURE__*/ function() {
    "use strict";
    function SvgRenderer(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SvgRenderer);
        this.w = w;
        this.ctx = ctx;
        this.kind = "svg";
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SvgRenderer, [
        {
            // ── lifecycle (SVG builds its layer via the existing plotChartType flow) ──
            key: "beginSeries",
            value: function beginSeries() {}
        },
        {
            key: "present",
            value: function present() {
                return null;
            }
        },
        {
            key: "clear",
            value: function clear() {}
        },
        {
            // ── emit primitives (delegate to Graphics: the canvas renderer mirrors
            //    this exact surface) ──
            /** @param {any} attrs */ key: "group",
            value: function group(attrs) {
                return this.ctx.graphics.group(attrs);
            }
        },
        {
            /** @param {any} opts */ key: "drawPath",
            value: function drawPath(opts) {
                return this.ctx.graphics.drawPath(opts);
            }
        },
        {
            /** @param {any[]} args */ key: "drawLine",
            value: function drawLine() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                var _this_ctx_graphics;
                return (_this_ctx_graphics = this.ctx.graphics).drawLine.apply(_this_ctx_graphics, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(args));
            }
        },
        {
            /** @param {any[]} args */ key: "drawRect",
            value: function drawRect() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                var _this_ctx_graphics;
                return (_this_ctx_graphics = this.ctx.graphics).drawRect.apply(_this_ctx_graphics, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(args));
            }
        },
        {
            /**
   * @param {number} r
   * @param {any} attrs
   */ key: "drawCircle",
            value: function drawCircle(r, attrs) {
                return this.ctx.graphics.drawCircle(r, attrs);
            }
        },
        {
            /** @param {any} opts */ key: "drawText",
            value: function drawText(opts) {
                return this.ctx.graphics.drawText(opts);
            }
        },
        {
            /**
   * A series mark path (animation-aware). Faithful passthrough to Graphics.
   * Note: the SVG emit path in the per-type draw() methods routes through
   * `seriesEmitter`, which returns the caller's own `Graphics` in SVG mode: so
   * this method is the interface contract surface (mirrored by the canvas
   * renderer), not the hot path.
   * @param {any} opts
   */ key: "renderPaths",
            value: function renderPaths(opts) {
                return this.ctx.graphics.renderPaths(opts);
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {any} opts
   */ key: "drawMarker",
            value: function drawMarker(x, y) {
                var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
                return this.ctx.graphics.drawMarker(x, y, opts);
            }
        },
        {
            // ── capabilities: SVG supports everything the interface enumerates ──
            /** @param {string} _feature */ key: "supports",
            value: function supports(_feature) {
                return true;
            }
        },
        {
            // ── interaction: the DOM does this natively in SVG mode ──
            key: "hitTest",
            value: function hitTest() {
                return null;
            }
        },
        {
            key: "restyle",
            value: function restyle() {}
        },
        {
            // ── export: SVG serializes directly; no bitmap to composite ──
            key: "toBitmap",
            value: function toBitmap() {
                return null;
            }
        },
        {
            key: "destroy",
            value: function destroy() {}
        }
    ]);
    return SvgRenderer;
}();
var RENDERER_REGISTRY_KEY = "__apexcharts_renderers__";
function getRendererRegistry() {
    var g = /** @type {any} */ globalThis;
    if (!g[RENDERER_REGISTRY_KEY]) g[RENDERER_REGISTRY_KEY] = /* @__PURE__ */ new Map();
    return g[RENDERER_REGISTRY_KEY];
}
var RendererController = /*#__PURE__*/ function() {
    "use strict";
    function RendererController(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, RendererController);
        this.w = w;
        this.ctx = ctx;
        this.svg = new SvgRenderer(w, ctx);
        this.active = this.svg;
        this._activeKind = "svg";
        this._instances = {};
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(RendererController, [
        {
            /**
   * The kind selection WANTS (before availability/fallback). Pure.
   * @returns {import('../renderers/Renderer').RendererKind}
   */ key: "_desiredKind",
            value: function _desiredKind() {
                var cfg = this.w.config.chart;
                var mode = cfg.renderer || "svg";
                if (!Environment.isBrowser()) return "svg";
                if (mode === "svg") return "svg";
                if (hasCanvasUnsupportedFeature(this.w)) return "svg";
                if (mode === "canvas") return "canvas";
                var marks = computeMarkCount(this.w);
                var threshold = cfg.rendererThreshold || 8e3;
                return marks >= threshold ? "canvas" : "svg";
            }
        },
        {
            /**
   * Resolve + instantiate the active renderer and set `ctx.renderer`. Falls
   * back to SVG (with a warning only when canvas was explicitly requested) if
   * the desired backend is not registered.
   * @returns {import('../renderers/Renderer').RendererKind}
   */ key: "resolve",
            value: function resolve() {
                var mode = this.w.config.chart.renderer || "svg";
                var desired = this._desiredKind();
                if (desired !== "svg") {
                    var factory = getRendererRegistry().get(desired);
                    if (factory) {
                        if (!this._instances[desired]) {
                            this._instances[desired] = factory(this.w, this.ctx);
                        }
                        this.active = this._instances[desired];
                        this._activeKind = desired;
                        this.ctx.renderer = this.active;
                        this.w.globals.activeRenderer = this.active;
                        return this._activeKind;
                    }
                    if (mode === desired) {
                        console.warn('[apexcharts] renderer:"'.concat(desired, "\" requested but that renderer is not bundled (import 'apexcharts/features/renderer-").concat(desired, "'); falling back to SVG."));
                    }
                } else if (mode === "canvas" && hasCanvasUnsupportedFeature(this.w)) {
                    console.warn('[apexcharts] renderer:"canvas" requested but this chart uses a feature the canvas renderer does not render yet (gradient/pattern/image fill or a state color-matrix filter); falling back to SVG.');
                }
                this.active = this.svg;
                this._activeKind = "svg";
                this.ctx.renderer = this.active;
                this.w.globals.activeRenderer = this.active;
                return this._activeKind;
            }
        },
        {
            /** @returns {import('../renderers/Renderer').RendererKind} */ key: "getActiveKind",
            value: function getActiveKind() {
                return this._activeKind;
            }
        },
        {
            /** Destroy the owned non-SVG renderer instances (full chart destroy). */ key: "teardown",
            value: function teardown() {
                for(var kind in this._instances){
                    var r = this._instances[kind];
                    if (r && typeof r.destroy === "function") r.destroy();
                }
                this._instances = {};
                this.active = this.svg;
                this._activeKind = "svg";
            }
        }
    ], [
        {
            key: "_rendererRegistry",
            get: /** Same Map as getRendererRegistry(); exposed for tests/tooling. */ function get() {
                return getRendererRegistry();
            }
        },
        {
            key: "registerRenderer",
            value: /**
   * @param {string} kind
   * @param {(w: any, ctx: any) => any} factory
   */ function registerRenderer(kind, factory) {
                getRendererRegistry().set(kind, factory);
            }
        },
        {
            key: "unregisterRenderer",
            value: /**
   * Remove a registered renderer backend (tests / hot-reload). Charts fall
   * back to SVG on their next resolve().
   * @param {string} kind
   */ function unregisterRenderer(kind) {
                getRendererRegistry().delete(kind);
            }
        }
    ]);
    return RendererController;
}();
var SVGElement = /*#__PURE__*/ function() {
    "use strict";
    function SVGElement(node) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGElement);
        this.node = node;
        if (node) {
            node.instance = this;
        }
        this._listeners = [];
        this._filter = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGElement, [
        {
            // ---- Attribute methods ----
            /**
   * @param {any} a
   * @param {any} [v]
   */ key: "attr",
            value: function attr(a, v) {
                if (typeof a === "string" && v === void 0) {
                    return this.node.getAttribute(a);
                }
                var attrs = typeof a === "string" ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])({}, a, v) : a;
                for(var key in attrs){
                    var val = attrs[key];
                    if (val === null) {
                        this.node.removeAttribute(key);
                    } else if (val !== void 0) {
                        if (typeof val === "number" && isNaN(val)) val = 0;
                        this.node.setAttribute(key, val);
                    }
                }
                if (this.node.nodeName === "text" && attrs.x != null) {
                    var tspans = this.node.querySelectorAll("tspan[data-newline]");
                    for(var i = 0; i < tspans.length; i++){
                        tspans[i].setAttribute("x", attrs.x);
                    }
                }
                return this;
            }
        },
        {
            /**
   * @param {Record<string, string>} styles
   */ key: "css",
            value: function css(styles) {
                for(var k in styles){
                    this.node.style[k] = styles[k];
                }
                return this;
            }
        },
        {
            /**
   * @param {any} v
   */ key: "fill",
            value: function fill(v) {
                if ((typeof v === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(v)) === "object") {
                    return this.attr(v);
                }
                return this.attr("fill", v);
            }
        },
        {
            /**
   * @param {any} v
   */ key: "stroke",
            value: function stroke(v) {
                if ((typeof v === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(v)) === "object") {
                    if (v.color !== void 0) this.attr("stroke", v.color);
                    if (v.width !== void 0) this.attr("stroke-width", v.width);
                    if (v.dasharray !== void 0) this.attr("stroke-dasharray", v.dasharray);
                    if (v.linecap !== void 0) this.attr("stroke-linecap", v.linecap);
                    if (v.opacity !== void 0) this.attr("stroke-opacity", v.opacity);
                    return this;
                }
                return this.attr("stroke", v);
            }
        },
        {
            /**
   * @param {number} w
   * @param {number} h
   */ key: "size",
            value: function size(w, h) {
                return this.attr({
                    width: w,
                    height: h
                });
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   */ key: "move",
            value: function move(x, y) {
                return this.attr({
                    x: x,
                    y: y
                });
            }
        },
        {
            /**
   * @param {number} cx
   * @param {number} cy
   */ key: "center",
            value: function center(cx, cy) {
                if (this.node.nodeName === "g") {
                    var box = this.bbox();
                    var dx = cx - (box.x + box.width / 2);
                    var dy = cy - (box.y + box.height / 2);
                    return this.attr("transform", "translate(".concat(dx, ", ").concat(dy, ")"));
                }
                return this.attr({
                    cx: cx,
                    cy: cy
                });
            }
        },
        {
            // ---- Tree operations ----
            /**
   * @param {any} child
   */ key: "add",
            value: function add(child) {
                if (child && child.__isCanvasMark) return this;
                this.node.appendChild(child.node || child);
                return this;
            }
        },
        {
            /**
   * @param {any} parent
   */ key: "addTo",
            value: function addTo(parent) {
                var p = parent.node || parent;
                p.appendChild(this.node);
                return this;
            }
        },
        {
            key: "remove",
            value: function remove() {
                if (this.node.parentNode) {
                    this.node.parentNode.removeChild(this.node);
                }
                return this;
            }
        },
        {
            key: "clear",
            value: function clear() {
                while(this.node.firstChild){
                    this.node.removeChild(this.node.firstChild);
                }
                return this;
            }
        },
        {
            // ---- Query ----
            /**
   * @param {string} selector
   */ key: "find",
            value: function find(selector) {
                return Array.from(this.node.querySelectorAll(selector)).map(function(n) {
                    return n.instance || new SVGElement(n);
                });
            }
        },
        {
            /**
   * @param {string} selector
   */ key: "findOne",
            value: function findOne(selector) {
                var n = this.node.querySelector(selector);
                return n ? n.instance || new SVGElement(n) : null;
            }
        },
        {
            // ---- Events ----
            /**
   * @param {Event} event
   * @param {Function} handler
   */ key: "on",
            value: function on(event, handler) {
                var eventType = /** @type {string} */ /** @type {any} */ event.split(".")[0];
                this._listeners.push({
                    event: event,
                    eventType: eventType,
                    handler: handler
                });
                this.node.addEventListener(eventType, handler);
                return this;
            }
        },
        {
            /**
   * @param {Event} event
   * @param {Function} handler
   */ key: "off",
            value: function off(event, handler) {
                var _this = this;
                if (!event && !handler) {
                    this._listeners.forEach(function(l) {
                        _this.node.removeEventListener(l.eventType, l.handler);
                    });
                    this._listeners = [];
                } else if (event && !handler) {
                    var eventType = /** @type {string} */ /** @type {any} */ event.split(".")[0];
                    this._listeners = this._listeners.filter(function(l) {
                        if (l.eventType === eventType) {
                            _this.node.removeEventListener(l.eventType, l.handler);
                            return false;
                        }
                        return true;
                    });
                } else {
                    var eventType1 = /** @type {string} */ /** @type {any} */ event.split(".")[0];
                    this._listeners = this._listeners.filter(function(l) {
                        if (l.eventType === eventType1 && l.handler === handler) {
                            _this.node.removeEventListener(l.eventType, l.handler);
                            return false;
                        }
                        return true;
                    });
                }
                return this;
            }
        },
        {
            // ---- Iteration ----
            /**
   * @param {Function} fn
   * @param {boolean} deep
   */ key: "each",
            value: function each(fn, deep) {
                var children = Array.from(this.node.children);
                children.forEach(function(child) {
                    var inst = child.instance || new SVGElement(child);
                    fn.call(inst);
                    if (deep) inst.each(fn, deep);
                });
                return this;
            }
        },
        {
            // ---- CSS classes ----
            /**
   * @param {string} cls
   */ key: "removeClass",
            value: function removeClass(cls) {
                if (cls === "*") {
                    this.node.removeAttribute("class");
                } else {
                    this.node.classList.remove(cls);
                }
                return this;
            }
        },
        {
            // ---- Children ----
            key: "children",
            value: function children() {
                return Array.from(this.node.childNodes).filter(function(n) {
                    return n.nodeType === 1;
                }).map(function(n) {
                    return n.instance || new SVGElement(n);
                });
            }
        },
        {
            // ---- Visibility ----
            key: "hide",
            value: function hide() {
                this.node.style.display = "none";
                return this;
            }
        },
        {
            key: "show",
            value: function show() {
                this.node.style.display = "";
                return this;
            }
        },
        {
            // ---- Measurement ----
            key: "bbox",
            value: function bbox() {
                if (typeof this.node.getBBox === "function") {
                    try {
                        return this.node.getBBox();
                    } catch (e) {}
                }
                return {
                    x: 0,
                    y: 0,
                    width: 0,
                    height: 0
                };
            }
        },
        {
            // ---- Text-specific ----
            /**
   * @param {string} text
   */ key: "tspan",
            value: function tspan(text) {
                var tspan = BrowserAPIs.createElementNS("http://www.w3.org/2000/svg", "tspan");
                tspan.textContent = text;
                this.node.appendChild(tspan);
                return new SVGElement(tspan);
            }
        },
        {
            // ---- Path-specific ----
            /**
   * @param {string} d
   */ key: "plot",
            value: function plot(d) {
                if (typeof d === "string") {
                    this.attr("d", d);
                }
                return this;
            }
        },
        {
            // ---- Animation (overridden by SVGAnimation mixin) ----
            key: "animate",
            value: function animate() {
                throw new Error("Animation module not loaded");
            }
        },
        {
            // ---- Filter methods (set up by SVGFilter module) ----
            // `filterWith`, `unfilter` and `filterer` are installed on the prototype by
            // installFilterMethods() (svg/index.js). This stub only exists so a call
            // before the filter module is installed fails with a clear message.
            key: "filterWith",
            value: function filterWith() {
                throw new Error("Filter module not loaded");
            }
        }
    ]);
    return SVGElement;
}();
var gradientCounter = 0;
var SVGGradient = /*#__PURE__*/ function(SVGElement) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGGradient, SVGElement);
    function SVGGradient(container, type, builder) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGGradient);
        var _this;
        var tag = type === "radial" ? "radialGradient" : "linearGradient";
        var node = BrowserAPIs.createElementNS(SVGNS$2, tag);
        _this = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGGradient, [
            node
        ]);
        _this._id = "SvgjsGradient" + ++gradientCounter;
        _this.attr("id", _this._id);
        if (typeof builder === "function") {
            builder(new StopBuilder(_this));
        }
        var defs = container.node.querySelector("defs");
        if (!defs) {
            defs = BrowserAPIs.createElementNS(SVGNS$2, "defs");
            container.node.appendChild(defs);
        }
        defs.appendChild(_this.node);
        return _this;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGGradient, [
        {
            /**
   * @param {any} offset
   * @param {string} color
   * @param {number} opacity
   */ key: "stop",
            value: function stop(offset, color, opacity) {
                var s = BrowserAPIs.createElementNS(SVGNS$2, "stop");
                s.setAttribute("offset", offset);
                s.setAttribute("stop-color", color);
                if (opacity !== void 0) s.setAttribute("stop-opacity", String(opacity));
                this.node.appendChild(s);
                return this;
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   */ key: "from",
            value: function from(x, y) {
                return this.attr({
                    x1: x,
                    y1: y
                });
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   */ key: "to",
            value: function to(x, y) {
                return this.attr({
                    x2: x,
                    y2: y
                });
            }
        },
        {
            key: "url",
            value: function url() {
                return "url(#" + this._id + ")";
            }
        },
        {
            key: "toString",
            value: function toString() {
                return this.url();
            }
        },
        {
            key: "valueOf",
            value: function valueOf() {
                return this.url();
            }
        },
        {
            key: "fill",
            value: function fill() {
                return this.url();
            }
        }
    ]);
    return SVGGradient;
}((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_wrap_native_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGElement));
var StopBuilder = /*#__PURE__*/ function() {
    "use strict";
    function StopBuilder(gradient) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, StopBuilder);
        this.gradient = gradient;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(StopBuilder, [
        {
            /**
   * @param {any} offset
   * @param {string} color
   * @param {number} opacity
   */ key: "stop",
            value: function stop(offset, color, opacity) {
                this.gradient.stop(offset, color, opacity);
                return this;
            }
        }
    ]);
    return StopBuilder;
}();
var patternCounter = 0;
var SVGPattern = /*#__PURE__*/ function(SVGElement) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGPattern, SVGElement);
    function SVGPattern(container, w, h, builder) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGPattern);
        var _this;
        var node = BrowserAPIs.createElementNS(SVGNS$2, "pattern");
        _this = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGPattern, [
            node
        ]);
        _this._id = "SvgjsPattern" + ++patternCounter;
        _this.attr({
            id: _this._id,
            width: w,
            height: h,
            patternUnits: "userSpaceOnUse"
        });
        if (typeof builder === "function") {
            var patternContainer = new SVGContainer(_this.node);
            builder(patternContainer);
        }
        var defs = container.node.querySelector("defs");
        if (!defs) {
            defs = BrowserAPIs.createElementNS(SVGNS$2, "defs");
            container.node.appendChild(defs);
        }
        defs.appendChild(_this.node);
        return _this;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGPattern, [
        {
            key: "url",
            value: function url() {
                return "url(#" + this._id + ")";
            }
        },
        {
            key: "toString",
            value: function toString() {
                return this.url();
            }
        },
        {
            key: "valueOf",
            value: function valueOf() {
                return this.url();
            }
        },
        {
            key: "fill",
            value: function fill() {
                return this.url();
            }
        }
    ]);
    return SVGPattern;
}((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_wrap_native_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGElement));
var SVGContainer = /*#__PURE__*/ function(SVGElement1) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGContainer, SVGElement1);
    function SVGContainer() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGContainer);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGContainer, arguments);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGContainer, [
        {
            /**
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   */ key: "line",
            value: function line(x1, y1, x2, y2) {
                var el = this._make("line");
                if (x1 !== void 0) {
                    el.attr({
                        x1: x1,
                        y1: y1,
                        x2: x2,
                        y2: y2
                    });
                }
                return el;
            }
        },
        {
            /**
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} h
   */ key: "rect",
            value: function rect(w, h) {
                var el = this._make("rect");
                if (w !== void 0) {
                    el.attr({
                        width: w,
                        height: h
                    });
                }
                return el;
            }
        },
        {
            /**
   * @param {number} d
   */ key: "circle",
            value: function circle(d) {
                var el = this._make("circle");
                if (d !== void 0) {
                    el.attr({
                        r: d / 2,
                        cx: d / 2,
                        cy: d / 2
                    });
                }
                return el;
            }
        },
        {
            /**
   * @param {string} d
   */ key: "path",
            value: function path(d) {
                var el = this._make("path");
                if (d) el.attr("d", d);
                return el;
            }
        },
        {
            /**
   * @param {string} pts
   */ key: "polygon",
            value: function polygon(pts) {
                var el = this._make("polygon");
                if (pts) el.attr("points", pts);
                return el;
            }
        },
        {
            key: "group",
            value: function group() {
                return this._makeContainer("g");
            }
        },
        {
            key: "defs",
            value: function defs() {
                return this._makeContainer("defs");
            }
        },
        {
            /**
   * @param {string} textContent
   */ key: "plain",
            value: function plain(textContent) {
                var node = BrowserAPIs.createElementNS(SVGNS$2, "text");
                node.textContent = textContent;
                var el = new SVGElement(node);
                this.node.appendChild(node);
                return el;
            }
        },
        {
            /**
   * @param {object} builder
   */ key: "text",
            value: function text(builder) {
                var node = BrowserAPIs.createElementNS(SVGNS$2, "text");
                var el = new SVGElement(node);
                this.node.appendChild(node);
                if (typeof builder === "function") {
                    builder(new TspanBuilder(node));
                }
                return el;
            }
        },
        {
            /**
   * @param {string} url
   * @param {Function} callback
   */ key: "image",
            value: function image(url, callback) {
                var node = BrowserAPIs.createElementNS(SVGNS$2, "image");
                node.setAttributeNS("http://www.w3.org/1999/xlink", "href", url);
                var el = new SVGElement(node);
                this.node.appendChild(node);
                if (typeof callback === "function" && Environment.isBrowser()) {
                    var img = new Image();
                    img.onload = function() {
                        el.size(img.width, img.height);
                        callback.call(el, {
                            width: img.width,
                            height: img.height
                        });
                    };
                    img.src = url;
                }
                return el;
            }
        },
        {
            /**
   * @param {string} type
   * @param {object} builder
   */ key: "gradient",
            value: function gradient(type, builder) {
                return new SVGGradient(this, type, builder);
            }
        },
        {
            /**
   * @param {number} w
   * @param {number} h
   * @param {Function} builder
   */ key: "pattern",
            value: function pattern(w, h, builder) {
                return new SVGPattern(this, w, h, builder);
            }
        },
        {
            /**
   * @param {string} tag
   */ key: "_make",
            value: function _make(tag) {
                var node = BrowserAPIs.createElementNS(SVGNS$2, tag);
                this.node.appendChild(node);
                return new SVGElement(node);
            }
        },
        {
            /**
   * @param {string} tag
   */ key: "_makeContainer",
            value: function _makeContainer(tag) {
                var node = BrowserAPIs.createElementNS(SVGNS$2, tag);
                this.node.appendChild(node);
                return new SVGContainer(node);
            }
        }
    ]);
    return SVGContainer;
}((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_wrap_native_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGElement));
var TspanBuilder = /*#__PURE__*/ function() {
    "use strict";
    function TspanBuilder(textNode) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, TspanBuilder);
        this.textNode = textNode;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(TspanBuilder, [
        {
            /**
   * @param {string} text
   */ key: "tspan",
            value: function tspan(text) {
                var tspan = BrowserAPIs.createElementNS(SVGNS$2, "tspan");
                tspan.textContent = text;
                this.textNode.appendChild(tspan);
                return new TspanWrapper(tspan, this.textNode);
            }
        }
    ]);
    return TspanBuilder;
}();
var TspanWrapper = /*#__PURE__*/ function() {
    "use strict";
    function TspanWrapper(node, textNode) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, TspanWrapper);
        this.node = node;
        this.textNode = textNode;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(TspanWrapper, [
        {
            key: "newLine",
            value: function newLine() {
                this.node.setAttribute("dy", "1.1em");
                this.node.dataset.newline = "1";
                return this;
            }
        }
    ]);
    return TspanWrapper;
}();
var filterCounter = 0;
var SVGFilter = /*#__PURE__*/ function(SVGElement) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGFilter, SVGElement);
    function SVGFilter() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGFilter);
        var _this;
        var node = BrowserAPIs.createElementNS(SVGNS$2, "filter");
        _this = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SVGFilter, [
            node
        ]);
        _this._id = "SvgjsFilter" + ++filterCounter;
        _this.attr("id", _this._id);
        return _this;
    }
    return SVGFilter;
}((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_wrap_native_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SVGElement));
var FilterBuilder = /*#__PURE__*/ function() {
    "use strict";
    function FilterBuilder(filter) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, FilterBuilder);
        this.filter = filter;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(FilterBuilder, [
        {
            /**
   * @param {object} attrs
   */ key: "colorMatrix",
            value: function colorMatrix(attrs) {
                return this._primitive("feColorMatrix", attrs);
            }
        },
        {
            /**
   * @param {object} attrs
   */ key: "offset",
            value: function offset(attrs) {
                return this._primitive("feOffset", attrs);
            }
        },
        {
            /**
   * @param {object} attrs
   */ key: "gaussianBlur",
            value: function gaussianBlur(attrs) {
                return this._primitive("feGaussianBlur", attrs);
            }
        },
        {
            /**
   * @param {object} attrs
   */ key: "flood",
            value: function flood(attrs) {
                return this._primitive("feFlood", attrs);
            }
        },
        {
            /**
   * @param {object} attrs
   */ key: "composite",
            value: function composite(attrs) {
                return this._primitive("feComposite", attrs);
            }
        },
        {
            /**
   * @param {string[]} sources
   */ key: "merge",
            value: function merge(sources) {
                var m = BrowserAPIs.createElementNS(SVGNS$2, "feMerge");
                sources.forEach(function(src) {
                    var mn = BrowserAPIs.createElementNS(SVGNS$2, "feMergeNode");
                    mn.setAttribute("in", src);
                    m.appendChild(mn);
                });
                this.filter.node.appendChild(m);
                return new SVGElement(m);
            }
        },
        {
            /**
   * @param {string} tag
   * @param {Record<string, any>} attrs
   */ key: "_primitive",
            value: function _primitive(tag, attrs) {
                var el = BrowserAPIs.createElementNS(SVGNS$2, tag);
                for(var key in attrs){
                    el.setAttribute(key, attrs[key]);
                }
                this.filter.node.appendChild(el);
                return new SVGElement(el);
            }
        }
    ]);
    return FilterBuilder;
}();
function installFilterMethods(ElementClass) {
    ElementClass.prototype.filterWith = function(fn) {
        var filter = new SVGFilter();
        this._filter = filter;
        var svgRoot = this.node;
        while(svgRoot && svgRoot.nodeName !== "svg"){
            svgRoot = svgRoot.parentNode;
        }
        if (svgRoot) {
            var defs = svgRoot.querySelector("defs");
            if (!defs) {
                defs = BrowserAPIs.createElementNS(SVGNS$2, "defs");
                svgRoot.insertBefore(defs, svgRoot.firstChild);
            }
            defs.appendChild(filter.node);
        }
        fn(new FilterBuilder(filter));
        this.attr("filter", "url(#" + filter._id + ")");
        return this;
    };
    ElementClass.prototype.unfilter = function(all) {
        if (this._filter) {
            this.node.removeAttribute("filter");
            if (all && this._filter.node && this._filter.node.parentNode) {
                this._filter.node.parentNode.removeChild(this._filter.node);
            }
            this._filter = null;
        }
        return this;
    };
    ElementClass.prototype.filterer = function() {
        return this._filter;
    };
}
function installDraggable(ElementClass) {
    ElementClass.prototype.draggable = function(opts) {
        if (opts === false) {
            if (this._dragCleanup) {
                this._dragCleanup();
                this._dragCleanup = null;
            }
            return this;
        }
        var el = this;
        var constraints = opts || {};
        var onPointerDown = function onPointerDown(e) {
            if (e.button && e.button !== 0) return;
            e.stopPropagation();
            var isTouch = e.type === "touchstart";
            var ev = isTouch ? e.touches[0] : e;
            var svgEl = el.node;
            var startAttrX = parseFloat(svgEl.getAttribute("x")) || 0;
            var startAttrY = parseFloat(svgEl.getAttribute("y")) || 0;
            var startClientX = ev.clientX;
            var startClientY = ev.clientY;
            var svgRoot = svgEl.ownerSVGElement;
            var ctm = null;
            if (svgRoot) {
                ctm = svgRoot.getScreenCTM();
            }
            var onMove = function onMove(me) {
                var mev = me.type === "touchmove" ? me.touches[0] : me;
                var dx = mev.clientX - startClientX;
                var dy = mev.clientY - startClientY;
                if (ctm) {
                    dx = dx / ctm.a;
                    dy = dy / ctm.d;
                }
                var newX = startAttrX + dx;
                var newY = startAttrY + dy;
                var w = parseFloat(svgEl.getAttribute("width")) || 0;
                var h = parseFloat(svgEl.getAttribute("height")) || 0;
                if (constraints.minX !== void 0 && newX < constraints.minX) newX = constraints.minX;
                if (constraints.minY !== void 0 && newY < constraints.minY) newY = constraints.minY;
                if (constraints.maxX !== void 0 && newX + w > constraints.maxX) newX = constraints.maxX - w;
                if (constraints.maxY !== void 0 && newY + h > constraints.maxY) newY = constraints.maxY - h;
                var box = {
                    x: newX,
                    y: newY,
                    w: w,
                    h: h,
                    x2: newX + w,
                    y2: newY + h
                };
                var event = new CustomEvent("dragmove", {
                    detail: {
                        handler: {
                            /**
               * @param {number} x
               * @param {number} y
               */ move: function move(x, y) {
                                svgEl.setAttribute("x", x);
                                svgEl.setAttribute("y", y);
                            }
                        },
                        box: box
                    }
                });
                svgEl.dispatchEvent(event);
            };
            var onUp = function onUp1() {
                if (Environment.isBrowser()) {
                    document.removeEventListener("mousemove", onMove);
                    document.removeEventListener("touchmove", onMove);
                    document.removeEventListener("mouseup", onUp);
                    document.removeEventListener("touchend", onUp);
                }
                el._activeDrag = null;
            };
            if (Environment.isBrowser()) {
                document.addEventListener("mousemove", onMove);
                document.addEventListener("touchmove", onMove);
                document.addEventListener("mouseup", onUp);
                document.addEventListener("touchend", onUp);
                el._activeDrag = {
                    onMove: onMove,
                    onUp: onUp
                };
            }
        };
        el.node.addEventListener("mousedown", onPointerDown);
        el.node.addEventListener("touchstart", onPointerDown);
        el._dragCleanup = function() {
            el.node.removeEventListener("mousedown", onPointerDown);
            el.node.removeEventListener("touchstart", onPointerDown);
            if (el._activeDrag && Environment.isBrowser()) {
                document.removeEventListener("mousemove", el._activeDrag.onMove);
                document.removeEventListener("touchmove", el._activeDrag.onMove);
                document.removeEventListener("mouseup", el._activeDrag.onUp);
                document.removeEventListener("touchend", el._activeDrag.onUp);
                el._activeDrag = null;
            }
        };
        return el;
    };
}
function installSelectable(ElementClass) {
    ElementClass.prototype.select = function(opts) {
        if (opts === false) {
            if (this._selectCleanup) {
                this._selectCleanup();
                this._selectCleanup = null;
            }
            return this;
        }
        var el = this;
        var createHandle = opts.createHandle, updateHandle = opts.updateHandle;
        var handleGroup = document.createElementNS(SVGNS$2, "g");
        handleGroup.setAttribute("class", "svg_select_points");
        var parent = el.node.parentNode;
        if (parent) {
            parent.appendChild(handleGroup);
        }
        var handles = {};
        var handleNames = [
            "t",
            "b",
            "l",
            "r",
            "lt",
            "rt",
            "lb",
            "rb"
        ];
        handleNames.forEach(function(name2, index) {
            var subGroup = new SVGContainer(document.createElementNS(SVGNS$2, "g"));
            handleGroup.appendChild(subGroup.node);
            var handle = createHandle(subGroup, [
                0,
                0
            ], index, [], name2);
            handles[name2] = {
                group: subGroup,
                handle: handle
            };
        });
        var updatePositions = function updatePositions() {
            var x = parseFloat(el.attr("x")) || 0;
            var y = parseFloat(el.attr("y")) || 0;
            var w = parseFloat(el.attr("width")) || 0;
            var h = parseFloat(el.attr("height")) || 0;
            var elTransform = el.node.getAttribute("transform");
            if (elTransform) {
                handleGroup.setAttribute("transform", elTransform);
            } else {
                handleGroup.removeAttribute("transform");
            }
            var positions = {
                t: [
                    x + w / 2,
                    y
                ],
                b: [
                    x + w / 2,
                    y + h
                ],
                l: [
                    x,
                    y + h / 2
                ],
                r: [
                    x + w,
                    y + h / 2
                ],
                lt: [
                    x,
                    y
                ],
                rt: [
                    x + w,
                    y
                ],
                lb: [
                    x,
                    y + h
                ],
                rb: [
                    x + w,
                    y + h
                ]
            };
            handleNames.forEach(function(name2) {
                if (handles[name2] && positions[name2]) {
                    updateHandle(handles[name2].group, positions[name2]);
                }
            });
        };
        updatePositions();
        el._selectHandles = handleGroup;
        el._selectHandlesMap = handles;
        el._updateSelectPositions = updatePositions;
        el._selectCleanup = function() {
            if (handleGroup.parentNode) {
                handleGroup.parentNode.removeChild(handleGroup);
            }
            el._selectHandles = null;
            el._selectHandlesMap = null;
            el._updateSelectPositions = null;
        };
        return el;
    };
    ElementClass.prototype.resize = function(enable) {
        if (enable === false) {
            if (this._resizeCleanup) {
                this._resizeCleanup();
                this._resizeCleanup = null;
            }
            return this;
        }
        var el = this;
        var handles = el._selectHandlesMap;
        if (!handles) return el;
        var cleanupFns = [];
        var makeHandleDraggable = function makeHandleDraggable(name2) {
            var handleInfo = handles[name2];
            if (!handleInfo || !handleInfo.group || !handleInfo.group.node) return;
            var handleNode = handleInfo.group.node;
            var onPointerDown = function onPointerDown(e) {
                if (e.button && e.button !== 0) return;
                e.stopPropagation();
                var isTouch = e.type === "touchstart";
                var ev = isTouch ? e.touches[0] : e;
                var startClientX = ev.clientX;
                var svgRoot = el.node.ownerSVGElement;
                var ctm = null;
                if (svgRoot) {
                    ctm = svgRoot.getScreenCTM();
                }
                var startX = parseFloat(el.attr("x")) || 0;
                var startW = parseFloat(el.attr("width")) || 0;
                var onMove = function onMove(me) {
                    var mev = me.type === "touchmove" ? me.touches[0] : me;
                    var dx = mev.clientX - startClientX;
                    if (ctm) dx = dx / /** @type {any} */ ctm.a;
                    var newX = startX;
                    var newW = startW;
                    if (name2 === "l") {
                        newX = startX + dx;
                        newW = startW - dx;
                    } else if (name2 === "r") {
                        newW = startW + dx;
                    }
                    if (newW < 0) {
                        newW = 0;
                    }
                    el.attr({
                        x: newX,
                        width: newW
                    });
                    if (el._updateSelectPositions) {
                        el._updateSelectPositions();
                    }
                    var event = new CustomEvent("resize", {
                        detail: {
                            el: el
                        }
                    });
                    el.node.dispatchEvent(event);
                };
                var onUp = function onUp1() {
                    if (Environment.isBrowser()) {
                        document.removeEventListener("mousemove", onMove);
                        document.removeEventListener("touchmove", onMove);
                        document.removeEventListener("mouseup", onUp);
                        document.removeEventListener("touchend", onUp);
                    }
                    var event = new CustomEvent("resize", {
                        detail: {
                            el: el
                        }
                    });
                    el.node.dispatchEvent(event);
                };
                if (Environment.isBrowser()) {
                    document.addEventListener("mousemove", onMove);
                    document.addEventListener("touchmove", onMove);
                    document.addEventListener("mouseup", onUp);
                    document.addEventListener("touchend", onUp);
                }
            };
            handleNode.addEventListener("mousedown", onPointerDown);
            handleNode.addEventListener("touchstart", onPointerDown);
            cleanupFns.push(function() {
                handleNode.removeEventListener("mousedown", onPointerDown);
                handleNode.removeEventListener("touchstart", onPointerDown);
            });
        };
        makeHandleDraggable("l");
        makeHandleDraggable("r");
        el._resizeCleanup = function() {
            cleanupFns.forEach(function(fn) {
                return fn();
            });
        };
        return el;
    };
}
installFilterMethods(SVGElement);
installAnimationMethods(SVGElement);
installDraggable(SVGElement);
installSelectable(SVGElement);
function SVG() {
    var svgEl = BrowserAPIs.createElementNS(SVGNS$2, "svg");
    var svg = new SVGContainer(svgEl);
    svg.attr({
        xmlns: SVGNS$2
    });
    return svg;
}
SVG.xlink = "http://www.w3.org/1999/xlink";
if (Environment.isBrowser() && typeof window.SVG === "undefined") {
    window.SVG = SVG;
}
if (Environment.isBrowser()) {
    if (typeof window.SVG === "undefined") {
        window.SVG = SVG;
    }
    if (typeof window.Apex === "undefined") {
        window.Apex = {};
    }
} else {
    if ("TURBOPACK compile-time truthy", 1) {
        if (typeof /** @type {any} */ /*TURBOPACK member replacement*/ __turbopack_context__.g.Apex === "undefined") {
            /*TURBOPACK member replacement*/ __turbopack_context__.g.Apex = {};
        }
        if (typeof /** @type {any} */ /*TURBOPACK member replacement*/ __turbopack_context__.g.SVG === "undefined") {
            /*TURBOPACK member replacement*/ __turbopack_context__.g.SVG = SVG;
        }
    }
}
var _InitCtxVariables = /*#__PURE__*/ function() {
    "use strict";
    function _InitCtxVariables(ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, _InitCtxVariables);
        this.ctx = ctx;
        this.w = ctx.w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(_InitCtxVariables, [
        {
            key: "initModules",
            value: function initModules() {
                var _this = this;
                var _this1 = this;
                var _this_ctx_updateHelpers;
                this.ctx.publicMethods = [
                    "updateOptions",
                    "updateSeries",
                    "appendData",
                    "appendSeries",
                    "isSeriesHidden",
                    "highlightSeries",
                    "toggleSeries",
                    "showSeries",
                    "hideSeries",
                    "setLocale",
                    "resetSeries",
                    "zoomX",
                    "toggleDataPointSelection",
                    "dataURI",
                    "exportToCSV",
                    "addXaxisAnnotation",
                    "addYaxisAnnotation",
                    "addPointAnnotation",
                    "clearAnnotations",
                    "removeAnnotation",
                    "drillDown",
                    "drillUp",
                    "drillToRoot",
                    "paper",
                    "getActiveRenderer",
                    "destroy"
                ];
                this.ctx.eventList = [
                    "click",
                    "mousedown",
                    "mousemove",
                    "mouseleave",
                    "touchstart",
                    "touchmove",
                    "touchleave",
                    "mouseup",
                    "touchend",
                    "keydown",
                    "keyup"
                ];
                this.ctx.animations = new Animations(this.w, this.ctx);
                this.ctx.axes = new Axes(this.w, this.ctx);
                this.ctx.core = new Core(this.ctx.el, this.w, this.ctx);
                this.ctx.config = new Config({});
                this.ctx.data = new Data(this.w, {
                    resetGlobals: function resetGlobals() {
                        return _this.ctx.core.resetGlobals();
                    },
                    isMultipleY: function isMultipleY() {
                        return _this.ctx.core.isMultipleY();
                    }
                });
                this.ctx.grid = new Grid(this.w, this.ctx);
                this.ctx.graphics = new Graphics(this.w, this.ctx);
                this.ctx.coreUtils = new CoreUtils(this.w);
                this.ctx.crosshairs = new Crosshairs(this.w);
                this.ctx.events = new Events(this.w, this.ctx);
                this.ctx.fill = new Fill(this.w);
                this.ctx.localization = new Localization(this.w);
                this.ctx.options = new Options();
                this.ctx.responsive = new Responsive(this.w);
                this.ctx.series = new Series(this.w, {
                    // legend may not be registered — guard with ?.
                    toggleDataSeries: function toggleDataSeries() {
                        for(var _len = arguments.length, a = new Array(_len), _key = 0; _key < _len; _key++){
                            a[_key] = arguments[_key];
                        }
                        var _a_legendHelpers;
                        var _a;
                        return (_a = _this1.ctx.legend) == null ? void 0 : (_a_legendHelpers = _a.legendHelpers).toggleDataSeries.apply(_a_legendHelpers, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(a));
                    },
                    revertDefaultAxisMinMax: function revertDefaultAxisMinMax() {
                        return _this.ctx.updateHelpers.revertDefaultAxisMinMax();
                    },
                    updateSeries: function updateSeries() {
                        for(var _len = arguments.length, a = new Array(_len), _key = 0; _key < _len; _key++){
                            a[_key] = arguments[_key];
                        }
                        return (_this_ctx_updateHelpers = _this1.ctx.updateHelpers)._updateSeries.apply(_this_ctx_updateHelpers, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(a));
                    }
                });
                this.ctx.theme = new Theme(this.w);
                this.ctx.formatters = new Formatters(this.w);
                this.ctx.titleSubtitle = new TitleSubtitle(this.w);
                this.ctx.dimensions = new Dimensions(this.w, this.ctx);
                this.ctx.updateHelpers = new UpdateHelpers(this.w, this.ctx);
                this.ctx.rendererController = new RendererController(this.w, this.ctx);
                this.ctx.renderer = this.ctx.rendererController.active;
                var tooltipInstance = new Tooltip(this.w, this.ctx);
                this.w.globals.tooltip = tooltipInstance;
                Object.defineProperty(this.ctx, "tooltip", {
                    get: function get() {
                        return this.w.globals.tooltip;
                    },
                    configurable: true
                });
                this._initOptionalModules();
            }
        },
        {
            /**
   * Instantiate optional feature modules from the registry.
   *
   * Modules that are not registered are set to null on ctx so that call sites
   * can safely use optional-chaining (ctx.tooltip?.drawTooltip(...)).
   *
   * Lazy-getter features (toolbar, zoomPanSelection, keyboardNavigation) are
   * installed as on-demand getters so they are only constructed if accessed,
   * and only if their constructor was registered.
   */ key: "_initOptionalModules",
            value: function _initOptionalModules() {
                var reg = _InitCtxVariables._featureRegistry;
                var w = this.w;
                var ctx = this.ctx;
                var ExportsCtor = reg.get("exports");
                ctx.exports = ExportsCtor ? new ExportsCtor(w, ctx) : null;
                var LegendCtor = reg.get("legend");
                ctx.legend = LegendCtor ? new LegendCtor(w, ctx) : null;
                var MorphCtor = reg.get("morphTypeChange");
                ctx.morphTypeChange = MorphCtor ? new MorphCtor(w, ctx) : null;
                var DrilldownCtor = reg.get("drilldown");
                ctx.drilldown = DrilldownCtor ? new DrilldownCtor(w, ctx) : null;
                var PerspectivesCtor = reg.get("perspectives");
                ctx.perspectives = PerspectivesCtor ? new PerspectivesCtor(w, ctx) : null;
                var StoryboardCtor = reg.get("storyboard");
                ctx.storyboard = StoryboardCtor ? new StoryboardCtor(w, ctx) : null;
                var HistoryCtor = reg.get("history");
                ctx.history = HistoryCtor ? new HistoryCtor(w, ctx) : null;
                var LinkedViewsCtor = reg.get("linkedViews");
                ctx.linkedViews = LinkedViewsCtor ? new LinkedViewsCtor(w, ctx) : null;
                var InkCtor = reg.get("ink");
                ctx.ink = InkCtor ? new InkCtor(w, ctx) : null;
                var MeasureCtor = reg.get("measure");
                ctx.measure = MeasureCtor ? new MeasureCtor(w, ctx) : null;
                var ContextMenuCtor = reg.get("contextMenu");
                ctx.contextMenu = ContextMenuCtor ? new ContextMenuCtor(w, ctx) : null;
                var WeaveCtor = reg.get("weave");
                ctx.weave = WeaveCtor ? new WeaveCtor(w, ctx) : null;
                var OSThemeCtor = reg.get("osThemeWatcher");
                ctx.osThemeWatcher = OSThemeCtor ? new OSThemeCtor(w, ctx) : null;
                var ToolbarCtor = reg.get("toolbar");
                Object.defineProperty(ctx, "toolbar", {
                    get: function get() {
                        var _a;
                        if (!this._toolbar && ToolbarCtor) this._toolbar = new ToolbarCtor(w, this);
                        return (_a = this._toolbar) != null ? _a : null;
                    },
                    configurable: true
                });
                var ZoomPanCtor = reg.get("zoomPanSelection");
                Object.defineProperty(ctx, "zoomPanSelection", {
                    get: function get() {
                        var _a;
                        if (!this._zoomPanSelection && ZoomPanCtor) this._zoomPanSelection = new ZoomPanCtor(w, this);
                        return (_a = this._zoomPanSelection) != null ? _a : null;
                    },
                    configurable: true
                });
                var KeyboardCtor = reg.get("keyboardNavigation");
                Object.defineProperty(ctx, "keyboardNavigation", {
                    get: function get() {
                        var _a;
                        if (!this._keyboardNavigation && KeyboardCtor) this._keyboardNavigation = new KeyboardCtor(w, this);
                        return (_a = this._keyboardNavigation) != null ? _a : null;
                    },
                    configurable: true
                });
            }
        }
    ], [
        {
            key: "registerFeatures",
            value: /**
   * Register one or more optional feature modules.
   *
   * @param {Record<string, new (w: object, ctx: object) => unknown>} featureMap
   *   Plain object mapping ctx property name → constructor.
   *
   * Example (called from src/features/legend.js):
   *   InitCtxVariables.registerFeatures({ legend: Legend })
   */ function registerFeatures(featureMap) {
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = Object.entries(featureMap)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var _step_value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(_step.value, 2), key = _step_value[0], Ctor = _step_value[1];
                        _InitCtxVariables._featureRegistry.set(key, Ctor);
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
            }
        }
    ]);
    return _InitCtxVariables;
}();
/**
 * Registry of optional feature modules.
 *
 * Populated by ApexCharts.registerFeatures() (called from feature entry
 * files such as src/features/legend.js). Keys match the ctx property name
 * the module is stored under (e.g. 'legend', 'exports').
 *
 * Core modules that every chart needs are NOT in this registry — they are
 * always instantiated unconditionally in initModules().
 */ __publicField(_InitCtxVariables, "_featureRegistry", /* @__PURE__ */ new Map());
var InitCtxVariables = _InitCtxVariables;
var Destroy = /*#__PURE__*/ function() {
    "use strict";
    function Destroy(ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Destroy);
        this.ctx = ctx;
        this.w = ctx.w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Destroy, [
        {
            /**
   * @param {{ isUpdating: boolean }} opts
   */ key: "clear",
            value: function clear(param) {
                var isUpdating = param.isUpdating;
                var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
                (_a = this.ctx.weave) == null ? void 0 : _a.teardown(isUpdating);
                if (!isUpdating) {
                    this.w.globals.isDestroyed = true;
                }
                if (this.ctx._zoomPanSelection) {
                    this.ctx._zoomPanSelection.destroy();
                }
                if (this.ctx._toolbar) {
                    this.ctx._toolbar.destroy();
                }
                if (this.w.globals.resizeObserver && typeof this.w.globals.resizeObserver.disconnect === "function") {
                    this.w.globals.resizeObserver.disconnect();
                    this.w.globals.resizeObserver = null;
                }
                PerformanceCache.invalidateAll(this.w);
                if (isUpdating) {
                    this.ctx._zoomPanSelection = null;
                    this.ctx._toolbar = null;
                    (_b = this.ctx._keyboardNavigation) == null ? void 0 : _b.destroy();
                    this.ctx._keyboardNavigation = null;
                } else {
                    (_c = this.ctx.perspectives) == null ? void 0 : _c.teardown();
                    this.ctx.perspectives = null;
                    (_d = this.ctx.storyboard) == null ? void 0 : _d.teardown();
                    this.ctx.storyboard = null;
                    (_e = this.ctx.history) == null ? void 0 : _e.teardown();
                    this.ctx.history = null;
                    (_f = this.ctx.linkedViews) == null ? void 0 : _f.teardown();
                    this.ctx.linkedViews = null;
                    (_g = this.ctx.ink) == null ? void 0 : _g.teardown();
                    this.ctx.ink = null;
                    (_h = this.ctx.measure) == null ? void 0 : _h.teardown();
                    this.ctx.measure = null;
                    (_i = this.ctx.contextMenu) == null ? void 0 : _i.teardown();
                    this.ctx.contextMenu = null;
                    (_j = this.ctx.osThemeWatcher) == null ? void 0 : _j.teardown();
                    this.ctx.osThemeWatcher = null;
                    this.ctx.weave = null;
                    (_l = (_k = this.ctx.rendererController) == null ? void 0 : _k.teardown) == null ? void 0 : _l.call(_k);
                    this.ctx.rendererController = null;
                    this.ctx.renderer = null;
                    this.ctx.drilldown = null;
                    this.ctx.morphTypeChange = null;
                    this.ctx.exports = null;
                    this.ctx.animations = null;
                    this.ctx.axes = null;
                    this.ctx.annotations = null;
                    this.ctx.core = null;
                    this.ctx.data = null;
                    this.ctx.grid = null;
                    this.ctx.series = null;
                    this.ctx.responsive = null;
                    this.ctx.theme = null;
                    this.ctx.formatters = null;
                    this.ctx.titleSubtitle = null;
                    this.ctx.legend = null;
                    this.ctx.dimensions = null;
                    this.ctx.options = null;
                    this.ctx.crosshairs = null;
                    this.ctx._zoomPanSelection = null;
                    this.ctx.updateHelpers = null;
                    this.ctx._toolbar = null;
                    this.ctx.localization = null;
                    this.ctx._keyboardNavigation = null;
                    this.ctx.w.globals.tooltip = null;
                }
                this.clearDomElements({
                    isUpdating: isUpdating
                });
            }
        },
        {
            /**
   * @param {any} draw
   */ key: "killSVG",
            value: function killSVG(draw) {
                draw.each(/** @this {any} */ function() {
                    this.removeClass("*");
                    this.off();
                }, true);
                draw.clear();
            }
        },
        {
            /**
   * @param {{ isUpdating: boolean }} opts
   */ key: "clearDomElements",
            value: function clearDomElements(param) {
                var _this = this;
                var isUpdating = param.isUpdating;
                var domEls = /** @type {any} */ this.w.dom;
                if (Environment.isBrowser() && domEls.Paper) {
                    var elSVG = domEls.Paper.node;
                    if (elSVG.parentNode && elSVG.parentNode.parentNode && !isUpdating) {
                        elSVG.parentNode.parentNode.style.minHeight = "unset";
                    }
                    var baseEl = domEls.baseEl;
                    if (baseEl) {
                        this.ctx.eventList.forEach(function(event) {
                            baseEl.removeEventListener(event, _this.ctx.events.documentEvent);
                        });
                    }
                    if (this.ctx.el !== null) {
                        while(this.ctx.el.firstChild){
                            this.ctx.el.removeChild(this.ctx.el.firstChild);
                        }
                    }
                    this.killSVG(domEls.Paper);
                    domEls.Paper.remove();
                }
                domEls.Paper = null;
                domEls.elWrap = null;
                domEls.elGraphical = null;
                domEls.elLegendWrap = null;
                domEls.elLegendForeign = null;
                domEls.baseEl = null;
                domEls.elGridRect = null;
                domEls.elGridRectMask = null;
                domEls.elGridRectBarMask = null;
                domEls.elGridRectMarkerMask = null;
                domEls.elForecastMask = null;
                domEls.elNonForecastMask = null;
                domEls.elDefs = null;
            }
        }
    ]);
    return Destroy;
}();
var REGISTRY_KEY$1 = "__apexcharts_plugins__";
function getRegistry() {
    var g = /** @type {any} */ globalThis;
    if (!g[REGISTRY_KEY$1]) g[REGISTRY_KEY$1] = {};
    return g[REGISTRY_KEY$1];
}
function registerPlugin(def) {
    if (!def || typeof def.name !== "string" || typeof def.setup !== "function") {
        console.error("[apexcharts] registerPlugin: a plugin needs a { name, setup } shape.");
        return;
    }
    getRegistry()[def.name] = def;
}
function getPlugin(name2) {
    return getRegistry()[name2] || null;
}
function unregisterPlugin(name2) {
    delete getRegistry()[name2];
}
var ros = /* @__PURE__ */ new WeakMap();
function addResizeListener(el, fn) {
    if (Environment.isSSR()) return;
    var called = false;
    if (el.nodeType !== Node.DOCUMENT_FRAGMENT_NODE) {
        var elRect = el.getBoundingClientRect();
        if (el.style.display === "none" || elRect.width === 0) {
            called = true;
        }
    }
    var ro = new ResizeObserver(function(r) {
        if (called) {
            fn.call(el, r);
        }
        called = true;
    });
    if (el.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
        Array.from(el.children).forEach(function(c) {
            return ro.observe(c);
        });
    } else {
        ro.observe(el);
    }
    ros.set(fn, ro);
}
function removeResizeListener(el, fn) {
    if (Environment.isSSR()) return;
    var ro = ros.get(fn);
    if (ro) {
        ro.disconnect();
        ros.delete(fn);
    }
}
var apexCSS = '@keyframes opaque {\n  0% {\n    opacity: 0\n  }\n\n  to {\n    opacity: 1\n  }\n}\n\n@keyframes resizeanim {\n\n  0%,\n  to {\n    opacity: 0\n  }\n}\n\n.apexcharts-canvas {\n  position: relative;\n  direction: ltr !important;\n  user-select: none;\n  /* Focus indicator colour. Themes override below. */\n  --apexcharts-focus-color: #008FFB;\n}\n\n/* Dark theme & high-contrast: brighter focus colour for sufficient contrast. */\n.apexcharts-canvas .apexcharts-theme-dark,\n.apexcharts-theme-dark.apexcharts-canvas {\n  --apexcharts-focus-color: #FFD500;\n}\n.apexcharts-canvas.apexcharts-high-contrast,\n.apexcharts-high-contrast.apexcharts-canvas {\n  --apexcharts-focus-color: #FFFF00;\n}\n\n/* Visually-hidden aria-live status region (WCAG 4.1.3 Status Messages). */\n.apexcharts-sr-status {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  padding: 0;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0, 0, 0, 0);\n  white-space: nowrap;\n  border: 0;\n}\n\n/* Respect OS-level reduced-motion preference (WCAG 2.3.3). */\n@media (prefers-reduced-motion: reduce) {\n  .apexcharts-canvas *,\n  .apexcharts-canvas *::before,\n  .apexcharts-canvas *::after {\n    animation-duration: 0.01ms !important;\n    animation-iteration-count: 1 !important;\n    transition-duration: 0.01ms !important;\n  }\n}\n\n.apexcharts-canvas ::-webkit-scrollbar {\n  -webkit-appearance: none;\n  width: 6px\n}\n\n.apexcharts-canvas ::-webkit-scrollbar-thumb {\n  border-radius: 4px;\n  background-color: rgba(0, 0, 0, .5);\n  box-shadow: 0 0 1px rgba(255, 255, 255, .5);\n  -webkit-box-shadow: 0 0 1px rgba(255, 255, 255, .5)\n}\n\n.apexcharts-inner {\n  position: relative\n}\n\n.apexcharts-text tspan {\n  font-family: inherit\n}\n\nrect.legend-mouseover-inactive,\n.legend-mouseover-inactive rect,\n.legend-mouseover-inactive path,\n.legend-mouseover-inactive circle,\n.legend-mouseover-inactive line,\n.legend-mouseover-inactive text.apexcharts-yaxis-title-text,\n.legend-mouseover-inactive text.apexcharts-yaxis-label {\n  transition: .15s ease all;\n  opacity: .2\n}\n\n/* Linked Views (#4): per-mark crossfilter dim. Applied to individual data\n   marks (not whole series) whose x is outside the brushed range. Opacity is\n   overridable per chart via the --apx-cf-dim custom property. */\n.apexcharts-crossfilter-dimmed {\n  transition: opacity .25s ease;\n  opacity: var(--apx-cf-dim, .2)\n}\n\n/* Linked Views (#4): default styling for the built-in crossfilter data table\n   (cf.dataTable). Deliberately light so host styles can override. */\n.apexcharts-cf-table {\n  border-collapse: collapse;\n  width: 100%;\n  font-size: 13px;\n}\n.apexcharts-cf-table caption {\n  caption-side: bottom;\n  text-align: right;\n  padding: 6px 2px;\n  font-size: 12px;\n  opacity: .7\n}\n.apexcharts-cf-table th,\n.apexcharts-cf-table td {\n  padding: 6px 10px;\n  text-align: left;\n  border-bottom: 1px solid rgba(0, 0, 0, .08)\n}\n.apexcharts-cf-table th {\n  font-weight: 600;\n  border-bottom-width: 2px\n}\n.apexcharts-cf-table tbody tr:hover {\n  background: rgba(99, 102, 241, .06)\n}\n\n/* Measure ruler (#18): measure / delta ruler.\n   Theme via these classes or the --apx-measure-* custom properties below\n   (config `chart.measure.colors` overrides both). The ruler group also carries\n   a direction class: apexcharts-measure-up | -down | -flat.\n   Element classes:\n     .apexcharts-measure-band     shaded span band\n     .apexcharts-measure-vline    vertical guide lines\n     .apexcharts-measure-line     free-mode diagonal line\n     .apexcharts-measure-label-bg readout box     .apexcharts-measure-label text\n   Colors are applied as SVG presentation attributes, so any rule you write on\n   these classes overrides them. */\n.apexcharts-canvas {\n  --apx-measure-up: #16a34a;\n  --apx-measure-down: #dc2626;\n  --apx-measure-neutral: #64748b;\n  --apx-measure-guide: #94a3b8;\n}\n.apexcharts-measure-capture {\n  cursor: crosshair;\n}\n\n/* Radial Actions (#chrome): right-click context menu. Theme via these classes\n   or the --apx-menu-* custom properties. */\n.apexcharts-canvas {\n  --apx-menu-bg: #ffffff;\n  --apx-menu-fg: #1e293b;\n  --apx-menu-border: #e2e8f0;\n  --apx-menu-hover: #f1f5f9;\n  --apx-menu-shadow: rgba(15, 23, 42, 0.18);\n}\n.apexcharts-context-menu {\n  min-width: 168px;\n  padding: 4px;\n  border-radius: 8px;\n  background: var(--apx-menu-bg);\n  border: 1px solid var(--apx-menu-border);\n  box-shadow: 0 6px 22px var(--apx-menu-shadow);\n  font-family: Helvetica, Arial, sans-serif;\n  font-size: 13px;\n  z-index: 20;\n  user-select: none;\n}\n.apexcharts-context-menu-item {\n  display: block;\n  width: 100%;\n  box-sizing: border-box;\n  text-align: left;\n  padding: 7px 12px;\n  border: 0;\n  border-radius: 5px;\n  background: transparent;\n  color: var(--apx-menu-fg);\n  font: inherit;\n  cursor: pointer;\n}\n.apexcharts-context-menu-item:hover,\n.apexcharts-context-menu-item--active {\n  background: var(--apx-menu-hover);\n}\n.apexcharts-context-menu-item:focus {\n  outline: none;\n}\n\n/* Ink Layer (#7): the floating note editor card, opened by clicking an\n   ink-managed annotation. Theme via these classes or the --apx-ink-* vars. */\n.apexcharts-canvas {\n  --apx-ink-card-bg: #ffffff;\n  --apx-ink-card-fg: #1e293b;\n  --apx-ink-card-border: #e2e8f0;\n  --apx-ink-card-hover: #f1f5f9;\n  --apx-ink-card-accent: #6366f1;\n  --apx-ink-card-shadow: rgba(15, 23, 42, 0.18);\n}\n.apexcharts-ink-card {\n  position: absolute;\n  z-index: 25;\n  display: flex;\n  flex-direction: column;\n  gap: 6px;\n  padding: 8px;\n  border-radius: 8px;\n  background: var(--apx-ink-card-bg);\n  border: 1px solid var(--apx-ink-card-border);\n  box-shadow: 0 6px 22px var(--apx-ink-card-shadow);\n  font-family: Helvetica, Arial, sans-serif;\n  font-size: 12px;\n  color: var(--apx-ink-card-fg);\n  user-select: none;\n}\n.apexcharts-ink-card-row {\n  display: flex;\n  align-items: center;\n  gap: 4px;\n}\n.apexcharts-ink-card input.apexcharts-ink-editor {\n  flex: 1 1 auto;\n  width: 150px;\n  min-width: 0;\n  box-sizing: border-box;\n  padding: 4px 6px;\n  font: inherit;\n  color: inherit;\n  background: transparent;\n  border: 1px solid var(--apx-ink-card-border);\n  border-radius: 5px;\n}\n.apexcharts-ink-card input.apexcharts-ink-editor:focus {\n  outline: none;\n  border-color: var(--apx-ink-card-accent);\n}\n.apexcharts-ink-btn {\n  flex: 0 0 auto;\n  width: 24px;\n  height: 24px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  padding: 0;\n  border: 0;\n  border-radius: 5px;\n  background: transparent;\n  color: inherit;\n  font: inherit;\n  font-size: 12px;\n  line-height: 1;\n  cursor: pointer;\n}\n.apexcharts-ink-btn:hover,\n.apexcharts-ink-btn--active {\n  background: var(--apx-ink-card-hover);\n}\n.apexcharts-ink-btn:focus-visible,\n.apexcharts-ink-swatch:focus-visible {\n  outline: 2px solid var(--apx-ink-card-accent);\n  outline-offset: 1px;\n}\n.apexcharts-ink-btn--bold {\n  font-weight: 700;\n}\n.apexcharts-ink-btn--delete:hover {\n  color: #dc2626;\n}\n.apexcharts-ink-swatch {\n  flex: 0 0 auto;\n  width: 16px;\n  height: 16px;\n  padding: 0;\n  border: 1px solid rgba(100, 116, 139, 0.45);\n  border-radius: 50%;\n  cursor: pointer;\n}\n.apexcharts-ink-swatch--active {\n  box-shadow:\n    0 0 0 2px var(--apx-ink-card-bg),\n    0 0 0 4px var(--apx-ink-card-accent);\n}\n.apexcharts-ink-sep {\n  flex: 0 0 auto;\n  width: 1px;\n  height: 16px;\n  margin: 0 2px;\n  background: var(--apx-ink-card-border);\n}\n.apexcharts-ink-cardlabel {\n  flex: 0 0 auto;\n  font-size: 10px;\n  letter-spacing: 0.4px;\n  text-transform: uppercase;\n  opacity: 0.65;\n  margin-right: 2px;\n}\n.apexcharts-ink-marker-size {\n  flex: 0 0 auto;\n  min-width: 16px;\n  text-align: center;\n  font-variant-numeric: tabular-nums;\n}\n\n.apexcharts-legend-text {\n  padding-left: 15px;\n  margin-left: -15px;\n}\n\n.apexcharts-legend-series[role="button"]:focus {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: 2px;\n}\n\n.apexcharts-legend-series[role="button"]:focus:not(:focus-visible) {\n  outline: none;\n}\n\n.apexcharts-legend-series[role="button"]:focus-visible {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: 2px;\n}\n\n.apexcharts-series-collapsed {\n  opacity: 0\n}\n\n.apexcharts-canvas svg:focus:not(:focus-visible) {\n  outline: none;\n}\n\n/* Keyboard navigation focus indicator on SVG data elements.\n   SVG elements don\'t support CSS outline, so we use stroke. */\n.apexcharts-bar-area.apexcharts-keyboard-focused,\n.apexcharts-candlestick-area.apexcharts-keyboard-focused,\n.apexcharts-boxPlot-area.apexcharts-keyboard-focused,\n.apexcharts-rangebar-area.apexcharts-keyboard-focused,\n.apexcharts-pie-area.apexcharts-keyboard-focused,\n.apexcharts-heatmap-rect.apexcharts-keyboard-focused,\n.apexcharts-treemap-rect.apexcharts-keyboard-focused {\n  stroke: var(--apexcharts-focus-color, #008FFB);\n  stroke-width: 2;\n  stroke-opacity: 1;\n}\n\n.apexcharts-tooltip {\n  --apx-tt-bg: #ffffff;\n  --apx-tt-border: rgba(15, 23, 42, 0.06);\n  /* Layered shadow: tight inner contact + soft outer drop. The two Y\n   * offsets are exposed as variables so they flip in sync with the\n   * arrow when the tooltip is below the data point — see the\n   * `[data-placement="bottom"]` rule further down. */\n  --apx-tt-shadow-y-mid: 8px;\n  --apx-tt-shadow-y-far: 16px;\n  --apx-tt-shadow: 0 0 0 1px rgba(15, 23, 42, 0.04), 0 var(--apx-tt-shadow-y-mid) 16px -6px rgba(15, 23, 42, 0.12), 0 var(--apx-tt-shadow-y-far) 36px -12px rgba(15, 23, 42, 0.18);\n  --apx-tt-arrow-bg: var(--apx-tt-bg);\n  /* Two stacked drop-shadows: the first is a tight contact halo for\n   * edge definition against light chart backgrounds; the second is a\n   * softer directional drop that lifts the arrow off the surface.\n   * `--apx-tt-arrow-drop-y` is the Y offset of the directional drop;\n   * a per-placement rule below flips it to negative when the tooltip\n   * is below the data point (arrow on top) so the shadow always\n   * casts outward instead of into the tooltip body. */\n  --apx-tt-arrow-drop-y: 2px;\n  --apx-tt-arrow-shadow: drop-shadow(0 0 0.5px rgba(15, 23, 42, 0.2)) drop-shadow(0 var(--apx-tt-arrow-drop-y) 4px rgba(15, 23, 42, 0.2));\n  --apx-tt-color: #0f172a;\n  --apx-tt-color-muted: rgba(15, 23, 42, 0.55);\n  border-radius: 8px;\n  background: var(--apx-tt-bg);\n  border: 1px solid var(--apx-tt-border);\n  box-shadow: var(--apx-tt-shadow);\n  color: var(--apx-tt-color);\n  cursor: default;\n  font-size: 13px;\n  left: 0;\n  top: 0;\n  opacity: 0;\n  pointer-events: none;\n  position: absolute;\n  display: flex;\n  flex-direction: column;\n  padding: 2px 0;\n  white-space: nowrap;\n  z-index: 12;\n  transition: opacity .12s ease\n}\n\n/* While the tooltip is visible, smoothly animate position changes\n * between data points. Kept short (160 ms) and ease-out so it stays\n * responsive — too long would feel laggy when sweeping across many\n * points fast. The position transition is only attached after the\n * first paint (Position.applyTooltipPosition flips `data-positioned`\n * once the tooltip has been placed) so the *first* show doesn\'t slide\n * the tooltip in from the previously-stale (0,0) coordinates. */\n.apexcharts-tooltip.apexcharts-active {\n  opacity: 1;\n  transition: opacity .12s ease\n}\n.apexcharts-tooltip.apexcharts-active[data-positioned="true"] {\n  transition: opacity .12s ease, left .16s ease-out, top .16s ease-out\n}\n\n.apexcharts-tooltip.apexcharts-theme-light {\n  /* defaults already set above; class kept for backward-compat selectors */\n}\n\n.apexcharts-tooltip.apexcharts-theme-dark {\n  --apx-tt-bg: #1c1c1f;\n  --apx-tt-border: rgba(255, 255, 255, 0.08);\n  --apx-tt-shadow: 0 0 0 1px rgba(0, 0, 0, 0.4), 0 var(--apx-tt-shadow-y-mid) 16px -6px rgba(0, 0, 0, 0.45), 0 var(--apx-tt-shadow-y-far) 36px -12px rgba(0, 0, 0, 0.55);\n  --apx-tt-arrow-shadow: drop-shadow(0 0 0.5px rgba(0, 0, 0, 0.55)) drop-shadow(0 var(--apx-tt-arrow-drop-y) 4px rgba(0, 0, 0, 0.45));\n  --apx-tt-color: #f3f4f6;\n  --apx-tt-color-muted: rgba(243, 244, 246, 0.55);\n}\n\n.apexcharts-tooltip * {\n  font-family: inherit\n}\n\n/* Point-annotation hover tooltip (apexcharts/apexcharts.js#2424). Reuses the\n * glass body/border/shadow from `.apexcharts-tooltip` but holds free-form\n * content, so it needs its own padding, wrapping and a sane max width. */\n.apexcharts-tooltip.apexcharts-annotation-tooltip {\n  padding: 6px 10px;\n  max-width: 240px;\n  white-space: normal;\n  line-height: 1.4;\n  pointer-events: none;\n  z-index: 13\n}\n\n.apexcharts-tooltip-title {\n  padding: 8px 12px 4px;\n  font-size: 12px;\n  font-weight: 600;\n  letter-spacing: 0.01em;\n  color: var(--apx-tt-color-muted);\n  background: transparent;\n  border-bottom: none;\n  margin-bottom: 0\n}\n\n.apexcharts-tooltip.apexcharts-theme-light .apexcharts-tooltip-title,\n.apexcharts-tooltip.apexcharts-theme-dark .apexcharts-tooltip-title {\n  background: transparent;\n  border-bottom: none\n}\n\n/* `fillSeriesColor`: each series-group already paints itself with the\n * series colour. Drop the glass body entirely (transparent bg, no\n * border, no backdrop-filter, no padding) and clip the coloured\n * series-group(s) to the tooltip\'s rounded corners so they fill the\n * shell edge-to-edge. Text inside the coloured group is forced to\n * white for contrast. */\n.apexcharts-tooltip.apexcharts-tooltip-fill-series {\n  background: transparent;\n  -webkit-backdrop-filter: none;\n  backdrop-filter: none;\n  border: none;\n  padding: 0;\n  overflow: hidden;\n  color: #fff\n}\n\n.apexcharts-tooltip.apexcharts-tooltip-fill-series .apexcharts-tooltip-title {\n  background: rgba(0, 0, 0, 0.22);\n  color: #fff;\n  opacity: 1;\n  padding: 6px 12px\n}\n\n.apexcharts-tooltip.apexcharts-tooltip-fill-series .apexcharts-tooltip-series-group {\n  color: #fff\n}\n\n/* Arrow connector — sits *entirely outside* the tooltip body. Shares\n * the body\'s solid fill so it reads as a single shape. `filter:\n * drop-shadow` traces the clipped triangle outline (a regular\n * `box-shadow` would be erased by the `clip-path`). */\n.apexcharts-tooltip-arrow {\n  position: absolute;\n  width: 7px;\n  height: 14px;\n  background: var(--apx-tt-arrow-bg);\n  /* The variable already contains the full `drop-shadow(...) ...` filter\n   * chain (stacked shadows) so it\'s applied raw. */\n  -webkit-filter: var(--apx-tt-arrow-shadow);\n  filter: var(--apx-tt-arrow-shadow);\n  pointer-events: none;\n  top: calc(var(--apx-tt-arrow-y, 50%) - 7px)\n}\n\n.apexcharts-tooltip[data-placement="right"] .apexcharts-tooltip-arrow {\n  left: -7px;\n  clip-path: polygon(0 50%, 100% 0, 100% 100%)\n}\n\n.apexcharts-tooltip[data-placement="left"] .apexcharts-tooltip-arrow {\n  right: -7px;\n  clip-path: polygon(100% 50%, 0 0, 0 100%)\n}\n\n/* Vertical arrow variants: tooltip is above/below the data point and the\n * arrow points down/up. The base rule above uses `--apx-tt-arrow-y` for\n * left/right placement; for top/bottom we re-orient the rectangle and\n * use `--apx-tt-arrow-x` (set by applyTooltipPosition). */\n.apexcharts-tooltip[data-placement="top"] .apexcharts-tooltip-arrow,\n.apexcharts-tooltip[data-placement="bottom"] .apexcharts-tooltip-arrow {\n  width: 14px;\n  height: 7px;\n  top: auto;\n  left: calc(var(--apx-tt-arrow-x, 50%) - 7px)\n}\n\n.apexcharts-tooltip[data-placement="top"] .apexcharts-tooltip-arrow {\n  bottom: -7px;\n  clip-path: polygon(50% 100%, 0 0, 100% 0)\n}\n\n.apexcharts-tooltip[data-placement="bottom"] .apexcharts-tooltip-arrow {\n  top: -7px;\n  clip-path: polygon(50% 0, 0 100%, 100% 100%)\n}\n\n/* When the tooltip is flipped below the data point (arrow on top\n * pointing up), the default downward-biased shadows leave the top\n * edge of both the body *and* the arrow undefined. Flipping every\n * Y offset to negative casts the entire elevation upward so the\n * shadow falls between the tooltip and the bar above. */\n.apexcharts-tooltip[data-placement="bottom"] {\n  --apx-tt-shadow-y-mid: -8px;\n  --apx-tt-shadow-y-far: -16px;\n  --apx-tt-arrow-drop-y: -2px\n}\n\n.apexcharts-tooltip-text-goals-value,\n.apexcharts-tooltip-text-y-value,\n.apexcharts-tooltip-text-z-value {\n  display: inline-block;\n  margin-left: 5px;\n  font-weight: 600\n}\n\n.apexcharts-tooltip-text-goals-label:empty,\n.apexcharts-tooltip-text-goals-value:empty,\n.apexcharts-tooltip-text-y-label:empty,\n.apexcharts-tooltip-text-y-value:empty,\n.apexcharts-tooltip-text-z-value:empty,\n.apexcharts-tooltip-title:empty {\n  display: none\n}\n\n.apexcharts-tooltip-text-goals-label,\n.apexcharts-tooltip-text-goals-value {\n  padding: 6px 0 5px\n}\n\n.apexcharts-tooltip-goals-group,\n.apexcharts-tooltip-text-goals-label,\n.apexcharts-tooltip-text-goals-value {\n  display: flex\n}\n\n.apexcharts-tooltip-text-goals-label:not(:empty),\n.apexcharts-tooltip-text-goals-value:not(:empty) {\n  margin-top: -6px\n}\n\n.apexcharts-tooltip-marker {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  position: relative;\n  width: 12px;\n  height: 12px;\n  margin-right: 6px;\n  vertical-align: middle;\n  color: inherit;\n}\n\n.apexcharts-tooltip-marker svg {\n  width: 100%;\n  height: 100%;\n  display: block;\n}\n\n.apexcharts-tooltip-series-group {\n  padding: 4px 12px;\n  display: none;\n  gap: 8px;\n  text-align: left;\n  justify-content: left;\n  align-items: center\n}\n\n.apexcharts-tooltip-series-group.apexcharts-active .apexcharts-tooltip-marker {\n  opacity: 1\n}\n\n.apexcharts-tooltip-series-group.apexcharts-active:last-child,\n.apexcharts-tooltip-series-group:last-child {\n  padding-bottom: 8px\n}\n\n.apexcharts-tooltip-y-group {\n  padding: 6px 0 5px\n}\n\n.apexcharts-custom-tooltip,\n.apexcharts-tooltip-box {\n  padding: 4px 8px\n}\n\n.apexcharts-tooltip-boxPlot {\n  display: flex;\n  flex-direction: column-reverse\n}\n\n.apexcharts-tooltip-box>div {\n  margin: 4px 0\n}\n\n.apexcharts-tooltip-box span.value {\n  font-weight: 700\n}\n\n.apexcharts-tooltip-rangebar {\n  padding: 5px 8px\n}\n\n.apexcharts-tooltip-rangebar .category {\n  font-weight: 600;\n  color: #777\n}\n\n.apexcharts-tooltip-rangebar .series-name {\n  font-weight: 700;\n  display: block;\n  margin-bottom: 5px\n}\n\n/* X/Y axis tooltips — small popovers that label the crosshair on the\n * axes. Restyled to match the modern data-tooltip palette: solid white\n * body with a subtle border + soft drop-shadow, smaller font, rounded\n * corners. The arrows still use the CSS border-triangle technique\n * (cheap, crisp at small sizes); their colours flow from CSS variables\n * so light/dark themes only need one override per axis. */\n.apexcharts-xaxistooltip,\n.apexcharts-yaxistooltip {\n  --apx-axt-bg: #ffffff;\n  --apx-axt-border: rgba(15, 23, 42, 0.08);\n  --apx-axt-color: #0f172a;\n  --apx-axt-shadow: 0 4px 12px -4px rgba(15, 23, 42, 0.18), 0 1px 3px -1px rgba(15, 23, 42, 0.12);\n  opacity: 0;\n  pointer-events: none;\n  color: var(--apx-axt-color);\n  font-size: 12px;\n  font-weight: 500;\n  text-align: center;\n  border-radius: 6px;\n  position: absolute;\n  z-index: 10;\n  background: var(--apx-axt-bg);\n  border: 1px solid var(--apx-axt-border);\n  box-shadow: var(--apx-axt-shadow)\n}\n\n.apexcharts-xaxistooltip.apexcharts-theme-dark,\n.apexcharts-yaxistooltip.apexcharts-theme-dark {\n  --apx-axt-bg: #1c1c1f;\n  --apx-axt-border: rgba(255, 255, 255, 0.1);\n  --apx-axt-color: #f3f4f6;\n  --apx-axt-shadow: 0 4px 12px -4px rgba(0, 0, 0, 0.55), 0 1px 3px -1px rgba(0, 0, 0, 0.45)\n}\n\n.apexcharts-xaxistooltip {\n  padding: 4px 8px;\n  transition: .15s ease all\n}\n\n.apexcharts-xaxistooltip:after,\n.apexcharts-xaxistooltip:before {\n  left: 50%;\n  border: solid transparent;\n  content: " ";\n  height: 0;\n  width: 0;\n  position: absolute;\n  pointer-events: none\n}\n\n/* :before paints the 1px border outline of the triangle (slightly larger\n * than :after); :after sits inside and paints the fill — leaves a 1px\n * ring of :before visible at the edges. */\n.apexcharts-xaxistooltip:after {\n  border-color: transparent;\n  border-width: 5px;\n  margin-left: -5px\n}\n\n.apexcharts-xaxistooltip:before {\n  border-color: transparent;\n  border-width: 6px;\n  margin-left: -6px\n}\n\n.apexcharts-xaxistooltip-bottom:after,\n.apexcharts-xaxistooltip-bottom:before {\n  bottom: 100%\n}\n\n.apexcharts-xaxistooltip-top:after,\n.apexcharts-xaxistooltip-top:before {\n  top: 100%\n}\n\n.apexcharts-xaxistooltip-bottom:after {\n  border-bottom-color: var(--apx-axt-bg)\n}\n\n.apexcharts-xaxistooltip-bottom:before {\n  border-bottom-color: var(--apx-axt-border)\n}\n\n.apexcharts-xaxistooltip-top:after {\n  border-top-color: var(--apx-axt-bg)\n}\n\n.apexcharts-xaxistooltip-top:before {\n  border-top-color: var(--apx-axt-border)\n}\n\n.apexcharts-xaxistooltip.apexcharts-active {\n  opacity: 1;\n  transition: .15s ease all\n}\n\n.apexcharts-yaxistooltip {\n  padding: 3px 8px\n}\n\n.apexcharts-yaxistooltip:after,\n.apexcharts-yaxistooltip:before {\n  top: 50%;\n  border: solid transparent;\n  content: " ";\n  height: 0;\n  width: 0;\n  position: absolute;\n  pointer-events: none\n}\n\n.apexcharts-yaxistooltip:after {\n  border-color: transparent;\n  border-width: 5px;\n  margin-top: -5px\n}\n\n.apexcharts-yaxistooltip:before {\n  border-color: transparent;\n  border-width: 6px;\n  margin-top: -6px\n}\n\n.apexcharts-yaxistooltip-left:after,\n.apexcharts-yaxistooltip-left:before {\n  left: 100%\n}\n\n.apexcharts-yaxistooltip-right:after,\n.apexcharts-yaxistooltip-right:before {\n  right: 100%\n}\n\n.apexcharts-yaxistooltip-left:after {\n  border-left-color: var(--apx-axt-bg)\n}\n\n.apexcharts-yaxistooltip-left:before {\n  border-left-color: var(--apx-axt-border)\n}\n\n.apexcharts-yaxistooltip-right:after {\n  border-right-color: var(--apx-axt-bg)\n}\n\n.apexcharts-yaxistooltip-right:before {\n  border-right-color: var(--apx-axt-border)\n}\n\n.apexcharts-yaxistooltip.apexcharts-active {\n  opacity: 1\n}\n\n.apexcharts-yaxistooltip-hidden {\n  display: none\n}\n\n.apexcharts-xcrosshairs,\n.apexcharts-ycrosshairs {\n  pointer-events: none;\n  opacity: 0;\n  transition: .15s ease all\n}\n\n.apexcharts-xcrosshairs.apexcharts-active,\n.apexcharts-ycrosshairs.apexcharts-active {\n  opacity: 1;\n  transition: .15s ease all\n}\n\n.apexcharts-ycrosshairs-hidden {\n  opacity: 0\n}\n\n.apexcharts-selection-rect {\n  cursor: move\n}\n\n.svg_select_shape {\n  stroke-width: 1;\n  stroke-dasharray: 10 10;\n  stroke: black;\n  stroke-opacity: 0.1;\n  pointer-events: none;\n  fill: none;\n}\n\n.svg_select_handle {\n  stroke-width: 3;\n  stroke: black;\n  fill: none;\n}\n\n.svg_select_handle_r {\n  cursor: e-resize;\n}\n\n.svg_select_handle_l {\n  cursor: w-resize;\n}\n\n.apexcharts-svg.apexcharts-zoomable.hovering-zoom {\n  cursor: crosshair\n}\n\n.apexcharts-svg.apexcharts-zoomable.hovering-pan {\n  cursor: move\n}\n\n.apexcharts-menu-icon,\n.apexcharts-measure-icon,\n.apexcharts-pan-icon,\n.apexcharts-reset-icon,\n.apexcharts-selection-icon,\n.apexcharts-toolbar-custom-icon,\n.apexcharts-zoom-icon,\n.apexcharts-zoomin-icon,\n.apexcharts-zoomout-icon {\n  cursor: pointer;\n  /* WCAG 2.5.8 Target Size (Minimum): 24×24 CSS px hit target. */\n  width: 26px;\n  height: 24px;\n  line-height: 24px;\n  color: #6e8192;\n  text-align: center;\n  /* Reset native <button> chrome — these are styled via SVG icons. */\n  padding: 0;\n  margin: 0;\n  background: transparent;\n  border: 0;\n  border-radius: 5px;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  transition: background-color .12s ease, color .12s ease;\n}\n\n.apexcharts-menu-icon svg,\n.apexcharts-measure-icon svg,\n.apexcharts-pan-icon svg,\n.apexcharts-reset-icon svg,\n.apexcharts-selection-icon svg,\n.apexcharts-zoom-icon svg,\n.apexcharts-zoomin-icon svg,\n.apexcharts-zoomout-icon svg {\n  width: 18px;\n  height: 18px;\n  fill: none;\n  stroke: currentColor;\n  stroke-width: 2;\n  stroke-linecap: round;\n  stroke-linejoin: round\n}\n\n.apexcharts-theme-dark .apexcharts-menu-icon,\n.apexcharts-theme-dark .apexcharts-measure-icon,\n.apexcharts-theme-dark .apexcharts-pan-icon,\n.apexcharts-theme-dark .apexcharts-reset-icon,\n.apexcharts-theme-dark .apexcharts-selection-icon,\n.apexcharts-theme-dark .apexcharts-toolbar-custom-icon,\n.apexcharts-theme-dark .apexcharts-zoom-icon,\n.apexcharts-theme-dark .apexcharts-zoomin-icon,\n.apexcharts-theme-dark .apexcharts-zoomout-icon {\n  color: #d4d6dc\n}\n\n.apexcharts-canvas .apexcharts-measure-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-pan-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-reset-zoom-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-selection-icon.apexcharts-selected,\n.apexcharts-canvas .apexcharts-zoom-icon.apexcharts-selected {\n  background: rgba(0, 143, 251, 0.12);\n  color: #008ffb\n}\n\n.apexcharts-theme-light .apexcharts-menu-icon:hover,\n.apexcharts-theme-light .apexcharts-measure-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-pan-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-reset-icon:hover,\n.apexcharts-theme-light .apexcharts-selection-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-zoom-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-light .apexcharts-zoomin-icon:hover,\n.apexcharts-theme-light .apexcharts-zoomout-icon:hover {\n  background: rgba(15, 23, 42, 0.06);\n  color: #1f2937\n}\n\n.apexcharts-theme-dark .apexcharts-menu-icon:hover,\n.apexcharts-theme-dark .apexcharts-measure-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-pan-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-reset-icon:hover,\n.apexcharts-theme-dark .apexcharts-selection-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-zoom-icon:not(.apexcharts-selected):hover,\n.apexcharts-theme-dark .apexcharts-zoomin-icon:hover,\n.apexcharts-theme-dark .apexcharts-zoomout-icon:hover {\n  background: rgba(255, 255, 255, 0.08);\n  color: #fff\n}\n\n.apexcharts-menu-icon,\n.apexcharts-selection-icon {\n  position: relative\n}\n\n.apexcharts-toolbar {\n  position: absolute;\n  z-index: 11;\n  display: inline-flex;\n  align-items: center;\n  gap: 1px;\n  padding: 3px;\n  border-radius: 8px;\n  background: rgba(255, 255, 255, 0.85);\n  backdrop-filter: blur(8px);\n  -webkit-backdrop-filter: blur(8px);\n}\n\n.apexcharts-theme-dark .apexcharts-toolbar {\n  background: rgba(28, 28, 31, 0.82);\n}\n\n.apexcharts-menu {\n  background: rgba(255, 255, 255, 0.95);\n  backdrop-filter: blur(8px);\n  -webkit-backdrop-filter: blur(8px);\n  position: absolute;\n  top: calc(100% + 4px);\n  border: 1px solid rgba(15, 23, 42, 0.08);\n  border-radius: 8px;\n  padding: 4px;\n  right: 0;\n  opacity: 0;\n  min-width: 120px;\n  transition: opacity .15s ease, transform .15s ease;\n  transform: translateY(-2px);\n  pointer-events: none;\n  box-shadow: 0 4px 16px -4px rgba(15, 23, 42, 0.12), 0 2px 4px -1px rgba(15, 23, 42, 0.06)\n}\n\n.apexcharts-menu.apexcharts-menu-open {\n  opacity: 1;\n  transform: translateY(0);\n  pointer-events: all\n}\n\n.apexcharts-menu-item {\n  padding: 6px 9px;\n  font-size: 12px;\n  border-radius: 5px;\n  cursor: pointer\n}\n\n.apexcharts-theme-light .apexcharts-menu-item:hover {\n  background: rgba(15, 23, 42, 0.06)\n}\n\n.apexcharts-theme-dark .apexcharts-menu {\n  background: rgba(28, 28, 31, 0.92);\n  border-color: rgba(255, 255, 255, 0.08);\n  color: #f3f4f6;\n  box-shadow: 0 4px 16px -4px rgba(0, 0, 0, 0.5), 0 2px 4px -1px rgba(0, 0, 0, 0.4)\n}\n\n.apexcharts-theme-dark .apexcharts-menu-item:hover {\n  background: rgba(255, 255, 255, 0.08)\n}\n\n@media screen and (min-width:768px) {\n  .apexcharts-canvas:hover .apexcharts-toolbar {\n    opacity: 1\n  }\n}\n\n/* Toolbar keyboard accessibility: show toolbar when any button inside it is focused */\n.apexcharts-toolbar:focus-within {\n  opacity: 1\n}\n\n/* Focus indicator for toolbar icon buttons */\n.apexcharts-menu-icon:focus-visible,\n.apexcharts-measure-icon:focus-visible,\n.apexcharts-pan-icon:focus-visible,\n.apexcharts-reset-icon:focus-visible,\n.apexcharts-selection-icon:focus-visible,\n.apexcharts-toolbar-custom-icon:focus-visible,\n.apexcharts-zoom-icon:focus-visible,\n.apexcharts-zoomin-icon:focus-visible,\n.apexcharts-zoomout-icon:focus-visible {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: 1px;\n  border-radius: 5px\n}\n\n/* Focus indicator for hamburger menu items */\n.apexcharts-menu-item:focus-visible {\n  outline: 2px solid var(--apexcharts-focus-color, #008FFB);\n  outline-offset: -2px;\n  background: #eee\n}\n\n.apexcharts-canvas .apexcharts-element-hidden,\n.apexcharts-datalabel.apexcharts-element-hidden,\n.apexcharts-hide .apexcharts-series-points {\n  opacity: 0;\n}\n\n.apexcharts-hidden-element-shown {\n  opacity: 1;\n  transition: 0.25s ease all;\n}\n\n.apexcharts-datalabel,\n.apexcharts-datalabel-label,\n.apexcharts-datalabel-value,\n.apexcharts-datalabels,\n.apexcharts-pie-label,\n.apexcharts-pie-name-label,\n.apexcharts-pie-name-label-group,\n.apexcharts-pie-label-connector {\n  cursor: default;\n  pointer-events: none\n}\n\n.apexcharts-pie-label-connector {\n  fill: none\n}\n\n.apexcharts-pie-label-delay {\n  opacity: 0;\n  animation-name: opaque;\n  animation-duration: .3s;\n  animation-fill-mode: forwards;\n  animation-timing-function: ease\n}\n\n.apexcharts-radialbar-label {\n  cursor: pointer;\n}\n\n.apexcharts-annotation-rect,\n.apexcharts-area-series .apexcharts-area,\n.apexcharts-gridline,\n.apexcharts-line,\n.apexcharts-point-annotation-label,\n.apexcharts-radar-series path:not(.apexcharts-marker),\n.apexcharts-radar-series polygon,\n.apexcharts-toolbar svg,\n.apexcharts-tooltip .apexcharts-marker,\n.apexcharts-xaxis-annotation-label,\n.apexcharts-yaxis-annotation-label,\n.apexcharts-zoom-rect,\n.no-pointer-events {\n  pointer-events: none\n}\n\n.apexcharts-tooltip-active .apexcharts-marker {\n  transition: .15s ease all\n}\n\n.apexcharts-radar-series .apexcharts-yaxis {\n  pointer-events: none;\n}\n\n.resize-triggers {\n  animation: 1ms resizeanim;\n  visibility: hidden;\n  opacity: 0;\n  height: 100%;\n  width: 100%;\n  overflow: hidden\n}\n\n.contract-trigger:before,\n.resize-triggers,\n.resize-triggers>div {\n  content: " ";\n  display: block;\n  position: absolute;\n  top: 0;\n  left: 0\n}\n\n.resize-triggers>div {\n  height: 100%;\n  width: 100%;\n  background: #eee;\n  overflow: auto\n}\n\n.contract-trigger:before {\n  overflow: hidden;\n  width: 200%;\n  height: 200%\n}\n\n.apexcharts-bar-goals-markers {\n  pointer-events: none\n}\n\n.apexcharts-bar-shadows {\n  pointer-events: none\n}\n\n.apexcharts-rangebar-goals-markers {\n  pointer-events: none\n}\n\n.apexcharts-drilldown-target {\n  cursor: pointer\n}\n\n.apexcharts-breadcrumb {\n  position: absolute;\n  z-index: 11;\n  display: inline-flex;\n  align-items: center;\n  gap: 2px;\n  font-size: 12px;\n  font-family: inherit;\n  padding: 2px 4px\n}\n\n.apexcharts-breadcrumb-item {\n  background: transparent;\n  border: none;\n  padding: 2px 6px;\n  border-radius: 3px;\n  font: inherit;\n  color: inherit;\n  cursor: pointer;\n  line-height: 1.2\n}\n\n.apexcharts-breadcrumb-item:hover:not(.apexcharts-breadcrumb-current) {\n  background: rgba(0, 0, 0, 0.08)\n}\n\n.apexcharts-breadcrumb-arrow {\n  margin-right: 4px;\n  font-weight: 600;\n  user-select: none\n}\n\n.apexcharts-breadcrumb-current {\n  cursor: default;\n  font-weight: 600;\n  opacity: 0.85\n}\n\n.apexcharts-breadcrumb-separator {\n  opacity: 0.5;\n  user-select: none\n}\n\n.apexcharts-theme-dark .apexcharts-breadcrumb-item:hover:not(.apexcharts-breadcrumb-current) {\n  background: rgba(255, 255, 255, 0.12)\n}\n\n.apexcharts-disable-transitions * {\n  transition: none !important;\n}';
var PUBLIC_KEYS_SPKI_BASE64 = [
    "MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEQIaK9UMD6n0oR/FIy8QdL0uSzKMQlf1BB+tOrji4/WuHsyRNxeDhVykoSsNURozMi1xhmqWvBH1L//xIfugTPA=="
];
var LEGACY_KEYS_ACCEPTED_UNTIL = /* @__PURE__ */ new Date("2027-07-31T00:00:00Z");
var KEY_PREFIX = "APEX-";
var signatureVerdicts = /* @__PURE__ */ new Map();
var verifying = /* @__PURE__ */ new Set();
var listeners = /* @__PURE__ */ new Set();
var warnedUnverifiable = false;
function base64Decode(encoded) {
    if (typeof atob === "function") return atob(encoded);
    if (typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"] !== "undefined") {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(encoded, "base64").toString("binary");
    }
    throw new Error("no base64 decoder available");
}
function base64ToBytes(base64) {
    var normalised = base64.replace(/-/g, "+").replace(/_/g, "/");
    var padded = normalised.padEnd(Math.ceil(normalised.length / 4) * 4, "=");
    var binary = base64Decode(padded);
    var bytes = new Uint8Array(binary.length);
    for(var i = 0; i < binary.length; i++)bytes[i] = binary.charCodeAt(i);
    return bytes;
}
function canonicalPayload(data) {
    var domains = data.domains && data.domains.length > 0 ? data.domains.join(",") : "";
    return "v1|".concat(data.issueDate, "|").concat(data.expiryDate, "|").concat(data.plan, "|").concat(domains);
}
function currentHostname() {
    return typeof window !== "undefined" && window.location ? window.location.hostname : "";
}
function signatureOf(encodedData) {
    try {
        var raw = JSON.parse(base64Decode(encodedData));
        return typeof raw.sig === "string" && raw.sig ? raw.sig : null;
    } catch (e) {
        return null;
    }
}
function notify(result) {
    listeners.forEach(function(listener) {
        try {
            listener(result);
        } catch (e) {}
    });
}
function verifySignature(key, data, signature) {
    return __async(this, null, function() {
        var subtle, accepted, signed, verified, _iteratorNormalCompletion, _didIteratorError, _iteratorError, _iterator, _step, spki, publicKey, e, err;
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__$5f$_generator__as__$5f3e$__["_"])(this, function(_state) {
            switch(_state.label){
                case 0:
                    if (verifying.has(key) || signatureVerdicts.has(key)) return [
                        2
                    ];
                    verifying.add(key);
                    subtle = globalThis.crypto ? globalThis.crypto.subtle : void 0;
                    accepted = LicenseManager.publicKeysSpki;
                    if (!subtle || accepted.length === 0) {
                        verifying.delete(key);
                        if (!warnedUnverifiable) {
                            warnedUnverifiable = true;
                            console.warn(subtle ? "[Apex] No license signing key is configured in this build, so license signatures cannot be verified." : "[Apex] Web Crypto is unavailable (a secure context is required), so the license signature cannot be verified.");
                        }
                        return [
                            2
                        ];
                    }
                    signed = new TextEncoder().encode(canonicalPayload(data));
                    verified = false;
                    _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                    _state.label = 1;
                case 1:
                    _state.trys.push([
                        1,
                        10,
                        11,
                        12
                    ]);
                    _iterator = accepted[Symbol.iterator]();
                    _state.label = 2;
                case 2:
                    if (!!(_iteratorNormalCompletion = (_step = _iterator.next()).done)) return [
                        3,
                        9
                    ];
                    spki = _step.value;
                    _state.label = 3;
                case 3:
                    _state.trys.push([
                        3,
                        6,
                        ,
                        7
                    ]);
                    return [
                        4,
                        subtle.importKey("spki", base64ToBytes(spki), {
                            name: "ECDSA",
                            namedCurve: "P-256"
                        }, false, [
                            "verify"
                        ])
                    ];
                case 4:
                    publicKey = _state.sent();
                    return [
                        4,
                        subtle.verify({
                            hash: "SHA-256",
                            name: "ECDSA"
                        }, publicKey, base64ToBytes(signature), signed)
                    ];
                case 5:
                    verified = _state.sent();
                    return [
                        3,
                        7
                    ];
                case 6:
                    e = _state.sent();
                    verified = false;
                    return [
                        3,
                        7
                    ];
                case 7:
                    if (verified) return [
                        3,
                        9
                    ];
                    _state.label = 8;
                case 8:
                    _iteratorNormalCompletion = true;
                    return [
                        3,
                        2
                    ];
                case 9:
                    return [
                        3,
                        12
                    ];
                case 10:
                    err = _state.sent();
                    _didIteratorError = true;
                    _iteratorError = err;
                    return [
                        3,
                        12
                    ];
                case 11:
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                    return [
                        7
                    ];
                case 12:
                    verifying.delete(key);
                    signatureVerdicts.set(key, verified);
                    if (!verified) {
                        console.error("[Apex] Invalid license key. The license signature does not verify.");
                    }
                    notify(LicenseManager.validateKey(key));
                    return [
                        2
                    ];
            }
        });
    });
}
var LicenseManager = /*#__PURE__*/ function() {
    "use strict";
    function LicenseManager() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, LicenseManager);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(LicenseManager, null, [
        {
            key: "decodeLicenseData",
            value: /**
   * Decode license data from an encoded string (base64 + JSON).
   * @param {string} encodedData
   * @returns {LicenseData | null}
   */ function decodeLicenseData(encodedData) {
                try {
                    var data = JSON.parse(base64Decode(encodedData));
                    if (!data.issueDate || !data.expiryDate || !data.plan) {
                        return null;
                    }
                    return {
                        domains: Array.isArray(data.domains) ? data.domains : void 0,
                        expiryDate: data.expiryDate,
                        issueDate: data.issueDate,
                        plan: data.plan,
                        valid: true
                    };
                } catch (e) {
                    return null;
                }
            }
        },
        {
            key: "getKey",
            value: /**
   * The key set via setLicense (or null). Lets the enforcer resolve the
   * chart.license -> setLicense -> Apex.license precedence.
   * @returns {null | string}
   */ function getKey() {
                return this.licenseKey;
            }
        },
        {
            key: "getLicenseStatus",
            value: /**
   * Validation result for the singleton key.
   * @returns {LicenseValidationResult}
   */ function getLicenseStatus() {
                if (!this.licenseKey) {
                    return {
                        expired: false,
                        valid: false
                    };
                }
                this.validationResult = this.validateKey(this.licenseKey);
                return this.validationResult;
            }
        },
        {
            key: "isKeyValid",
            value: /**
   * Whether a specific key is valid (pure; no singleton mutation).
   * @param {string | undefined | null} key
   * @returns {boolean}
   */ function isKeyValid(key) {
                if (!key) return false;
                return this.validateKey(key).valid;
            }
        },
        {
            key: "isLicenseValid",
            value: /** @returns {boolean} whether the singleton key is valid */ function isLicenseValid() {
                if (!this.licenseKey) return false;
                return this.getLicenseStatus().valid;
            }
        },
        {
            key: "onChange",
            value: /**
   * Subscribe to signature verdicts arriving. Returns an unsubscribe function.
   *
   * Without this a forged key would go unnoticed by any chart that asked once and
   * painted. `LicenseEnforcer` uses it to re-evaluate every live chart.
   *
   * @param {(result: LicenseValidationResult) => void} listener
   * @returns {() => void}
   */ function onChange(listener) {
                listeners.add(listener);
                return function() {
                    listeners.delete(listener);
                };
            }
        },
        {
            key: "setLicense",
            value: /**
   * Set the global (singleton) license key. console.errors when invalid, to
   * match the rest of the family.
   * @param {string} key
   */ function setLicense(key) {
                this.licenseKey = key;
                this.validationResult = this.validateKey(key);
                if (!this.validationResult.valid) {
                    console.error("[Apex] ".concat(this.validationResult.message));
                }
            }
        },
        {
            key: "validateKey",
            value: /**
   * Validate an arbitrary key WITHOUT mutating the singleton. Used to resolve
   * per-chart (`chart.license`) and global (`window.Apex.license`) keys, which
   * bypass setLicense. This is a superset of the family (which keeps
   * validateLicense private); the format and rules are identical.
   *
   * Synchronous by contract, because it runs during render. Signature checking is
   * started here and settles later; see `onChange`.
   *
   * @param {string} key
   * @returns {LicenseValidationResult}
   */ function validateKey(key) {
                try {
                    if (typeof key !== "string" || !key.startsWith(KEY_PREFIX)) {
                        return {
                            expired: false,
                            message: 'Invalid license key format. License key must start with "APEX-".',
                            signatureVerified: false,
                            valid: false
                        };
                    }
                    var encodedData = key.slice(KEY_PREFIX.length);
                    if (!encodedData) {
                        return {
                            expired: false,
                            message: "Invalid license key format. Expected format: APEX-{encoded-data}.",
                            signatureVerified: false,
                            valid: false
                        };
                    }
                    var licenseData = this.decodeLicenseData(encodedData);
                    if (!licenseData) {
                        return {
                            expired: false,
                            message: "Invalid license key. Unable to decode license data.",
                            signatureVerified: false,
                            valid: false
                        };
                    }
                    var signature = signatureOf(encodedData);
                    if (!signature && /* @__PURE__ */ new Date() >= LEGACY_KEYS_ACCEPTED_UNTIL) {
                        return {
                            data: licenseData,
                            expired: false,
                            message: "This license key is in the old unsigned format, which is no longer accepted. Please request a replacement key.",
                            signatureVerified: false,
                            valid: false
                        };
                    }
                    var now = /* @__PURE__ */ new Date();
                    var expiryDate = new Date(licenseData.expiryDate);
                    if (expiryDate < now) {
                        return {
                            data: licenseData,
                            expired: true,
                            message: "License expired on ".concat(licenseData.expiryDate, ". Please renew your license."),
                            signatureVerified: false,
                            valid: false
                        };
                    }
                    if (licenseData.domains && licenseData.domains.length > 0) {
                        var hostname = currentHostname();
                        var allowed = licenseData.domains.some(function(domain) {
                            return hostname === domain || hostname.endsWith(".".concat(domain));
                        });
                        if (!allowed) {
                            return {
                                data: licenseData,
                                expired: false,
                                message: "License is not valid for this domain (".concat(hostname, "). Allowed domains: ").concat(licenseData.domains.join(", "), "."),
                                signatureVerified: false,
                                valid: false
                            };
                        }
                    }
                    if (signature) {
                        var verdict = signatureVerdicts.get(key);
                        if (verdict === false) {
                            return {
                                data: licenseData,
                                expired: false,
                                message: "Invalid license key. The license signature does not verify.",
                                signatureVerified: true,
                                valid: false
                            };
                        }
                        if (verdict === void 0) {
                            void verifySignature(key, licenseData, signature);
                        }
                        return {
                            data: licenseData,
                            expired: false,
                            signatureVerified: verdict === true,
                            valid: true
                        };
                    }
                    return {
                        data: licenseData,
                        expired: false,
                        signatureVerified: false,
                        valid: true
                    };
                } catch (e) {
                    return {
                        expired: false,
                        message: "Invalid license key format or corrupted data.",
                        signatureVerified: false,
                        valid: false
                    };
                }
            }
        },
        {
            key: "_resetSignatureState",
            value: /** Test-only: forget signature verdicts and the one-time warnings. */ function _resetSignatureState() {
                signatureVerdicts.clear();
                verifying.clear();
                warnedUnverifiable = false;
            }
        }
    ]);
    return LicenseManager;
}();
/** @type {null | string} */ __publicField(LicenseManager, "licenseKey", null);
/**
 * Accepted signing keys. Replaced by tests with an ephemeral keypair, since
 * they cannot sign for the production key. Not public API.
 * @type {string[]}
 */ __publicField(LicenseManager, "publicKeysSpki", PUBLIC_KEYS_SPKI_BASE64);
/** @type {LicenseValidationResult | null} */ __publicField(LicenseManager, "validationResult", null);
var WATERMARK_ATTR = "data-apexcharts-watermark";
var WATERMARK_TEXT = "APEXCHARTS";
var CRITICAL_STYLES = {
    position: "absolute",
    top: "0",
    right: "0",
    bottom: "0",
    left: "0",
    pointerEvents: "none",
    userSelect: "none",
    webkitUserSelect: "none",
    msUserSelect: "none",
    zIndex: "10000",
    display: "block",
    visibility: "visible",
    opacity: "1"
};
function createWatermarkPattern() {
    var svg = '\n    <svg xmlns="http://www.w3.org/2000/svg" width="300" height="200">\n      <text\n        x="50%"\n        y="50%"\n        dominant-baseline="middle"\n        text-anchor="middle"\n        font-family="-apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Arial, sans-serif"\n        font-size="18"\n        font-weight="600"\n        fill="rgba(134, 134, 134, 0.1)"\n        transform="rotate(-35, 100, 60)"\n      >'.concat(WATERMARK_TEXT, "</text>\n    </svg>\n  ");
    return 'url("data:image/svg+xml,'.concat(encodeURIComponent(svg.trim()), '")');
}
var Watermark = /*#__PURE__*/ function() {
    "use strict";
    function Watermark() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Watermark);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Watermark, null, [
        {
            key: "applyStyles",
            value: /**
   * Apply the overlay's critical styles + background to a node. Split out so a
   * MutationObserver can restore styles after tampering.
   * @param {HTMLElement} el
   */ function applyStyles(el) {
                Object.assign(el.style, CRITICAL_STYLES, {
                    backgroundImage: createWatermarkPattern(),
                    backgroundRepeat: "repeat"
                });
            }
        },
        {
            key: "add",
            value: /**
   * Add the watermark to a container, reusing the existing node if present (so
   * a style-tamper observer bound to it stays valid across re-renders). No-op
   * when there is no document (SSR) or no container.
   * @param {HTMLElement | null | undefined} container
   * @returns {HTMLElement | null} the watermark node
   */ function add(container) {
                if (!container || typeof document === "undefined") return null;
                var watermark = this.node(container);
                if (!watermark) {
                    watermark = document.createElement("div");
                    watermark.setAttribute(WATERMARK_ATTR, "");
                    container.appendChild(watermark);
                }
                this.applyStyles(watermark);
                if (typeof getComputedStyle === "function" && getComputedStyle(container).position === "static") {
                    container.style.position = "relative";
                }
                return watermark;
            }
        },
        {
            key: "node",
            value: /**
   * @param {HTMLElement | null | undefined} container
   * @returns {HTMLElement | null} the watermark node, if present
   */ function node(container) {
                if (!container) return null;
                return /** @type {HTMLElement | null} */ container.querySelector("[".concat(WATERMARK_ATTR, "]"));
            }
        },
        {
            key: "exists",
            value: /**
   * @param {HTMLElement | null | undefined} container
   * @returns {boolean}
   */ function exists(container) {
                return !!this.node(container);
            }
        },
        {
            key: "remove",
            value: /**
   * Remove the watermark from a container.
   * @param {HTMLElement | null | undefined} container
   */ function remove(container) {
                var existing = this.node(container);
                if (existing) existing.remove();
            }
        }
    ]);
    return Watermark;
}();
__publicField(Watermark, "ATTR", WATERMARK_ATTR);
var PRICING_URL = "https://apexcharts.com/pricing";
var _perspectivesTokenDecoded = false;
var enforced = /* @__PURE__ */ new Set();
function untrackChart(ctx) {
    enforced.delete(ctx);
}
function markPerspectivesTokenDecoded() {
    _perspectivesTokenDecoded = true;
    reevaluateLicenseAcrossCharts();
}
function premiumFeaturesInUse(w, ctx) {
    var chart = w && w.config && w.config.chart || {};
    var used = [];
    if (chart.type === "unit") used.push("unit");
    if (ctx.storyboard && ctx.storyboard._used) used.push("storyboard");
    var link = chart.link;
    if (ctx.linkedViews && link && (link.enabled === true || typeof link.dimension === "function")) {
        used.push("link");
    }
    if (ctx.ink && chart.ink && chart.ink.enabled === true) used.push("ink");
    if (ctx.measure && chart.measure && chart.measure.enabled === true) {
        used.push("measure");
    }
    if (ctx.contextMenu && chart.contextMenu && chart.contextMenu.enabled === true) {
        used.push("context-menu");
    }
    if (ctx.perspectives && (ctx.perspectives._used || _perspectivesTokenDecoded)) {
        used.push("perspectives");
    }
    if (ctx.history && chart.history && chart.history.enabled === true) {
        used.push("history");
    }
    return used;
}
function resolveKey(w) {
    var perChart = w && w.config && w.config.chart && w.config.chart.license;
    if (perChart) return perChart;
    var singleton = LicenseManager.getKey();
    if (singleton) return singleton;
    var apex = Environment.getApex();
    if (apex && apex.license) return apex.license;
    return null;
}
var PREMIUM_PLANS = /* @__PURE__ */ new Set([
    "premium",
    "enterprise"
]);
function licensedForPremium(key) {
    if (!key) return false;
    var result = LicenseManager.validateKey(key);
    if (!result.valid) return false;
    var plan = result.data && result.data.plan;
    return typeof plan === "string" && PREMIUM_PLANS.has(plan.toLowerCase());
}
function reinstateWatermark(ctx, elWrap) {
    var node = Watermark.add(elWrap);
    if (!node || typeof MutationObserver === "undefined") return;
    if (ctx._wmNodeObserver && ctx._wmObservedNode === node) return;
    if (ctx._wmNodeObserver) ctx._wmNodeObserver.disconnect();
    var nodeObs = new MutationObserver(function() {
        var n = Watermark.node(elWrap);
        if (!n) return;
        nodeObs.disconnect();
        Watermark.applyStyles(n);
        nodeObs.takeRecords();
        nodeObs.observe(n, {
            attributes: true,
            attributeFilter: [
                "style"
            ]
        });
    });
    nodeObs.observe(node, {
        attributes: true,
        attributeFilter: [
            "style"
        ]
    });
    ctx._wmNodeObserver = nodeObs;
    ctx._wmObservedNode = node;
}
function addWatermark(ctx, elWrap) {
    reinstateWatermark(ctx, elWrap);
    if (typeof MutationObserver === "undefined" || ctx._wmWrapObserver) return;
    var wrapObs = new MutationObserver(function() {
        if (!Watermark.node(elWrap)) reinstateWatermark(ctx, elWrap);
    });
    wrapObs.observe(elWrap, {
        childList: true
    });
    ctx._wmWrapObserver = wrapObs;
}
function teardownWatermark(ctx, elWrap) {
    if (ctx._wmWrapObserver) {
        ctx._wmWrapObserver.disconnect();
        ctx._wmWrapObserver = null;
    }
    if (ctx._wmNodeObserver) {
        ctx._wmNodeObserver.disconnect();
        ctx._wmNodeObserver = null;
    }
    ctx._wmObservedNode = null;
    var wrap = elWrap || ctx.w && ctx.w.dom && ctx.w.dom.elWrap;
    if (wrap) Watermark.remove(wrap);
}
function notifyTrial(ctx, key, features) {
    if (ctx._premiumLicenseNotified) return;
    ctx._premiumLicenseNotified = true;
    var many = features.length > 1;
    if (!key) {
        console.warn("[ApexCharts] Premium feature".concat(many ? "s" : "", " in use (").concat(features.join(", "), ") without a license. Running in trial mode with a watermark. Get a license: ").concat(PRICING_URL));
        return;
    }
    var result = LicenseManager.validateKey(key);
    if (result.valid) {
        var plan = result.data && result.data.plan || "current";
        console.warn("[ApexCharts] Premium feature".concat(many ? "s" : "", " in use (").concat(features.join(", "), ") require a Premium or Enterprise license; the ").concat(plan, " plan does not include ").concat(many ? "them" : "it", ". Running in trial mode with a watermark. Upgrade: ").concat(PRICING_URL));
        return;
    }
    if (key !== LicenseManager.getKey()) {
        console.error("[Apex] ".concat(result.message));
    }
}
function enforceLicense(w, ctx) {
    try {
        if (!Environment.isBrowser()) return;
        if (w && w.globals && w.globals.isDestroyed) {
            enforced.delete(ctx);
            return;
        }
        var elWrap = w && w.dom && w.dom.elWrap;
        if (!elWrap) return;
        var features = premiumFeaturesInUse(w, ctx);
        if (features.length === 0) {
            enforced.delete(ctx);
            teardownWatermark(ctx, elWrap);
            return;
        }
        enforced.add(ctx);
        var key = resolveKey(w);
        if (licensedForPremium(key)) {
            teardownWatermark(ctx, elWrap);
            return;
        }
        addWatermark(ctx, elWrap);
        notifyTrial(ctx, key, features);
    } catch (e) {}
}
function reevaluateLicenseAcrossCharts() {
    if (!Environment.isBrowser()) return;
    var visited = /* @__PURE__ */ new Set();
    var apex = Environment.getApex();
    var instances = apex && apex._chartInstances;
    if (Array.isArray(instances)) {
        instances.forEach(function(entry) {
            var chart = entry && entry.chart;
            if (chart && chart.w && !chart.w.globals.isDestroyed) {
                visited.add(chart);
                enforceLicense(chart.w, chart);
            }
        });
    }
    Array.from(enforced).forEach(function(ctx) {
        var w = ctx && ctx.w;
        var elWrap = w && w.dom && w.dom.elWrap;
        if (!w || w.globals.isDestroyed || !elWrap || elWrap.isConnected === false) {
            enforced.delete(ctx);
            return;
        }
        if (visited.has(ctx)) return;
        enforceLicense(w, ctx);
    });
}
LicenseManager.onChange(reevaluateLicenseAcrossCharts);
var _ApexCharts = /*#__PURE__*/ function() {
    "use strict";
    function _ApexCharts(el, opts) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, _ApexCharts);
        // Module properties set dynamically by InitCtxVariables.initModules().
        // Declared as typed class fields so @ts-check resolves them throughout the
        // class body without errors. Each field typed as `any` since the modules are
        // plain objects whose specific shapes are not yet typed.
        /** @type {any} */ __publicField(this, "core");
        /** @type {any} */ __publicField(this, "responsive");
        /** @type {any} */ __publicField(this, "axes");
        /** @type {any} */ __publicField(this, "grid");
        /** @type {any} */ __publicField(this, "graphics");
        /** @type {any} */ __publicField(this, "coreUtils");
        /** @type {any} */ __publicField(this, "crosshairs");
        /** @type {any} */ __publicField(this, "events");
        /** @type {any} */ __publicField(this, "fill");
        /** @type {any} */ __publicField(this, "localization");
        /** @type {any} */ __publicField(this, "options");
        /** @type {any} */ __publicField(this, "series");
        /** @type {any} */ __publicField(this, "theme");
        /** @type {any} */ __publicField(this, "formatters");
        /** @type {any} */ __publicField(this, "titleSubtitle");
        /** @type {any} */ __publicField(this, "dimensions");
        /** @type {any} */ __publicField(this, "updateHelpers");
        /** @type {any} */ __publicField(this, "tooltip");
        /** @type {any} */ __publicField(this, "data");
        /** @type {any} */ __publicField(this, "animations");
        /** @type {any} */ __publicField(this, "exports");
        /** @type {any} */ __publicField(this, "legend");
        /** @type {any} */ __publicField(this, "toolbar");
        /** @type {any} */ __publicField(this, "zoomPanSelection");
        /** @type {any} */ __publicField(this, "keyboardNavigation");
        /** @type {any} */ __publicField(this, "annotations");
        /** @type {any} */ __publicField(this, "morphTypeChange");
        /** @type {any} */ __publicField(this, "timeScale");
        /** @type {any} */ __publicField(this, "_keyboardNavigation");
        /** @type {any} */ __publicField(this, "_zoomPanSelection");
        /** @type {any} */ __publicField(this, "windowResizeHandler");
        /** @type {any} */ __publicField(this, "parentResizeHandler");
        /** @type {string[]} */ __publicField(this, "publicMethods", []);
        /** @type {string[]} */ __publicField(this, "eventList", []);
        /** @type {Promise<any> | null} */ __publicField(this, "_renderPromise", null);
        /** @type {any} */ __publicField(this, "config");
        /** @type {any} */ __publicField(this, "perspectives");
        /** @type {any} */ __publicField(this, "storyboard");
        /** @type {any} */ __publicField(this, "history");
        /** @type {any} */ __publicField(this, "linkedViews");
        /** @type {any} */ __publicField(this, "ink");
        /** @type {any} */ __publicField(this, "measure");
        /** @type {any} */ __publicField(this, "contextMenu");
        /** @type {any} */ __publicField(this, "weave");
        /** @type {any} */ __publicField(this, "renderer");
        /** @type {any} */ __publicField(this, "rendererController");
        this.opts = opts;
        this.ctx = this;
        this.w = new Base(opts).init();
        this.el = el;
        this.w.globals.cuid = Utils$1.randomId();
        this.w.globals.chartID = this.w.config.chart.id ? Utils$1.escapeString(this.w.config.chart.id) : this.w.globals.cuid;
        applyAnimationPolicy(this.w);
        var initCtx = new InitCtxVariables(this);
        initCtx.initModules();
        this.lastUpdateOptions = null;
        this._updateStats = {
            fast: 0,
            fastWithAxes: 0,
            full: 0
        };
        this.create = this.create.bind(this);
        if (Environment.isBrowser()) {
            this.windowResizeHandler = this._windowResizeHandler.bind(this);
            this.parentResizeHandler = this._parentResizeCallback.bind(this);
        }
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(_ApexCharts, [
        {
            /**
   * Renders the chart. Must be called once after construction.
   *
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after mount.
   */ key: "render",
            value: function render() {
                var _this = this;
                var _a, _b;
                if (!((_b = (_a = this.w) == null ? void 0 : _a.config) == null ? void 0 : _b.chart)) {
                    return Promise.reject(new Error("ApexCharts: chart configuration is missing or invalid. Ensure the options object includes a `chart` property."));
                }
                if (this._renderPromise) return this._renderPromise;
                var renderPromise = new Promise(function(resolve, reject) {
                    var _a2;
                    if (Utils$1.elementExists(_this.el)) {
                        if (typeof Apex._chartInstances === "undefined") {
                            Apex._chartInstances = [];
                        }
                        if (_this.w.config.chart.id) {
                            Apex._chartInstances.push({
                                id: _this.w.globals.chartID,
                                group: _this.w.config.chart.group,
                                chart: _this
                            });
                        }
                        _this.setLocale(_this.w.config.chart.defaultLocale);
                        var beforeMount = _this.w.config.chart.events.beforeMount;
                        if (typeof beforeMount === "function") {
                            beforeMount(_this, _this.w);
                        }
                        _this.events.fireEvent("beforeMount", [
                            _this,
                            _this.w
                        ]);
                        if (Environment.isBrowser()) {
                            window.addEventListener("resize", _this.windowResizeHandler);
                            addResizeListener(/** @type {HTMLElement} */ _this.el.parentNode, _this.parentResizeHandler);
                            var rootNode = /** @type {any} */ _this.el.getRootNode && _this.el.getRootNode();
                            var inShadowRoot = Utils$1.is("ShadowRoot", rootNode);
                            var doc = _this.el.ownerDocument;
                            var css = inShadowRoot ? rootNode.getElementById("apexcharts-css") : doc.getElementById("apexcharts-css");
                            if (!css) {
                                css = BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "style");
                                css.id = "apexcharts-css";
                                css.textContent = apexCSS;
                                var nonce = ((_a2 = _this.opts.chart) == null ? void 0 : _a2.nonce) || _this.w.config.chart.nonce;
                                if (nonce) {
                                    css.setAttribute("nonce", nonce);
                                }
                                if (inShadowRoot) {
                                    rootNode.prepend(css);
                                } else if (_this.w.config.chart.injectStyleSheet !== false) {
                                    doc.head.appendChild(css);
                                }
                            }
                        }
                        var graphData = _this.create(_this.w.config.series, {});
                        if (!graphData) return resolve(_this);
                        _this.mount(graphData).then(function() {
                            if (typeof _this.w.config.chart.events.mounted === "function") {
                                _this.w.config.chart.events.mounted(_this, _this.w);
                            }
                            _this.events.fireEvent("mounted", [
                                _this,
                                _this.w
                            ]);
                            resolve(graphData);
                        }).catch(function(e) {
                            var _a3, _b2;
                            var enriched = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(e, Error) ? e : new Error(String(e));
                            var err = /** @type {any} */ enriched;
                            err.chartId = (_b2 = (_a3 = _this.w) == null ? void 0 : _a3.globals) == null ? void 0 : _b2.chartID;
                            err.el = _this.el;
                            reject(enriched);
                        });
                    } else {
                        reject(new Error("Element not found"));
                    }
                });
                this._renderPromise = renderPromise;
                renderPromise.catch(function() {
                    if (_this._renderPromise === renderPromise) _this._renderPromise = null;
                });
                return renderPromise;
            }
        },
        {
            /**
   * @param {any[]} ser
   * @param {object} opts
   */ key: "create",
            value: function create(ser, opts) {
                var _this = this;
                var _a, _b, _c, _d, _e, _f;
                var w = this.w;
                if (!this.core) {
                    var initCtx = new InitCtxVariables(this);
                    initCtx.initModules();
                }
                var gl = this.w.globals;
                gl.noData = false;
                gl.animationEnded = false;
                if (!Utils$1.elementExists(this.el)) {
                    gl.animationEnded = true;
                    return null;
                }
                this.responsive.checkResponsiveConfig(opts);
                applyAnimationPolicy(w);
                if (w.config.xaxis.convertedCatToNumeric) {
                    var defaults = new Defaults(w.config);
                    defaults.convertCatToNumericXaxis(w.config, this.ctx);
                }
                this.core.setupElements();
                if (w.config.chart.type === "treemap") {
                    w.config.grid.show = false;
                    w.config.yaxis[0].show = false;
                }
                if (gl.svgWidth === 0) {
                    gl.animationEnded = true;
                    return null;
                }
                var series = ser;
                ser.forEach(function(s, realIndex) {
                    if (s.hidden) {
                        series = _this.legend.legendHelpers.getSeriesAfterCollapsing({
                            realIndex: realIndex
                        });
                    }
                });
                var combo = CoreUtils.checkComboSeries(series, w.config.chart.type);
                gl.comboCharts = combo.comboCharts;
                gl.comboBarCount = combo.comboBarCount;
                var allSeriesAreEmpty = series.every(function(s) {
                    return s.data && s.data.length === 0;
                });
                if (series.length === 0 || allSeriesAreEmpty && gl.collapsedSeries.length < 1) {
                    this.series.handleNoData();
                }
                if (Environment.isBrowser()) {
                    this.events.setupEventHandlers();
                }
                var parsedState = this.data.parseData(series);
                this._writeParsedSeriesData(parsedState.seriesData);
                this._writeParsedRangeData(parsedState.rangeData);
                this._writeParsedCandleData(parsedState.candleData);
                this._writeParsedLabelData(parsedState.labelData);
                this._writeParsedAxisFlags(parsedState.axisFlags);
                (_a = this.rendererController) == null ? void 0 : _a.resolve();
                (_b = this.weave) == null ? void 0 : _b.dispatch("afterParse");
                this.theme.init();
                var markers = new Markers(this.w, this);
                markers.setGlobalMarkerSize();
                this.formatters.setLabelFormatters();
                this.titleSubtitle.draw();
                if (!gl.noData || gl.collapsedSeries.length === w.seriesData.series.length || w.config.legend.showForSingleSeries) {
                    (_c = this.legend) == null ? void 0 : _c.init();
                }
                this.series.hasAllSeriesEqualX();
                if (gl.axisCharts) {
                    this.core.coreCalculations();
                    if (w.config.xaxis.type !== "category") {
                        this.formatters.setLabelFormatters();
                    }
                    if (this.ctx.toolbar) {
                        this.ctx.toolbar.minX = w.globals.minX;
                        this.ctx.toolbar.maxX = w.globals.maxX;
                    }
                }
                this.formatters.heatmapLabelFormatters();
                var coreUtils = new CoreUtils(this.w);
                coreUtils.getLargestMarkerSize();
                var layoutState = this.dimensions.plotCoords();
                this._writeLayoutCoords(layoutState.layout);
                var xyRatios = this.core.xySettings();
                (_d = this.weave) == null ? void 0 : _d.dispatch("afterScales", {
                    xyRatios: xyRatios
                });
                this.grid.createGridMask();
                var elGraph = this.core.plotChartType(series, xyRatios);
                var dataLabels = new DataLabels(this.w, this);
                dataLabels.bringForward();
                if (w.config.dataLabels.background.enabled) {
                    dataLabels.dataLabelsBackground();
                }
                this.core.shiftGraphPosition();
                (_f = (_e = this.legend) == null ? void 0 : _e.heatmapGradientLegend) == null ? void 0 : _f.repositionToPlot();
                if (w.globals.dataPoints > 50) {
                    w.dom.elWrap.classList.add("apexcharts-disable-transitions");
                }
                var dim = {
                    plot: {
                        left: w.layout.translateX,
                        top: w.layout.translateY,
                        width: w.layout.gridWidth,
                        height: w.layout.gridHeight
                    }
                };
                return {
                    elGraph: elGraph,
                    xyRatios: xyRatios,
                    dimensions: dim
                };
            }
        },
        {
            /**
   * @param {any} graphData
   */ key: "mount",
            value: function mount() {
                var _this = this;
                var graphData = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : null;
                var me = this;
                var w = me.w;
                return new Promise(function(resolve, reject) {
                    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
                    if (me.el === null) {
                        return reject(new Error("Not enough data to display or target element not found"));
                    } else if (w.globals.allSeriesCollapsed) {
                        me.series.handleNoData();
                    }
                    me.grid = new Grid(me.w, me);
                    var elgrid = me.grid.drawGrid();
                    var AnnotationsCtor = InitCtxVariables._featureRegistry.get("annotations");
                    me.annotations = AnnotationsCtor ? new AnnotationsCtor(me.w, {
                        theme: me.theme,
                        timeScale: me.timeScale
                    }) : null;
                    (_a = me.annotations) == null ? void 0 : _a.drawImageAnnos();
                    (_b = me.annotations) == null ? void 0 : _b.drawTextAnnos();
                    if (w.config.grid.position === "back") {
                        if (elgrid) {
                            w.dom.elGraphical.add(elgrid.el);
                        }
                        if ((_c = elgrid == null ? void 0 : elgrid.elGridBorders) == null ? void 0 : _c.node) {
                            w.dom.elGraphical.add(elgrid.elGridBorders);
                        }
                    }
                    if (Array.isArray(graphData.elGraph)) {
                        for(var g = 0; g < graphData.elGraph.length; g++){
                            w.dom.elGraphical.add(graphData.elGraph[g]);
                        }
                    } else {
                        w.dom.elGraphical.add(graphData.elGraph);
                    }
                    if (w.config.grid.position === "front") {
                        if (elgrid) {
                            w.dom.elGraphical.add(elgrid.el);
                        }
                        if ((_d = elgrid == null ? void 0 : elgrid.elGridBorders) == null ? void 0 : _d.node) {
                            w.dom.elGraphical.add(elgrid.elGridBorders);
                        }
                    }
                    if (w.config.xaxis.crosshairs.position === "front") {
                        me.crosshairs.drawXCrosshairs();
                    }
                    if (w.config.yaxis[0].crosshairs.position === "front") {
                        me.crosshairs.drawYCrosshairs();
                    }
                    if (w.config.chart.type !== "treemap") {
                        me.axes.drawAxis(w.config.chart.type, elgrid);
                    }
                    var xAxis = new XAxis(_this.w, _this.ctx, elgrid);
                    var yaxis = new YAxis(_this.w, {
                        theme: _this.theme,
                        timeScale: _this.timeScale
                    }, elgrid);
                    if (elgrid !== null) {
                        xAxis.xAxisLabelCorrections();
                        yaxis.setYAxisTextAlignments();
                        w.config.yaxis.map(function(yaxe, index) {
                            if (w.globals.ignoreYAxisIndexes.indexOf(index) === -1) {
                                yaxis.yAxisTitleRotate(index, yaxe.opposite);
                            }
                        });
                    }
                    (_e = me.annotations) == null ? void 0 : _e.drawAxesAnnotations();
                    if (!w.globals.noData) {
                        if (Environment.isBrowser() && w.config.tooltip.enabled && !w.globals.noData) {
                            (_f = me.w.globals.tooltip) == null ? void 0 : _f.drawTooltip(graphData.xyRatios);
                        }
                        if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.keyboard.enabled && w.config.chart.accessibility.keyboard.navigation.enabled) {
                            (_g = me.keyboardNavigation) == null ? void 0 : _g.init();
                        }
                        if (Environment.isBrowser() && w.globals.axisCharts && (w.axisFlags.isXNumeric || /** @type {Record<string,any>} */ w.config.xaxis.convertedCatToNumeric || w.axisFlags.isRangeBar)) {
                            if (w.config.chart.zoom.enabled || w.config.chart.selection && w.config.chart.selection.enabled || // @ts-ignore — chart.pan is an internal toolbar config property
                            w.config.chart.pan && w.config.chart.pan.enabled) {
                                (_h = me.zoomPanSelection) == null ? void 0 : _h.init({
                                    xyRatios: graphData.xyRatios
                                });
                            }
                        } else {
                            var tools = w.config.chart.toolbar.tools;
                            var toolsArr = [
                                "zoom",
                                "zoomin",
                                "zoomout",
                                "selection",
                                "pan",
                                "reset"
                            ];
                            toolsArr.forEach(function(t) {
                                tools[t] = false;
                            });
                        }
                        if (w.config.chart.toolbar.show && !w.globals.allSeriesCollapsed) {
                            (_i = me.toolbar) == null ? void 0 : _i.createToolbar();
                        }
                    }
                    (_j = me.weave) == null ? void 0 : _j.dispatch("draw", {
                        pass: "full",
                        xyRatios: graphData == null ? void 0 : graphData.xyRatios
                    });
                    if (w.globals.memory.methodsToExec.length > 0) {
                        w.globals.memory.methodsToExec.forEach(function(fn) {
                            fn.method(fn.params, false, fn.context);
                        });
                    }
                    if (!w.globals.axisCharts && !w.globals.noData) {
                        me.core.resizeNonAxisCharts();
                    }
                    enforceLicense(w, me);
                    resolve(me);
                });
            }
        },
        {
            /**
   * Destroys the chart instance, removes all DOM elements and event listeners.
   * After calling this, the instance should not be used again.
   */ key: "destroy",
            value: function destroy() {
                var _a;
                this._renderPromise = null;
                if (Environment.isBrowser()) {
                    window.removeEventListener("resize", this.windowResizeHandler);
                    removeResizeListener(/** @type {Element} */ this.el.parentNode, this.parentResizeHandler);
                    clearTimeout((_a = this.w.globals.resizeTimer) != null ? _a : void 0);
                }
                var chartID = this.w.config.chart.id;
                if (chartID && Array.isArray(Apex._chartInstances)) {
                    Apex._chartInstances.forEach(function(c, i) {
                        if (c.id === Utils$1.escapeString(chartID)) {
                            Apex._chartInstances.splice(i, 1);
                        }
                    });
                }
                if (this._keyboardNavigation) {
                    this._keyboardNavigation.destroy();
                }
                teardownWatermark(this);
                untrackChart(this);
                new Destroy(this.ctx).clear({
                    isUpdating: false
                });
            }
        },
        {
            /**
   * Merges new options into the existing config and re-renders the chart.
   *
   * @param {ApexOptions} options - Partial config object merged with the existing config.
   * @param {boolean} [redraw=false] - When true, redraws the chart from scratch instead of animating from previous paths.
   * @param {boolean} [animate=true] - Whether to animate the update.
   * @param {boolean} [updateSyncedCharts=true] - Whether to propagate the update to charts in the same group.
   * @param {boolean} [overwriteInitialConfig=true] - When true, replaces the stored initial config used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */ key: "updateOptions",
            value: function updateOptions(options2) {
                var _this = this;
                var redraw = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false, animate = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true, updateSyncedCharts = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : true, overwriteInitialConfig = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : true;
                var w = this.w;
                w.interact.selection = void 0;
                if (this.lastUpdateOptions) {
                    if (Utils$1.shallowEqual(this.lastUpdateOptions, options2)) {
                        return Promise.resolve(this);
                    }
                    if (options2.series && this.lastUpdateOptions.series && !_ApexCharts._optionsTooBigToCompare(options2)) {
                        if (JSON.stringify(this.lastUpdateOptions.series) === JSON.stringify(options2.series)) {
                            var optionsWithoutSeries = __spreadValues({}, options2);
                            var lastWithoutSeries = __spreadValues({}, this.lastUpdateOptions);
                            delete optionsWithoutSeries.series;
                            delete lastWithoutSeries.series;
                            if (Utils$1.shallowEqual(optionsWithoutSeries, lastWithoutSeries)) {
                                return Promise.resolve(this);
                            }
                        }
                    }
                }
                if (options2.series) {
                    this.data.resetParsingFlags();
                    this.series.resetSeries(false, true, false);
                    if (options2.series.length && options2.series[0].data) {
                        options2.series = options2.series.map(function(s, i) {
                            return _this.updateHelpers._extendSeries(s, i);
                        });
                    }
                    this.updateHelpers.revertDefaultAxisMinMax();
                }
                if (options2.xaxis) {
                    options2 = this.updateHelpers.forceXAxisUpdate(options2);
                }
                if (options2.yaxis) {
                    options2 = this.updateHelpers.forceYAxisUpdate(options2);
                }
                if (w.globals.collapsedSeriesIndices.length > 0) {
                    this.series.clearPreviousPaths();
                }
                if (options2.theme) {
                    options2 = this.theme.updateThemeOptions(options2);
                }
                return this.updateHelpers._updateOptions(options2, redraw, animate, updateSyncedCharts, overwriteInitialConfig);
            }
        },
        {
            /**
   * Replaces the chart's series data and re-renders.
   *
   * @param {ApexAxisChartSeries | ApexNonAxisChartSeries} [newSeries=[]] - The replacement series array.
   * @param {boolean} [animate=true] - Whether to animate the update.
   * @param {boolean} [overwriteInitialSeries=true] - When true, replaces the stored initial series used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */ key: "updateSeries",
            value: function updateSeries() {
                var newSeries = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [], animate = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true, overwriteInitialSeries = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
                this.data.resetParsingFlags();
                this.series.prepareDataUpdate();
                this.updateHelpers.revertDefaultAxisMinMax();
                return this.updateHelpers._updateSeries(newSeries, animate, overwriteInitialSeries);
            }
        },
        {
            /**
   * Appends a new series to the existing series array and re-renders.
   *
   * @param {ApexAxisChartSeries[0] | ApexNonAxisChartSeries} newSerie - The series object to append.
   * @param {boolean} [animate=true] - Whether to animate the update.
   * @param {boolean} [overwriteInitialSeries=true] - When true, replaces the stored initial series used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */ key: "appendSeries",
            value: function appendSeries(newSerie) {
                var animate = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true, overwriteInitialSeries = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
                this.data.resetParsingFlags();
                var newSeries = this.w.config.series.slice();
                newSeries.push(/** @type {any} */ newSerie);
                this.series.prepareDataUpdate();
                this.updateHelpers.revertDefaultAxisMinMax();
                return this.updateHelpers._updateSeries(newSeries, animate, overwriteInitialSeries);
            }
        },
        {
            /**
   * Appends data points to existing series without replacing them.
   * Each element of `newData` corresponds to the series at the same index.
   *
   * @param {Array<{ data: any[] }>} newData - Data to append, in the same shape as series[].data.
   * @param {boolean} [overwriteInitialSeries=true] - When true, updates the stored initial series used by resetSeries().
   * @returns {Promise<ApexCharts>} Resolves with the chart instance after re-render.
   */ key: "appendData",
            value: function appendData(newData) {
                var overwriteInitialSeries = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
                var me = this;
                me.data.resetParsingFlags();
                me.w.globals.dataChanged = true;
                if (me.w.config.chart.animations.enabled) {
                    me.series.getPreviousPaths();
                }
                var newSeries = me.w.config.series.slice();
                for(var i = 0; i < newSeries.length; i++){
                    if (newData[i] !== null && typeof newData[i] !== "undefined") {
                        var srcSerie = /** @type {any} */ newData[i];
                        var dstSerie = /** @type {any} */ newSeries[i];
                        for(var j = 0; j < srcSerie.data.length; j++){
                            dstSerie.data.push(srcSerie.data[j]);
                        }
                    }
                }
                trimStreamingSeries(newSeries, me.w);
                me.w.config.series = newSeries;
                if (overwriteInitialSeries) {
                    me.w.globals.initialSeries = me.w.config.series;
                }
                return this.update();
            }
        },
        {
            /**
   * @param {object} [options]
   */ key: "update",
            value: function update(options2) {
                var _this = this;
                return new Promise(function(resolve, reject) {
                    if (options2 && _this.lastUpdateOptions && !_ApexCharts._optionsTooBigToCompare(options2) && JSON.stringify(_this.lastUpdateOptions) === JSON.stringify(options2)) {
                        return resolve(_this);
                    }
                    _this.lastUpdateOptions = options2 && !_ApexCharts._optionsTooBigToCompare(options2) ? Utils$1.clone(options2) : null;
                    new Destroy(_this.ctx).clear({
                        isUpdating: true
                    });
                    var graphData = _this.create(_this.w.config.series, options2 != null ? options2 : {});
                    if (!graphData) return resolve(_this);
                    _this.mount(graphData).then(function() {
                        var _a;
                        (_a = _this.morphTypeChange) == null ? void 0 : _a.applyChromeFade();
                        applyAxisTransition(_this.w);
                        applyDataLabelTransition(_this.w);
                        if (typeof _this.w.config.chart.events.updated === "function") {
                            _this.w.config.chart.events.updated(_this, _this.w);
                        }
                        _this.events.fireEvent("updated", [
                            _this,
                            _this.w
                        ]);
                        _this.w.globals.isDirty = true;
                        resolve(_this);
                    }).catch(function(e) {
                        reject(e);
                    });
                });
            }
        },
        {
            /**
   * Redraws the scale-dependent chrome (grid lines, x-axis, y-axes) IN PLACE
   * within the frozen layout after a data-only update changed the axis
   * domain. The rebuilt groups replace the old nodes positionally, so z-order
   * (grid back/front) is preserved without re-running mount. Legend,
   * annotations containers, toolbar, defs/masks, and the plot geometry are
   * untouched.
   *
   * Returns false when the refresh cannot faithfully reproduce the chart and
   * the caller must fall back to a full render:
   * - horizontal bar charts (inversed axes draw through a different path)
   * - charts with annotations or ink notes (their positions are scale-bound
   *   and are laid out by the full render)
   * - the new y labels no longer fit the width reserved at layout time
   *
   * @param {any} _xyRatios
   * @returns {boolean} true when the chrome was refreshed in place
   */ key: "_fastAxisChromeRefresh",
            value: function _fastAxisChromeRefresh(_xyRatios) {
                var w = this.w;
                var gl = w.globals;
                this._fastAxisBailReason = "";
                try {
                    if (gl.isBarHorizontal) {
                        this._fastAxisBailReason = "barHorizontal";
                        return false;
                    }
                    if (w.config.chart.sparkline.enabled) return true;
                    var a = w.config.annotations;
                    if (a && (a.yaxis && a.yaxis.length || a.xaxis && a.xaxis.length || a.points && a.points.length || a.texts && a.texts.length || a.images && a.images.length)) {
                        this._fastAxisBailReason = "annotations";
                        return false;
                    }
                    if (w.config.chart.ink && w.config.chart.ink.enabled) {
                        this._fastAxisBailReason = "ink";
                        return false;
                    }
                    var dim = this.dimensions;
                    if (!dim || !dim.dimYAxis) {
                        this._fastAxisBailReason = "noDimensions";
                        return false;
                    }
                    var prevYLabelsCoords = w.layout.yLabelsCoords;
                    var prevYTitleCoords = w.layout.yTitleCoords;
                    var yaxisLabelCoords = dim.dimYAxis.getyAxisLabelsCoords();
                    var yTitleCoords = dim.dimYAxis.getyAxisTitleCoords();
                    w.layout.yLabelsCoords = [];
                    w.layout.yTitleCoords = [];
                    w.config.yaxis.map(function(_yaxe, index) {
                        w.layout.yLabelsCoords.push({
                            width: yaxisLabelCoords[index].width,
                            index: index
                        });
                        w.layout.yTitleCoords.push(/** @type {any} */ {
                            width: yTitleCoords[index].width,
                            index: index
                        });
                    });
                    var newYAxisWidth = dim.dimYAxis.getTotalYAxisWidth();
                    if (newYAxisWidth > dim.yAxisWidth + 2) {
                        w.layout.yLabelsCoords = prevYLabelsCoords;
                        w.layout.yTitleCoords = prevYTitleCoords;
                        this._fastAxisBailReason = "labelWidth ".concat(newYAxisWidth, " > ").concat(dim.yAxisWidth);
                        return false;
                    }
                    var innerEl = w.dom.elGraphical.node;
                    var oldGrid = innerEl.querySelector(".apexcharts-grid");
                    var oldGridBorders = innerEl.querySelector(".apexcharts-grid-borders");
                    if (!oldGrid) {
                        this._fastAxisBailReason = "missingGridNode";
                        return false;
                    }
                    var gridParent = oldGrid.parentNode;
                    var gridNext = oldGridBorders ? oldGridBorders.nextSibling : oldGrid.nextSibling;
                    oldGrid.remove();
                    if (oldGridBorders) oldGridBorders.remove();
                    innerEl.querySelectorAll(".apexcharts-xaxis-tick").forEach(function(t) {
                        return t.remove();
                    });
                    this.grid = new Grid(w, this);
                    var elgrid = this.grid.drawGrid();
                    if (elgrid && elgrid.el) {
                        gridParent.insertBefore(elgrid.el.node, gridNext);
                        if (elgrid.elGridBorders && elgrid.elGridBorders.node) {
                            gridParent.insertBefore(elgrid.elGridBorders.node, gridNext);
                        }
                    }
                    var xAxis = new XAxis(this.w, this.ctx, elgrid);
                    var oldXaxis = innerEl.querySelector(".apexcharts-xaxis");
                    if (oldXaxis) {
                        var xParent = oldXaxis.parentNode;
                        var xNext = oldXaxis.nextSibling;
                        oldXaxis.remove();
                        var elXaxis = xAxis.drawXaxis();
                        xParent.insertBefore(elXaxis.node, xNext);
                    }
                    var yAxis = new YAxis(this.w, {
                        theme: this.theme,
                        timeScale: this.timeScale
                    }, elgrid);
                    for(var index = 0; index < w.config.yaxis.length; index++){
                        if (gl.ignoreYAxisIndexes.indexOf(index) !== -1) continue;
                        var oldY = w.dom.baseEl.querySelector(".apexcharts-yaxis[rel='".concat(index, "']"));
                        if (!oldY) {
                            this._fastAxisBailReason = "missingYAxisNode";
                            return false;
                        }
                        var yParent = oldY.parentNode;
                        if (!yParent) {
                            this._fastAxisBailReason = "missingYAxisParent";
                            return false;
                        }
                        var yNext = oldY.nextSibling;
                        oldY.remove();
                        var elYaxis = yAxis.drawYaxis(index);
                        yParent.insertBefore(elYaxis.node, yNext);
                    }
                    if (elgrid !== null) {
                        xAxis.xAxisLabelCorrections();
                        yAxis.setYAxisTextAlignments();
                        w.config.yaxis.map(function(yaxe, index) {
                            if (gl.ignoreYAxisIndexes.indexOf(index) === -1) {
                                yAxis.yAxisTitleRotate(index, yaxe.opposite);
                            }
                        });
                    }
                    return true;
                } catch (e) {
                    this._fastAxisBailReason = "error: " + (e && /** @type {any} */ e.message);
                    return false;
                }
            }
        },
        {
            /**
   * Fast update path for data-only series changes.
   *
   * Skips rebuilding grid, axes, dimensions, legend, annotations, tooltip DOM,
   * and toolbar. Only recalculates scales and replots the series paths.
   * Called automatically by _updateSeries() when the fast path is eligible.
   *
   * @param {boolean} animate - Whether to animate the update.
   * @param {string} [prevAxisScaleSig] - Signature of the on-screen axis scale
   *   captured by _updateSeries() before parseData recomputed bounds. When the
   *   recomputed scale differs, the fast path can't repaint the ruler in place,
   *   so it delegates to a full render. Omitted -> the check is skipped.
   * @returns {Promise<ApexCharts>} Resolves with the chart instance.
   */ key: "fastUpdate",
            value: function fastUpdate(animate, prevAxisScaleSig) {
                var _this = this;
                return new Promise(function(resolve, reject) {
                    var _a, _b, _c;
                    try {
                        var w = _this.w;
                        var gl = w.globals;
                        gl.shouldAnimate = animate;
                        gl.dataChanged = true;
                        gl.animationEnded = false;
                        PerformanceCache.invalidateSelectors(w);
                        var gl2 = w.globals;
                        gl2.maxY = -Number.MAX_VALUE;
                        gl2.minY = Number.MIN_VALUE;
                        gl2.minYArr = [];
                        gl2.maxYArr = [];
                        gl2.maxX = -Number.MAX_VALUE;
                        gl2.minX = Number.MAX_VALUE;
                        gl2.initialMaxX = -Number.MAX_VALUE;
                        gl2.initialMinX = Number.MAX_VALUE;
                        gl2.yAxisScale = [];
                        gl2.xAxisScale = null;
                        gl2.xAxisTicksPositions = [];
                        gl2.xRange = 0;
                        gl2.yRange = [];
                        gl2.zRange = 0;
                        gl2.xTickAmount = 0;
                        gl2.multiAxisTickAmount = 0;
                        gl2.pointsArray = [];
                        gl2.barCanvasCoords = null;
                        gl2.dataLabelsRects = [];
                        gl2.lastDrawnDataLabelsIndexes = [];
                        gl2.textRectsCache = /* @__PURE__ */ new Map();
                        gl2.domCache = /* @__PURE__ */ new Map();
                        gl2.cachedSelectors = {};
                        gl2.disableZoomIn = false;
                        gl2.disableZoomOut = false;
                        if (gl.axisCharts) {
                            _this.core.coreCalculations();
                            if (w.config.xaxis.type !== "category") {
                                _this.formatters.setLabelFormatters();
                            }
                        }
                        _this.formatters.heatmapLabelFormatters();
                        var xyRatios = _this.core.xySettings();
                        if (_this._zoomPanSelection) _this._zoomPanSelection.xyRatios = xyRatios;
                        var newAxisScaleSig = JSON.stringify({
                            y: (gl.yAxisScale || []).map(function(s) {
                                return s ? s.result : null;
                            }),
                            xMin: gl.minX,
                            xMax: gl.maxX
                        });
                        var scaleChanged = gl.axisCharts && prevAxisScaleSig != null && newAxisScaleSig !== prevAxisScaleSig;
                        if (scaleChanged && !_this._fastAxisChromeRefresh(xyRatios)) {
                            _this._updateStats.full++;
                            return _this.update().then(function() {
                                return resolve(_this);
                            }).catch(reject);
                        }
                        if (scaleChanged) {
                            _this._updateStats.fastWithAxes++;
                        } else {
                            _this._updateStats.fast++;
                        }
                        (_a = _this.weave) == null ? void 0 : _a.dispatch("afterScales", {
                            pass: "fast",
                            xyRatios: xyRatios
                        });
                        var rr = /** @type {any} */ _this.ctx.renderer;
                        var reuseCanvasHost = !!(rr && rr.kind === "canvas" && rr.canRepaintInPlace && rr.canRepaintInPlace());
                        if (reuseCanvasHost) rr._repaintHostInPlace = true;
                        var innerEl = w.dom.elGraphical.node;
                        var toRemove = innerEl.querySelectorAll((reuseCanvasHost ? "" : ".apexcharts-canvas-series-wrap, ") + ".apexcharts-plot-series, .apexcharts-series, .apexcharts-datalabels, .apexcharts-datalabels-background");
                        toRemove.forEach(function(el) {
                            var _a2;
                            return (_a2 = el.parentNode) == null ? void 0 : _a2.removeChild(el);
                        });
                        var elGraph = _this.core.plotChartType(w.config.series, xyRatios);
                        var gridEl = innerEl.querySelector(".apexcharts-grid");
                        var xaxisEl = innerEl.querySelector(".apexcharts-xaxis");
                        var graphs = Array.isArray(elGraph) ? elGraph : [
                            elGraph
                        ];
                        var anchor = gridEl && w.config.grid.position === "front" ? gridEl : xaxisEl;
                        if (anchor) {
                            graphs.forEach(function(g) {
                                var node = g && g.node ? g.node : g;
                                if (node) innerEl.insertBefore(node, anchor);
                            });
                        } else {
                            graphs.forEach(function(g) {
                                w.dom.elGraphical.add(g);
                            });
                        }
                        var dataLabels = new DataLabels(w, _this);
                        dataLabels.bringForward();
                        if (w.config.dataLabels.background.enabled) {
                            dataLabels.dataLabelsBackground();
                        }
                        if (Environment.isBrowser() && w.config.tooltip.enabled && !gl.noData) {
                            (_b = w.globals.tooltip) == null ? void 0 : _b.drawTooltip(xyRatios);
                        }
                        (_c = _this.weave) == null ? void 0 : _c.dispatch("draw", {
                            pass: "fast",
                            xyRatios: xyRatios
                        });
                        if (typeof w.config.chart.events.updated === "function") {
                            w.config.chart.events.updated(_this, w);
                        }
                        _this.events.fireEvent("updated", [
                            _this,
                            w
                        ]);
                        enforceLicense(w, _this);
                        gl.isDirty = true;
                        resolve(_this);
                    } catch (e) {
                        reject(e);
                    }
                });
            }
        },
        {
            /**
   * Returns all charts in the same `chart.group` (including this instance),
   * used to synchronise zoom/pan across grouped charts.
   *
   * @returns {ApexCharts[]}
   */ key: "getSyncedCharts",
            value: function getSyncedCharts() {
                var group = /** @type {ApexCharts[]} */ this.getGroupedCharts();
                group.splice(0, 0, this);
                return group;
            }
        },
        {
            /**
   * Returns all charts in the same `chart.group`, excluding this instance.
   * Used internally to apply hover/zoom effects to sibling charts.
   *
   * @returns {ApexCharts[]}
   */ key: "getGroupedCharts",
            value: function getGroupedCharts() {
                var _this = this;
                return Apex._chartInstances.filter(function(ch) {
                    return _this !== ch.chart && !!_this.w.config.chart.group && _this.w.config.chart.group === ch.group;
                }).map(function(ch) {
                    return ch.chart;
                });
            }
        },
        {
            /**
   * Linked Views (#4): clear crossfilter dimming across this chart and every
   * chart in its `chart.group`. No-op unless the `link` feature is bundled.
   */ key: "clearCrossfilter",
            value: function clearCrossfilter() {
                var _a;
                (_a = this.linkedViews) == null ? void 0 : _a.clearGroup();
            }
        },
        {
            /**
   * Measure ruler (#18): arm a sticky measure-ruler mode (drag A->B on the
   * plot to read dx/dy/%change/slope). Alternatively hold the measure key
   * (chart.measure.key, default 'm') and drag. No-op unless the `measure`
   * feature is bundled and chart.measure.enabled.
   */ key: "startMeasure",
            value: function startMeasure() {
                var _a;
                (_a = this.measure) == null ? void 0 : _a.startMeasure();
            }
        },
        {
            /** Measure ruler (#18): leave measure mode. */ key: "stopMeasure",
            value: function stopMeasure() {
                var _a;
                (_a = this.measure) == null ? void 0 : _a.stopMeasure();
            }
        },
        {
            /** Measure ruler (#18): remove all pinned measure rulers. */ key: "clearMeasures",
            value: function clearMeasures() {
                var _a;
                (_a = this.measure) == null ? void 0 : _a.clearMeasures();
            }
        },
        {
            /**
   * Toggles (show/hide) the series identified by name.
   * Mirrors a click on the corresponding legend item.
   *
   * @param {string} seriesName
   * @returns {object | undefined} The collapsed series object, if now hidden.
   */ key: "toggleSeries",
            value: function toggleSeries(seriesName) {
                return this.series.toggleSeries(seriesName);
            }
        },
        {
            /**
   * Highlights or un-highlights a series when the user hovers a legend item.
   * Called internally by the legend; not typically called by consumers.
   *
   * @param {MouseEvent} e
   * @param {HTMLElement} targetElement - The legend marker element being hovered.
   */ key: "highlightSeriesOnLegendHover",
            value: function highlightSeriesOnLegendHover(e, targetElement) {
                return this.series.toggleSeriesOnHover(e, targetElement);
            }
        },
        {
            /**
   * Makes a previously hidden series visible and re-renders.
   *
   * @param {string} seriesName
   */ key: "showSeries",
            value: function showSeries(seriesName) {
                this.series.showSeries(seriesName);
            }
        },
        {
            /**
   * Hides a visible series and re-renders.
   *
   * @param {string} seriesName
   */ key: "hideSeries",
            value: function hideSeries(seriesName) {
                this.series.hideSeries(seriesName);
            }
        },
        {
            /**
   * Highlights (dims all other series) the series identified by name.
   *
   * @param {string} seriesName
   */ key: "highlightSeries",
            value: function highlightSeries(seriesName) {
                this.series.highlightSeries(seriesName);
            }
        },
        {
            /**
   * Returns whether the series identified by name is currently hidden.
   *
   * @param {string} seriesName
   * @returns {boolean}
   */ key: "isSeriesHidden",
            value: function isSeriesHidden(seriesName) {
                return this.series.isSeriesHidden(seriesName);
            }
        },
        {
            /**
   * Resets the chart to the initial series and optionally the initial zoom level.
   *
   * @param {boolean} [shouldUpdateChart=true] - When true, triggers a re-render.
   * @param {boolean} [shouldResetZoom=true] - When true, restores the initial zoom level.
   */ key: "resetSeries",
            value: function resetSeries() {
                var shouldUpdateChart = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true, shouldResetZoom = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
                this.series.resetSeries(shouldUpdateChart, shouldResetZoom);
            }
        },
        {
            /**
   * Subscribes to a chart event by name.
   * Supported event names mirror the `chart.events` option keys
   * (e.g. `'mounted'`, `'updated'`, `'dataPointMouseEnter'`).
   *
   * @param {string} name - Event name.
   * @param {Function} handler - Callback invoked when the event fires.
   */ key: "addEventListener",
            value: function addEventListener(name2, handler) {
                this.events.addEventListener(name2, handler);
            }
        },
        {
            /**
   * Removes a previously registered event listener.
   *
   * @param {string} name - Event name.
   * @param {Function} handler - The exact function reference passed to addEventListener.
   */ key: "removeEventListener",
            value: function removeEventListener(name2, handler) {
                this.events.removeEventListener(name2, handler);
            }
        },
        {
            /**
   * Adds an x-axis annotation dynamically after render.
   *
   * @param {XAxisAnnotations} opts - Annotation configuration.
   * @param {boolean} [pushToMemory=true] - When true, the annotation persists across re-renders.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */ key: "addXaxisAnnotation",
            value: function addXaxisAnnotation(opts) {
                var pushToMemory = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true, context = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : void 0;
                var _a;
                var me = /** @type {ApexCharts} */ /** @type {unknown} */ this;
                if (context) {
                    me = context;
                }
                (_a = me.annotations) == null ? void 0 : _a.addXaxisAnnotationExternal(opts, pushToMemory, me);
            }
        },
        {
            /**
   * Adds a y-axis annotation dynamically after render.
   *
   * @param {YAxisAnnotations} opts - Annotation configuration.
   * @param {boolean} [pushToMemory=true] - When true, the annotation persists across re-renders.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */ key: "addYaxisAnnotation",
            value: function addYaxisAnnotation(opts) {
                var pushToMemory = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true, context = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : void 0;
                var _a;
                var me = /** @type {ApexCharts} */ /** @type {unknown} */ this;
                if (context) {
                    me = context;
                }
                (_a = me.annotations) == null ? void 0 : _a.addYaxisAnnotationExternal(opts, pushToMemory, me);
            }
        },
        {
            /**
   * Adds a point annotation dynamically after render.
   *
   * @param {PointAnnotations} opts - Annotation configuration.
   * @param {boolean} [pushToMemory=true] - When true, the annotation persists across re-renders.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */ key: "addPointAnnotation",
            value: function addPointAnnotation(opts) {
                var pushToMemory = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true, context = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : void 0;
                var _a;
                var me = /** @type {ApexCharts} */ /** @type {unknown} */ this;
                if (context) {
                    me = context;
                }
                (_a = me.annotations) == null ? void 0 : _a.addPointAnnotationExternal(opts, pushToMemory, me);
            }
        },
        {
            /**
   * Removes all annotations from the chart.
   *
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */ key: "clearAnnotations",
            value: function clearAnnotations() {
                var context = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : void 0;
                var _a;
                var me = /** @type {ApexCharts} */ /** @type {unknown} */ this;
                if (context) {
                    me = context;
                }
                me.lastUpdateOptions = null;
                (_a = me.annotations) == null ? void 0 : _a.clearAnnotations(me);
            }
        },
        {
            /**
   * Removes a specific annotation by its `id`.
   *
   * @param {string} id - The annotation id as set in the annotation config.
   * @param {ApexCharts} [context] - Override the target chart instance (used by exec()).
   */ key: "removeAnnotation",
            value: function removeAnnotation(id) {
                var context = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : void 0;
                var _a;
                var me = /** @type {ApexCharts} */ /** @type {unknown} */ this;
                if (context) {
                    me = context;
                }
                me.lastUpdateOptions = null;
                (_a = me.annotations) == null ? void 0 : _a.removeAnnotation(me, id);
            }
        },
        {
            /**
   * Returns the inner SVG group element that contains all chart graphics.
   *
   * @returns {Element | null}
   */ key: "getChartArea",
            value: function getChartArea() {
                var el = this.w.dom.baseEl.querySelector(".apexcharts-inner");
                return el;
            }
        },
        {
            /**
   * Returns the sum of all data points whose x value falls within [minX, maxX].
   *
   * @param {number} minX
   * @param {number} maxX
   * @returns {number[]} One total per series.
   */ key: "getSeriesTotalXRange",
            value: function getSeriesTotalXRange(minX, maxX) {
                return this.coreUtils.getSeriesTotalsXRange(minX, maxX);
            }
        },
        {
            /**
   * Returns the highest y value in the specified series.
   *
   * @param {number} [seriesIndex=0]
   * @returns {number}
   */ key: "getHighestValueInSeries",
            value: function getHighestValueInSeries() {
                var seriesIndex = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
                var range = new Range(this.w);
                return range.getMinYMaxY(seriesIndex).highestY;
            }
        },
        {
            /**
   * Returns the lowest y value in the specified series.
   *
   * @param {number} [seriesIndex=0]
   * @returns {number}
   */ key: "getLowestValueInSeries",
            value: function getLowestValueInSeries() {
                var seriesIndex = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
                var range = new Range(this.w);
                return range.getMinYMaxY(seriesIndex).lowestY;
            }
        },
        {
            /**
   * Returns the sum of each series (the totals used for percentage calculations).
   *
   * @returns {number[]}
   */ key: "getSeriesTotal",
            value: function getSeriesTotal() {
                return this.w.globals.seriesTotals;
            }
        },
        {
            /**
   * Returns a curated snapshot of chart state for use in formatters, events,
   * and external integrations. Prefer this over accessing `chart.w` directly.
   *
   * The shape of this object is stable and versioned. `chart.w` is internal
   * and will be restricted in a future major version.
   */ key: "getState",
            value: function getState() {
                var w = this.w;
                var gl = w.globals;
                return {
                    // Series data — computed/parsed form used for rendering
                    series: w.seriesData.series,
                    seriesNames: w.seriesData.seriesNames,
                    colors: gl.colors,
                    labels: w.labelData.labels,
                    seriesTotals: gl.seriesTotals,
                    seriesPercent: gl.seriesPercent,
                    seriesXvalues: gl.seriesXvalues,
                    seriesYvalues: gl.seriesYvalues,
                    // Axis bounds — updated after each render
                    minX: gl.minX,
                    maxX: gl.maxX,
                    minY: gl.minY,
                    maxY: gl.maxY,
                    minYArr: gl.minYArr,
                    maxYArr: gl.maxYArr,
                    minXDiff: gl.minXDiff,
                    dataPoints: gl.dataPoints,
                    // Axis scale objects — computed tick/scale results
                    xAxisScale: gl.xAxisScale,
                    yAxisScale: gl.yAxisScale,
                    xTickAmount: gl.xTickAmount,
                    // Axis type flags
                    isXNumeric: w.axisFlags.isXNumeric,
                    // Multi-axis series mapping
                    seriesYAxisMap: gl.seriesYAxisMap,
                    seriesYAxisReverseMap: gl.seriesYAxisReverseMap,
                    // Chart dimensions — updated after each render/resize
                    svgWidth: gl.svgWidth,
                    svgHeight: gl.svgHeight,
                    gridWidth: w.layout.gridWidth,
                    gridHeight: w.layout.gridHeight,
                    // Interactive state
                    selectedDataPoints: w.interact.selectedDataPoints,
                    collapsedSeriesIndices: gl.collapsedSeriesIndices,
                    zoomed: w.interact.zoomed,
                    // Chart-type-specific series data (null when not applicable)
                    seriesX: w.seriesData.seriesX,
                    seriesZ: w.seriesData.seriesZ,
                    seriesCandleO: w.candleData.seriesCandleO,
                    seriesCandleH: w.candleData.seriesCandleH,
                    seriesCandleM: w.candleData.seriesCandleM,
                    seriesCandleL: w.candleData.seriesCandleL,
                    seriesCandleC: w.candleData.seriesCandleC,
                    seriesRangeStart: w.rangeData.seriesRangeStart,
                    seriesRangeEnd: w.rangeData.seriesRangeEnd,
                    seriesGoals: w.seriesData.seriesGoals
                };
            }
        },
        {
            /**
   * Programmatically selects or deselects a data point.
   * Equivalent to a user click on the data point.
   *
   * @param {number} seriesIndex - Zero-based series index.
   * @param {number} [dataPointIndex] - Zero-based data point index within the series.
   * @returns {number[][] | null} Updated selectedDataPoints array, or null.
   */ key: "toggleDataPointSelection",
            value: function toggleDataPointSelection(seriesIndex, dataPointIndex) {
                return this.updateHelpers.toggleDataPointSelection(seriesIndex, dataPointIndex);
            }
        },
        {
            /**
   * Programmatically zooms the x-axis to the given range.
   * Requires zoom to be enabled (`chart.zoom.enabled: true`).
   *
   * @param {number} min - The minimum x value (timestamp or numeric).
   * @param {number} max - The maximum x value (timestamp or numeric).
   */ key: "zoomX",
            value: function zoomX(min, max) {
                var _a;
                (_a = this.ctx.toolbar) == null ? void 0 : _a.zoomUpdateOptions(min, max);
            }
        },
        {
            /**
   * Switches the active locale, updating all locale-dependent labels (toolbar tooltips, month names, etc.).
   *
   * @param {string} localeName - Must match a locale name defined in `chart.locales`.
   */ key: "setLocale",
            value: function setLocale(localeName) {
                this.localization.setCurrentLocaleValues(localeName);
            }
        },
        {
            /**
   * Exports the chart to a PNG or SVG data URI.
   * Requires the Exports feature: `import 'apexcharts/features/exports'`.
   *
   * @param {{ scale?: number, width?: number }} [options]
   * @returns {Promise<{ imgURI: string } | { blob: Blob }>}
   */ key: "dataURI",
            value: function dataURI(options2) {
                if (!this.ctx.exports) throw new Error("apexcharts: Exports feature is not registered. Import apexcharts/features/exports.");
                return this.ctx.exports.dataURI(options2);
            }
        },
        {
            /**
   * Returns the chart's SVG markup as a string, optionally scaled.
   * Requires the Exports feature: `import 'apexcharts/features/exports'`.
   *
   * @param {number} [scale=1]
   * @returns {Promise<string>}
   */ key: "getSvgString",
            value: function getSvgString(scale) {
                if (!this.ctx.exports) throw new Error("apexcharts: Exports feature is not registered. Import apexcharts/features/exports.");
                return this.ctx.exports.getSvgString(scale);
            }
        },
        {
            /**
   * Triggers a CSV download of the chart's data.
   * Requires the Exports feature: `import 'apexcharts/features/exports'`.
   *
   * @param {{ series?: any, fileName?: string, columnDelimiter?: string, lineDelimiter?: string }} [options]
   */ key: "exportToCSV",
            value: function exportToCSV() {
                var options2 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                if (!this.ctx.exports) throw new Error("apexcharts: Exports feature is not registered. Import apexcharts/features/exports.");
                return this.ctx.exports.exportToCSV(options2);
            }
        },
        {
            key: "paper",
            value: function paper() {
                return this.w.dom.Paper;
            }
        },
        {
            /**
   * Returns the active series renderer for the last render: `'svg'` (default)
   * or `'canvas'` (Strata #2). `'auto'`/`'canvas'` resolve to `'svg'` unless the
   * canvas renderer feature is bundled and no canvas-unsupported feature is in
   * use. See `chart.renderer` / `chart.rendererThreshold`.
   *
   * @returns {'svg' | 'canvas' | 'gpu'}
   */ key: "getActiveRenderer",
            value: function getActiveRenderer() {
                return this.rendererController ? this.rendererController.getActiveKind() : "svg";
            }
        },
        {
            /**
   * Facet (#13): re-resolve the `--apx-*` design tokens and re-render.
   *
   * Tokens are read from the CSS cascade once per render, so a runtime change
   * that is NOT an OS color-scheme flip (e.g. the host app swaps its own
   * design-system theme by toggling a class or setting style properties) is
   * invisible until the next render, and `updateOptions({})` is memoized away.
   * This busts the memo and re-renders, picking up the current token values.
   * @returns {Promise<any>}
   */ key: "refreshTokens",
            value: function refreshTokens() {
                this.lastUpdateOptions = null;
                return this.update();
            }
        },
        {
            /**
   * Drills into the child level referenced by `id` (a `chart.drilldown.series` entry).
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @param {string|number} id - The drilldown series id to navigate into.
   * @returns {Promise<ApexCharts>}
   */ key: "drillDown",
            value: function drillDown(id) {
                if (!this.ctx.drilldown) throw new Error("apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown.");
                return this.ctx.drilldown.drillDown(id);
            }
        },
        {
            /**
   * Navigates back one drilldown level.
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @returns {Promise<ApexCharts>}
   */ key: "drillUp",
            value: function drillUp() {
                if (!this.ctx.drilldown) throw new Error("apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown.");
                return this.ctx.drilldown.drillUp();
            }
        },
        {
            /**
   * Navigates back to the root drilldown level.
   * Requires the Drilldown feature: `import 'apexcharts/features/drilldown'`.
   *
   * @returns {Promise<ApexCharts>}
   */ key: "drillToRoot",
            value: function drillToRoot() {
                if (!this.ctx.drilldown) throw new Error("apexcharts: Drilldown feature is not registered. Import apexcharts/features/drilldown.");
                return this.ctx.drilldown.drillToRoot();
            }
        },
        {
            /**
   * @param {Partial<import('./types/internal').SeriesData>} slice
   */ key: "_writeParsedSeriesData",
            value: function _writeParsedSeriesData(slice) {
                _ApexCharts._writeDataProps(this.w.seriesData, slice);
            }
        },
        {
            /**
   * @param {Partial<import('./types/internal').RangeData>} slice
   */ key: "_writeParsedRangeData",
            value: function _writeParsedRangeData(slice) {
                _ApexCharts._writeDataProps(this.w.rangeData, slice);
            }
        },
        {
            /**
   * @param {Partial<import('./types/internal').CandleData>} slice
   */ key: "_writeParsedCandleData",
            value: function _writeParsedCandleData(slice) {
                _ApexCharts._writeDataProps(this.w.candleData, slice);
            }
        },
        {
            /**
   * @param {Partial<import('./types/internal').LabelData>} slice
   */ key: "_writeParsedLabelData",
            value: function _writeParsedLabelData(slice) {
                _ApexCharts._writeDataProps(this.w.labelData, slice);
            }
        },
        {
            /**
   * @param {Partial<import('./types/internal').AxisFlags>} slice
   */ key: "_writeParsedAxisFlags",
            value: function _writeParsedAxisFlags(slice) {
                _ApexCharts._writeDataProps(this.w.axisFlags, slice);
            }
        },
        {
            /**
   * @param {Partial<import('./types/internal').LayoutCoords>} slice
   */ key: "_writeLayoutCoords",
            value: function _writeLayoutCoords(slice) {
                _ApexCharts._writeDataProps(this.w.layout, slice);
            }
        },
        {
            key: "_parentResizeCallback",
            value: function _parentResizeCallback() {
                if (this.w.globals.animationEnded && this.w.config.chart.redrawOnParentResize) {
                    this._windowResize();
                }
            }
        },
        {
            /**
   * Handle window resize and re-draw the whole chart.
   */ key: "_windowResize",
            value: function _windowResize() {
                var _this = this;
                this.w.globals.resizeTimer = window.setTimeout(function() {
                    var gl = _this.w.globals;
                    if (_this.core && gl.lastResizeSignature) {
                        var sig = _this.core.getResizeSignature();
                        if (sig.w === gl.lastResizeSignature.w && sig.h === gl.lastResizeSignature.h && sig.iw === gl.lastResizeSignature.iw) {
                            return;
                        }
                    }
                    gl.resized = true;
                    gl.dataChanged = false;
                    _this.ctx.update();
                }, 150);
            }
        },
        {
            key: "_windowResizeHandler",
            value: function _windowResizeHandler() {
                var _a;
                clearTimeout((_a = this.w.globals.resizeTimer) != null ? _a : void 0);
                var _this_w_config_chart = this.w.config.chart, redraw = _this_w_config_chart.redrawOnWindowResize;
                if (typeof redraw === "function") {
                    redraw = /** @type {any} */ redraw();
                }
                redraw && this._windowResize();
            }
        }
    ], [
        {
            key: "_optionsTooBigToCompare",
            value: /**
   * True when an options object carries enough series data that a
   * JSON.stringify equality check (and the Utils.clone needed to store it for
   * later comparison) would cost more than the re-render it tries to avoid.
   * @param {any} options
   * @returns {boolean}
   */ function _optionsTooBigToCompare(options2) {
                var series = options2 && options2.series;
                if (!Array.isArray(series)) return false;
                var points = 0;
                for(var i = 0; i < series.length; i++){
                    var d = series[i] && series[i].data;
                    points += Array.isArray(d) ? d.length : 1;
                    if (points > 1e3) return true;
                }
                return false;
            }
        },
        {
            key: "getChartByID",
            value: /**
   * Retrieves a rendered chart instance by its `chart.id` config value.
   *
   * @param {string} id - The chart ID set via `chart.id` in options.
   * @returns {ApexCharts | undefined}
   */ function getChartByID(id) {
                var chartId = Utils$1.escapeString(id);
                if (!Apex._chartInstances) return void 0;
                var c = Apex._chartInstances.filter(function(ch) {
                    return ch.id === chartId;
                })[0];
                return c && c.chart;
            }
        },
        {
            key: "initOnLoad",
            value: /**
   * Scans the document for elements with a `data-apexcharts` attribute and
   * `data-options` JSON, then renders a chart in each one automatically.
   * Useful for non-framework HTML pages.
   */ function initOnLoad() {
                var _a;
                var els = document.querySelectorAll("[data-apexcharts]");
                for(var i = 0; i < els.length; i++){
                    var el = /** @type {HTMLElement} */ els[i];
                    var options2 = JSON.parse((_a = els[i].getAttribute("data-options")) != null ? _a : "");
                    var apexChart = new _ApexCharts(el, options2);
                    apexChart.render();
                }
            }
        },
        {
            key: "exec",
            value: /**
   * This static method allows users to call chart methods without necessarily from the
   * instance of the chart in case user has assigned chartID to the targeted chart.
   * The chartID is used for mapping the instance stored in Apex._chartInstances global variable
   *
   * This is helpful in cases when you don't have reference of the chart instance
   * easily and need to call the method from anywhere.
   * For eg, in React/Vue applications when you have many parent/child components,
   * and need easy reference to other charts for performing dynamic operations
   *
   * @param {string} chartID - The unique identifier which will be used to call methods
   * on that chart instance
   * @param {string} fn - The method name to call
   * @param {...any} opts - The parameters which are accepted in the original method will be passed here in the same order.
   */ function exec(chartID, fn) {
                for(var _len = arguments.length, opts = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++){
                    opts[_key - 2] = arguments[_key];
                }
                var chart = this.getChartByID(chartID);
                if (!chart) return;
                chart.w.globals.isExecCalled = true;
                var ret = null;
                if (chart.publicMethods.indexOf(fn) !== -1) {
                    var /** @type {any} */ _chart;
                    ret = (_chart = chart)[fn].apply(_chart, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(opts));
                }
                return ret;
            }
        },
        {
            key: "merge",
            value: /**
   * Deep-merges `source` into `target` and returns the result.
   * Thin wrapper around the internal `Utils.extend` utility.
   *
   * @param {object} target
   * @param {object} source
   * @returns {object}
   */ function merge(target, source) {
                return Utils$1.extend(target, source);
            }
        },
        {
            key: "getThemePalettes",
            value: function getThemePalettes1() {
                return getThemePalettes();
            }
        },
        {
            key: "use",
            value: /**
   * Register additional chart types. Used by sub-entry points so that only
   * the types they include are bundled.
   *
   * @param {Record<string, new (...args: any[]) => any>} typeMap  e.g. { line: Line, area: Line }
   */ function use(typeMap) {
                register(typeMap);
            }
        },
        {
            key: "registerFeatures",
            value: /**
   * Register optional feature modules (Exports, Legend, Toolbar,
   * ZoomPanSelection, KeyboardNavigation, Annotations).
   *
   * Call this before rendering any chart. Feature entry files (e.g.
   * `apexcharts/features/legend`) call this automatically when imported.
   * Note: Tooltip is part of core and does not need to be registered.
   *
   * @param {Record<string, new (...args: any[]) => any>} featureMap  e.g. { legend: Legend, exports: Exports }
   */ function registerFeatures(featureMap) {
                InitCtxVariables.registerFeatures(featureMap);
            }
        },
        {
            key: "setLicense",
            value: /**
   * Set the license key that unlocks the premium features (storyboard, link /
   * crossfilter, ink, measure, contextMenu, perspectives, history). Without a
   * valid key those features still work but the chart shows an "APEXCHARTS"
   * trial watermark; a valid key removes it. Keys are shared across the whole
   * ApexCharts family (apexgantt, apextree, apexsankey, apex-grid-enterprise,
   * apexstock), so one customer key works everywhere.
   *
   * Call before render(). The watermark is re-evaluated on every render/update,
   * so a late setLicense(validKey) followed by chart.update() clears it.
   *
   * Precedence per chart: `chart.license` (most specific) -> this key ->
   * `window.Apex.license` -> unlicensed (trial).
   *
   * @param {string} key  the `APEX-<base64(JSON)>` license key
   * @returns {typeof ApexCharts}
   */ function setLicense(key) {
                LicenseManager.setLicense(key);
                reevaluateLicenseAcrossCharts();
                return _ApexCharts;
            }
        },
        {
            key: "registerPlugin",
            value: /**
   * Register a Weave plugin definition (a plain { name, setup } object).
   * Lives in core so plugins can always be registered; they only activate when
   * the Weave host is bundled (`import 'apexcharts/features/weave'`, included in
   * the full bundle) and listed in a chart's `plugins` config.
   *
   * @param {{ name: string, apiVersion?: number, setup: Function, destroy?: Function }} def
   * @returns {typeof ApexCharts}
   */ function registerPlugin1(def) {
                registerPlugin(def);
                return _ApexCharts;
            }
        },
        {
            key: "unregisterPlugin",
            value: /**
   * Remove a registered Weave plugin definition. Charts already holding an
   * active instance keep it until their plugins config changes or they are
   * destroyed; the name simply stops resolving for new activations. Intended
   * for tests and hot-reload flows.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */ function unregisterPlugin1(name2) {
                unregisterPlugin(name2);
                return _ApexCharts;
            }
        },
        {
            key: "registerRenderer",
            value: /**
   * Register a non-SVG series renderer (Strata #2). SVG is built in; the canvas
   * backend registers itself via `import 'apexcharts/features/renderer-canvas'`.
   * When a `kind` is not registered, selection falls back to SVG.
   *
   * @param {string} kind  e.g. 'canvas'
   * @param {(w: any, ctx: any) => any} factory  returns a Renderer instance
   */ function registerRenderer(kind, factory) {
                RendererController.registerRenderer(kind, factory);
            }
        },
        {
            key: "registerSeriesType",
            value: /**
   * Register a custom series type (Marks #11): a `{ renderItem }` definition
   * that draws primitives (path/line/rect/circle/text) per datum. Requires the
   * Marks feature to be bundled (`import 'apexcharts/features/marks'`, included
   * in the full bundle); without it this warns and no-ops. Once registered, use
   * it via `series[].type` or `chart.type`.
   *
   * @param {string} name  the type name, e.g. 'dumbbell'
   * @param {{ renderItem: Function, dataType?: string, yExtent?: Function, tooltip?: Function }} def
   * @returns {typeof ApexCharts}
   */ function registerSeriesType(name2, def) {
                var factory = /** @type {any} */ _ApexCharts._customSeriesFactory;
                if (!factory) {
                    console.warn('[apexcharts] registerSeriesType("'.concat(name2, "\") requires the Marks feature: import 'apexcharts/features/marks'."));
                    return _ApexCharts;
                }
                if (!def || typeof def.renderItem !== "function") {
                    console.warn('[apexcharts] registerSeriesType("'.concat(name2, '") needs a def with a renderItem() function.'));
                    return _ApexCharts;
                }
                if (hasChartClass(name2) && !isCustom(name2)) {
                    console.warn('[apexcharts] registerSeriesType("'.concat(name2, '") would override the built-in "').concat(name2, '" chart type; pick another name.'));
                    return _ApexCharts;
                }
                register((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])({}, name2, factory(name2, def)));
                markCustom(name2);
                return _ApexCharts;
            }
        },
        {
            key: "unregisterSeriesType",
            value: /**
   * Remove a custom series type registered via registerSeriesType. Built-in
   * chart types cannot be unregistered. Intended for tests and hot-reload.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */ function unregisterSeriesType(name2) {
                if (isCustom(name2)) unregister(name2);
                return _ApexCharts;
            }
        },
        {
            key: "registerTheme",
            value: /**
   * Facet (#13): register a named theme (palette + design tokens + mode)
   * referenceable via `theme: { name }`. The theme sits below explicit config
   * and CSS `--apx-*` tokens, above the built-in palette/mode defaults.
   *
   * @param {string} name  the theme name, e.g. 'brand'
   * @param {any} def  { mode?, palette?, tokens?, monochrome?, accessibility? }
   * @returns {typeof ApexCharts}
   */ function registerTheme1(name2, def) {
                registerTheme(name2, def);
                return _ApexCharts;
            }
        },
        {
            key: "unregisterTheme",
            value: /**
   * Remove a theme registered via registerTheme. Charts referencing it by
   * `theme.name` fall back to the built-in defaults on their next render.
   * Intended for tests and hot-reload flows.
   * @param {string} name
   * @returns {typeof ApexCharts}
   */ function unregisterTheme1(name2) {
                unregisterTheme(name2);
                return _ApexCharts;
            }
        },
        {
            key: "registerEasing",
            value: /**
   * Cadence (#6): register a named easing function referenceable via
   * `chart.animations.easing: '<name>'`. `fn` maps linear progress t in [0,1]
   * to eased progress (back/elastic curves may overshoot 1).
   *
   * @param {string} name  the easing name, e.g. 'bounce'
   * @param {(t:number)=>number} fn
   * @returns {typeof ApexCharts}
   */ function registerEasing1(name2, fn) {
                registerEasing(name2, fn);
                return _ApexCharts;
            }
        },
        {
            key: "crossfilter",
            value: /**
   * Linked Views (#4) Phase 2: get-or-create a crossfilter coordinator by id.
   * Register one shared record set, then let each chart declare a dimension +
   * reduction under `chart.link`. Selecting in one chart re-aggregates the
   * others over the filtered subset.
   *
   * Lives in core (always callable) but the engine ships in the `link` feature
   * (`import 'apexcharts/features/link'`, included in the full bundle); without
   * it this warns and returns null so the engine shakes out when unused.
   *
   * @param {{ id: string, records?: any[] }} opts
   * @returns {any} the coordinator handle, or null if the feature is absent
   */ function crossfilter(opts) {
                var factory = /** @type {any} */ _ApexCharts._crossfilterFactory;
                if (!factory) {
                    console.warn("[apexcharts] ApexCharts.crossfilter(...) requires the link feature: import 'apexcharts/features/link'.");
                    return null;
                }
                var coordinator = factory(opts);
                reevaluateLicenseAcrossCharts();
                return coordinator;
            }
        },
        {
            key: "getCrossfilter",
            value: /**
   * Look up an existing crossfilter coordinator by id (null if none / feature
   * absent).
   * @param {string} id
   * @returns {any}
   */ function getCrossfilter(id) {
                var get = /** @type {any} */ _ApexCharts._crossfilterGet;
                return get ? get(id) : null;
            }
        },
        {
            key: "_writeDataProps",
            value: // ─── Slice write-back stubs ─────────────────────────────────────────────────
            /**
   * Copy own DATA properties of a parse-state slice onto a live w.* slice.
   * Never Object.assign here: several w.* fields (and, historically, snapshot
   * fields) are lazy accessors, and [[Get]]-ing an accessor while copying
   * forces its deferred computation (a deep initialSeries clone plus O(n)
   * stacked-totals passes on every render/update) and can replace the live
   * accessor with a materialized value for the life of the instance.
   * @param {any} target
   * @param {any} slice
   */ function _writeDataProps(target, slice) {
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = Object.keys(slice)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var key = _step.value;
                        var d = Object.getOwnPropertyDescriptor(slice, key);
                        if (d && "value" in d) target[key] = d.value;
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
            }
        }
    ]);
    return _ApexCharts;
}();
/**
 * Static Perspectives helpers (decode/fromURL), populated by the perspectives
 * feature when imported (`import 'apexcharts/features/perspectives'`); null
 * otherwise. Declared here as a placeholder so core stays free of the
 * Perspectives module while the assignment in the feature file type-checks.
 * @type {any}
 */ __publicField(_ApexCharts, "perspectives", null);
var ApexCharts = _ApexCharts;
var Exports = /*#__PURE__*/ function() {
    "use strict";
    function Exports(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Exports);
        this.w = w;
        this.ctx = ctx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Exports, [
        {
            /**
   * @param {string} svgString
   */ key: "svgStringToNode",
            value: function svgStringToNode(svgString) {
                var parser = new DOMParser();
                var svgDoc = parser.parseFromString(svgString, "image/svg+xml");
                return svgDoc.documentElement;
            }
        },
        {
            /**
   * @param {any} svg
   * @param {number} scale
   */ key: "scaleSvgNode",
            value: function scaleSvgNode(svg, scale) {
                var svgWidth = parseFloat(svg.getAttributeNS(null, "width"));
                var svgHeight = parseFloat(svg.getAttributeNS(null, "height"));
                svg.setAttributeNS(null, "width", svgWidth * scale);
                svg.setAttributeNS(null, "height", svgHeight * scale);
                svg.setAttributeNS(null, "viewBox", "0 0 " + svgWidth + " " + svgHeight);
            }
        },
        {
            /**
   * Inline any Strata canvas series layer into the clone as an SVG `<image>`.
   * A serialized `<canvas>` loses its bitmap, so a canvas-mode export would drop
   * the series; an `<image>` carrying the canvas `toDataURL()` preserves it in
   * place. Because it replaces the `<foreignObject>` at the same DOM position,
   * the grid-behind / annotations-in-front z-order is retained automatically.
   * No-op in SVG mode (no series canvas present).
   * @param {any} clonedNode the cloned elWrap about to be serialized
   */ key: "inlineCanvasLayers",
            value: function inlineCanvasLayers(clonedNode) {
                var w = this.w;
                var XLINK = "http://www.w3.org/1999/xlink";
                var origCanvases = w.dom.elWrap.querySelectorAll(".apexcharts-series-canvas");
                if (!origCanvases.length) return;
                var clonedFOs = clonedNode.querySelectorAll(".apexcharts-canvas-series");
                for(var i = 0; i < origCanvases.length && i < clonedFOs.length; i++){
                    var dataURL = void 0;
                    try {
                        dataURL = /** @type {HTMLCanvasElement} */ origCanvases[i].toDataURL();
                    } catch (e) {
                        continue;
                    }
                    var fo = clonedFOs[i];
                    var img = document.createElementNS(SVGNS$2, "image");
                    img.setAttribute("x", fo.getAttribute("x") || "0");
                    img.setAttribute("y", fo.getAttribute("y") || "0");
                    img.setAttribute("width", fo.getAttribute("width") || "0");
                    img.setAttribute("height", fo.getAttribute("height") || "0");
                    img.setAttribute("href", dataURL);
                    img.setAttributeNS(XLINK, "xlink:href", dataURL);
                    if (fo.parentNode) fo.parentNode.replaceChild(img, fo);
                }
            }
        },
        {
            /**
   * `querySelectorAll` as a typed array. Both HTML and SVG elements carry
   * `style` / `classList`, but `NodeListOf<Element>` does not.
   * @param {ParentNode} root
   * @param {string} selector
   * @returns {Array<HTMLElement | SVGElement>}
   */ key: "queryStyleable",
            value: function queryStyleable(root, selector) {
                return /** @type {Array<HTMLElement | SVGElement>} */ Array.prototype.slice.call(root.querySelectorAll(selector));
            }
        },
        {
            /**
   * Applies `styles` only where the element has no inline value for that
   * property yet.
   *
   * The rules being re-applied here came from a stylesheet, so anything a
   * module set inline has to keep winning exactly like it does in the live
   * DOM: `legend.fontSize` (Legend.js) and the heatmap gradient legend's
   * deliberate `display`/`overflow`/`padding` overrides on the legend wrap
   * (HeatmapGradientLegend.js) would otherwise be clobbered in the export.
   * @param {HTMLElement | SVGElement} el
   * @param {Record<string, string>} styles
   */ key: "setStyleDefaults",
            value: function setStyleDefaults(el, styles) {
                Object.keys(styles).forEach(function(prop) {
                    if (el.style.getPropertyValue(prop) === "") {
                        el.style.setProperty(prop, styles[prop]);
                    }
                });
            }
        },
        {
            /**
   * Re-applies, as inline styles on the clone, the rules that used to reach
   * the exported SVG through an injected `<style>` block. A strict
   * Content-Security-Policy without `'unsafe-inline'` blocks that block and
   * breaks the export, so the export must not depend on one. See #5146.
   *
   * Mirrors `src/assets/apexcharts-legend.css`. Interaction-only rules
   * (`cursor`, `pointer-events`) are carried over for parity even though they
   * do nothing in a static image; the layout rules are what matter.
   * @param {HTMLElement} clonedNode the cloned elWrap about to be serialized
   */ key: "applyExportStyles",
            value: function applyExportStyles(clonedNode) {
                var _this = this;
                var w = this.w;
                this.queryStyleable(clonedNode, "style").forEach(function(el) {
                    return el.remove();
                });
                this.queryStyleable(clonedNode, [
                    ".apexcharts-tooltip",
                    ".apexcharts-toolbar",
                    ".apexcharts-xaxistooltip",
                    ".apexcharts-yaxistooltip",
                    ".apexcharts-xcrosshairs",
                    ".apexcharts-ycrosshairs",
                    ".apexcharts-zoom-rect",
                    ".apexcharts-selection-rect"
                ].join(", ")).forEach(function(el) {
                    el.style.setProperty("display", "none", "important");
                });
                this.queryStyleable(clonedNode, ".apexcharts-flip-y").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        transform: "scaleY(-1) translateY(-100%)",
                        "transform-origin": "top",
                        "transform-box": "fill-box"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-flip-x").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        transform: "scaleX(-1)",
                        "transform-origin": "center",
                        "transform-box": "fill-box"
                    });
                });
                if (!w.config.legend.show || !w.dom.elLegendWrap || !w.dom.elLegendWrap.children.length) {
                    return;
                }
                this.queryStyleable(clonedNode, ".apexcharts-legend").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        display: "flex",
                        overflow: "auto",
                        padding: "0 10px"
                    });
                    var cl = el.classList;
                    var isSide = cl.contains("apx-legend-position-left") || cl.contains("apx-legend-position-right");
                    var isTopOrBottom = cl.contains("apx-legend-position-top") || cl.contains("apx-legend-position-bottom");
                    if (cl.contains("apexcharts-legend-group-horizontal")) {
                        _this.setStyleDefaults(el, {
                            "flex-direction": "column"
                        });
                    }
                    if (isSide) {
                        _this.setStyleDefaults(el, {
                            "flex-direction": "column",
                            bottom: "0"
                        });
                    }
                    if (isTopOrBottom) {
                        _this.setStyleDefaults(el, {
                            "flex-wrap": "wrap"
                        });
                    }
                    if (isSide || isTopOrBottom && cl.contains("apexcharts-align-left")) {
                        _this.setStyleDefaults(el, {
                            "justify-content": "flex-start",
                            "align-items": "flex-start"
                        });
                    } else if (isTopOrBottom && cl.contains("apexcharts-align-center")) {
                        _this.setStyleDefaults(el, {
                            "justify-content": "center",
                            "align-items": "center"
                        });
                    } else if (isTopOrBottom && cl.contains("apexcharts-align-right")) {
                        _this.setStyleDefaults(el, {
                            "justify-content": "flex-end",
                            "align-items": "flex-end"
                        });
                    }
                });
                this.queryStyleable(clonedNode, ".apexcharts-legend-group").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        display: "flex"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-legend-group-vertical").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        "flex-direction": "column-reverse"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-legend-series").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        cursor: el.classList.contains("apexcharts-no-click") ? "auto" : "pointer",
                        "line-height": "normal",
                        display: "flex",
                        "align-items": "center"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-legend-text").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        position: "relative",
                        "font-size": "14px"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-legend-marker").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        position: "relative",
                        display: "flex",
                        "align-items": "center",
                        "justify-content": "center",
                        cursor: "pointer",
                        "margin-right": "1px"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-inactive-legend").forEach(function(el) {
                    _this.setStyleDefaults(el, {
                        opacity: "0.45"
                    });
                });
                this.queryStyleable(clonedNode, ".apexcharts-legend .apexcharts-hidden-zero-series, .apexcharts-legend .apexcharts-hidden-null-series").forEach(function(el) {
                    el.style.setProperty("display", "none", "important");
                });
            }
        },
        {
            /**
   * @param {number} [_scale]
   */ key: "getSvgString",
            value: function getSvgString(_scale) {
                var _this = this;
                return new Promise(function(resolve) {
                    var w = _this.w;
                    var scale = _scale || w.config.chart.toolbar.export.scale || w.config.chart.toolbar.export.width / w.globals.svgWidth;
                    if (!scale) {
                        scale = 1;
                    }
                    var width = w.globals.svgWidth * scale;
                    var height = w.globals.svgHeight * scale;
                    var clonedNode = /** @type {HTMLElement} */ w.dom.elWrap.cloneNode(true);
                    clonedNode.style.width = width + "px";
                    clonedNode.style.height = height + "px";
                    _this.inlineCanvasLayers(clonedNode);
                    _this.applyExportStyles(clonedNode);
                    var serializedNode = new XMLSerializer().serializeToString(clonedNode);
                    var svgString = '\n        <svg xmlns="http://www.w3.org/2000/svg"\n          version="1.1"\n          xmlns:xlink="http://www.w3.org/1999/xlink"\n          class="apexcharts-svg"\n          xmlns:data="ApexChartsNS"\n          transform="translate(0, 0)"\n          width="'.concat(w.globals.svgWidth, 'px" height="').concat(w.globals.svgHeight, 'px">\n          <foreignObject width="100%" height="100%">\n            <div xmlns="http://www.w3.org/1999/xhtml" style="width:').concat(width, "px; height:").concat(height, 'px;">\n              ').concat(serializedNode, "\n            </div>\n          </foreignObject>\n        </svg>\n      ");
                    var svgNode = _this.svgStringToNode(svgString);
                    if (scale !== 1) {
                        _this.scaleSvgNode(svgNode, scale);
                    }
                    _this.convertImagesToBase64(svgNode).then(function() {
                        svgString = new XMLSerializer().serializeToString(svgNode);
                        resolve(svgString.replace(/&nbsp;/g, "&#160;"));
                    });
                });
            }
        },
        {
            /**
   * @param {any} svgNode
   */ key: "convertImagesToBase64",
            value: function convertImagesToBase64(svgNode) {
                var _this = this;
                var images = svgNode.getElementsByTagName("image");
                var promises = Array.from(images).map(function(img) {
                    var href = img.getAttributeNS("http://www.w3.org/1999/xlink", "href");
                    if (href && !href.startsWith("data:")) {
                        return _this.getBase64FromUrl(href).then(function(base64) {
                            img.setAttributeNS("http://www.w3.org/1999/xlink", "href", base64);
                        }).catch(function(error) {
                            console.error("Error converting image to base64:", error);
                        });
                    }
                    return Promise.resolve();
                });
                return Promise.all(promises);
            }
        },
        {
            /**
   * @param {string} url
   */ key: "getBase64FromUrl",
            value: function getBase64FromUrl(url) {
                if (Environment.isSSR()) return Promise.resolve(url);
                return new Promise(function(resolve, reject) {
                    var img = new Image();
                    img.crossOrigin = "Anonymous";
                    img.onload = function() {
                        var canvas = document.createElement("canvas");
                        canvas.width = img.width;
                        canvas.height = img.height;
                        var ctx = canvas.getContext("2d");
                        if (ctx) ctx.drawImage(img, 0, 0);
                        resolve(canvas.toDataURL());
                    };
                    img.onerror = reject;
                    img.src = url;
                });
            }
        },
        {
            key: "svgUrl",
            value: function svgUrl() {
                var _this = this;
                return new Promise(function(resolve) {
                    _this.getSvgString().then(function(svgData) {
                        var svgBlob = new Blob([
                            svgData
                        ], {
                            type: "image/svg+xml;charset=utf-8"
                        });
                        resolve(URL.createObjectURL(svgBlob));
                    });
                });
            }
        },
        {
            /**
   * @param {Record<string, any> | undefined} options
   */ key: "dataURI",
            value: function dataURI(options2) {
                var _this = this;
                if (Environment.isSSR()) return Promise.resolve({
                    imgURI: ""
                });
                return new Promise(function(resolve) {
                    var w = _this.w;
                    var scale = options2 ? options2.scale || options2.width / w.globals.svgWidth : 1;
                    var canvas = document.createElement("canvas");
                    canvas.width = w.globals.svgWidth * scale;
                    canvas.height = parseInt(w.dom.elWrap.style.height, 10) * scale;
                    var canvasBg = w.config.chart.background === "transparent" || !w.config.chart.background ? "#fff" : w.config.chart.background;
                    var ctx = canvas.getContext("2d");
                    if (!ctx) return;
                    ctx.fillStyle = canvasBg;
                    ctx.fillRect(0, 0, canvas.width, canvas.height);
                    _this.getSvgString(scale).then(function(svgData) {
                        var svgUrl = "data:image/svg+xml," + encodeURIComponent(svgData);
                        var img = new Image();
                        img.crossOrigin = "anonymous";
                        img.onload = function() {
                            ctx.drawImage(img, 0, 0);
                            var edgeCanvas = canvas;
                            if (edgeCanvas.msToBlob) {
                                var blob = edgeCanvas.msToBlob();
                                resolve({
                                    blob: blob
                                });
                            } else {
                                var imgURI = canvas.toDataURL("image/png");
                                resolve({
                                    imgURI: imgURI
                                });
                            }
                        };
                        img.src = svgUrl;
                    });
                });
            }
        },
        {
            key: "exportToSVG",
            value: function exportToSVG() {
                var _this = this;
                this.svgUrl().then(function(url) {
                    _this.triggerDownload(url, _this.w.config.chart.toolbar.export.svg.filename, ".svg");
                });
            }
        },
        {
            key: "exportToPng",
            value: function exportToPng() {
                var _this = this;
                var scale = this.w.config.chart.toolbar.export.scale;
                var width = this.w.config.chart.toolbar.export.width;
                var option = scale ? {
                    scale: scale
                } : width ? {
                    width: width
                } : void 0;
                this.dataURI(option).then(function(param) {
                    var imgURI = param.imgURI, blob = param.blob;
                    if (blob) {
                        navigator.msSaveOrOpenBlob(blob, _this.w.globals.chartID + ".png");
                    } else {
                        _this.triggerDownload(imgURI, _this.w.config.chart.toolbar.export.png.filename, ".png");
                    }
                });
            }
        },
        {
            /** @param {{ series?: any, fileName?: any, columnDelimiter?: string, lineDelimiter?: string }} opts */ key: "exportToCSV",
            value: function exportToCSV(param) {
                var _this = this;
                var series = param.series, fileName = param.fileName, _param_columnDelimiter = param.columnDelimiter, columnDelimiter = _param_columnDelimiter === void 0 ? "," : _param_columnDelimiter, _param_lineDelimiter = param.lineDelimiter, lineDelimiter = _param_lineDelimiter === void 0 ? "\n" : _param_lineDelimiter;
                var _Math;
                var w = this.w;
                if (!series) series = w.config.series;
                var columns = [];
                var rows = [];
                var result = "";
                var universalBOM = "\uFEFF";
                var gSeries = w.seriesData.series.map(function(s, i) {
                    return w.globals.collapsedSeriesIndices.indexOf(i) === -1 ? s : [];
                });
                var csvSafe = function csvSafe(val) {
                    if (val == null || Utils$1.isNumber(val)) return val;
                    var s = String(val);
                    return /^[=+\-@\t\r]/.test(s) ? "'".concat(s) : s;
                };
                var getFormattedCategory = function getFormattedCategory(cat) {
                    if (typeof w.config.chart.toolbar.export.csv.categoryFormatter === "function") {
                        return w.config.chart.toolbar.export.csv.categoryFormatter(cat);
                    }
                    if (w.config.xaxis.type === "datetime" && String(cat).length >= 10) {
                        return new Date(cat).toDateString();
                    }
                    return Utils$1.isNumber(cat) ? cat : csvSafe(cat.split(columnDelimiter).join(""));
                };
                var getFormattedValue = function getFormattedValue(value) {
                    return typeof w.config.chart.toolbar.export.csv.valueFormatter === "function" ? w.config.chart.toolbar.export.csv.valueFormatter(value) : csvSafe(value);
                };
                var seriesMaxDataLength = (_Math = Math).max.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(series.map(function(s) {
                    return s.data ? s.data.length : 0;
                })));
                var dataFormat = new Data(this.w);
                var axesUtils = new AxesUtils(this.w, {
                    theme: this.ctx.theme,
                    timeScale: this.ctx.timeScale
                });
                var getCat = function getCat(i) {
                    var cat = "";
                    if (!w.globals.axisCharts) {
                        cat = w.config.labels[i];
                    } else {
                        if (w.config.xaxis.type === "category" || w.config.xaxis.convertedCatToNumeric) {
                            if (w.globals.isBarHorizontal) {
                                var lbFormatter = w.formatters.yLabelFormatters[0];
                                var sr = new Series(_this.ctx.w);
                                var activeSeries = sr.getActiveConfigSeriesIndex();
                                cat = lbFormatter(w.labelData.labels[i], {
                                    seriesIndex: activeSeries,
                                    dataPointIndex: i,
                                    w: w
                                });
                            } else {
                                cat = axesUtils.getLabel(w.labelData.labels, w.labelData.timescaleLabels, 0, i).text;
                            }
                        }
                        if (w.config.xaxis.type === "datetime") {
                            if (w.config.xaxis.categories.length) {
                                cat = w.config.xaxis.categories[i];
                            } else if (w.config.labels.length) {
                                cat = w.config.labels[i];
                            }
                        }
                    }
                    if (cat === null) return "nullvalue";
                    if (Array.isArray(cat)) {
                        cat = cat.join(" ");
                    }
                    return Utils$1.isNumber(cat) ? cat : cat.split(columnDelimiter).join("");
                };
                var getEmptyDataForCsvColumn = function getEmptyDataForCsvColumn() {
                    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Array(seriesMaxDataLength)).map(function() {
                        return "";
                    });
                };
                var handleAxisRowsColumns = function handleAxisRowsColumns(s, sI) {
                    var _a, _b, _c, _d, _e, _f;
                    if (columns.length && sI === 0) {
                        rows.push(columns.join(columnDelimiter));
                    }
                    if (s.data) {
                        var rowData = s.data.length ? s.data : getEmptyDataForCsvColumn();
                        for(var i = 0; i < rowData.length; i++){
                            columns = [];
                            var cat = getCat(i);
                            if (cat === "nullvalue") continue;
                            if (!cat) {
                                if (dataFormat.isFormatXY()) {
                                    cat = series[sI].data[i].x;
                                } else if (dataFormat.isFormat2DArray()) {
                                    cat = series[sI].data[i] ? series[sI].data[i][0] : "";
                                }
                            }
                            if (sI === 0) {
                                columns.push(getFormattedCategory(cat));
                                for(var ci = 0; ci < w.seriesData.series.length; ci++){
                                    var value = dataFormat.isFormatXY() ? (_a = series[ci].data[i]) == null ? void 0 : _a.y : gSeries[ci][i];
                                    columns.push(getFormattedValue(value));
                                }
                            }
                            if (w.config.chart.type === "candlestick" || s.type && s.type === "candlestick") {
                                columns.pop();
                                columns.push(w.candleData.seriesCandleO[sI][i]);
                                columns.push(w.candleData.seriesCandleH[sI][i]);
                                columns.push(w.candleData.seriesCandleL[sI][i]);
                                columns.push(w.candleData.seriesCandleC[sI][i]);
                            }
                            if (w.config.chart.type === "boxPlot" || s.type && s.type === "boxPlot") {
                                columns.pop();
                                columns.push(w.candleData.seriesCandleO[sI][i]);
                                columns.push(w.candleData.seriesCandleH[sI][i]);
                                columns.push(w.candleData.seriesCandleM[sI][i]);
                                columns.push(w.candleData.seriesCandleL[sI][i]);
                                columns.push(w.candleData.seriesCandleC[sI][i]);
                            }
                            if (w.config.chart.type === "rangeBar") {
                                columns.pop();
                                columns.push(w.rangeData.seriesRangeStart[sI][i]);
                                columns.push(w.rangeData.seriesRangeEnd[sI][i]);
                            }
                            if (w.config.chart.type === "violin" || s.type && s.type === "violin") {
                                columns.pop();
                                columns.push((_b = w.violinData.seriesViolinMin[sI]) == null ? void 0 : _b[i]);
                                columns.push((_c = w.violinData.seriesViolinMax[sI]) == null ? void 0 : _c[i]);
                                columns.push((_f = (_e = (_d = w.violinData.seriesViolinPoints[sI]) == null ? void 0 : _d[i]) == null ? void 0 : _e.length) != null ? _f : 0);
                            }
                            if (columns.length) {
                                rows.push(columns.join(columnDelimiter));
                            }
                        }
                    }
                };
                var handleUnequalXValues = function handleUnequalXValues() {
                    var categories = /* @__PURE__ */ new Set();
                    var data = {};
                    series.forEach(function(s, sI) {
                        s == null ? void 0 : s.data.forEach(function(dataItem) {
                            var cat, value;
                            if (dataFormat.isFormatXY()) {
                                cat = dataItem.x;
                                value = dataItem.y;
                            } else if (dataFormat.isFormat2DArray()) {
                                cat = dataItem[0];
                                value = dataItem[1];
                            } else {
                                return;
                            }
                            if (!/** @type {Record<string,any>} */ data[cat]) {
                                data[cat] = Array(series.length).fill("");
                            }
                            data[cat][sI] = getFormattedValue(value);
                            categories.add(cat);
                        });
                    });
                    if (columns.length) {
                        rows.push(columns.join(columnDelimiter));
                    }
                    Array.from(categories).sort().forEach(function(cat) {
                        var values = /** @type {Record<string,any>} */ data[cat];
                        rows.push([
                            getFormattedCategory(cat)
                        ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(values)).join(columnDelimiter));
                    });
                };
                columns.push(w.config.chart.toolbar.export.csv.headerCategory);
                if (w.config.chart.type === "boxPlot") {
                    columns.push("minimum");
                    columns.push("q1");
                    columns.push("median");
                    columns.push("q3");
                    columns.push("maximum");
                } else if (w.config.chart.type === "candlestick") {
                    columns.push("open");
                    columns.push("high");
                    columns.push("low");
                    columns.push("close");
                } else if (w.config.chart.type === "rangeBar") {
                    columns.push("minimum");
                    columns.push("maximum");
                } else if (w.config.chart.type === "violin") {
                    columns.push("minimum");
                    columns.push("maximum");
                    columns.push("observations");
                } else {
                    series.map(function(s, sI) {
                        var sname = (s.name ? s.name : "series-".concat(sI)) + "";
                        if (w.globals.axisCharts) {
                            columns.push(sname.split(columnDelimiter).join("") ? sname.split(columnDelimiter).join("") : "series-".concat(sI));
                        }
                    });
                }
                if (!w.globals.axisCharts) {
                    columns.push(w.config.chart.toolbar.export.csv.headerValue);
                    rows.push(columns.join(columnDelimiter));
                }
                if (!w.globals.allSeriesHasEqualX && w.globals.axisCharts && !w.config.xaxis.categories.length && !w.config.labels.length) {
                    handleUnequalXValues();
                } else {
                    series.map(function(s, sI) {
                        if (w.globals.axisCharts) {
                            handleAxisRowsColumns(s, sI);
                        } else {
                            columns = [];
                            columns.push(getFormattedCategory(w.labelData.labels[sI]));
                            columns.push(getFormattedValue(gSeries[sI]));
                            rows.push(columns.join(columnDelimiter));
                        }
                    });
                }
                result += rows.join(lineDelimiter);
                this.triggerDownload("data:text/csv; charset=utf-8," + encodeURIComponent(universalBOM + result), fileName ? fileName : w.config.chart.toolbar.export.csv.filename, ".csv");
            }
        },
        {
            /**
   * @param {string} href
   * @param {string} filename
   * @param {string} ext
   */ key: "triggerDownload",
            value: function triggerDownload(href, filename, ext) {
                if (Environment.isSSR()) return;
                var downloadLink = document.createElement("a");
                downloadLink.href = href;
                downloadLink.download = (filename ? filename : this.w.globals.chartID) + ext;
                document.body.appendChild(downloadLink);
                downloadLink.click();
                document.body.removeChild(downloadLink);
            }
        }
    ]);
    return Exports;
}();
ApexCharts.registerFeatures({
    exports: Exports
});
var apexchartsLegendCSS = ".apexcharts-flip-y {\n  transform: scaleY(-1) translateY(-100%);\n  transform-origin: top;\n  transform-box: fill-box;\n}\n.apexcharts-flip-x {\n  transform: scaleX(-1);\n  transform-origin: center;\n  transform-box: fill-box;\n}\n.apexcharts-legend {\n  display: flex;\n  overflow: auto;\n  padding: 0 10px;\n}\n.apexcharts-legend.apexcharts-legend-group-horizontal {\n  flex-direction: column;\n}\n.apexcharts-legend-group {\n  display: flex;\n}\n.apexcharts-legend-group-vertical {\n  flex-direction: column-reverse;\n}\n.apexcharts-legend.apx-legend-position-bottom, .apexcharts-legend.apx-legend-position-top {\n  flex-wrap: wrap\n}\n.apexcharts-legend.apx-legend-position-right, .apexcharts-legend.apx-legend-position-left {\n  flex-direction: column;\n  bottom: 0;\n}\n.apexcharts-legend.apx-legend-position-bottom.apexcharts-align-left, .apexcharts-legend.apx-legend-position-top.apexcharts-align-left, .apexcharts-legend.apx-legend-position-right, .apexcharts-legend.apx-legend-position-left {\n  justify-content: flex-start;\n  align-items: flex-start;\n}\n.apexcharts-legend.apx-legend-position-bottom.apexcharts-align-center, .apexcharts-legend.apx-legend-position-top.apexcharts-align-center {\n  justify-content: center;\n  align-items: center;\n}\n.apexcharts-legend.apx-legend-position-bottom.apexcharts-align-right, .apexcharts-legend.apx-legend-position-top.apexcharts-align-right {\n  justify-content: flex-end;\n  align-items: flex-end;\n}\n.apexcharts-legend-series {\n  cursor: pointer;\n  line-height: normal;\n  display: flex;\n  align-items: center;\n}\n.apexcharts-legend-text {\n  position: relative;\n  font-size: 14px;\n}\n.apexcharts-legend-text *, .apexcharts-legend-marker * {\n  pointer-events: none;\n}\n.apexcharts-legend-marker {\n  position: relative;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  cursor: pointer;\n  margin-right: 1px;\n}\n\n.apexcharts-legend-series.apexcharts-no-click {\n  cursor: auto;\n}\n.apexcharts-legend .apexcharts-hidden-zero-series, .apexcharts-legend .apexcharts-hidden-null-series {\n  display: none !important;\n}\n.apexcharts-inactive-legend {\n  opacity: 0.45;\n} ";
var Helpers$3 = /*#__PURE__*/ function() {
    "use strict";
    function Helpers2(lgCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Helpers2);
        this.w = lgCtx.w;
        this.lgCtx = lgCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Helpers2, [
        {
            key: "getLegendStyles",
            value: function getLegendStyles() {
                if (Environment.isSSR()) return null;
                var stylesheet = document.createElement("style");
                stylesheet.setAttribute("type", "text/css");
                var nonce = this.w.config.chart.nonce;
                if (nonce) {
                    stylesheet.setAttribute("nonce", nonce);
                }
                var rule = document.createTextNode(apexchartsLegendCSS);
                stylesheet.appendChild(rule);
                return stylesheet;
            }
        },
        {
            key: "getLegendDimensions",
            value: function getLegendDimensions() {
                var w = this.w;
                var currLegendsWrap = w.dom.baseEl.querySelector(".apexcharts-legend");
                if (!currLegendsWrap) {
                    return {
                        clwh: 0,
                        clww: 0
                    };
                }
                var _currLegendsWrap_getBoundingClientRect = currLegendsWrap.getBoundingClientRect(), currLegendsWrapWidth = _currLegendsWrap_getBoundingClientRect.width, currLegendsWrapHeight = _currLegendsWrap_getBoundingClientRect.height;
                return {
                    clwh: currLegendsWrapHeight,
                    clww: currLegendsWrapWidth
                };
            }
        },
        {
            key: "appendToForeignObject",
            value: function appendToForeignObject() {
                var _a;
                var legendStyles = this.getLegendStyles();
                if (this.w.config.chart.injectStyleSheet !== false && legendStyles) {
                    (_a = this.w.dom.elLegendForeign) == null ? void 0 : _a.appendChild(legendStyles);
                }
            }
        },
        {
            /**
   * @param {number} seriesCnt
   * @param {boolean} isHidden
   */ key: "toggleDataSeries",
            value: function toggleDataSeries(seriesCnt, isHidden) {
                var _this = this;
                var _a, _b, _c;
                var w = this.w;
                if (w.globals.axisCharts || w.config.chart.type === "radialBar") {
                    w.globals.resized = true;
                    var seriesEl = null;
                    var realIndex = null;
                    w.globals.risingSeries = [];
                    if (w.globals.axisCharts) {
                        seriesEl = (_a = Array.prototype.find.call(w.dom.baseEl.querySelectorAll(".apexcharts-series"), function(el) {
                            return el.getAttribute("data:realIndex") === String(seriesCnt);
                        })) != null ? _a : null;
                        if (!seriesEl) return;
                        realIndex = parseInt((_b = seriesEl.getAttribute("data:realIndex")) != null ? _b : "", 10);
                    } else {
                        seriesEl = w.dom.baseEl.querySelector(".apexcharts-series[rel='".concat(seriesCnt + 1, "']"));
                        if (!seriesEl) return;
                        realIndex = parseInt((_c = seriesEl.getAttribute("rel")) != null ? _c : "", 10) - 1;
                    }
                    if (isHidden) {
                        var seriesToMakeVisible = [
                            {
                                cs: w.globals.collapsedSeries,
                                csi: w.globals.collapsedSeriesIndices
                            },
                            {
                                cs: w.globals.ancillaryCollapsedSeries,
                                csi: w.globals.ancillaryCollapsedSeriesIndices
                            }
                        ];
                        seriesToMakeVisible.forEach(function(r) {
                            var cs = /** @type {any} */ r.cs;
                            var csi = /** @type {any} */ r.csi;
                            _this.riseCollapsedSeries(cs, csi, /** @type {number} */ realIndex);
                        });
                    } else {
                        this.hideSeries({
                            seriesEl: seriesEl,
                            realIndex: realIndex
                        });
                    }
                    if (w.config.chart.accessibility.enabled) {
                        var legendItem = w.dom.baseEl.querySelector('.apexcharts-legend-series[rel="'.concat(seriesCnt + 1, '"]'));
                        if (legendItem) {
                            var isCollapsed = w.globals.collapsedSeriesIndices.includes(realIndex) || w.globals.ancillaryCollapsedSeriesIndices.includes(realIndex);
                            legendItem.setAttribute("aria-pressed", isCollapsed ? "true" : "false");
                            var legendTextEl = legendItem.querySelector(".apexcharts-legend-text");
                            var seriesName = legendTextEl ? legendTextEl.textContent : w.seriesData.seriesNames[seriesCnt];
                            var statusText = isCollapsed ? "hidden" : "visible";
                            legendItem.setAttribute("aria-label", "".concat(seriesName, ", ").concat(statusText, ". Press Enter or Space to toggle."));
                        }
                    }
                } else {
                    w.globals.resized = true;
                    w.globals.risingSeries = [];
                    if (isHidden) {
                        this.riseCollapsedSeries(w.globals.collapsedSeries, w.globals.collapsedSeriesIndices, seriesCnt);
                    } else {
                        var series = this.getSeriesAfterCollapsing({
                            realIndex: seriesCnt
                        });
                        this.lgCtx.updateSeries(series, w.config.chart.animations.dynamicAnimation.enabled);
                    }
                    if (w.config.chart.accessibility.enabled) {
                        var legendItem1 = w.dom.baseEl.querySelector('.apexcharts-legend-series[rel="'.concat(seriesCnt + 1, '"]'));
                        if (legendItem1) {
                            var isCollapsed1 = w.globals.collapsedSeriesIndices.includes(seriesCnt);
                            legendItem1.setAttribute("aria-pressed", isCollapsed1 ? "true" : "false");
                            var legendTextEl1 = legendItem1.querySelector(".apexcharts-legend-text");
                            var seriesName1 = legendTextEl1 ? legendTextEl1.textContent : w.seriesData.seriesNames[seriesCnt];
                            var statusText1 = isCollapsed1 ? "hidden" : "visible";
                            legendItem1.setAttribute("aria-label", "".concat(seriesName1, ", ").concat(statusText1, ". Press Enter or Space to toggle."));
                        }
                    }
                }
            }
        },
        {
            /**
   * Non-axis "slice" container. A pie/donut/polarArea slice is normally a
   * top-level series element (numeric form: `series = [n, n, n]`), but object
   * form (`series = [{ data: [{ x, y, drilldown }, ...] }]`, which pie/donut
   * drilldown requires) packs every slice as a data point inside a single
   * series. Return the array that actually holds the slice values so a
   * slice/legend index addresses the right thing. Scoped to the pie family so
   * unit charts (which share this non-axis path) are untouched.
   * @param {any[]} series
   * @returns {any[]}
   */ key: "_nonAxisSliceContainer",
            value: function _nonAxisSliceContainer(series) {
                var type = this.w.config.chart.type;
                if ((type === "pie" || type === "donut" || type === "polarArea" || type === "sunburst") && series.length === 1 && series[0] && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(series[0]) === "object" && Array.isArray(series[0].data)) {
                    return series[0].data;
                }
                return series;
            }
        },
        {
            /**
   * Read a non-axis slice value (handles `{ x, y }` data points and plain
   * numbers).
   * @param {any} sliceEntry
   * @returns {number}
   */ key: "_readSliceValue",
            value: function _readSliceValue(sliceEntry) {
                if (this.w.config.chart.type === "unit" && sliceEntry && Array.isArray(sliceEntry.data)) {
                    return sliceEntry.data;
                }
                return sliceEntry && (typeof sliceEntry === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(sliceEntry)) === "object" ? sliceEntry.y : sliceEntry;
            }
        },
        {
            /**
   * Write a non-axis slice value in place, preserving `{ x, drilldown, ... }`
   * on object data points.
   * @param {any[]} container
   * @param {number} i
   * @param {number} value
   */ key: "_writeSliceValue",
            value: function _writeSliceValue(container, i, value) {
                var entry = container[i];
                if (this.w.config.chart.type === "unit" && entry && Array.isArray(entry.data)) {
                    entry.data = Array.isArray(value) ? value : [];
                    return;
                }
                if (entry && (typeof entry === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(entry)) === "object") {
                    entry.y = value;
                } else {
                    container[i] = value;
                }
            }
        },
        {
            /** @param {{realIndex: any}} opts */ key: "getSeriesAfterCollapsing",
            value: function getSeriesAfterCollapsing(param) {
                var realIndex = param.realIndex;
                var _a, _b;
                var w = this.w;
                var gl = w.globals;
                var series = Utils$1.clone(w.config.series);
                if (gl.axisCharts) {
                    var yaxis = w.config.yaxis[gl.seriesYAxisReverseMap[realIndex]];
                    var collapseData = {
                        index: realIndex,
                        data: series[realIndex].data.slice(),
                        type: series[realIndex].type || w.config.chart.type,
                        // The category name pins the hide across a data update that reorders or
                        // regroups categories (e.g. a storyboard beat): the collapse is
                        // reconciled by name, not index. See Series.reconcileCollapsedByName.
                        name: (gl.seriesNames || [])[realIndex]
                    };
                    if (yaxis && yaxis.show && yaxis.showAlways) {
                        if (gl.ancillaryCollapsedSeriesIndices.indexOf(realIndex) < 0) {
                            gl.ancillaryCollapsedSeries.push(collapseData);
                            gl.ancillaryCollapsedSeriesIndices.push(realIndex);
                        }
                    } else {
                        if (gl.collapsedSeriesIndices.indexOf(realIndex) < 0) {
                            gl.collapsedSeries.push(collapseData);
                            gl.collapsedSeriesIndices.push(realIndex);
                            var removeIndexOfRising = gl.risingSeries.indexOf(realIndex);
                            gl.risingSeries.splice(removeIndexOfRising, 1);
                        }
                    }
                } else {
                    if (gl.collapsedSeriesIndices.indexOf(realIndex) < 0) {
                        var container = this._nonAxisSliceContainer(series);
                        gl.collapsedSeries.push({
                            index: realIndex,
                            // Store the original slice VALUE so it can be restored on rise. In
                            // object form this is a data point's `y`, not the whole series entry.
                            data: this._readSliceValue(container[realIndex]),
                            type: /** @type {any} */ (_b = (_a = w.config.series[realIndex]) == null ? void 0 : _a.type) != null ? _b : "line",
                            // Pin the hide by category name so it survives a regroup (see above).
                            name: (gl.seriesNames || [])[realIndex]
                        });
                        gl.collapsedSeriesIndices.push(realIndex);
                    }
                }
                var seriesCount = gl.axisCharts ? w.config.series.length : this._nonAxisSliceContainer(series).length;
                gl.allSeriesCollapsed = gl.collapsedSeries.length + gl.ancillaryCollapsedSeries.length === seriesCount;
                return this._getSeriesBasedOnCollapsedState(series);
            }
        },
        {
            /** @param {{seriesEl: any, realIndex: any}} opts */ key: "hideSeries",
            value: function hideSeries(param) {
                var seriesEl = param.seriesEl, realIndex = param.realIndex;
                var w = this.w;
                var series = this.getSeriesAfterCollapsing({
                    realIndex: realIndex
                });
                var seriesChildren = seriesEl.childNodes;
                for(var sc = 0; sc < seriesChildren.length; sc++){
                    if (seriesChildren[sc].classList.contains("apexcharts-series-markers-wrap")) {
                        if (seriesChildren[sc].classList.contains("apexcharts-hide")) {
                            seriesChildren[sc].classList.remove("apexcharts-hide");
                        } else {
                            seriesChildren[sc].classList.add("apexcharts-hide");
                        }
                    }
                }
                this.lgCtx.updateSeries(series, w.config.chart.animations.dynamicAnimation.enabled);
            }
        },
        {
            /**
   * @param {any[]} collapsedSeries
   * @param {number[]} seriesIndices
   * @param {number} realIndex
   */ key: "riseCollapsedSeries",
            value: function riseCollapsedSeries(collapsedSeries, seriesIndices, realIndex) {
                var w = this.w;
                var series = Utils$1.clone(w.config.series);
                if (collapsedSeries.length > 0) {
                    for(var c = 0; c < collapsedSeries.length; c++){
                        if (collapsedSeries[c].index === realIndex) {
                            if (w.globals.axisCharts) {
                                series[realIndex].data = collapsedSeries[c].data.slice();
                                series[realIndex].hidden = false;
                            } else {
                                var container = this._nonAxisSliceContainer(series);
                                this._writeSliceValue(container, realIndex, collapsedSeries[c].data);
                            }
                            collapsedSeries.splice(c, 1);
                            seriesIndices.splice(c, 1);
                            w.globals.risingSeries.push(realIndex);
                            c--;
                        }
                    }
                    series = this._getSeriesBasedOnCollapsedState(series);
                    this.lgCtx.updateSeries(series, w.config.chart.animations.dynamicAnimation.enabled);
                }
            }
        },
        {
            /**
   * @param {any[]} series
   */ key: "_getSeriesBasedOnCollapsedState",
            value: function _getSeriesBasedOnCollapsedState(series) {
                var _this = this;
                var w = this.w;
                var collapsed = 0;
                if (w.globals.axisCharts) {
                    series.forEach(function(s, sI) {
                        if (!(w.globals.collapsedSeriesIndices.indexOf(sI) < 0 && w.globals.ancillaryCollapsedSeriesIndices.indexOf(sI) < 0)) {
                            series[sI].data = [];
                            collapsed++;
                        }
                    });
                } else {
                    var container = this._nonAxisSliceContainer(series);
                    container.forEach(function(s, sI) {
                        if (!(w.globals.collapsedSeriesIndices.indexOf(sI) < 0)) {
                            _this._writeSliceValue(container, sI, 0);
                            collapsed++;
                        }
                    });
                }
                var seriesCount = w.globals.axisCharts ? series.length : this._nonAxisSliceContainer(series).length;
                w.globals.allSeriesCollapsed = collapsed === seriesCount;
                return series;
            }
        }
    ]);
    return Helpers2;
}();
var SVG_NS = "http://www.w3.org/2000/svg";
var HeatmapGradientLegend = /*#__PURE__*/ function() {
    "use strict";
    function HeatmapGradientLegend(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, HeatmapGradientLegend);
        this.w = w;
        this.ctx = ctx;
        this.svgEl = null;
        this.arrowEl = null;
        this.hoverValueEl = null;
        this._min = 0;
        this._max = 0;
        this._geom = null;
        this._bandHitEls = [];
        this._activeBandIndex = -1;
        this._onCellEnter = this._onCellEnter.bind(this);
        this._onCellLeave = this._onCellLeave.bind(this);
        this._onBandEnter = this._onBandEnter.bind(this);
        this._onBandLeave = this._onBandLeave.bind(this);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(HeatmapGradientLegend, [
        {
            /** Default value formatter for min/max labels and the hover tooltip. */ key: "_getFormatter",
            value: function _getFormatter() {
                var cfg = this.w.config.plotOptions.heatmap.colorScale.gradientLegend;
                if (typeof cfg.formatter === "function") return cfg.formatter;
                return function(v) {
                    if (!Number.isFinite(v)) return String(v);
                    var abs = Math.abs(v);
                    if (abs >= 1e3) return v.toFixed(0);
                    if (abs >= 10) return v.toFixed(1);
                    return v.toFixed(2);
                };
            }
        },
        {
            /**
   * Build the gradient legend DOM into `elLegendWrap`.
   * Caller is responsible for clearing the wrap first.
   */ key: "draw",
            value: function draw() {
                var _this = this;
                var _a, _b, _c, _d, _e, _f, _g, _h;
                var w = this.w;
                var elLegendWrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                if (!elLegendWrap) return;
                var cfg = w.config.plotOptions.heatmap.colorScale.gradientLegend;
                var position = w.config.legend.position;
                var isVertical = position === "left" || position === "right";
                var arrowSize = (_b = (_a = cfg.arrow) == null ? void 0 : _a.size) != null ? _b : 8;
                var arrowGutter = arrowSize + 4;
                var labelPadAlongStrip = cfg.showLabels ? 28 : 4;
                var labelPadAcrossStrip = cfg.showLabels ? 20 : 4;
                var minLabelWidth = cfg.showLabels ? 44 : 0;
                var stripLength = this._resolveStripLength(isVertical ? cfg.height : cfg.width, isVertical);
                var stripThickness = cfg.thickness;
                var svgWidth = isVertical ? Math.max(stripThickness + arrowGutter + 4, minLabelWidth) : stripLength + labelPadAlongStrip * 2;
                var svgHeight = isVertical ? stripLength + labelPadAcrossStrip * 2 : stripThickness + arrowGutter + 4;
                var verticalGroupWidth = stripThickness + arrowGutter;
                var verticalGroupLeftPad = (svgWidth - verticalGroupWidth) / 2;
                var stripX = isVertical ? position === "left" ? verticalGroupLeftPad : verticalGroupLeftPad + arrowGutter : labelPadAlongStrip;
                var stripY = isVertical ? labelPadAcrossStrip : position === "top" ? arrowGutter : 4;
                var svg = BrowserAPIs.createElementNS(SVG_NS, "svg");
                svg.setAttribute("class", "apexcharts-heatmap-gradient-legend");
                svg.setAttribute("width", String(svgWidth));
                svg.setAttribute("height", String(svgHeight));
                svg.setAttribute("overflow", "visible");
                var defs = BrowserAPIs.createElementNS(SVG_NS, "defs");
                var gradId = "apexcharts-heatmap-gradient-".concat(w.globals.cuid);
                var linearGrad = BrowserAPIs.createElementNS(SVG_NS, "linearGradient");
                linearGrad.setAttribute("id", gradId);
                if (isVertical) {
                    linearGrad.setAttribute("x1", "0");
                    linearGrad.setAttribute("y1", "1");
                    linearGrad.setAttribute("x2", "0");
                    linearGrad.setAttribute("y2", "0");
                } else {
                    linearGrad.setAttribute("x1", "0");
                    linearGrad.setAttribute("y1", "0");
                    linearGrad.setAttribute("x2", "1");
                    linearGrad.setAttribute("y2", "0");
                }
                var _this__computeStops = this._computeStops(), min = _this__computeStops.min, max = _this__computeStops.max, stops = _this__computeStops.stops, bands = _this__computeStops.bands;
                this._min = min;
                this._max = max;
                stops.forEach(function(s) {
                    var stopEl = BrowserAPIs.createElementNS(SVG_NS, "stop");
                    stopEl.setAttribute("offset", "".concat((s.percent * 100).toFixed(2), "%"));
                    stopEl.setAttribute("stop-color", s.color);
                    linearGrad.appendChild(stopEl);
                });
                defs.appendChild(linearGrad);
                svg.appendChild(defs);
                var rect = BrowserAPIs.createElementNS(SVG_NS, "rect");
                rect.setAttribute("x", String(stripX));
                rect.setAttribute("y", String(stripY));
                rect.setAttribute("width", String(isVertical ? stripThickness : stripLength));
                rect.setAttribute("height", String(isVertical ? stripLength : stripThickness));
                rect.setAttribute("rx", "2");
                rect.setAttribute("fill", "url(#".concat(gradId, ")"));
                svg.appendChild(rect);
                if (cfg.showLabels) {
                    var labelColor = ((_c = cfg.labelStyle) == null ? void 0 : _c.colors) || (Array.isArray(w.config.legend.labels.colors) ? w.config.legend.labels.colors[0] : w.config.legend.labels.colors) || w.config.chart.foreColor;
                    var labelFontSize = ((_d = cfg.labelStyle) == null ? void 0 : _d.fontSize) || "11px";
                    var labelFontFamily = ((_e = cfg.labelStyle) == null ? void 0 : _e.fontFamily) || w.config.chart.fontFamily;
                    var fmt = this._getFormatter();
                    var makeLabel = function makeLabel(text, x, y, anchor) {
                        var t = BrowserAPIs.createElementNS(SVG_NS, "text");
                        t.setAttribute("x", String(x));
                        t.setAttribute("y", String(y));
                        t.setAttribute("text-anchor", anchor);
                        t.setAttribute("dominant-baseline", "middle");
                        t.setAttribute("fill", labelColor);
                        t.setAttribute("font-size", labelFontSize);
                        if (labelFontFamily) t.setAttribute("font-family", labelFontFamily);
                        t.textContent = String(text);
                        return t;
                    };
                    if (isVertical) {
                        var midX = stripX + stripThickness / 2;
                        svg.appendChild(makeLabel(fmt(min), midX, stripY + stripLength + 10, "middle"));
                        svg.appendChild(makeLabel(fmt(max), midX, stripY - 10, "middle"));
                    } else {
                        var midY = stripY + stripThickness / 2;
                        svg.appendChild(makeLabel(fmt(min), stripX - 6, midY, "end"));
                        svg.appendChild(makeLabel(fmt(max), stripX + stripLength + 6, midY, "start"));
                    }
                }
                var arrowColor = ((_f = cfg.arrow) == null ? void 0 : _f.color) || w.config.chart.foreColor;
                var arrow = this._buildArrow(arrowSize, arrowColor, position);
                svg.appendChild(arrow);
                this.arrowEl = arrow;
                this._bandHitEls = [];
                if (w.config.legend.onItemHover.highlightDataSeries && bands.length > 0) {
                    bands.forEach(function(b) {
                        var hit = BrowserAPIs.createElementNS(SVG_NS, "rect");
                        if (isVertical) {
                            var yTop = stripY + stripLength - b.p2 * stripLength;
                            var yBot = stripY + stripLength - b.p1 * stripLength;
                            hit.setAttribute("x", String(stripX));
                            hit.setAttribute("y", String(yTop));
                            hit.setAttribute("width", String(stripThickness));
                            hit.setAttribute("height", String(Math.max(0, yBot - yTop)));
                        } else {
                            hit.setAttribute("x", String(stripX + b.p1 * stripLength));
                            hit.setAttribute("y", String(stripY));
                            hit.setAttribute("width", String(Math.max(0, (b.p2 - b.p1) * stripLength)));
                            hit.setAttribute("height", String(stripThickness));
                        }
                        hit.setAttribute("fill", "transparent");
                        hit.setAttribute("class", "apexcharts-heatmap-gradient-band");
                        hit.setAttribute("data:range-index", String(b.index));
                        hit.style.cursor = "pointer";
                        svg.appendChild(hit);
                        _this._bandHitEls.push(hit);
                    });
                }
                this._geom = {
                    isVertical: isVertical,
                    position: position,
                    stripX: stripX,
                    stripY: stripY,
                    stripLength: stripLength,
                    stripThickness: stripThickness,
                    arrowSize: arrowSize,
                    svgWidth: svgWidth,
                    svgHeight: svgHeight
                };
                if (cfg.showHoverValue) {
                    var tt = BrowserAPIs.createElement("div");
                    tt.classList.add("apexcharts-heatmap-gradient-legend-value");
                    tt.style.position = "absolute";
                    tt.style.fontSize = ((_g = cfg.labelStyle) == null ? void 0 : _g.fontSize) || "11px";
                    tt.style.fontFamily = ((_h = cfg.labelStyle) == null ? void 0 : _h.fontFamily) || w.config.chart.fontFamily || "";
                    tt.style.color = w.config.chart.foreColor;
                    tt.style.background = "rgba(0,0,0,0.65)";
                    tt.style.color = "#fff";
                    tt.style.padding = "2px 6px";
                    tt.style.borderRadius = "3px";
                    tt.style.pointerEvents = "none";
                    tt.style.whiteSpace = "nowrap";
                    tt.style.opacity = "0";
                    tt.style.transition = "opacity 120ms ease";
                    this.hoverValueEl = tt;
                }
                elLegendWrap.classList.add("apexcharts-heatmap-gradient-legend-wrap");
                elLegendWrap.classList.add("apx-legend-position-" + position);
                elLegendWrap.appendChild(svg);
                if (this.hoverValueEl) elLegendWrap.appendChild(this.hoverValueEl);
                this.svgEl = svg;
                this._applyWrapAlignment(elLegendWrap, position, isVertical, svgWidth, svgHeight);
                this._attachHoverListeners();
                this._attachBandHoverListeners();
            }
        },
        {
            /**
   * Resolve a configured length (number = px, string ending in '%' =
   * percentage of the chart's SVG width/height) to a pixel length.
   * @param {number|string} value
   * @param {boolean} isVertical
   * @returns {number}
   */ key: "_resolveStripLength",
            value: function _resolveStripLength(value, isVertical) {
                var w = this.w;
                var basis = isVertical ? w.globals.svgHeight || w.config.chart.height || 300 : w.globals.svgWidth || w.config.chart.width || 600;
                if (typeof value === "string") {
                    var trimmed = value.trim();
                    if (trimmed.endsWith("%")) {
                        var pct = parseFloat(trimmed) || 0;
                        return Math.max(20, basis * pct / 100);
                    }
                    var n = parseFloat(trimmed);
                    return Number.isFinite(n) ? n : 200;
                }
                if (typeof value === "number" && Number.isFinite(value)) return value;
                return 200;
            }
        },
        {
            /**
   * Position the legend wrap and align the gradient strip within it. The
   * wrap spans the chart's long axis (full width for top/bottom; full
   * height for left/right) and uses flexbox to honor the `align` config.
   * Bypasses the standard `setLegendWrapXY` which sizes the wrap to its
   * content.
   * @param {HTMLElement} elLegendWrap
   * @param {'top'|'right'|'bottom'|'left'} position
   * @param {boolean} isVertical
   * @param {number} svgWidth
   * @param {number} svgHeight
   */ key: "_applyWrapAlignment",
            value: function _applyWrapAlignment(elLegendWrap, position, isVertical, svgWidth, svgHeight) {
                var w = this.w;
                var cfg = w.config.plotOptions.heatmap.colorScale.gradientLegend;
                var align = cfg.align || "center";
                var edgePad = 12;
                var chartWidth = w.globals.svgWidth || w.config.chart.width || 600;
                var chartHeight = w.globals.svgHeight || w.config.chart.height || 300;
                var userOffsetX = w.config.legend.offsetX || 0;
                var userOffsetY = w.config.legend.offsetY || 0;
                elLegendWrap.style.position = "absolute";
                elLegendWrap.style.display = "block";
                elLegendWrap.style.overflow = "visible";
                elLegendWrap.style.padding = "0";
                elLegendWrap.style.width = svgWidth + "px";
                elLegendWrap.style.height = svgHeight + "px";
                elLegendWrap.style.right = "auto";
                elLegendWrap.style.bottom = "auto";
                if (isVertical) {
                    var availableHeight = chartHeight - svgHeight - edgePad * 2;
                    var y;
                    if (align === "start") y = edgePad;
                    else if (align === "end") y = edgePad + Math.max(0, availableHeight);
                    else y = edgePad + Math.max(0, availableHeight) / 2;
                    elLegendWrap.style.top = y + userOffsetY + "px";
                    if (position === "left") {
                        elLegendWrap.style.left = edgePad + userOffsetX + "px";
                    } else {
                        elLegendWrap.style.left = chartWidth - svgWidth - edgePad + userOffsetX + "px";
                    }
                } else {
                    var availableWidth = chartWidth - svgWidth - edgePad * 2;
                    var x;
                    if (align === "start") x = edgePad;
                    else if (align === "end") x = edgePad + Math.max(0, availableWidth);
                    else x = edgePad + Math.max(0, availableWidth) / 2;
                    elLegendWrap.style.left = x + userOffsetX + "px";
                    if (position === "top") {
                        elLegendWrap.style.top = edgePad + userOffsetY + "px";
                    } else {
                        elLegendWrap.style.top = chartHeight - svgHeight - edgePad + userOffsetY + "px";
                    }
                }
            }
        },
        {
            /**
   * Re-position the strip once the final layout is known.
   *
   * `_applyWrapAlignment` (called during `draw()`, before `plotCoords()`) can
   * only pin to the chart's outer edge. This runs after layout — when
   * `translateX/Y`, `gridWidth/Height` and `xAxisHeight` are populated — and:
   *   - centers the strip within its reserved band on the perpendicular axis
   *     (between the title and the plot for `top`; the x-axis and the chart
   *     bottom for `bottom`; the chart edge and the plot for `left`/`right`),
   *     so the slack is split evenly instead of dumped on one side, and
   *   - aligns it along the plot's own extent (so `align: 'center'` centers
   *     over the heatmap, not the whole canvas).
   * Honors `legend.offsetX/offsetY` for user nudging. Safe to call repeatedly.
   */ key: "repositionToPlot",
            value: function repositionToPlot() {
                var _this = this;
                var _a, _b;
                if (!Environment.isBrowser()) return;
                var w = this.w;
                var g = w.globals;
                var wrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                if (!wrap || !this._geom) return;
                if (!Number.isFinite(g.gridWidth) || !Number.isFinite(g.gridHeight)) return;
                var _this__geom = this._geom, isVertical = _this__geom.isVertical, position = _this__geom.position, svgWidth = _this__geom.svgWidth, svgHeight = _this__geom.svgHeight, stripX = _this__geom.stripX, stripY = _this__geom.stripY, stripThickness = _this__geom.stripThickness;
                var align = w.config.plotOptions.heatmap.colorScale.gradientLegend.align || "center";
                var ox = w.config.legend.offsetX || 0;
                var oy = w.config.legend.offsetY || 0;
                var dimHelpers = (_b = (_a = this.ctx) == null ? void 0 : _a.dimensions) == null ? void 0 : _b.dimHelpers;
                var titleArea = dimHelpers ? dimHelpers.getTitleSubtitleCoords("title").height + dimHelpers.getTitleSubtitleCoords("subtitle").height : 0;
                var xAxisArea = w.layout.xAxisHeight || 0;
                var alongOffset = function alongOffset(extent, size) {
                    var avail = Math.max(0, extent - size);
                    if (align === "start") return 0;
                    if (align === "end") return avail;
                    return avail / 2;
                };
                if (isVertical) {
                    wrap.style.top = g.translateY + alongOffset(g.gridHeight, svgHeight) + oy + "px";
                    var bandStart = position === "left" ? 0 : g.translateX + g.gridWidth;
                    var bandEnd = position === "left" ? g.translateX : g.svgWidth;
                    var stripCenter = (bandStart + bandEnd) / 2;
                    wrap.style.left = stripCenter - stripX - stripThickness / 2 + ox + "px";
                } else {
                    wrap.style.left = g.translateX + alongOffset(g.gridWidth, svgWidth) + ox + "px";
                    var bandStart1 = position === "top" ? titleArea : g.translateY + g.gridHeight + xAxisArea;
                    var bandEnd1 = position === "top" ? g.translateY : g.svgHeight;
                    var stripCenter1 = (bandStart1 + bandEnd1) / 2;
                    wrap.style.top = stripCenter1 - stripY - stripThickness / 2 + oy + "px";
                }
                BrowserAPIs.requestAnimationFrame(function() {
                    return _this._enforceMinPlotGap();
                });
            }
        },
        {
            /**
   * Guarantee a minimum gap between the strip's chart-facing edge and the plot.
   * Measured in viewport space (immune to the wrap↔SVG coordinate offset) and
   * applied as a *relative* shift to the wrap's current position, so it only
   * nudges a strip that ended up too close — placements with ample room are
   * left exactly where centering put them. Runs post-paint (see caller).
   */ key: "_enforceMinPlotGap",
            value: function _enforceMinPlotGap() {
                var w = this.w;
                var wrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                var strip = this.svgEl && this.svgEl.querySelector("rect");
                var grid = w.dom.baseEl.querySelector(".apexcharts-grid");
                if (!wrap || !strip || !grid || !this._geom) return;
                var s = strip.getBoundingClientRect();
                var gr = grid.getBoundingClientRect();
                if (!s.width || !s.height || !gr.width || !gr.height) return;
                var MIN_GAP = 16;
                var _this__geom = this._geom, isVertical = _this__geom.isVertical, position = _this__geom.position;
                if (isVertical) {
                    var gap = position === "left" ? gr.left - s.right : s.left - gr.right;
                    if (gap < MIN_GAP) {
                        var curLeft = parseFloat(wrap.style.left) || 0;
                        var shift = MIN_GAP - gap;
                        wrap.style.left = curLeft + (position === "left" ? -shift : shift) + "px";
                    }
                } else {
                    var gap1 = position === "top" ? gr.top - s.bottom : s.top - gr.bottom;
                    if (gap1 < MIN_GAP) {
                        var curTop = parseFloat(wrap.style.top) || 0;
                        var shift1 = MIN_GAP - gap1;
                        wrap.style.top = curTop + (position === "top" ? -shift1 : shift1) + "px";
                    }
                }
            }
        },
        {
            /**
   * Tear down listeners (called before re-render).
   */ key: "destroy",
            value: function destroy() {
                var _a, _b, _c, _d, _e, _f, _g;
                for(var i = 0; i < this._bandHitEls.length; i++){
                    var el = this._bandHitEls[i];
                    (_a = el.removeEventListener) == null ? void 0 : _a.call(el, "mousemove", this._onBandEnter);
                    (_b = el.removeEventListener) == null ? void 0 : _b.call(el, "mouseout", this._onBandLeave);
                }
                this._bandHitEls = [];
                this._activeBandIndex = -1;
                if (!((_c = this.ctx) == null ? void 0 : _c.events)) return;
                try {
                    (_e = (_d = this.ctx.events).removeEventListener) == null ? void 0 : _e.call(_d, "dataPointMouseEnter", this._onCellEnter);
                    (_g = (_f = this.ctx.events).removeEventListener) == null ? void 0 : _g.call(_f, "dataPointMouseLeave", this._onCellLeave);
                } catch (_) {}
            }
        },
        {
            /** Wire mousemove/mouseout on each per-band hit-region (ranges mode). */ key: "_attachBandHoverListeners",
            value: function _attachBandHoverListeners() {
                if (!Environment.isBrowser()) return;
                for(var i = 0; i < this._bandHitEls.length; i++){
                    var el = this._bandHitEls[i];
                    el.addEventListener("mousemove", this._onBandEnter);
                    el.addEventListener("mouseout", this._onBandLeave);
                }
            }
        },
        {
            /**
   * Hovering a gradient band highlights its cells and dims the rest. Guarded
   * so the repeated mousemove stream only re-applies on an actual band change.
   * @param {Event} e
   */ key: "_onBandEnter",
            value: function _onBandEnter(e) {
                var _a, _b, _c, _d;
                var w = this.w;
                var target = /** @type {Element} */ e.currentTarget;
                var idx = parseInt((_a = target.getAttribute("data:range-index")) != null ? _a : "-1", 10);
                if (idx < 0 || idx === this._activeBandIndex) return;
                this._activeBandIndex = idx;
                (_d = (_c = (_b = this.ctx) == null ? void 0 : _b.events) == null ? void 0 : _c.fireEvent) == null ? void 0 : _d.call(_c, "legendHover", [
                    this.ctx,
                    idx,
                    w
                ]);
                new Series(w).highlightRangeInSeries(idx, "highlight");
            }
        },
        {
            /** Leaving a band clears the highlight. */ key: "_onBandLeave",
            value: function _onBandLeave() {
                if (this._activeBandIndex < 0) return;
                var idx = this._activeBandIndex;
                this._activeBandIndex = -1;
                new Series(this.w).highlightRangeInSeries(idx, "reset");
            }
        },
        {
            key: "_attachHoverListeners",
            value: function _attachHoverListeners() {
                var _a, _b;
                if (!Environment.isBrowser()) return;
                if (!((_b = (_a = this.ctx) == null ? void 0 : _a.events) == null ? void 0 : _b.addEventListener)) return;
                this.ctx.events.addEventListener("dataPointMouseEnter", this._onCellEnter);
                this.ctx.events.addEventListener("dataPointMouseLeave", this._onCellLeave);
            }
        },
        {
            /**
   * dataPointMouseEnter fires as `(e, ctx, { seriesIndex, dataPointIndex, w })`.
   * Graphics._fireEvent forwards listener args in the same shape.
   * @param {...any} args
   */ key: "_onCellEnter",
            value: function _onCellEnter() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                var _a, _b;
                var w = this.w;
                if (!this.arrowEl) return;
                var opts = args[args.length - 1];
                if (!opts || (typeof opts === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(opts)) !== "object") return;
                var i = opts.seriesIndex;
                var j = opts.dataPointIndex;
                if (typeof i !== "number" || typeof j !== "number") return;
                if (w.config.chart.type !== "heatmap") return;
                var row = (_b = (_a = w.seriesData) == null ? void 0 : _a.series) == null ? void 0 : _b[i];
                var val = row == null ? void 0 : row[j];
                if (val == null || Number.isNaN(val)) return;
                this._positionArrow(val);
            }
        },
        {
            key: "_onCellLeave",
            value: function _onCellLeave() {
                if (!this.arrowEl) return;
                this.arrowEl.setAttribute("opacity", "0");
                if (this.hoverValueEl) {
                    this.hoverValueEl.style.opacity = "0";
                }
            }
        },
        {
            /**
   * Move the arrow to the position corresponding to `val` along the strip.
   * @param {number} val
   */ key: "_positionArrow",
            value: function _positionArrow(val) {
                if (!this.arrowEl || !this._geom) return;
                var _this__geom = this._geom, isVertical = _this__geom.isVertical, position = _this__geom.position, stripX = _this__geom.stripX, stripY = _this__geom.stripY, stripLength = _this__geom.stripLength, stripThickness = _this__geom.stripThickness, arrowSize = _this__geom.arrowSize;
                var min = this._min;
                var max = this._max;
                var span = max - min;
                var pct;
                if (span === 0) {
                    pct = 0.5;
                } else {
                    pct = (val - min) / span;
                }
                if (pct < 0) pct = 0;
                if (pct > 1) pct = 1;
                if (isVertical) {
                    var yCenter = stripY + stripLength - pct * stripLength;
                    var tipX, baseX;
                    if (position === "left") {
                        tipX = stripX + stripThickness;
                        baseX = tipX + arrowSize;
                    } else {
                        tipX = stripX;
                        baseX = tipX - arrowSize;
                    }
                    var points = [
                        "".concat(tipX, ",").concat(yCenter),
                        "".concat(baseX, ",").concat(yCenter - arrowSize / 2),
                        "".concat(baseX, ",").concat(yCenter + arrowSize / 2)
                    ].join(" ");
                    this.arrowEl.setAttribute("points", points);
                } else {
                    var xCenter = stripX + pct * stripLength;
                    var tipY, baseY;
                    if (position === "top") {
                        tipY = stripY + stripThickness;
                        baseY = tipY + arrowSize;
                    } else {
                        tipY = stripY;
                        baseY = tipY - arrowSize;
                    }
                    var points1 = [
                        "".concat(xCenter, ",").concat(tipY),
                        "".concat(xCenter - arrowSize / 2, ",").concat(baseY),
                        "".concat(xCenter + arrowSize / 2, ",").concat(baseY)
                    ].join(" ");
                    this.arrowEl.setAttribute("points", points1);
                }
                this.arrowEl.setAttribute("opacity", "1");
                if (this.hoverValueEl) {
                    var fmt = this._getFormatter();
                    this.hoverValueEl.textContent = fmt(val);
                    if (isVertical) {
                        var yCenter1 = stripY + stripLength - pct * stripLength;
                        if (position === "left") {
                            this.hoverValueEl.style.left = "".concat(stripX + stripThickness + arrowSize + 8, "px");
                        } else {
                            this.hoverValueEl.style.left = "".concat(stripX - arrowSize - 8, "px");
                            this.hoverValueEl.style.transform = "translateX(-100%)";
                        }
                        this.hoverValueEl.style.top = "".concat(yCenter1 - 9, "px");
                    } else {
                        var xCenter1 = stripX + pct * stripLength;
                        this.hoverValueEl.style.left = "".concat(xCenter1, "px");
                        this.hoverValueEl.style.transform = "translateX(-50%)";
                        if (position === "top") {
                            this.hoverValueEl.style.top = "".concat(stripY + stripThickness + arrowSize + 8, "px");
                        } else {
                            this.hoverValueEl.style.top = "".concat(stripY - arrowSize - 18, "px");
                        }
                    }
                    this.hoverValueEl.style.opacity = "1";
                }
            }
        },
        {
            /**
   * @param {number} size
   * @param {string} color
   * @param {'top'|'right'|'bottom'|'left'} _position
   */ key: "_buildArrow",
            value: function _buildArrow(size, color, _position) {
                var polygon = BrowserAPIs.createElementNS(SVG_NS, "polygon");
                polygon.setAttribute("fill", color);
                polygon.setAttribute("opacity", "0");
                polygon.setAttribute("class", "apexcharts-heatmap-gradient-arrow");
                polygon.setAttribute("points", "0,0 0,0 0,0");
                polygon.setAttribute("pointer-events", "none");
                return polygon;
            }
        },
        {
            /**
   * Build gradient stops + return effective min/max.
   * - If `colorScale.ranges` is set, stops are placed at each range boundary
   *   so the gradient reflects the user's discrete palette.
   * - Otherwise, samples N stops from the same shadeColor function the cells
   *   use, so the strip visually matches the heatmap.
   * @returns {{ min: number, max: number, stops: Array<{percent:number,color:string}>, bands: Array<{index:number,p1:number,p2:number}> }}
   */ key: "_computeStops",
            value: function _computeStops() {
                var _a, _b;
                var w = this.w;
                var cs = w.config.plotOptions.heatmap.colorScale;
                var cfg = cs.gradientLegend;
                var dataMin = Infinity;
                var dataMax = -Infinity;
                var rows = ((_a = w.seriesData) == null ? void 0 : _a.series) || [];
                for(var i = 0; i < rows.length; i++){
                    var row = rows[i];
                    if (!row) continue;
                    for(var j = 0; j < row.length; j++){
                        var v = row[j];
                        if (v == null || Number.isNaN(v)) continue;
                        if (v < dataMin) dataMin = v;
                        if (v > dataMax) dataMax = v;
                    }
                }
                if (!Number.isFinite(dataMin)) dataMin = 0;
                if (!Number.isFinite(dataMax)) dataMax = 0;
                var min = dataMin;
                var max = dataMax;
                if (typeof cs.min !== "undefined") {
                    min = cs.min < dataMin ? cs.min : dataMin;
                }
                if (typeof cs.max !== "undefined") {
                    max = cs.max > dataMax ? cs.max : dataMax;
                }
                var stops = [];
                var bands = [];
                if (cs.ranges && cs.ranges.length > 0) {
                    var ranges = cs.ranges.map(function(r, originalIndex) {
                        return __spreadProps(__spreadValues({}, r), {
                            _originalIndex: originalIndex
                        });
                    }).sort(function(a, b) {
                        return a.from - b.from;
                    });
                    var lo = ranges[0].from;
                    var hi = ranges[ranges.length - 1].to;
                    min = lo;
                    max = hi;
                    var span = hi - lo || 1;
                    ranges.forEach(function(r) {
                        var p1 = (r.from - lo) / span;
                        var p2 = (r.to - lo) / span;
                        stops.push({
                            percent: (p1 + p2) / 2,
                            color: r.color
                        });
                        bands.push({
                            index: r._originalIndex,
                            p1: p1,
                            p2: p2
                        });
                    });
                } else {
                    var baseColor = w.globals.colors[0] || "#008FFB";
                    var utils = new Utils$1();
                    var shadeIntensity = (_b = w.config.plotOptions.heatmap.shadeIntensity) != null ? _b : 0.5;
                    var hasNegs = /** @type {any} */ w.globals.hasNegs;
                    var n = Math.max(2, cfg.stops || 16);
                    for(var s = 0; s < n; s++){
                        var t = s / (n - 1);
                        var v1 = min + t * (max - min);
                        var total = Math.abs(max) + Math.abs(min);
                        var percent_v = total === 0 ? 0 : 100 * v1 / total;
                        var colorShadePercent = void 0;
                        if (hasNegs) {
                            if (w.config.plotOptions.heatmap.reverseNegativeShade) {
                                colorShadePercent = percent_v < 0 ? percent_v / 100 * (shadeIntensity * 1.25) : (1 - percent_v / 100) * (shadeIntensity * 1.25);
                            } else {
                                colorShadePercent = percent_v <= 0 ? 1 - (1 + percent_v / 100) * shadeIntensity : (1 - percent_v / 100) * shadeIntensity;
                            }
                        } else {
                            colorShadePercent = 1 - percent_v / 100;
                        }
                        if (colorShadePercent > 1) colorShadePercent = 1;
                        if (colorShadePercent < -1) colorShadePercent = -1;
                        var shaded = w.config.plotOptions.heatmap.enableShades ? utils.shadeColor(w.config.theme.mode === "dark" ? colorShadePercent * -1 : colorShadePercent, baseColor) : baseColor;
                        stops.push({
                            percent: t,
                            color: shaded
                        });
                    }
                }
                return {
                    min: min,
                    max: max,
                    stops: stops,
                    bands: bands
                };
            }
        }
    ], [
        {
            key: "isEnabled",
            value: /**
   * True when the user has opted into the gradient legend variant.
   * @param {any} w
   */ function isEnabled(w) {
                var _a, _b, _c, _d;
                var cfg = (_d = (_c = (_b = (_a = w == null ? void 0 : w.config) == null ? void 0 : _a.plotOptions) == null ? void 0 : _b.heatmap) == null ? void 0 : _c.colorScale) == null ? void 0 : _d.gradientLegend;
                return !!(cfg && cfg.enabled);
            }
        }
    ]);
    return HeatmapGradientLegend;
}();
var Legend = /*#__PURE__*/ function() {
    "use strict";
    function Legend(w, ctx) {
        var _ctx_updateHelpers;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Legend);
        this.w = w;
        this.ctx = ctx;
        this.updateSeries = function() {
            for(var _len = arguments.length, a = new Array(_len), _key = 0; _key < _len; _key++){
                a[_key] = arguments[_key];
            }
            return (_ctx_updateHelpers = ctx.updateHelpers)._updateSeries.apply(_ctx_updateHelpers, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(a));
        };
        this.onLegendClick = this.onLegendClick.bind(this);
        this.onLegendHovered = this.onLegendHovered.bind(this);
        this.isBarsDistributed = this.w.config.chart.type === "bar" && this.w.config.plotOptions.bar.distributed && this.w.config.series.length === 1;
        this.legendHelpers = new Helpers$3(this);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Legend, [
        {
            key: "init",
            value: function init() {
                var w = this.w;
                var gl = w.globals;
                var cnf = w.config;
                this.isBarsDistributed = cnf.chart.type === "bar" && cnf.plotOptions.bar.distributed && cnf.series.length === 1;
                var showLegendAlways = cnf.legend.showForSingleSeries && this.w.seriesData.series.length === 1 || this.isBarsDistributed || // Heatmap legends are colorScale-driven (discrete ranges or the
                // gradient strip), not series-driven, so they must render even for a
                // single-row heatmap.
                cnf.chart.type === "heatmap" || this.w.seriesData.series.length > 1;
                this.legendHelpers.appendToForeignObject();
                if ((showLegendAlways || !gl.axisCharts) && cnf.legend.show) {
                    var elLegendWrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                    while(elLegendWrap.firstChild){
                        elLegendWrap.removeChild(elLegendWrap.firstChild);
                    }
                    if (this.heatmapGradientLegend) {
                        this.heatmapGradientLegend.destroy();
                        this.heatmapGradientLegend = null;
                    }
                    if (cnf.chart.type === "heatmap" && HeatmapGradientLegend.isEnabled(w)) {
                        this.heatmapGradientLegend = new HeatmapGradientLegend(w, this.ctx);
                        this.heatmapGradientLegend.draw();
                    } else {
                        this.drawLegends();
                        if (cnf.legend.position === "bottom" || cnf.legend.position === "top") {
                            this.legendAlignHorizontal();
                        } else if (cnf.legend.position === "right" || cnf.legend.position === "left") {
                            this.legendAlignVertical();
                        }
                    }
                }
            }
        },
        {
            key: "createLegendMarker",
            value: function createLegendMarker(param) {
                var i = param.i, fillcolor = param.fillcolor;
                var w = this.w;
                var elMarker = BrowserAPIs.createElement("span");
                elMarker.classList.add("apexcharts-legend-marker");
                var mShape = w.config.legend.markers.shape || w.config.markers.shape;
                var shape = mShape;
                if (Array.isArray(mShape)) {
                    shape = mShape[i];
                }
                var mSize = Array.isArray(w.config.legend.markers.size) ? parseFloat(w.config.legend.markers.size[i]) : parseFloat(w.config.legend.markers.size);
                var mOffsetX = Array.isArray(w.config.legend.markers.offsetX) ? parseFloat(w.config.legend.markers.offsetX[i]) : parseFloat(w.config.legend.markers.offsetX);
                var mOffsetY = Array.isArray(w.config.legend.markers.offsetY) ? parseFloat(w.config.legend.markers.offsetY[i]) : parseFloat(w.config.legend.markers.offsetY);
                var mBorderWidth = Array.isArray(w.config.legend.markers.strokeWidth) ? parseFloat(w.config.legend.markers.strokeWidth[i]) : parseFloat(w.config.legend.markers.strokeWidth);
                var mStyle = elMarker.style;
                mStyle.height = (mSize + mBorderWidth) * 2 + "px";
                mStyle.width = (mSize + mBorderWidth) * 2 + "px";
                mStyle.left = mOffsetX + "px";
                mStyle.top = mOffsetY + "px";
                if (w.config.legend.markers.customHTML) {
                    mStyle.background = "transparent";
                    mStyle.color = fillcolor[i];
                    if (Array.isArray(w.config.legend.markers.customHTML)) {
                        if (w.config.legend.markers.customHTML[i]) {
                            elMarker.innerHTML = w.config.legend.markers.customHTML[i]();
                        }
                    } else {
                        elMarker.innerHTML = w.config.legend.markers.customHTML();
                    }
                } else {
                    var markers = new Markers(this.ctx.w, this.ctx);
                    var markerConfig = markers.getMarkerConfig({
                        cssClass: "apexcharts-legend-marker apexcharts-marker apexcharts-marker-".concat(shape),
                        seriesIndex: i,
                        strokeWidth: mBorderWidth,
                        size: mSize
                    });
                    var SVGLib = Environment.isBrowser() ? /** @type {any} */ window.SVG : /** @type {any} */ /*TURBOPACK member replacement*/ __turbopack_context__.g.SVG;
                    var SVGMarker = SVGLib().addTo(elMarker).size("100%", "100%");
                    var marker = new Graphics(this.w).drawMarker(0, 0, __spreadProps(__spreadValues({}, markerConfig), {
                        pointFillColor: Array.isArray(fillcolor) ? fillcolor[i] : markerConfig.pointFillColor,
                        shape: shape
                    }));
                    var shapesEls = w.dom.Paper.find(".apexcharts-legend-marker.apexcharts-marker");
                    shapesEls.forEach(function(shapeEl) {
                        if (shapeEl.node.classList.contains("apexcharts-marker-triangle")) {
                            shapeEl.node.style.transform = "translate(50%, 45%)";
                        } else {
                            shapeEl.node.style.transform = "translate(50%, 50%)";
                        }
                    });
                    SVGMarker.add(marker);
                }
                return elMarker;
            }
        },
        {
            key: "drawLegends",
            value: function drawLegends() {
                var _this, _loop = function(i) {
                    var text = legendFormatter(legendNames[i], {
                        seriesIndex: i,
                        w: w
                    });
                    var collapsedSeries = false;
                    var ancillaryCollapsedSeries = false;
                    if (w.globals.collapsedSeries.length > 0) {
                        for(var c = 0; c < w.globals.collapsedSeries.length; c++){
                            if (w.globals.collapsedSeries[c].index === i) {
                                collapsedSeries = true;
                            }
                        }
                    }
                    if (w.globals.ancillaryCollapsedSeriesIndices.length > 0) {
                        for(var c1 = 0; c1 < w.globals.ancillaryCollapsedSeriesIndices.length; c1++){
                            if (w.globals.ancillaryCollapsedSeriesIndices[c1] === i) {
                                ancillaryCollapsedSeries = true;
                            }
                        }
                    }
                    var elMarker = _this.createLegendMarker({
                        i: i,
                        fillcolor: fillcolor
                    });
                    Graphics.setAttrs(elMarker, {
                        rel: i + 1,
                        "data:collapsed": collapsedSeries || ancillaryCollapsedSeries
                    });
                    if (collapsedSeries || ancillaryCollapsedSeries) {
                        elMarker.classList.add("apexcharts-inactive-legend");
                    }
                    var elLegend = BrowserAPIs.createElement("div");
                    if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.keyboard.enabled) {
                        elLegend.setAttribute("role", "button");
                        elLegend.setAttribute("tabindex", "0");
                        var seriesName = Array.isArray(text) ? text.join(" ") : text;
                        var isCollapsed = collapsedSeries || ancillaryCollapsedSeries;
                        var statusText = isCollapsed ? "hidden" : "visible";
                        elLegend.setAttribute("aria-label", "".concat(seriesName, ", ").concat(statusText, ". Press Enter or Space to toggle."));
                        elLegend.setAttribute("aria-pressed", isCollapsed ? "true" : "false");
                    }
                    var elLegendText = BrowserAPIs.createElement("span");
                    elLegendText.classList.add("apexcharts-legend-text");
                    elLegendText.innerHTML = Array.isArray(text) ? text.join(" ") : text;
                    var textColor = w.config.legend.labels.useSeriesColors ? w.globals.colors[i] : Array.isArray(w.config.legend.labels.colors) ? (_a = w.config.legend.labels.colors) == null ? void 0 : _a[i] : w.config.legend.labels.colors;
                    if (!textColor) {
                        textColor = w.config.chart.foreColor;
                    }
                    elLegendText.style.color = textColor;
                    elLegendText.style.fontSize = w.config.legend.fontSize;
                    elLegendText.style.fontWeight = w.config.legend.fontWeight;
                    elLegendText.style.fontFamily = fontFamily || w.config.chart.fontFamily;
                    Graphics.setAttrs(elLegendText, {
                        rel: i + 1,
                        i: i,
                        "data:default-text": encodeURIComponent(text),
                        "data:collapsed": collapsedSeries || ancillaryCollapsedSeries
                    });
                    elLegend.appendChild(elMarker);
                    elLegend.appendChild(elLegendText);
                    var coreUtils = new CoreUtils(_this.w);
                    if (!w.config.legend.showForZeroSeries) {
                        var total = coreUtils.getSeriesTotalByIndex(i);
                        if (total === 0 && coreUtils.seriesHaveSameValues(i) && !coreUtils.isSeriesNull(i) && w.globals.collapsedSeriesIndices.indexOf(i) === -1 && w.globals.ancillaryCollapsedSeriesIndices.indexOf(i) === -1) {
                            elLegend.classList.add("apexcharts-hidden-zero-series");
                        }
                    }
                    if (!w.config.legend.showForNullSeries) {
                        if (coreUtils.isSeriesNull(i) && w.globals.collapsedSeriesIndices.indexOf(i) === -1 && w.globals.ancillaryCollapsedSeriesIndices.indexOf(i) === -1) {
                            elLegend.classList.add("apexcharts-hidden-null-series");
                        }
                    }
                    if (legendGroups.length) {
                        w.labelData.seriesGroups.forEach(function(group, gi) {
                            var _a2, _b;
                            if (group.includes(/** @type {Record<string,any>} */ (_b = (_a2 = w.config.series[i]) == null ? void 0 : _a2.name) != null ? _b : "")) {
                                elLegendWrap.appendChild(legendGroups[gi]);
                                legendGroups[gi].appendChild(elLegend);
                            }
                        });
                    } else {
                        elLegendWrap.appendChild(elLegend);
                    }
                    elLegendWrap.classList.add("apexcharts-align-".concat(w.config.legend.horizontalAlign));
                    elLegendWrap.classList.add("apx-legend-position-" + w.config.legend.position);
                    elLegend.classList.add("apexcharts-legend-series");
                    elLegend.style.margin = "".concat(w.config.legend.itemMargin.vertical, "px ").concat(w.config.legend.itemMargin.horizontal, "px");
                    elLegendWrap.style.width = w.config.legend.width ? w.config.legend.width + "px" : "";
                    elLegendWrap.style.height = w.config.legend.height ? w.config.legend.height + "px" : "";
                    Graphics.setAttrs(elLegend, {
                        rel: i + 1,
                        seriesName: Utils$1.escapeString(legendNames[i]),
                        "data:collapsed": collapsedSeries || ancillaryCollapsedSeries
                    });
                    if (collapsedSeries || ancillaryCollapsedSeries) {
                        elLegend.classList.add("apexcharts-inactive-legend");
                    }
                    if (!w.config.legend.onItemClick.toggleDataSeries) {
                        elLegend.classList.add("apexcharts-no-click");
                    }
                };
                var _a;
                var me = this;
                var w = this.w;
                var elLegendWrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                var fontFamily = w.config.legend.fontFamily;
                var legendNames = w.seriesData.seriesNames;
                var fillcolor = w.config.legend.markers.fillColors ? w.config.legend.markers.fillColors.slice() : w.globals.colors.slice();
                if (w.config.chart.type === "heatmap") {
                    var ranges = w.config.plotOptions.heatmap.colorScale.ranges;
                    legendNames = ranges.map(function(colorScale) {
                        return colorScale.name ? colorScale.name : colorScale.from + " - " + colorScale.to;
                    });
                    fillcolor = ranges.map(function(color) {
                        return color.color;
                    });
                } else if (this.isBarsDistributed) {
                    legendNames = w.labelData.labels.slice();
                }
                if (w.config.legend.customLegendItems.length) {
                    legendNames = w.config.legend.customLegendItems;
                }
                var legendFormatter = w.formatters.legendFormatter;
                var isLegendInversed = w.config.legend.inverseOrder;
                var legendGroups = [];
                if (w.labelData.seriesGroups.length > 1 && w.config.legend.clusterGroupedSeries) {
                    w.labelData.seriesGroups.forEach(function(_, gi) {
                        legendGroups[gi] = BrowserAPIs.createElement("div");
                        legendGroups[gi].classList.add("apexcharts-legend-group", "apexcharts-legend-group-".concat(gi));
                        if (w.config.legend.clusterGroupedSeriesOrientation === "horizontal") {
                            elLegendWrap.classList.add("apexcharts-legend-group-horizontal");
                        } else {
                            legendGroups[gi].classList.add("apexcharts-legend-group-vertical");
                        }
                    });
                }
                for(var i = isLegendInversed ? legendNames.length - 1 : 0; isLegendInversed ? i >= 0 : i <= legendNames.length - 1; isLegendInversed ? i-- : i++)_this = this, _loop(i);
                w.dom.elWrap.addEventListener("click", me.onLegendClick, true);
                if (w.config.legend.onItemHover.highlightDataSeries && w.config.legend.customLegendItems.length === 0) {
                    w.dom.elWrap.addEventListener("mousemove", me.onLegendHovered, true);
                    w.dom.elWrap.addEventListener("mouseout", me.onLegendHovered, true);
                }
                if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.keyboard.enabled) {
                    w.dom.elWrap.addEventListener("keydown", me.onLegendKeyDown.bind(me), true);
                }
            }
        },
        {
            /**
   * @param {number} offsetX
   * @param {number} offsetY
   */ key: "setLegendWrapXY",
            value: function setLegendWrapXY(offsetX, offsetY) {
                var w = this.w;
                var elLegendWrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                var legendHeight = elLegendWrap.clientHeight;
                var x = 0;
                var y = 0;
                if (w.config.legend.position === "bottom") {
                    y = w.globals.svgHeight - Math.min(legendHeight, w.globals.svgHeight / 2) - 5;
                } else if (w.config.legend.position === "top") {
                    var dim = new Dimensions(this.w, this.ctx);
                    var titleH = dim.dimHelpers.getTitleSubtitleCoords("title").height;
                    var subtitleH = dim.dimHelpers.getTitleSubtitleCoords("subtitle").height;
                    y = (titleH > 0 ? titleH - 10 : 0) + (subtitleH > 0 ? subtitleH - 10 : 0);
                }
                elLegendWrap.style.position = "absolute";
                x = x + offsetX + w.config.legend.offsetX;
                y = y + offsetY + w.config.legend.offsetY;
                elLegendWrap.style.left = x + "px";
                elLegendWrap.style.top = y + "px";
                if (w.config.legend.position === "right") {
                    elLegendWrap.style.left = "auto";
                    elLegendWrap.style.right = 25 + w.config.legend.offsetX + "px";
                }
                var fixedHeigthWidth = /** @type {const} */ [
                    "width",
                    "height"
                ];
                fixedHeigthWidth.forEach(function(hw) {
                    if (elLegendWrap && elLegendWrap.style[hw]) {
                        elLegendWrap.style[hw] = parseInt(String(w.config.legend[hw]), 10) + "px";
                    }
                });
            }
        },
        {
            key: "legendAlignHorizontal",
            value: function legendAlignHorizontal() {
                var w = this.w;
                var elLegendWrap = /** @type {HTMLElement} */ w.dom.elLegendWrap;
                elLegendWrap.style.right = "0";
                var dimensions = new Dimensions(this.w, this.ctx);
                var titleRect = dimensions.dimHelpers.getTitleSubtitleCoords("title");
                var subtitleRect = dimensions.dimHelpers.getTitleSubtitleCoords("subtitle");
                var offsetX = 20;
                var offsetY = 0;
                if (w.config.legend.position === "top") {
                    offsetY = titleRect.height + subtitleRect.height + w.config.title.margin + w.config.subtitle.margin - 10;
                }
                this.setLegendWrapXY(offsetX, offsetY);
            }
        },
        {
            key: "legendAlignVertical",
            value: function legendAlignVertical() {
                var w = this.w;
                var lRect = this.legendHelpers.getLegendDimensions();
                var offsetY = 20;
                var offsetX = 0;
                if (w.config.legend.position === "left") {
                    offsetX = 20;
                }
                if (w.config.legend.position === "right") {
                    offsetX = w.globals.svgWidth - lRect.clww - 10;
                }
                this.setLegendWrapXY(offsetX, offsetY);
            }
        },
        {
            /**
   * @param {MouseEvent} e
   */ key: "onLegendHovered",
            value: function onLegendHovered(e) {
                var _a;
                var w = this.w;
                var target = /** @type {Element} */ e.target;
                var hoverOverLegend = target.classList.contains("apexcharts-legend-series") || target.classList.contains("apexcharts-legend-text") || target.classList.contains("apexcharts-legend-marker");
                if (w.config.chart.type !== "heatmap" && !this.isBarsDistributed) {
                    if (!target.classList.contains("apexcharts-inactive-legend") && hoverOverLegend) {
                        var series = new Series(this.ctx.w);
                        series.toggleSeriesOnHover(e, target);
                    }
                } else {
                    if (hoverOverLegend) {
                        var seriesCnt = parseInt((_a = target.getAttribute("rel")) != null ? _a : "0", 10) - 1;
                        this.ctx.events.fireEvent("legendHover", [
                            this.ctx,
                            seriesCnt,
                            this.w
                        ]);
                        var series1 = new Series(this.ctx.w);
                        if (e.type === "mousemove") {
                            series1.highlightRangeInSeries(seriesCnt, "highlight");
                        } else if (e.type === "mouseout") {
                            series1.highlightRangeInSeries(seriesCnt, "reset");
                        }
                    }
                }
            }
        },
        {
            /**
   * @param {KeyboardEvent} e
   */ key: "onLegendKeyDown",
            value: function onLegendKeyDown(e) {
                var me = this;
                var w = this.w;
                var target = /** @type {Element} */ e.target;
                var isLegendItem = target.classList.contains("apexcharts-legend-series") || target.classList.contains("apexcharts-legend-text") || target.classList.contains("apexcharts-legend-marker");
                if (!isLegendItem) return;
                if (e.key === "Enter" || e.key === " ") {
                    e.preventDefault();
                    var rel = target.getAttribute("rel");
                    me.onLegendClick(e);
                    if (rel !== null && w.config.legend.onItemClick.toggleDataSeries) {
                        requestAnimationFrame(function() {
                            var restored = w.dom.baseEl.querySelector('.apexcharts-legend-series[rel="'.concat(rel, '"]'));
                            if (restored) restored.focus();
                        });
                    }
                }
            }
        },
        {
            /**
   * @param {Event} e
   */ key: "onLegendClick",
            value: function onLegendClick(e) {
                var _a;
                var w = this.w;
                var target = /** @type {Element} */ e.target;
                if (w.config.legend.customLegendItems.length) return;
                if (target.classList.contains("apexcharts-legend-series") || target.classList.contains("apexcharts-legend-text") || target.classList.contains("apexcharts-legend-marker")) {
                    var seriesCnt = parseInt((_a = target.getAttribute("rel")) != null ? _a : "0", 10) - 1;
                    var isHidden = target.getAttribute("data:collapsed") === "true";
                    var legendClick = this.w.config.chart.events.legendClick;
                    if (typeof legendClick === "function") {
                        legendClick(this.ctx, seriesCnt, this.w);
                    }
                    this.ctx.events.fireEvent("legendClick", [
                        this.ctx,
                        seriesCnt,
                        this.w
                    ]);
                    var markerClick = this.w.config.legend.markers.onClick;
                    if (typeof markerClick === "function" && target.classList.contains("apexcharts-legend-marker")) {
                        markerClick(this.ctx, seriesCnt, this.w);
                        this.ctx.events.fireEvent("legendMarkerClick", [
                            this.ctx,
                            seriesCnt,
                            this.w
                        ]);
                    }
                    var clickAllowed = w.config.chart.type !== "treemap" && w.config.chart.type !== "heatmap" && !this.isBarsDistributed;
                    if (clickAllowed && w.config.legend.onItemClick.toggleDataSeries) {
                        this.legendHelpers.toggleDataSeries(seriesCnt, isHidden);
                    }
                }
            }
        }
    ]);
    return Legend;
}();
ApexCharts.registerFeatures({
    legend: Legend
});
var icoPan = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M5 9 2 12l3 3"/>\n    <path d="M9 5l3-3 3 3"/>\n    <path d="M15 19l-3 3-3-3"/>\n    <path d="M19 9l3 3-3 3"/>\n    <path d="M2 12h20"/>\n    <path d="M12 2v20"/>\n</svg>\n';
var icoZoom = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <circle cx="11" cy="11" r="7"/>\n    <path d="m21 21-4.3-4.3M8 11h6M11 8v6"/>\n</svg>\n';
var icoReset = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8"/>\n    <path d="M3 3v5h5"/>\n</svg>\n';
var icoZoomIn = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M12 5v14M5 12h14"/>\n</svg>\n';
var icoZoomOut = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M5 12h14"/>\n</svg>\n';
var icoSelect = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M5 3a2 2 0 0 0-2 2"/>\n    <path d="M19 3a2 2 0 0 1 2 2"/>\n    <path d="M21 19a2 2 0 0 1-2 2"/>\n    <path d="M5 21a2 2 0 0 1-2-2"/>\n    <path d="M9 3h1M14 3h1M9 21h1M14 21h1M3 9v1M3 14v1M21 9v1M21 14v1"/>\n</svg>\n';
var icoMeasure = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M21.3 15.3a2.4 2.4 0 0 1 0 3.4l-2.6 2.6a2.4 2.4 0 0 1-3.4 0L2.7 8.7a2.4 2.4 0 0 1 0-3.4l2.6-2.6a2.4 2.4 0 0 1 3.4 0Z"/>\n    <path d="m14.5 12.5 2-2"/>\n    <path d="m11.5 9.5 2-2"/>\n    <path d="m8.5 6.5 2-2"/>\n    <path d="m17.5 15.5 2-2"/>\n</svg>\n';
var icoMenu = '<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">\n    <path d="M4 6h16M4 12h16M4 18h16"/>\n</svg>\n';
var Toolbar = /*#__PURE__*/ function() {
    "use strict";
    function Toolbar(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Toolbar);
        this.w = w;
        this.ctx = ctx;
        this.ev = this.w.config.chart.events;
        this.selectedClass = "apexcharts-selected";
        this.localeValues = this.w.globals.locale.toolbar;
        this.minX = w.globals.minX;
        this.maxX = w.globals.maxX;
        this.elZoom = null;
        this.elZoomIn = null;
        this.elZoomOut = null;
        this.elPan = null;
        this.elSelection = null;
        this.elMeasure = null;
        this.elZoomReset = null;
        this.elMenuIcon = null;
        this.elMenu = null;
        this.elMenuItems = [];
        this.t = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Toolbar, [
        {
            key: "createToolbar",
            value: function createToolbar() {
                var _this = this;
                var _a, _b;
                var w = this.w;
                var createDiv = function createDiv() {
                    return BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                };
                var createBtn = function createBtn() {
                    var btn = /** @type {HTMLButtonElement} */ BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "button");
                    btn.setAttribute("type", "button");
                    return btn;
                };
                var elToolbarWrap = createDiv();
                elToolbarWrap.setAttribute("class", "apexcharts-toolbar");
                elToolbarWrap.style.top = w.config.chart.toolbar.offsetY + "px";
                elToolbarWrap.style.right = -w.config.chart.toolbar.offsetX + 3 + "px";
                w.dom.elWrap.appendChild(elToolbarWrap);
                this.elZoom = createBtn();
                this.elZoomIn = createBtn();
                this.elZoomOut = createBtn();
                this.elPan = createBtn();
                this.elSelection = createBtn();
                this.elMeasure = createBtn();
                this.elZoomReset = createBtn();
                this.elMenuIcon = createBtn();
                this.elMenu = createDiv();
                this.elCustomIcons = [];
                this.t = w.config.chart.toolbar.tools;
                if (Array.isArray(this.t.customIcons)) {
                    for(var i = 0; i < this.t.customIcons.length; i++){
                        this.elCustomIcons.push(createBtn());
                    }
                }
                var toolbarControls = [];
                var appendZoomControl = function appendZoomControl(type, el, ico) {
                    var tool = type.toLowerCase();
                    if (_this.t[tool] && w.config.chart.zoom.enabled) {
                        toolbarControls.push({
                            el: el,
                            icon: typeof _this.t[tool] === "string" ? _this.t[tool] : ico,
                            title: /** @type {any} */ _this.localeValues[type],
                            class: "apexcharts-".concat(tool, "-icon")
                        });
                    }
                };
                appendZoomControl("zoomIn", this.elZoomIn, icoZoomIn);
                appendZoomControl("zoomOut", this.elZoomOut, icoZoomOut);
                var zoomSelectionCtrls = function zoomSelectionCtrls(z) {
                    if (_this.t[z] && w.config.chart[z].enabled) {
                        toolbarControls.push({
                            el: z === "zoom" ? _this.elZoom : _this.elSelection,
                            icon: typeof _this.t[z] === "string" ? _this.t[z] : z === "zoom" ? icoZoom : icoSelect,
                            title: /** @type {any} */ _this.localeValues[z === "zoom" ? "selectionZoom" : "selection"],
                            class: "apexcharts-".concat(z, "-icon")
                        });
                    }
                };
                zoomSelectionCtrls("zoom");
                zoomSelectionCtrls("selection");
                if (this.t.pan && w.config.chart.zoom.enabled) {
                    toolbarControls.push({
                        el: this.elPan,
                        icon: typeof this.t.pan === "string" ? this.t.pan : icoPan,
                        title: this.localeValues.pan,
                        class: "apexcharts-pan-icon"
                    });
                }
                if (this.t.measure && w.config.chart.measure && w.config.chart.measure.enabled) {
                    toolbarControls.push({
                        el: this.elMeasure,
                        icon: typeof this.t.measure === "string" ? this.t.measure : icoMeasure,
                        title: /** @type {any} */ this.localeValues.measure || "Measure",
                        class: "apexcharts-measure-icon"
                    });
                }
                appendZoomControl("reset", this.elZoomReset, icoReset);
                if (this.t.download) {
                    toolbarControls.push({
                        el: this.elMenuIcon,
                        icon: typeof this.t.download === "string" ? this.t.download : icoMenu,
                        title: this.localeValues.menu,
                        class: "apexcharts-menu-icon"
                    });
                }
                for(var i1 = 0; i1 < this.elCustomIcons.length; i1++){
                    toolbarControls.push({
                        el: this.elCustomIcons[i1],
                        icon: this.t.customIcons[i1].icon,
                        title: this.t.customIcons[i1].title,
                        index: this.t.customIcons[i1].index,
                        class: "apexcharts-toolbar-custom-icon " + this.t.customIcons[i1].class
                    });
                }
                toolbarControls.forEach(function(t, index) {
                    if (t.index) {
                        Utils$1.moveIndexInArray(toolbarControls, index, t.index);
                    }
                });
                for(var i2 = 0; i2 < toolbarControls.length; i2++){
                    Graphics.setAttrs(toolbarControls[i2].el, {
                        class: toolbarControls[i2].class,
                        title: toolbarControls[i2].title,
                        "aria-label": toolbarControls[i2].title
                    });
                    toolbarControls[i2].el.innerHTML = toolbarControls[i2].icon;
                    elToolbarWrap.appendChild(toolbarControls[i2].el);
                }
                if (this.elZoom.parentNode) {
                    this.elZoom.setAttribute("aria-pressed", String(!!w.interact.zoomEnabled));
                }
                if (this.elSelection.parentNode) {
                    this.elSelection.setAttribute("aria-pressed", String(!!w.interact.selectionEnabled));
                }
                if (this.elPan.parentNode) {
                    this.elPan.setAttribute("aria-pressed", String(!!w.interact.panEnabled));
                }
                if (this.elMeasure.parentNode) {
                    this.elMeasure.setAttribute("aria-pressed", String(!!w.interact.measureEnabled));
                }
                if (this.elMenuIcon.parentNode) {
                    this.elMenuIcon.setAttribute("aria-haspopup", "true");
                    this.elMenuIcon.setAttribute("aria-expanded", "false");
                }
                this._createHamburgerMenu(elToolbarWrap);
                if (w.interact.zoomEnabled) {
                    this.elZoom.classList.add(this.selectedClass);
                } else if (w.interact.panEnabled) {
                    this.elPan.classList.add(this.selectedClass);
                } else if (w.interact.selectionEnabled) {
                    this.elSelection.classList.add(this.selectedClass);
                } else if (w.interact.measureEnabled && this.elMeasure) {
                    this.elMeasure.classList.add(this.selectedClass);
                    (_b = (_a = this.ctx.measure) == null ? void 0 : _a.startMeasure) == null ? void 0 : _b.call(_a);
                }
                this.addToolbarEventListeners();
            }
        },
        {
            /**
   * @param {Element} parent
   */ key: "_createHamburgerMenu",
            value: function _createHamburgerMenu(parent) {
                this.elMenuItems = [];
                parent.appendChild(/** @type {Node} */ this.elMenu);
                Graphics.setAttrs(this.elMenu, {
                    class: "apexcharts-menu",
                    role: "menu"
                });
                var menuItems = [
                    {
                        name: "exportSVG",
                        title: this.localeValues.exportToSVG
                    },
                    {
                        name: "exportPNG",
                        title: this.localeValues.exportToPNG
                    },
                    {
                        name: "exportCSV",
                        title: this.localeValues.exportToCSV
                    }
                ];
                for(var i = 0; i < menuItems.length; i++){
                    this.elMenuItems.push(BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div"));
                    this.elMenuItems[i].innerHTML = menuItems[i].title;
                    Graphics.setAttrs(this.elMenuItems[i], {
                        class: "apexcharts-menu-item ".concat(menuItems[i].name),
                        title: menuItems[i].title,
                        role: "menuitem",
                        tabindex: "-1"
                    });
                    this.elMenu.appendChild(this.elMenuItems[i]);
                }
            }
        },
        {
            key: "addToolbarEventListeners",
            value: function addToolbarEventListeners() {
                var _this = this;
                var _a, _b, _c, _d, _e, _f, _g, _h, _i;
                (_a = this.elZoomReset) == null ? void 0 : _a.addEventListener("click", this.handleZoomReset.bind(this));
                (_b = this.elSelection) == null ? void 0 : _b.addEventListener("click", this.toggleZoomSelection.bind(this, "selection"));
                (_c = this.elZoom) == null ? void 0 : _c.addEventListener("click", this.toggleZoomSelection.bind(this, "zoom"));
                (_d = this.elZoomIn) == null ? void 0 : _d.addEventListener("click", this.handleZoomIn.bind(this));
                (_e = this.elZoomOut) == null ? void 0 : _e.addEventListener("click", this.handleZoomOut.bind(this));
                (_f = this.elPan) == null ? void 0 : _f.addEventListener("click", this.togglePanning.bind(this));
                (_g = this.elMeasure) == null ? void 0 : _g.addEventListener("click", this.toggleMeasure.bind(this));
                (_h = this.elMenuIcon) == null ? void 0 : _h.addEventListener("click", this.toggleMenu.bind(this));
                this.elMenuItems.forEach(function(m) {
                    if (m.classList.contains("exportSVG")) {
                        m.addEventListener("click", _this.handleDownload.bind(_this, "svg"));
                    } else if (m.classList.contains("exportPNG")) {
                        m.addEventListener("click", _this.handleDownload.bind(_this, "png"));
                    } else if (m.classList.contains("exportCSV")) {
                        m.addEventListener("click", _this.handleDownload.bind(_this, "csv"));
                    }
                });
                for(var i = 0; i < this.t.customIcons.length; i++){
                    this.elCustomIcons[i].addEventListener("click", this.t.customIcons[i].click.bind(this, this.ctx, this.ctx.w));
                }
                var toolbarButtons = [
                    this.elZoomReset,
                    this.elSelection,
                    this.elZoom,
                    this.elZoomIn,
                    this.elZoomOut,
                    this.elPan,
                    this.elMeasure,
                    this.elMenuIcon
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this.elCustomIcons));
                toolbarButtons.forEach(function(btn) {
                    btn.addEventListener("keydown", function(e) {
                        if (e.key === "Enter" || e.key === " ") {
                            e.preventDefault();
                            var btnClass = btn.className;
                            btn.click();
                            requestAnimationFrame(function() {
                                var baseEl = _this.w.dom.baseEl;
                                if (!baseEl) return;
                                var apexClass = btnClass.split(" ").find(function(c) {
                                    return c.startsWith("apexcharts-");
                                });
                                if (!apexClass) return;
                                var restored = baseEl.querySelector(".".concat(apexClass));
                                if (restored) restored.focus();
                            });
                        }
                    });
                });
                (_i = this.elMenuIcon) == null ? void 0 : _i.addEventListener("keydown", function(e) {
                    var _a2;
                    if (e.key === "ArrowDown" || e.key === "ArrowUp") {
                        e.preventDefault();
                        if (!((_a2 = _this.elMenu) == null ? void 0 : _a2.classList.contains("apexcharts-menu-open"))) {
                            _this.toggleMenu();
                        }
                        window.setTimeout(function() {
                            var idx = e.key === "ArrowDown" ? 0 : _this.elMenuItems.length - 1;
                            if (_this.elMenuItems[idx]) _this.elMenuItems[idx].focus();
                        }, 20);
                    }
                });
                this.elMenuItems.forEach(function(m, idx) {
                    m.addEventListener("keydown", function(e) {
                        var _a2;
                        if (e.key === "ArrowDown") {
                            e.preventDefault();
                            var next = _this.elMenuItems[idx + 1] || _this.elMenuItems[0];
                            next.focus();
                        } else if (e.key === "ArrowUp") {
                            e.preventDefault();
                            var prev = _this.elMenuItems[idx - 1] || _this.elMenuItems[_this.elMenuItems.length - 1];
                            prev.focus();
                        } else if (e.key === "Escape" || e.key === "Tab") {
                            _this._closeMenu();
                            (_a2 = _this.elMenuIcon) == null ? void 0 : _a2.focus();
                            if (e.key === "Tab") ;
                            else {
                                e.preventDefault();
                            }
                        } else if (e.key === "Enter" || e.key === " ") {
                            e.preventDefault();
                            m.click();
                        }
                    });
                });
            }
        },
        {
            /**
   * @param {string} type
   */ key: "toggleZoomSelection",
            value: function toggleZoomSelection(type) {
                var charts = this.ctx.getSyncedCharts();
                charts.forEach(function(ch) {
                    var tb = ch.ctx.toolbar;
                    var enabledType = type === "selection" ? "selectionEnabled" : "zoomEnabled";
                    var wasEnabled = !!ch.w.globals[enabledType];
                    tb.toggleOtherControls();
                    var el = type === "selection" ? tb.elSelection : tb.elZoom;
                    if (!wasEnabled) {
                        ch.w.globals[enabledType] = true;
                        el.classList.add(tb.selectedClass);
                    }
                    el.setAttribute("aria-pressed", String(!!ch.w.globals[enabledType]));
                });
            }
        },
        {
            /**
   * Toggle the measure ruler tool. Mutually exclusive with zoom/pan/selection
   * (toggleOtherControls deselects those and disarms any active measure), so a
   * fresh enable arms the ruler via the Measure module's sticky mode.
   */ key: "toggleMeasure",
            value: function toggleMeasure() {
                var _a, _b, _c, _d;
                var w = this.w;
                var enabling = !w.interact.measureEnabled;
                this.toggleOtherControls();
                if (enabling) {
                    w.interact.measureEnabled = true;
                    (_a = this.elMeasure) == null ? void 0 : _a.classList.add(this.selectedClass);
                    (_c = (_b = this.ctx.measure) == null ? void 0 : _b.startMeasure) == null ? void 0 : _c.call(_b);
                }
                (_d = this.elMeasure) == null ? void 0 : _d.setAttribute("aria-pressed", String(w.interact.measureEnabled));
            }
        },
        {
            key: "getToolbarIconsReference",
            value: function getToolbarIconsReference() {
                var w = this.w;
                if (!this.elZoom) {
                    this.elZoom = w.dom.baseEl.querySelector(".apexcharts-zoom-icon");
                }
                if (!this.elPan) {
                    this.elPan = w.dom.baseEl.querySelector(".apexcharts-pan-icon");
                }
                if (!this.elSelection) {
                    this.elSelection = w.dom.baseEl.querySelector(".apexcharts-selection-icon");
                }
                if (!this.elMeasure) {
                    this.elMeasure = w.dom.baseEl.querySelector(".apexcharts-measure-icon");
                }
            }
        },
        {
            /**
   * @param {string} type
   */ key: "enableZoomPanFromToolbar",
            value: function enableZoomPanFromToolbar(type) {
                this.toggleOtherControls();
                type === "pan" ? this.w.interact.panEnabled = true : this.w.interact.zoomEnabled = true;
                var el = type === "pan" ? this.elPan : this.elZoom;
                var el2 = type === "pan" ? this.elZoom : this.elPan;
                if (el) {
                    el.classList.add(this.selectedClass);
                }
                if (el2) {
                    el2.classList.remove(this.selectedClass);
                }
            }
        },
        {
            key: "togglePanning",
            value: function togglePanning() {
                var charts = this.ctx.getSyncedCharts();
                charts.forEach(function(ch) {
                    var tb = ch.ctx.toolbar;
                    var wasEnabled = !!ch.w.interact.panEnabled;
                    tb.toggleOtherControls();
                    if (!wasEnabled) {
                        ch.w.interact.panEnabled = true;
                        tb.elPan.classList.add(tb.selectedClass);
                    }
                    tb.elPan.setAttribute("aria-pressed", String(!!ch.w.interact.panEnabled));
                });
            }
        },
        {
            key: "toggleOtherControls",
            value: function toggleOtherControls() {
                var _this = this;
                var _a, _b, _c;
                var w = this.w;
                w.interact.panEnabled = false;
                w.interact.zoomEnabled = false;
                w.interact.selectionEnabled = false;
                if (w.interact.measureEnabled) {
                    w.interact.measureEnabled = false;
                    (_b = (_a = this.ctx.measure) == null ? void 0 : _a.stopMeasure) == null ? void 0 : _b.call(_a);
                    (_c = this.elMeasure) == null ? void 0 : _c.setAttribute("aria-pressed", "false");
                }
                this.getToolbarIconsReference();
                var toggleEls = [
                    this.elPan,
                    this.elSelection,
                    this.elZoom,
                    this.elMeasure
                ];
                toggleEls.forEach(function(el) {
                    if (el) {
                        el.classList.remove(_this.selectedClass);
                    }
                });
            }
        },
        {
            /**
   * Read the current x-range from globals at click time.
   * Toolbar instance is kept alive across updates (Phase 8 lazy
   * instantiation), so cached this.minX/maxX go stale after a zoom.
   * @returns {{minX: number, maxX: number}}
   */ key: "_currentXRange",
            value: function _currentXRange() {
                var w = this.w;
                if (w.axisFlags.isRangeBar) {
                    return {
                        minX: w.globals.minY,
                        maxX: w.globals.maxY
                    };
                }
                return {
                    minX: w.globals.minX,
                    maxX: w.globals.maxX
                };
            }
        },
        {
            key: "handleZoomIn",
            value: function handleZoomIn() {
                var w = this.w;
                var _this__currentXRange = this._currentXRange(), minX = _this__currentXRange.minX, maxX = _this__currentXRange.maxX;
                this.minX = minX;
                this.maxX = maxX;
                var centerX = (minX + maxX) / 2;
                var newMinX = (minX + centerX) / 2;
                var newMaxX = (maxX + centerX) / 2;
                var newMinXMaxX = this._getNewMinXMaxX(newMinX, newMaxX);
                if (!w.interact.disableZoomIn) {
                    this.zoomUpdateOptions(newMinXMaxX.minX, newMinXMaxX.maxX);
                }
            }
        },
        {
            key: "handleZoomOut",
            value: function handleZoomOut() {
                var w = this.w;
                var _this__currentXRange = this._currentXRange(), minX = _this__currentXRange.minX, maxX = _this__currentXRange.maxX;
                this.minX = minX;
                this.maxX = maxX;
                if (w.config.xaxis.type === "datetime" && new Date(minX).getUTCFullYear() < 1e3) {
                    return;
                }
                var centerX = (minX + maxX) / 2;
                var newMinX = minX - (centerX - minX);
                var newMaxX = maxX - (centerX - maxX);
                var newMinXMaxX = this._getNewMinXMaxX(newMinX, newMaxX);
                if (!w.interact.disableZoomOut) {
                    this.zoomUpdateOptions(newMinXMaxX.minX, newMinXMaxX.maxX);
                }
            }
        },
        {
            /**
   * @param {number} newMinX
   * @param {number} newMaxX
   */ key: "_getNewMinXMaxX",
            value: function _getNewMinXMaxX(newMinX, newMaxX) {
                var shouldFloor = this.w.config.xaxis.convertedCatToNumeric;
                return {
                    minX: shouldFloor ? Math.floor(newMinX) : newMinX,
                    maxX: shouldFloor ? Math.floor(newMaxX) : newMaxX
                };
            }
        },
        {
            /**
   * @param {number} newMinX
   * @param {number} newMaxX
   */ key: "zoomUpdateOptions",
            value: function zoomUpdateOptions(newMinX, newMaxX) {
                var w = this.w;
                if (newMinX === void 0 && newMaxX === void 0) {
                    this.handleZoomReset();
                    return;
                }
                if (w.config.xaxis.convertedCatToNumeric) {
                    if (newMinX < 1) {
                        newMinX = 1;
                        newMaxX = w.globals.dataPoints;
                    }
                    if (newMaxX - newMinX < 2) {
                        return;
                    }
                }
                var xaxis = {
                    min: newMinX,
                    max: newMaxX
                };
                var beforeZoomRange = this.getBeforeZoomRange(xaxis, /** @type {any} */ void 0);
                if (beforeZoomRange) {
                    xaxis = beforeZoomRange.xaxis;
                }
                var options2 = {
                    xaxis: xaxis
                };
                if (!w.globals.initialConfig) return;
                var yaxis = Utils$1.clone(w.globals.initialConfig.yaxis);
                if (!w.config.chart.group) {
                    options2.yaxis = yaxis;
                }
                this.w.interact.zoomed = true;
                this.ctx.updateHelpers._updateOptions(options2, false, this.w.config.chart.animations.dynamicAnimation.enabled);
                this.zoomCallback(xaxis, yaxis);
            }
        },
        {
            /**
   * @param {Record<string, any>} xaxis
   * @param {Record<string, any>} yaxis
   */ key: "zoomCallback",
            value: function zoomCallback(xaxis, yaxis) {
                if (typeof this.ev.zoomed === "function") {
                    this.ev.zoomed(this.ctx, {
                        xaxis: xaxis,
                        yaxis: yaxis
                    });
                    this.ctx.events.fireEvent("zoomed", {
                        xaxis: xaxis,
                        yaxis: yaxis
                    });
                }
            }
        },
        {
            /**
   * @param {Record<string, any>} xaxis
   * @param {Record<string, any>} yaxis
   */ key: "getBeforeZoomRange",
            value: function getBeforeZoomRange(xaxis, yaxis) {
                var newRange = null;
                if (typeof this.ev.beforeZoom === "function") {
                    newRange = this.ev.beforeZoom(this, {
                        xaxis: xaxis,
                        yaxis: yaxis
                    });
                }
                return newRange;
            }
        },
        {
            key: "toggleMenu",
            value: function toggleMenu() {
                var _this = this;
                window.setTimeout(function() {
                    var _a, _b, _c;
                    if ((_a = _this.elMenu) == null ? void 0 : _a.classList.contains("apexcharts-menu-open")) {
                        _this._closeMenu();
                    } else {
                        (_b = _this.elMenu) == null ? void 0 : _b.classList.add("apexcharts-menu-open");
                        (_c = _this.elMenuIcon) == null ? void 0 : _c.setAttribute("aria-expanded", "true");
                    }
                }, 0);
            }
        },
        {
            key: "_closeMenu",
            value: function _closeMenu() {
                var _a, _b;
                (_a = this.elMenu) == null ? void 0 : _a.classList.remove("apexcharts-menu-open");
                (_b = this.elMenuIcon) == null ? void 0 : _b.setAttribute("aria-expanded", "false");
            }
        },
        {
            /**
   * @param {string} type
   */ key: "handleDownload",
            value: function handleDownload(type) {
                var w = this.w;
                var exprt = new Exports(this.w, this.ctx);
                switch(type){
                    case "svg":
                        exprt.exportToSVG();
                        break;
                    case "png":
                        exprt.exportToPng();
                        break;
                    case "csv":
                        exprt.exportToCSV({
                            series: w.config.series,
                            columnDelimiter: w.config.chart.toolbar.export.csv.columnDelimiter
                        });
                        break;
                }
            }
        },
        {
            key: "handleZoomReset",
            value: function handleZoomReset() {
                var charts = this.ctx.getSyncedCharts();
                charts.forEach(function(ch) {
                    var w = ch.w;
                    if (!w.interact.zoomed) return;
                    w.globals.lastXAxis.min = w.globals.initialConfig.xaxis.min;
                    w.globals.lastXAxis.max = w.globals.initialConfig.xaxis.max;
                    ch.updateHelpers.revertDefaultAxisMinMax();
                    if (typeof w.config.chart.events.beforeResetZoom === "function") {
                        var resetZoomRange = w.config.chart.events.beforeResetZoom(ch, w);
                        if (resetZoomRange) {
                            ch.updateHelpers.revertDefaultAxisMinMax(resetZoomRange);
                        }
                    }
                    if (typeof w.config.chart.events.zoomed === "function") {
                        ch.ctx.toolbar.zoomCallback({
                            min: w.config.xaxis.min,
                            max: w.config.xaxis.max
                        });
                    }
                    var series = ch.ctx.series.emptyCollapsedSeries(Utils$1.clone(w.globals.initialSeries));
                    ch.updateHelpers._updateSeries(series, w.config.chart.animations.dynamicAnimation.enabled);
                    w.interact.zoomed = false;
                });
            }
        },
        {
            key: "destroy",
            value: function destroy() {
                this.elZoom = null;
                this.elZoomIn = null;
                this.elZoomOut = null;
                this.elPan = null;
                this.elSelection = null;
                this.elMeasure = null;
                this.elZoomReset = null;
                this.elMenuIcon = null;
            }
        }
    ]);
    return Toolbar;
}();
var AxisMapping = /*#__PURE__*/ function() {
    "use strict";
    function AxisMapping() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, AxisMapping);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(AxisMapping, null, [
        {
            key: "xRatio",
            value: /**
   * Pixels per data-unit on the x-axis. Derived from `minX..maxX` so it is the
   * exact inverse used by both {@link dataXToPx} and {@link pxToDataX}.
   * @param {import('../types/internal').ChartStateW} w
   * @returns {number}
   */ function xRatio(w) {
                var gw = w.layout.gridWidth || 1;
                return (w.globals.maxX - w.globals.minX) / gw;
            }
        },
        {
            key: "dataXToPx",
            value: /**
   * Data-x -> pixels from the plot origin (usable as an SVG `x` attribute).
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} dataX
   * @returns {number}
   */ function dataXToPx(w, dataX) {
                return (dataX - w.globals.minX) / AxisMapping.xRatio(w);
            }
        },
        {
            key: "pxToDataX",
            value: /**
   * Pixels from the plot origin -> data-x. Feed it `screenX - svgLeft - translateX`.
   * @param {import('../types/internal').ChartStateW} w
   * @param {number} px
   * @returns {number}
   */ function pxToDataX(w, px) {
                return w.globals.minX + px * AxisMapping.xRatio(w);
            }
        }
    ]);
    return AxisMapping;
}();
var WHEEL_ZOOM_PIXELS_PER_2X = 240;
var INERTIA_MIN_RELEASE_VELOCITY = 0.05;
var INERTIA_DEFAULT_FRICTION = 0.92;
var INERTIA_STOP_VELOCITY = 0.02;
var FRAME_MS_60FPS = 16.6667;
var PAN_NUDGE_DIVISOR = 15;
var ZoomPanSelection = /*#__PURE__*/ function(Toolbar) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ZoomPanSelection, Toolbar);
    function ZoomPanSelection(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, ZoomPanSelection);
        var _this;
        _this = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, ZoomPanSelection, [
            w,
            ctx
        ]);
        _this.w = w;
        _this.ctx = ctx;
        _this.dragged = false;
        _this.graphics = new Graphics(_this.w);
        _this.eventList = [
            "mousedown",
            "mouseleave",
            "mousemove",
            "touchstart",
            "touchmove",
            "mouseup",
            "touchend"
        ];
        _this.clientX = 0;
        _this.clientY = 0;
        _this.startX = 0;
        _this.endX = 0;
        _this.dragX = 0;
        _this.startY = 0;
        _this.endY = 0;
        _this.dragY = 0;
        _this.moveDirection = "none";
        return _this;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ZoomPanSelection, [
        {
            /** @param {{xyRatios: any}} opts */ key: "init",
            value: function init(param) {
                var _this = this;
                var xyRatios = param.xyRatios;
                var w = this.w;
                var me = this;
                this.xyRatios = xyRatios;
                this.zoomRect = this.graphics.drawRect(0, 0, 0, 0);
                this.selectionRect = this.graphics.drawRect(0, 0, 0, 0);
                this.constraints = new Box(0, 0, w.layout.gridWidth, w.layout.gridHeight);
                this.zoomRect.node.classList.add("apexcharts-zoom-rect");
                this.selectionRect.node.classList.add("apexcharts-selection-rect");
                w.dom.Paper.add(this.zoomRect);
                w.dom.Paper.add(this.selectionRect);
                if (w.config.chart.selection.type === "x") {
                    this.slDraggableRect = this.selectionRect.draggable({
                        minX: 0,
                        minY: 0,
                        maxX: w.layout.gridWidth,
                        maxY: w.layout.gridHeight
                    }).on("dragmove.namespace", this.selectionDragging.bind(this, "dragging"));
                } else if (w.config.chart.selection.type === "y") {
                    this.slDraggableRect = this.selectionRect.draggable({
                        minX: 0,
                        maxX: w.layout.gridWidth
                    }).on("dragmove.namespace", this.selectionDragging.bind(this, "dragging"));
                } else {
                    this.slDraggableRect = this.selectionRect.draggable().on("dragmove.namespace", this.selectionDragging.bind(this, "dragging"));
                }
                this.preselectedSelection();
                this.hoverArea = /** @type {Element} */ w.dom.baseEl.querySelector("".concat(w.globals.chartClass, " .apexcharts-svg"));
                if (!this.hoverArea) return;
                this.hoverArea.classList.add("apexcharts-zoomable");
                this.eventList.forEach(function(event) {
                    var _a;
                    (_a = _this.hoverArea) == null ? void 0 : _a.addEventListener(event, me.svgMouseEvents.bind(me, xyRatios), {
                        capture: false,
                        passive: true
                    });
                });
                if (w.config.chart.zoom.enabled && w.config.chart.zoom.allowMouseWheelZoom) {
                    this.hoverArea.addEventListener("wheel", me.mouseWheelEvent.bind(me), {
                        capture: false,
                        passive: false
                    });
                }
                if (this._momentumEnabled()) {
                    [
                        "touchstart",
                        "touchmove",
                        "touchend",
                        "touchcancel"
                    ].forEach(function(event) {
                        var _a;
                        (_a = _this.hoverArea) == null ? void 0 : _a.addEventListener(event, me.momentumTouch.bind(me), {
                            capture: false,
                            passive: false
                        });
                    });
                }
            }
        },
        {
            // remove the event listeners which were previously added on hover area
            key: "destroy",
            value: function destroy() {
                if (this.slDraggableRect) {
                    this.slDraggableRect.draggable(false);
                    this.slDraggableRect.off();
                    this.selectionRect.off();
                }
                this.selectionRect = null;
                this.zoomRect = null;
            }
        },
        {
            /**
   * @param {import('../types/internal').XYRatios} xyRatios
   * @param {any} e
   */ key: "svgMouseEvents",
            value: function svgMouseEvents(xyRatios, e) {
                var w = this.w;
                var toolbar = this.ctx.toolbar;
                if (w.interact.momentum && w.interact.momentum.busy) return;
                if (this._momentumEnabled() && e.touches && e.touches.length > 1) {
                    return;
                }
                var zoomtype = w.interact.zoomEnabled ? w.config.chart.zoom.type : w.config.chart.selection.type;
                var autoSelected = w.config.chart.toolbar.autoSelected;
                if (autoSelected !== "measure") {
                    if (e.shiftKey) {
                        w.interact.shiftWasPressed = true;
                        toolbar.enableZoomPanFromToolbar(autoSelected === "pan" ? "zoom" : "pan");
                    } else {
                        if (w.interact.shiftWasPressed) {
                            toolbar.enableZoomPanFromToolbar(autoSelected);
                            w.interact.shiftWasPressed = false;
                        }
                    }
                }
                if (!e.target) return;
                var tc = e.target.classList;
                var pc;
                if (e.target.parentNode && e.target.parentNode !== null) {
                    pc = e.target.parentNode.classList;
                }
                var falsePositives = tc.contains("apexcharts-legend-marker") || tc.contains("apexcharts-legend-text") || pc && pc.contains("apexcharts-toolbar");
                if (falsePositives) return;
                this.clientX = e.type === "touchmove" || e.type === "touchstart" ? e.touches[0].clientX : e.type === "touchend" ? e.changedTouches[0].clientX : e.clientX;
                this.clientY = e.type === "touchmove" || e.type === "touchstart" ? e.touches[0].clientY : e.type === "touchend" ? e.changedTouches[0].clientY : e.clientY;
                if (e.type === "mousedown" && e.which === 1 || e.type === "touchstart") {
                    var gridRectDim = this._gridRect();
                    if (!gridRectDim) return;
                    this.startX = this._screenXToPlotPx(this.clientX);
                    this.startY = this.clientY - gridRectDim.top;
                    this.dragged = false;
                    this.w.interact.mousedown = true;
                }
                if (e.type === "mousemove" && e.which === 1 || e.type === "touchmove") {
                    this.dragged = true;
                    if (w.interact.panEnabled) {
                        w.interact.selection = null;
                        if (this.w.interact.mousedown) {
                            this.panDragging({
                                context: this,
                                zoomtype: zoomtype,
                                xyRatios: this.xyRatios
                            });
                        }
                    } else {
                        if (this.w.interact.mousedown && w.interact.zoomEnabled || this.w.interact.mousedown && w.interact.selectionEnabled) {
                            this.selection = this.selectionDrawing({
                                context: this,
                                zoomtype: zoomtype
                            });
                        }
                    }
                }
                if (e.type === "mouseup" || e.type === "touchend" || e.type === "mouseleave") {
                    this.handleMouseUp({
                        zoomtype: zoomtype
                    });
                }
                this.makeSelectionRectDraggable();
            }
        },
        {
            /** @param {{ zoomtype?: any, isResized?: any }} opts */ key: "handleMouseUp",
            value: function handleMouseUp(param) {
                var zoomtype = param.zoomtype, isResized = param.isResized;
                var w = this.w;
                var gridRectDim = this._gridRect();
                if (gridRectDim && (this.w.interact.mousedown || isResized)) {
                    this.endX = this._screenXToPlotPx(this.clientX);
                    this.endY = this.clientY - gridRectDim.top;
                    this.dragX = Math.abs(this.endX - this.startX);
                    this.dragY = Math.abs(this.endY - this.startY);
                    if (w.interact.zoomEnabled || w.interact.selectionEnabled) {
                        this.selectionDrawn({
                            context: this,
                            zoomtype: zoomtype
                        });
                    }
                }
                if (w.interact.zoomEnabled) {
                    this.hideSelectionRect(this.selectionRect);
                }
                this.dragged = false;
                this.w.interact.mousedown = false;
            }
        },
        {
            // ---------------------------------------------------------------------------
            // Wheel zoom: continuous, cursor-anchored zoom on mouse wheel / trackpad.
            //
            // Each wheel event multiplies a pending zoom factor scaled to its deltaY (so
            // a trackpad's stream of tiny deltas and a discrete wheel's ±100 notches both
            // feel proportional), and the accumulated factor is applied at most once per
            // animation frame through the same immediate, animation-free fast path the
            // touch pinch uses (_applyXRange). Deliberately instant, trading-chart style:
            // no per-step morph and no easing between steps (an animated variant was
            // tried and rejected). The original implementation instead ran a fixed
            // 0.5x/1.5x animated update at most once per 400ms and dropped every wheel
            // event in between, which read as lag on continuous scrolling.
            //
            // Like Momentum (see the comment above momentumTouch), applying a frame
            // triggers _updateOptions, which destroys and recreates this instance
            // mid-gesture, so all wheel-gesture state lives on w.interact.wheel rather
            // than on the instance.
            // ---------------------------------------------------------------------------
            /** Lazily-created, re-render-surviving wheel-gesture state. */ key: "_wheel",
            value: function _wheel() {
                var it = this.w.interact;
                if (!it.wheel) {
                    it.wheel = {
                        factor: 1,
                        clientX: 0,
                        /** @type {number|null} */ rafId: null,
                        /** @type {any} */ endTimer: null
                    };
                }
                return it.wheel;
            }
        },
        {
            /**
   * @param {any} e
   */ key: "mouseWheelEvent",
            value: function mouseWheelEvent(e) {
                var _this = this;
                e.preventDefault();
                var st = this._wheel();
                var dy = e.deltaY;
                if (e.deltaMode === 1) dy *= 33;
                else if (e.deltaMode === 2) dy *= 330;
                st.factor *= Math.pow(2, dy / WHEEL_ZOOM_PIXELS_PER_2X);
                st.clientX = e.clientX;
                if (st.rafId == null) {
                    st.rafId = requestAnimationFrame(function() {
                        return _this._applyWheelZoom();
                    });
                }
                if (st.endTimer) clearTimeout(st.endTimer);
                st.endTimer = setTimeout(function() {
                    return _this._endWheelZoom();
                }, 150);
            }
        },
        {
            /**
   * Apply the zoom factor accumulated since the last animation frame, keeping
   * the data value under the cursor pinned (both zooming in and out).
   */ key: "_applyWheelZoom",
            value: function _applyWheelZoom() {
                var w = this.w;
                var st = this._wheel();
                st.rafId = null;
                var scale = st.factor;
                st.factor = 1;
                if (scale === 1 || w.globals.isDestroyed) return;
                var gridRectDim = this._gridRect();
                if (!gridRectDim || !gridRectDim.width) return;
                var _this__currentXWindow = this._currentXWindow(), min = _this__currentXWindow.min, max = _this__currentXWindow.max;
                var range = max - min;
                var mouseX = Math.min(Math.max((st.clientX - gridRectDim.left) / gridRectDim.width, 0), 1);
                var newRange = range * scale;
                var bounds = this._clampBounds();
                if (bounds) {
                    var minXDiff = w.globals.minXDiff > 0 && isFinite(w.globals.minXDiff) ? w.globals.minXDiff : 0;
                    var minRange = Math.max(minXDiff * 2, (bounds.max - bounds.min) * 1e-6);
                    if (newRange < minRange) newRange = minRange;
                    if (newRange > bounds.max - bounds.min) newRange = bounds.max - bounds.min;
                }
                var anchor = min + mouseX * range;
                var newMinX = anchor - mouseX * newRange;
                var newMaxX = newMinX + newRange;
                var eps = range * 1e-9;
                if (Math.abs(newMinX - min) < eps && Math.abs(newMaxX - max) < eps) return;
                if (isNaN(newMinX) || isNaN(newMaxX)) return;
                var beforeZoomRange = this.getBeforeZoomRange({
                    min: newMinX,
                    max: newMaxX
                }, /** @type {any} */ void 0);
                if (beforeZoomRange && beforeZoomRange.xaxis) {
                    newMinX = beforeZoomRange.xaxis.min;
                    newMaxX = beforeZoomRange.xaxis.max;
                }
                this._applyXRange(newMinX, newMaxX, true);
            }
        },
        {
            /** Fire the zoomed callback once the wheel gesture settles (mirrors _endPinch). */ key: "_endWheelZoom",
            value: function _endWheelZoom() {
                var w = this.w;
                var st = this._wheel();
                st.endTimer = null;
                if (w.globals.isDestroyed || !w.interact.zoomed) return;
                var _this__currentXWindow = this._currentXWindow(), min = _this__currentXWindow.min, max = _this__currentXWindow.max;
                var yaxis = w.globals.initialConfig ? Utils$1.clone(w.globals.initialConfig.yaxis) : [];
                var toolbar = this.ctx.toolbar;
                if (toolbar) toolbar.zoomCallback({
                    min: min,
                    max: max
                }, yaxis);
            }
        },
        {
            key: "makeSelectionRectDraggable",
            value: function makeSelectionRectDraggable() {
                var _this = this;
                var w = this.w;
                if (!this.selectionRect) return;
                var rectDim = this.selectionRect.node.getBoundingClientRect();
                if (rectDim.width > 0 && rectDim.height > 0) {
                    this.selectionRect.select(false).resize(false);
                    this.selectionRect.select({
                        createRot: function createRot() {},
                        updateRot: function updateRot() {},
                        createHandle: function createHandle(group, p, index, pointArr, handleName) {
                            if (handleName === "l" || handleName === "r") return group.circle(8).css({
                                "stroke-width": 1,
                                stroke: "#333",
                                fill: "#fff"
                            });
                            return group.circle(0);
                        },
                        updateHandle: function updateHandle(group, p) {
                            return group.center(p[0], p[1]);
                        }
                    }).resize().on("resize", function() {
                        var _a;
                        if (w.interact.selectionEnabled) {
                            w.interact.selection = {
                                x: parseFloat(_this.selectionRect.node.getAttribute("x")),
                                y: parseFloat(_this.selectionRect.node.getAttribute("y")),
                                width: parseFloat(_this.selectionRect.node.getAttribute("width")),
                                height: parseFloat(_this.selectionRect.node.getAttribute("height"))
                            };
                            clearTimeout((_a = _this.w.globals.selectionResizeTimer) != null ? _a : void 0);
                            _this.w.globals.selectionResizeTimer = window.setTimeout(function() {
                                _this._emitSelectionFromRect();
                            }, 30);
                        } else {
                            var zoomtype = w.interact.zoomEnabled ? w.config.chart.zoom.type : w.config.chart.selection.type;
                            _this.handleMouseUp({
                                zoomtype: zoomtype,
                                isResized: true
                            });
                        }
                    });
                }
            }
        },
        {
            key: "preselectedSelection",
            value: function preselectedSelection() {
                var w = this.w;
                var xyRatios = this.xyRatios;
                if (!w.interact.zoomEnabled) {
                    if (typeof w.interact.selection !== "undefined" && w.interact.selection !== null) {
                        this.drawSelectionRect(__spreadProps(__spreadValues({}, w.interact.selection), {
                            translateX: w.layout.translateX,
                            translateY: w.layout.translateY
                        }));
                    } else {
                        if (w.config.chart.selection.xaxis.min !== void 0 && w.config.chart.selection.xaxis.max !== void 0) {
                            var x = AxisMapping.dataXToPx(w, w.config.chart.selection.xaxis.min);
                            var width = AxisMapping.dataXToPx(w, w.config.chart.selection.xaxis.max) - x;
                            if (w.axisFlags.isRangeBar) {
                                x = (w.config.chart.selection.xaxis.min - w.globals.yAxisScale[0].niceMin) / xyRatios.invertedYRatio;
                                width = (w.config.chart.selection.xaxis.max - w.config.chart.selection.xaxis.min) / xyRatios.invertedYRatio;
                            }
                            var selectionRect = {
                                x: x,
                                y: 0,
                                width: width,
                                height: w.layout.gridHeight,
                                translateX: w.layout.translateX,
                                translateY: w.layout.translateY,
                                selectionEnabled: true
                            };
                            this.drawSelectionRect(selectionRect);
                            this.makeSelectionRectDraggable();
                            if (typeof w.config.chart.events.selection === "function") {
                                w.config.chart.events.selection(this.ctx, {
                                    xaxis: {
                                        min: w.config.chart.selection.xaxis.min,
                                        max: w.config.chart.selection.xaxis.max
                                    },
                                    yaxis: {}
                                });
                            }
                        }
                    }
                }
            }
        },
        {
            /** @param {{x: any, y: any, width: any, height: any, translateX: any, translateY: any}} opts */ key: "drawSelectionRect",
            value: function drawSelectionRect(param) {
                var x = param.x, y = param.y, width = param.width, height = param.height, _param_translateX = param.translateX, translateX = _param_translateX === void 0 ? 0 : _param_translateX, _param_translateY = param.translateY, translateY = _param_translateY === void 0 ? 0 : _param_translateY;
                var w = this.w;
                var zoomRect = this.zoomRect;
                var selectionRect = this.selectionRect;
                if (this.dragged || w.interact.selection !== null) {
                    var scalingAttrs = {
                        transform: "translate(" + translateX + ", " + translateY + ")"
                    };
                    if (w.interact.zoomEnabled && this.dragged) {
                        if (width < 0) width = 1;
                        zoomRect.attr({
                            x: x,
                            y: y,
                            width: width,
                            height: height,
                            fill: w.config.chart.zoom.zoomedArea.fill.color,
                            "fill-opacity": w.config.chart.zoom.zoomedArea.fill.opacity,
                            stroke: w.config.chart.zoom.zoomedArea.stroke.color,
                            "stroke-width": w.config.chart.zoom.zoomedArea.stroke.width,
                            "stroke-opacity": w.config.chart.zoom.zoomedArea.stroke.opacity
                        });
                        Graphics.setAttrs(zoomRect.node, scalingAttrs);
                    }
                    if (w.interact.selectionEnabled) {
                        selectionRect.attr({
                            x: x,
                            y: y,
                            width: width > 0 ? width : 0,
                            height: height > 0 ? height : 0,
                            fill: w.config.chart.selection.fill.color,
                            "fill-opacity": w.config.chart.selection.fill.opacity,
                            stroke: w.config.chart.selection.stroke.color,
                            "stroke-width": w.config.chart.selection.stroke.width,
                            "stroke-dasharray": w.config.chart.selection.stroke.dashArray,
                            "stroke-opacity": w.config.chart.selection.stroke.opacity
                        });
                        Graphics.setAttrs(selectionRect.node, scalingAttrs);
                    }
                }
            }
        },
        {
            /**
   * @param {any} rect
   */ key: "hideSelectionRect",
            value: function hideSelectionRect(rect) {
                if (rect) {
                    rect.attr({
                        x: 0,
                        y: 0,
                        width: 0,
                        height: 0
                    });
                }
            }
        },
        {
            key: "selectionDrawing",
            value: function selectionDrawing(param) {
                var context = param.context, zoomtype = param.zoomtype;
                var w = this.w;
                var me = context;
                var gridRectDim = this._gridRect();
                if (!gridRectDim) return;
                var startX = me.startX - 1;
                var startY = me.startY;
                var inversedX = false;
                var inversedY = false;
                var left = this._screenXToPlotPx(me.clientX);
                var top = me.clientY - gridRectDim.top;
                var selectionWidth = left - startX;
                var selectionHeight = top - startY;
                var selectionRect = {
                    translateX: w.layout.translateX,
                    translateY: w.layout.translateY
                };
                if (Math.abs(selectionWidth + startX) > w.layout.gridWidth) {
                    selectionWidth = w.layout.gridWidth - startX;
                } else if (left < 0) {
                    selectionWidth = startX;
                }
                if (startX > left) {
                    inversedX = true;
                    selectionWidth = Math.abs(selectionWidth);
                }
                if (startY > top) {
                    inversedY = true;
                    selectionHeight = Math.abs(selectionHeight);
                }
                if (zoomtype === "x") {
                    selectionRect = {
                        x: inversedX ? startX - selectionWidth : startX,
                        y: 0,
                        width: selectionWidth,
                        height: w.layout.gridHeight
                    };
                } else if (zoomtype === "y") {
                    selectionRect = {
                        x: 0,
                        y: inversedY ? startY - selectionHeight : startY,
                        width: w.layout.gridWidth,
                        height: selectionHeight
                    };
                } else {
                    selectionRect = {
                        x: inversedX ? startX - selectionWidth : startX,
                        y: inversedY ? startY - selectionHeight : startY,
                        width: selectionWidth,
                        height: selectionHeight
                    };
                }
                selectionRect = __spreadProps(__spreadValues({}, selectionRect), {
                    translateX: w.layout.translateX,
                    translateY: w.layout.translateY
                });
                me.drawSelectionRect(selectionRect);
                me.selectionDragging("resizing");
                return selectionRect;
            }
        },
        {
            /**
   * @param {string} type
   * @param {CustomEvent} e
   */ key: "selectionDragging",
            value: function selectionDragging(type, e) {
                var _this = this;
                var _a;
                var w = this.w;
                if (!e) return;
                e.preventDefault();
                var _e_detail = e.detail, handler = _e_detail.handler, box = _e_detail.box;
                var constraints = /** @type {any} */ this.constraints;
                var x = box.x, y = box.y;
                if (x < constraints.x) {
                    x = constraints.x;
                }
                if (y < constraints.y) {
                    y = constraints.y;
                }
                if (box.x2 > constraints.x2) {
                    x = constraints.x2 - box.w;
                }
                if (box.y2 > constraints.y2) {
                    y = constraints.y2 - box.h;
                }
                handler.move(x, y);
                var selRect = this.selectionRect;
                var timerInterval = 0;
                if (type === "resizing") {
                    timerInterval = 30;
                }
                var getSelAttr = function getSelAttr(attr) {
                    return parseFloat(selRect.node.getAttribute(attr));
                };
                var draggedProps = {
                    x: getSelAttr("x"),
                    y: getSelAttr("y"),
                    width: getSelAttr("width"),
                    height: getSelAttr("height")
                };
                w.interact.selection = draggedProps;
                var link = w.config.chart.link;
                var linkActive = !!(link && (link.enabled || typeof link.dimension === "function"));
                if ((typeof w.config.chart.events.selection === "function" || linkActive) && w.interact.selectionEnabled) {
                    clearTimeout((_a = this.w.globals.selectionResizeTimer) != null ? _a : void 0);
                    this.w.globals.selectionResizeTimer = window.setTimeout(function() {
                        _this._emitSelectionFromRect();
                    }, timerInterval);
                }
            }
        },
        {
            /**
   * Recompute the reported x/y range from the CURRENT persistent selection rect
   * (via the shared AxisMapping) and notify listeners: chart.events.selection,
   * brushScrolled, and the crossfilter coordinator. Shared by the rect-body drag
   * (selectionDragging) and the handle resize (makeSelectionRectDraggable) so
   * every gesture re-reports through ONE mapping and the reported range always
   * matches the rect the user sees. No dragged/threshold gate: reaching here
   * already means the user moved or resized the persistent rect.
   */ key: "_emitSelectionFromRect",
            value: function _emitSelectionFromRect() {
                var _a;
                var w = this.w;
                if (!w.interact.selectionEnabled) return;
                var link = w.config.chart.link;
                var linkActive = !!(link && (link.enabled || typeof link.dimension === "function"));
                if (typeof w.config.chart.events.selection !== "function" && !linkActive) {
                    return;
                }
                var gridRectDim = this._gridRect();
                if (!gridRectDim) return;
                var selectionRect = this.selectionRect.node.getBoundingClientRect();
                var xyRatios = this.xyRatios;
                var minX, maxX, minY, maxY;
                var relLeft = this._screenXToPlotPx(selectionRect.left);
                var relRight = this._screenXToPlotPx(selectionRect.right);
                if (!w.axisFlags.isRangeBar) {
                    if (!w.globals.xAxisScale) return;
                    minX = AxisMapping.pxToDataX(w, relLeft);
                    maxX = AxisMapping.pxToDataX(w, relRight);
                    minY = w.globals.yAxisScale[0].niceMin + (gridRectDim.bottom - selectionRect.bottom) * xyRatios.yRatio[0];
                    maxY = w.globals.yAxisScale[0].niceMax - (selectionRect.top - gridRectDim.top) * xyRatios.yRatio[0];
                } else {
                    minX = w.globals.yAxisScale[0].niceMin + relLeft * xyRatios.invertedYRatio;
                    maxX = w.globals.yAxisScale[0].niceMin + relRight * xyRatios.invertedYRatio;
                    minY = 0;
                    maxY = 1;
                }
                var xyAxis = {
                    xaxis: {
                        min: minX,
                        max: maxX
                    },
                    yaxis: {
                        min: minY,
                        max: maxY
                    }
                };
                if (typeof w.config.chart.events.selection === "function") {
                    w.config.chart.events.selection(this.ctx, xyAxis);
                }
                if (w.config.chart.brush.enabled && w.config.chart.events.brushScrolled !== void 0) {
                    w.config.chart.events.brushScrolled(this.ctx, xyAxis);
                }
                (_a = this.ctx.linkedViews) == null ? void 0 : _a.onSourceSelection(xyAxis.xaxis);
            }
        },
        {
            /** @param {{context: any, zoomtype: any}} opts */ key: "selectionDrawn",
            value: function selectionDrawn(param) {
                var context = param.context, zoomtype = param.zoomtype;
                var _a;
                var w = this.w;
                var me = context;
                var xyRatios = this.xyRatios;
                var toolbar = this.ctx.toolbar;
                var selRect = w.interact.zoomEnabled ? me.zoomRect.node.getBoundingClientRect() : me.selectionRect.node.getBoundingClientRect();
                var gridRectDim = me._gridRect();
                if (!gridRectDim) return;
                var localStartX = this._screenXToPlotPx(selRect.left);
                var localEndX = this._screenXToPlotPx(selRect.right);
                var localStartY = selRect.top - gridRectDim.top;
                var localEndY = selRect.bottom - gridRectDim.top;
                var xLowestValue, xHighestValue;
                if (!w.axisFlags.isRangeBar) {
                    xLowestValue = AxisMapping.pxToDataX(w, localStartX);
                    xHighestValue = AxisMapping.pxToDataX(w, localEndX);
                } else {
                    xLowestValue = w.globals.yAxisScale[0].niceMin + localStartX * xyRatios.invertedYRatio;
                    xHighestValue = w.globals.yAxisScale[0].niceMin + localEndX * xyRatios.invertedYRatio;
                }
                var yHighestValue = [];
                var yLowestValue = [];
                w.config.yaxis.forEach(function(yaxe, index) {
                    var seriesIndex = w.globals.seriesYAxisMap[index][0];
                    var highestVal = w.globals.yAxisScale[index].niceMax - xyRatios.yRatio[seriesIndex] * localStartY;
                    var lowestVal = w.globals.yAxisScale[index].niceMax - xyRatios.yRatio[seriesIndex] * localEndY;
                    yHighestValue.push(highestVal);
                    yLowestValue.push(lowestVal);
                });
                if (me.dragged && (me.dragX > 10 || me.dragY > 10) && xLowestValue !== xHighestValue) {
                    if (w.interact.zoomEnabled) {
                        if (!w.globals.initialConfig) return;
                        var yaxis = Utils$1.clone(w.globals.initialConfig.yaxis);
                        var xaxis = Utils$1.clone(w.globals.initialConfig.xaxis);
                        w.interact.zoomed = true;
                        if (w.config.xaxis.convertedCatToNumeric) {
                            xLowestValue = Math.floor(xLowestValue);
                            xHighestValue = Math.floor(xHighestValue);
                            if (xLowestValue < 1) {
                                xLowestValue = 1;
                                xHighestValue = w.globals.dataPoints;
                            }
                            if (xHighestValue - xLowestValue < 2) {
                                xHighestValue = xLowestValue + 1;
                            }
                        }
                        if (zoomtype === "xy" || zoomtype === "x") {
                            xaxis = {
                                min: xLowestValue,
                                max: xHighestValue
                            };
                        }
                        if (zoomtype === "xy" || zoomtype === "y") {
                            yaxis.forEach(function(yaxe, index) {
                                yaxis[index].min = yLowestValue[index];
                                yaxis[index].max = yHighestValue[index];
                            });
                        }
                        if (toolbar) {
                            var beforeZoomRange = toolbar.getBeforeZoomRange(xaxis, yaxis);
                            if (beforeZoomRange) {
                                xaxis = beforeZoomRange.xaxis ? beforeZoomRange.xaxis : xaxis;
                                yaxis = beforeZoomRange.yaxis ? beforeZoomRange.yaxis : yaxis;
                            }
                        }
                        var options2 = {
                            xaxis: xaxis
                        };
                        if (!w.config.chart.group) {
                            options2.yaxis = yaxis;
                        }
                        me.ctx.updateHelpers._updateOptions(options2, false, me.w.config.chart.animations.dynamicAnimation.enabled);
                        if (typeof w.config.chart.events.zoomed === "function") {
                            toolbar.zoomCallback(xaxis, yaxis);
                        }
                    } else if (w.interact.selectionEnabled) {
                        var yaxis1 = null;
                        var xaxis1 = null;
                        xaxis1 = {
                            min: xLowestValue,
                            max: xHighestValue
                        };
                        if (zoomtype === "xy" || zoomtype === "y") {
                            var yaxisCopy = /** @type {ApexYAxis[]} */ Utils$1.clone(w.config.yaxis);
                            yaxis1 = yaxisCopy;
                            yaxisCopy.forEach(function(yaxe, index) {
                                yaxisCopy[index].min = yLowestValue[index];
                                yaxisCopy[index].max = yHighestValue[index];
                            });
                        }
                        w.interact.selection = me.selection;
                        if (typeof w.config.chart.events.selection === "function") {
                            w.config.chart.events.selection(me.ctx, {
                                xaxis: xaxis1,
                                yaxis: yaxis1
                            });
                        }
                        (_a = me.ctx.linkedViews) == null ? void 0 : _a.onSourceSelection(xaxis1);
                    }
                }
            }
        },
        {
            /** @param {{ context?: any, zoomtype?: any, xyRatios?: any }} opts */ key: "panDragging",
            value: function panDragging(param) {
                var context = param.context;
                var _a;
                var w = this.w;
                var me = context;
                if (typeof w.interact.lastClientPosition.x !== "undefined") {
                    var deltaX = w.interact.lastClientPosition.x - me.clientX;
                    var deltaY = ((_a = w.interact.lastClientPosition.y) != null ? _a : 0) - me.clientY;
                    if (Math.abs(deltaX) > Math.abs(deltaY) && deltaX > 0) {
                        this.moveDirection = "left";
                    } else if (Math.abs(deltaX) > Math.abs(deltaY) && deltaX < 0) {
                        this.moveDirection = "right";
                    } else if (Math.abs(deltaY) > Math.abs(deltaX) && deltaY > 0) {
                        this.moveDirection = "up";
                    } else if (Math.abs(deltaY) > Math.abs(deltaX) && deltaY < 0) {
                        this.moveDirection = "down";
                    }
                }
                w.interact.lastClientPosition = {
                    x: me.clientX,
                    y: me.clientY
                };
                var xLowestValue = w.axisFlags.isRangeBar ? w.globals.minY : w.globals.minX;
                var xHighestValue = w.axisFlags.isRangeBar ? w.globals.maxY : w.globals.maxX;
                me.panScrolled(xLowestValue, xHighestValue);
            }
        },
        {
            // delayedPanScrolled() {
            //   const w = this.w
            //   let newMinX = w.globals.minX
            //   let newMaxX = w.globals.maxX
            //   const centerX = (w.globals.maxX - w.globals.minX) / 2
            //   if (this.moveDirection === 'left') {
            //     newMinX = w.globals.minX + centerX
            //     newMaxX = w.globals.maxX + centerX
            //   } else if (this.moveDirection === 'right') {
            //     newMinX = w.globals.minX - centerX
            //     newMaxX = w.globals.maxX - centerX
            //   }
            //   newMinX = Math.floor(newMinX)
            //   newMaxX = Math.floor(newMaxX)
            //   this.updateScrolledChart(
            //     { xaxis: { min: newMinX, max: newMaxX } },
            //     newMinX,
            //     newMaxX
            //   )
            // }
            /**
   * @param {number} xLowestValue
   * @param {number} xHighestValue
   */ key: "panScrolled",
            value: function panScrolled(xLowestValue, xHighestValue) {
                var _a, _b;
                var w = this.w;
                var xyRatios = this.xyRatios;
                if (!w.globals.initialConfig) return;
                var yaxis = Utils$1.clone(w.globals.initialConfig.yaxis);
                var xRatio = xyRatios.xRatio;
                var minX = w.globals.minX;
                var maxX = w.globals.maxX;
                if (w.axisFlags.isRangeBar) {
                    xRatio = xyRatios.invertedYRatio;
                    minX = w.globals.minY;
                    maxX = w.globals.maxY;
                }
                if (this.moveDirection === "left") {
                    xLowestValue = minX + w.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
                    xHighestValue = maxX + w.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
                } else if (this.moveDirection === "right") {
                    xLowestValue = minX - w.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
                    xHighestValue = maxX - w.layout.gridWidth / PAN_NUDGE_DIVISOR * xRatio;
                }
                if (!w.axisFlags.isRangeBar) {
                    var clampMin = (_a = w.globals.dataReducerRawMinX) != null ? _a : w.globals.initialMinX;
                    var clampMax = (_b = w.globals.dataReducerRawMaxX) != null ? _b : w.globals.initialMaxX;
                    if (xLowestValue < clampMin || xHighestValue > clampMax) {
                        xLowestValue = minX;
                        xHighestValue = maxX;
                    }
                }
                var xaxis = {
                    min: xLowestValue,
                    max: xHighestValue
                };
                var options2 = {
                    xaxis: xaxis
                };
                if (!w.config.chart.group) {
                    options2.yaxis = yaxis;
                }
                this.updateScrolledChart(options2, xLowestValue, xHighestValue);
            }
        },
        {
            /**
   * @param {object} options
   * @param {number} xLowestValue
   * @param {number} xHighestValue
   */ key: "updateScrolledChart",
            value: function updateScrolledChart(options2, xLowestValue, xHighestValue) {
                var w = this.w;
                this.ctx.updateHelpers._updateOptions(options2, false, false);
                if (typeof w.config.chart.events.scrolled === "function") {
                    var args = {
                        xaxis: {
                            min: xLowestValue,
                            max: xHighestValue
                        }
                    };
                    w.config.chart.events.scrolled(this.ctx, args);
                    this.ctx.events.fireEvent("scrolled", args);
                }
            }
        },
        {
            // ---------------------------------------------------------------------------
            // Momentum: multi-touch pinch-zoom, two-finger pan and kinetic inertia.
            //
            // Every _updateOptions destroys and recreates this instance, and applying a
            // gesture frame IS an _updateOptions, so the gesture must not depend on the
            // instance surviving. All runtime state lives on w.interact.momentum (the
            // interaction slice that persists across re-renders, like the crude pan's
            // lastClientPosition). The instance that received touchstart keeps driving
            // the gesture off the persistent state; inertia is a self-contained rAF loop
            // that stops on w.globals.isDestroyed (a real destroy) rather than being
            // cancelled by the per-update destroy().
            // ---------------------------------------------------------------------------
            key: "_momentumEnabled",
            value: function _momentumEnabled() {
                return this._pinchEnabled() || this._panInertiaEnabled();
            }
        },
        {
            key: "_pinchEnabled",
            value: function _pinchEnabled() {
                var c = this.w.config.chart;
                return !!(c.zoom && c.zoom.enabled && c.zoom.pinch);
            }
        },
        {
            key: "_panInertiaEnabled",
            value: function _panInertiaEnabled() {
                var c = this.w.config.chart;
                return !!(c.pan && c.pan.inertia);
            }
        },
        {
            /** Lazily-created, re-render-surviving gesture state on the interaction slice. */ key: "_m",
            value: function _m() {
                var it = this.w.interact;
                if (!it.momentum) {
                    it.momentum = {
                        busy: false,
                        /** @type {any} */ pinch: null,
                        /** @type {any} */ panState: null,
                        /** @type {{x:number,t:number}[]} */ samples: [],
                        /** @type {number|null} */ inertiaRAF: null
                    };
                }
                return it.momentum;
            }
        },
        {
            /** Current x data-window (rangeBars carry the datetime domain on y). */ key: "_currentXWindow",
            value: function _currentXWindow() {
                var w = this.w;
                return w.axisFlags.isRangeBar ? {
                    min: w.globals.minY,
                    max: w.globals.maxY
                } : {
                    min: w.globals.minX,
                    max: w.globals.maxX
                };
            }
        },
        {
            /** Live grid rect from the current DOM. Never cache the grid node on the
   * instance: a full render replaces this whole instance, but the fast update
   * path (fastUpdate/_fastAxisChromeRefresh) keeps the instance while swapping
   * the grid node, and a cached node would go stale (detached nodes report an
   * all-zero bounding rect, silently corrupting selection geometry). */ key: "_gridRect",
            value: function _gridRect() {
                var baseEl = this.w.dom.baseEl;
                var grid = baseEl && baseEl.querySelector(".apexcharts-grid");
                return grid ? grid.getBoundingClientRect() : null;
            }
        },
        {
            /**
   * Convert an absolute (client) x pixel to the plot-origin coordinate space
   * that bar placement and the selection rect transform both use:
   * `screenX - svgLeft - translateX`. This is the ONLY correct reference for the
   * numeric/datetime x mapping (see AxisMapping): do NOT measure from the
   * `.apexcharts-grid` box and subtract barPadForNumericAxis, because on a
   * numeric bar chart that box extends barPad to the LEFT of the plot origin, so
   * the two corrections are a fragile pair that only cancels while the grid box
   * happens to extend exactly barPad. Anchoring on translateX (the same origin
   * the bars use) is stable regardless of grid padding.
   * @param {number} screenX
   * @returns {number}
   */ key: "_screenXToPlotPx",
            value: function _screenXToPlotPx(screenX) {
                var baseEl = this.w.dom.baseEl;
                var svg = baseEl && baseEl.querySelector(".apexcharts-svg");
                var svgLeft = svg ? svg.getBoundingClientRect().left : 0;
                return screenX - svgLeft - this.w.layout.translateX;
            }
        },
        {
            /**
   * Raw data bounds to clamp against. When zoom-aware downsampling is active,
   * the raw stash tracks the full domain; fall back to the initial window.
   * Returns null for rangeBars (no raw-x clamp available).
   * @returns {{min:number, max:number}|null}
   */ key: "_clampBounds",
            value: function _clampBounds() {
                var _a, _b;
                var w = this.w;
                if (w.axisFlags.isRangeBar) return null;
                return {
                    min: (_a = w.globals.dataReducerRawMinX) != null ? _a : w.globals.initialMinX,
                    max: (_b = w.globals.dataReducerRawMaxX) != null ? _b : w.globals.initialMaxX
                };
            }
        },
        {
            /**
   * Apply an x-window immediately (no animation), mirroring panScrolled but
   * pixel-accurate: clamp to the raw bounds (preserving window width so a pan
   * stops flush at the edge rather than shrinking), floor for category axes,
   * then route through the fast _updateOptions path.
   * @param {number} newMinX @param {number} newMaxX @param {boolean} isZoom
   * @returns {{minX:number, maxX:number}|false} applied window, or false if rejected
   */ key: "_applyXRange",
            value: function _applyXRange(newMinX, newMaxX, isZoom) {
                var w = this.w;
                if (!w.globals.initialConfig) return false;
                var cur = this._currentXWindow();
                var zoomingOut = isZoom && newMaxX - newMinX > cur.max - cur.min;
                var bounds = this._clampBounds();
                if (bounds) {
                    var range = newMaxX - newMinX;
                    if (newMinX < bounds.min) {
                        newMinX = bounds.min;
                        newMaxX = newMinX + range;
                    }
                    if (newMaxX > bounds.max) {
                        newMaxX = bounds.max;
                        newMinX = newMaxX - range;
                    }
                    if (newMinX < bounds.min) newMinX = bounds.min;
                }
                if (w.config.xaxis.convertedCatToNumeric) {
                    newMinX = Math.floor(newMinX);
                    newMaxX = zoomingOut ? Math.ceil(newMaxX) : Math.floor(newMaxX);
                    if (newMinX < 1) newMinX = 1;
                    if (bounds && newMaxX > bounds.max) newMaxX = Math.floor(bounds.max);
                    if (newMaxX - newMinX < 2) return false;
                }
                if (!(newMaxX > newMinX)) return false;
                var options2 = {
                    xaxis: {
                        min: newMinX,
                        max: newMaxX
                    }
                };
                if (!w.config.chart.group) {
                    options2.yaxis = Utils$1.clone(w.globals.initialConfig.yaxis);
                }
                if (isZoom) w.interact.zoomed = true;
                this.ctx.updateHelpers._updateOptions(options2, false, false);
                return {
                    minX: newMinX,
                    maxX: newMaxX
                };
            }
        },
        {
            key: "_cancelInertia",
            value: function _cancelInertia() {
                var m = this._m();
                if (m.inertiaRAF != null) {
                    cancelAnimationFrame(m.inertiaRAF);
                    m.inertiaRAF = null;
                }
            }
        },
        {
            key: "_fireScrolled",
            value: function _fireScrolled() {
                var w = this.w;
                if (typeof w.config.chart.events.scrolled !== "function") return;
                var _this__currentXWindow = this._currentXWindow(), min = _this__currentXWindow.min, max = _this__currentXWindow.max;
                var args = {
                    xaxis: {
                        min: min,
                        max: max
                    }
                };
                w.config.chart.events.scrolled(this.ctx, args);
                this.ctx.events.fireEvent("scrolled", args);
            }
        },
        {
            /** @param {number} x @param {number} t */ key: "_pushSample",
            value: function _pushSample(x, t) {
                var s = this._m().samples;
                s.push({
                    x: x,
                    t: t
                });
                while(s.length > 6)s.shift();
            }
        },
        {
            /**
   * Single passive:false handler for all touch phases. Two fingers => pinch /
   * two-finger pan (zoom). One finger, in pan mode => kinetic pan with inertia.
   * @param {any} e
   */ key: "momentumTouch",
            value: function momentumTouch(e) {
                var w = this.w;
                var m = this._m();
                var type = e.type;
                if (type === "touchstart") {
                    this._cancelInertia();
                    var gridRectDim = this._gridRect();
                    if (!gridRectDim) return;
                    if (e.touches.length >= 2 && this._pinchEnabled()) {
                        e.preventDefault();
                        m.busy = true;
                        m.panState = null;
                        this._beginPinch(e, gridRectDim);
                    } else if (e.touches.length === 1 && this._panInertiaEnabled() && w.interact.panEnabled) {
                        m.busy = true;
                        m.pinch = null;
                        var t = e.touches[0];
                        var win = this._currentXWindow();
                        var gw = w.layout.gridWidth || 1;
                        m.panState = {
                            startX: t.clientX,
                            startY: t.clientY,
                            axis: null,
                            // decided on first move (rails)
                            minX0: win.min,
                            maxX0: win.max,
                            ratio0: (win.max - win.min) / gw
                        };
                        m.samples = [
                            {
                                x: t.clientX,
                                t: e.timeStamp
                            }
                        ];
                    }
                    return;
                }
                if (type === "touchmove") {
                    if (m.pinch && e.touches.length >= 2) {
                        e.preventDefault();
                        this._movePinch(e);
                    } else if (m.panState && e.touches.length === 1) {
                        this._movePan(e);
                    }
                    return;
                }
                if (m.pinch) {
                    if (e.touches.length < 2) this._endPinch();
                } else if (m.panState) {
                    if (e.touches.length === 0) this._endPan();
                }
                if (e.touches.length === 0) {
                    w.interact.mousedown = false;
                    this.dragged = false;
                    if (m.inertiaRAF == null && !m.pinch && !m.panState) {
                        m.busy = false;
                    }
                }
            }
        },
        {
            /** @param {any} e @param {DOMRect} gridRectDim */ key: "_beginPinch",
            value: function _beginPinch(e, gridRectDim) {
                var w = this.w;
                var t0 = e.touches[0];
                var t1 = e.touches[1];
                var dist = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY) || 1;
                var cx = (t0.clientX + t1.clientX) / 2 - gridRectDim.left - w.globals.barPadForNumericAxis;
                var _this__currentXWindow = this._currentXWindow(), min = _this__currentXWindow.min, max = _this__currentXWindow.max;
                this._m().pinch = {
                    d0: dist,
                    cx0: cx,
                    minX0: min,
                    maxX0: max,
                    gridWidth: w.layout.gridWidth || 1
                };
            }
        },
        {
            /** @param {any} e */ key: "_movePinch",
            value: function _movePinch(e) {
                var w = this.w;
                var p = this._m().pinch;
                if (!p) return;
                var gridRectDim = this._gridRect();
                if (!gridRectDim) return;
                var t0 = e.touches[0];
                var t1 = e.touches[1];
                var dist = Math.hypot(t1.clientX - t0.clientX, t1.clientY - t0.clientY) || 1;
                var cx = (t0.clientX + t1.clientX) / 2 - gridRectDim.left - w.globals.barPadForNumericAxis;
                var range0 = p.maxX0 - p.minX0;
                var newRange = range0 * (p.d0 / dist);
                var anchorData = p.minX0 + p.cx0 / p.gridWidth * range0;
                var newMinX = anchorData - cx / p.gridWidth * newRange;
                var newMaxX = newMinX + newRange;
                var bounds = this._clampBounds();
                if (bounds) {
                    var minXDiff = w.globals.minXDiff > 0 && isFinite(w.globals.minXDiff) ? w.globals.minXDiff : 0;
                    var minRange = Math.max(minXDiff * 2, (bounds.max - bounds.min) * 1e-6);
                    if (newMaxX - newMinX < minRange) {
                        var mid = (newMinX + newMaxX) / 2;
                        newMinX = mid - minRange / 2;
                        newMaxX = mid + minRange / 2;
                    }
                }
                this._applyXRange(newMinX, newMaxX, true);
            }
        },
        {
            key: "_endPinch",
            value: function _endPinch() {
                var w = this.w;
                var m = this._m();
                m.pinch = null;
                var _this__currentXWindow = this._currentXWindow(), min = _this__currentXWindow.min, max = _this__currentXWindow.max;
                var xaxis = {
                    min: min,
                    max: max
                };
                var yaxis = w.globals.initialConfig ? Utils$1.clone(w.globals.initialConfig.yaxis) : [];
                var toolbar = this.ctx.toolbar;
                if (toolbar) toolbar.zoomCallback(xaxis, yaxis);
            }
        },
        {
            /** @param {any} e */ key: "_movePan",
            value: function _movePan(e) {
                var m = this._m();
                var s = m.panState;
                var t = e.touches[0];
                if (!s.axis) {
                    var dx = Math.abs(t.clientX - s.startX);
                    var dy = Math.abs(t.clientY - s.startY);
                    if (dx < 6 && dy < 6) {
                        this._pushSample(t.clientX, e.timeStamp);
                        return;
                    }
                    if (dy > dx) {
                        m.busy = false;
                        m.panState = null;
                        return;
                    }
                    s.axis = "x";
                }
                if (s.axis !== "x") return;
                e.preventDefault();
                var totalDeltaPx = t.clientX - s.startX;
                var deltaData = totalDeltaPx * s.ratio0;
                this._pushSample(t.clientX, e.timeStamp);
                this._applyXRange(s.minX0 - deltaData, s.maxX0 - deltaData, false);
            }
        },
        {
            key: "_endPan",
            value: function _endPan() {
                var m = this._m();
                var s = m.panState;
                m.panState = null;
                var vel = 0;
                var samples = m.samples;
                if (samples.length >= 2) {
                    var a = samples[0];
                    var b = samples[samples.length - 1];
                    var dt = b.t - a.t;
                    if (dt > 0) vel = (b.x - a.x) / dt;
                }
                m.samples = [];
                if (s && s.axis === "x" && this._panInertiaEnabled() && Math.abs(vel) > INERTIA_MIN_RELEASE_VELOCITY) {
                    this._startInertia(vel);
                } else {
                    m.busy = false;
                    this._fireScrolled();
                }
            }
        },
        {
            /**
   * Kinetic glide after a one-finger pan release: decay the velocity by
   * `friction` each frame and shift the window, stopping at the data edge
   * (clamp, not elastic overshoot). The loop is w-driven, so it keeps running
   * across the re-renders each frame triggers and stops only on a real destroy.
   * @param {number} vel0 px/ms, sign is the finger direction
   */ key: "_startInertia",
            value: function _startInertia(vel0) {
                var _this = this;
                var w = this.w;
                var m = this._m();
                var cfgFriction = w.config.chart.pan && w.config.chart.pan.friction;
                var friction = typeof cfgFriction === "number" ? Math.min(Math.max(cfgFriction, 0.5), 0.999) : INERTIA_DEFAULT_FRICTION;
                var vel = vel0;
                var lastT = null;
                m.busy = true;
                var step = function step1(ts) {
                    if (w.globals.isDestroyed) {
                        m.inertiaRAF = null;
                        m.busy = false;
                        return;
                    }
                    if (lastT == null) {
                        lastT = ts;
                        m.inertiaRAF = requestAnimationFrame(step);
                        return;
                    }
                    var dt = ts - lastT;
                    lastT = ts;
                    vel *= Math.pow(friction, dt / FRAME_MS_60FPS);
                    if (Math.abs(vel) < INERTIA_STOP_VELOCITY) {
                        m.inertiaRAF = null;
                        m.busy = false;
                        _this._fireScrolled();
                        return;
                    }
                    var win = _this._currentXWindow();
                    var gw = w.layout.gridWidth || 1;
                    var ratio = (win.max - win.min) / gw;
                    var deltaData = vel * dt * ratio;
                    var applied = _this._applyXRange(win.min - deltaData, win.max - deltaData, false);
                    var bounds = _this._clampBounds();
                    var hitEdge = !applied || bounds && (deltaData > 0 && applied.minX <= bounds.min + (bounds.max - bounds.min) * 1e-6 || deltaData < 0 && applied.maxX >= bounds.max - (bounds.max - bounds.min) * 1e-6);
                    if (hitEdge) {
                        m.inertiaRAF = null;
                        m.busy = false;
                        _this._fireScrolled();
                        return;
                    }
                    m.inertiaRAF = requestAnimationFrame(step);
                };
                m.inertiaRAF = requestAnimationFrame(step);
            }
        }
    ]);
    return ZoomPanSelection;
}(Toolbar);
ApexCharts.registerFeatures({
    toolbar: Toolbar,
    zoomPanSelection: ZoomPanSelection
});
var Helpers$2 = /*#__PURE__*/ function() {
    "use strict";
    function Helpers3(annoCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Helpers3);
        this.w = annoCtx.w;
        this.annoCtx = annoCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Helpers3, [
        {
            /**
   * @param {Record<string, any>} anno
   * @param {number | null} [annoIndex]
   */ key: "setOrientations",
            value: function setOrientations(anno) {
                var annoIndex = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
                var _a, _b;
                var w = this.w;
                if (anno.label.orientation === "vertical") {
                    var i = annoIndex !== null ? annoIndex : 0;
                    var xAnno = w.dom.baseEl.querySelector(".apexcharts-xaxis-annotations .apexcharts-xaxis-annotation-label[rel='".concat(i, "']"));
                    if (xAnno !== null) {
                        var xAnnoCoord = /** @type {SVGGraphicsElement} */ xAnno.getBBox();
                        xAnno.setAttribute("x", String(parseFloat((_a = xAnno.getAttribute("x")) != null ? _a : "0") - xAnnoCoord.height + 4));
                        var yOffset = anno.label.position === "top" ? xAnnoCoord.width : -xAnnoCoord.width;
                        xAnno.setAttribute("y", String(parseFloat((_b = xAnno.getAttribute("y")) != null ? _b : "0") + yOffset));
                        var _this_annoCtx_graphics_rotateAroundCenter = this.annoCtx.graphics.rotateAroundCenter(xAnno), x = _this_annoCtx_graphics_rotateAroundCenter.x, y = _this_annoCtx_graphics_rotateAroundCenter.y;
                        xAnno.setAttribute("transform", "rotate(-90 ".concat(x, " ").concat(y, ")"));
                    }
                }
            }
        },
        {
            /**
   * @param {any} annoEl
   * @param {Record<string, any>} anno
   */ key: "addBackgroundToAnno",
            value: function addBackgroundToAnno(annoEl, anno) {
                var w = this.w;
                if (!annoEl || !anno.label.text || !String(anno.label.text).trim()) {
                    return null;
                }
                var gridEl = w.dom.baseEl.querySelector(".apexcharts-grid");
                if (!gridEl) return null;
                var elGridRect = gridEl.getBoundingClientRect();
                var gridBBox = /** @type {SVGGraphicsElement} */ gridEl.getBBox();
                var zoom = elGridRect.width / gridBBox.width || 1;
                var coords = annoEl.getBoundingClientRect();
                var _anno_label_style_padding = anno.label.style.padding, pleft = _anno_label_style_padding.left, pright = _anno_label_style_padding.right, ptop = _anno_label_style_padding.top, pbottom = _anno_label_style_padding.bottom;
                if (anno.label.orientation === "vertical") {
                    var ref;
                    ref = [
                        pleft,
                        pright,
                        ptop,
                        pbottom
                    ], ptop = ref[0], pbottom = ref[1], pleft = ref[2], pright = ref[3], ref;
                }
                var x1 = (coords.left - elGridRect.left) / zoom - pleft;
                var y1 = (coords.top - elGridRect.top) / zoom - ptop;
                var elRect = this.annoCtx.graphics.drawRect(x1 - w.globals.barPadForNumericAxis, y1, coords.width / zoom + pleft + pright, coords.height / zoom + ptop + pbottom, anno.label.borderRadius, anno.label.style.background, 1, anno.label.borderWidth, anno.label.borderColor, 0);
                if (anno.id) {
                    elRect.node.classList.add(anno.id);
                }
                return elRect;
            }
        },
        {
            key: "annotationsBackground",
            value: function annotationsBackground() {
                var _this = this;
                var w = this.w;
                var add = function add(anno, i, type) {
                    var annoLabel = w.dom.baseEl.querySelector(".apexcharts-".concat(type, "-annotations .apexcharts-").concat(type, "-annotation-label[rel='").concat(i, "']"));
                    if (annoLabel) {
                        var parent = annoLabel.parentNode;
                        var elRect = _this.addBackgroundToAnno(annoLabel, anno);
                        if (elRect) {
                            parent == null ? void 0 : parent.insertBefore(elRect.node, annoLabel);
                            var labelX = annoLabel.getAttribute("x");
                            if (labelX !== null) {
                                applyProgressiveReveal(elRect, parseFloat(labelX), w);
                            }
                            if (anno.label.mouseEnter) {
                                elRect.node.addEventListener("mouseenter", anno.label.mouseEnter.bind(_this, anno));
                            }
                            if (anno.label.mouseLeave) {
                                elRect.node.addEventListener("mouseleave", anno.label.mouseLeave.bind(_this, anno));
                            }
                            if (anno.label.click) {
                                elRect.node.addEventListener("click", anno.label.click.bind(_this, anno));
                            }
                        }
                    }
                };
                w.config.annotations.xaxis.forEach(function(anno, i) {
                    return add(anno, i, "xaxis");
                });
                w.config.annotations.yaxis.forEach(function(anno, i) {
                    return add(anno, i, "yaxis");
                });
                w.config.annotations.points.forEach(function(anno, i) {
                    return add(anno, i, "point");
                });
            }
        },
        {
            /**
   * @param {string} type
   * @param {Record<string, any>} anno
   */ key: "getY1Y2",
            value: function getY1Y2(type, anno) {
                var _a, _b;
                var w = this.w;
                var y = type === "y1" ? anno.y : anno.y2;
                var yP;
                var clipped = false;
                if (this.annoCtx.invertAxis) {
                    var labels = w.config.xaxis.convertedCatToNumeric ? w.labelData.categoryLabels : w.labelData.labels;
                    var catIndex = labels.indexOf(y);
                    var xLabel = w.dom.baseEl.querySelector(".apexcharts-yaxis-texts-g text:nth-child(".concat(catIndex + 1, ")"));
                    yP = xLabel ? parseFloat((_a = xLabel.getAttribute("y")) != null ? _a : "0") : (w.layout.gridHeight / labels.length - 1) * (catIndex + 1) - w.globals.barHeight;
                    if (anno.seriesIndex !== void 0 && w.globals.barHeight) {
                        yP -= w.globals.barHeight / 2 * (w.seriesData.series.length - 1) - w.globals.barHeight * anno.seriesIndex;
                    }
                } else {
                    var yAxisMap = w.globals.seriesYAxisMap[anno.yAxisIndex];
                    if (!yAxisMap || yAxisMap[0] == null || !w.config.yaxis[anno.yAxisIndex]) {
                        return {
                            yP: 0,
                            clipped: true
                        };
                    }
                    var seriesIndex = yAxisMap[0];
                    var yPos = w.config.yaxis[anno.yAxisIndex].logarithmic ? new CoreUtils(this.w).getLogVal(w.config.yaxis[anno.yAxisIndex].logBase, y, seriesIndex) / /** @type {any} */ w.globals.yLogRatio[seriesIndex] : (y - w.globals.minYArr[seriesIndex]) / (w.globals.yRange[seriesIndex] / w.layout.gridHeight);
                    yP = w.layout.gridHeight - Math.min(Math.max(yPos, 0), w.layout.gridHeight);
                    clipped = yPos > w.layout.gridHeight || yPos < 0;
                    if (anno.marker && (anno.y === void 0 || anno.y === null)) {
                        yP = 0;
                    }
                    if ((_b = w.config.yaxis[anno.yAxisIndex]) == null ? void 0 : _b.reversed) {
                        yP = yPos;
                    }
                }
                if (typeof y === "string" && y.includes("px")) {
                    yP = parseFloat(y);
                }
                return {
                    yP: yP,
                    clipped: clipped
                };
            }
        },
        {
            /**
   * @param {string} type
   * @param {Record<string, any>} anno
   */ key: "getX1X2",
            value: function getX1X2(type, anno) {
                var w = this.w;
                var x = type === "x1" ? anno.x : anno.x2;
                var min = this.annoCtx.invertAxis ? w.globals.minY : w.globals.minX;
                var max = this.annoCtx.invertAxis ? w.globals.maxY : w.globals.maxX;
                var range = this.annoCtx.invertAxis ? w.globals.yRange[0] : w.globals.xRange;
                var clipped = false;
                var xP = this.annoCtx.inversedReversedAxis ? (max - x) / (range / w.layout.gridWidth) : (x - min) / (range / w.layout.gridWidth);
                if ((w.config.xaxis.type === "category" || w.config.xaxis.convertedCatToNumeric) && !this.annoCtx.invertAxis && !w.axisFlags.dataFormatXNumeric) {
                    if (!w.config.chart.sparkline.enabled) {
                        xP = this.getStringX(x);
                    }
                }
                if (typeof x === "string" && x.includes("px")) {
                    xP = parseFloat(x);
                }
                if ((x === void 0 || x === null) && anno.marker) {
                    xP = w.layout.gridWidth;
                }
                if (anno.seriesIndex !== void 0 && w.globals.barWidth && !this.annoCtx.invertAxis) {
                    xP -= w.globals.barWidth / 2 * (w.seriesData.series.length - 1) - w.globals.barWidth * anno.seriesIndex;
                }
                if (typeof xP !== "number") {
                    xP = 0;
                    clipped = true;
                }
                if (parseFloat(xP.toFixed(10)) > parseFloat(w.layout.gridWidth.toFixed(10))) {
                    xP = w.layout.gridWidth;
                    clipped = true;
                } else if (xP < 0) {
                    xP = 0;
                    clipped = true;
                }
                return {
                    x: xP,
                    clipped: clipped
                };
            }
        },
        {
            /**
   * @param {number} x
   */ key: "getStringX",
            value: function getStringX(x) {
                var _a;
                var w = this.w;
                var rX = x;
                if (w.config.xaxis.convertedCatToNumeric && w.labelData.categoryLabels.length) {
                    var strX = String(x);
                    x = w.labelData.categoryLabels.findIndex(function(l) {
                        return String(l) === strX;
                    }) + 1;
                }
                var catIndex = w.labelData.labels.map(function(item) {
                    return Array.isArray(item) ? item.join(" ") : item;
                }).indexOf(x);
                var xLabel = w.dom.baseEl.querySelector(".apexcharts-xaxis-texts-g text:nth-child(".concat(catIndex + 1, ")"));
                if (xLabel) {
                    rX = parseFloat((_a = xLabel.getAttribute("x")) != null ? _a : "0");
                }
                return rX;
            }
        }
    ]);
    return Helpers3;
}();
var XAnnotations = /*#__PURE__*/ function() {
    "use strict";
    function XAnnotations(annoCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, XAnnotations);
        this.w = annoCtx.w;
        this.annoCtx = annoCtx;
        this.invertAxis = this.annoCtx.invertAxis;
        this.helpers = new Helpers$2(this.annoCtx);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(XAnnotations, [
        {
            /**
   * @param {XAxisAnnotations} anno
   * @param {Element} parent
   * @param {number} index
   */ key: "addXaxisAnnotation",
            value: function addXaxisAnnotation(anno, parent, index) {
                var w = this.w;
                var result = this.helpers.getX1X2("x1", anno);
                var x1 = result.x;
                var clipX1 = result.clipped;
                var clipX2 = true;
                var x2;
                var text = anno.label.text;
                var strokeDashArray = anno.strokeDashArray;
                if (!Utils$1.isNumber(x1)) return;
                if (anno.x2 === null || typeof anno.x2 === "undefined") {
                    if (!clipX1) {
                        var line = this.annoCtx.graphics.drawLine(x1 + anno.offsetX, // x1
                        0 + anno.offsetY, // y1
                        x1 + anno.offsetX, // x2
                        w.layout.gridHeight + anno.offsetY, // y2
                        anno.borderColor, // lineColor
                        strokeDashArray, //dashArray
                        anno.borderWidth);
                        parent.appendChild(line.node);
                        if (anno.id) {
                            line.node.classList.add(anno.id);
                        }
                        applyProgressiveReveal(line, x1 + anno.offsetX, w);
                    }
                } else {
                    var result2 = this.helpers.getX1X2("x2", anno);
                    x2 = result2.x;
                    clipX2 = result2.clipped;
                    if (x2 < x1) {
                        var temp = x1;
                        x1 = x2;
                        x2 = temp;
                    }
                    var rect = this.annoCtx.graphics.drawRect(x1 + anno.offsetX, // x1
                    0 + anno.offsetY, // y1
                    x2 - x1, // x2
                    w.layout.gridHeight + anno.offsetY, // y2
                    0, // radius
                    anno.fillColor, // color
                    anno.opacity, // opacity,
                    1, // strokeWidth
                    anno.borderColor, // strokeColor
                    strokeDashArray);
                    rect.node.classList.add("apexcharts-annotation-rect");
                    rect.attr("clip-path", "url(#gridRectMask".concat(w.globals.cuid, ")"));
                    parent.appendChild(rect.node);
                    if (anno.id) {
                        rect.node.classList.add(anno.id);
                    }
                    applyProgressiveReveal(rect, x1 + anno.offsetX, w);
                }
                if (!(clipX1 && clipX2)) {
                    var textRects = this.annoCtx.graphics.getTextRects(text, anno.label.style.fontSize);
                    var textY = anno.label.position === "top" ? 4 : anno.label.position === "center" ? w.layout.gridHeight / 2 + (anno.label.orientation === "vertical" ? textRects.width / 2 : 0) : w.layout.gridHeight;
                    var elText = this.annoCtx.graphics.drawText({
                        x: x1 + anno.label.offsetX,
                        y: textY + anno.label.offsetY - (anno.label.orientation === "vertical" ? anno.label.position === "top" ? textRects.width / 2 - 12 : -textRects.width / 2 : 0),
                        text: text,
                        textAnchor: anno.label.textAnchor,
                        fontSize: anno.label.style.fontSize,
                        fontFamily: anno.label.style.fontFamily,
                        fontWeight: anno.label.style.fontWeight,
                        foreColor: anno.label.style.color,
                        cssClass: "apexcharts-xaxis-annotation-label ".concat(anno.label.style.cssClass, " ").concat(anno.id ? anno.id : "")
                    });
                    elText.attr({
                        rel: index
                    });
                    parent.appendChild(elText.node);
                    applyProgressiveReveal(elText, x1 + anno.label.offsetX, w);
                    this.annoCtx.helpers.setOrientations(anno, index);
                }
            }
        },
        {
            key: "drawXAxisAnnotations",
            value: function drawXAxisAnnotations() {
                var _this = this;
                var w = this.w;
                var elg = this.annoCtx.graphics.group({
                    class: "apexcharts-xaxis-annotations"
                });
                w.config.annotations.xaxis.map(function(anno, index) {
                    _this.addXaxisAnnotation(anno, elg.node, index);
                });
                return elg;
            }
        }
    ]);
    return XAnnotations;
}();
var YAnnotations = /*#__PURE__*/ function() {
    "use strict";
    function YAnnotations(annoCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, YAnnotations);
        this.w = annoCtx.w;
        this.annoCtx = annoCtx;
        this.helpers = new Helpers$2(this.annoCtx);
        this.axesUtils = new AxesUtils(this.annoCtx.w, {
            theme: this.annoCtx.theme,
            timeScale: this.annoCtx.timeScale
        });
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(YAnnotations, [
        {
            /**
   * @param {YAxisAnnotations} anno
   * @param {Element} parent
   * @param {number} index
   */ key: "addYaxisAnnotation",
            value: function addYaxisAnnotation(anno, parent, index) {
                var w = this.w;
                var strokeDashArray = anno.strokeDashArray;
                var result = this.helpers.getY1Y2("y1", anno);
                var y1 = result.yP;
                var clipY1 = result.clipped;
                var y2;
                var clipY2 = true;
                var drawn = false;
                var text = anno.label.text;
                if (anno.y2 === null || typeof anno.y2 === "undefined") {
                    if (!clipY1) {
                        drawn = true;
                        var line = this.annoCtx.graphics.drawLine(0 + anno.offsetX, // x1
                        y1 + anno.offsetY, // y1
                        this._getYAxisAnnotationWidth(anno), // x2
                        y1 + anno.offsetY, // y2
                        anno.borderColor, // lineColor
                        strokeDashArray, // dashArray
                        anno.borderWidth);
                        parent.appendChild(line.node);
                        if (anno.id) {
                            line.node.classList.add(anno.id);
                        }
                    }
                } else {
                    result = this.helpers.getY1Y2("y2", anno);
                    y2 = result.yP;
                    clipY2 = result.clipped;
                    if (y2 > y1) {
                        var temp = y1;
                        y1 = y2;
                        y2 = temp;
                    }
                    if (!(clipY1 && clipY2)) {
                        drawn = true;
                        var rect = this.annoCtx.graphics.drawRect(0 + anno.offsetX, // x1
                        y2 + anno.offsetY, // y1
                        this._getYAxisAnnotationWidth(anno), // x2
                        y1 - y2, // y2
                        0, // radius
                        anno.fillColor, // color
                        anno.opacity, // opacity,
                        1, // strokeWidth
                        anno.borderColor, // strokeColor
                        strokeDashArray);
                        rect.node.classList.add("apexcharts-annotation-rect");
                        rect.attr("clip-path", "url(#gridRectMask".concat(w.globals.cuid, ")"));
                        parent.appendChild(rect.node);
                        if (anno.id) {
                            rect.node.classList.add(anno.id);
                        }
                    }
                }
                if (drawn) {
                    var textX = anno.label.position === "right" ? w.layout.gridWidth : anno.label.position === "center" ? w.layout.gridWidth / 2 : 0;
                    var elText = this.annoCtx.graphics.drawText({
                        x: textX + anno.label.offsetX,
                        y: (y2 != null ? y2 : y1) + anno.label.offsetY - 3,
                        text: text,
                        textAnchor: anno.label.textAnchor,
                        fontSize: anno.label.style.fontSize,
                        fontFamily: anno.label.style.fontFamily,
                        fontWeight: anno.label.style.fontWeight,
                        foreColor: anno.label.style.color,
                        cssClass: "apexcharts-yaxis-annotation-label ".concat(anno.label.style.cssClass, " ").concat(anno.id ? anno.id : "")
                    });
                    elText.attr({
                        rel: index
                    });
                    parent.appendChild(elText.node);
                }
            }
        },
        {
            /**
   * @param {YAxisAnnotations} anno
   */ key: "_getYAxisAnnotationWidth",
            value: function _getYAxisAnnotationWidth(anno) {
                var w = this.w;
                var width = w.layout.gridWidth;
                if (anno.width.indexOf("%") > -1) {
                    width = w.layout.gridWidth * parseInt(anno.width, 10) / 100;
                } else {
                    width = parseInt(anno.width, 10);
                }
                return width + anno.offsetX;
            }
        },
        {
            key: "drawYAxisAnnotations",
            value: function drawYAxisAnnotations() {
                var _this = this;
                var w = this.w;
                var elg = this.annoCtx.graphics.group({
                    class: "apexcharts-yaxis-annotations"
                });
                w.config.annotations.yaxis.forEach(function(anno, index) {
                    anno.yAxisIndex = _this.axesUtils.translateYAxisIndex(anno.yAxisIndex);
                    if (!(_this.axesUtils.isYAxisHidden(anno.yAxisIndex) && _this.axesUtils.yAxisAllSeriesCollapsed(anno.yAxisIndex))) {
                        _this.addYaxisAnnotation(anno, elg.node, index);
                    }
                });
                return elg;
            }
        }
    ]);
    return YAnnotations;
}();
var PointAnnotations = /*#__PURE__*/ function() {
    "use strict";
    function PointAnnotations(annoCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, PointAnnotations);
        this.w = annoCtx.w;
        this.annoCtx = annoCtx;
        this.helpers = new Helpers$2(this.annoCtx);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(PointAnnotations, [
        {
            /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */ key: "addPointAnnotation",
            value: function addPointAnnotation(anno, parent, index) {
                var _this = this;
                var w = this.w;
                if (w.globals.collapsedSeriesIndices.indexOf(anno.seriesIndex) > -1) {
                    return;
                }
                var resultX = this.helpers.getX1X2("x1", anno);
                var x = resultX.x;
                var clipX = resultX.clipped;
                var resultY = this.helpers.getY1Y2("y1", anno);
                var y = resultY.yP;
                var clipY = resultY.clipped;
                if (!Utils$1.isNumber(x)) return;
                if (!(clipY || clipX)) {
                    var optsPoints = {
                        pSize: anno.marker.size,
                        pointStrokeWidth: anno.marker.strokeWidth,
                        pointFillColor: anno.marker.fillColor,
                        pointStrokeColor: anno.marker.strokeColor,
                        shape: anno.marker.shape,
                        pRadius: anno.marker.radius,
                        class: "apexcharts-point-annotation-marker ".concat(anno.marker.cssClass, " ").concat(anno.id ? anno.id : "")
                    };
                    var point = this.annoCtx.graphics.drawMarker(x + anno.marker.offsetX, y + anno.marker.offsetY, optsPoints);
                    parent.appendChild(point.node);
                    var tooltipTargets = [
                        point.node
                    ];
                    applyProgressiveReveal(point, x, w);
                    var text = anno.label.text ? anno.label.text : "";
                    var elText = this.annoCtx.graphics.drawText({
                        x: x + anno.label.offsetX,
                        y: y + anno.label.offsetY - anno.marker.size - parseFloat(anno.label.style.fontSize) / 1.6,
                        text: text,
                        textAnchor: anno.label.textAnchor,
                        fontSize: anno.label.style.fontSize,
                        fontFamily: anno.label.style.fontFamily,
                        fontWeight: anno.label.style.fontWeight,
                        foreColor: anno.label.style.color,
                        cssClass: "apexcharts-point-annotation-label ".concat(anno.label.style.cssClass, " ").concat(anno.id ? anno.id : "")
                    });
                    elText.attr({
                        rel: index
                    });
                    parent.appendChild(elText.node);
                    applyProgressiveReveal(elText, x, w);
                    if (anno.customSVG.SVG) {
                        var g = this.annoCtx.graphics.group({
                            class: "apexcharts-point-annotations-custom-svg " + anno.customSVG.cssClass
                        });
                        g.attr({
                            transform: "translate(".concat(x + anno.customSVG.offsetX, ", ").concat(y + anno.customSVG.offsetY, ")")
                        });
                        g.node.innerHTML = anno.customSVG.SVG;
                        parent.appendChild(g.node);
                        tooltipTargets.push(g.node);
                    }
                    if (anno.image.path) {
                        var imgWidth = anno.image.width ? anno.image.width : 20;
                        var imgHeight = anno.image.height ? anno.image.height : 20;
                        point = this.annoCtx.addImage({
                            x: x + anno.image.offsetX - imgWidth / 2,
                            y: y + anno.image.offsetY - imgHeight / 2,
                            width: imgWidth,
                            height: imgHeight,
                            path: anno.image.path,
                            appendTo: ".apexcharts-point-annotations"
                        });
                        tooltipTargets.push(point.node);
                    }
                    if (anno.tooltip && anno.tooltip.enabled) {
                        tooltipTargets.forEach(function(node) {
                            node.addEventListener("mouseenter", function() {
                                _this.showPointTooltip(anno, node);
                            });
                            node.addEventListener("mouseleave", function() {
                                _this.hidePointTooltip();
                            });
                        });
                    }
                    if (anno.mouseEnter) {
                        point.node.addEventListener("mouseenter", anno.mouseEnter.bind(this, anno));
                    }
                    if (anno.mouseLeave) {
                        point.node.addEventListener("mouseleave", anno.mouseLeave.bind(this, anno));
                    }
                    if (anno.click) {
                        point.node.addEventListener("click", anno.click.bind(this, anno));
                    }
                }
            }
        },
        {
            /**
   * Lazily create (once per chart) and return the shared HTML element used to
   * render point-annotation tooltips. Reuses the `.apexcharts-tooltip` glass
   * styling; the `.apexcharts-annotation-tooltip` modifier adds padding and
   * text wrapping for free-form content.
   * @returns {HTMLElement}
   */ key: "getPointTooltipEl",
            value: function getPointTooltipEl() {
                var w = this.w;
                var el = /** @type {HTMLElement | null} */ w.dom.elWrap.querySelector(".apexcharts-annotation-tooltip");
                if (!el) {
                    el = /** @type {HTMLElement} */ BrowserAPIs.createElementNS("http://www.w3.org/1999/xhtml", "div");
                    el.classList.add("apexcharts-tooltip", "apexcharts-annotation-tooltip");
                    w.dom.elWrap.appendChild(el);
                }
                return el;
            }
        },
        {
            /**
   * Resolve the tooltip markup for a point annotation. Precedence:
   * `tooltip.formatter` (fn) -> `tooltip.text` -> `label.text`. Arrays are
   * joined with line breaks.
   * @param {Record<string, any>} anno
   * @returns {string}
   */ key: "getPointTooltipContent",
            value: function getPointTooltipContent(anno) {
                var w = this.w;
                var tt = anno.tooltip || {};
                if (typeof tt.formatter === "function") {
                    return tt.formatter({
                        annotation: anno,
                        seriesIndex: anno.seriesIndex,
                        id: anno.id,
                        w: w
                    });
                }
                var content = tt.text != null ? tt.text : anno.label && anno.label.text;
                if (Array.isArray(content)) {
                    content = content.join("<br/>");
                }
                return content == null ? "" : String(content);
            }
        },
        {
            /**
   * @param {Record<string, any>} anno
   * @param {Element} targetNode the hovered marker / image / custom-SVG node
   */ key: "showPointTooltip",
            value: function showPointTooltip(anno, targetNode) {
                var w = this.w;
                var content = this.getPointTooltipContent(anno);
                if (!content) return;
                var el = this.getPointTooltipEl();
                el.innerHTML = content;
                var theme = anno.tooltip.theme || w.config.tooltip.theme || "light";
                el.classList.remove("apexcharts-theme-light", "apexcharts-theme-dark");
                el.classList.add("apexcharts-theme-".concat(theme));
                el.classList.add("apexcharts-active");
                var wrapRect = w.dom.elWrap.getBoundingClientRect();
                var markRect = targetNode.getBoundingClientRect();
                var ttRect = el.getBoundingClientRect();
                var offsetX = anno.tooltip.offsetX || 0;
                var offsetY = anno.tooltip.offsetY || 0;
                var left = markRect.left - wrapRect.left + markRect.width / 2 - ttRect.width / 2;
                var top = markRect.top - wrapRect.top - ttRect.height - 10;
                left = Math.max(0, Math.min(left, wrapRect.width - ttRect.width));
                if (top < 0) {
                    top = markRect.top - wrapRect.top + markRect.height + 10;
                }
                el.style.left = left + offsetX + "px";
                el.style.top = top + offsetY + "px";
            }
        },
        {
            key: "hidePointTooltip",
            value: function hidePointTooltip() {
                var el = /** @type {HTMLElement | null} */ this.w.dom.elWrap.querySelector(".apexcharts-annotation-tooltip");
                if (el) {
                    el.classList.remove("apexcharts-active");
                }
            }
        },
        {
            key: "drawPointAnnotations",
            value: function drawPointAnnotations() {
                var _this = this;
                var w = this.w;
                var elg = this.annoCtx.graphics.group({
                    class: "apexcharts-point-annotations"
                });
                w.config.annotations.points.map(function(anno, index) {
                    _this.addPointAnnotation(anno, elg.node, index);
                });
                return elg;
            }
        }
    ]);
    return PointAnnotations;
}();
var Annotations = /*#__PURE__*/ function() {
    "use strict";
    function Annotations(w) {
        var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref_theme = _ref.theme, theme = _ref_theme === void 0 ? null : _ref_theme, _ref_timeScale = _ref.timeScale, timeScale = _ref_timeScale === void 0 ? null : _ref_timeScale;
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Annotations);
        this.w = w;
        this.theme = theme;
        this.timeScale = timeScale;
        this.invertAxis = void 0;
        this.inversedReversedAxis = void 0;
        this.graphics = new Graphics(this.w);
        if (this.w.globals.isBarHorizontal) {
            this.invertAxis = true;
        }
        this.helpers = new Helpers$2(this);
        this.xAxisAnnotations = new XAnnotations(this);
        this.yAxisAnnotations = new YAnnotations(this);
        this.pointsAnnotations = new PointAnnotations(this);
        if (this.w.globals.isBarHorizontal && this.w.config.yaxis[0].reversed) {
            this.inversedReversedAxis = true;
        }
        this.xDivision = this.w.layout.gridWidth / this.w.globals.dataPoints;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Annotations, [
        {
            key: "drawAxesAnnotations",
            value: function drawAxesAnnotations() {
                var w = this.w;
                if (w.globals.axisCharts && w.globals.dataPoints) {
                    var yAnnotations = this.yAxisAnnotations.drawYAxisAnnotations();
                    var xAnnotations = this.xAxisAnnotations.drawXAxisAnnotations();
                    var pointAnnotations = this.pointsAnnotations.drawPointAnnotations();
                    var initialAnim = w.config.chart.animations.enabled;
                    var annoArray = [
                        yAnnotations,
                        xAnnotations,
                        pointAnnotations
                    ];
                    var annoElArray = [
                        xAnnotations.node,
                        yAnnotations.node,
                        pointAnnotations.node
                    ];
                    var progressiveAnnos = w.config.chart.type === "line" || w.config.chart.type === "area" || w.config.chart.type === "rangeArea";
                    var skipGroupHide = [
                        progressiveAnnos,
                        false,
                        progressiveAnnos
                    ];
                    for(var i = 0; i < 3; i++){
                        w.dom.elGraphical.add(annoArray[i]);
                        if (initialAnim && !w.globals.resized && !w.globals.dataChanged) {
                            if (w.config.chart.type !== "scatter" && w.config.chart.type !== "bubble" && w.globals.dataPoints > 1 && !skipGroupHide[i]) {
                                annoElArray[i].classList.add("apexcharts-element-hidden");
                            }
                        }
                        w.globals.delayedElements.push({
                            el: annoElArray[i],
                            index: 0
                        });
                    }
                    this.helpers.annotationsBackground();
                }
            }
        },
        {
            key: "drawImageAnnos",
            value: function drawImageAnnos() {
                var _this = this;
                var w = this.w;
                w.config.annotations.images.map(function(s) {
                    _this.addImage(s);
                });
            }
        },
        {
            key: "drawTextAnnos",
            value: function drawTextAnnos() {
                var _this = this;
                var w = this.w;
                w.config.annotations.texts.map(function(t) {
                    _this.addText(t);
                });
            }
        },
        {
            /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */ key: "addXaxisAnnotation",
            value: function addXaxisAnnotation(anno, parent, index) {
                this.xAxisAnnotations.addXaxisAnnotation(anno, parent, index);
            }
        },
        {
            /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */ key: "addYaxisAnnotation",
            value: function addYaxisAnnotation(anno, parent, index) {
                this.yAxisAnnotations.addYaxisAnnotation(anno, parent, index);
            }
        },
        {
            /**
   * @param {Record<string, any>} anno
   * @param {Element} parent
   * @param {number} index
   */ key: "addPointAnnotation",
            value: function addPointAnnotation(anno, parent, index) {
                this.pointsAnnotations.addPointAnnotation(anno, parent, index);
            }
        },
        {
            /**
   * @param {Record<string, any>} params
   */ key: "addText",
            value: function addText(params) {
                var x = params.x, y = params.y, text = params.text, textAnchor = params.textAnchor, foreColor = params.foreColor, fontSize = params.fontSize, fontFamily = params.fontFamily, fontWeight = params.fontWeight, cssClass = params.cssClass, backgroundColor = params.backgroundColor, borderWidth = params.borderWidth, strokeDashArray = params.strokeDashArray, borderRadius = params.borderRadius, borderColor = params.borderColor, _params_appendTo = params.appendTo, appendTo = _params_appendTo === void 0 ? ".apexcharts-svg" : _params_appendTo, _params_paddingLeft = params.paddingLeft, paddingLeft = _params_paddingLeft === void 0 ? 4 : _params_paddingLeft, _params_paddingRight = params.paddingRight, paddingRight = _params_paddingRight === void 0 ? 4 : _params_paddingRight, _params_paddingBottom = params.paddingBottom, paddingBottom = _params_paddingBottom === void 0 ? 2 : _params_paddingBottom, _params_paddingTop = params.paddingTop, paddingTop = _params_paddingTop === void 0 ? 2 : _params_paddingTop;
                var w = this.w;
                var elText = this.graphics.drawText({
                    x: x,
                    y: y,
                    text: text,
                    textAnchor: textAnchor || "start",
                    fontSize: fontSize || "12px",
                    fontWeight: fontWeight || "regular",
                    fontFamily: fontFamily || w.config.chart.fontFamily,
                    foreColor: foreColor || w.config.chart.foreColor,
                    cssClass: ("TURBOPACK compile-time truthy", 1) ? cssClass : "TURBOPACK unreachable"
                });
                var parent = w.dom.baseEl.querySelector(appendTo);
                if (parent) {
                    parent.appendChild(elText.node);
                }
                var textRect = elText.bbox();
                if (text) {
                    var elRect = this.graphics.drawRect(textRect.x - paddingLeft, textRect.y - paddingTop, textRect.width + paddingLeft + paddingRight, textRect.height + paddingBottom + paddingTop, borderRadius, backgroundColor ? backgroundColor : "transparent", 1, borderWidth, borderColor, strokeDashArray);
                    parent.insertBefore(elRect.node, elText.node);
                }
            }
        },
        {
            /**
   * @param {Record<string, any>} params
   */ key: "addImage",
            value: function addImage(params) {
                var w = this.w;
                var path = params.path, _params_x = params.x, x = _params_x === void 0 ? 0 : _params_x, _params_y = params.y, y = _params_y === void 0 ? 0 : _params_y, _params_width = params.width, width = _params_width === void 0 ? 20 : _params_width, _params_height = params.height, height = _params_height === void 0 ? 20 : _params_height, _params_appendTo = params.appendTo, appendTo = _params_appendTo === void 0 ? ".apexcharts-svg" : _params_appendTo;
                var img = w.dom.Paper.image(path);
                img.size(width, height).move(x, y);
                var parent = w.dom.baseEl.querySelector(appendTo);
                if (parent) {
                    parent.appendChild(img.node);
                }
                return img;
            }
        },
        {
            // The addXaxisAnnotation method requires a parent class, and user calling this method externally on the chart instance may not specify parent, hence a different method
            /**
   * @param {Record<string, any>} params
   * @param {boolean} pushToMemory
   * @param {any} context
   */ key: "addXaxisAnnotationExternal",
            value: function addXaxisAnnotationExternal(params, pushToMemory, context) {
                this.addAnnotationExternal({
                    params: params,
                    pushToMemory: pushToMemory,
                    context: context,
                    type: "xaxis",
                    contextMethod: context.addXaxisAnnotation
                });
                return context;
            }
        },
        {
            /**
   * @param {Record<string, any>} params
   * @param {boolean} pushToMemory
   * @param {any} context
   */ key: "addYaxisAnnotationExternal",
            value: function addYaxisAnnotationExternal(params, pushToMemory, context) {
                this.addAnnotationExternal({
                    params: params,
                    pushToMemory: pushToMemory,
                    context: context,
                    type: "yaxis",
                    contextMethod: context.addYaxisAnnotation
                });
                return context;
            }
        },
        {
            /**
   * @param {Record<string, any>} params
   * @param {boolean} pushToMemory
   * @param {any} context
   */ key: "addPointAnnotationExternal",
            value: function addPointAnnotationExternal(params, pushToMemory, context) {
                if (typeof this.invertAxis === "undefined") {
                    this.invertAxis = context.w.globals.isBarHorizontal;
                }
                this.addAnnotationExternal({
                    params: params,
                    pushToMemory: pushToMemory,
                    context: context,
                    type: "point",
                    contextMethod: context.addPointAnnotation
                });
                return context;
            }
        },
        {
            /** @param {{params: any, pushToMemory: any, context: any, type: any, contextMethod: any}} opts */ key: "addAnnotationExternal",
            value: function addAnnotationExternal(param) {
                var params = param.params, pushToMemory = param.pushToMemory, context = param.context, type = param.type, contextMethod = param.contextMethod;
                var me = context;
                var w = me.w;
                var parent = w.dom.baseEl.querySelector(".apexcharts-".concat(type, "-annotations"));
                var index = parent.childNodes.length + 1;
                var options2 = new Options();
                var axesAnno = Object.assign({}, type === "xaxis" ? options2.xAxisAnnotation : type === "yaxis" ? options2.yAxisAnnotation : options2.pointAnnotation);
                var anno = Utils$1.extend(axesAnno, params);
                switch(type){
                    case "xaxis":
                        this.addXaxisAnnotation(anno, parent, index);
                        break;
                    case "yaxis":
                        this.addYaxisAnnotation(anno, parent, index);
                        break;
                    case "point":
                        this.addPointAnnotation(anno, parent, index);
                        break;
                }
                var axesAnnoLabel = w.dom.baseEl.querySelector(".apexcharts-".concat(type, "-annotations .apexcharts-").concat(type, "-annotation-label[rel='").concat(index, "']"));
                var elRect = this.helpers.addBackgroundToAnno(axesAnnoLabel, anno);
                if (elRect) {
                    parent.insertBefore(elRect.node, axesAnnoLabel);
                }
                if (pushToMemory) {
                    w.globals.memory.methodsToExec.push({
                        context: me,
                        id: anno.id ? anno.id : Utils$1.randomId(),
                        method: contextMethod,
                        label: "addAnnotation",
                        params: params
                    });
                }
                return context;
            }
        },
        {
            /**
   * Remove the shared point-annotation hover tooltip node.
   *
   * `hidePointTooltip` is wired only to the marker's `mouseleave`; if the marker
   * is torn down while hovered (clearAnnotations / removeAnnotation / a redraw),
   * that never fires and the tooltip is left `.apexcharts-active`. A stale active
   * annotation tooltip then permanently suppresses the series tooltip (see the
   * guard in Tooltip.drawTooltip added by b1369f5ab). Removing the node clears
   * both the ghost box and the stale state; it is recreated on the next hover.
   * @param {any} w
   */ key: "_removeAnnotationTooltip",
            value: function _removeAnnotationTooltip(w) {
                var el = w.dom.elWrap && w.dom.elWrap.querySelector(".apexcharts-annotation-tooltip");
                if (el && el.parentNode) {
                    el.parentNode.removeChild(el);
                }
            }
        },
        {
            /**
   * @param {import('../../types/internal').ChartContext} ctx
   */ key: "clearAnnotations",
            value: function clearAnnotations(ctx) {
                var w = ctx.w;
                this._removeAnnotationTooltip(w);
                var annos = w.dom.baseEl.querySelectorAll(".apexcharts-yaxis-annotations, .apexcharts-xaxis-annotations, .apexcharts-point-annotations");
                for(var i = w.globals.memory.methodsToExec.length - 1; i >= 0; i--){
                    if (w.globals.memory.methodsToExec[i].label === "addText" || w.globals.memory.methodsToExec[i].label === "addAnnotation") {
                        w.globals.memory.methodsToExec.splice(i, 1);
                    }
                }
                Array.prototype.forEach.call(annos, function(a) {
                    while(a.firstChild){
                        a.removeChild(a.firstChild);
                    }
                });
            }
        },
        {
            /**
   * @param {import('../../types/internal').ChartContext} ctx
   * @param {string} id
   */ key: "removeAnnotation",
            value: function removeAnnotation(ctx, id) {
                var w = ctx.w;
                this._removeAnnotationTooltip(w);
                var annos = w.dom.baseEl.querySelectorAll(".".concat(id));
                if (annos) {
                    w.globals.memory.methodsToExec.map(function(m, i) {
                        if (m.id === id) {
                            w.globals.memory.methodsToExec.splice(i, 1);
                        }
                    });
                    Object.keys(w.config.annotations).forEach(function(key) {
                        var annotationArray = w.config.annotations[key];
                        if (Array.isArray(annotationArray)) {
                            w.config.annotations[key] = annotationArray.filter(function(m) {
                                return m.id !== id;
                            });
                        }
                    });
                    Array.prototype.forEach.call(annos, function(a) {
                        a.parentElement.removeChild(a);
                    });
                }
            }
        }
    ]);
    return Annotations;
}();
ApexCharts.registerFeatures({
    annotations: Annotations
});
var KeyboardNavigation = /*#__PURE__*/ function() {
    "use strict";
    function KeyboardNavigation(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, KeyboardNavigation);
        this.w = w;
        this.ctx = ctx;
        this.seriesIndex = 0;
        this.dataPointIndex = 0;
        this.active = false;
        this._tooltipDismissed = false;
        this._focusedEl = null;
        this._hoveredBarEl = null;
        this._enlargedScatterMarker = null;
        this._onKeyDown = this._onKeyDown.bind(this);
        this._onFocus = this._onFocus.bind(this);
        this._onBlur = this._onBlur.bind(this);
        this._onLegendClick = this._onLegendClick.bind(this);
        this._onPointerDown = this._onPointerDown.bind(this);
        this._lastPointerDownAt = 0;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(KeyboardNavigation, [
        {
            // ─── Public API ───────────────────────────────────────────────────────────
            /**
   * Called after the chart and tooltip have been fully rendered.
   * Attaches event listeners and makes the SVG keyboard-focusable.
   */ key: "init",
            value: function init() {
                var w = this.w;
                var svgEl = w.dom.Paper.node;
                if (!svgEl) return;
                svgEl.setAttribute("tabindex", "0");
                svgEl.addEventListener("focus", this._onFocus);
                svgEl.addEventListener("blur", this._onBlur);
                svgEl.addEventListener("mousedown", this._onPointerDown, {
                    capture: true
                });
                svgEl.addEventListener("pointerdown", this._onPointerDown, {
                    capture: true
                });
                svgEl.addEventListener("touchstart", this._onPointerDown, {
                    capture: true,
                    passive: true
                });
                svgEl.addEventListener("keydown", this._onKeyDown, {
                    passive: false
                });
                this.ctx.events.addEventListener("legendClick", this._onLegendClick);
            }
        },
        {
            /**
   * Removes all event listeners. Called from chart.destroy().
   */ key: "destroy",
            value: function destroy() {
                var w = this.w;
                var svgEl = w.dom.Paper && w.dom.Paper.node;
                this.ctx.events.removeEventListener("legendClick", this._onLegendClick);
                if (!svgEl) return;
                svgEl.removeEventListener("focus", this._onFocus);
                svgEl.removeEventListener("blur", this._onBlur);
                svgEl.removeEventListener("keydown", this._onKeyDown);
                svgEl.removeEventListener("mousedown", this._onPointerDown, /** @type {any} */ {
                    capture: true
                });
                svgEl.removeEventListener("pointerdown", this._onPointerDown, /** @type {any} */ {
                    capture: true
                });
                svgEl.removeEventListener("touchstart", this._onPointerDown, /** @type {any} */ {
                    capture: true
                });
            }
        },
        {
            // Records the timestamp of the most recent pointer-down inside the SVG.
            // `_onFocus` reads this to distinguish keyboard-driven focus (no recent
            // pointer activity) from mouse-driven focus (pointer event within the
            // last 100 ms). Stays a no-op for keyboard users.
            key: "_onPointerDown",
            value: function _onPointerDown() {
                this._lastPointerDownAt = Date.now();
            }
        },
        {
            /**
   * Called from Events.js keydown handler. Navigation keys are already handled
   * by the direct SVG listener (which can call preventDefault). This entry
   * point is intentionally a no-op — Events.js still fires the public keyDown
   * callback and fireEvent('keydown') independently.
   * @param {Event} _e
   */ key: "handleKey",
            value: function handleKey(_e) {}
        },
        {
            // ─── Focus / blur ─────────────────────────────────────────────────────────
            key: "_onFocus",
            value: function _onFocus() {
                if (!this._isNavEnabled()) return;
                if (Date.now() - this._lastPointerDownAt < 100) {
                    return;
                }
                this.active = true;
                this._clampCursor();
                this._snapToVisibleRange();
                this._showCurrentPoint();
            }
        },
        {
            key: "_onBlur",
            value: function _onBlur() {
                this.active = false;
                this._tooltipDismissed = false;
                this._hideFocus();
            }
        },
        {
            // Called when the user clicks a legend item (collapse/expand a series).
            // Hide the keyboard-nav tooltip — the chart is about to re-render and the
            // current position may no longer be valid.
            key: "_onLegendClick",
            value: function _onLegendClick() {
                if (!this.active) return;
                this.active = false;
                this._hideFocus();
            }
        },
        {
            // ─── Key handler ──────────────────────────────────────────────────────────
            /**
   * @param {KeyboardEvent} e
   */ key: "_onKeyDown",
            value: function _onKeyDown(e) {
                var _a, _b, _c;
                if (!this._isNavEnabled() || !this.active) return;
                if (e.shiftKey && (e.key === "ArrowRight" || e.key === "ArrowLeft") && this._canPan()) {
                    e.preventDefault();
                    this._panBy(e.key === "ArrowRight" ? 1 : -1);
                    return;
                }
                switch(e.key){
                    case "ArrowRight":
                        e.preventDefault();
                        this._move(0, 1);
                        break;
                    case "ArrowLeft":
                        e.preventDefault();
                        this._move(0, -1);
                        break;
                    case "ArrowUp":
                        e.preventDefault();
                        this._move(-1, 0);
                        break;
                    case "ArrowDown":
                        e.preventDefault();
                        this._move(1, 0);
                        break;
                    case "Home":
                        e.preventDefault();
                        this.dataPointIndex = 0;
                        this._skipNullForward();
                        this._showCurrentPoint();
                        break;
                    case "End":
                        e.preventDefault();
                        this.dataPointIndex = this._getDataPointCount(this.seriesIndex) - 1;
                        this._skipNullBackward();
                        this._showCurrentPoint();
                        break;
                    case "Enter":
                    case " ":
                        e.preventDefault();
                        this._fireClick();
                        break;
                    case "+":
                    case "=":
                        if (this._canZoom()) {
                            e.preventDefault();
                            (_a = this.ctx.toolbar) == null ? void 0 : _a.handleZoomIn();
                            this._announce("Zoomed in");
                        }
                        break;
                    case "-":
                    case "_":
                        if (this._canZoom()) {
                            e.preventDefault();
                            (_b = this.ctx.toolbar) == null ? void 0 : _b.handleZoomOut();
                            this._announce("Zoomed out");
                        }
                        break;
                    case "0":
                        if (this._canZoom() && this.w.interact.zoomed) {
                            e.preventDefault();
                            (_c = this.ctx.toolbar) == null ? void 0 : _c.handleZoomReset();
                            this._announce("Zoom reset");
                        }
                        break;
                    case "Escape":
                        e.preventDefault();
                        if (!this._tooltipDismissed) {
                            this._tooltipDismissed = true;
                            this._hideFocus();
                        } else {
                            this.active = false;
                            this._tooltipDismissed = false;
                            this._hideFocus();
                        }
                        break;
                }
            }
        },
        {
            // ─── Zoom / pan (keyboard alternatives for drag gestures) ─────────────────
            key: "_canZoom",
            value: function _canZoom() {
                var w = this.w;
                return Boolean(w.globals.axisCharts && w.config.chart.zoom && w.config.chart.zoom.enabled);
            }
        },
        {
            key: "_canPan",
            value: function _canPan() {
                return this._canZoom();
            }
        },
        {
            /**
   * Shift the visible x-range by ~10% in the given direction.
   * @param {number} direction +1 = right, -1 = left
   */ key: "_panBy",
            value: function _panBy(direction) {
                var w = this.w;
                var toolbar = this.ctx.toolbar;
                if (!toolbar) return;
                var minX = Number(w.globals.minX);
                var maxX = Number(w.globals.maxX);
                if (!isFinite(minX) || !isFinite(maxX) || minX === maxX) return;
                var span = maxX - minX;
                var step = span * 0.1 * direction;
                toolbar.zoomUpdateOptions(minX + step, maxX + step);
                this._announce(direction > 0 ? "Panned right" : "Panned left");
            }
        },
        {
            // ─── Navigation ───────────────────────────────────────────────────────────
            /**
   * @param {number} dSeries
   * @param {number} dPoint
   */ key: "_move",
            value: function _move(dSeries, dPoint) {
                var w = this.w;
                var wrapAround = w.config.chart.accessibility.keyboard.navigation.wrapAround;
                if (dSeries !== 0) {
                    var ttCtx = this.w.globals.tooltip;
                    if (ttCtx && ttCtx.tConfig && ttCtx.tConfig.shared) {
                        var j = this.dataPointIndex;
                        var isActuallyShared = ttCtx.tooltipUtil && ttCtx.tooltipUtil.isXoverlap(j) && ttCtx.tooltipUtil.isInitialSeriesSameLen();
                        if (isActuallyShared) return;
                    }
                    var total = this._getSeriesCount();
                    var si = this.seriesIndex + dSeries;
                    var attempts = 0;
                    while(attempts < total){
                        if (si < 0) si = wrapAround ? total - 1 : 0;
                        if (si >= total) si = wrapAround ? 0 : total - 1;
                        if (!w.globals.collapsedSeriesIndices.includes(si)) break;
                        si += dSeries;
                        attempts++;
                    }
                    this.seriesIndex = si;
                    var dpCount = this._getDataPointCount(si);
                    if (this.dataPointIndex >= dpCount) {
                        this.dataPointIndex = dpCount - 1;
                    }
                }
                if (dPoint !== 0) {
                    var dpCount1 = this._getDataPointCount(this.seriesIndex);
                    var di = this.dataPointIndex + dPoint;
                    if (di < 0) di = wrapAround ? dpCount1 - 1 : 0;
                    if (di >= dpCount1) di = wrapAround ? 0 : dpCount1 - 1;
                    this.dataPointIndex = di;
                    if (dPoint > 0) {
                        this._skipNullForward();
                    } else {
                        this._skipNullBackward();
                    }
                    if (!this._isDataPointVisible(this.seriesIndex, this.dataPointIndex)) {
                        this._snapToVisibleRangeInDirection(dPoint);
                    }
                }
                this._showCurrentPoint();
            }
        },
        {
            /** Advance dataPointIndex forward past any nulls */ key: "_skipNullForward",
            value: function _skipNullForward() {
                var w = this.w;
                var si = this.seriesIndex;
                var dpCount = this._getDataPointCount(si);
                var di = this.dataPointIndex;
                var attempts = 0;
                if (!Array.isArray(w.seriesData.series[si])) return;
                while(attempts < dpCount && w.seriesData.series[si][di] === null){
                    di = (di + 1) % dpCount;
                    attempts++;
                }
                this.dataPointIndex = di;
            }
        },
        {
            /** Retreat dataPointIndex backward past any nulls */ key: "_skipNullBackward",
            value: function _skipNullBackward() {
                var w = this.w;
                var si = this.seriesIndex;
                var dpCount = this._getDataPointCount(si);
                var di = this.dataPointIndex;
                var attempts = 0;
                if (!Array.isArray(w.seriesData.series[si])) return;
                while(attempts < dpCount && w.seriesData.series[si][di] === null){
                    di = (di - 1 + dpCount) % dpCount;
                    attempts++;
                }
                this.dataPointIndex = di;
            }
        },
        {
            // ─── Display ──────────────────────────────────────────────────────────────
            key: "_showCurrentPoint",
            value: function _showCurrentPoint() {
                var _this = this, i = _this.seriesIndex, j = _this.dataPointIndex;
                var w = this.w;
                var ttCtx = w.globals.tooltip;
                if (!ttCtx || !ttCtx.ttItems) return;
                w.interact.capturedSeriesIndex = i;
                w.interact.capturedDataPointIndex = j;
                this._applyFocusClass(i, j);
                this._showTooltip(i, j, /** @type {any} */ ttCtx);
            }
        },
        {
            key: "_hideFocus",
            value: function _hideFocus() {
                var w = this.w;
                var ttCtx = /** @type {any} */ w.globals.tooltip;
                this._removeFocusClass();
                this._leaveHoveredBar();
                if (!ttCtx) return;
                if (ttCtx.marker) {
                    ttCtx.marker.resetPointsSize();
                }
                this._enlargedScatterMarker = null;
                var tooltipEl = ttCtx.getElTooltip();
                if (tooltipEl) {
                    tooltipEl.classList.remove("apexcharts-active");
                    if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.announcements.enabled) {
                        tooltipEl.setAttribute("aria-hidden", "true");
                    }
                }
                w.dom.baseEl.classList.remove("apexcharts-tooltip-active");
                var xcrosshairs = ttCtx.getElXCrosshairs();
                if (xcrosshairs) xcrosshairs.classList.remove("apexcharts-active");
            }
        },
        {
            // ─── Tooltip display per chart type ───────────────────────────────────────
            /**
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */ key: "_showTooltip",
            value: function _showTooltip(i, j, ttCtx) {
                var w = this.w;
                var type = w.config.chart.type;
                var tooltipEl = ttCtx.getElTooltip();
                if (!tooltipEl) return;
                var cachedDims = ttCtx.getCachedDimensions();
                ttCtx.tooltipRect = {
                    x: 0,
                    y: 0,
                    ttWidth: cachedDims.ttWidth || 0,
                    ttHeight: cachedDims.ttHeight || 0
                };
                this._setSyntheticEvent(i, j, ttCtx);
                w.dom.baseEl.classList.add("apexcharts-tooltip-active");
                tooltipEl.classList.add("apexcharts-active");
                if (w.config.chart.accessibility.enabled && w.config.chart.accessibility.announcements.enabled) {
                    tooltipEl.removeAttribute("aria-hidden");
                }
                if (type === "pie" || type === "donut" || type === "polarArea") {
                    this._showTooltipNonAxis(i, j, ttCtx, tooltipEl);
                } else if (type === "radialBar") {
                    this._showTooltipRadialBar(i, j, ttCtx, tooltipEl);
                } else if (type === "heatmap" || type === "treemap") {
                    this._showTooltipHeatTree(i, j, ttCtx, tooltipEl, type);
                } else if (type === "bar" || type === "candlestick" || type === "boxPlot" || type === "violin" || type === "rangeBar") {
                    this._showTooltipBar(i, j, ttCtx);
                } else {
                    this._showTooltipAxisLine(i, j, ttCtx);
                }
            }
        },
        {
            /**
   * Set ttCtx.e to a synthetic mouse-event-like object whose clientX/Y point
   * to the centre of the current data-point element.  This ensures that any
   * positioning helper that reads ttCtx.e (followCursor path in moveTooltip,
   * moveStickyTooltipOverBars, moveDynamicPointsOnHover, etc.) gets valid
   * coordinates rather than crashing on undefined.
   *
   * For chart types that don't have a concrete SVG element per data point
   * (pie, radialBar) we fall back to the SVG centre.
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */ key: "_setSyntheticEvent",
            value: function _setSyntheticEvent(i, j, ttCtx) {
                var w = this.w;
                var type = w.config.chart.type;
                var clientX = 0;
                var clientY = 0;
                var el = this._getFocusableElement(i, j);
                if (el) {
                    var rect = el.getBoundingClientRect();
                    clientX = rect.left + rect.width / 2;
                    clientY = rect.top + rect.height / 2;
                } else if (w.globals.pointsArray && w.globals.pointsArray[i] && w.globals.pointsArray[i][j]) {
                    var pt = w.globals.pointsArray[i][j];
                    var elGrid = ttCtx.getElGrid && ttCtx.getElGrid();
                    if (elGrid) {
                        var gridRect = elGrid.getBoundingClientRect();
                        clientX = gridRect.left + (pt[0] || 0);
                        clientY = gridRect.top + (pt[1] || 0);
                    }
                } else {
                    var svgEl = w.dom.Paper && w.dom.Paper.node;
                    if (svgEl) {
                        var svgRect = svgEl.getBoundingClientRect();
                        clientX = svgRect.left + svgRect.width / 2;
                        clientY = svgRect.top + svgRect.height / 2;
                    }
                }
                if (type === "line" || type === "area" || type === "rangeArea" || type === "scatter" || type === "bubble" || type === "radar") {
                    if (w.globals.pointsArray && w.globals.pointsArray[i] && w.globals.pointsArray[i][j]) {
                        var pt1 = w.globals.pointsArray[i][j];
                        var elGrid1 = ttCtx.getElGrid && ttCtx.getElGrid();
                        if (elGrid1) {
                            var gridRect1 = elGrid1.getBoundingClientRect();
                            clientX = gridRect1.left + (pt1[0] || 0);
                            clientY = gridRect1.top + (pt1[1] || 0);
                        }
                    }
                }
                ttCtx.e = {
                    type: "mousemove",
                    clientX: clientX,
                    clientY: clientY
                };
            }
        },
        {
            /**
   * bar / column / candlestick / boxPlot / rangeBar
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */ key: "_showTooltipBar",
            value: function _showTooltipBar(i, j, ttCtx) {
                var _a, _b, _c, _d;
                var w = this.w;
                var shared = ttCtx.tConfig.shared && (ttCtx.tooltipUtil.isXoverlap(j) || w.globals.isBarHorizontal) && ttCtx.tooltipUtil.isInitialSeriesSameLen();
                var rangeData = /** @type {any} */ (_d = (_c = (_b = (_a = w.rangeData.seriesRange) == null ? void 0 : _a[i]) == null ? void 0 : _b[j]) == null ? void 0 : _c.y) == null ? void 0 : _d[0];
                ttCtx.tooltipLabels.drawSeriesTexts(__spreadProps(__spreadValues(__spreadValues({
                    ttItems: ttCtx.ttItems,
                    i: i,
                    j: j
                }, (rangeData == null ? void 0 : rangeData.y1) !== void 0 && {
                    y1: rangeData.y1
                }), (rangeData == null ? void 0 : rangeData.y2) !== void 0 && {
                    y2: rangeData.y2
                }), {
                    shared: shared
                }));
                var parent = ".apexcharts-series[data\\:realIndex='".concat(i, "']");
                var elPath = w.dom.Paper.findOne("".concat(parent, " path[j='").concat(j, "'], ").concat(parent, " circle[j='").concat(j, "'], ").concat(parent, " rect[j='").concat(j, "']"));
                if (elPath) {
                    this._leaveHoveredBar();
                    var graphics = new Graphics(this.w, this.ctx);
                    graphics.pathMouseEnter(elPath, null);
                    this._hoveredBarEl = elPath;
                }
                if (w.globals.isBarHorizontal) {
                    var barDomEl = elPath && elPath.node;
                    if (barDomEl) {
                        var wrapRect = w.dom.elWrap.getBoundingClientRect();
                        var barRect = barDomEl.getBoundingClientRect();
                        var barCx = barRect.left - wrapRect.left;
                        var barCy = barRect.top - wrapRect.top;
                        var bh = barRect.height;
                        var bw = barRect.width;
                        var ttWidth = ttCtx.tooltipRect.ttWidth || 0;
                        var ttHeight = ttCtx.tooltipRect.ttHeight || 0;
                        var y = barCy + bh / 2 - ttHeight / 2;
                        var x = barCx + bw;
                        var baselineX = ttCtx.xyRatios && ttCtx.xyRatios.baseLineInvertedY != null ? ttCtx.xyRatios.baseLineInvertedY : wrapRect.width / 2;
                        if (barCx < baselineX) {
                            x = barCx - ttWidth;
                        }
                        var tooltipEl = ttCtx.getElTooltip();
                        if (tooltipEl) {
                            tooltipEl.style.left = x + "px";
                            tooltipEl.style.top = y + "px";
                        }
                    }
                } else {
                    ttCtx.tooltipPosition.moveStickyTooltipOverBars(j, i);
                }
            }
        },
        {
            /**
   * line / area / scatter / bubble / radar / rangeArea
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */ key: "_showTooltipAxisLine",
            value: function _showTooltipAxisLine(i, j, ttCtx) {
                var w = this.w;
                var type = w.config.chart.type;
                var sharedConfigured = ttCtx.tConfig.shared;
                var shared = sharedConfigured && ttCtx.tooltipUtil.isXoverlap(j) && ttCtx.tooltipUtil.isInitialSeriesSameLen();
                ttCtx.tooltipLabels.drawSeriesTexts({
                    ttItems: ttCtx.ttItems,
                    i: i,
                    j: j,
                    shared: shared
                });
                var isScatterLike = type === "scatter" || type === "bubble";
                var hasVisibleMarkers = w.globals.markers.largestSize > 0;
                if (isScatterLike) {
                    this._showScatterBubblePoint(i, j, ttCtx);
                } else if (hasVisibleMarkers) {
                    if (shared) {
                        ttCtx.marker.enlargePoints(j);
                    } else {
                        ttCtx.tooltipPosition.moveDynamicPointOnHover(j, i);
                    }
                } else if (shared) {
                    ttCtx.tooltipPosition.moveDynamicPointsOnHover(j);
                } else {
                    ttCtx.tooltipPosition.moveDynamicPointOnHover(j, i);
                }
            }
        },
        {
            /**
   * Scatter / bubble: find the specific marker element for (seriesIndex i,
   * dataPointIndex j), resize only that element, and position the tooltip at
   * its coordinates — mirroring what Position.moveMarkers does for mouse hover.
   *
   * Unlike enlargePoints(j) which queries ALL series for rel===j (causing
   * multiple bubbles to enlarge and tooltip to land on the wrong one), this
   * method queries by both series index AND data-point index for precision.
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   */ key: "_showScatterBubblePoint",
            value: function _showScatterBubblePoint(i, j, ttCtx) {
                var baseEl = this.w.dom.baseEl;
                if (this._enlargedScatterMarker) {
                    ttCtx.marker.oldPointSize(this._enlargedScatterMarker);
                    this._enlargedScatterMarker = null;
                }
                var seriesEl = baseEl.querySelector(".apexcharts-series[data\\:realIndex='".concat(i, "']"));
                if (!seriesEl) return;
                var markerEl = seriesEl.querySelector(".apexcharts-marker[rel='".concat(j, "']"));
                if (!markerEl) return;
                ttCtx.marker.enlargeCurrentPoint(j, markerEl);
                this._enlargedScatterMarker = markerEl;
            }
        },
        {
            /**
   * pie / donut / polarArea
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   * @param {HTMLElement} tooltipEl
   */ key: "_showTooltipNonAxis",
            value: function _showTooltipNonAxis(i, j, ttCtx, tooltipEl) {
                var _a, _b;
                var w = this.w;
                ttCtx.tooltipLabels.drawSeriesTexts({
                    ttItems: ttCtx.ttItems,
                    i: j,
                    shared: false
                });
                var tooltipBound = tooltipEl.getBoundingClientRect();
                var ttWidth = tooltipBound.width || ttCtx.tooltipRect.ttWidth || 0;
                var ttHeight = tooltipBound.height || ttCtx.tooltipRect.ttHeight || 0;
                var sliceEl = w.dom.baseEl.querySelector(".apexcharts-pie-area[j='".concat(j, "']"));
                if (sliceEl) {
                    var cx = parseFloat((_a = sliceEl.getAttribute("data:cx")) != null ? _a : "");
                    var cy = parseFloat((_b = sliceEl.getAttribute("data:cy")) != null ? _b : "");
                    if (!isNaN(cx) && !isNaN(cy)) {
                        var svgBound = w.dom.Paper.node.getBoundingClientRect();
                        var wrapBound = w.dom.elWrap.getBoundingClientRect();
                        var offsetX = svgBound.left - wrapBound.left;
                        var offsetY = svgBound.top - wrapBound.top;
                        tooltipEl.style.left = offsetX + cx - ttWidth / 2 + "px";
                        tooltipEl.style.top = offsetY + cy - ttHeight - 10 + "px";
                    }
                }
            }
        },
        {
            /**
   * radialBar — one ring per series, single value each
   * @param {number} i
   * @param {any} _j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   * @param {HTMLElement} tooltipEl
   */ key: "_showTooltipRadialBar",
            value: function _showTooltipRadialBar(i, _j, ttCtx, tooltipEl) {
                var _a;
                var w = this.w;
                ttCtx.tooltipLabels.drawSeriesTexts({
                    ttItems: ttCtx.ttItems,
                    i: i,
                    shared: false
                });
                var _ttCtx_getCachedDimensions = ttCtx.getCachedDimensions(), _ttCtx_getCachedDimensions_ttWidth = _ttCtx_getCachedDimensions.ttWidth, ttWidth = _ttCtx_getCachedDimensions_ttWidth === void 0 ? 0 : _ttCtx_getCachedDimensions_ttWidth, _ttCtx_getCachedDimensions_ttHeight = _ttCtx_getCachedDimensions.ttHeight, ttHeight = _ttCtx_getCachedDimensions_ttHeight === void 0 ? 0 : _ttCtx_getCachedDimensions_ttHeight;
                var arcEl = w.dom.baseEl.querySelector(".apexcharts-radialbar-series[data\\:realIndex='".concat(i, "'] path"));
                if (arcEl) {
                    var angle = parseFloat((_a = arcEl.getAttribute("data:angle")) != null ? _a : "") || 0;
                    var initialAngle = w.config.plotOptions.radialBar.startAngle || 0;
                    var midAngle = initialAngle + angle / 2;
                    var centerX = w.layout.gridWidth / 2;
                    var centerY = w.layout.gridHeight / 2;
                    var radialSize = w.globals.radialSize || Math.min(w.layout.gridWidth, w.layout.gridHeight) / 2;
                    var seriesCount = w.seriesData.series.length;
                    var trackSize = radialSize / Math.max(seriesCount, 1);
                    var outerRadius = radialSize - i * trackSize;
                    var innerRadius = outerRadius - trackSize;
                    var ringRadius = (outerRadius + innerRadius) / 2;
                    var centroid = Utils$1.polarToCartesian(centerX, centerY, ringRadius, midAngle);
                    var x = centroid.x + (w.layout.translateX || 0);
                    var y = centroid.y + (w.layout.translateY || 0);
                    tooltipEl.style.left = x - ttWidth / 2 + "px";
                    tooltipEl.style.top = y - ttHeight - 10 + "px";
                }
            }
        },
        {
            /**
   * heatmap / treemap — position tooltip using element bounding rect
   * @param {number} i
   * @param {number} j
   * @param {import('../tooltip/Tooltip').default} ttCtx
   * @param {HTMLElement} tooltipEl
   * @param {string} type
   */ key: "_showTooltipHeatTree",
            value: function _showTooltipHeatTree(i, j, ttCtx, tooltipEl, type) {
                var _a, _b;
                var w = this.w;
                ttCtx.tooltipLabels.drawSeriesTexts({
                    ttItems: ttCtx.ttItems,
                    i: i,
                    j: j,
                    shared: false
                });
                var tooltipRect = tooltipEl.getBoundingClientRect();
                var ttWidth = tooltipRect.width || ttCtx.tooltipRect.ttWidth || 0;
                var ttHeight = tooltipRect.height || ttCtx.tooltipRect.ttHeight || 0;
                var rectClass = type === "heatmap" ? "apexcharts-heatmap-rect" : "apexcharts-treemap-rect";
                var cell = w.dom.baseEl.querySelector(".".concat(rectClass, "[i='").concat(i, "'][j='").concat(j, "']"));
                if (cell) {
                    var wrapRect = w.dom.elWrap.getBoundingClientRect();
                    var cellRect = cell.getBoundingClientRect();
                    var cellCx = cellRect.left - wrapRect.left;
                    var cellCy = cellRect.top - wrapRect.top;
                    var cellWidth = cellRect.width;
                    var cellHeight = cellRect.height;
                    var cx = parseFloat((_a = cell.getAttribute("cx")) != null ? _a : "");
                    var cellWidthAttr = parseFloat((_b = cell.getAttribute("width")) != null ? _b : "");
                    ttCtx.tooltipPosition.moveXCrosshairs(cx + cellWidthAttr / 2);
                    var x = cellCx + cellWidth + ttWidth / 2;
                    var y = cellCy + cellHeight / 2 - ttHeight / 2;
                    if (cellCx + cellWidth > w.layout.gridWidth / 2) {
                        x = cellCx - ttWidth / 2;
                    }
                    tooltipEl.style.left = x + "px";
                    tooltipEl.style.top = y + "px";
                }
            }
        },
        {
            // ─── Focus class management ───────────────────────────────────────────────
            /**
   * @param {number} i
   * @param {number} j
   */ key: "_applyFocusClass",
            value: function _applyFocusClass(i, j) {
                this._removeFocusClass();
                var el = this._getFocusableElement(i, j);
                if (el) {
                    el.classList.add("apexcharts-keyboard-focused");
                    el.setAttribute("role", "img");
                    var label = this._buildPointLabel(i, j);
                    if (label) el.setAttribute("aria-label", label);
                    this._focusedEl = el;
                }
            }
        },
        {
            key: "_removeFocusClass",
            value: function _removeFocusClass() {
                if (this._focusedEl) {
                    this._focusedEl.classList.remove("apexcharts-keyboard-focused");
                    this._focusedEl.removeAttribute("role");
                    this._focusedEl.removeAttribute("aria-label");
                    this._focusedEl = null;
                }
            }
        },
        {
            /**
   * Build an accessible label for the data point at (i, j) using the same
   * formatters the visible tooltip / axis labels use, so SR output matches
   * the visual presentation.
   * @param {number} i
   * @param {number} j
   * @returns {string}
   */ key: "_buildPointLabel",
            value: function _buildPointLabel(i, j) {
                var _a, _b, _c, _d, _e, _f, _g, _h;
                var w = this.w;
                var type = w.config.chart.type;
                var seriesNames = w.seriesData.seriesNames || [];
                var series = w.seriesData.series || [];
                if (type === "pie" || type === "donut" || type === "polarArea") {
                    var sliceLabel = (_b = ((_a = w.labelData) == null ? void 0 : _a.labels) && w.labelData.labels[j]) != null ? _b : "";
                    var value = Array.isArray(series) ? series[j] : "";
                    return sliceLabel ? "".concat(sliceLabel, ": ").concat(value) : "".concat(value);
                }
                if (type === "radialBar") {
                    var seriesName2 = seriesNames[i] || "Series ".concat(i + 1);
                    var value1 = Array.isArray(series) ? series[i] : "";
                    return "".concat(seriesName2, ": ").concat(value1);
                }
                var seriesName = seriesNames[i] || "Series ".concat(i + 1);
                var row = Array.isArray(series[i]) ? series[i] : [];
                var rawValue = row[j];
                var formattedValue = rawValue == null ? "" : String(rawValue);
                var yFormatter = (_d = (_c = w.formatters) == null ? void 0 : _c.yLabelFormatters) == null ? void 0 : _d[i];
                if (typeof yFormatter === "function") {
                    try {
                        formattedValue = yFormatter(rawValue, {
                            seriesIndex: i,
                            dataPointIndex: j,
                            w: w
                        });
                    } catch (e) {}
                }
                var category = "";
                var categoryLabels = (_e = w.labelData) == null ? void 0 : _e.categoryLabels;
                var seriesX = (_g = (_f = w.seriesData) == null ? void 0 : _f.seriesX) == null ? void 0 : _g[i];
                if (Array.isArray(categoryLabels) && categoryLabels[j] != null) {
                    category = String(categoryLabels[j]);
                } else if (Array.isArray(seriesX) && seriesX[j] != null) {
                    var xFormatter = (_h = w.formatters) == null ? void 0 : _h.xLabelFormatter;
                    if (typeof xFormatter === "function") {
                        try {
                            category = String(xFormatter(seriesX[j], {
                                seriesIndex: i,
                                dataPointIndex: j,
                                w: w
                            }));
                        } catch (e) {
                            category = String(seriesX[j]);
                        }
                    } else {
                        category = String(seriesX[j]);
                    }
                }
                return category ? "".concat(seriesName, ": ").concat(formattedValue, ", ").concat(category) : "".concat(seriesName, ": ").concat(formattedValue);
            }
        },
        {
            key: "_leaveHoveredBar",
            value: function _leaveHoveredBar() {
                if (this._hoveredBarEl) {
                    var graphics = new Graphics(this.w, this.ctx);
                    graphics.pathMouseLeave(this._hoveredBarEl, null);
                    this._hoveredBarEl = null;
                }
            }
        },
        {
            /**
   * @param {number} i
   * @param {number} j
   */ key: "_getFocusableElement",
            value: function _getFocusableElement(i, j) {
                var w = this.w;
                var type = w.config.chart.type;
                var baseEl = w.dom.baseEl;
                if (type === "pie" || type === "donut" || type === "polarArea") {
                    return baseEl.querySelector(".apexcharts-pie-area[j='".concat(j, "']"));
                }
                if (type === "heatmap") {
                    return baseEl.querySelector(".apexcharts-heatmap-rect[i='".concat(i, "'][j='").concat(j, "']"));
                }
                if (type === "treemap") {
                    return baseEl.querySelector(".apexcharts-treemap-rect[i='".concat(i, "'][j='").concat(j, "']"));
                }
                if (type === "radialBar") {
                    return baseEl.querySelector(".apexcharts-radialbar-series[data\\:realIndex='".concat(i, "'] path"));
                }
                if (type === "bar" || type === "candlestick" || type === "boxPlot" || type === "violin" || type === "rangeBar") {
                    return baseEl.querySelector(".apexcharts-series[data\\:realIndex='".concat(i, "'] path[j='").concat(j, "']"));
                }
                var marker = baseEl.querySelector(".apexcharts-series[data\\:realIndex='".concat(i, "'] .apexcharts-marker[rel='").concat(j, "']"));
                return marker || null;
            }
        },
        {
            // ─── Click / Enter ────────────────────────────────────────────────────────
            key: "_fireClick",
            value: function _fireClick() {
                var w = this.w;
                var ttCtx = w.globals.tooltip;
                if (!ttCtx) return;
                var syntheticEvent = {
                    type: "mouseup",
                    clientX: 0,
                    clientY: 0
                };
                ttCtx.markerClick(syntheticEvent, this.seriesIndex, this.dataPointIndex);
            }
        },
        {
            // ─── Helpers ──────────────────────────────────────────────────────────────
            key: "_isNavEnabled",
            value: function _isNavEnabled() {
                var a11y = this.w.config.chart.accessibility;
                return a11y.enabled && a11y.keyboard.enabled && a11y.keyboard.navigation.enabled;
            }
        },
        {
            key: "_getSeriesCount",
            value: function _getSeriesCount() {
                var w = this.w;
                var type = w.config.chart.type;
                if (type === "pie" || type === "donut" || type === "polarArea") {
                    return 1;
                }
                return w.seriesData.series.length;
            }
        },
        {
            /**
   * @param {number} si
   */ key: "_getDataPointCount",
            value: function _getDataPointCount(si) {
                var w = this.w;
                var type = w.config.chart.type;
                if (type === "pie" || type === "donut" || type === "polarArea") {
                    return w.seriesData.series.length;
                }
                var series = w.seriesData.series;
                return series[si] && Array.isArray(series[si]) ? series[si].length : 0;
            }
        },
        {
            key: "_clampCursor",
            value: function _clampCursor() {
                var seriesCount = this._getSeriesCount();
                if (this.seriesIndex >= seriesCount) this.seriesIndex = seriesCount - 1;
                if (this.seriesIndex < 0) this.seriesIndex = 0;
                var dpCount = this._getDataPointCount(this.seriesIndex);
                if (this.dataPointIndex >= dpCount) this.dataPointIndex = dpCount - 1;
                if (this.dataPointIndex < 0) this.dataPointIndex = 0;
            }
        },
        {
            /**
   * When the chart is zoomed in, the current dataPointIndex may point to a
   * data point that is outside the visible viewport. Snap the cursor to the
   * first data point whose x-value falls within [minX, maxX].
   *
   * Only adjusts when w.seriesData.seriesX is populated (numeric/datetime axes).
   * Category-only charts (seriesX entries are strings or auto-indices) are
   * unaffected — all points are always visible.
   */ key: "_snapToVisibleRange",
            value: function _snapToVisibleRange() {
                var w = this.w;
                var gl = w.globals;
                var si = this.seriesIndex;
                if (!w.interact.zoomed) return;
                var seriesX = w.seriesData.seriesX && w.seriesData.seriesX[si];
                if (!seriesX || !seriesX.length) return;
                var minX = gl.minX;
                var maxX = gl.maxX;
                if (minX === void 0 || maxX === void 0) return;
                var currentX = seriesX[this.dataPointIndex];
                if (currentX >= minX && currentX <= maxX) return;
                var dpCount = seriesX.length;
                for(var di = 0; di < dpCount; di++){
                    if (seriesX[di] >= minX && seriesX[di] <= maxX) {
                        this.dataPointIndex = di;
                        return;
                    }
                }
            }
        },
        {
            /**
   * Snap to the nearest visible data point in the given navigation direction.
   * direction > 0 → find the first visible point (left boundary of zoomed range)
   * direction < 0 → find the last visible point (right boundary of zoomed range)
   * @param {number} direction
   */ key: "_snapToVisibleRangeInDirection",
            value: function _snapToVisibleRangeInDirection(direction) {
                var w = this.w;
                var gl = w.globals;
                var si = this.seriesIndex;
                var seriesX = w.seriesData.seriesX && w.seriesData.seriesX[si];
                if (!seriesX || !seriesX.length) return;
                var minX = gl.minX;
                var maxX = gl.maxX;
                if (minX === void 0 || maxX === void 0) return;
                var dpCount = seriesX.length;
                if (direction >= 0) {
                    for(var di = 0; di < dpCount; di++){
                        if (seriesX[di] >= minX && seriesX[di] <= maxX) {
                            this.dataPointIndex = di;
                            return;
                        }
                    }
                } else {
                    for(var di1 = dpCount - 1; di1 >= 0; di1--){
                        if (seriesX[di1] >= minX && seriesX[di1] <= maxX) {
                            this.dataPointIndex = di1;
                            return;
                        }
                    }
                }
            }
        },
        {
            /**
   * Check whether the data point at (si, di) is within the current visible
   * x-axis range. Used to skip out-of-viewport points during keyboard nav.
   * @param {number} si
   * @param {number} di
   */ key: "_isDataPointVisible",
            value: function _isDataPointVisible(si, di) {
                var w = this.w;
                var gl = w.globals;
                if (!w.interact.zoomed) return true;
                var seriesX = w.seriesData.seriesX && w.seriesData.seriesX[si];
                if (!seriesX) return true;
                var x = seriesX[di];
                if (x === void 0) return true;
                return x >= gl.minX && x <= gl.maxX;
            }
        },
        {
            /**
   * Push a short status message to the visually-hidden aria-live region so
   * screen readers announce zoom / pan / reset events that have no inherent
   * tooltip update. Silently no-op if the region is missing or announcements
   * are disabled.
   * @param {string} message
   */ key: "_announce",
            value: function _announce(message) {
                var w = this.w;
                if (!w.config.chart.accessibility.announcements.enabled) return;
                var baseEl = w.dom.baseEl;
                if (!baseEl) return;
                var region = baseEl.querySelector(".apexcharts-sr-status");
                if (!region) return;
                region.textContent = "";
                setTimeout(function() {
                    region.textContent = message;
                }, 0);
            }
        }
    ]);
    return KeyboardNavigation;
}();
ApexCharts.registerFeatures({
    keyboardNavigation: KeyboardNavigation
});
var BAR_FAMILY = /* @__PURE__ */ new Set([
    "bar",
    "funnel",
    "pyramid"
]);
var RADIAL_FAMILY = /* @__PURE__ */ new Set([
    "pie",
    "donut",
    "polarArea",
    "radialBar",
    "gauge"
]);
var UNIT_FAMILY = /* @__PURE__ */ new Set([
    "unit"
]);
function familyOf(type) {
    if (BAR_FAMILY.has(type)) return "bar";
    if (RADIAL_FAMILY.has(type)) return "radial";
    if (UNIT_FAMILY.has(type)) return "unit";
    return null;
}
var MorphTypeChange = /*#__PURE__*/ function() {
    "use strict";
    function MorphTypeChange(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, MorphTypeChange);
        this.w = w;
        this.ctx = ctx;
        this._snapshot = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(MorphTypeChange, [
        {
            /**
   * @param {string} fromType
   * @param {string} toType
   * @returns {boolean}
   */ key: "canMorphTypes",
            value: function canMorphTypes(fromType, toType) {
                if (fromType === toType) return false;
                var ff = familyOf(fromType);
                var tf = familyOf(toType);
                if (!ff || !tf) return false;
                return true;
            }
        },
        {
            /**
   * @param {string} fromType
   * @param {string} toType
   * @param {any} newSeries
   * @returns {boolean}
   */ key: "isCompatibleSeriesShape",
            value: function isCompatibleSeriesShape(fromType, toType, newSeries) {
                if (!Array.isArray(newSeries) || newSeries.length === 0) return false;
                var ff = familyOf(fromType);
                var tf = familyOf(toType);
                if (tf === "radial" || tf === "unit") {
                    if (newSeries.every(function(v) {
                        return typeof v === "number";
                    })) return true;
                    return newSeries.length === 1 && newSeries[0] && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(newSeries[0]) === "object" && Array.isArray(newSeries[0].data);
                }
                if (tf === "bar") {
                    return newSeries.every(function(s) {
                        return s && (typeof s === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(s)) === "object" && Array.isArray(s.data);
                    });
                }
                return ff !== null && tf !== null;
            }
        },
        {
            /**
   * Capture the live DOM of the *current* (outgoing) chart and stash it on
   * this module. Called from `apexcharts._updateOptions` before the config
   * merge that flips `chart.type`.
   *
   * Returns true if a morph is queued — caller doesn't need the value, but
   * tests use it.
   *
   * @param {{ fromType: string, toType: string, newSeries: any }} args
   * @returns {boolean}
   */ key: "captureBeforeDestroy",
            value: function captureBeforeDestroy(param) {
                var fromType = param.fromType, toType = param.toType, newSeries = param.newSeries;
                this._snapshot = null;
                if (!Environment.isBrowser()) return false;
                var animCfg = this.w.config.chart.animations;
                if (!animCfg || animCfg.enabled === false) return false;
                if (animCfg.chartTypeMorph && animCfg.chartTypeMorph.enabled === false) return false;
                if (animCfg.respectReducedMotion && prefersReducedMotion()) return false;
                if (!this.canMorphTypes(fromType, toType)) return false;
                if (!this.isCompatibleSeriesShape(fromType, toType, newSeries)) return false;
                var captured = this._captureFromDOM(fromType);
                if (!captured.length) return false;
                var mapping = this._buildMapping(captured, fromType, toType, newSeries);
                if (mapping.size === 0) return false;
                this._snapshot = {
                    fromType: fromType,
                    toType: toType,
                    mapping: mapping,
                    oldLayout: {
                        translateX: this.w.layout.translateX || 0,
                        translateY: this.w.layout.translateY || 0
                    }
                };
                this.w.globals.previousPaths = [];
                return true;
            }
        },
        {
            /**
   * Walk the outgoing chart's DOM and collect path `d` strings keyed by
   * (realIndex, j). The selectors are scoped to the chart family — bar
   * elements have `pathTo` set; pie/radial elements use their final `d`.
   *
   * @param {string} fromType
   * @returns {Array<{ realIndex: number, j: number, d: string, fill: string|null }>}
   */ key: "_captureFromDOM",
            value: function _captureFromDOM(fromType) {
                var _this = this;
                var _a;
                var baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
                if (!baseEl) return [];
                var captured = [];
                var fam = familyOf(fromType);
                if (fam === "bar") {
                    var seriesNodes = baseEl.querySelectorAll(".apexcharts-bar-series .apexcharts-series");
                    seriesNodes.forEach(function(seriesNode) {
                        var _a2;
                        var realIndex = parseInt((_a2 = seriesNode.getAttribute("data:realIndex")) != null ? _a2 : "0", 10);
                        var paths = seriesNode.querySelectorAll("path[pathTo]");
                        paths.forEach(function(p, j) {
                            var d = p.getAttribute("pathTo") || p.getAttribute("d");
                            if (!d) return;
                            captured.push({
                                realIndex: realIndex,
                                j: j,
                                d: d,
                                fill: p.getAttribute("fill")
                            });
                        });
                    });
                } else if (fam === "radial") {
                    if (fromType === "radialBar" || fromType === "gauge") {
                        var centerX = this.w.layout.gridWidth / 2;
                        var centerY = Math.min(this.w.layout.gridWidth, this.w.layout.gridHeight) / 2;
                        var rings = baseEl.querySelectorAll(".apexcharts-radial-series .apexcharts-radialbar-area");
                        rings.forEach(function(p) {
                            var _a2;
                            var parent = /** @type {Element|null} */ p.parentElement;
                            var realIndex = parseInt((_a2 = parent == null ? void 0 : parent.getAttribute("data:realIndex")) != null ? _a2 : "0", 10);
                            var rawD = p.getAttribute("d");
                            if (!rawD) return;
                            var strokeWidth = parseFloat(p.getAttribute("stroke-width") || "0");
                            var d = strokeWidth > 1 ? _this._radialArcToFilledSegment(rawD, strokeWidth, centerX, centerY) || rawD : rawD;
                            captured.push({
                                realIndex: realIndex,
                                j: 0,
                                d: d,
                                fill: p.getAttribute("stroke")
                            });
                        });
                    } else {
                        var slices = baseEl.querySelectorAll(".apexcharts-pie-series .apexcharts-pie-area");
                        slices.forEach(function(p, i) {
                            var d = p.getAttribute("d");
                            if (!d) return;
                            captured.push({
                                realIndex: i,
                                j: 0,
                                d: d,
                                fill: p.getAttribute("fill")
                            });
                        });
                    }
                }
                return captured;
            }
        },
        {
            /**
   * Convert a radialBar's stroked open-arc `d` ("M x1 y1 A r r 0 large sweep
   * x2 y2") into a closed donut-segment polygon whose FILLED rendering
   * visually matches the original stroked arc — needed because the morph
   * target (pie/donut/polarArea) renders by fill, not stroke. Returns null
   * if the input doesn't match the expected M-then-A shape.
   *
   * @param {string} rawD
   * @param {number} strokeWidth
   * @param {number} centerX
   * @param {number} centerY
   * @returns {string | null}
   */ key: "_radialArcToFilledSegment",
            value: function _radialArcToFilledSegment(rawD, strokeWidth, centerX, centerY) {
                var m = rawD.match(/M\s*(-?[\d.]+)\s+(-?[\d.]+)\s+A\s*(-?[\d.]+)\s+(?:-?[\d.]+)\s+(?:-?[\d.]+)\s+(\d)\s+(\d)\s+(-?[\d.]+)\s+(-?[\d.]+)/);
                if (!m) return null;
                var x1 = parseFloat(m[1]);
                var y1 = parseFloat(m[2]);
                var r = parseFloat(m[3]);
                var large = parseInt(m[4], 10);
                var sweep = parseInt(m[5], 10);
                var x2 = parseFloat(m[6]);
                var y2 = parseFloat(m[7]);
                if (!isFinite(r) || r <= 0) return null;
                var half = strokeWidth / 2;
                var rOuter = r + half;
                var rInner = Math.max(0, r - half);
                var proj = function proj(px, py, newR) {
                    var dx = px - centerX;
                    var dy = py - centerY;
                    var dist = Math.sqrt(dx * dx + dy * dy);
                    if (dist === 0) return {
                        x: centerX,
                        y: centerY
                    };
                    var k = newR / dist;
                    return {
                        x: centerX + dx * k,
                        y: centerY + dy * k
                    };
                };
                var o1 = proj(x1, y1, rOuter);
                var o2 = proj(x2, y2, rOuter);
                var i1 = proj(x1, y1, rInner);
                var i2 = proj(x2, y2, rInner);
                var sweepBack = sweep ? 0 : 1;
                return "M ".concat(o1.x, " ").concat(o1.y, " A ").concat(rOuter, " ").concat(rOuter, " 0 ").concat(large, " ").concat(sweep, " ").concat(o2.x, " ").concat(o2.y, " L ").concat(i2.x, " ").concat(i2.y, " A ").concat(rInner, " ").concat(rInner, " 0 ").concat(large, " ").concat(sweepBack, " ").concat(i1.x, " ").concat(i1.y, " Z");
            }
        },
        {
            /**
   * Build a closed donut-segment path for the given polar arc geometry. Used
   * by Radial.drawArcs when morphing FROM a filled wedge (pie/donut/polarArea)
   * TO a radialBar arc: the final radialBar is rendered as a stroked open arc,
   * but during the morph we tween d toward this closed-segment form (which
   * looks identical to the stroked arc when filled with the same color) so
   * the in-between frames remain visually consistent filled shapes rather
   * than a thick-outlined wedge.
   *
   * @param {number} centerX
   * @param {number} centerY
   * @param {number} ringRadius - centerline radius of the radialBar ring
   * @param {number} strokeWidth - the ring's stroke thickness
   * @param {number} startAngleDeg - in degrees, 0° = top (12 o'clock)
   * @param {number} endAngleDeg
   * @returns {string}
   */ key: "buildRingSegmentPath",
            value: function buildRingSegmentPath(centerX, centerY, ringRadius, strokeWidth, startAngleDeg, endAngleDeg) {
                var halfStroke = strokeWidth / 2;
                var rOuter = ringRadius + halfStroke;
                var rInner = Math.max(0, ringRadius - halfStroke);
                var sRad = (startAngleDeg - 90) * Math.PI / 180;
                var eRad = (endAngleDeg - 90) * Math.PI / 180;
                var oStart = {
                    x: centerX + rOuter * Math.cos(sRad),
                    y: centerY + rOuter * Math.sin(sRad)
                };
                var oEnd = {
                    x: centerX + rOuter * Math.cos(eRad),
                    y: centerY + rOuter * Math.sin(eRad)
                };
                var iStart = {
                    x: centerX + rInner * Math.cos(sRad),
                    y: centerY + rInner * Math.sin(sRad)
                };
                var iEnd = {
                    x: centerX + rInner * Math.cos(eRad),
                    y: centerY + rInner * Math.sin(eRad)
                };
                var sweep = endAngleDeg > startAngleDeg ? 1 : 0;
                var large = Math.abs(endAngleDeg - startAngleDeg) > 180 ? 1 : 0;
                return "M ".concat(oStart.x, " ").concat(oStart.y, " A ").concat(rOuter, " ").concat(rOuter, " 0 ").concat(large, " ").concat(sweep, " ").concat(oEnd.x, " ").concat(oEnd.y, " L ").concat(iEnd.x, " ").concat(iEnd.y, " A ").concat(rInner, " ").concat(rInner, " 0 ").concat(large, " ").concat(1 - sweep, " ").concat(iStart.x, " ").concat(iStart.y, " Z");
            }
        },
        {
            /**
   * @returns {string | null} the chart-type the active snapshot was captured
   *   from, or null when no morph is in flight.
   */ key: "getFromType",
            value: function getFromType() {
                return this._snapshot ? this._snapshot.fromType : null;
            }
        },
        {
            /**
   * Build a (targetKey → captured) map. The targetKey matches the lookup
   * pattern each chart-type renderer uses when it asks
   * `getInitialPathFor(realIndex, j)`.
   *
   * Strategy: flatten the captured items into a linear sequence (matching the
   * source chart's natural DOM iteration order: series-then-point for bar,
   * ring-by-ring for radial), then walk the target's iteration positions in
   * the same order and pair them up 1:1. This handles every supported shape
   * without per-pair branching:
   *
   *   - bar (1 series, N pts) ↔ radial-family (N items)  → linear[k] ↔ k
   *   - bar (M series, 1 pt)  ↔ radial-family (M items)  → linear[k] ↔ k
   *   - radial-family (N items) ↔ bar (any matching shape) → linear[k] ↔ flat target
   *   - radial-family ↔ radial-family                    → linear[k] ↔ k
   *
   * @param {Array<{ realIndex: number, j: number, d: string, fill: string|null }>} captured
   * @param {string} _fromType
   * @param {string} toType
   * @param {any} newSeries - the series array being passed to the new chart;
   *   used only to derive the bar target's (realIndex, j) iteration positions.
   */ key: "_buildMapping",
            value: function _buildMapping(captured, _fromType, toType, newSeries) {
                var map = /* @__PURE__ */ new Map();
                var tf = familyOf(toType);
                var flat = captured.slice().sort(function(a, b) {
                    return a.realIndex - b.realIndex || a.j - b.j;
                });
                if (tf === "radial" || tf === "unit") {
                    flat.forEach(function(c, i) {
                        map.set("".concat(i, ":0"), {
                            d: c.d,
                            fill: c.fill
                        });
                    });
                    return map;
                }
                if (tf === "bar") {
                    var positions = [];
                    var series = Array.isArray(newSeries) ? newSeries : [];
                    series.forEach(function(s, seriesIdx) {
                        var data = s && Array.isArray(s.data) ? s.data : [];
                        for(var j = 0; j < data.length; j++){
                            positions.push({
                                realIndex: seriesIdx,
                                j: j
                            });
                        }
                    });
                    flat.forEach(function(c, i) {
                        var pos = positions[i];
                        if (pos) {
                            map.set("".concat(pos.realIndex, ":").concat(pos.j), {
                                d: c.d,
                                fill: c.fill
                            });
                        }
                    });
                    return map;
                }
                return map;
            }
        },
        {
            key: "isActive",
            value: function isActive() {
                return this._snapshot !== null;
            }
        },
        {
            /**
   * @param {number} realIndex
   * @param {number} j
   * @returns {string | null}
   */ key: "getInitialPathFor",
            value: function getInitialPathFor(realIndex, j) {
                if (!this._snapshot) return null;
                var entry = this._snapshot.mapping.get("".concat(realIndex, ":").concat(j));
                if (!entry) return null;
                var dx = this._snapshot.oldLayout.translateX - (this.w.layout.translateX || 0);
                var dy = this._snapshot.oldLayout.translateY - (this.w.layout.translateY || 0);
                return dx === 0 && dy === 0 ? entry.d : this._translatePathD(entry.d, dx, dy);
            }
        },
        {
            /**
   * Offset every absolute coordinate in an SVG path `d` by (dx, dy).
   *
   * Assumes the path uses only uppercase (absolute) commands — every path
   * ApexCharts generates does. Relative-command paths would pass through
   * unchanged at the lowercase, which is also semantically correct (deltas
   * don't shift under a parent translate).
   *
   * @param {string} d
   * @param {number} dx
   * @param {number} dy
   * @returns {string}
   */ key: "_translatePathD",
            value: function _translatePathD(d, dx, dy) {
                if (dx === 0 && dy === 0) return d;
                var commands = parsePath(d);
                return commands.map(/** @param {any[]} c */ function(c) {
                    var cmd = c[0];
                    if (cmd === "Z") return "Z";
                    if (cmd === "M" || cmd === "L" || cmd === "T") {
                        return "".concat(cmd, " ").concat(c[1] + dx, " ").concat(c[2] + dy);
                    }
                    if (cmd === "H") return "".concat(cmd, " ").concat(c[1] + dx);
                    if (cmd === "V") return "".concat(cmd, " ").concat(c[1] + dy);
                    if (cmd === "C") {
                        return "".concat(cmd, " ").concat(c[1] + dx, " ").concat(c[2] + dy, " ").concat(c[3] + dx, " ").concat(c[4] + dy, " ").concat(c[5] + dx, " ").concat(c[6] + dy);
                    }
                    if (cmd === "S" || cmd === "Q") {
                        return "".concat(cmd, " ").concat(c[1] + dx, " ").concat(c[2] + dy, " ").concat(c[3] + dx, " ").concat(c[4] + dy);
                    }
                    if (cmd === "A") {
                        return "".concat(cmd, " ").concat(c[1], " ").concat(c[2], " ").concat(c[3], " ").concat(c[4], " ").concat(c[5], " ").concat(c[6] + dx, " ").concat(c[7] + dy);
                    }
                    return c.join(" ");
                }).join(" ");
            }
        },
        {
            /**
   * The centre point (in the NEW chart's screen space) of the captured shape
   * for cluster `i`. Kept for callers that only need a point; the unit renderer
   * uses getInitialBBoxFor so its dots fill the shape rather than stack on a
   * single point.
   * @param {number} i
   * @returns {{ x: number, y: number } | null}
   */ key: "getInitialCenterFor",
            value: function getInitialCenterFor(i) {
                var box = this.getInitialBBoxFor(i);
                if (!box) return null;
                return {
                    x: box.x + box.width / 2,
                    y: box.y + box.height / 2
                };
            }
        },
        {
            /**
   * The bounding box (in the NEW chart's screen space) of the captured shape
   * for cluster `i`. The unit (dot-cluster) renderer distributes the cluster's
   * dots ACROSS this box as their start positions, so a tall bar visibly breaks
   * apart into a tall column of dots that then swarm into the cluster.
   * @param {number} i
   * @returns {{ x: number, y: number, width: number, height: number } | null}
   */ key: "getInitialBBoxFor",
            value: function getInitialBBoxFor(i) {
                if (!this._snapshot) return null;
                var entry = this._snapshot.mapping.get("".concat(i, ":0"));
                if (!entry) return null;
                var box = this._pathBBox(entry.d);
                if (!box) return null;
                var dx = this._snapshot.oldLayout.translateX - (this.w.layout.translateX || 0);
                var dy = this._snapshot.oldLayout.translateY - (this.w.layout.translateY || 0);
                return {
                    x: box.minX + dx,
                    y: box.minY + dy,
                    width: box.maxX - box.minX,
                    height: box.maxY - box.minY
                };
            }
        },
        {
            /**
   * Bounding box of an absolute-command SVG path `d`. Good enough as the burst
   * footprint (we only need where the shape sat, not exact geometry).
   * @param {string} d
   * @returns {{ minX:number, minY:number, maxX:number, maxY:number } | null}
   */ key: "_pathBBox",
            value: function _pathBBox(d) {
                var commands = parsePath(d);
                var minX = Infinity;
                var minY = Infinity;
                var maxX = -Infinity;
                var maxY = -Infinity;
                var seen = false;
                commands.forEach(/** @param {any[]} c */ function(c) {
                    var cmd = c[0];
                    if (cmd === "Z") return;
                    var pairs = [];
                    if (cmd === "H") pairs = [
                        [
                            c[1],
                            (minY + maxY) / 2 || c[1]
                        ]
                    ];
                    else if (cmd === "V") pairs = [
                        [
                            (minX + maxX) / 2 || c[1],
                            c[1]
                        ]
                    ];
                    else if (cmd === "A") pairs = [
                        [
                            c[6],
                            c[7]
                        ]
                    ];
                    else {
                        for(var k = 1; k + 1 < c.length; k += 2)pairs.push([
                            c[k],
                            c[k + 1]
                        ]);
                    }
                    pairs.forEach(function(param) {
                        var _param = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(param, 2), x = _param[0], y = _param[1];
                        if (!isFinite(x) || !isFinite(y)) return;
                        seen = true;
                        if (x < minX) minX = x;
                        if (x > maxX) maxX = x;
                        if (y < minY) minY = y;
                        if (y > maxY) maxY = y;
                    });
                });
                if (!seen) return null;
                return {
                    minX: minX,
                    minY: minY,
                    maxX: maxX,
                    maxY: maxY
                };
            }
        },
        {
            /**
   * @param {number} realIndex
   * @param {number} j
   * @returns {string | null}
   */ key: "getInitialFillFor",
            value: function getInitialFillFor(realIndex, j) {
                if (!this._snapshot) return null;
                var entry = this._snapshot.mapping.get("".concat(realIndex, ":").concat(j));
                return entry ? entry.fill : null;
            }
        },
        {
            /** @returns {number} */ key: "getSpeed",
            value: function getSpeed() {
                var animCfg = this.w.config.chart.animations;
                return animCfg.chartTypeMorph && animCfg.chartTypeMorph.speed || animCfg.speed || 600;
            }
        },
        {
            /**
   * Fade newly-mounted axes / grid / legend / titles from opacity 0 → 1 in
   * parallel with the morph. Without this the chart's chrome would pop in
   * abruptly while the series elements are still mid-tween, which reads as a
   * jarring layout shift.
   */ key: "applyChromeFade",
            value: function applyChromeFade() {
                var _this = this;
                var _a;
                if (!this._snapshot || !Environment.isBrowser()) return;
                var baseEl = (_a = this.w.globals.dom) == null ? void 0 : _a.baseEl;
                if (!baseEl) return;
                var speed = this.getSpeed();
                var chromeSelectors = [
                    ".apexcharts-xaxis",
                    ".apexcharts-yaxis",
                    ".apexcharts-grid",
                    ".apexcharts-gridlines-horizontal",
                    ".apexcharts-gridlines-vertical",
                    ".apexcharts-legend",
                    ".apexcharts-title-text",
                    ".apexcharts-subtitle-text"
                ];
                chromeSelectors.forEach(function(sel) {
                    baseEl.querySelectorAll(sel).forEach(function(el) {
                        if (!el.style) return;
                        el.style.opacity = "0";
                        el.style.transition = "opacity ".concat(speed, "ms ease-out");
                        BrowserAPIs.requestAnimationFrame(function() {
                            el.style.opacity = "1";
                        });
                        setTimeout(function() {
                            el.style.transition = "";
                            el.style.opacity = "";
                        }, speed + 80);
                    });
                });
                setTimeout(function() {
                    return _this.cleanup();
                }, speed + 100);
            }
        },
        {
            key: "cleanup",
            value: function cleanup() {
                this._snapshot = null;
            }
        }
    ]);
    return MorphTypeChange;
}();
ApexCharts.registerFeatures({
    morphTypeChange: MorphTypeChange
});
var XHTML$1 = "http://www.w3.org/1999/xhtml";
var Breadcrumb = /*#__PURE__*/ function() {
    "use strict";
    function Breadcrumb(w, ctx, drilldown) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Breadcrumb);
        this.w = w;
        this.ctx = ctx;
        this.drilldown = drilldown;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Breadcrumb, [
        {
            /**
   * @param {Array<string|number>} path - ['root', id, id, ...]
   */ key: "render",
            value: function render(path) {
                var _this = this;
                if (!Environment.isBrowser()) return;
                var w = this.w;
                var elWrap = w.dom.elWrap;
                if (!elWrap) return;
                var existing = elWrap.querySelector(".apexcharts-breadcrumb");
                if (existing && existing.parentNode) {
                    existing.parentNode.removeChild(existing);
                }
                var cfg = w.config.drilldown && w.config.drilldown.breadcrumb;
                if (!cfg || cfg.show === false) return;
                if (this.drilldown.depth === 0) return;
                var nav = BrowserAPIs.createElementNS(XHTML$1, "nav");
                nav.setAttribute("class", "apexcharts-breadcrumb");
                nav.setAttribute("aria-label", "Drilldown breadcrumb");
                this._position(nav, cfg);
                var separator = cfg.separator != null ? cfg.separator : " / ";
                path.forEach(function(id, i) {
                    if (i > 0) {
                        var sep = BrowserAPIs.createElementNS(XHTML$1, "span");
                        sep.setAttribute("class", "apexcharts-breadcrumb-separator");
                        sep.setAttribute("aria-hidden", "true");
                        sep.textContent = separator;
                        nav.appendChild(sep);
                    }
                    var label = _this._label(id, i);
                    var isCurrent = i === path.length - 1;
                    if (isCurrent) {
                        var cur = BrowserAPIs.createElementNS(XHTML$1, "span");
                        cur.setAttribute("class", "apexcharts-breadcrumb-item apexcharts-breadcrumb-current");
                        cur.setAttribute("aria-current", "page");
                        cur.textContent = label;
                        nav.appendChild(cur);
                    } else {
                        var btn = /** @type {HTMLButtonElement} */ BrowserAPIs.createElementNS(XHTML$1, "button");
                        btn.setAttribute("type", "button");
                        btn.setAttribute("class", "apexcharts-breadcrumb-item");
                        if (i === 0) {
                            var arrow = BrowserAPIs.createElementNS(XHTML$1, "span");
                            arrow.setAttribute("class", "apexcharts-breadcrumb-arrow");
                            arrow.setAttribute("aria-hidden", "true");
                            arrow.textContent = "←";
                            btn.appendChild(arrow);
                        }
                        var text = BrowserAPIs.createElementNS(XHTML$1, "span");
                        text.setAttribute("class", "apexcharts-breadcrumb-label");
                        text.textContent = label;
                        btn.appendChild(text);
                        btn.addEventListener("click", function() {
                            return _this.drilldown.drillToLevel(i);
                        });
                        nav.appendChild(btn);
                    }
                });
                elWrap.appendChild(nav);
                this._avoidChromeOverlap(nav);
            }
        },
        {
            /**
   * The breadcrumb is an absolute overlay, so at its default top-left it can
   * sit on top of a left-aligned title (or subtitle). After mounting, push it
   * below any chart chrome it intersects. (Sunburst's self-contained
   * breadcrumb applies the same rule.)
   * @param {HTMLElement} nav
   */ key: "_avoidChromeOverlap",
            value: function _avoidChromeOverlap(nav) {
                var _loop = function(pass) {
                    var nr = nav.getBoundingClientRect();
                    var hit = chrome.find(function(el) {
                        var r = el.getBoundingClientRect();
                        return nr.left < r.right && nr.right > r.left && nr.top < r.bottom && nr.bottom > r.top;
                    });
                    if (!hit) return "break";
                    nav.style.top = "".concat(hit.getBoundingClientRect().bottom - wrapTop + 4, "px");
                };
                var w = this.w;
                var chrome = /** @type {Element[]} */ [
                    ".apexcharts-title-text",
                    ".apexcharts-subtitle-text"
                ].map(function(s) {
                    return w.dom.baseEl.querySelector(s);
                }).filter(function(el) {
                    return el !== null;
                });
                if (!chrome.length) return;
                var wrapTop = w.dom.elWrap.getBoundingClientRect().top;
                for(var pass = 0; pass < chrome.length + 1; pass++){
                    var _ret = _loop(pass);
                    if (_ret === "break") break;
                }
            }
        },
        {
            /**
   * @param {string|number} id
   * @param {number} index
   * @returns {string}
   */ key: "_label",
            value: function _label(id, index) {
                var cfg = this.w.config.drilldown.breadcrumb;
                var label;
                if (index === 0) {
                    label = cfg.rootLabel != null ? cfg.rootLabel : "All";
                } else {
                    var list = (this.w.config.drilldown.series || []).find(function(s) {
                        return s && s.id === id;
                    });
                    label = list && list.name || String(id);
                }
                if (typeof cfg.formatter === "function") {
                    return cfg.formatter(label, {
                        index: index,
                        depth: this.drilldown.depth
                    });
                }
                return label;
            }
        },
        {
            /**
   * @param {HTMLElement} nav
   * @param {Record<string, any>} cfg
   */ key: "_position",
            value: function _position(nav, cfg) {
                var ox = cfg.offsetX || 0;
                var oy = cfg.offsetY || 0;
                nav.style.position = "absolute";
                nav.style.top = oy + "px";
                if (cfg.position === "top-right") {
                    nav.style.right = -ox + 3 + "px";
                } else {
                    nav.style.left = ox + "px";
                }
            }
        }
    ]);
    return Breadcrumb;
}();
var MAX_DEPTH = 32;
var Drilldown = /*#__PURE__*/ function() {
    "use strict";
    function Drilldown(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Drilldown);
        this.w = w;
        this.ctx = ctx;
        this.stack = [];
        this.rootSnapshot = null;
        this._wired = false;
        this.breadcrumb = new Breadcrumb(w, ctx, this);
        this._onPointSelect = this._onPointSelect.bind(this);
        this._afterRender = this._afterRender.bind(this);
        this.init();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Drilldown, [
        {
            key: "init",
            value: function init() {
                var w = this.w;
                if (!w.config.drilldown || !w.config.drilldown.enabled) return;
                if (this._wired) return;
                this._wired = true;
                this.ctx.addEventListener("dataPointSelection", this._onPointSelect);
                this.ctx.addEventListener("mounted", this._afterRender);
                this.ctx.addEventListener("updated", this._afterRender);
            }
        },
        {
            key: "path",
            get: // ─── Observable state ──────────────────────────────────────────────────────
            /** @returns {Array<string|number>} e.g. ['root', '2024-quarters'] */ function get() {
                return [
                    "root"
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this.stack.map(function(f) {
                    return f.id;
                })));
            }
        },
        {
            key: "depth",
            get: /** @returns {number} 0 at root */ function get() {
                return this.stack.length;
            }
        },
        {
            // ─── Navigation API ────────────────────────────────────────────────────────
            /**
   * Drill into the child level with the given id.
   * @param {string|number} id
   * @param {any} [triggerPoint] - the clicked data point (for events / async ctx)
   * @param {{ seriesIndex?: number, dataPointIndex?: number }} [meta]
   * @returns {Promise<any>}
   */ key: "drillDown",
            value: function drillDown(id, triggerPoint, meta) {
                var child = this._resolveChild(id);
                if (child) return this._drillInto(child, triggerPoint, meta);
                if (typeof this.w.config.drilldown.onDrillDown === "function") {
                    return this._drillDownAsync(id, triggerPoint, meta);
                }
                console.warn('ApexCharts: drilldown id "'.concat(id, '" not found in chart.drilldown.series, and no onDrillDown resolver is set.'));
                return Promise.resolve(this.ctx);
            }
        },
        {
            /**
   * Navigate back one level.
   * @returns {Promise<any>}
   */ key: "drillUp",
            value: function drillUp() {
                return this.drillToLevel(this.stack.length - 1);
            }
        },
        {
            /**
   * Navigate back to the root view.
   * @returns {Promise<any>}
   */ key: "drillToRoot",
            value: function drillToRoot() {
                return this.drillToLevel(0);
            }
        },
        {
            /**
   * Navigate to an arbitrary depth (0 = root). Used by breadcrumb clicks.
   * @param {number} targetDepth
   * @returns {Promise<any>}
   */ key: "drillToLevel",
            value: function drillToLevel(targetDepth) {
                var cur = this.stack.length;
                if (targetDepth < 0 || targetDepth >= cur) return Promise.resolve(this.ctx);
                var from = this.path[this.path.length - 1];
                var restore = targetDepth === 0 ? this.rootSnapshot : this.stack[targetDepth].restore;
                this.stack = this.stack.slice(0, targetDepth);
                var to = this.path[this.path.length - 1];
                return this._apply(this._viewFromSnapshot(restore), "up", {
                    from: from,
                    to: to
                });
            }
        },
        {
            // ─── Internals ─────────────────────────────────────────────────────────────
            /**
   * @param {string|number} id
   * @returns {any|null}
   */ key: "_resolveChild",
            value: function _resolveChild(id) {
                var list = this.w.config.drilldown && this.w.config.drilldown.series;
                if (!Array.isArray(list)) return null;
                return list.find(function(s) {
                    return s && s.id === id;
                }) || null;
            }
        },
        {
            /**
   * @param {any} child
   * @param {any} [triggerPoint]
   * @param {object} [meta]
   * @returns {Promise<any>}
   */ key: "_drillInto",
            value: function _drillInto(child, triggerPoint, meta) {
                if (this.stack.length >= MAX_DEPTH) {
                    console.warn("ApexCharts: drilldown max depth (".concat(MAX_DEPTH, ") reached."));
                    return Promise.resolve(this.ctx);
                }
                if (!this.rootSnapshot) this.rootSnapshot = this._snapshot();
                var from = this.path[this.path.length - 1];
                this.stack.push({
                    id: child.id,
                    name: child.name,
                    restore: this._snapshot()
                });
                return this._apply(this._viewFromChild(child), "down", {
                    from: from,
                    to: child.id,
                    point: triggerPoint,
                    seriesIndex: meta && meta.seriesIndex,
                    dataPointIndex: meta && meta.dataPointIndex
                });
            }
        },
        {
            /**
   * Minimal async resolver (loading overlay lands in Phase 3).
   * @param {string|number|null} id
   * @param {any} point
   * @param {object} [meta]
   * @returns {Promise<any>}
   */ key: "_drillDownAsync",
            value: function _drillDownAsync(id, point, meta) {
                var _this = this;
                var fn = this.w.config.drilldown.onDrillDown;
                var result;
                try {
                    result = fn({
                        point: point,
                        seriesIndex: meta && meta.seriesIndex,
                        dataPointIndex: meta && meta.dataPointIndex
                    });
                } catch (error) {
                    this._fire("drillDownError", {
                        id: id,
                        error: error
                    });
                    return Promise.resolve(this.ctx);
                }
                return Promise.resolve(result).then(function(child) {
                    if (!child || !child.data) return _this.ctx;
                    return _this._drillInto(child, point, meta);
                }, function(error) {
                    _this._fire("drillDownError", {
                        id: id,
                        error: error
                    });
                    return _this.ctx;
                });
            }
        },
        {
            /**
   * Capture the overridable surface of the current view so it can be restored.
   * Only fields that some drilldown.series entry can change are cloned; series
   * and chart.type/stacked are always captured.
   * @returns {object}
   */ key: "_snapshot",
            value: function _snapshot() {
                var c = this.w.config;
                var fields = this._overrideFields();
                var snap = {
                    series: this._uncollapseSeries(Utils$1.clone(c.series))
                };
                if (Array.isArray(c.labels) && c.labels.length) {
                    snap.labels = Utils$1.clone(c.labels);
                }
                snap.chart = {
                    type: c.chart.type,
                    stacked: c.chart.stacked
                };
                if (fields.has("xaxis")) snap.xaxis = Utils$1.clone(c.xaxis);
                if (fields.has("yaxis")) snap.yaxis = Utils$1.clone(c.yaxis);
                if (fields.has("colors")) snap.colors = c.colors ? Utils$1.clone(c.colors) : void 0;
                if (fields.has("plotOptions")) snap.plotOptions = Utils$1.clone(c.plotOptions);
                if (fields.has("fill")) snap.fill = Utils$1.clone(c.fill);
                if (fields.has("legend")) snap.legend = Utils$1.clone(c.legend);
                return snap;
            }
        },
        {
            /**
   * Restore any legend-collapsed slices/series to their original values in a
   * cloned series array, so a drill snapshot captures the pre-collapse data.
   * Mirrors legend Helpers' collapse addressing: object-form pie/donut packs
   * every slice as a data point inside `series[0].data`; numeric pie stores a
   * slice per top-level element; axis series carry a `data` array. No-op when
   * nothing is collapsed.
   * @param {any[]} series
   * @returns {any[]}
   */ key: "_uncollapseSeries",
            value: function _uncollapseSeries(series) {
                var w = this.w;
                var gl = w.globals;
                var entries = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(gl.collapsedSeries || []).concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(gl.ancillaryCollapsedSeries || []));
                if (!entries.length) return series;
                var type = w.config.chart.type;
                var objectFormPie = (type === "pie" || type === "donut" || type === "polarArea") && series.length === 1 && series[0] && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(series[0]) === "object" && Array.isArray(series[0].data);
                var container = objectFormPie ? series[0].data : series;
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = entries[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var entry = _step.value;
                        var i = entry.index;
                        if (gl.axisCharts) {
                            if (series[i]) {
                                series[i].data = Array.isArray(entry.data) ? entry.data.slice() : entry.data;
                            }
                        } else if (container[i] && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(container[i]) === "object") {
                            container[i].y = entry.data;
                        } else if (container[i] !== void 0) {
                            container[i] = entry.data;
                        }
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                return series;
            }
        },
        {
            /**
   * Union of overridable fields across all declared drilldown levels. Ensures a
   * deep drillToRoot restores everything any intermediate level may have changed.
   * @returns {Set<string>}
   */ key: "_overrideFields",
            value: function _overrideFields() {
                var fields = /* @__PURE__ */ new Set();
                var list = this.w.config.drilldown && this.w.config.drilldown.series || [];
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = list[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var s = _step.value;
                        if (!s) continue;
                        if (s.xaxis) fields.add("xaxis");
                        if (s.yaxis) fields.add("yaxis");
                        if (s.colors) fields.add("colors");
                        if (s.plotOptions) fields.add("plotOptions");
                        if (s.fill) fields.add("fill");
                        if (s.legend) fields.add("legend");
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                return fields;
            }
        },
        {
            /**
   * Copy the optional view fields shared by a drilldown child level and a
   * restore snapshot (`xaxis`, `yaxis`, `colors`, `plotOptions`, `fill`,
   * `legend`) from `src` onto `view`, only when present.
   * @param {Record<string, any>} view @param {Record<string, any>} src
   */ key: "_copyOptionalViewFields",
            value: function _copyOptionalViewFields(view, src) {
                if (src.xaxis) view.xaxis = src.xaxis;
                if (src.yaxis) view.yaxis = src.yaxis;
                if (src.colors) view.colors = src.colors;
                if (src.plotOptions) view.plotOptions = src.plotOptions;
                if (src.fill) view.fill = src.fill;
                if (src.legend) view.legend = src.legend;
            }
        },
        {
            /**
   * Build an updateOptions/updateSeries payload for drilling INTO a child level.
   * Works for axis charts and pie/donut alike: both accept series objects with a
   * `data` array of `{ x, y }` points (pie derives slice labels from `x`).
   * @param {any} child
   * @returns {Record<string, any>}
   */ key: "_viewFromChild",
            value: function _viewFromChild(child) {
                var view = {};
                if (Array.isArray(child.series)) {
                    view.series = child.series;
                } else {
                    view.series = [
                        {
                            name: child.name || "",
                            data: child.data
                        }
                    ];
                }
                var chart = {};
                if (child.chart && child.chart.type) chart.type = child.chart.type;
                if (child.chart && child.chart.stacked != null) chart.stacked = child.chart.stacked;
                if (Object.keys(chart).length) view.chart = chart;
                this._copyOptionalViewFields(view, child);
                return view;
            }
        },
        {
            /**
   * Build an updateOptions payload from a restore-snapshot.
   * @param {Record<string, any>} snap
   * @returns {Record<string, any>}
   */ key: "_viewFromSnapshot",
            value: function _viewFromSnapshot(snap) {
                var view = {
                    series: snap.series,
                    chart: snap.chart
                };
                if (snap.labels && snap.labels.length) view.labels = snap.labels;
                this._copyOptionalViewFields(view, snap);
                return view;
            }
        },
        {
            /**
   * Apply a view by delegating to the right update path, firing drill events
   * around it.
   * @param {Record<string, any>} view
   * @param {'down'|'up'} direction
   * @param {object} meta
   * @returns {Promise<any>}
   */ key: "_apply",
            value: function _apply(view, direction, meta) {
                var _this = this;
                var w = this.w;
                w.interact.selectedDataPoints = [];
                w.globals.collapsedSeries = [];
                w.globals.collapsedSeriesIndices = [];
                w.globals.ancillaryCollapsedSeries = [];
                w.globals.ancillaryCollapsedSeriesIndices = [];
                w.globals.allSeriesCollapsed = false;
                w.globals.risingSeries = [];
                var animate = (!w.config.drilldown.animation || w.config.drilldown.animation.enabled !== false) && w.config.chart.animations.enabled !== false;
                if (direction === "down") this._fire("drillDownStart", meta);
                var runUpdate = function runUpdate(anim) {
                    return _this.ctx.updateOptions(view, false, anim, false, false);
                };
                var done = function done() {
                    _this._fire(direction === "down" ? "drillDownEnd" : "drillUp", meta);
                    return _this.ctx;
                };
                if (animate && this._zoomEnabled()) {
                    var origin = this._triggerOrigin(meta);
                    if (origin) {
                        return this._zoomDrill(origin, direction, function() {
                            return runUpdate(false);
                        }).then(done);
                    }
                }
                return runUpdate(animate).then(done);
            }
        },
        {
            /** @returns {boolean} whether trigger-point zoom is configured on. */ key: "_zoomEnabled",
            value: function _zoomEnabled() {
                var a = this.w.config.drilldown && this.w.config.drilldown.animation;
                return !!(a && a.zoomFromPoint);
            }
        },
        {
            /** @returns {SVGSVGElement|null} the chart's root <svg> node, if present. */ key: "_svgNode",
            value: function _svgNode() {
                var paper = this.w.dom && this.w.dom.Paper;
                return paper && paper.node ? paper.node : null;
            }
        },
        {
            /**
   * The group wrapping ONLY the data marks (bars/cells/tiles) — not the axes,
   * grid, or titles. Animating this keeps the chart frame still while the marks
   * move. Covers bar/line/area (`.apexcharts-plot-series`), heatmap, and treemap.
   * @returns {SVGElement|null}
   */ key: "_markGroup",
            value: function _markGroup() {
                var svg = this._svgNode();
                if (!svg || typeof svg.querySelector !== "function") return null;
                return svg.querySelector(".apexcharts-plot-series, .apexcharts-heatmap, .apexcharts-treemap");
            }
        },
        {
            /**
   * Centre of the clicked point in the SVG's view-box pixel space, used as the
   * transform-origin for the mark-group scale (which uses `transform-box:
   * view-box`, so the origin is resolved in SVG coordinates and stays stable
   * across the parent and child renders). Falls back to the mark group's centre
   * when there is no trigger point (e.g. drillUp / imperative drill). Returns
   * null when the marks / SVG / WAAPI are unavailable (SSR / old browsers).
   * @param {object} meta
   * @returns {{ x: number, y: number }|null}
   */ key: "_triggerOrigin",
            value: function _triggerOrigin(meta) {
                if (!Environment.isBrowser()) return null;
                var svg = this._svgNode();
                var group = this._markGroup();
                if (!svg || !group || typeof group.animate !== "function" || typeof svg.getBoundingClientRect !== "function") {
                    return null;
                }
                var svgRect = svg.getBoundingClientRect();
                var el = null;
                if (meta && meta.seriesIndex != null && meta.dataPointIndex != null && this.w.dom.baseEl) {
                    el = this.w.dom.baseEl.querySelector('[index="'.concat(meta.seriesIndex, '"][j="').concat(meta.dataPointIndex, '"]'));
                }
                if (el && typeof el.getBoundingClientRect === "function") {
                    var r = el.getBoundingClientRect();
                    return {
                        x: r.left + r.width / 2 - svgRect.left,
                        y: r.top + r.height / 2 - svgRect.top
                    };
                }
                var gRect = group.getBoundingClientRect();
                return {
                    x: gRect.left + gRect.width / 2 - svgRect.left,
                    y: gRect.top + gRect.height / 2 - svgRect.top
                };
            }
        },
        {
            /**
   * Run the "expand from the clicked point" choreography around an instant
   * (un-animated) update. Only the data-mark group is animated — the axes, grid,
   * and titles stay fixed, so the effect doesn't drag the whole chart frame. The
   * current marks fade out near-in-place (a quick fade, not a balloon), the child
   * renders invisibly underneath, then the child marks unfold outward from the
   * clicked point: a horizontal-biased scale anchored there, so the bars read as
   * emerging from the column you clicked. Drilling up has no trigger column, so
   * it settles gently from the marks' centre.
   *
   * `transform-box: view-box` resolves the origin in SVG coordinates, so the same
   * origin applies cleanly to the parent and the freshly-rendered child group.
   * @param {{ x: number, y: number }} origin
   * @param {'down'|'up'} direction
   * @param {() => Promise<any>} runUpdate
   * @returns {Promise<void>}
   */ key: "_zoomDrill",
            value: function _zoomDrill(origin, direction, runUpdate) {
                return __async(this, null, function() {
                    var dur, down, outDur, outTo, inFrom, anchor, clear, outGroup, outAnim, e, inGroup, inAnim, e1;
                    return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$tslib$2f$tslib$2e$es6$2e$mjs__$5b$app$2d$client$5d$__$28$ecmascript$29$__$3c$export__$5f$_generator__as__$5f3e$__["_"])(this, function(_state) {
                        switch(_state.label){
                            case 0:
                                dur = this._zoomDuration();
                                down = direction === "down";
                                outDur = Math.round(dur * 0.55);
                                outTo = down ? "scale(1.03)" : "scale(0.97)";
                                inFrom = down ? "scaleX(0.55) scaleY(0.85)" : "scale(1.04)";
                                anchor = function anchor(el) {
                                    el.style.transformBox = "view-box";
                                    el.style.transformOrigin = "".concat(origin.x, "px ").concat(origin.y, "px");
                                };
                                clear = function clear(el) {
                                    el.style.transform = "";
                                    el.style.opacity = "";
                                    el.style.transformOrigin = "";
                                    el.style.transformBox = "";
                                };
                                outGroup = this._markGroup();
                                outAnim = null;
                                if (!outGroup) return [
                                    3,
                                    4
                                ];
                                anchor(outGroup);
                                outAnim = outGroup.animate([
                                    {
                                        transform: "scale(1)",
                                        opacity: 1
                                    },
                                    {
                                        transform: outTo,
                                        opacity: 0
                                    }
                                ], {
                                    duration: outDur,
                                    easing: "ease-in",
                                    fill: "forwards"
                                });
                                _state.label = 1;
                            case 1:
                                _state.trys.push([
                                    1,
                                    3,
                                    ,
                                    4
                                ]);
                                return [
                                    4,
                                    outAnim.finished
                                ];
                            case 2:
                                _state.sent();
                                return [
                                    3,
                                    4
                                ];
                            case 3:
                                e = _state.sent();
                                return [
                                    3,
                                    4
                                ];
                            case 4:
                                return [
                                    4,
                                    runUpdate()
                                ];
                            case 5:
                                _state.sent();
                                inGroup = this._markGroup();
                                if (!inGroup) return [
                                    3,
                                    10
                                ];
                                anchor(inGroup);
                                inGroup.style.opacity = "0";
                                inGroup.style.transform = inFrom;
                                if (outAnim && outGroup === inGroup) outAnim.cancel();
                                inAnim = inGroup.animate([
                                    {
                                        transform: inFrom,
                                        opacity: 0
                                    },
                                    {
                                        transform: "scale(1)",
                                        opacity: 1
                                    }
                                ], // Decelerating ease so the unfold settles softly into place.
                                {
                                    duration: dur,
                                    easing: "cubic-bezier(0.16, 1, 0.3, 1)",
                                    fill: "forwards"
                                });
                                _state.label = 6;
                            case 6:
                                _state.trys.push([
                                    6,
                                    8,
                                    ,
                                    9
                                ]);
                                return [
                                    4,
                                    inAnim.finished
                                ];
                            case 7:
                                _state.sent();
                                return [
                                    3,
                                    9
                                ];
                            case 8:
                                e1 = _state.sent();
                                return [
                                    3,
                                    9
                                ];
                            case 9:
                                clear(inGroup);
                                inAnim.cancel();
                                _state.label = 10;
                            case 10:
                                return [
                                    2
                                ];
                        }
                    });
                });
            }
        },
        {
            /** @returns {number} per-phase zoom duration in ms. */ key: "_zoomDuration",
            value: function _zoomDuration() {
                var a = this.w.config.drilldown && this.w.config.drilldown.animation;
                var speed = a && typeof a.speed === "number" ? a.speed : 260;
                return Math.max(80, speed);
            }
        },
        {
            /**
   * Fire a drill event through both the config callback and the listener registry.
   * @param {string} name
   * @param {object} payload
   */ key: "_fire",
            value: function _fire(name2, payload) {
                var cb = this.w.config.chart.events && this.w.config.chart.events[name2];
                if (typeof cb === "function") cb(payload, this.ctx, this.w);
                this.ctx.events.fireEvent(name2, [
                    payload,
                    this.ctx,
                    this.w
                ]);
            }
        },
        {
            // ─── Click + post-render hooks ───────────────────────────────────────────────
            /**
   * @param {Event} _event
   * @param {any} _ctx
   * @param {{ seriesIndex?: number, dataPointIndex?: number }} opts
   */ key: "_onPointSelect",
            value: function _onPointSelect(_event, _ctx, opts) {
                if (!opts) return void 0;
                var point = this._pointAt(opts.seriesIndex, opts.dataPointIndex);
                if (point && (typeof point === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(point)) === "object" && point.drilldown != null) {
                    return this.drillDown(point.drilldown, point, opts);
                }
                if (typeof this.w.config.drilldown.onDrillDown === "function") {
                    return this._drillDownAsync(null, point, opts);
                }
                return void 0;
            }
        },
        {
            /**
   * @param {number|undefined} seriesIndex
   * @param {number|undefined} dataPointIndex
   * @returns {any|null}
   */ key: "_pointAt",
            value: function _pointAt(seriesIndex, dataPointIndex) {
                var series = this.w.config.series;
                if (!Array.isArray(series) || seriesIndex == null || dataPointIndex == null) {
                    return null;
                }
                var s = series[seriesIndex];
                if (!s || !Array.isArray(s.data)) return null;
                return s.data[dataPointIndex] != null ? s.data[dataPointIndex] : null;
            }
        },
        {
            key: "_afterRender",
            value: function _afterRender() {
                if (!this.w.config.drilldown || !this.w.config.drilldown.enabled) return;
                this._markDrillableTargets();
                this.breadcrumb.render(this.path);
            }
        },
        {
            /**
   * Add the drilldown-target cursor class to every point that carries a
   * `drilldown` field. Best-effort and cosmetic — a missed selector is harmless.
   */ key: "_markDrillableTargets",
            value: function _markDrillableTargets() {
                if (!Environment.isBrowser()) return;
                var w = this.w;
                var baseEl = w.dom.baseEl;
                var series = w.config.series;
                if (!baseEl || !Array.isArray(series)) return;
                series.forEach(function(s, i) {
                    var data = s && Array.isArray(s.data) ? s.data : null;
                    if (!data) return;
                    data.forEach(function(point, j) {
                        if (!point || (typeof point === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(point)) !== "object" || point.drilldown == null) return;
                        var nodes = baseEl.querySelectorAll('[index="'.concat(i, '"][j="').concat(j, '"]'));
                        nodes.forEach(function(node) {
                            return node.classList.add("apexcharts-drilldown-target");
                        });
                    });
                });
            }
        }
    ]);
    return Drilldown;
}();
ApexCharts.registerFeatures({
    drilldown: Drilldown
});
var VIEWSTATE_VERSION = 1;
function axisWindow(min, max) {
    var hasMin = min !== void 0 && min !== null;
    var hasMax = max !== void 0 && max !== null;
    if (!hasMin && !hasMax) return null;
    return {
        min: hasMin ? min : null,
        max: hasMax ? max : null
    };
}
function cloneSelection(sel) {
    if (!Array.isArray(sel)) return [];
    return sel.map(function(a) {
        return Array.isArray(a) ? a.slice() : a;
    });
}
function annotationKind(method, ctx) {
    if (typeof method !== "function" || !ctx) return null;
    if (method === ctx.addXaxisAnnotation) return "xaxis";
    if (method === ctx.addYaxisAnnotation) return "yaxis";
    if (method === ctx.addPointAnnotation) return "point";
    return null;
}
function addMethodName(kind) {
    switch(kind){
        case "xaxis":
            return "addXaxisAnnotation";
        case "yaxis":
            return "addYaxisAnnotation";
        case "point":
            return "addPointAnnotation";
        default:
            return null;
    }
}
function captureAnnotations(w, ctx) {
    var staticAnno = w.config.annotations ? Utils$1.clone(w.config.annotations) : null;
    var dynamic = [];
    var mem = w.globals.memory && w.globals.memory.methodsToExec || [];
    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
    try {
        for(var _iterator = mem[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
            var entry = _step.value;
            if (!entry || entry.label !== "addAnnotation") continue;
            var kind = annotationKind(entry.method, ctx);
            if (!kind) continue;
            dynamic.push({
                kind: kind,
                params: Utils$1.clone(entry.params)
            });
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally{
        try {
            if (!_iteratorNormalCompletion && _iterator.return != null) {
                _iterator.return();
            }
        } finally{
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }
    return {
        static: staticAnno,
        dynamic: dynamic
    };
}
function captureMeasure(ctx) {
    var m = ctx && ctx.measure;
    if (!m || typeof m.getPins !== "function") return null;
    var pins = m.getPins();
    return Array.isArray(pins) && pins.length ? {
        pins: pins
    } : null;
}
function captureViewState(w, ctx) {
    var _a, _b;
    var cfgX = w.config.xaxis || {};
    var cfgYArr = Array.isArray(w.config.yaxis) ? w.config.yaxis : w.config.yaxis ? [
        w.config.yaxis
    ] : [];
    var yWindows = cfgYArr.map(function(y) {
        return axisWindow(y && y.min, y && y.max);
    });
    var anyY = yWindows.some(function(yw) {
        return yw !== null;
    });
    var theme = w.config.theme;
    var drilldown = ctx && ctx.drilldown;
    return {
        v: VIEWSTATE_VERSION,
        window: {
            xaxis: axisWindow(cfgX.min, cfgX.max),
            yaxis: anyY ? yWindows : null
        },
        zoomed: !!w.interact.zoomed,
        collapsed: (w.globals.collapsedSeriesIndices || []).slice(),
        ancillaryCollapsed: (w.globals.ancillaryCollapsedSeriesIndices || []).slice(),
        selectedDataPoints: cloneSelection(w.interact.selectedDataPoints),
        theme: theme ? {
            mode: (_a = theme.mode) != null ? _a : null,
            palette: (_b = theme.palette) != null ? _b : null
        } : null,
        locale: w.config.chart && w.config.chart.defaultLocale || null,
        annotations: captureAnnotations(w, ctx),
        drill: drilldown && drilldown.depth > 0 ? {
            path: drilldown.path.slice()
        } : null,
        measure: captureMeasure(ctx)
    };
}
function applyCollapsedSet(ctx, targetCollapsed, targetAncillary) {
    var w = ctx.w;
    if (targetCollapsed == null && targetAncillary == null) return;
    var names = w.globals.seriesNames || [];
    var target = /* @__PURE__ */ new Set((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(targetCollapsed || []).concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(targetAncillary || [])));
    var current = /* @__PURE__ */ new Set((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.globals.collapsedSeriesIndices || []).concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(w.globals.ancillaryCollapsedSeriesIndices || [])));
    for(var realIndex = 0; realIndex < names.length; realIndex++){
        var name2 = names[realIndex];
        if (name2 == null) continue;
        var shouldCollapse = target.has(realIndex);
        var isCollapsed = current.has(realIndex);
        if (shouldCollapse && !isCollapsed) {
            ctx.hideSeries(name2);
        } else if (!shouldCollapse && isCollapsed) {
            ctx.showSeries(name2);
        }
    }
}
function restoreSelection(ctx, selectedDataPoints) {
    if (!Array.isArray(selectedDataPoints)) return;
    ctx.w.interact.selectedDataPoints = cloneSelection(selectedDataPoints);
}
function applyViewState(ctx, view) {
    var _ref = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, _ref_animate = _ref.animate, animate = _ref_animate === void 0 ? true : _ref_animate, mergeOptions = _ref.mergeOptions;
    var _a, _b;
    if (!ctx || !view) return;
    if (typeof view.v === "number" && view.v > VIEWSTATE_VERSION) {
        console.warn("[apexcharts] ViewState v".concat(view.v, " is newer than this build understands (v").concat(VIEWSTATE_VERSION, "); applying best-effort."));
    }
    ctx.clearAnnotations();
    var options2 = mergeOptions ? Utils$1.clone(mergeOptions) : {};
    var xw = view.window && view.window.xaxis;
    options2.xaxis = Object.assign({}, options2.xaxis, xw ? {
        min: (_a = xw.min) != null ? _a : void 0,
        max: (_b = xw.max) != null ? _b : void 0
    } : {
        min: void 0,
        max: void 0
    });
    var yw = view.window && view.window.yaxis;
    if (Array.isArray(yw)) {
        options2.yaxis = yw.map(function(y) {
            var _a2, _b2;
            return y ? {
                min: (_a2 = y.min) != null ? _a2 : void 0,
                max: (_b2 = y.max) != null ? _b2 : void 0
            } : {};
        });
    }
    if (view.theme) {
        var theme = {};
        if (view.theme.mode != null) theme.mode = view.theme.mode;
        if (view.theme.palette != null) theme.palette = view.theme.palette;
        if (Object.keys(theme).length) {
            options2.theme = Object.assign({}, options2.theme, theme);
        }
    }
    if (view.annotations && view.annotations.static) {
        options2.annotations = Utils$1.clone(view.annotations.static);
    }
    ctx.updateOptions(options2, false, animate, false, false);
    applyViewInteraction(ctx, view);
}
function applyViewInteraction(ctx, view) {
    if (!ctx || !view) return;
    var w = ctx.w;
    w.interact.zoomed = !!view.zoomed;
    applyCollapsedSet(ctx, view.collapsed, view.ancillaryCollapsed);
    if (view.annotations && Array.isArray(view.annotations.dynamic)) {
        view.annotations.dynamic.forEach(function(a) {
            var method = addMethodName(a.kind);
            if (method && typeof ctx[method] === "function") {
                ctx[method](a.params, true);
            }
        });
    }
    restoreSelection(ctx, view.selectedDataPoints);
    if (view.locale && view.locale !== w.config.chart.defaultLocale) {
        ctx.setLocale(view.locale);
    }
    if (ctx.measure && typeof ctx.measure.setPins === "function") {
        ctx.measure.setPins(view.measure && view.measure.pins || []);
    }
}
var PERSPECTIVE_VERSION = 1;
var HASH_KEY = "apex";
function toBase64(str) {
    if (typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"] !== "undefined") {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(str, "utf-8").toString("base64");
    }
    var bytes = new TextEncoder().encode(str);
    var bin = "";
    for(var i = 0; i < bytes.length; i++)bin += String.fromCharCode(bytes[i]);
    return btoa(bin);
}
function fromBase64(b64) {
    if (typeof __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"] !== "undefined") {
        return __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$buffer$2f$index$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["Buffer"].from(b64, "base64").toString("utf-8");
    }
    var bin = atob(b64);
    var bytes = new Uint8Array(bin.length);
    for(var i = 0; i < bin.length; i++)bytes[i] = bin.charCodeAt(i);
    return new TextDecoder().decode(bytes);
}
function base64urlEncode(str) {
    return toBase64(str).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function base64urlDecode(b64url) {
    var b64 = b64url.replace(/-/g, "+").replace(/_/g, "/");
    while(b64.length % 4)b64 += "=";
    return fromBase64(b64);
}
function stripFunctions(obj) {
    try {
        return JSON.parse(JSON.stringify(obj));
    } catch (e) {
        return void 0;
    }
}
var Perspectives = /*#__PURE__*/ function() {
    "use strict";
    function Perspectives(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Perspectives);
        this.w = w;
        this.ctx = ctx;
        this._saved = [];
        this._counter = 0;
        this._used = false;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Perspectives, [
        {
            /**
   * Capture the current chart view as a Perspective token.
   * @returns {{ v: number, view: object, options?: Record<string, any> }}
   */ key: "capture",
            value: function capture() {
                var view = captureViewState(this.w, this.ctx);
                var token = /** @type {any} */ {
                    v: PERSPECTIVE_VERSION,
                    view: view
                };
                var options2 = this._serializableDelta();
                if (options2 && Object.keys(options2).length) token.options = options2;
                return token;
            }
        },
        {
            /**
   * Build the whitelisted, function-free option override recorded in the token.
   * @returns {Record<string, any>}
   * @private
   */ key: "_serializableDelta",
            value: function _serializableDelta() {
                var cfg = this.w.config;
                var whitelist = cfg.chart && cfg.chart.perspectives && cfg.chart.perspectives.serializeOptions || [
                    "theme",
                    "xaxis",
                    "yaxis",
                    "title",
                    "subtitle"
                ];
                var delta = {};
                whitelist.forEach(function(path) {
                    if (cfg[path] !== void 0) {
                        var stripped = stripFunctions(cfg[path]);
                        if (stripped !== void 0) delta[path] = stripped;
                    }
                });
                return delta;
            }
        },
        {
            /**
   * Encode a token (or the current capture) to a compact base64url string.
   * JSON.stringify drops any functions embedded in annotation params / option
   * overrides by construction: a shared link carries data; the opening page
   * supplies its own functions from config.
   * @param {any} [token]
   * @returns {string}
   */ key: "encode",
            value: function encode(token) {
                var t = token || this.capture();
                return base64urlEncode(JSON.stringify(t));
            }
        },
        {
            /**
   * Decode a base64url token string. Never throws: returns null on any error
   * or version mismatch (with a console warning).
   * @param {string} str
   * @returns {any | null}
   */ key: "decode",
            value: function decode(str) {
                return Perspectives.decode(str);
            }
        },
        {
            /**
   * Encode the current capture into a `#apex=<token>` URL hash fragment on the
   * current location. Browser-only; returns '' under SSR.
   * @returns {string}
   */ key: "toURL",
            value: function toURL() {
                if (!Environment.isBrowser()) return "";
                var encoded = this.encode(this.capture());
                var url = new URL(window.location.href);
                url.hash = "".concat(HASH_KEY, "=").concat(encoded);
                return url.toString();
            }
        },
        {
            /**
   * Restore a perspective. Accepts a token object or an encoded string. When
   * the chart is grouped, applies to every synced chart.
   *
   * The token's option overrides and `opts.mergeOptions` are folded into the
   * view restore's ONE updateOptions call (mergeOptions wins over
   * token.options; the view's own fields win over both). A single render is
   * deliberate: a second immediate updateOptions would kill the first one's
   * animation mid-flight, and a chart.type change applied this way morphs
   * (morph feature) inside the same re-render instead of being re-rendered
   * over. Consumed by Storyboard for per-beat option payloads.
   *
   * @param {any} tokenOrString
   * @param {{ animate?: boolean, mergeOptions?: Record<string, any> }} [opts]
   */ key: "apply",
            value: function apply(tokenOrString) {
                var opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
                var token = typeof tokenOrString === "string" ? Perspectives.decode(tokenOrString) : tokenOrString;
                if (!token || !token.view) return;
                this._used = true;
                enforceLicense(this.w, this.ctx);
                var animate = opts.animate !== void 0 ? opts.animate : true;
                var combined = Utils$1.extend(token.options ? Utils$1.clone(token.options) : {}, opts.mergeOptions || {});
                var mergeOptions = Object.keys(combined).length ? combined : void 0;
                var targets = this.w.config.chart.group ? this.ctx.getSyncedCharts() : [
                    this.ctx
                ];
                targets.forEach(function(chart) {
                    applyViewState(chart, token.view, {
                        animate: animate,
                        mergeOptions: mergeOptions
                    });
                });
            }
        },
        {
            /**
   * Save the current view under a name in the in-memory registry.
   * @param {string} name
   * @returns {string} generated id
   */ key: "save",
            value: function save(name2) {
                var id = "perspective-".concat(++this._counter);
                this._saved.push({
                    id: id,
                    name: name2 || id,
                    token: this.capture()
                });
                this._used = true;
                enforceLicense(this.w, this.ctx);
                return id;
            }
        },
        {
            /**
   * List saved perspectives.
   * @returns {{ id: string, name: string, token: any }[]}
   */ key: "list",
            value: function list() {
                return this._saved.map(function(s) {
                    return {
                        id: s.id,
                        name: s.name,
                        token: s.token
                    };
                });
            }
        },
        {
            /**
   * Delete a saved perspective by id.
   * @param {string} id
   */ key: "delete",
            value: function _delete(id) {
                var i = this._saved.findIndex(function(s) {
                    return s.id === id;
                });
                if (i > -1) this._saved.splice(i, 1);
            }
        },
        {
            /** Drop the saved-views registry (called on full destroy). */ key: "teardown",
            value: function teardown() {
                this._saved = [];
                this._counter = 0;
            }
        }
    ], [
        {
            key: "decode",
            value: // ── static, pure helpers (available once the feature is imported) ─────────
            /**
   * @param {string} str base64url token
   * @returns {any | null}
   */ function decode(str) {
                if (typeof str !== "string" || !str) return null;
                try {
                    var token = JSON.parse(base64urlDecode(str));
                    if (!token || (typeof token === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(token)) !== "object") return null;
                    if (token.v !== PERSPECTIVE_VERSION) {
                        console.warn("apexcharts: unsupported perspective version ".concat(token.v, " (expected ").concat(PERSPECTIVE_VERSION, ")."));
                        return null;
                    }
                    return token;
                } catch (e) {
                    console.warn("apexcharts: failed to decode perspective token.", e);
                    return null;
                }
            }
        },
        {
            key: "fromURL",
            value: /**
   * Parse a `#apex=<token>` fragment out of an href (or the current location in
   * a browser). Pure and Node-safe when given an explicit href.
   * @param {string} [href]
   * @returns {any | null}
   */ function fromURL(href) {
                try {
                    var target = href || (Environment.isBrowser() ? window.location.href : "");
                    if (!target) return null;
                    var url = new URL(target);
                    var hash = url.hash.replace(/^#/, "");
                    if (!hash) return null;
                    var pair = hash.split("&").map(function(p) {
                        return p.split("=");
                    }).find(function(p) {
                        return p[0] === HASH_KEY;
                    });
                    if (!pair || pair[1] == null) return null;
                    return Perspectives.decode(decodeURIComponent(pair[1]));
                } catch (e) {
                    return null;
                }
            }
        }
    ]);
    return Perspectives;
}();
ApexCharts.registerFeatures({
    perspectives: Perspectives
});
ApexCharts.perspectives = {
    /** @param {string} str */ decode: function decode(str) {
        markPerspectivesTokenDecoded();
        return Perspectives.decode(str);
    },
    /** @param {string} [href] */ fromURL: function fromURL(href) {
        markPerspectivesTokenDecoded();
        return Perspectives.fromURL(href);
    }
};
var Storyboard = /*#__PURE__*/ function() {
    "use strict";
    function Storyboard(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Storyboard);
        this.w = w;
        this.ctx = ctx;
        this._beats = [];
        this._observer = null;
        this._activeIndex = -1;
        this._animate = true;
        this._warnedNoPerspectives = false;
        this._used = false;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Storyboard, [
        {
            /**
   * Bind beats to scroll position. Rebinding replaces the previous binding.
   * @param {{
   *   beats?: Array<{ el?: Element, selector?: string, key?: string, view?: any, announce?: string, onEnter?: (chart: any, info: StoryboardBeatInfo) => void }>,
   *   scroller?: Element | string,
   *   offset?: number,
   *   animate?: boolean,
   * }} [opts]
   * @returns {number} the number of beats bound
   */ key: "bind",
            value: function bind() {
                var _this = this;
                var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                var _a;
                this.unbind();
                if (!Environment.isBrowser()) return 0;
                var doc = this.ctx.el && this.ctx.el.ownerDocument;
                if (!doc || typeof IntersectionObserver === "undefined") return 0;
                var root = null;
                if (opts.scroller) {
                    root = typeof opts.scroller === "string" ? doc.querySelector(opts.scroller) : opts.scroller;
                }
                this._beats = this._resolveBeats(doc, root, opts.beats);
                if (!this._beats.length) return 0;
                this._animate = opts.animate !== false;
                var offset = Math.min(Math.max((_a = opts.offset) != null ? _a : 0.5, 0), 1);
                var top = +(offset * 100).toFixed(3);
                var bottom = +(100 - offset * 100).toFixed(3);
                this._observer = new IntersectionObserver(function(entries) {
                    return _this._onIntersect(entries);
                }, {
                    root: root,
                    rootMargin: "-".concat(top, "% 0px -").concat(bottom, "%"),
                    threshold: 0
                });
                this._beats.forEach(function(b) {
                    var _a2;
                    return (_a2 = _this._observer) == null ? void 0 : _a2.observe(b.el);
                });
                this._used = true;
                enforceLicense(this.w, this.ctx);
                return this._beats.length;
            }
        },
        {
            /**
   * Normalize the beats option, or auto-discover [data-apex-beat] elements in
   * document order when no explicit list is given.
   * @param {Document} doc
   * @param {Element | null} root
   * @param {Array<any>} [beatsOpt]
   * @returns {StoryboardBeat[]}
   * @private
   */ key: "_resolveBeats",
            value: function _resolveBeats(doc, root, beatsOpt) {
                var beats = [];
                if (Array.isArray(beatsOpt)) {
                    beatsOpt.forEach(function(b, i) {
                        var _a, _b;
                        if (!b) return;
                        var el = b.el && (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(b.el) === "object" ? b.el : b.selector ? doc.querySelector(b.selector) : null;
                        if (!el) {
                            console.warn("apexcharts: storyboard beat ".concat(i, " has no resolvable element; skipped."));
                            return;
                        }
                        beats.push({
                            el: el,
                            key: (_b = (_a = b.key) != null ? _a : el.getAttribute("data-apex-beat")) != null ? _b : String(i),
                            view: b.view,
                            options: b.options,
                            announce: b.announce,
                            onEnter: typeof b.onEnter === "function" ? b.onEnter : void 0
                        });
                    });
                    return beats;
                }
                var scope = root || doc;
                scope.querySelectorAll("[data-apex-beat]").forEach(function(el, i) {
                    beats.push({
                        el: el,
                        key: el.getAttribute("data-apex-beat") || String(i),
                        view: el.getAttribute("data-apex-view") || void 0,
                        options: void 0,
                        announce: el.getAttribute("data-apex-announce") || void 0,
                        onEnter: void 0
                    });
                });
                return beats;
            }
        },
        {
            /**
   * @param {IntersectionObserverEntry[]} entries
   * @private
   */ key: "_onIntersect",
            value: function _onIntersect(entries) {
                var _this = this;
                entries.forEach(function(entry) {
                    var idx = _this._beats.findIndex(function(b) {
                        return b.el === entry.target;
                    });
                    if (idx < 0) return;
                    if (entry.isIntersecting) {
                        _this._activate(idx);
                    } else if (idx === _this._activeIndex && entry.rootBounds) {
                        if (entry.boundingClientRect.top >= entry.rootBounds.bottom && idx > 0) {
                            _this._activate(idx - 1);
                        }
                    }
                });
            }
        },
        {
            /**
   * Activate a beat: apply its view token, run its callback, announce it and
   * fire `beatChange`. Idempotent per beat (re-activating the current beat is
   * a no-op), so IO chatter never re-applies a view.
   * @param {number} idx
   * @param {{ animate?: boolean }} [opts]
   * @private
   */ key: "_activate",
            value: function _activate(idx) {
                var opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
                var _a, _b;
                if (idx === this._activeIndex) return;
                var beat = this._beats[idx];
                if (!beat) return;
                var direction = idx > this._activeIndex ? "down" : "up";
                this._activeIndex = idx;
                var animate = (opts.animate !== void 0 ? opts.animate : this._animate) && !prefersReducedMotion();
                if (beat.view != null || beat.options) {
                    if (this.ctx.perspectives) {
                        var v = (_a = beat.view) != null ? _a : {};
                        var token = typeof v === "string" || v.view ? v : {
                            view: this._normalizeView(v)
                        };
                        this.ctx.perspectives.apply(token, {
                            animate: animate,
                            mergeOptions: beat.options
                        });
                    } else if (!this._warnedNoPerspectives) {
                        this._warnedNoPerspectives = true;
                        console.warn('apexcharts: storyboard beats carry views but the perspectives feature is not bundled. import "apexcharts/features/storyboard" (which includes it) or drive beats via onEnter.');
                    }
                }
                var info = {
                    index: idx,
                    key: beat.key,
                    el: beat.el,
                    direction: direction
                };
                if (beat.onEnter) beat.onEnter(this.ctx, info);
                if (beat.announce) this._announce(beat.announce);
                if (typeof this.w.config.chart.events.beatChange === "function") {
                    this.w.config.chart.events.beatChange(this.ctx, info);
                }
                (_b = this.ctx.events) == null ? void 0 : _b.fireEvent("beatChange", [
                    this.ctx,
                    info
                ]);
            }
        },
        {
            /**
   * Fill in the parts of a hand-authored (bare) ViewState that would
   * otherwise LEAK between beats. updateOptions merges objects, so a beat
   * listing only xaxis annotations would keep a previous beat's point
   * annotations; padding every annotation kind with an empty array makes
   * each beat fully describe its own state, which is what allows scrubbing
   * in both directions. Full tokens (from capture()/encode()) already carry
   * the complete set and never pass through here.
   * @param {any} view
   * @returns {any}
   * @private
   */ key: "_normalizeView",
            value: function _normalizeView(view) {
                var provided = view.annotations && view.annotations.static || {};
                return __spreadProps(__spreadValues({}, view), {
                    annotations: {
                        static: __spreadValues({
                            points: [],
                            xaxis: [],
                            yaxis: [],
                            texts: [],
                            images: []
                        }, provided),
                        dynamic: view.annotations && view.annotations.dynamic || []
                    }
                });
            }
        },
        {
            /**
   * Programmatically jump to a beat by index or author key (also usable
   * without scrolling, e.g. from next/prev buttons).
   * @param {number | string} indexOrKey
   * @param {{ animate?: boolean }} [opts]
   */ key: "goTo",
            value: function goTo(indexOrKey) {
                var opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
                var idx = typeof indexOrKey === "number" ? indexOrKey : this._beats.findIndex(function(b) {
                    return b.key === indexOrKey;
                });
                if (idx >= 0 && idx < this._beats.length) this._activate(idx, opts);
            }
        },
        {
            /**
   * @returns {{ index: number, key: string | null } | null} the active beat
   */ key: "current",
            value: function current() {
                if (this._activeIndex < 0) return null;
                var beat = this._beats[this._activeIndex];
                return {
                    index: this._activeIndex,
                    key: beat ? beat.key : null
                };
            }
        },
        {
            /** Disconnect the observer and drop the beat list. */ key: "unbind",
            value: function unbind() {
                if (this._observer) {
                    this._observer.disconnect();
                    this._observer = null;
                }
                this._beats = [];
                this._activeIndex = -1;
                this._used = false;
                enforceLicense(this.w, this.ctx);
            }
        },
        {
            /** Full-destroy cleanup (called from Destroy). */ key: "teardown",
            value: function teardown() {
                this.unbind();
            }
        },
        {
            /**
   * Push a beat's announcement to the chart's visually-hidden aria-live
   * status region so screen-reader users follow the story too. No-op when
   * announcements are disabled or the region is absent.
   * @param {string} message
   * @private
   */ key: "_announce",
            value: function _announce(message) {
                var w = this.w;
                if (!w.config.chart.accessibility.announcements.enabled) return;
                var baseEl = w.dom.baseEl;
                if (!baseEl) return;
                var region = baseEl.querySelector(".apexcharts-sr-status");
                if (!region) return;
                region.textContent = "";
                setTimeout(function() {
                    region.textContent = message;
                }, 0);
            }
        }
    ]);
    return Storyboard;
}();
ApexCharts.registerFeatures({
    storyboard: Storyboard
});
function isEditableTarget(node) {
    if (!node) return false;
    var tag = node.tagName;
    return tag === "INPUT" || tag === "TEXTAREA" || tag === "SELECT" || node.isContentEditable === true;
}
var History = /*#__PURE__*/ function() {
    "use strict";
    function History(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, History);
        this.w = w;
        this.ctx = ctx;
        this.stack = [];
        this.pointer = -1;
        this.applying = false;
        this._batching = false;
        this._counter = 0;
        this._coalesceTimer = null;
        this._settleTimer = null;
        this._pendingLabel = void 0;
        this._keydownTarget = null;
        this._pointerTarget = null;
        this._engaged = false;
        this._wired = false;
        this._readConfig();
        this._onMounted = this._onMounted.bind(this);
        this._onUpdated = this._onUpdated.bind(this);
        this._onSelection = this._onSelection.bind(this);
        this._onKeyDown = this._onKeyDown.bind(this);
        this._onPointerDown = this._onPointerDown.bind(this);
        this.init();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(History, [
        {
            /**
   * (Re)read chart.history config. Called at construction and again on every
   * mounted/updated event so `updateOptions({ chart: { history: {...} } })`
   * takes effect at runtime: enabling wires the keyboard + starts capturing,
   * disabling stops capturing (the stack is kept for a later re-enable).
   */ key: "_readConfig",
            value: function _readConfig() {
                var w = this.w;
                var cfg = w.config.chart && w.config.chart.history || {};
                this.enabled = !!cfg.enabled;
                this.maxDepth = cfg.maxDepth > 0 ? cfg.maxDepth : 100;
                this.coalesceMs = cfg.coalesceMs != null ? cfg.coalesceMs : 250;
                this.keyboard = cfg.keyboard !== false;
            }
        },
        {
            /** Re-sync config, then wire/unwire the keyboard to match. */ key: "_syncConfig",
            value: function _syncConfig() {
                this._readConfig();
                if (this.enabled && this.keyboard) this._wireKeyboard();
                else this._unwireKeyboard();
            }
        },
        {
            key: "init",
            value: function init() {
                if (this._wired) return;
                this._wired = true;
                this.ctx.addEventListener("mounted", this._onMounted);
                this.ctx.addEventListener("updated", this._onUpdated);
                this.ctx.addEventListener("scrolled", this._onUpdated);
                this.ctx.addEventListener("dataPointSelection", this._onSelection);
                if (this.enabled && this.keyboard) this._wireKeyboard();
            }
        },
        {
            /**
   * Keyboard: Cmd/Ctrl+Z = undo, Shift+Cmd/Ctrl+Z or Ctrl+Y = redo. Bound on
   * the document (not the chart element) because pointer gestures that create
   * an undo step (annotation drag, zoom, pan) call preventDefault and so never
   * move focus into the chart, leaving an el-scoped listener unreachable. To
   * stay non-intrusive it acts only when this chart is "engaged" (see
   * _onKeyDown): a capture-phase pointerdown marks engagement so the shortcut
   * follows the chart the user last touched, and defers to text editing.
   */ key: "_wireKeyboard",
            value: function _wireKeyboard() {
                if (this._keydownTarget) return;
                var el = /** @type {any} */ this.ctx.el;
                var doc = el && el.ownerDocument;
                if (!Environment.isBrowser() || !doc) return;
                doc.addEventListener("keydown", this._onKeyDown);
                doc.addEventListener("pointerdown", this._onPointerDown, true);
                doc.addEventListener("mousedown", this._onPointerDown, true);
                this._keydownTarget = doc;
                this._pointerTarget = doc;
            }
        },
        {
            key: "_unwireKeyboard",
            value: function _unwireKeyboard() {
                if (this._keydownTarget) {
                    this._keydownTarget.removeEventListener("keydown", this._onKeyDown);
                    this._keydownTarget = null;
                }
                if (this._pointerTarget) {
                    this._pointerTarget.removeEventListener("pointerdown", this._onPointerDown, true);
                    this._pointerTarget.removeEventListener("mousedown", this._onPointerDown, true);
                    this._pointerTarget = null;
                }
                this._engaged = false;
            }
        },
        {
            // ─── Event handlers ─────────────────────────────────────────────────────
            key: "_onMounted",
            value: function _onMounted() {
                this._syncConfig();
                if (!this.enabled) return;
                if (this.stack.length === 0) this._commit("initial", true);
            }
        },
        {
            key: "_onUpdated",
            value: function _onUpdated() {
                this._syncConfig();
                if (!this.enabled) return;
                if (this.applying) {
                    this._refreshSettle();
                    return;
                }
                this._schedule("update");
            }
        },
        {
            key: "_onSelection",
            value: function _onSelection() {
                if (!this.enabled || this.applying) return;
                this._schedule("selection");
            }
        },
        {
            /**
   * Mark whether the chart is engaged: a capture-phase pointerdown inside `el`
   * engages it (runs before feature handlers stopPropagation), one elsewhere
   * releases it. Capture phase so an annotation/zoom gesture that stops
   * propagation still registers.
   * @param {any} e
   */ key: "_onPointerDown",
            value: function _onPointerDown(e) {
                var el = /** @type {any} */ this.ctx.el;
                this._engaged = !!(el && e.target && el.contains(e.target));
            }
        },
        {
            /**
   * @param {KeyboardEvent} e
   */ key: "_onKeyDown",
            value: function _onKeyDown(e) {
                if (!(e.metaKey || e.ctrlKey)) return;
                var key = (e.key || "").toLowerCase();
                if (key !== "z" && key !== "y") return;
                var el = /** @type {any} */ this.ctx.el;
                if (!el) return;
                var doc = el.ownerDocument;
                var active = doc && doc.activeElement;
                if (isEditableTarget(active)) return;
                if (!(el.contains(active) || this._engaged)) return;
                var redo = key === "y" || e.shiftKey;
                e.preventDefault();
                if (redo) this.redo();
                else this.undo();
            }
        },
        {
            // ─── Capture (coalesced) ────────────────────────────────────────────────
            /**
   * @param {string} label
   */ key: "_schedule",
            value: function _schedule(label) {
                var _this = this;
                if (this.applying || this._batching || !this.enabled) return;
                this._pendingLabel = label;
                if (this.coalesceMs > 0 && Environment.isBrowser()) {
                    clearTimeout(this._coalesceTimer);
                    this._coalesceTimer = setTimeout(function() {
                        return _this._commit(_this._pendingLabel);
                    }, this.coalesceMs);
                } else {
                    this._commit(label);
                }
            }
        },
        {
            /**
   * @param {string} [label]
   * @param {boolean} [force] bypass the applying/batching guard (baseline / transaction)
   */ key: "_commit",
            value: function _commit(label, force) {
                clearTimeout(this._coalesceTimer);
                this._coalesceTimer = null;
                if (!force && (this.applying || this._batching)) return;
                var cp = this._capture(label);
                var current = this.stack[this.pointer];
                if (current && current.sig === cp.sig) return;
                if (this.pointer < this.stack.length - 1) {
                    this.stack.splice(this.pointer + 1);
                }
                this.stack.push(cp);
                this.pointer = this.stack.length - 1;
                while(this.stack.length > this.maxDepth){
                    this.stack.shift();
                    this.pointer--;
                }
                this._emitChange();
            }
        },
        {
            /**
   * @param {string} [label]
   */ key: "_capture",
            value: function _capture(label) {
                var view = captureViewState(this.w, this.ctx);
                var _this__cloneConfigCOW = this._cloneConfigCOW(), config = _this__cloneConfigCOW.config, seriesSig = _this__cloneConfigCOW.seriesSig;
                return {
                    id: "hist-".concat(++this._counter),
                    view: view,
                    config: config,
                    seriesSig: seriesSig,
                    label: label || "change",
                    at: Environment.isBrowser() ? Date.now() : 0,
                    origin: "local",
                    // reserved for per-user scoping (Live Rooms)
                    sig: this._signature(view, config)
                };
            }
        },
        {
            /**
   * Clone w.config, sharing the previous checkpoint's cloned series when the
   * live series CONTENT is unchanged (copy-on-write). Sharing is decided by a
   * value signature, not reference identity: callers commonly mutate a kept
   * series array in place and pass the same reference back to updateSeries, and
   * an identity check would share a stale clone for exactly that case. The
   * stringify is not extra cost: _signature already serialises the series as
   * part of dedup.
   * @returns {{ config: any, seriesSig: string|null }}
   */ key: "_cloneConfigCOW",
            value: function _cloneConfigCOW() {
                var w = this.w;
                var prev = this.stack[this.pointer];
                var seriesSig = null;
                try {
                    seriesSig = JSON.stringify(w.config.series);
                } catch (e) {
                    seriesSig = null;
                }
                var cloned;
                if (prev && seriesSig !== null && prev.seriesSig === seriesSig) {
                    var _a = w.config, _series = _a.series, rest = __objRest(_a, [
                        "series"
                    ]);
                    cloned = Utils$1.clone(rest);
                    cloned.series = prev.config.series;
                } else {
                    cloned = Utils$1.clone(w.config);
                }
                return {
                    config: cloned,
                    seriesSig: seriesSig
                };
            }
        },
        {
            /**
   * Data-level signature for dedup. Functions are dropped by JSON (fine: a
   * checkpoint whose only change is a function reference is not a meaningful
   * undo step). Runs once per committed checkpoint, not per raw event.
   * @param {any} view
   * @param {any} config
   * @returns {string}
   */ key: "_signature",
            value: function _signature(view, config) {
                try {
                    return JSON.stringify(view) + "|" + JSON.stringify(config);
                } catch (e) {
                    return "nosig-".concat(this._counter);
                }
            }
        },
        {
            // ─── Restore ────────────────────────────────────────────────────────────
            /**
   * @param {any} cp
   * @param {boolean} animate
   */ key: "_restore",
            value: function _restore(cp, animate) {
                var _this = this;
                if (!cp) return;
                this.applying = true;
                var p;
                try {
                    this.ctx.clearAnnotations();
                    p = this.ctx.updateOptions(Utils$1.clone(cp.config), false, animate, false, false);
                } catch (e) {
                    this.applying = false;
                    throw e;
                }
                Promise.resolve(p).then(function() {
                    if (_this.w.globals.isDestroyed) {
                        _this.applying = false;
                        return;
                    }
                    applyViewInteraction(_this.ctx, cp.view);
                    _this._refreshSettle();
                    _this._emitChange();
                }).catch(function() {
                    _this.applying = false;
                });
            }
        },
        {
            /**
   * Hold `applying` true until the restore's burst of async 'updated' events
   * has drained (one macrotask after the last one). Refreshed by _onUpdated.
   */ key: "_refreshSettle",
            value: function _refreshSettle() {
                var _this = this;
                if (!Environment.isBrowser()) {
                    this.applying = false;
                    return;
                }
                clearTimeout(this._settleTimer);
                this._settleTimer = setTimeout(function() {
                    _this.applying = false;
                }, 0);
            }
        },
        {
            // ─── Public API ─────────────────────────────────────────────────────────
            /**
   * Commit a checkpoint of the current state now (a discrete undo step). Used by
   * callers that mutate `w.config` without going through a full re-render (e.g.
   * Ink Layer's targeted annotation redraws, which fire no 'updated' event).
   * No-op when disabled or while a restore is applying.
   * @param {string} [label]
   */ key: "snapshot",
            value: function snapshot(label) {
                if (!this.enabled || this.applying) return;
                this._commit(label || "change");
            }
        },
        {
            /**
   * @param {boolean} [animate]
   */ key: "undo",
            value: function undo() {
                var animate = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
                if (!this.canUndo()) return;
                this.pointer--;
                this._restore(this.stack[this.pointer], animate);
            }
        },
        {
            /**
   * @param {boolean} [animate]
   */ key: "redo",
            value: function redo() {
                var animate = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
                if (!this.canRedo()) return;
                this.pointer++;
                this._restore(this.stack[this.pointer], animate);
            }
        },
        {
            key: "canUndo",
            value: function canUndo() {
                return this.pointer > 0;
            }
        },
        {
            key: "canRedo",
            value: function canRedo() {
                return this.pointer > -1 && this.pointer < this.stack.length - 1;
            }
        },
        {
            /**
   * @param {string} id
   * @param {boolean} [animate]
   */ key: "jump",
            value: function jump(id) {
                var animate = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
                var idx = this.stack.findIndex(function(c) {
                    return c.id === id;
                });
                if (idx === -1 || idx === this.pointer) return;
                this.pointer = idx;
                this._restore(this.stack[idx], animate);
            }
        },
        {
            /** Clear the history, keeping the current state as the new baseline. */ key: "clear",
            value: function clear() {
                clearTimeout(this._coalesceTimer);
                this._coalesceTimer = null;
                var current = this.stack[this.pointer];
                this.stack = current ? [
                    current
                ] : [];
                this.pointer = this.stack.length - 1;
                this._emitChange();
            }
        },
        {
            /**
   * Group multiple edits into a single undo step. `fn` may be async; await your
   * updateOptions/updateSeries calls inside it so the intermediate 'updated'
   * events are suppressed and only one checkpoint is committed afterwards.
   * @param {() => (void | Promise<any>)} fn
   * @param {{ label?: string }} [opts]
   * @returns {Promise<void>}
   */ key: "transaction",
            value: function transaction(fn) {
                var _this = this;
                var opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
                if (typeof fn !== "function") return Promise.resolve();
                var wasBatching = this._batching;
                this._batching = true;
                return Promise.resolve().then(function() {
                    return fn();
                }).finally(function() {
                    _this._batching = wasBatching;
                    if (!wasBatching) _this._commit(opts.label || "transaction", true);
                });
            }
        },
        {
            /**
   * @returns {{ id: string, label: string, at: number }[]}
   */ key: "entries",
            value: function entries() {
                return this.stack.map(function(c) {
                    return {
                        id: c.id,
                        label: c.label,
                        at: c.at
                    };
                });
            }
        },
        {
            /** Lightweight state for the historyChange event / a history-rail UI. */ key: "state",
            value: function state() {
                return {
                    canUndo: this.canUndo(),
                    canRedo: this.canRedo(),
                    index: this.pointer,
                    length: this.stack.length
                };
            }
        },
        {
            key: "_emitChange",
            value: function _emitChange() {
                this.ctx.events.fireEvent("historyChange", [
                    this.ctx,
                    this.state()
                ]);
            }
        },
        {
            /** Drop the stack + detach listeners (called on full destroy). */ key: "teardown",
            value: function teardown() {
                var _a, _b, _c, _d, _e, _f, _g, _h;
                clearTimeout(this._coalesceTimer);
                clearTimeout(this._settleTimer);
                this._coalesceTimer = null;
                this._settleTimer = null;
                this._unwireKeyboard();
                if (this._wired) {
                    (_b = (_a = this.ctx).removeEventListener) == null ? void 0 : _b.call(_a, "mounted", this._onMounted);
                    (_d = (_c = this.ctx).removeEventListener) == null ? void 0 : _d.call(_c, "updated", this._onUpdated);
                    (_f = (_e = this.ctx).removeEventListener) == null ? void 0 : _f.call(_e, "scrolled", this._onUpdated);
                    (_h = (_g = this.ctx).removeEventListener) == null ? void 0 : _h.call(_g, "dataPointSelection", this._onSelection);
                }
                this.stack = [];
                this.pointer = -1;
                this._wired = false;
            }
        }
    ]);
    return History;
}();
ApexCharts.registerFeatures({
    history: History
});
var WEAVE_API_VERSION = 1;
var PLUGIN_CHART_METHODS = [
    "updateOptions",
    "updateSeries",
    "appendData",
    "appendSeries",
    "toggleSeries",
    "showSeries",
    "hideSeries",
    "highlightSeries",
    "isSeriesHidden",
    "zoomX",
    "addXaxisAnnotation",
    "addYaxisAnnotation",
    "addPointAnnotation",
    "clearAnnotations",
    "removeAnnotation",
    "dataURI",
    "exportToCSV"
];
function buildBoundPublicMethods(ctx) {
    var out = {};
    PLUGIN_CHART_METHODS.forEach(function(m) {
        if (typeof ctx[m] === "function") out[m] = ctx[m].bind(ctx);
    });
    return Object.freeze(out);
}
function makeLayerHandle(g, graphics) {
    var add = function add(el) {
        if (el) g.add(el);
        return el;
    };
    var handle = {
        get node () {
            return g.node;
        },
        /** @param {any} opts */ path: function path() {
            var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
            var _opts_d = opts.d, d = _opts_d === void 0 ? "" : _opts_d, _opts_stroke = opts.stroke, stroke = _opts_stroke === void 0 ? "#000" : _opts_stroke, _opts_width = opts.width, width = _opts_width === void 0 ? 1 : _opts_width, _opts_fill = opts.fill, fill = _opts_fill === void 0 ? "none" : _opts_fill, _opts_opacity = opts.opacity, opacity = _opts_opacity === void 0 ? 1 : _opts_opacity, _opts_dash = opts.dash, dash = _opts_dash === void 0 ? 0 : _opts_dash, _opts_className = opts.className, className = _opts_className === void 0 ? "" : _opts_className;
            return add(graphics.drawPath({
                d: d,
                stroke: stroke,
                strokeWidth: width,
                fill: fill,
                fillOpacity: fill === "none" ? 0 : opacity,
                strokeOpacity: opacity,
                strokeDashArray: dash,
                classes: className
            }));
        },
        /** @param {any} opts */ line: function line() {
            var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
            var x1 = opts.x1, y1 = opts.y1, x2 = opts.x2, y2 = opts.y2, _opts_stroke = opts.stroke, stroke = _opts_stroke === void 0 ? "#000" : _opts_stroke, _opts_width = opts.width, width = _opts_width === void 0 ? 1 : _opts_width, _opts_dash = opts.dash, dash = _opts_dash === void 0 ? 0 : _opts_dash;
            return add(graphics.drawLine(x1, y1, x2, y2, stroke, dash, width));
        },
        /** @param {any} opts */ rect: function rect() {
            var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
            var _opts_x = opts.x, x = _opts_x === void 0 ? 0 : _opts_x, _opts_y = opts.y, y = _opts_y === void 0 ? 0 : _opts_y, _opts_w = opts.w, w = _opts_w === void 0 ? 0 : _opts_w, _opts_h = opts.h, h = _opts_h === void 0 ? 0 : _opts_h, _opts_r = opts.r, r = _opts_r === void 0 ? 0 : _opts_r, _opts_fill = opts.fill, fill = _opts_fill === void 0 ? "#000" : _opts_fill, _opts_stroke = opts.stroke, stroke = _opts_stroke === void 0 ? null : _opts_stroke, _opts_opacity = opts.opacity, opacity = _opts_opacity === void 0 ? 1 : _opts_opacity;
            return add(graphics.drawRect(x, y, w, h, r, fill, opacity, stroke != null ? 1 : null, stroke));
        },
        /** @param {any} opts */ circle: function circle() {
            var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
            var _opts_cx = opts.cx, cx = _opts_cx === void 0 ? 0 : _opts_cx, _opts_cy = opts.cy, cy = _opts_cy === void 0 ? 0 : _opts_cy, _opts_r = opts.r, r = _opts_r === void 0 ? 0 : _opts_r, _opts_fill = opts.fill, fill = _opts_fill === void 0 ? "#000" : _opts_fill, _opts_stroke = opts.stroke, stroke = _opts_stroke === void 0 ? null : _opts_stroke;
            return add(graphics.drawCircle(r, {
                cx: cx,
                cy: cy,
                fill: fill,
                stroke: stroke || "none"
            }));
        },
        /** @param {any} opts */ text: function text() {
            var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
            var _opts_x = opts.x, x = _opts_x === void 0 ? 0 : _opts_x, _opts_y = opts.y, y = _opts_y === void 0 ? 0 : _opts_y, _opts_text = opts.text, text = _opts_text === void 0 ? "" : _opts_text, color = opts.color, size = opts.size, _opts_anchor = opts.anchor, anchor = _opts_anchor === void 0 ? "start" : _opts_anchor, weight = opts.weight;
            return add(graphics.drawText({
                x: x,
                y: y,
                text: text,
                textAnchor: anchor,
                fontSize: size,
                foreColor: color,
                fontWeight: weight
            }));
        },
        clear: function clear() {
            var node = g.node;
            while(node.firstChild)node.removeChild(node.firstChild);
            return handle;
        }
    };
    return handle;
}
function buildPluginAPI(host, record) {
    var ctx = host.ctx;
    var w = host.w;
    var api = {
        name: record.def.name,
        version: WEAVE_API_VERSION,
        // Live: reconcile refreshes record.options when the chart's plugins config
        // changes, so updateOptions({ plugins: [{ name, options }] }) reconfigures
        // an active plugin in place. The returned object is frozen.
        get options () {
            return record.options;
        },
        // ── lifecycle subscription ──
        /**
     * @param {string} hook
     * @param {Function} fn
     */ on: function on(hook, fn) {
            var m = record.handlers;
            if (!m.has(hook)) m.set(hook, []);
            m.get(hook).push(fn);
            return api;
        },
        /**
     * @param {string} hook
     * @param {Function} fn
     */ off: function off(hook, fn) {
            var a = record.handlers.get(hook);
            if (a) {
                var i = a.indexOf(fn);
                if (i > -1) a.splice(i, 1);
            }
            return api;
        },
        // ── per-plugin, per-chart scratch state (survives updates, dropped on
        //    destroy). The api object is frozen, but this object is mutable. ──
        store: {},
        // ── drawing (renderer-agnostic) ──
        // Call this INSIDE each draw handler: the host wipes plugin layers at the
        // start of every draw pass, so a handle cached across draws points at a
        // detached node and its writes vanish silently.
        /** @param {any} [opts] */ layer: function layer(opts) {
            return host._layer(record.def.name, opts || {});
        },
        // ── reads ──
        get scales () {
            return host._currentScales;
        },
        // Served from the per-dispatch snapshot when one exists (invalidated at
        // every dispatch), so reading api.data in a loop does not rebuild the
        // point arrays on each property access.
        get data () {
            return host._lastData || (host._lastData = host._dataSnapshot());
        },
        theme: Object.freeze({
            get mode () {
                return w.config.theme.mode;
            },
            get foreColor () {
                return w.config.chart.foreColor;
            },
            /** @param {number} i */ seriesColor: function seriesColor(i) {
                return w.globals.colors[i];
            },
            /** @param {string} name */ token: function token(name2) {
                return host._token(name2);
            }
        }),
        // ── curated actions (bound public methods only; NEVER raw w) ──
        chart: buildBoundPublicMethods(ctx),
        // ── custom events out to the host app ──
        /**
     * Fires as `plugin:<pluginName>:<name>` on the chart's event bus. The
     * namespace is not optional: the bus also carries the internal lifecycle
     * events ('updated', 'mounted', ...), and an un-namespaced emit could
     * trigger every internal subscriber (history capture, re-render hooks).
     * Listen with chart.addEventListener('plugin:myplugin:myevent', fn).
     * @param {string} name
     * @param {any} [detail]
     */ emit: function emit(name2, detail) {
            ctx.events.fireEvent("plugin:".concat(record.def.name, ":").concat(name2), [
                ctx,
                detail
            ]);
        },
        // ── host element (read; lazy: baseEl is not set until render) ──
        get el () {
            return w.dom.baseEl;
        }
    };
    return Object.freeze(api);
}
var WeaveHost = /*#__PURE__*/ function() {
    "use strict";
    function WeaveHost(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, WeaveHost);
        this.w = w;
        this.ctx = ctx;
        this.active = [];
        this._layers = /* @__PURE__ */ new Map();
        this._currentScales = null;
        this._lastPluginsRef = null;
        this._lastData = null;
        this._updatedWired = false;
        this._onUpdated = this._onUpdated.bind(this);
        this._init();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(WeaveHost, [
        {
            key: "_init",
            value: function _init() {
                var _this = this;
                var list = this.w.config.plugins || [];
                list.map(function(entry, i) {
                    return {
                        entry: entry,
                        order: entry.order != null ? entry.order : i
                    };
                }).sort(function(a, b) {
                    return a.order - b.order;
                }).forEach(function(o) {
                    return _this._activate(o.entry);
                });
                this._lastPluginsRef = this.w.config.plugins;
                this._wireUpdated();
            }
        },
        {
            key: "_wireUpdated",
            value: function _wireUpdated() {
                if (this._updatedWired) return;
                this.ctx.addEventListener("updated", this._onUpdated);
                this._updatedWired = true;
            }
        },
        {
            key: "_onUpdated",
            value: function _onUpdated() {
                this.dispatch("afterUpdate", {
                    pass: "update"
                });
            }
        },
        {
            /**
   * @param {any} entry { name, options?, order? }
   */ key: "_activate",
            value: function _activate(entry) {
                var def = getPlugin(entry.name);
                if (!def) {
                    console.error('[apexcharts] plugin "'.concat(entry.name, '" is not registered.'));
                    return;
                }
                var v = def.apiVersion != null ? def.apiVersion : 1;
                if (Math.trunc(v) !== WEAVE_API_VERSION) {
                    console.error('[apexcharts] plugin "'.concat(def.name, '" targets Weave API v').concat(v, ", host is v").concat(WEAVE_API_VERSION, "; skipped."));
                    return;
                }
                var record = {
                    def: def,
                    options: Object.freeze(__spreadValues({}, entry.options || {})),
                    handlers: /* @__PURE__ */ new Map(),
                    disabled: false,
                    failures: 0,
                    api: null
                };
                record.api = buildPluginAPI(this, record);
                this.active.push(record);
                this._guard(record, "setup", function() {
                    return def.setup(record.api);
                });
            }
        },
        {
            /**
   * @param {string} hook
   * @param {{ pass?: string, xyRatios?: any }} [extra]
   */ key: "dispatch",
            value: function dispatch(hook, extra) {
                if (hook === "draw") {
                    this._reconcile();
                    this._resetLayers();
                }
                this._lastData = null;
                if (!this.active.length) return;
                if (hook === "afterParse") {
                    this._currentScales = null;
                } else if (extra && "xyRatios" in extra) {
                    this._setScales(extra.xyRatios);
                }
                var pass = extra && extra.pass || "full";
                var data = null;
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    var _this, _loop = function() {
                        var record = _step.value;
                        if (record.disabled) return "continue";
                        var fns = record.handlers.get(hook);
                        if (!fns || !fns.length) return "continue";
                        if (data === null) {
                            data = _this._dataSnapshot();
                            _this._lastData = data;
                        }
                        var payload = {
                            api: record.api,
                            scales: _this._currentScales,
                            data: data,
                            pass: pass,
                            hook: hook
                        };
                        var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                        try {
                            var _this1, _loop = function() {
                                var fn = _step1.value;
                                _this1._guard(record, hook, function() {
                                    return fn(payload);
                                });
                            };
                            for(var _iterator = fns.slice()[Symbol.iterator](), _step1; !(_iteratorNormalCompletion = (_step1 = _iterator.next()).done); _iteratorNormalCompletion = true)_this1 = _this, _loop();
                        } catch (err) {
                            _didIteratorError = true;
                            _iteratorError = err;
                        } finally{
                            try {
                                if (!_iteratorNormalCompletion && _iterator.return != null) {
                                    _iterator.return();
                                }
                            } finally{
                                if (_didIteratorError) {
                                    throw _iteratorError;
                                }
                            }
                        }
                    };
                    for(var _iterator = this.active[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true)_this = this, _loop();
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
            }
        },
        {
            /**
   * @param {any} record
   * @param {string} where
   * @param {Function} fn
   */ key: "_guard",
            value: function _guard(record, where, fn) {
                if (record.disabled) return;
                try {
                    fn();
                } catch (e) {
                    console.error('[apexcharts] plugin "'.concat(record.def.name, '" threw in "').concat(where, '":'), e);
                    record.failures = (record.failures || 0) + 1;
                    if (record.failures >= 3) {
                        record.disabled = true;
                        console.error('[apexcharts] plugin "'.concat(record.def.name, '" disabled after repeated errors.'));
                    }
                }
            }
        },
        {
            // ─── Scales facade ──────────────────────────────────────────────────────
            /**
   * Build api.scales from the SAME xyRatios the series were drawn with, so
   * plugin pixels align with series pixels by construction.
   * @param {any} xyRatios
   */ key: "_setScales",
            value: function _setScales(xyRatios) {
                var w = this.w;
                var gl = w.globals;
                var L = w.layout;
                if (!xyRatios || !gl.axisCharts) {
                    this._currentScales = null;
                    return;
                }
                var xRatio = xyRatios.xRatio;
                var yRatio = xyRatios.yRatio || [];
                var yr = function yr(axis) {
                    return yRatio[axis] != null ? yRatio[axis] : yRatio[0];
                };
                var maxY = function maxY(axis) {
                    return gl.maxYArr[axis] != null ? gl.maxYArr[axis] : gl.maxY;
                };
                var minY = function minY(axis) {
                    return gl.minYArr[axis] != null ? gl.minYArr[axis] : gl.minY;
                };
                this._currentScales = {
                    /** @param {number} v */ x: function x(v) {
                        return L.translateX + (v - gl.minX) / xRatio;
                    },
                    /**
       * @param {number} v
       * @param {number} [axis]
       */ y: function y(v) {
                        var axis = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
                        return L.translateY + (maxY(axis) - v) / yr(axis);
                    },
                    domainX: [
                        gl.minX,
                        gl.maxX
                    ],
                    /** @param {number} [axis] */ domainY: function domainY() {
                        var axis = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
                        return [
                            minY(axis),
                            maxY(axis)
                        ];
                    },
                    gridWidth: L.gridWidth,
                    gridHeight: L.gridHeight,
                    ratios: xyRatios
                };
            }
        },
        {
            // ─── Read-only data snapshot ────────────────────────────────────────────
            /**
   * @returns {any[]} defensive per-series snapshot (never the live slice)
   */ key: "_dataSnapshot",
            value: function _dataSnapshot() {
                var w = this.w;
                var gl = w.globals;
                var series = w.seriesData.series || [];
                var seriesX = w.seriesData.seriesX || [];
                return series.map(function(sData, i) {
                    var xs = seriesX[i] || [];
                    var points = (sData || []).map(function(y, j) {
                        return {
                            x: xs[j] != null ? xs[j] : j,
                            y: y
                        };
                    });
                    return {
                        name: gl.seriesNames ? gl.seriesNames[i] : void 0,
                        hidden: (gl.collapsedSeriesIndices || []).includes(i),
                        color: gl.colors ? gl.colors[i] : void 0,
                        points: points
                    };
                });
            }
        },
        {
            // ─── Theme tokens ───────────────────────────────────────────────────────
            /**
   * @param {string} name
   * @returns {any}
   */ key: "_token",
            value: function _token(name2) {
                var w = this.w;
                var gl = w.globals;
                switch(name2){
                    case "foreColor":
                        return w.config.chart.foreColor;
                    case "background":
                        return w.config.chart.background;
                    case "accent":
                    case "primary":
                        return gl.colors ? gl.colors[0] : void 0;
                    default:
                        if (/^series-\d+$/.test(name2)) {
                            return gl.colors ? gl.colors[Number(name2.split("-")[1])] : void 0;
                        }
                        return void 0;
                }
            }
        },
        {
            // ─── Layers ─────────────────────────────────────────────────────────────
            /**
   * @param {string} name
   * @param {{ z?: 'front'|'behind', className?: string }} opts
   */ key: "_layer",
            value: function _layer(name2) {
                var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref_z = _ref.z, z = _ref_z === void 0 ? "front" : _ref_z, _ref_className = _ref.className, className = _ref_className === void 0 ? "" : _ref_className;
                var g = this._layers.get(name2);
                if (!g) {
                    g = this.ctx.graphics.group({
                        class: "apexcharts-plugin-".concat(name2, " ").concat(className).trim()
                    });
                    var parent = this.w.dom.elGraphical.node;
                    if (z === "behind") parent.insertBefore(g.node, parent.firstChild);
                    else parent.appendChild(g.node);
                    g.node.setAttribute("aria-hidden", "true");
                    this._layers.set(name2, g);
                }
                return makeLayerHandle(g, this.ctx.graphics);
            }
        },
        {
            /**
   * Remove all plugin layers. Run at the start of every `draw` because
   * fastUpdate only removes series/data-label groups (not arbitrary plugin
   * groups), so without this, fast-path redraws would duplicate plugin output.
   */ key: "_resetLayers",
            value: function _resetLayers() {
                var el = this.w.dom.elGraphical;
                var parent = el && el.node;
                if (parent) {
                    var groups = parent.querySelectorAll('g[class*="apexcharts-plugin-"]');
                    Array.prototype.forEach.call(groups, function(n) {
                        return n.remove();
                    });
                }
                this._layers.clear();
            }
        },
        {
            // ─── Config-change reconciliation ───────────────────────────────────────
            /**
   * Diff w.config.plugins by name: teardown removed, activate added; unchanged
   * plugins keep their instance + store, but their `options` are refreshed from
   * the new entry (api.options is a live getter), so
   * updateOptions({ plugins: [{ name, options }] }) reconfigures in place.
   * Skipped when the plugins array reference is unchanged (fast redraws), so it
   * costs nothing on hover.
   */ key: "_reconcile",
            value: function _reconcile() {
                var _this, _loop = function(i) {
                    var r = _this.active[i];
                    var want = desired.get(r.def.name);
                    if (!want) {
                        _this._guard(r, "destroy", function() {
                            return r.def.destroy && r.def.destroy(r.api);
                        });
                        _this.active.splice(i, 1);
                    } else {
                        r.options = Object.freeze(__spreadValues({}, want.entry.options || {}));
                    }
                };
                var _this1 = this;
                var plugins = this.w.config.plugins || [];
                if (plugins === this._lastPluginsRef) return;
                this._lastPluginsRef = plugins;
                var desired = new Map(plugins.map(function(e, i) {
                    return [
                        e.name,
                        {
                            entry: e,
                            order: e.order != null ? e.order : i
                        }
                    ];
                }));
                for(var i = this.active.length - 1; i >= 0; i--)_this = this, _loop(i);
                var activeNames = new Set(this.active.map(function(r) {
                    return r.def.name;
                }));
                var toAdd = [];
                desired.forEach(function(v, name2) {
                    if (!activeNames.has(name2)) toAdd.push(v);
                });
                toAdd.sort(function(a, b) {
                    return a.order - b.order;
                }).forEach(function(v) {
                    return _this1._activate(v.entry);
                });
            }
        },
        {
            /**
   * @param {boolean} [isUpdating]
   */ key: "teardown",
            value: function teardown(isUpdating) {
                if (!isUpdating) {
                    this.dispatch("destroy");
                    var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                    try {
                        var _this, _loop = function() {
                            var record = _step.value;
                            _this._guard(record, "destroy", function() {
                                return record.def.destroy && record.def.destroy(record.api);
                            });
                        };
                        for(var _iterator = this.active[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true)_this = this, _loop();
                    } catch (err) {
                        _didIteratorError = true;
                        _iteratorError = err;
                    } finally{
                        try {
                            if (!_iteratorNormalCompletion && _iterator.return != null) {
                                _iterator.return();
                            }
                        } finally{
                            if (_didIteratorError) {
                                throw _iteratorError;
                            }
                        }
                    }
                    this.active = [];
                    if (this._updatedWired) {
                        this.ctx.removeEventListener && this.ctx.removeEventListener("updated", this._onUpdated);
                        this._updatedWired = false;
                    }
                }
                this._layers.clear();
            }
        }
    ]);
    return WeaveHost;
}();
ApexCharts.registerFeatures({
    weave: WeaveHost
});
var STYLE_KEYS = {
    fill: "fill",
    stroke: "stroke",
    "stroke-width": "strokeWidth",
    "stroke-dasharray": "strokeDash",
    "stroke-linecap": "lineCap",
    "fill-opacity": "fillOpacity",
    "stroke-opacity": "strokeOpacity",
    "fill-rule": "fillRule"
};
var NEVER = Symbol("never");
var SHAPE_ID = {
    circle: 0,
    square: 1,
    rect: 1,
    triangle: 2,
    diamond: 3,
    star: 4,
    sparkle: 5,
    cross: 6,
    plus: 7,
    line: 8
};
var SHAPE_NAME = [
    "circle",
    "square",
    "triangle",
    "diamond",
    "star",
    "sparkle",
    "cross",
    "plus",
    "line"
];
var NOOP_RUNNER = {
    /** @returns {any} */ attr: function attr() {
        return NOOP_RUNNER;
    },
    plot: function plot() {
        return NOOP_RUNNER;
    },
    during: function during() {
        return NOOP_RUNNER;
    },
    after: function after(fn) {
        if (typeof fn === "function") fn();
        return NOOP_RUNNER;
    },
    animate: function animate() {
        return NOOP_RUNNER;
    },
    delay: function delay() {
        return NOOP_RUNNER;
    },
    loop: function loop() {
        return NOOP_RUNNER;
    },
    finish: function finish() {
        return NOOP_RUNNER;
    },
    stop: function stop() {
        return NOOP_RUNNER;
    }
};
var SHARED_MARKER_NODE = {
    nodeName: "path",
    style: {},
    classList: {
        add: function add() {},
        remove: function remove() {},
        toggle: function toggle() {},
        contains: function contains() {
            return false;
        }
    },
    setAttribute: function setAttribute() {},
    getAttribute: function getAttribute() {
        return null;
    },
    removeAttribute: function removeAttribute() {},
    hasAttribute: function hasAttribute() {
        return false;
    },
    addEventListener: function addEventListener() {},
    removeEventListener: function removeEventListener() {},
    appendChild: function appendChild() {},
    getBBox: function getBBox() {
        return {
            x: 0,
            y: 0,
            width: 0,
            height: 0
        };
    }
};
var SHARED_GROUP = {
    __isCanvasMark: true,
    node: {
        nodeName: "g",
        instance: null,
        style: {},
        classList: {
            add: function add() {},
            remove: function remove() {},
            toggle: function toggle() {},
            contains: function contains() {
                return false;
            }
        },
        setAttribute: function setAttribute() {},
        getAttribute: function getAttribute() {
            return null;
        },
        removeAttribute: function removeAttribute() {},
        addEventListener: function addEventListener() {},
        removeEventListener: function removeEventListener() {},
        appendChild: function appendChild() {},
        getBBox: function getBBox() {
            return {
                x: 0,
                y: 0,
                width: 0,
                height: 0
            };
        }
    },
    /** @returns {any} */ attr: function attr() {
        return SHARED_GROUP;
    },
    add: function add() {
        return SHARED_GROUP;
    },
    addTo: function addTo() {
        return SHARED_GROUP;
    },
    remove: function remove() {
        return SHARED_GROUP;
    },
    clear: function clear() {
        return SHARED_GROUP;
    },
    css: function css() {
        return SHARED_GROUP;
    },
    hide: function hide() {
        return SHARED_GROUP;
    },
    show: function show() {
        return SHARED_GROUP;
    },
    removeClass: function removeClass() {
        return SHARED_GROUP;
    },
    animate: function animate() {
        return NOOP_RUNNER;
    }
};
var CanvasMarkerRef = /*#__PURE__*/ function() {
    "use strict";
    function CanvasMarkerRef(g, i) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CanvasMarkerRef);
        this.__isCanvasMark = true;
        this._g = g;
        this._i = i;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CanvasMarkerRef, [
        {
            key: "node",
            get: function get() {
                return SHARED_MARKER_NODE;
            }
        },
        {
            /**
   * @param {any} a
   * @param {any} [v]
   * @returns {any}
   */ key: "attr",
            value: function attr(a, v) {
                if (typeof a === "string") {
                    if (a === "fill" && v !== void 0) this._g._setMarkerFill(this._i, v);
                    return v === void 0 ? null : this;
                }
                if (a && a.fill !== void 0) this._g._setMarkerFill(this._i, a.fill);
                return this;
            }
        },
        {
            /** @param {any} _c */ key: "add",
            value: function add(_c) {
                return this;
            }
        },
        {
            /** @param {any} _p */ key: "addTo",
            value: function addTo(_p) {
                return this;
            }
        },
        {
            key: "remove",
            value: function remove() {
                return this;
            }
        },
        {
            /** @param {any} _s */ key: "css",
            value: function css(_s) {
                return this;
            }
        },
        {
            /** @param {any} _v */ key: "fill",
            value: function fill(_v) {
                if (_v !== void 0) this._g._setMarkerFill(this._i, _v);
                return this;
            }
        },
        {
            /** @param {any} _v */ key: "stroke",
            value: function stroke(_v) {
                return this;
            }
        },
        {
            key: "hide",
            value: function hide() {
                return this;
            }
        },
        {
            key: "show",
            value: function show() {
                return this;
            }
        },
        {
            /** @param {string} _c */ key: "removeClass",
            value: function removeClass(_c) {
                return this;
            }
        },
        {
            key: "animate",
            value: function animate() {
                return NOOP_RUNNER;
            }
        }
    ]);
    return CanvasMarkerRef;
}();
var SHARED_RECT_REF = {
    __isCanvasMark: true,
    node: SHARED_MARKER_NODE,
    /** @returns {any} */ attr: function attr() {
        return SHARED_RECT_REF;
    },
    add: function add() {
        return SHARED_RECT_REF;
    },
    addTo: function addTo() {
        return SHARED_RECT_REF;
    },
    remove: function remove() {
        return SHARED_RECT_REF;
    },
    /** @returns {any} */ css: function css() {
        return SHARED_RECT_REF;
    },
    animate: function animate() {
        return NOOP_RUNNER;
    }
};
var CanvasMark = /*#__PURE__*/ function() {
    "use strict";
    function CanvasMark(cmd) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CanvasMark);
        this.__isCanvasMark = true;
        this._cmd = cmd;
        var self = this;
        this.node = {
            nodeName: cmd ? cmd.tag : "g",
            instance: this,
            style: {},
            classList: {
                add: function add() {},
                remove: function remove() {},
                toggle: function toggle() {},
                contains: function contains() {
                    return false;
                }
            },
            /** @param {string} k @param {any} v */ setAttribute: function setAttribute(k, v) {
                self._applyAttr(k, v);
            },
            getAttribute: function getAttribute() {
                return null;
            },
            removeAttribute: function removeAttribute() {},
            hasAttribute: function hasAttribute() {
                return false;
            },
            addEventListener: function addEventListener() {},
            removeEventListener: function removeEventListener() {},
            appendChild: function appendChild() {},
            getBBox: function getBBox() {
                return {
                    x: 0,
                    y: 0,
                    width: 0,
                    height: 0
                };
            }
        };
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CanvasMark, [
        {
            /**
   * @param {string} k
   * @param {any} v
   */ key: "_applyAttr",
            value: function _applyAttr(k, v) {
                var cmd = this._cmd;
                if (!cmd) return;
                var sk = STYLE_KEYS[k];
                if (sk !== void 0) cmd[sk] = v;
            }
        },
        {
            /**
   * @param {any} a
   * @param {any} [v]
   * @returns {any}
   */ key: "attr",
            value: function attr(a, v) {
                if (typeof a === "string") {
                    if (v === void 0) return null;
                    this._applyAttr(a, v);
                    return this;
                }
                for(var k in a){
                    if (a[k] !== void 0) this._applyAttr(k, a[k]);
                }
                return this;
            }
        },
        {
            /** @param {any} _c */ key: "add",
            value: function add(_c) {
                return this;
            }
        },
        {
            /** @param {any} _p */ key: "addTo",
            value: function addTo(_p) {
                return this;
            }
        },
        {
            key: "remove",
            value: function remove() {
                return this;
            }
        },
        {
            key: "clear",
            value: function clear() {
                return this;
            }
        },
        {
            /** @param {any} _s */ key: "css",
            value: function css(_s) {
                return this;
            }
        },
        {
            /** @param {any} v */ key: "fill",
            value: function fill(v) {
                if ((typeof v === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(v)) === "object") return this.attr(v);
                return this.attr("fill", v);
            }
        },
        {
            /** @param {any} v */ key: "stroke",
            value: function stroke(v) {
                if ((typeof v === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(v)) === "object") {
                    if (v.color !== void 0) this.attr("stroke", v.color);
                    if (v.width !== void 0) this.attr("stroke-width", v.width);
                    return this;
                }
                return this.attr("stroke", v);
            }
        },
        {
            /** @param {string} d */ key: "plot",
            value: function plot(d) {
                if (typeof d === "string" && this._cmd && this._cmd.tag === "path") {
                    this._cmd.d = d;
                }
                return this;
            }
        },
        {
            key: "hide",
            value: function hide() {
                return this;
            }
        },
        {
            key: "show",
            value: function show() {
                return this;
            }
        },
        {
            /** @param {string} _c */ key: "removeClass",
            value: function removeClass(_c) {
                return this;
            }
        },
        {
            key: "bbox",
            value: function bbox() {
                return {
                    x: 0,
                    y: 0,
                    width: 0,
                    height: 0
                };
            }
        },
        {
            key: "animate",
            value: function animate() {
                return NOOP_RUNNER;
            }
        }
    ]);
    return CanvasMark;
}();
var CanvasGraphics = /*#__PURE__*/ function() {
    "use strict";
    function CanvasGraphics(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CanvasGraphics);
        this.w = w;
        this._g = new Graphics(w);
        this._list = [];
        this._mx = new Float64Array(16);
        this._my = new Float64Array(16);
        this._msize = new Float64Array(16);
        this._mshape = new Int16Array(16);
        this._mstyle = new Int32Array(16);
        this._msi = new Int32Array(16);
        this._mn = 0;
        this._mcap = 16;
        this._crx = new Float64Array(16);
        this._cry = new Float64Array(16);
        this._crw = new Float64Array(16);
        this._crh = new Float64Array(16);
        this._crstyle = new Int32Array(16);
        this._crsi = new Int32Array(16);
        this._crdi = new Int32Array(16);
        this._crn = 0;
        this._crcap = 16;
        this._cellRadius = 0;
        this._styles = [];
        this._styleMap = /* @__PURE__ */ new Map();
        this._lf = NEVER;
        this._ls = NEVER;
        this._lsw = NEVER;
        this._ld = NEVER;
        this._lfo = NEVER;
        this._lso = NEVER;
        this._lid = -1;
        this._lofFill = NEVER;
        this._lofBase = -1;
        this._lofId = -1;
        this._rlf = NEVER;
        this._rls = NEVER;
        this._rlsw = NEVER;
        this._rlfo = NEVER;
        this._rlso = NEVER;
        this._rlid = -1;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CanvasGraphics, [
        {
            key: "_resetStyleCache",
            value: function _resetStyleCache() {
                this._lf = NEVER;
                this._ls = NEVER;
                this._lsw = NEVER;
                this._ld = NEVER;
                this._lfo = NEVER;
                this._lso = NEVER;
                this._lid = -1;
                this._lofFill = NEVER;
                this._lofBase = -1;
                this._lofId = -1;
                this._rlf = NEVER;
                this._rls = NEVER;
                this._rlsw = NEVER;
                this._rlfo = NEVER;
                this._rlso = NEVER;
                this._rlid = -1;
            }
        },
        {
            /** Start a fresh scene (columnar marker store + object-command list). */ key: "reset",
            value: function reset() {
                this._list = [];
                this._mn = 0;
                this._styles = [];
                this._styleMap = /* @__PURE__ */ new Map();
                this._resetStyleCache();
                var series = this.w.config.series || [];
                var cap = 16;
                for(var i = 0; i < series.length; i++){
                    var d = series[i] && series[i].data;
                    if (Array.isArray(d)) cap += d.length;
                }
                cap = Math.ceil(cap * 1.15) + 16;
                if (cap > this._mcap) this._allocMarkers(cap);
                this._crn = 0;
                this._cellRadius = 0;
                if (cap > this._crcap) this._allocRects(cap);
            }
        },
        {
            /** @param {number} cap */ key: "_allocRects",
            value: function _allocRects(cap) {
                this._crcap = cap;
                this._crx = new Float64Array(cap);
                this._cry = new Float64Array(cap);
                this._crw = new Float64Array(cap);
                this._crh = new Float64Array(cap);
                this._crstyle = new Int32Array(cap);
                this._crsi = new Int32Array(cap);
                this._crdi = new Int32Array(cap);
            }
        },
        {
            /** Grow the rect columns (rare: capacity estimate was low). */ key: "_growRects",
            value: function _growRects() {
                var cap = this._crcap * 2;
                var nx = new Float64Array(cap);
                nx.set(this._crx);
                this._crx = nx;
                var ny = new Float64Array(cap);
                ny.set(this._cry);
                this._cry = ny;
                var nw = new Float64Array(cap);
                nw.set(this._crw);
                this._crw = nw;
                var nh = new Float64Array(cap);
                nh.set(this._crh);
                this._crh = nh;
                var nst = new Int32Array(cap);
                nst.set(this._crstyle);
                this._crstyle = nst;
                var nsi = new Int32Array(cap);
                nsi.set(this._crsi);
                this._crsi = nsi;
                var ndi = new Int32Array(cap);
                ndi.set(this._crdi);
                this._crdi = ndi;
                this._crcap = cap;
            }
        },
        {
            /** @param {number} cap */ key: "_allocMarkers",
            value: function _allocMarkers(cap) {
                this._mcap = cap;
                this._mx = new Float64Array(cap);
                this._my = new Float64Array(cap);
                this._msize = new Float64Array(cap);
                this._mshape = new Int16Array(cap);
                this._mstyle = new Int32Array(cap);
                this._msi = new Int32Array(cap);
            }
        },
        {
            /** Grow the marker columns (rare: capacity estimate was low). */ key: "_growMarkers",
            value: function _growMarkers() {
                var cap = this._mcap * 2;
                var nx = new Float64Array(cap);
                nx.set(this._mx);
                this._mx = nx;
                var ny = new Float64Array(cap);
                ny.set(this._my);
                this._my = ny;
                var ns = new Float64Array(cap);
                ns.set(this._msize);
                this._msize = ns;
                var nsh = new Int16Array(cap);
                nsh.set(this._mshape);
                this._mshape = nsh;
                var nst = new Int32Array(cap);
                nst.set(this._mstyle);
                this._mstyle = nst;
                var nsi = new Int32Array(cap);
                nsi.set(this._msi);
                this._msi = nsi;
                this._mcap = cap;
            }
        },
        {
            key: "displayList",
            value: function displayList() {
                return this._list;
            }
        },
        {
            key: "markerCount",
            value: function markerCount() {
                return this._mn;
            }
        },
        {
            /**
   * Intern a marker style; returns its palette id. Keeps the per-point columns
   * numeric (no retained per-point object).
   * @param {any} fill @param {any} stroke @param {any} sw @param {any} dash
   * @param {any} fo @param {any} so
   * @returns {number}
   */ key: "_internStyle",
            value: function _internStyle(fill, stroke, sw, dash, fo, so) {
                var key = "".concat(fill, "|").concat(stroke, "|").concat(sw, "|").concat(dash, "|").concat(fo, "|").concat(so);
                var cached = this._styleMap.get(key);
                if (cached !== void 0) return cached;
                var id = this._styles.length;
                this._styles.push({
                    fill: fill,
                    stroke: stroke,
                    strokeWidth: sw,
                    strokeDash: dash,
                    fillOpacity: fo,
                    strokeOpacity: so
                });
                this._styleMap.set(key, id);
                return id;
            }
        },
        {
            /**
   * Override a recorded marker's fill (scatter sets a per-point fill via `attr`
   * right after drawMarker). Like the draw path, the string-key intern is cached
   * on (base style, fill) so the Map/string work stays OFF the per-point path
   * (it otherwise mixes with the `_mstyle[i]` typed-array write → the ~80× slow
   * path). For a scatter series the base + fill are uniform, so it interns once.
   * @param {number} i @param {any} fill
   */ key: "_setMarkerFill",
            value: function _setMarkerFill(i, fill) {
                var base = this._mstyle[i];
                if (fill === this._lofFill && base === this._lofBase) {
                    this._mstyle[i] = this._lofId;
                    return;
                }
                var s = this._styles[base];
                if (!s || s.fill === fill) {
                    this._lofFill = fill;
                    this._lofBase = base;
                    this._lofId = base;
                    return;
                }
                var id = this._internStyle(fill, s.stroke, s.strokeWidth, s.strokeDash, s.fillOpacity, s.strokeOpacity);
                this._mstyle[i] = id;
                this._lofFill = fill;
                this._lofBase = base;
                this._lofId = id;
            }
        },
        {
            /** @param {number} i @returns {any} the style object for a marker index */ key: "markerStyle",
            value: function markerStyle(i) {
                return this._styles[this._mstyle[i]];
            }
        },
        {
            /** @param {number} i @returns {number} series (realIndex) of a marker, -1 if none */ key: "markerSeries",
            value: function markerSeries(i) {
                return this._msi[i];
            }
        },
        {
            /** @param {number} id @returns {string} */ key: "shapeName",
            value: function shapeName(id) {
                return SHAPE_NAME[id] || "circle";
            }
        },
        {
            // ── columnar rect cell (heatmap): parallel unboxed arrays, no per-cell object ──
            /**
   * Record a heatmap-style cell (a filled, optionally stroked rect). Geometry
   * and style are captured up front into the columns; the returned handle is a
   * shared no-op (the emit site sets nothing back on it in canvas mode).
   * @param {number} x @param {number} y @param {number} w @param {number} h
   * @param {any} opts {fill, fillOpacity, stroke, strokeWidth, radius, seriesIndex, dataPointIndex}
   * @returns {any}
   */ key: "drawRectCell",
            value: function drawRectCell(x, y, w, h) {
                var opts = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : {};
                var styleId = this._rectStyleId(opts);
                if (this._crn >= this._crcap) this._growRects();
                var i = this._crn++;
                this._crx[i] = x || 0;
                this._cry[i] = y || 0;
                this._crw[i] = w > 0 ? w : 0;
                this._crh[i] = h > 0 ? h : 0;
                this._crstyle[i] = styleId;
                this._crsi[i] = opts.seriesIndex == null ? -1 : opts.seriesIndex;
                this._crdi[i] = opts.dataPointIndex == null ? -1 : opts.dataPointIndex;
                if (opts.radius) this._cellRadius = opts.radius;
                return SHARED_RECT_REF;
            }
        },
        {
            /**
   * Resolve (and dedupe) a rect-cell style → shared-palette id. A last-style
   * cache keeps the Map/string work off the path for runs of same-style cells.
   * @param {any} opts
   * @returns {number}
   */ key: "_rectStyleId",
            value: function _rectStyleId(opts) {
                var fill = opts.fill;
                var stroke = opts.stroke;
                var sw = opts.strokeWidth;
                var fo = opts.fillOpacity;
                var so = opts.strokeOpacity;
                if (fill === this._rlf && stroke === this._rls && sw === this._rlsw && fo === this._rlfo && so === this._rlso) {
                    return this._rlid;
                }
                var id = this._internStyle(fill, stroke, sw, 0, fo, so);
                this._rlf = fill;
                this._rls = stroke;
                this._rlsw = sw;
                this._rlfo = fo;
                this._rlso = so;
                this._rlid = id;
                return id;
            }
        },
        {
            /** @returns {number} number of recorded rect cells */ key: "rectCount",
            value: function rectCount() {
                return this._crn;
            }
        },
        {
            /** @param {number} i @returns {any} the style object for a rect cell */ key: "rectStyle",
            value: function rectStyle(i) {
                return this._styles[this._crstyle[i]];
            }
        },
        {
            /** @param {number} i @returns {number} series (realIndex) of a cell, -1 if none */ key: "rectSeries",
            value: function rectSeries(i) {
                return this._crsi[i];
            }
        },
        {
            /**
   * @param {string} tag
   * @param {number} z
   * @returns {any}
   */ key: "_cmd",
            value: function _cmd(tag, z) {
                var cmd = {
                    tag: tag,
                    z: z || 0,
                    fill: void 0,
                    stroke: void 0,
                    strokeWidth: void 0,
                    strokeDash: void 0,
                    lineCap: void 0,
                    fillOpacity: void 0,
                    strokeOpacity: void 0,
                    fillRule: void 0
                };
                this._list.push(cmd);
                return cmd;
            }
        },
        {
            // ── organizational (groups don't paint or record) ──
            // Series draw() creates a wrap group PER POINT (scatter.draw / plotChartMarkers
            // run per point), so allocating a handle per group is ~50k heavy allocations
            // at scale: enough transient churn to tip V8 into a GC blow-up. Groups carry
            // no paint state in canvas mode (attr/add are no-ops), so every group shares
            // one singleton: zero per-point allocation.
            /** @param {any} _attrs */ key: "group",
            value: function group(_attrs) {
                return SHARED_GROUP;
            }
        },
        {
            // ── per-point marker (line/area markers, scatter, bubble): COLUMNAR ──
            /**
   * @param {number} x
   * @param {number} y
   * @param {any} opts
   */ key: "drawMarker",
            value: function drawMarker(x, y) {
                var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
                var _a;
                var styleId = this._markerStyleId(opts);
                if (this._mn >= this._mcap) this._growMarkers();
                var i = this._mn++;
                this._mx[i] = x || 0;
                this._my[i] = typeof y === "number" ? y : NaN;
                this._msize[i] = opts.pSize || 0;
                this._mshape[i] = (_a = SHAPE_ID[opts.shape || "circle"]) != null ? _a : 0;
                this._mstyle[i] = styleId;
                this._msi[i] = opts.seriesIndex == null ? -1 : opts.seriesIndex;
                return new CanvasMarkerRef(this, i);
            }
        },
        {
            /**
   * Resolve (and dedupe) a marker style → palette id. A last-style cache keeps
   * the Map/string work off the path when consecutive markers share a style
   * (the common case), so intern runs once per style run.
   * @param {any} opts
   * @returns {number}
   */ key: "_markerStyleId",
            value: function _markerStyleId(opts) {
                var shape = opts.shape || "circle";
                var strokeTinted = shape === "line" || shape === "plus" || shape === "cross";
                var fill = strokeTinted ? "none" : opts.pointFillColor;
                var stroke = strokeTinted ? opts.pointFillColor : opts.pointStrokeColor;
                var sw = opts.pointStrokeWidth;
                var dash = opts.pointStrokeDashArray;
                var fo = opts.pointFillOpacity;
                var so = strokeTinted ? opts.pointFillOpacity : opts.pointStrokeOpacity;
                if (fill === this._lf && stroke === this._ls && sw === this._lsw && dash === this._ld && fo === this._lfo && so === this._lso) {
                    return this._lid;
                }
                var id = this._internStyle(fill, stroke, sw, dash, fo, so);
                this._lf = fill;
                this._ls = stroke;
                this._lsw = sw;
                this._ld = dash;
                this._lfo = fo;
                this._lso = so;
                this._lid = id;
                return id;
            }
        },
        {
            // ── series body path (line/area/bar): object command ──
            /** @param {any} opts */ key: "renderPaths",
            value: function renderPaths(opts) {
                var cmd = this._cmd("path", opts.realIndex);
                cmd.d = opts.pathTo;
                if (opts.pathToNumeric) {
                    cmd.nxs = opts.pathToNumeric.xs;
                    cmd.nys = opts.pathToNumeric.ys;
                    cmd.ncloseY = opts.pathToNumeric.closeY;
                }
                cmd.stroke = opts.stroke;
                cmd.strokeWidth = opts.strokeWidth;
                cmd.fill = opts.fill;
                cmd.lineCap = opts.strokeLinecap;
                cmd.si = opts.realIndex;
                this.w.globals.animationEnded = true;
                return new CanvasMark(cmd);
            }
        },
        {
            /** @param {any} opts */ key: "drawPath",
            value: function drawPath(opts) {
                var cmd = this._cmd("path", 0);
                cmd.d = opts.d;
                cmd.stroke = opts.stroke;
                cmd.strokeWidth = opts.strokeWidth;
                cmd.fill = opts.fill;
                cmd.fillOpacity = opts.fillOpacity;
                cmd.strokeOpacity = opts.strokeOpacity;
                cmd.lineCap = opts.strokeLinecap;
                cmd.strokeDash = opts.strokeDashArray;
                return new CanvasMark(cmd);
            }
        },
        {
            // ── remaining primitives (contract completeness / Marks #11 forward-compat) ──
            /**
   * @param {number} x1 @param {number} y1 @param {number} x2 @param {number} y2
   * @param {string} lineColor @param {any} dashArray @param {number} strokeWidth
   */ key: "drawLine",
            value: function drawLine(x1, y1, x2, y2) {
                var lineColor = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : "#a8a8a8", dashArray = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : 0, strokeWidth = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : 1;
                var cmd = this._cmd("line", 0);
                cmd.lx1 = x1;
                cmd.ly1 = y1;
                cmd.lx2 = x2;
                cmd.ly2 = y2;
                cmd.stroke = lineColor;
                cmd.strokeDash = dashArray;
                cmd.strokeWidth = strokeWidth;
                return new CanvasMark(cmd);
            }
        },
        {
            /**
   * Mirrors Graphics.drawRect's full signature (including stroke), so callers
   * that stroke rects (Marks api.rect, chart code) paint the same on canvas.
   * @param {number} x1 @param {number} y1 @param {number} x2 @param {number} y2
   * @param {number} radius @param {string} color @param {number} opacity
   * @param {number|null} [strokeWidth] @param {string|null} [strokeColor]
   * @param {any} [strokeDashArray]
   */ key: "drawRect",
            value: function drawRect() {
                var x1 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0, y1 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, x2 = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0, y2 = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0, radius = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 0, color = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : "#fefefe", opacity = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : 1, strokeWidth = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : null, strokeColor = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : null, strokeDashArray = arguments.length > 9 && arguments[9] !== void 0 ? arguments[9] : 0;
                var cmd = this._cmd("rect", 0);
                cmd.x1 = x1;
                cmd.y1 = y1;
                cmd.rw = x2 > 0 ? x2 : 0;
                cmd.rh = y2 > 0 ? y2 : 0;
                cmd.radius = radius;
                cmd.fill = color;
                cmd.fillOpacity = opacity;
                if (strokeColor != null) {
                    cmd.stroke = strokeColor;
                    cmd.strokeWidth = strokeWidth == null ? 1 : strokeWidth;
                    cmd.strokeDash = strokeDashArray;
                }
                return new CanvasMark(cmd);
            }
        },
        {
            /**
   * @param {number} radius
   * @param {any} attrs
   */ key: "drawCircle",
            value: function drawCircle(radius) {
                var attrs = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
                var cmd = this._cmd("circle", 0);
                cmd.r = radius < 0 ? 0 : radius;
                if (attrs) {
                    cmd.cx = attrs.cx;
                    cmd.cy = attrs.cy;
                    if (attrs.fill !== void 0) cmd.fill = attrs.fill;
                    if (attrs.stroke !== void 0) cmd.stroke = attrs.stroke;
                }
                return new CanvasMark(cmd);
            }
        },
        {
            /** @param {any} opts */ key: "drawText",
            value: function drawText(opts) {
                var cmd = this._cmd("text", 0);
                cmd.text = Array.isArray(opts.text) ? opts.text.join(" ") : opts.text;
                cmd.tx = opts.x;
                cmd.ty = opts.y;
                cmd.textAnchor = opts.textAnchor || "start";
                cmd.fontSize = opts.fontSize;
                cmd.fontFamily = opts.fontFamily;
                cmd.fill = opts.foreColor;
                return new CanvasMark(cmd);
            }
        },
        {
            /**
   * Resolve a marker's SVG path `d` (non-circle shapes) lazily at paint time.
   * @param {number} x @param {number} y @param {number} shapeId @param {number} size
   * @returns {string}
   */ key: "markerPath",
            value: function markerPath(x, y, shapeId, size) {
                return this._g.getMarkerPath(x, y, SHAPE_NAME[shapeId] || "circle", size);
            }
        }
    ]);
    return CanvasGraphics;
}();
var TWO_PI = Math.PI * 2;
var DPR_CAP = 2;
var CanvasCompositor = /*#__PURE__*/ function() {
    "use strict";
    function CanvasCompositor(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CanvasCompositor);
        this.w = w;
        this._host = null;
        this._canvas = null;
        this._c2d = null;
        this._margin = 0;
        this._dpr = 1;
        this._dim = null;
        this._alpha = 1;
        this._unitPaths = /* @__PURE__ */ new Map();
        this._markerBatches = 0;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CanvasCompositor, [
        {
            /** Marker style-batches applied during the last paint() (dev/test hook). */ key: "markerBatchCount",
            value: function markerBatchCount() {
                return this._markerBatches;
            }
        },
        {
            /**
   * Opacity multiplier for a series index under the active dim spec: 1 for the
   * highlighted series (or when not dimming, or for unidentified marks), else
   * the inactive opacity.
   * @param {number} si
   * @returns {number}
   */ key: "_seriesAlpha",
            value: function _seriesAlpha(si) {
                var d = this._dim;
                if (!d || d.active == null || d.active < 0 || si == null || si < 0) return 1;
                return si === d.active ? 1 : d.opacity == null ? 0.2 : d.opacity;
            }
        },
        {
            key: "_plotDims",
            value: function _plotDims() {
                var _a;
                var gw = Math.max(0, Math.ceil(this.w.layout.gridWidth || 0));
                var gh = Math.max(0, Math.ceil(this.w.layout.gridHeight || 0));
                var largest = ((_a = this.w.globals.markers) == null ? void 0 : _a.largestSize) || 0;
                var margin = Math.ceil(largest + 8);
                return {
                    gw: gw,
                    gh: gh,
                    margin: margin
                };
            }
        },
        {
            /**
   * Create (or recreate) the foreignObject + canvas sized to the plot rect and
   * return the SVGElement host that `plotChartType` inserts into the tree.
   * @returns {any}
   */ key: "createHost",
            value: function createHost() {
                var win = BrowserAPIs.getWindow();
                this._dpr = Math.min(DPR_CAP, win && win.devicePixelRatio || 1);
                var _this__plotDims = this._plotDims(), gw = _this__plotDims.gw, gh = _this__plotDims.gh, margin = _this__plotDims.margin;
                this._margin = margin;
                var w = gw + margin * 2;
                var h = gh + margin * 2;
                var fo = BrowserAPIs.createElementNS(SVGNS$2, "foreignObject");
                fo.setAttribute("x", String(-margin));
                fo.setAttribute("y", String(-margin));
                fo.setAttribute("width", String(w));
                fo.setAttribute("height", String(h));
                fo.setAttribute("class", "apexcharts-canvas-series");
                fo.style.overflow = "visible";
                var canvas = /** @type {any} */ BrowserAPIs.createElement("canvas");
                canvas.setAttribute("class", "apexcharts-series-canvas");
                canvas.width = Math.max(1, Math.round(w * this._dpr));
                canvas.height = Math.max(1, Math.round(h * this._dpr));
                canvas.style.width = w + "px";
                canvas.style.height = h + "px";
                canvas.style.pointerEvents = "none";
                fo.appendChild(canvas);
                this._canvas = canvas;
                this._c2d = canvas.getContext("2d");
                this._host = new SVGElement(fo);
                return this._host;
            }
        },
        {
            key: "getHost",
            value: function getHost() {
                return this._host;
            }
        },
        {
            key: "clear",
            value: function clear() {
                if (!this._c2d || !this._canvas) return;
                this._c2d.setTransform(1, 0, 0, 1, 0, 0);
                this._c2d.clearRect(0, 0, this._canvas.width, this._canvas.height);
            }
        },
        {
            /**
   * Re-check devicePixelRatio before painting: a restyle() repaint after the
   * window moved between monitors would otherwise keep the stale backing-store
   * scale (blurry or over-sized) until the next full render rebuilds the host.
   * The canvas CSS size is unchanged; only the backing store is resized.
   */ key: "_syncDpr",
            value: function _syncDpr() {
                if (!this._canvas) return;
                var win = BrowserAPIs.getWindow();
                var dpr = Math.min(DPR_CAP, win && win.devicePixelRatio || 1);
                if (dpr === this._dpr) return;
                this._dpr = dpr;
                var wCss = parseFloat(this._canvas.style.width) || 0;
                var hCss = parseFloat(this._canvas.style.height) || 0;
                this._canvas.width = Math.max(1, Math.round(wCss * dpr));
                this._canvas.height = Math.max(1, Math.round(hCss * dpr));
            }
        },
        {
            /**
   * Paint the recorded scene: object commands (series bodies / rects / lines /
   * text) first, then the columnar markers on top (matching SVG z-order where
   * markers sit above the series path). `shim` supplies the columnar marker
   * arrays + lazy non-circle marker geometry.
   * @param {any[]} list
   * @param {any} shim
   * @param {{active:number, opacity:number}|null} [dim] per-series dim spec
   *   (hover / legend restyle); null repaints at full opacity.
   */ key: "paint",
            value: function paint(list, shim2) {
                var dim = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : null;
                var ctx = this._c2d;
                if (!ctx) return;
                this._dim = dim || null;
                this._syncDpr();
                this.clear();
                var dpr = this._dpr;
                var m = this._margin;
                ctx.setTransform(dpr, 0, 0, dpr, m * dpr, m * dpr);
                if (list.length) {
                    var ordered = list.length > 1 ? list.map(function(c, i) {
                        return [
                            c,
                            i
                        ];
                    }).sort(function(a, b) {
                        return a[0].z === b[0].z ? a[1] - b[1] : a[0].z - b[0].z;
                    }).map(function(pair) {
                        return pair[0];
                    }) : list;
                    for(var i = 0; i < ordered.length; i++){
                        var c = ordered[i];
                        this._alpha = this._dim ? this._seriesAlpha(c.si) : 1;
                        this._paintOne(ctx, c);
                    }
                }
                this._paintRects(ctx, shim2);
                this._paintMarkers(ctx, shim2);
                this._alpha = 1;
            }
        },
        {
            /**
   * Paint the columnar rect cells (heatmap) as STYLE BATCHES: one fill/stroke
   * state application per run of consecutive same-style cells, then a fast
   * fillRect (or a roundRect path when the shared corner radius is non-zero)
   * per cell. Clipped to the plot rect so cells never bleed into the canvas
   * margin (mirrors the SVG gridRectMask). Per-cell globalAlpha carries the
   * hover/legend dim multiplier when a dim spec is active.
   * @param {any} ctx
   * @param {any} shim
   */ key: "_paintRects",
            value: function _paintRects(ctx, shim2) {
                var n = shim2.rectCount ? shim2.rectCount() : 0;
                if (!n) return;
                var rx = shim2._crx;
                var ry = shim2._cry;
                var rw = shim2._crw;
                var rh = shim2._crh;
                var rstyle = shim2._crstyle;
                var radius = shim2._cellRadius || 0;
                var cx = /** @type {any} */ ctx;
                var useRound = radius > 0 && typeof cx.roundRect === "function";
                var dimming = !!this._dim;
                var gw = Math.max(0, this.w.layout.gridWidth || 0);
                var gh = Math.max(0, this.w.layout.gridHeight || 0);
                ctx.save();
                ctx.beginPath();
                ctx.rect(0, 0, gw, gh);
                ctx.clip();
                var i = 0;
                while(i < n){
                    var styleId = rstyle[i];
                    var style = shim2.rectStyle(i);
                    if (!style) {
                        i++;
                        continue;
                    }
                    var fill = style.fill;
                    var doFill = fill && fill !== "none" && !(typeof fill === "string" && fill.indexOf("url(") === 0);
                    var stroke = style.stroke;
                    var sw = style.strokeWidth == null ? 0 : Number(style.strokeWidth);
                    var doStroke = stroke && stroke !== "none" && sw > 0 && !(typeof stroke === "string" && stroke.indexOf("url(") === 0);
                    if (doFill) ctx.fillStyle = fill;
                    if (doStroke) {
                        ctx.strokeStyle = stroke;
                        ctx.lineWidth = sw;
                        ctx.setLineDash([]);
                    }
                    var baseFillA = style.fillOpacity == null ? 1 : Number(style.fillOpacity);
                    var baseStrokeA = style.strokeOpacity == null ? 1 : Number(style.strokeOpacity);
                    var j = i;
                    while(j < n && rstyle[j] === styleId){
                        var w = rw[j];
                        var h = rh[j];
                        if (w > 0 && h > 0) {
                            var f = dimming ? this._seriesAlpha(shim2.rectSeries(j)) : 1;
                            if (useRound) {
                                ctx.beginPath();
                                cx.roundRect(rx[j], ry[j], w, h, radius);
                                if (doFill) {
                                    ctx.globalAlpha = baseFillA * f;
                                    ctx.fill();
                                }
                                if (doStroke) {
                                    ctx.globalAlpha = baseStrokeA * f;
                                    ctx.stroke();
                                }
                            } else {
                                if (doFill) {
                                    ctx.globalAlpha = baseFillA * f;
                                    ctx.fillRect(rx[j], ry[j], w, h);
                                }
                                if (doStroke) {
                                    ctx.globalAlpha = baseStrokeA * f;
                                    ctx.strokeRect(rx[j], ry[j], w, h);
                                }
                            }
                        }
                        j++;
                    }
                    i = j;
                }
                ctx.globalAlpha = 1;
                ctx.restore();
            }
        },
        {
            /**
   * Reusable unit Path2D for a (shape, size): the shape's geometry built at the
   * origin once, then translated per marker via setTransform. Returns null when
   * the geometry string cannot be parsed.
   * @param {any} shim
   * @param {number} shapeId
   * @param {number} size
   * @returns {any}
   */ key: "_unitPath",
            value: function _unitPath(shim2, shapeId, size) {
                var key = shapeId + "|" + size;
                var p = this._unitPaths.get(key);
                if (p === void 0) {
                    try {
                        p = new Path2D(shim2.markerPath(0, 0, shapeId, size));
                    } catch (e) {
                        p = null;
                    }
                    this._unitPaths.set(key, p);
                }
                return p;
            }
        },
        {
            /**
   * Markers paint as STYLE BATCHES: one fill/stroke state application per run
   * of consecutive same-style markers (a uniform single-series scatter is
   * exactly one batch), then per-marker geometry inside the run. Per-marker
   * geometry stays painter's-ordered (fill+stroke per marker) so overlapping
   * semi-transparent markers composite exactly as SVG does.
   * @param {any} ctx
   * @param {any} shim
   */ key: "_paintMarkers",
            value: function _paintMarkers(ctx, shim2) {
                this._markerBatches = 0;
                var n = shim2.markerCount();
                if (!n) return;
                var mx = shim2._mx;
                var my = shim2._my;
                var msize = shim2._msize;
                var mshape = shim2._mshape;
                var mstyle = shim2._mstyle;
                var dimming = !!this._dim;
                if (!dimming) this._alpha = 1;
                var i = 0;
                while(i < n){
                    var styleId = mstyle[i];
                    var shapeId = mshape[i];
                    var style = shim2.markerStyle(i);
                    if (!style) {
                        i++;
                        continue;
                    }
                    var doFill = this._applyFill(ctx, style);
                    var doStroke = this._applyStroke(ctx, style);
                    this._markerBatches++;
                    var baseFillA = style.fillOpacity == null ? 1 : Number(style.fillOpacity);
                    var baseStrokeA = style.strokeOpacity == null ? 1 : Number(style.strokeOpacity);
                    if (shapeId === 0) {
                        var j = i;
                        while(j < n && mshape[j] === 0 && mstyle[j] === styleId){
                            var r = msize[j] || 0;
                            var y = my[j];
                            if (r > 0 && y === y) {
                                ctx.beginPath();
                                ctx.arc(mx[j], y, r, 0, TWO_PI);
                                if (dimming) {
                                    var f = this._seriesAlpha(shim2.markerSeries(j));
                                    if (doFill) {
                                        ctx.globalAlpha = baseFillA * f;
                                        ctx.fill();
                                    }
                                    if (doStroke) {
                                        ctx.globalAlpha = baseStrokeA * f;
                                        ctx.stroke();
                                    }
                                } else {
                                    if (doFill) ctx.fill();
                                    if (doStroke) ctx.stroke();
                                }
                            }
                            j++;
                        }
                        ctx.globalAlpha = 1;
                        i = j;
                    } else {
                        var dpr = this._dpr;
                        var m = this._margin;
                        var j1 = i;
                        while(j1 < n && mshape[j1] === shapeId && mstyle[j1] === styleId){
                            var y1 = my[j1];
                            var size = msize[j1];
                            if (y1 === y1 && size > 0) {
                                var p = this._unitPath(shim2, shapeId, size);
                                if (p) {
                                    ctx.setTransform(dpr, 0, 0, dpr, (m + mx[j1]) * dpr, (m + y1) * dpr);
                                    var f1 = dimming ? this._seriesAlpha(shim2.markerSeries(j1)) : 1;
                                    if (doFill) {
                                        ctx.globalAlpha = baseFillA * f1;
                                        ctx.fill(p);
                                    }
                                    if (doStroke) {
                                        ctx.globalAlpha = baseStrokeA * f1;
                                        ctx.stroke(p);
                                    }
                                }
                            }
                            j1++;
                        }
                        ctx.setTransform(dpr, 0, 0, dpr, m * dpr, m * dpr);
                        ctx.globalAlpha = 1;
                        i = j1;
                    }
                }
            }
        },
        {
            /**
   * Paint a series path from its numeric fast-path coords: a direct
   * moveTo/lineTo loop over the typed arrays, no Path2D and no d-string
   * parse. `ncloseY` (areas) closes the polygon down to the baseline exactly
   * like the string form's `L xLast bottom L x0 bottom z` tail.
   * @param {any} ctx
   * @param {any} cmd
   */ key: "_paintNumericPath",
            value: function _paintNumericPath(ctx, cmd) {
                var xs = cmd.nxs;
                var ys = cmd.nys;
                var n = xs.length;
                if (!n) return;
                ctx.beginPath();
                ctx.moveTo(xs[0], ys[0]);
                for(var k = 1; k < n; k++){
                    ctx.lineTo(xs[k], ys[k]);
                }
                if (cmd.ncloseY != null) {
                    ctx.lineTo(xs[n - 1], cmd.ncloseY);
                    ctx.lineTo(xs[0], cmd.ncloseY);
                    ctx.closePath();
                }
                if (this._applyFill(ctx, cmd)) {
                    ctx.fill(cmd.fillRule === "evenodd" ? "evenodd" : "nonzero");
                }
                if (this._applyStroke(ctx, cmd)) {
                    ctx.stroke();
                }
                ctx.globalAlpha = 1;
            }
        },
        {
            /**
   * @param {any} ctx
   * @param {any} cmd style-bearing flat command
   */ key: "_paintOne",
            value: function _paintOne(ctx, cmd) {
                switch(cmd.tag){
                    case "path":
                        {
                            if (cmd.nxs) {
                                this._paintNumericPath(ctx, cmd);
                                break;
                            }
                            if (!cmd.d) return;
                            if (!cmd.path2d) {
                                try {
                                    cmd.path2d = new Path2D(cmd.d);
                                } catch (e) {
                                    return;
                                }
                            }
                            this._fillStrokePath(ctx, cmd, cmd.path2d);
                            break;
                        }
                    case "rect":
                        {
                            var p = new Path2D();
                            if (cmd.radius && typeof /** @type {any} */ p.roundRect === "function") {
                                p.roundRect(cmd.x1, cmd.y1, cmd.rw, cmd.rh, cmd.radius);
                            } else {
                                p.rect(cmd.x1, cmd.y1, cmd.rw, cmd.rh);
                            }
                            this._fillStrokePath(ctx, cmd, p);
                            break;
                        }
                    case "circle":
                        {
                            if (!(cmd.r > 0)) return;
                            ctx.beginPath();
                            ctx.arc(cmd.cx, cmd.cy, cmd.r, 0, TWO_PI);
                            this._fillStroke(ctx, cmd);
                            break;
                        }
                    case "line":
                        {
                            ctx.beginPath();
                            ctx.moveTo(cmd.lx1, cmd.ly1);
                            ctx.lineTo(cmd.lx2, cmd.ly2);
                            this._strokeOnly(ctx, cmd);
                            break;
                        }
                    case "text":
                        {
                            if (cmd.text == null) return;
                            ctx.save();
                            ctx.globalAlpha = this._alpha;
                            ctx.fillStyle = cmd.fill || "#000";
                            var size = cmd.fontSize || "11px";
                            ctx.font = "".concat(typeof size === "number" ? size + "px" : size, " ").concat(cmd.fontFamily || "Helvetica, Arial, sans-serif");
                            ctx.textAlign = cmd.textAnchor === "middle" ? "center" : cmd.textAnchor === "end" ? "right" : "left";
                            ctx.fillText(String(cmd.text), cmd.tx, cmd.ty);
                            ctx.restore();
                            break;
                        }
                }
            }
        },
        {
            /**
   * @param {any} ctx
   * @param {any} style
   * @param {any} path2d
   */ key: "_fillStrokePath",
            value: function _fillStrokePath(ctx, style, path2d) {
                if (this._applyFill(ctx, style)) {
                    ctx.fill(path2d, style.fillRule === "evenodd" ? "evenodd" : "nonzero");
                }
                if (this._applyStroke(ctx, style)) {
                    ctx.stroke(path2d);
                }
                ctx.globalAlpha = 1;
            }
        },
        {
            /**
   * @param {any} ctx
   * @param {any} style
   */ key: "_fillStroke",
            value: function _fillStroke(ctx, style) {
                if (this._applyFill(ctx, style)) ctx.fill();
                if (this._applyStroke(ctx, style)) ctx.stroke();
                ctx.globalAlpha = 1;
            }
        },
        {
            /**
   * @param {any} ctx
   * @param {any} style
   */ key: "_strokeOnly",
            value: function _strokeOnly(ctx, style) {
                if (this._applyStroke(ctx, style)) ctx.stroke();
                ctx.globalAlpha = 1;
            }
        },
        {
            /**
   * Set fill state. Returns false when there's nothing to fill.
   * @param {any} ctx
   * @param {any} style
   */ key: "_applyFill",
            value: function _applyFill(ctx, style) {
                var fill = style.fill;
                if (!fill || fill === "none") return false;
                if (typeof fill === "string" && fill.indexOf("url(") === 0) return false;
                ctx.globalAlpha = (style.fillOpacity == null ? 1 : Number(style.fillOpacity)) * this._alpha;
                ctx.fillStyle = fill;
                return true;
            }
        },
        {
            /**
   * Set stroke state. Returns false when there's nothing to stroke.
   * @param {any} ctx
   * @param {any} style
   */ key: "_applyStroke",
            value: function _applyStroke(ctx, style) {
                var stroke = style.stroke;
                var sw = style.strokeWidth == null ? 1 : Number(style.strokeWidth);
                if (!stroke || stroke === "none" || !(sw > 0)) return false;
                if (typeof stroke === "string" && stroke.indexOf("url(") === 0) return false;
                ctx.globalAlpha = (style.strokeOpacity == null ? 1 : Number(style.strokeOpacity)) * this._alpha;
                ctx.strokeStyle = stroke;
                ctx.lineWidth = sw;
                ctx.lineCap = style.lineCap || "butt";
                var dash = style.strokeDash;
                if (dash && dash !== 0) {
                    ctx.setLineDash(Array.isArray(dash) ? dash : [
                        Number(dash)
                    ]);
                } else {
                    ctx.setLineDash([]);
                }
                return true;
            }
        },
        {
            /** Series bitmap for the export composite bridge (P4). @returns {string|null} */ key: "toDataURL",
            value: function toDataURL() {
                return this._canvas ? this._canvas.toDataURL() : null;
            }
        },
        {
            key: "destroy",
            value: function destroy() {
                this._host = null;
                this._canvas = null;
                this._c2d = null;
                this._unitPaths.clear();
            }
        }
    ]);
    return CanvasCompositor;
}();
var CanvasRenderer = /*#__PURE__*/ function() {
    "use strict";
    function CanvasRenderer(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CanvasRenderer);
        this.w = w;
        this.ctx = ctx;
        this.kind = "canvas";
        this._g = new CanvasGraphics(w);
        this._compositor = new CanvasCompositor(w);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CanvasRenderer, [
        {
            // ── lifecycle ──
            /** Start a fresh series display list for this render pass. */ key: "beginSeries",
            value: function beginSeries() {
                this._g.reset();
            }
        },
        {
            /**
   * Finalize the pass: paint the recorded display list and return the SVG host
   * (a `<foreignObject><canvas>`) for `plotChartType` to composite into the tree.
   * @returns {any}
   */ key: "present",
            value: function present() {
                var host = this._compositor.createHost();
                this._compositor.paint(this._g.displayList(), this._g);
                return host;
            }
        },
        {
            /** Fast-path wipe of the series layer. */ key: "clear",
            value: function clear() {
                this._compositor.clear();
            }
        },
        {
            /**
   * Whether the existing <canvas> host can be repainted in place (it exists
   * and is still mounted). Used by the data-only fast update path to skip
   * recreating the foreignObject + backing store on every tick.
   * @returns {boolean}
   */ key: "canRepaintInPlace",
            value: function canRepaintInPlace() {
                var host = this._compositor.getHost();
                return !!(host && host.node && host.node.isConnected);
            }
        },
        {
            /** Repaint the freshly recorded display list into the EXISTING canvas. */ key: "repaintInPlace",
            value: function repaintInPlace() {
                this._compositor.paint(this._g.displayList(), this._g);
            }
        },
        {
            // ── emit primitives (delegate to the display-list shim) ──
            /** @param {any} attrs */ key: "group",
            value: function group(attrs) {
                return this._g.group(attrs);
            }
        },
        {
            /** @param {any} opts */ key: "drawPath",
            value: function drawPath(opts) {
                return this._g.drawPath(opts);
            }
        },
        {
            /** @param {any[]} args */ key: "drawLine",
            value: function drawLine() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                var /** @type {any} */ _this__g;
                return (_this__g = this._g).drawLine.apply(_this__g, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(args));
            }
        },
        {
            /** @param {any[]} args */ key: "drawRect",
            value: function drawRect() {
                for(var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++){
                    args[_key] = arguments[_key];
                }
                var /** @type {any} */ _this__g;
                return (_this__g = this._g).drawRect.apply(_this__g, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(args));
            }
        },
        {
            /**
   * Columnar heatmap-cell rect (dense same-shape rects): recorded into typed
   * arrays, painted as style batches. Distinct from drawRect (object command)
   * so 100k cells don't allocate 100k retained commands.
   * @param {number} x @param {number} y @param {number} w @param {number} h
   * @param {any} opts
   */ key: "drawRectCell",
            value: function drawRectCell(x, y, w, h, opts) {
                return this._g.drawRectCell(x, y, w, h, opts);
            }
        },
        {
            /**
   * @param {number} r
   * @param {any} attrs
   */ key: "drawCircle",
            value: function drawCircle(r, attrs) {
                return this._g.drawCircle(r, attrs);
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {any} opts
   */ key: "drawMarker",
            value: function drawMarker(x, y, opts) {
                return this._g.drawMarker(x, y, opts);
            }
        },
        {
            /** @param {any} opts */ key: "renderPaths",
            value: function renderPaths(opts) {
                return this._g.renderPaths(opts);
            }
        },
        {
            /** @param {any} opts */ key: "drawText",
            value: function drawText(opts) {
                return this._g.drawText(opts);
            }
        },
        {
            // ── capabilities ──
            /** @param {string} feature */ key: "supports",
            value: function supports(feature) {
                return feature === "solidFill" || feature === "dashArray";
            }
        },
        {
            // ── interaction ──
            // Line/area/bar/scatter tooltips resolve via coordinate lookup (pointsArray),
            // so those need no per-mark query. Heatmap cells, however, are hovered by
            // point (the SVG path hit-tests the <rect> under the cursor); with cells on
            // canvas there is no node, so hitTest resolves the columnar rect store.
            /**
   * Find the cell under a plot-local point (0,0 = plot origin, the same space
   * as the recorded cell geometry). Reverse scan so a later-painted cell wins
   * when cells overlap (continuous-x edges). A linear scan stays well under a
   * frame even at 100k cells (~100k integer compares). Returns the cell's
   * series/dataPoint index plus its geometry for tooltip positioning, or null
   * when the point is off every cell.
   * @param {number} px
   * @param {number} py
   * @returns {({seriesIndex:number,dataPointIndex:number,x:number,y:number,width:number,height:number})|null}
   */ key: "hitTest",
            value: function hitTest(px, py) {
                var g = this._g;
                var n = g.rectCount ? g.rectCount() : 0;
                if (!n) return null;
                var rx = g._crx;
                var ry = g._cry;
                var rw = g._crw;
                var rh = g._crh;
                for(var k = n - 1; k >= 0; k--){
                    var w = rw[k];
                    var h = rh[k];
                    if (w <= 0 || h <= 0) continue;
                    if (px >= rx[k] && px < rx[k] + w && py >= ry[k] && py < ry[k] + h) {
                        return {
                            seriesIndex: g._crsi[k],
                            dataPointIndex: g._crdi[k],
                            x: rx[k],
                            y: ry[k],
                            width: w,
                            height: h
                        };
                    }
                }
                return null;
            }
        },
        {
            /**
   * Repaint the retained series scene with a per-series dim spec (hover /
   * legend restyle). No geometry recompute: reuses the display list + marker
   * columns recorded at render time. Pass null to repaint at full opacity.
   * @param {{active:number, opacity:number}|null} [dim]
   */ key: "restyle",
            value: function restyle(dim) {
                this._compositor.paint(this._g.displayList(), this._g, dim || null);
            }
        },
        {
            // ── export ── toBitmap() and the compositor's toDataURL() back
            //    Exports.inlineCanvasLayers, which inlines the series bitmap as an SVG
            //    <image> so PNG/SVG export includes the canvas layer in correct z-order.
            /** @returns {{dataURL:string,x:number,y:number,w:number,h:number}|null} */ key: "toBitmap",
            value: function toBitmap() {
                var url = this._compositor.toDataURL();
                if (!url) return null;
                var gl = this.w.globals;
                var cfg = this.w.config.chart;
                var margin = this._compositor._margin;
                return {
                    dataURL: url,
                    x: (gl.translateX || 0) + (cfg.offsetX || 0) - margin,
                    y: (gl.translateY || 0) + (cfg.offsetY || 0) - margin,
                    w: (this.w.layout.gridWidth || 0) + margin * 2,
                    h: (this.w.layout.gridHeight || 0) + margin * 2
                };
            }
        },
        {
            key: "destroy",
            value: function destroy() {
                this._compositor.destroy();
            }
        }
    ]);
    return CanvasRenderer;
}();
ApexCharts.registerRenderer("canvas", /**
   * @param {any} w
   * @param {any} ctx
   */ function(w, ctx) {
    return new CanvasRenderer(w, ctx);
});
function makeCustomSeriesClass(name2, def) {
    var cls = /*#__PURE__*/ function() {
        "use strict";
        function CustomSeries(w, ctx, xyRatios) {
            (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CustomSeries);
            this.w = w;
            this.ctx = ctx;
            this.xyRatios = xyRatios;
            this._warned = false;
        }
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CustomSeries, [
            {
                /**
     * @param {any[]} series parsed y-arrays (one per drawn series)
     * @param {string} [_ctype]
     * @param {number[]} [seriesIndices] realIndex per entry (combo dispatch)
     * @returns {any} the wrap group
     */ key: "draw",
                value: function draw(series, _ctype, seriesIndices) {
                    var _this = this;
                    var _a, _b;
                    var w = this.w;
                    var graphics = new Graphics(w, this.ctx);
                    var emit = seriesEmitter(this.ctx, graphics);
                    var ret = graphics.group({
                        class: "apexcharts-marks-series"
                    });
                    series.forEach(function(_s, idx) {
                        var _a2;
                        var realIndex = Array.isArray(seriesIndices) ? seriesIndices[idx] : idx;
                        var elSeries = graphics.group({
                            class: "apexcharts-series",
                            rel: realIndex + 1,
                            seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex]),
                            "data:realIndex": realIndex
                        });
                        var scales = _this._scales(realIndex, (w.seriesData.series[realIndex] || []).length);
                        var color = w.globals.colors[realIndex];
                        var rawData = /** @type {any} */ ((_a2 = w.config.series[realIndex]) == null ? void 0 : _a2.data) || [];
                        var xvals = w.seriesData.seriesX[realIndex] || [];
                        var yvals = w.seriesData.series[realIndex] || [];
                        w.globals.seriesXvalues[realIndex] = [];
                        w.globals.seriesYvalues[realIndex] = [];
                        if (typeof w.globals.pointsArray[realIndex] === "undefined") {
                            w.globals.pointsArray[realIndex] = [];
                        }
                        for(var j = 0; j < yvals.length; j++){
                            var yVal = yvals[j];
                            if (yVal === null || typeof yVal === "undefined") continue;
                            var xVal = xvals[j];
                            var xPx = scales.xAt(j, xVal);
                            var yPx = scales.y(yVal);
                            var api = _this._api(emit, elSeries, realIndex, j);
                            try {
                                def.renderItem({
                                    datum: rawData[j],
                                    x: xPx,
                                    y: yPx,
                                    scales: scales,
                                    api: api,
                                    seriesIndex: realIndex,
                                    dataPointIndex: j,
                                    color: color
                                });
                            } catch (e) {
                                if (!_this._warned) {
                                    console.warn('[apexcharts] renderItem for series type "'.concat(name2, '" threw; skipping datum:'), e);
                                    _this._warned = true;
                                }
                            }
                            w.globals.seriesXvalues[realIndex][j] = xPx;
                            w.globals.seriesYvalues[realIndex][j] = yPx;
                            w.globals.pointsArray[realIndex][j] = [
                                xPx,
                                yPx
                            ];
                        }
                        graphics.setupEventDelegation(elSeries, ".apexcharts-marks-mark");
                        ret.add(elSeries);
                    });
                    (_b = (_a = this.ctx.animations) == null ? void 0 : _a.animationCompleted) == null ? void 0 : _b.call(_a, ret);
                    return ret;
                }
            },
            {
                /**
     * Series-space (elGraphical-local, translate-free) scales, matching how the
     * built-ins compute pixels, so custom marks align with axes and gridlines
     * and paint correctly on the elGraphical-local canvas.
     * @param {number} realIndex
     * @param {number} [nPts] number of data points (for categorical band sizing)
     */ key: "_scales",
                value: function _scales(realIndex, nPts) {
                    var _a, _b, _c, _d;
                    var gl = this.w.globals;
                    var cnf = this.w.config;
                    var xRatio = this.xyRatios.xRatio;
                    var yRatioArr = this.xyRatios.yRatio;
                    var axis = (_b = (_a = gl.seriesYAxisReverseMap) == null ? void 0 : _a[realIndex]) != null ? _b : 0;
                    var yr = Array.isArray(yRatioArr) ? (_c = yRatioArr[axis]) != null ? _c : yRatioArr[0] : yRatioArr;
                    var maxYArr = /** @type {any} */ gl.maxYArr;
                    var maxY = Array.isArray(maxYArr) && maxYArr.length ? (_d = maxYArr[axis]) != null ? _d : gl.maxY : gl.maxY;
                    var gridWidth = gl.gridWidth;
                    var gridHeight = gl.gridHeight;
                    var catMode = !gl.isXNumeric;
                    var n = nPts || gl.dataPoints || 1;
                    var bandW = n > 0 ? gridWidth / n : gridWidth;
                    var tickOn = cnf.xaxis.tickPlacement === "on";
                    var x = function x(v) {
                        return xRatio ? (v - gl.minX) / xRatio : gridWidth / 2;
                    };
                    var y = function y(v) {
                        return (maxY - v) / yr;
                    };
                    var xAt = function xAt(index, v) {
                        if (!catMode) return x(v);
                        if (tickOn && n > 1) return index / (n - 1) * gridWidth;
                        return (index + 0.5) * bandW;
                    };
                    var step = gl.minXDiff || 1;
                    var band = catMode ? bandW : xRatio ? step / xRatio : gridWidth;
                    return {
                        x: x,
                        xAt: xAt,
                        y: y,
                        gridWidth: gridWidth,
                        gridHeight: gridHeight,
                        band: band
                    };
                }
            },
            {
                /**
     * Per-datum primitive API. Each call emits immediately (canvas-aware via
     * `emit`), tags the node with the datum's identity, and adds it to the
     * series group; on canvas the tag/add are inert (marks live on the canvas,
     * events are coordinate-based).
     * @param {any} emit @param {any} elSeries @param {number} realIndex @param {number} j
     */ key: "_api",
                value: function _api(emit, elSeries, realIndex, j) {
                    var tag = function tag(el) {
                        if (el) {
                            try {
                                el.node.setAttribute("index", String(realIndex));
                                el.node.setAttribute("j", String(j));
                                el.node.classList.add("apexcharts-marks-mark");
                            } catch (e) {}
                            elSeries.add(el);
                        }
                        return el;
                    };
                    return {
                        /** @param {any} o */ path: function path() {
                            var o = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                            var _a, _b, _c, _d, _e, _f, _g, _h, _i;
                            return tag(emit.drawPath({
                                d: o.d || "",
                                stroke: (_a = o.stroke) != null ? _a : "#000",
                                strokeWidth: (_c = (_b = o.width) != null ? _b : o.strokeWidth) != null ? _c : 1,
                                fill: (_d = o.fill) != null ? _d : "none",
                                fillOpacity: (_f = o.fillOpacity) != null ? _f : o.fill && o.fill !== "none" ? (_e = o.opacity) != null ? _e : 1 : 0,
                                strokeOpacity: (_h = (_g = o.strokeOpacity) != null ? _g : o.opacity) != null ? _h : 1,
                                strokeDashArray: (_i = o.dash) != null ? _i : 0,
                                strokeLinecap: o.lineCap
                            }));
                        },
                        /** @param {any} o */ line: function line() {
                            var o = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                            var _a, _b, _c, _d;
                            return tag(emit.drawLine(o.x1, o.y1, o.x2, o.y2, (_a = o.stroke) != null ? _a : "#000", (_b = o.dash) != null ? _b : 0, (_d = (_c = o.width) != null ? _c : o.strokeWidth) != null ? _d : 1));
                        },
                        /** @param {any} o */ rect: function rect() {
                            var o = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                            var _a, _b, _c, _d, _e, _f, _g, _h;
                            return tag(emit.drawRect((_a = o.x) != null ? _a : 0, (_b = o.y) != null ? _b : 0, (_c = o.w) != null ? _c : 0, (_d = o.h) != null ? _d : 0, (_e = o.r) != null ? _e : 0, (_f = o.fill) != null ? _f : "#000", (_g = o.opacity) != null ? _g : 1, o.stroke != null ? (_h = o.strokeWidth) != null ? _h : 1 : null, o.stroke));
                        },
                        /** @param {any} o */ circle: function circle() {
                            var o = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                            var _a, _b, _c, _d, _e;
                            return tag(emit.drawCircle((_a = o.r) != null ? _a : 0, {
                                cx: (_b = o.cx) != null ? _b : 0,
                                cy: (_c = o.cy) != null ? _c : 0,
                                fill: (_d = o.fill) != null ? _d : "#000",
                                stroke: o.stroke || "none",
                                "stroke-width": (_e = o.strokeWidth) != null ? _e : o.stroke ? 1 : 0
                            }));
                        },
                        /** @param {any} o */ text: function text() {
                            var o = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
                            var _a, _b, _c, _d;
                            return tag(emit.drawText({
                                x: (_a = o.x) != null ? _a : 0,
                                y: (_b = o.y) != null ? _b : 0,
                                text: (_c = o.text) != null ? _c : "",
                                textAnchor: (_d = o.anchor) != null ? _d : "start",
                                fontSize: o.size,
                                foreColor: o.color,
                                fontWeight: o.weight
                            }));
                        }
                    };
                }
            }
        ]);
        return CustomSeries;
    }();
    cls.dataType = def.dataType || "xy";
    cls.yExtent = typeof def.yExtent === "function" ? def.yExtent : null;
    return cls;
}
ApexCharts._customSeriesFactory = makeCustomSeriesClass;
var DARK_QUERY = "(prefers-color-scheme: dark)";
var CONTRAST_QUERY = "(prefers-contrast: more)";
var OSThemeWatcher = /*#__PURE__*/ function() {
    "use strict";
    function OSThemeWatcher(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, OSThemeWatcher);
        this.w = w;
        this.ctx = ctx;
        if (w.config.theme.follow !== "os" || !Environment.isBrowser()) return;
        var media = this._ensureMedia();
        if (!media) return;
        this._applyToConfig(media);
        this._ensureListeners(media);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(OSThemeWatcher, [
        {
            /**
   * Create (once per instance) the MediaQueryLists and stash them on `ctx` so
   * they persist across the re-render that `updateOptions` triggers.
   * @returns {{dark: MediaQueryList|null, contrast: MediaQueryList|null, handler: null|(()=>void)}|null}
   */ key: "_ensureMedia",
            value: function _ensureMedia() {
                if (!this.ctx._osThemeMedia) {
                    var dark = BrowserAPIs.matchMedia(DARK_QUERY);
                    var contrast = BrowserAPIs.matchMedia(CONTRAST_QUERY);
                    if (!dark && !contrast) return null;
                    this.ctx._osThemeMedia = {
                        dark: dark,
                        contrast: contrast,
                        handler: null
                    };
                }
                return this.ctx._osThemeMedia;
            }
        },
        {
            /**
   * Write the OS-resolved mode / high-contrast onto the live `w.config.theme`.
   * @param {{dark: MediaQueryList|null, contrast: MediaQueryList|null}} media
   */ key: "_applyToConfig",
            value: function _applyToConfig(media) {
                var theme = this.w.config.theme;
                if (media.dark) {
                    theme.mode = media.dark.matches ? "dark" : "light";
                }
                if (media.contrast && media.contrast.matches) {
                    theme.accessibility = theme.accessibility || {};
                    theme.accessibility.colorBlindMode = "highContrast";
                }
            }
        },
        {
            /**
   * Attach the `change` listener once. The handler closes over `ctx` + `media`
   * (both stable across re-renders), NOT over `this` (a fresh watcher is built
   * each create), so it never goes stale.
   * @param {{dark: MediaQueryList|null, contrast: MediaQueryList|null, handler: null|(()=>void)}} media
   */ key: "_ensureListeners",
            value: function _ensureListeners(media) {
                if (media.handler) return;
                var ctx = this.ctx;
                var handler = function handler() {
                    var m = ctx._osThemeMedia;
                    if (!m) return;
                    var themeOpt = {
                        mode: m.dark && m.dark.matches ? "dark" : "light"
                    };
                    if (m.contrast && m.contrast.matches) {
                        themeOpt.accessibility = {
                            colorBlindMode: "highContrast"
                        };
                    } else {
                        themeOpt.accessibility = {
                            colorBlindMode: ""
                        };
                    }
                    ctx.updateOptions({
                        theme: themeOpt
                    }, false, true, false);
                };
                OSThemeWatcher._add(media.dark, handler);
                OSThemeWatcher._add(media.contrast, handler);
                media.handler = handler;
            }
        },
        {
            /** Remove the listeners and drop the stashed media. Called on full destroy. */ key: "teardown",
            value: function teardown() {
                var media = this.ctx._osThemeMedia;
                if (!media) return;
                if (media.handler) {
                    OSThemeWatcher._remove(media.dark, media.handler);
                    OSThemeWatcher._remove(media.contrast, media.handler);
                }
                this.ctx._osThemeMedia = null;
            }
        }
    ], [
        {
            key: "_add",
            value: /**
   * @param {MediaQueryList|null} mql @param {()=>void} handler
   */ function _add(mql, handler) {
                if (!mql) return;
                if (typeof mql.addEventListener === "function") {
                    mql.addEventListener("change", handler);
                } else if (typeof /** @type {any} */ mql.addListener === "function") {
                    mql.addListener(handler);
                }
            }
        },
        {
            key: "_remove",
            value: /**
   * @param {MediaQueryList|null} mql @param {()=>void} handler
   */ function _remove(mql, handler) {
                if (!mql) return;
                if (typeof mql.removeEventListener === "function") {
                    mql.removeEventListener("change", handler);
                } else if (typeof /** @type {any} */ mql.removeListener === "function") {
                    mql.removeListener(handler);
                }
            }
        }
    ]);
    return OSThemeWatcher;
}();
ApexCharts.registerFeatures({
    osThemeWatcher: OSThemeWatcher
});
var REGISTRY_KEY = "__apexcharts_crossfilters__";
function cleanFloat(x) {
    if (!Number.isFinite(x)) return x;
    var n = Number(x.toPrecision(12));
    return Object.is(n, -0) ? 0 : n;
}
function isNum(v) {
    return typeof v === "number" && Number.isFinite(v);
}
function makeReducer(reduce) {
    if (typeof reduce === "function") return reduce;
    if (reduce && (typeof reduce === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(reduce)) === "object") {
        if (typeof reduce.sum === "string") {
            var f = reduce.sum;
            return function(rows) {
                return rows.reduce(function(a, r) {
                    return a + (Number(r[f]) || 0);
                }, 0);
            };
        }
        if (typeof reduce.avg === "string") {
            var f1 = reduce.avg;
            return function(rows) {
                return rows.length ? rows.reduce(function(a, r) {
                    return a + (Number(r[f1]) || 0);
                }, 0) / rows.length : 0;
            };
        }
        if (typeof reduce.min === "string") {
            var _Math;
            var f2 = reduce.min;
            return function(rows) {
                return rows.length ? (_Math = Math).min.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(rows.map(function(r) {
                    return Number(r[f2]) || 0;
                }))) : 0;
            };
        }
        if (typeof reduce.max === "string") {
            var _Math1;
            var f3 = reduce.max;
            return function(rows) {
                return rows.length ? (_Math1 = Math).max.apply(_Math1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(rows.map(function(r) {
                    return Number(r[f3]) || 0;
                }))) : 0;
            };
        }
    }
    return function(rows) {
        return rows.length;
    };
}
function applyOrder(keys, order) {
    if (typeof order === "function") return keys.slice().sort(order);
    if (order === "asc") return keys.slice().sort(function(a, b) {
        return a > b ? 1 : a < b ? -1 : 0;
    });
    if (order === "desc") return keys.slice().sort(function(a, b) {
        return a < b ? 1 : a > b ? -1 : 0;
    });
    return keys;
}
function categoryDomain(records, accessor, order) {
    var seen = /* @__PURE__ */ new Set();
    var keys = [];
    for(var i = 0; i < records.length; i++){
        var k = accessor(records[i]);
        if (k == null) continue;
        if (!seen.has(k)) {
            seen.add(k);
            keys.push(k);
        }
    }
    return applyOrder(keys, order);
}
function matrixDomain(records, accessor, order) {
    var xSeen = /* @__PURE__ */ new Set();
    var ySeen = /* @__PURE__ */ new Set();
    var xLabels = [];
    var yLabels = [];
    for(var i = 0; i < records.length; i++){
        var pair = accessor(records[i]);
        if (!pair) continue;
        var x = pair[0];
        var y = pair[1];
        if (x != null && !xSeen.has(x)) {
            xSeen.add(x);
            xLabels.push(x);
        }
        if (y != null && !ySeen.has(y)) {
            ySeen.add(y);
            yLabels.push(y);
        }
    }
    return {
        xLabels: applyOrder(xLabels, order),
        yLabels: applyOrder(yLabels, order)
    };
}
function rangeEdges(records, accessor, bins) {
    if (bins && Array.isArray(bins.thresholds) && bins.thresholds.length >= 2) {
        var t = Array.from(new Set(bins.thresholds.filter(isNum))).sort(function(a, b) {
            return a - b;
        });
        return t.length >= 2 ? t.map(cleanFloat) : [
            0,
            1
        ];
    }
    var min = Infinity;
    var max = -Infinity;
    for(var i = 0; i < records.length; i++){
        var v = accessor(records[i]);
        if (!isNum(v)) continue;
        if (v < min) min = v;
        if (v > max) max = v;
    }
    if (min === Infinity) return [
        0,
        1
    ];
    if (min === max) {
        var pad = Math.abs(min) > 0 ? Math.abs(min) : 1;
        return [
            cleanFloat(min),
            cleanFloat(min + pad)
        ];
    }
    if (bins && isNum(bins.width) && bins.width > 0) {
        var w2 = bins.width;
        var start = Math.floor(min / w2) * w2;
        var end = Math.ceil(max / w2) * w2;
        if (end <= start) end = start + w2;
        var count2 = Math.max(1, Math.round((end - start) / w2));
        var edges2 = new Array(count2 + 1);
        for(var i1 = 0; i1 <= count2; i1++)edges2[i1] = cleanFloat(start + i1 * w2);
        return edges2;
    }
    var count = bins && isNum(bins.count) && bins.count >= 1 ? Math.floor(bins.count) : 30;
    var w = (max - min) / count;
    var edges = new Array(count + 1);
    for(var i2 = 0; i2 <= count; i2++)edges[i2] = cleanFloat(min + i2 * w);
    edges[count] = cleanFloat(max);
    return edges;
}
function binIndexOf(v, edges) {
    if (!isNum(v)) return -1;
    var last = edges.length - 1;
    if (v < edges[0] || v > edges[last]) return -1;
    if (v === edges[last]) return last - 1;
    for(var i = 0; i < last; i++){
        if (v >= edges[i] && v < edges[i + 1]) return i;
    }
    return -1;
}
function binCenters(edges) {
    var centers = [];
    for(var i = 0; i < edges.length - 1; i++){
        centers.push(cleanFloat((edges[i] + edges[i + 1]) / 2));
    }
    return centers;
}
var Crossfilter = /*#__PURE__*/ function() {
    "use strict";
    function Crossfilter(id, records) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Crossfilter);
        this.id = id;
        this.records = Array.isArray(records) ? records : [];
        this.dims = /* @__PURE__ */ new Map();
        this._listeners = /* @__PURE__ */ new Map();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Crossfilter, [
        {
            // ----- data + dimensions ------------------------------------------------
            /**
   * Swap the shared dataset and recompute every dimension's domain. Existing
   * filters are kept where still valid (categorical keys no longer present are
   * pruned); the change is broadcast.
   * @param {any[]} records
   */ key: "setRecords",
            value: function setRecords(records) {
                var _this = this;
                this.records = Array.isArray(records) ? records : [];
                this.dims.forEach(function(dim) {
                    return _this._recomputeDomain(dim);
                });
                this._emit("records", this.state());
                this._emit("change", this.state());
                return this;
            }
        },
        {
            /**
   * Register (or replace) a chart's dimension + reduction.
   * @param {string} chartId
   * @param {{
   *   dimension:(row:any)=>any, reduce?:any, type?:'category'|'range',
   *   bins?:{width?:number,count?:number,thresholds?:number[]},
   *   order?:'first-seen'|'asc'|'desc'|((a:any,b:any)=>number),
   *   filter?:any }} spec
   */ key: "registerDimension",
            value: function registerDimension(chartId, spec) {
                if (!spec || typeof spec.dimension !== "function") {
                    throw new Error('crossfilter.registerDimension("'.concat(chartId, '") needs a dimension function.'));
                }
                var type = spec.type || (spec.bins ? "range" : "category");
                var dim = {
                    accessor: spec.dimension,
                    reducer: makeReducer(spec.reduce),
                    type: type,
                    bins: spec.bins,
                    order: spec.order,
                    /** @type {Set<any>|[number,number]|null} */ filter: null,
                    /** @type {any[]} */ labels: [],
                    /** @type {number[]|null} */ edges: null
                };
                this.dims.set(chartId, dim);
                this._recomputeDomain(dim);
                if (spec.filter != null) this._setFilterOn(dim, spec.filter);
                return this;
            }
        },
        {
            /** @param {string} chartId @returns {boolean} */ key: "hasDimension",
            value: function hasDimension(chartId) {
                return this.dims.has(chartId);
            }
        },
        {
            /** @param {string} chartId */ key: "removeDimension",
            value: function removeDimension(chartId) {
                var dim = this.dims.get(chartId);
                var hadFilter = dim ? this._hasFilter(dim) : false;
                this.dims.delete(chartId);
                if (hadFilter) this._emit("change", this.state());
                return this;
            }
        },
        {
            /** @param {any} dim */ key: "_recomputeDomain",
            value: function _recomputeDomain(dim) {
                if (dim.type === "matrix") {
                    var dom = matrixDomain(this.records, dim.accessor, dim.order);
                    dim.xLabels = dom.xLabels;
                    dim.yLabels = dom.yLabels;
                    dim.edges = null;
                    return;
                }
                if (dim.type === "range") {
                    dim.edges = rangeEdges(this.records, dim.accessor, dim.bins);
                    dim.labels = binCenters(dim.edges);
                } else {
                    dim.labels = categoryDomain(this.records, dim.accessor, dim.order);
                    dim.edges = null;
                    if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dim.filter, Set)) {
                        var domain = new Set(dim.labels);
                        Array.from(dim.filter).forEach(function(k) {
                            if (!domain.has(k)) dim.filter.delete(k);
                        });
                    }
                }
            }
        },
        {
            // ----- filters ----------------------------------------------------------
            /**
   * Set (replace) a chart's filter. Categorical: an array/Set of keys (or null
   * to clear). Range: a `[min,max]` tuple (or null to clear).
   * @param {string} chartId
   * @param {any[]|Set<any>|[number,number]|null} filter
   */ key: "filter",
            value: function filter(chartId, _filter) {
                var dim = this.dims.get(chartId);
                if (!dim) return this;
                this._setFilterOn(dim, _filter);
                this._emit("change", this.state());
                return this;
            }
        },
        {
            /**
   * Toggle one categorical key in a chart's filter Set (multi-select, OR).
   * @param {string} chartId @param {any} key
   */ key: "toggleKey",
            value: function toggleKey(chartId, key) {
                var dim = this.dims.get(chartId);
                if (!dim || dim.type !== "category") return this;
                if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dim.filter, Set)) dim.filter = /* @__PURE__ */ new Set();
                var set = /** @type {Set<any>} */ dim.filter;
                if (set.has(key)) set.delete(key);
                else set.add(key);
                if (set.size === 0) dim.filter = null;
                this._emit("change", this.state());
                return this;
            }
        },
        {
            /** @param {any} dim @param {any} filter */ key: "_setFilterOn",
            value: function _setFilterOn(dim, filter) {
                if (filter == null) {
                    dim.filter = null;
                    return;
                }
                if (dim.type === "range") {
                    if (Array.isArray(filter) && filter.length === 2 && filter.every(isNum)) {
                        dim.filter = [
                            Math.min(filter[0], filter[1]),
                            Math.max(filter[0], filter[1])
                        ];
                    } else {
                        dim.filter = null;
                    }
                    return;
                }
                var set = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(filter, Set) ? new Set(filter) : new Set(filter);
                dim.filter = set.size ? set : null;
            }
        },
        {
            /**
   * Clear one chart's filter.
   * @param {string} chartId
   */ key: "clear",
            value: function clear(chartId) {
                var dim = this.dims.get(chartId);
                if (dim) dim.filter = null;
                this._emit("change", this.state());
                return this;
            }
        },
        {
            /** Clear all filters across every dimension. */ key: "reset",
            value: function reset() {
                this.dims.forEach(function(dim) {
                    dim.filter = null;
                });
                this._emit("change", this.state());
                return this;
            }
        },
        {
            /** @param {any} dim @returns {boolean} does this dimension have an active filter */ key: "_hasFilter",
            value: function _hasFilter(dim) {
                if (dim.filter == null) return false;
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dim.filter, Set)) return dim.filter.size > 0;
                return true;
            }
        },
        {
            /** @param {any} dim @param {any} row @returns {boolean} does row pass this dim's filter */ key: "_passes",
            value: function _passes(dim, row) {
                if (!this._hasFilter(dim)) return true;
                var v = dim.accessor(row);
                if ((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dim.filter, Set)) return dim.filter.has(v);
                if (!isNum(v)) return false;
                return v >= dim.filter[0] && v <= dim.filter[1];
            }
        },
        {
            // ----- aggregation ------------------------------------------------------
            /**
   * Records passing every ACTIVE filter except the one on `exceptChartId`
   * (pass null/undefined to apply all filters).
   * @param {string|null} [exceptChartId]
   * @returns {any[]}
   */ key: "filteredRecords",
            value: function filteredRecords(exceptChartId) {
                var _this = this;
                var active = [];
                this.dims.forEach(function(dim, id) {
                    if (id === exceptChartId) return;
                    if (_this._hasFilter(dim)) active.push(dim);
                });
                if (active.length === 0) return this.records;
                return this.records.filter(function(row) {
                    return active.every(function(dim) {
                        return _this._passes(dim, row);
                    });
                });
            }
        },
        {
            /** Rows passing ALL active filters (the fully filtered set). @returns {any[]} */ key: "filteredRows",
            value: function filteredRows() {
                return this.filteredRecords(null);
            }
        },
        {
            /**
   * The crossfilter aggregation for one chart: reduce over records passing all
   * OTHER charts' filters, bucketed by this chart's dimension. Category/range
   * dims return `{type, labels, values, keys, edges?}`; a matrix (2D) dim
   * returns `{type:'matrix', xLabels, yLabels, matrix}`.
   * @param {string} chartId
   * @returns {any}
   */ key: "aggregateFor",
            value: function aggregateFor(chartId) {
                var dim = this.dims.get(chartId);
                if (!dim) return {
                    type: "category",
                    labels: [],
                    values: [],
                    keys: []
                };
                var rows = this.filteredRecords(chartId);
                if (dim.type === "matrix") {
                    var xIndex = new Map(dim.xLabels.map(function(k, i) {
                        return [
                            k,
                            i
                        ];
                    }));
                    var yIndex = new Map(dim.yLabels.map(function(k, i) {
                        return [
                            k,
                            i
                        ];
                    }));
                    var buckets = dim.yLabels.map(function() {
                        return dim.xLabels.map(function() {
                            return [];
                        });
                    });
                    for(var i = 0; i < rows.length; i++){
                        var pair = dim.accessor(rows[i]);
                        if (!pair) continue;
                        var xi = xIndex.get(pair[0]);
                        var yi = yIndex.get(pair[1]);
                        if (xi != null && yi != null) buckets[yi][xi].push(rows[i]);
                    }
                    return {
                        type: "matrix",
                        xLabels: dim.xLabels.slice(),
                        yLabels: dim.yLabels.slice(),
                        matrix: buckets.map(function(brow) {
                            return brow.map(function(cell) {
                                return dim.reducer(cell);
                            });
                        })
                    };
                }
                if (dim.type === "range") {
                    var edges = dim.edges || [
                        0,
                        1
                    ];
                    var nBins = edges.length - 1;
                    var buckets1 = Array.from({
                        length: nBins
                    }, function() {
                        return [];
                    });
                    for(var i1 = 0; i1 < rows.length; i1++){
                        var idx = binIndexOf(dim.accessor(rows[i1]), edges);
                        if (idx >= 0) buckets1[idx].push(rows[i1]);
                    }
                    return {
                        type: "range",
                        labels: binCenters(edges),
                        // plot bars at bin centers, not lower edges
                        values: buckets1.map(function(b) {
                            return dim.reducer(b);
                        }),
                        keys: buckets1.map(function(_, i) {
                            return [
                                edges[i],
                                edges[i + 1]
                            ];
                        }),
                        edges: edges
                    };
                }
                var index = /* @__PURE__ */ new Map();
                dim.labels.forEach(function(k) {
                    return index.set(k, []);
                });
                for(var i2 = 0; i2 < rows.length; i2++){
                    var k = dim.accessor(rows[i2]);
                    var bucket = index.get(k);
                    if (bucket) bucket.push(rows[i2]);
                }
                return {
                    type: "category",
                    labels: dim.labels.slice(),
                    values: dim.labels.map(function(k) {
                        return dim.reducer(index.get(k) || []);
                    }),
                    keys: dim.labels.slice()
                };
            }
        },
        {
            /**
   * Aggregate every registered chart.
   * @returns {Record<string, ReturnType<Crossfilter['aggregateFor']>>}
   */ key: "aggregateAll",
            value: function aggregateAll() {
                var _this = this;
                var out = {};
                this.dims.forEach(function(_dim, id) {
                    out[id] = _this.aggregateFor(id);
                });
                return out;
            }
        },
        {
            // ----- state + events ---------------------------------------------------
            /**
   * A serializable snapshot: active filters, filtered/total record counts.
   * @returns {{filters:Record<string, any[]|[number,number]>, filteredCount:number, total:number}}
   */ key: "state",
            value: function state() {
                var _this = this;
                var filters = {};
                this.dims.forEach(function(dim, id) {
                    if (!_this._hasFilter(dim)) return;
                    filters[id] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dim.filter, Set) ? Array.from(dim.filter) : dim.filter.slice();
                });
                return {
                    filters: filters,
                    filteredCount: this.filteredRows().length,
                    total: this.records.length
                };
            }
        },
        {
            /** @param {string} chartId @returns {any} the current filter (Set copy / range copy / null) */ key: "filterOf",
            value: function filterOf(chartId) {
                var dim = this.dims.get(chartId);
                if (!dim || !this._hasFilter(dim)) return null;
                return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(dim.filter, Set) ? new Set(dim.filter) : dim.filter.slice();
            }
        },
        {
            /**
   * Subscribe to an event ('change' | 'records'). Returns an unsubscribe fn.
   * @param {string} event @param {Function} cb
   */ key: "on",
            value: function on(event, cb) {
                var _this = this;
                var set = this._listeners.get(event);
                if (!set) {
                    set = /* @__PURE__ */ new Set();
                    this._listeners.set(event, set);
                }
                set.add(cb);
                return function() {
                    return _this.off(event, cb);
                };
            }
        },
        {
            /** @param {string} event @param {Function} cb */ key: "off",
            value: function off(event, cb) {
                var _a;
                (_a = this._listeners.get(event)) == null ? void 0 : _a.delete(cb);
                return this;
            }
        },
        {
            /** @param {string} event @param {any} payload */ key: "_emit",
            value: function _emit(event, payload) {
                var _a;
                (_a = this._listeners.get(event)) == null ? void 0 : _a.forEach(function(cb) {
                    try {
                        cb(payload);
                    } catch (e) {
                        console.error(e);
                    }
                });
            }
        },
        {
            // ----- data table (presentation helper) ---------------------------------
            // Renders the filtered rows into a user-provided element and keeps it in sync
            // on every filter change. Only the passed `el` is touched (no window/document
            // globals), so the engine stays SSR-safe and unit-testable.
            /** @param {string} s @returns {string} HTML-escaped */ key: "_esc",
            value: function _esc(s) {
                return String(s == null ? "" : s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
            }
        },
        {
            /**
   * @param {Array<string|{field:string,label?:string,format?:(v:any,row:any)=>any}>|undefined} columns
   * @returns {Array<{field:string,label:string,format?:Function}>}
   */ key: "_resolveColumns",
            value: function _resolveColumns(columns) {
                if (Array.isArray(columns) && columns.length) {
                    return columns.map(function(c) {
                        return typeof c === "string" ? {
                            field: c,
                            label: c
                        } : {
                            field: c.field,
                            label: c.label || c.field,
                            format: c.format
                        };
                    });
                }
                var first = this.records[0];
                var fields = first ? Object.keys(first) : [];
                return fields.map(function(f) {
                    return {
                        field: f,
                        label: f
                    };
                });
            }
        },
        {
            /**
   * @param {Array<{field:string,label:string,format?:Function}>} columns
   * @param {any[]} rows @param {number} total
   * @returns {string}
   */ key: "_tableHTML",
            value: function _tableHTML(columns, rows, total) {
                var _this = this;
                var head = "<thead><tr>" + columns.map(function(c) {
                    return "<th>".concat(_this._esc(c.label), "</th>");
                }).join("") + "</tr></thead>";
                var body = "<tbody>" + rows.map(function(row) {
                    return "<tr>" + columns.map(function(c) {
                        var raw = row[c.field];
                        var val = c.format ? c.format(raw, row) : raw;
                        return "<td>".concat(_this._esc(val), "</td>");
                    }).join("") + "</tr>";
                }).join("") + "</tbody>";
                var caption = "<caption>".concat(rows.length, " of ").concat(total, " rows</caption>");
                return '<table class="apexcharts-cf-table">'.concat(caption).concat(head).concat(body, "</table>");
            }
        },
        {
            /**
   * Bind an HTML table of the filtered rows to `el`; it re-renders on every
   * filter change. Returns a handle with refresh()/destroy().
   * @param {HTMLElement} el
   * @param {{columns?:any[], page?:number, pageSize?:number}} [opts]
   */ key: "dataTable",
            value: function dataTable(el, opts) {
                var _this = this;
                if (!el) return {
                    refresh: function refresh() {},
                    destroy: function destroy() {}
                };
                var o = opts || {};
                var columns = this._resolveColumns(o.columns);
                var pageSize = o.pageSize || 0;
                var page = o.page || 0;
                var render = function render() {
                    var rows = _this.filteredRows();
                    var shown = pageSize ? rows.slice(page * pageSize, page * pageSize + pageSize) : rows;
                    el.innerHTML = _this._tableHTML(columns, shown, rows.length);
                };
                render();
                var off = this.on("change", render);
                return {
                    refresh: render,
                    destroy: function destroy() {
                        off();
                        el.innerHTML = "";
                    }
                };
            }
        },
        {
            /** Remove this coordinator from the registry and drop all state. */ key: "destroy",
            value: function destroy() {
                Crossfilter._store().delete(this.id);
                this.dims.clear();
                this._listeners.clear();
                this.records = [];
            }
        }
    ], [
        {
            key: "_store",
            value: // ----- registry ---------------------------------------------------------
            /** @returns {Map<string, Crossfilter>} */ function _store() {
                var g = /** @type {any} */ globalThis;
                if (!g[REGISTRY_KEY]) g[REGISTRY_KEY] = /* @__PURE__ */ new Map();
                return g[REGISTRY_KEY];
            }
        },
        {
            key: "getOrCreate",
            value: /**
   * Get-or-create a coordinator by id. Passing `records` on an existing
   * coordinator swaps its dataset (re-aggregates).
   * @param {{id:string, records?:any[]}} opts
   * @returns {Crossfilter}
   */ function getOrCreate(opts) {
                if (!opts || typeof opts.id !== "string") {
                    throw new Error("ApexCharts.crossfilter requires an { id } string.");
                }
                var store = Crossfilter._store();
                var cf = store.get(opts.id);
                if (cf) {
                    if (opts.records) cf.setRecords(opts.records);
                    return cf;
                }
                cf = new Crossfilter(opts.id, opts.records);
                store.set(opts.id, cf);
                return cf;
            }
        },
        {
            key: "get",
            value: /** @param {string} id @returns {Crossfilter|null} */ function get(id) {
                return Crossfilter._store().get(id) || null;
            }
        }
    ]);
    return Crossfilter;
}();
var MARK_SELECTOR = [
    ".apexcharts-bar-area",
    ".apexcharts-candlestick-area",
    ".apexcharts-boxPlot-area",
    ".apexcharts-rangebar-area",
    ".apexcharts-marker"
].join(", ");
var FILTER_MARK_SELECTOR = [
    ".apexcharts-pie-area",
    ".apexcharts-bar-area"
].join(", ");
var DIMMED_CLASS = "apexcharts-crossfilter-dimmed";
var PIE_TYPES = [
    "pie",
    "donut",
    "polarArea",
    "radialBar"
];
var LinkedViews = /*#__PURE__*/ function() {
    "use strict";
    function LinkedViews(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, LinkedViews);
        this.w = w;
        this.ctx = ctx;
        this._dimmed = false;
        this._wired = false;
        this._pending = false;
        this._lastValues = null;
        this._onPointSelect = this._onPointSelect.bind(this);
        this._afterRender = this._afterRender.bind(this);
        this._onChange = this._onChange.bind(this);
        if (this._mode() === "filter") this._initEngine();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(LinkedViews, [
        {
            /** @returns {'highlight'|'filter'|'off'} */ key: "_mode",
            value: function _mode() {
                var link = this.w.config.chart.link;
                if (link && typeof link.dimension === "function") return "filter";
                if (link && link.enabled) return "highlight";
                return "off";
            }
        },
        {
            key: "_enabled",
            value: function _enabled() {
                var link = this.w.config.chart.link;
                return !!(link && link.enabled);
            }
        },
        {
            /**
   * The source chart's rectangle brush produced a data-x range. In FILTER mode
   * this becomes a `[min,max]` range filter on the chart's dimension (the other
   * charts re-aggregate). In HIGHLIGHT mode (P1) it dims out-of-range marks
   * across the group. Called (null-safe) from ZoomPanSelection selectionDrawn /
   * selectionDragging.
   * @param {{min:number, max:number}} xaxis
   */ key: "onSourceSelection",
            value: function onSourceSelection(xaxis) {
                var _a;
                var mode = this._mode();
                if (mode === "off") return;
                if (!xaxis || xaxis.min == null || xaxis.max == null) return;
                var min = Math.min(xaxis.min, xaxis.max);
                var max = Math.max(xaxis.min, xaxis.max);
                var gMinX = this.w.globals.minX;
                var gMaxX = this.w.globals.maxX;
                if (isFinite(gMinX) && isFinite(gMaxX) && gMaxX > gMinX) {
                    var tol = (gMaxX - gMinX) * 1e-6;
                    if (min - gMinX <= tol) min = gMinX;
                    if (gMaxX - max <= tol) max = gMaxX;
                }
                if (mode === "filter") {
                    var cf = this._cf();
                    if (!cf) return;
                    cf.filter(this._chartId(), [
                        min,
                        max
                    ]);
                    this._fireFilterChange(cf, [
                        min,
                        max
                    ]);
                    return;
                }
                this._group().forEach(function(ch) {
                    var _a2;
                    (_a2 = ch == null ? void 0 : ch.linkedViews) == null ? void 0 : _a2.applyDim(min, max);
                });
                var args = {
                    xaxis: {
                        min: min,
                        max: max
                    },
                    sourceChartID: this.w.globals.chartID
                };
                if (typeof this.w.config.chart.events.crossFilter === "function") {
                    this.w.config.chart.events.crossFilter(this.ctx, args);
                }
                (_a = this.ctx.events) == null ? void 0 : _a.fireEvent("crossFilter", [
                    this.ctx,
                    args
                ]);
            }
        },
        {
            /** self + grouped siblings (dedup-safe; getGroupedCharts excludes self). */ key: "_group",
            value: function _group() {
                var siblings = typeof this.ctx.getGroupedCharts === "function" ? this.ctx.getGroupedCharts() : [];
                return [
                    this.ctx
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(siblings));
            }
        },
        {
            /**
   * Dim this chart's marks whose x is outside [min,max]; un-dim those inside.
   * No re-render, so mark identities are preserved.
   * @param {number} min @param {number} max
   */ key: "applyDim",
            value: function applyDim(min, max) {
                if (!this._enabled()) return;
                var w = this.w;
                var baseEl = w.dom.baseEl;
                if (!baseEl) return;
                var dimOpacity = w.config.chart.link.dimOpacity;
                if (w.dom.elWrap && typeof dimOpacity === "number") {
                    w.dom.elWrap.style.setProperty("--apx-cf-dim", String(dimOpacity));
                }
                var seriesX = w.globals.seriesX || [];
                var marks = baseEl.querySelectorAll(MARK_SELECTOR);
                marks.forEach(function(node) {
                    var jAttr = node.getAttribute("j");
                    if (jAttr === null) return;
                    var j = parseInt(jAttr, 10);
                    var iAttr = node.getAttribute("index");
                    var i = iAttr === null ? 0 : parseInt(iAttr, 10);
                    var row = seriesX[i] || seriesX[0];
                    if (!row) return;
                    var x = row[j];
                    if (x == null) return;
                    node.classList.toggle(DIMMED_CLASS, x < min || x > max);
                });
                this._dimmed = true;
            }
        },
        {
            /** Remove dimming from this chart only. */ key: "clear",
            value: function clear() {
                var baseEl = this.w.dom.baseEl;
                if (!baseEl) return;
                baseEl.querySelectorAll("." + DIMMED_CLASS).forEach(function(n) {
                    return n.classList.remove(DIMMED_CLASS);
                });
                this._dimmed = false;
            }
        },
        {
            /** Clear dimming across the whole group (backs chart.clearCrossfilter). */ key: "clearGroup",
            value: function clearGroup() {
                if (this._mode() === "filter") {
                    var cf = this._cf();
                    if (cf) cf.reset();
                    return;
                }
                this._group().forEach(function(ch) {
                    var _a;
                    return (_a = ch == null ? void 0 : ch.linkedViews) == null ? void 0 : _a.clear();
                });
            }
        },
        {
            // ─── FILTER mode (crossfilter engine glue) ───────────────────────────────
            /**
   * The chart's stable internal id (keys its dimension in the coordinator).
   * Always set by the ApexCharts constructor (falls back to a cuid).
   * @returns {string}
   */ key: "_chartId",
            value: function _chartId() {
                return /** @type {string} */ this.w.globals.chartID;
            }
        },
        {
            /** @returns {import('./Crossfilter').default|null} the coordinator, or null */ key: "_cf",
            value: function _cf() {
                var link = this.w.config.chart.link;
                var id = link && (link.id || this.w.config.chart.group);
                return id ? Crossfilter.get(id) : null;
            }
        },
        {
            key: "_isPie",
            value: function _isPie() {
                return PIE_TYPES.indexOf(this.w.config.chart.type) !== -1;
            }
        },
        {
            key: "_isHeatmap",
            value: function _isHeatmap() {
                return this.w.config.chart.type === "heatmap";
            }
        },
        {
            /**
   * Before the first render: resolve the coordinator, register this chart's
   * dimension, inject the initial aggregated series into w.config (so the first
   * paint is already aggregated, no empty flash), and wire the listeners.
   */ key: "_initEngine",
            value: function _initEngine() {
                var cf = this._cf();
                var link = this.w.config.chart.link;
                if (!cf) {
                    var id = link && link.id || this.w.config.chart.group;
                    console.warn('[apexcharts] chart.link.dimension is set but no crossfilter coordinator "'.concat(id, '" exists. Call ApexCharts.crossfilter({ id, records }) before creating the chart.'));
                    return;
                }
                var chartId = this._chartId();
                if (!cf.hasDimension(chartId)) {
                    cf.registerDimension(chartId, {
                        dimension: link.dimension,
                        reduce: link.reduce,
                        // heatmap => 2D matrix dimension (accessor returns [xKey, yKey]).
                        type: link.type || (this._isHeatmap() ? "matrix" : void 0),
                        bins: link.bins,
                        order: link.order
                    });
                }
                this._injectSeries(cf.aggregateFor(chartId));
                this._wire(cf);
            }
        },
        {
            /**
   * Build the chart's series value from an aggregation, shaped by chart type:
   *   matrix (heatmap) -> [{ name:yKey, data:[{x:xKey, y:value}] }]
   *   pie/donut  -> number[]
   *   axis + category -> [{ name, data:number[] }] (categories set separately)
   *   axis + range    -> [{ name, data:[x,value][] }] on a numeric/time x-axis
   * @param {any} agg
   */ key: "_seriesFromAgg",
            value: function _seriesFromAgg(agg) {
                if (agg.type === "matrix") {
                    return agg.yLabels.map(function(yl, yi) {
                        return {
                            name: String(yl),
                            data: agg.xLabels.map(function(xl, xi) {
                                return {
                                    x: String(xl),
                                    y: agg.matrix[yi][xi]
                                };
                            })
                        };
                    });
                }
                if (this._isPie()) return agg.values.slice();
                var name2 = this.w.config.chart.link.seriesName || "Count";
                if (agg.type === "range") {
                    return [
                        {
                            name: name2,
                            data: agg.labels.map(function(x, i) {
                                return [
                                    x,
                                    agg.values[i]
                                ];
                            })
                        }
                    ];
                }
                return [
                    {
                        name: name2,
                        data: agg.values.slice()
                    }
                ];
            }
        },
        {
            /**
   * Value signature used to skip a reflow when only dimming changed.
   * @param {any} agg
   */ key: "_sigOf",
            value: function _sigOf(agg) {
                return JSON.stringify(agg.matrix || agg.values);
            }
        },
        {
            /**
   * Write the aggregation into w.config as the chart's series/labels. Runs once
   * before the first paint; later updates go through updateSeries.
   * @param {any} agg
   */ key: "_injectSeries",
            value: function _injectSeries(agg) {
                var w = this.w;
                this._lastValues = this._sigOf(agg);
                w.config.series = this._seriesFromAgg(agg);
                if (agg.type === "matrix") return;
                if (this._isPie()) {
                    w.config.labels = agg.labels.map(String);
                } else if (agg.type === "category") {
                    if (!w.config.xaxis) w.config.xaxis = {};
                    w.config.xaxis.categories = agg.labels.map(String);
                } else if (agg.type === "range") {
                    this._pinRangeDomain(agg.edges);
                }
            }
        },
        {
            /**
   * Pin the numeric/datetime x-axis to the outer bin edges of a range-binned
   * dimension (unless the user set xaxis.min/max explicitly). See _injectSeries.
   * @param {number[]|null|undefined} edges
   */ key: "_pinRangeDomain",
            value: function _pinRangeDomain(edges) {
                if (!Array.isArray(edges) || edges.length < 2) return;
                var w = this.w;
                if (!w.config.xaxis) w.config.xaxis = /** @type {any} */ {};
                if (w.config.xaxis.min == null) w.config.xaxis.min = edges[0];
                if (w.config.xaxis.max == null) w.config.xaxis.max = edges[edges.length - 1];
            }
        },
        {
            /** @param {import('./Crossfilter').default} cf */ key: "_wire",
            value: function _wire(cf) {
                if (this._wired) return;
                this._wired = true;
                this.ctx.addEventListener("dataPointSelection", this._onPointSelect);
                this.ctx.addEventListener("mounted", this._afterRender);
                this.ctx.addEventListener("updated", this._afterRender);
                cf.on("change", this._onChange);
            }
        },
        {
            /**
   * A pie slice / bar was clicked: toggle its bucket key on the coordinator.
   * @param {any} _e @param {any} _ctx @param {{dataPointIndex?:number}} opts
   */ key: "_onPointSelect",
            value: function _onPointSelect(_e, _ctx, opts) {
                if (this._mode() !== "filter" || !opts || opts.dataPointIndex == null) return;
                var cf = this._cf();
                if (!cf) return;
                var chartId = this._chartId();
                var agg = cf.aggregateFor(chartId);
                if (agg.type === "matrix") return;
                var key = agg.keys[opts.dataPointIndex];
                if (key == null) return;
                cf.toggleKey(chartId, key);
                this._fireFilterChange(cf, key);
            }
        },
        {
            /** Coordinator filter changed: re-aggregate this chart on a microtask so the
   *  triggering click handler unwinds before we destroy/redraw the DOM. */ key: "_onChange",
            value: function _onChange() {
                var _this = this;
                if (this._mode() !== "filter" || this._pending) return;
                this._pending = true;
                Promise.resolve().then(function() {
                    _this._pending = false;
                    if (_this.w.globals.isDestroyed) return;
                    _this._applyAggregation();
                });
            }
        },
        {
            /**
   * Pull this chart's crossfilter aggregation and push it through updateSeries
   * (animated). When the values are unchanged (e.g. only this chart's own
   * filter moved, which it ignores for itself), skip the reflow and just
   * refresh the self-dim.
   */ key: "_applyAggregation",
            value: function _applyAggregation() {
                if (this._mode() !== "filter") return;
                var cf = this._cf();
                if (!cf) return;
                var agg = cf.aggregateFor(this._chartId());
                var sig = this._sigOf(agg);
                if (sig === this._lastValues) {
                    this._applySelfDim();
                    return;
                }
                this._lastValues = sig;
                this.ctx.updateSeries(this._seriesFromAgg(agg), true);
            }
        },
        {
            key: "_afterRender",
            value: function _afterRender() {
                if (this._mode() !== "filter") return;
                var series = this.w.config.series;
                if (!series || series.length === 0) {
                    this._reassertSeries();
                    return;
                }
                this._applySelfDim();
            }
        },
        {
            /** Restore the aggregated series after an external updateSeries emptied it.
   *  Deferred a microtask so the triggering update fully unwinds first. */ key: "_reassertSeries",
            value: function _reassertSeries() {
                var _this = this;
                if (this._pending) return;
                this._pending = true;
                Promise.resolve().then(function() {
                    _this._pending = false;
                    if (_this.w.globals.isDestroyed) return;
                    var cf = _this._cf();
                    if (!cf) return;
                    var agg = cf.aggregateFor(_this._chartId());
                    var series = _this._seriesFromAgg(agg);
                    if (!series.length) return;
                    _this._lastValues = _this._sigOf(agg);
                    _this.ctx.updateSeries(series, true);
                });
            }
        },
        {
            /**
   * Dim this chart's own buckets that are not in its own filter (no filter ->
   * none dimmed). Categorical: dim buckets whose key is not in the selected Set.
   * Range: dim bins lying fully outside the selected `[min,max]`. Keyed by each
   * mark's `j` (dataPointIndex) -> the aggregation key.
   */ key: "_applySelfDim",
            value: function _applySelfDim() {
                var cf = this._cf();
                if (!cf) return;
                var w = this.w;
                var baseEl = w.dom.baseEl;
                if (!baseEl) return;
                var chartId = this._chartId();
                var filter = cf.filterOf(chartId);
                var dimOpacity = w.config.chart.link.dimOpacity;
                if (w.dom.elWrap && typeof dimOpacity === "number") {
                    w.dom.elWrap.style.setProperty("--apx-cf-dim", String(dimOpacity));
                }
                var isCategory = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_instanceof$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(filter, Set);
                var isRange = Array.isArray(filter);
                var keys = cf.aggregateFor(chartId).keys;
                baseEl.querySelectorAll(FILTER_MARK_SELECTOR).forEach(function(node) {
                    var jAttr = node.getAttribute("j");
                    if (jAttr === null) return;
                    var key = keys[parseInt(jAttr, 10)];
                    var dim = false;
                    if (isCategory) {
                        dim = !/** @type {Set<any>} */ filter.has(key);
                    } else if (isRange && Array.isArray(key)) {
                        dim = key[1] <= filter[0] || key[0] >= filter[1];
                    }
                    node.classList.toggle(DIMMED_CLASS, dim);
                });
                this._dimmed = !!filter;
            }
        },
        {
            /**
   * Fire the `filterChange` event on this (source) chart.
   * @param {import('./Crossfilter').default} cf @param {any} key
   */ key: "_fireFilterChange",
            value: function _fireFilterChange(cf, key) {
                var _a;
                var args = __spreadProps(__spreadValues({}, cf.state()), {
                    sourceChartID: this._chartId(),
                    key: key
                });
                var events = this.w.config.chart.events;
                if (typeof events.filterChange === "function") {
                    events.filterChange(this.ctx, args);
                }
                (_a = this.ctx.events) == null ? void 0 : _a.fireEvent("filterChange", [
                    this.ctx,
                    args
                ]);
            }
        },
        {
            key: "teardown",
            value: function teardown() {
                var _a, _b, _c, _d, _e, _f;
                this.clear();
                if (this._wired) {
                    (_b = (_a = this.ctx).removeEventListener) == null ? void 0 : _b.call(_a, "dataPointSelection", this._onPointSelect);
                    (_d = (_c = this.ctx).removeEventListener) == null ? void 0 : _d.call(_c, "mounted", this._afterRender);
                    (_f = (_e = this.ctx).removeEventListener) == null ? void 0 : _f.call(_e, "updated", this._afterRender);
                    var cf = this._cf();
                    if (cf) {
                        cf.off("change", this._onChange);
                        cf.removeDimension(this._chartId());
                    }
                    this._wired = false;
                }
            }
        }
    ]);
    return LinkedViews;
}();
ApexCharts.registerFeatures({
    linkedViews: LinkedViews
});
var AC = /** @type {any} */ ApexCharts;
AC._crossfilterFactory = function(opts) {
    return Crossfilter.getOrCreate(opts);
};
AC._crossfilterGet = function(id) {
    return Crossfilter.get(id);
};
var DRAG_CLASS = "apexcharts-ink-draggable";
var TYPES = [
    "point",
    "xaxis",
    "yaxis"
];
var EDGE_PX = 8;
var CLICK_SLOP_PX = 2;
var FONT_STEPS = [
    10,
    11,
    12,
    14,
    17,
    20
];
var MARKER_SHAPES = [
    "circle",
    "square",
    "diamond",
    "triangle"
];
var SHAPE_GLYPHS = {
    circle: "●",
    square: "■",
    diamond: "◆",
    triangle: "▲"
};
var NOTE_COLORS = [
    "#ffffff",
    "#334155",
    "#2563eb",
    "#16a34a",
    "#d97706",
    "#dc2626"
];
var TRASH_ICON = '<svg viewBox="0 0 24 24" width="13" height="13" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 6h18M8 6V4a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1v2m3 0v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>';
var InkLayer = /*#__PURE__*/ function() {
    "use strict";
    function InkLayer(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, InkLayer);
        this.w = w;
        this.ctx = ctx;
        this._wired = false;
        this._drag = null;
        this._editor = null;
        this._creating = false;
        this._createSeq = 0;
        this._attach = this._attach.bind(this);
        this._onRerender = this._onRerender.bind(this);
        this._onMove = this._onMove.bind(this);
        this._onUp = this._onUp.bind(this);
        this._onCreateClick = this._onCreateClick.bind(this);
        this._onDocDownEditor = this._onDocDownEditor.bind(this);
        if (this._enabledGlobally() || this._hasDraggable() || this._paletteEnabled()) {
            this._wire();
        }
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(InkLayer, [
        {
            key: "_enabledGlobally",
            value: function _enabledGlobally() {
                var ink = this.w.config.chart.ink;
                return !!(ink && ink.enabled);
            }
        },
        {
            key: "_paletteEnabled",
            value: function _paletteEnabled() {
                var ink = this.w.config.chart.ink;
                return !!(ink && ink.palette);
            }
        },
        {
            key: "_snapEnabled",
            value: function _snapEnabled() {
                var ink = this.w.config.chart.ink;
                return !!(ink && ink.snap);
            }
        },
        {
            // ─── P5: snap to gridlines ────────────────────────────────────────────────
            /** @param {number} v @param {number[]} ticks @returns {number} nearest tick */ key: "_nearest",
            value: function _nearest(v, ticks) {
                var best = v;
                var bd = Infinity;
                for(var i = 0; i < ticks.length; i++){
                    var d = Math.abs(ticks[i] - v);
                    if (d < bd) {
                        bd = d;
                        best = ticks[i];
                    }
                }
                return best;
            }
        },
        {
            /**
   * Snap an x value to the nearest x gridline (numeric axes only).
   * @param {number} x
   */ key: "_snapX",
            value: function _snapX(x) {
                if (!this._snapEnabled() || typeof x !== "number") return x;
                var s = this.w.globals.xAxisScale;
                return s && Array.isArray(s.result) && s.result.length ? this._nearest(x, s.result) : x;
            }
        },
        {
            /**
   * Snap a y value to the nearest y gridline.
   * @param {number} y @param {number} si
   */ key: "_snapY",
            value: function _snapY(y, si) {
                if (!this._snapEnabled() || typeof y !== "number") return y;
                var scales = this.w.globals.yAxisScale;
                var s = scales && scales[si];
                return s && Array.isArray(s.result) && s.result.length ? this._nearest(y, s.result) : y;
            }
        },
        {
            /** @param {string} type @returns {any[]} the config annotations of a type */ key: "_annoList",
            value: function _annoList(type) {
                var a = this.w.config.annotations;
                if (!a) return [];
                var key = type === "point" ? "points" : type;
                return Array.isArray(a[key]) ? a[key] : [];
            }
        },
        {
            /** @param {any} anno */ key: "_isDraggable",
            value: function _isDraggable(anno) {
                if (!anno) return false;
                if (anno.draggable === true) return true;
                if (anno.draggable === false) return false;
                return this._enabledGlobally();
            }
        },
        {
            key: "_hasDraggable",
            value: function _hasDraggable() {
                var _this = this;
                return TYPES.some(function(t) {
                    return _this._annoList(t).some(function(p) {
                        return _this._isDraggable(p);
                    });
                });
            }
        },
        {
            key: "_wire",
            value: function _wire() {
                if (this._wired) return;
                this._wired = true;
                this.ctx.addEventListener("mounted", this._onRerender);
                this.ctx.addEventListener("updated", this._onRerender);
            }
        },
        {
            /**
   * A full (re)render rebuilds the SVG and may swap the annotations config
   * (updateOptions, undo restore), so an open editor card points at stale
   * state: drop it (without committing) before rebinding handlers. Targeted
   * redraws call _attach() directly and keep the card open.
   */ key: "_onRerender",
            value: function _onRerender() {
                this._closeEditor(false);
                this._attach();
                if (this._creating) {
                    var svg = this.w.dom.Paper && this.w.dom.Paper.node;
                    if (svg) {
                        svg.style.cursor = "crosshair";
                        svg.addEventListener("click", this._onCreateClick, true);
                    }
                }
            }
        },
        {
            /**
   * After each (re)render, bind drag + edit handlers to every draggable
   * annotation's elements. Idempotent via a per-node flag so a targeted redraw
   * re-runs this without double-binding the untouched annotations.
   */ key: "_attach",
            value: function _attach() {
                var _this = this;
                var w = this.w;
                var baseEl = w.dom.baseEl;
                if (!baseEl) return;
                TYPES.forEach(function(type) {
                    _this._annoList(type).forEach(function(anno, index) {
                        if (!_this._isDraggable(anno)) return;
                        if (!anno.id) {
                            anno.id = "apexcharts-ink-" + type + "-" + index + "-" + w.globals.chartID;
                        }
                        baseEl.querySelectorAll("." + anno.id).forEach(function(el) {
                            if (el.__inkBound) return;
                            el.__inkBound = true;
                            el.style.cursor = "move";
                            el.classList.add(DRAG_CLASS);
                            el.addEventListener("mousedown", function(e) {
                                return _this._onDown(e, type, index);
                            });
                            el.addEventListener("touchstart", function(e) {
                                return _this._onDown(e, type, index);
                            });
                            el.addEventListener("dblclick", function(e) {
                                e.preventDefault();
                                e.stopPropagation();
                                _this._startEdit(type, index, {
                                    select: true
                                });
                            });
                        });
                    });
                });
                if (this._paletteEnabled()) this._renderPalette();
            }
        },
        {
            // ─── drag / resize ────────────────────────────────────────────────────────
            /**
   * @param {any} e @param {string} type @param {number} index
   */ key: "_onDown",
            value: function _onDown(e, type, index) {
                if (e.button && e.button !== 0) return;
                var w = this.w;
                var doc = w.dom.baseEl && w.dom.baseEl.ownerDocument;
                if (!doc) return;
                e.stopPropagation();
                if (e.cancelable) e.preventDefault();
                var isTouch = e.type === "touchstart";
                var ev = isTouch ? e.touches[0] : e;
                var svgRoot = w.dom.Paper && w.dom.Paper.node;
                var ctm = svgRoot && svgRoot.getScreenCTM ? svgRoot.getScreenCTM() : null;
                var anno = this._annoList(type)[index];
                var mode = "move";
                var rect = null;
                var origX = 0;
                var origW = 0;
                if (type === "xaxis" && anno.x2 != null) {
                    rect = w.dom.baseEl.querySelector(".apexcharts-annotation-rect." + anno.id);
                    if (rect) {
                        var r = rect.getBoundingClientRect();
                        if (Math.abs(ev.clientX - r.left) <= EDGE_PX) mode = "resize-x1";
                        else if (Math.abs(ev.clientX - r.right) <= EDGE_PX) mode = "resize-x2";
                        origX = parseFloat(rect.getAttribute("x")) || 0;
                        origW = parseFloat(rect.getAttribute("width")) || 0;
                    }
                }
                this._drag = {
                    type: type,
                    index: index,
                    anno: anno,
                    els: Array.from(w.dom.baseEl.querySelectorAll("." + anno.id)),
                    mode: mode,
                    rect: rect,
                    origX: origX,
                    origW: origW,
                    startX: ev.clientX,
                    startY: ev.clientY,
                    scaleX: ctm && ctm.a ? ctm.a : 1,
                    scaleY: ctm && ctm.d ? ctm.d : 1,
                    dxPixel: 0,
                    dyPixel: 0,
                    moved: false
                };
                doc.addEventListener("mousemove", this._onMove);
                doc.addEventListener("touchmove", this._onMove, {
                    passive: false
                });
                doc.addEventListener("mouseup", this._onUp);
                doc.addEventListener("touchend", this._onUp);
            }
        },
        {
            /** @param {any} me */ key: "_onMove",
            value: function _onMove(me) {
                var d = this._drag;
                if (!d) return;
                if (me.cancelable) me.preventDefault();
                var mev = me.type === "touchmove" ? me.touches[0] : me;
                d.dxPixel = (mev.clientX - d.startX) / d.scaleX;
                d.dyPixel = (mev.clientY - d.startY) / d.scaleY;
                if (Math.abs(d.dxPixel) > CLICK_SLOP_PX || Math.abs(d.dyPixel) > CLICK_SLOP_PX) {
                    d.moved = true;
                }
                if (d.mode === "move") {
                    var t = "translate(".concat(d.dxPixel, " ").concat(d.dyPixel, ")");
                    d.els.forEach(function(el) {
                        return el.setAttribute("transform", t);
                    });
                } else if (d.rect) {
                    if (d.mode === "resize-x1") {
                        d.rect.setAttribute("x", d.origX + d.dxPixel);
                        d.rect.setAttribute("width", Math.max(1, d.origW - d.dxPixel));
                    } else if (d.mode === "resize-x2") {
                        d.rect.setAttribute("width", Math.max(1, d.origW + d.dxPixel));
                    }
                }
            }
        },
        {
            key: "_onUp",
            value: function _onUp() {
                var d = this._drag;
                this._drag = null;
                this._teardownDocListeners();
                if (!d || !d.moved) {
                    if (d) {
                        d.els.forEach(function(el) {
                            return el.removeAttribute("transform");
                        });
                        this._startEdit(d.type, d.index);
                    }
                    return;
                }
                var anno = this._annoList(d.type)[d.index];
                if (!anno) return;
                this._applyDelta(d, anno);
                d.els.forEach(function(el) {
                    return el.removeAttribute("transform");
                });
                this._redrawAnno(d.type, anno, d.index);
                this._checkpoint("ink:drag");
                this._fireDragged(d.type, anno, d.index);
            }
        },
        {
            /**
   * Record a Rewind (undo) checkpoint for an ink edit. Targeted redraws fire no
   * 'updated' event, so History would otherwise miss them. No-op when the
   * history feature is absent or disabled.
   * @param {string} label
   */ key: "_checkpoint",
            value: function _checkpoint(label) {
                var _a, _b;
                (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, label);
            }
        },
        {
            /**
   * Mutate the annotation's config from the pixel drag delta (type + mode aware).
   * @param {any} d @param {any} anno
   */ key: "_applyDelta",
            value: function _applyDelta(d, anno) {
                var w = this.w;
                var dxData = w.layout.gridWidth ? d.dxPixel * (w.globals.xRange / w.layout.gridWidth) : 0;
                if (d.type === "point") {
                    var _this__invertPoint = this._invertPoint(anno, d.dxPixel, d.dyPixel), newX = _this__invertPoint.newX, newY = _this__invertPoint.newY;
                    anno.x = this._snapX(newX);
                    if (newY != null) {
                        var yi = anno.yAxisIndex || 0;
                        var map = w.globals.seriesYAxisMap;
                        anno.y = this._snapY(newY, map && map[yi] ? map[yi][0] : 0);
                    }
                    return;
                }
                if (d.type === "xaxis") {
                    if (typeof anno.x !== "number") return;
                    if (d.mode === "move") {
                        if (typeof anno.x2 === "number") {
                            anno.x += dxData;
                            anno.x2 += dxData;
                        } else {
                            anno.x = this._snapX(anno.x + dxData);
                        }
                    } else if (d.mode === "resize-x1" || d.mode === "resize-x2") {
                        var xIsLeft = anno.x2 == null || anno.x <= anno.x2;
                        var grow = d.mode === "resize-x2" ? !xIsLeft : xIsLeft;
                        if (grow) anno.x = this._snapX(anno.x + dxData);
                        else if (typeof anno.x2 === "number") anno.x2 = this._snapX(anno.x2 + dxData);
                    }
                    return;
                }
                if (d.type === "yaxis") {
                    var yi1 = anno.yAxisIndex || 0;
                    var map1 = w.globals.seriesYAxisMap;
                    var si = map1 && map1[yi1] ? map1[yi1][0] : 0;
                    var yRange = w.globals.yRange ? w.globals.yRange[si] : null;
                    if (yRange == null || !w.layout.gridHeight) return;
                    var dyData = -d.dyPixel * (yRange / w.layout.gridHeight);
                    if (typeof anno.y2 === "number") {
                        if (typeof anno.y === "number") anno.y += dyData;
                        anno.y2 += dyData;
                    } else if (typeof anno.y === "number") {
                        anno.y = this._snapY(anno.y + dyData, si);
                    }
                }
            }
        },
        {
            /**
   * Invert a pixel drag delta to a point annotation's data x/y.
   * @param {any} anno @param {number} dxPixel @param {number} dyPixel
   * @returns {{newX:any, newY:any}}
   */ key: "_invertPoint",
            value: function _invertPoint(anno, dxPixel, dyPixel) {
                var w = this.w;
                var categoryX = (w.config.xaxis.type === "category" || w.config.xaxis.convertedCatToNumeric) && !w.axisFlags.dataFormatXNumeric;
                var newX = anno.x;
                if (!categoryX && typeof anno.x === "number" && w.layout.gridWidth) {
                    newX = anno.x + dxPixel * (w.globals.xRange / w.layout.gridWidth);
                }
                var newY = anno.y;
                var yi = anno.yAxisIndex || 0;
                var map = w.globals.seriesYAxisMap;
                var si = map && map[yi] ? map[yi][0] : 0;
                var yRange = w.globals.yRange ? w.globals.yRange[si] : null;
                var logY = w.config.yaxis[yi] && w.config.yaxis[yi].logarithmic;
                if (typeof anno.y === "number" && yRange != null && !logY && w.layout.gridHeight) {
                    newY = anno.y - dyPixel * (yRange / w.layout.gridHeight);
                }
                return {
                    newX: newX,
                    newY: newY
                };
            }
        },
        {
            /**
   * Targeted redraw of one annotation: drop its elements and re-add the shape +
   * label + label background at the current config coordinates (no full chart
   * re-render, and repeat-safe unlike updateOptions({})).
   * @param {string} type @param {any} anno @param {number} index
   */ key: "_redrawAnno",
            value: function _redrawAnno(type, anno, index) {
                var w = this.w;
                var baseEl = w.dom.baseEl;
                var annotations = this.ctx.annotations;
                if (!baseEl || !annotations) return;
                baseEl.querySelectorAll("." + anno.id).forEach(function(el) {
                    return el.remove();
                });
                var group = baseEl.querySelector(".apexcharts-" + type + "-annotations");
                if (!group) return;
                if (type === "point" && annotations.pointsAnnotations) {
                    annotations.pointsAnnotations.addPointAnnotation(anno, group, index);
                } else if (type === "xaxis" && annotations.xAxisAnnotations) {
                    annotations.xAxisAnnotations.addXaxisAnnotation(anno, group, index);
                } else if (type === "yaxis" && annotations.yAxisAnnotations) {
                    annotations.yAxisAnnotations.addYaxisAnnotation(anno, group, index);
                }
                var labelEl = baseEl.querySelector(".apexcharts-" + type + "-annotation-label." + anno.id);
                if (labelEl && annotations.helpers && anno.label && anno.label.text) {
                    var elRect = annotations.helpers.addBackgroundToAnno(labelEl, anno);
                    if (elRect && labelEl.parentNode) {
                        labelEl.parentNode.insertBefore(elRect.node, labelEl);
                    }
                }
                this._attach();
            }
        },
        {
            /**
   * Dispatch an ink annotation lifecycle event both to the user callback
   * (`chart.events[name]`) and the internal event bus, in that order.
   * @param {string} name @param {any} args
   */ key: "_fireAnnotationEvent",
            value: function _fireAnnotationEvent(name2, args) {
                var _a;
                var events = this.w.config.chart.events;
                if (typeof events[name2] === "function") {
                    events[name2](this.ctx, args);
                }
                (_a = this.ctx.events) == null ? void 0 : _a.fireEvent(name2, [
                    this.ctx,
                    args
                ]);
            }
        },
        {
            /** @param {string} type @param {any} anno @param {number} index */ key: "_fireDragged",
            value: function _fireDragged(type, anno, index) {
                var args = {
                    type: type,
                    id: anno.id,
                    index: index,
                    x: anno.x,
                    y: anno.y
                };
                if (anno.x2 != null) args.x2 = anno.x2;
                if (anno.y2 != null) args.y2 = anno.y2;
                this._fireAnnotationEvent("annotationDragged", args);
            }
        },
        {
            // ─── P3: click-to-create ─────────────────────────────────────────────────
            /**
   * Enter create mode: the next click on the plot area drops a new draggable
   * point annotation there and opens its label editor.
   */ key: "startCreate",
            value: function startCreate() {
                if (this._creating) return;
                var svg = this.w.dom.Paper && this.w.dom.Paper.node;
                if (!svg) return;
                this._creating = true;
                svg.style.cursor = "crosshair";
                svg.addEventListener("click", this._onCreateClick, true);
                this._syncPalette();
            }
        },
        {
            /** Leave create mode. */ key: "stopCreate",
            value: function stopCreate() {
                if (!this._creating) return;
                this._creating = false;
                var svg = this.w.dom.Paper && this.w.dom.Paper.node;
                if (svg) {
                    svg.style.cursor = "";
                    svg.removeEventListener("click", this._onCreateClick, true);
                }
                this._syncPalette();
            }
        },
        {
            /** @param {any} e */ key: "_onCreateClick",
            value: function _onCreateClick(e) {
                if (!this._creating) return;
                e.preventDefault();
                e.stopPropagation();
                var pos = this._pixelToData(e.clientX, e.clientY);
                this.stopCreate();
                if (!pos) return;
                this.createAt(pos.x, pos.y);
            }
        },
        {
            /**
   * Create a draggable note at data coordinates and open its editor card.
   * Public: the context menu's "Add note here" routes here so its notes are
   * config-backed too, and thus draggable, editable, persistable and undoable.
   * @param {any} x @param {any} y @param {{text?: string}} [opts]
   * @returns {any} the created annotation config
   */ key: "createAt",
            value: function createAt(x, y) {
                var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
                var w = this.w;
                this._wire();
                this._createSeq += 1;
                var id = "apexcharts-ink-new-" + this._createSeq + "-" + w.globals.chartID;
                var anno = Utils$1.extend(new Options().pointAnnotation, {
                    x: x,
                    y: y,
                    id: id,
                    draggable: true,
                    label: {
                        text: opts.text || "Note"
                    }
                });
                if (!w.config.annotations) w.config.annotations = {};
                if (!Array.isArray(w.config.annotations.points)) w.config.annotations.points = [];
                w.config.annotations.points.push(anno);
                var index = w.config.annotations.points.length - 1;
                this._redrawAnno("point", anno, index);
                this._checkpoint("ink:create");
                this._fireCreated("point", anno, index);
                this._startEdit("point", index, {
                    select: true
                });
                return anno;
            }
        },
        {
            /**
   * Create a draggable dashed LINE annotation at a data value and open its
   * editor card: axis 'x' drops a vertical line at a data x, axis 'y' a
   * horizontal line at a data y. Public: the context menu's "Annotate here"
   * routes here so its lines are config-backed too, and thus draggable,
   * editable, persistable and undoable. Lines only: x2/y2 are never set, so
   * this can never produce a range rectangle.
   * @param {'x'|'y'} axis @param {any} val
   * @param {{text?: string, strokeDashArray?: number, color?: string, select?: boolean}} [opts]
   * @returns {any} the created annotation config
   */ key: "createLineAt",
            value: function createLineAt(axis, val) {
                var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
                var w = this.w;
                this._wire();
                this._createSeq += 1;
                var id = "apexcharts-ink-new-" + this._createSeq + "-" + w.globals.chartID;
                var type = axis === "y" ? "yaxis" : "xaxis";
                var defaults = type === "yaxis" ? new Options().yAxisAnnotation : new Options().xAxisAnnotation;
                var over = {
                    id: id,
                    draggable: true,
                    strokeDashArray: opts.strokeDashArray != null ? opts.strokeDashArray : 4,
                    label: {
                        text: opts.text || ""
                    }
                };
                if (opts.color) {
                    over.borderColor = opts.color;
                    over.label.borderColor = opts.color;
                }
                if (type === "yaxis") over.y = val;
                else over.x = val;
                var anno = Utils$1.extend(defaults, over);
                if (!w.config.annotations) w.config.annotations = {};
                if (!Array.isArray(w.config.annotations[type])) w.config.annotations[type] = [];
                w.config.annotations[type].push(anno);
                var index = w.config.annotations[type].length - 1;
                this._redrawAnno(type, anno, index);
                this._checkpoint("ink:create");
                this._fireCreated(type, anno, index);
                if (opts.select !== false) this._startEdit(type, index, {
                    select: true
                });
                return anno;
            }
        },
        {
            /**
   * Convert a client-space point to data coordinates (absolute, for create).
   * @param {number} clientX @param {number} clientY
   * @returns {{x:any, y:any}|null}
   */ key: "_pixelToData",
            value: function _pixelToData(clientX, clientY) {
                var w = this.w;
                var gridEl = w.dom.baseEl && w.dom.baseEl.querySelector(".apexcharts-grid");
                if (!gridEl) return null;
                var g = gridEl.getBoundingClientRect();
                if (!g.width || !g.height) return null;
                var fx = (clientX - g.left) / g.width;
                var fy = (clientY - g.top) / g.height;
                var minX = w.globals.minX;
                var xRange = w.globals.xRange;
                var minY = w.globals.minYArr && w.globals.minYArr[0] != null ? w.globals.minYArr[0] : w.globals.minY;
                var yRange = w.globals.yRange && w.globals.yRange[0] != null ? w.globals.yRange[0] : w.globals.maxY - w.globals.minY;
                var x = minX + fx * xRange;
                var y = minY + (1 - fy) * yRange;
                var categoryX = (w.config.xaxis.type === "category" || w.config.xaxis.convertedCatToNumeric) && !w.axisFlags.dataFormatXNumeric;
                if (categoryX) x = Math.round(x);
                return {
                    x: x,
                    y: y
                };
            }
        },
        {
            /** @param {string} type @param {any} anno @param {number} index */ key: "_fireCreated",
            value: function _fireCreated(type, anno, index) {
                var args = {
                    type: type,
                    id: anno.id,
                    index: index
                };
                if (typeof anno.x !== "undefined") args.x = anno.x;
                if (typeof anno.y !== "undefined") args.y = anno.y;
                this._fireAnnotationEvent("annotationCreated", args);
            }
        },
        {
            // ─── P3: tool palette ────────────────────────────────────────────────────
            /** Render a minimal "add note" toggle into the chart wrap (once per render). */ key: "_renderPalette",
            value: function _renderPalette() {
                var _this = this;
                var w = this.w;
                var elWrap = w.dom.elWrap;
                if (!elWrap || elWrap.querySelector(".apexcharts-ink-palette")) return;
                var doc = elWrap.ownerDocument;
                var bar = doc.createElement("div");
                bar.className = "apexcharts-ink-palette";
                var s = bar.style;
                s.position = "absolute";
                s.top = "6px";
                s.left = "6px";
                s.zIndex = "15";
                var btn = doc.createElement("button");
                btn.type = "button";
                btn.className = "apexcharts-ink-add";
                btn.textContent = "+ Note";
                var bs = btn.style;
                bs.cursor = "pointer";
                bs.font = "12px sans-serif";
                bs.padding = "4px 9px";
                bs.borderRadius = "5px";
                bs.border = "1px solid #6366f1";
                bs.color = "#4338ca";
                bs.background = "#fff";
                btn.addEventListener("click", function(e) {
                    e.stopPropagation();
                    if (_this._creating) _this.stopCreate();
                    else _this.startCreate();
                });
                bar.appendChild(btn);
                elWrap.appendChild(bar);
                this._syncPalette();
            }
        },
        {
            /** Reflect create-mode state on the palette button. */ key: "_syncPalette",
            value: function _syncPalette() {
                var elWrap = this.w.dom.elWrap;
                var btn = /** @type {any} */ elWrap && elWrap.querySelector(".apexcharts-ink-add");
                if (!btn) return;
                if (this._creating) {
                    btn.style.background = "#6366f1";
                    btn.style.color = "#fff";
                    btn.textContent = "Click chart...";
                } else {
                    btn.style.background = "#fff";
                    btn.style.color = "#4338ca";
                    btn.textContent = "+ Note";
                }
            }
        },
        {
            // ─── P2 + P6: the floating note editor card ──────────────────────────────
            // Click (or double-click) an ink-managed annotation to open a small card
            // anchored to it: rename inline, recolor via accent swatches, toggle bold,
            // step the font size, size/reshape the marker (points), or delete the note.
            /** @returns {string[]} the accent swatches offered by the editor */ key: "_noteColors",
            value: function _noteColors() {
                var ink = this.w.config.chart.ink;
                return ink && Array.isArray(ink.noteColors) && ink.noteColors.length ? ink.noteColors : NOTE_COLORS;
            }
        },
        {
            /**
   * Small icon/text button for the editor card. mousedown is prevented so the
   * text input keeps focus while formatting.
   * @param {any} doc @param {string} content @param {string} title
   * @param {Function} onClick @param {string} [extraClass] @param {boolean} [isSvg]
   */ key: "_cardBtn",
            value: function _cardBtn(doc, content, title, onClick, extraClass, isSvg) {
                var b = doc.createElement("button");
                b.type = "button";
                b.className = "apexcharts-ink-btn" + (extraClass ? " " + extraClass : "");
                b.title = title;
                b.setAttribute("aria-label", title);
                if (isSvg) b.innerHTML = content;
                else b.textContent = content;
                b.addEventListener("mousedown", function(e) {
                    return e.preventDefault();
                });
                b.addEventListener("click", function(e) {
                    e.stopPropagation();
                    onClick();
                });
                return b;
            }
        },
        {
            /**
   * Uppercase row caption ("Label" / "Line" / "Marker") for the editor card.
   * @param {any} doc @param {string} text
   */ key: "_cardLabel",
            value: function _cardLabel(doc, text) {
                var lab = doc.createElement("span");
                lab.className = "apexcharts-ink-cardlabel";
                lab.textContent = text;
                return lab;
            }
        },
        {
            /**
   * Color swatch button for the editor card. mousedown is prevented so the
   * text input keeps focus while restyling.
   * @param {any} doc @param {string} c
   * @param {(c: string) => void} pick @param {string} [extraClass]
   */ key: "_mkSwatch",
            value: function _mkSwatch(doc, c, pick, extraClass) {
                var sw = doc.createElement("button");
                sw.type = "button";
                sw.className = "apexcharts-ink-swatch" + (extraClass ? " " + extraClass : "");
                sw.title = c;
                sw.setAttribute("aria-label", "Color " + c);
                sw.dataset.color = c;
                sw.style.background = c;
                sw.addEventListener("mousedown", function(e) {
                    return e.preventDefault();
                });
                sw.addEventListener("click", function(e) {
                    e.stopPropagation();
                    pick(c);
                });
                return sw;
            }
        },
        {
            /**
   * Open the floating editor card for an annotation.
   * @param {string} type @param {number} index
   * @param {{select?: boolean}} [opts] select: preselect the text (create / dblclick)
   */ key: "_startEdit",
            value: function _startEdit(type, index) {
                var _this = this;
                var opts = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
                var w = this.w;
                var anno = this._annoList(type)[index];
                var baseEl = w.dom.baseEl;
                var elWrap = w.dom.elWrap;
                if (!anno || !anno.id || !baseEl || !elWrap) return;
                this._closeEditor(false);
                var doc = baseEl.ownerDocument;
                var card = doc.createElement("div");
                card.className = "apexcharts-ink-card";
                card.setAttribute("role", "dialog");
                card.setAttribute("aria-label", "Edit note");
                card.style.visibility = "hidden";
                var rowText = doc.createElement("div");
                rowText.className = "apexcharts-ink-card-row";
                var input = doc.createElement("input");
                input.type = "text";
                input.className = "apexcharts-ink-editor";
                input.placeholder = "Note text";
                input.value = anno.label && anno.label.text || "";
                rowText.appendChild(input);
                rowText.appendChild(this._cardBtn(doc, TRASH_ICON, "Delete note", function() {
                    return _this._deleteAnno();
                }, "apexcharts-ink-btn--delete", true));
                card.appendChild(rowText);
                var rowStyle = doc.createElement("div");
                rowStyle.className = "apexcharts-ink-card-row";
                if (type !== "point") {
                    rowStyle.appendChild(this._cardLabel(doc, "Label"));
                }
                this._noteColors().forEach(function(c) {
                    rowStyle.appendChild(_this._mkSwatch(doc, c, function(col) {
                        return _this._applyColor(col);
                    }));
                });
                var sep = doc.createElement("span");
                sep.className = "apexcharts-ink-sep";
                rowStyle.appendChild(sep);
                rowStyle.appendChild(this._cardBtn(doc, "B", "Bold", function() {
                    return _this._toggleBold();
                }, "apexcharts-ink-btn--bold"));
                rowStyle.appendChild(this._cardBtn(doc, "A-", "Smaller text", function() {
                    return _this._stepFont(-1);
                }));
                rowStyle.appendChild(this._cardBtn(doc, "A+", "Larger text", function() {
                    return _this._stepFont(1);
                }));
                card.appendChild(rowStyle);
                if (type !== "point") {
                    var rowLine = doc.createElement("div");
                    rowLine.className = "apexcharts-ink-card-row";
                    rowLine.appendChild(this._cardLabel(doc, "Line"));
                    this._noteColors().forEach(function(c) {
                        rowLine.appendChild(_this._mkSwatch(doc, c, function(col) {
                            return _this._applyLineColor(col);
                        }, "apexcharts-ink-swatch--line"));
                    });
                    card.appendChild(rowLine);
                }
                if (type === "point") {
                    var rowMarker = doc.createElement("div");
                    rowMarker.className = "apexcharts-ink-card-row";
                    rowMarker.appendChild(this._cardLabel(doc, "Marker"));
                    rowMarker.appendChild(this._cardBtn(doc, "-", "Smaller marker", function() {
                        return _this._stepMarker(-1);
                    }));
                    var sizeOut = doc.createElement("span");
                    sizeOut.className = "apexcharts-ink-marker-size";
                    rowMarker.appendChild(sizeOut);
                    rowMarker.appendChild(this._cardBtn(doc, "+", "Larger marker", function() {
                        return _this._stepMarker(1);
                    }));
                    rowMarker.appendChild(this._cardBtn(doc, SHAPE_GLYPHS.circle, "Marker shape", function() {
                        return _this._cycleShape();
                    }, "apexcharts-ink-btn--shape"));
                    card.appendChild(rowMarker);
                }
                elWrap.appendChild(card);
                this._editor = {
                    card: card,
                    input: input,
                    type: type,
                    index: index
                };
                this._positionCard();
                this._syncCard();
                card.style.visibility = "";
                input.focus();
                if (opts.select) input.select();
                card.addEventListener("keydown", function(e) {
                    if (e.key === "Escape") {
                        e.preventDefault();
                        e.stopPropagation();
                        _this._closeEditor(false);
                    } else if (e.key === "Enter" && e.target === input) {
                        e.preventDefault();
                        _this._closeEditor(true);
                    }
                });
                doc.addEventListener("mousedown", this._onDocDownEditor, true);
                doc.addEventListener("touchstart", this._onDocDownEditor, true);
            }
        },
        {
            /**
   * Commit + close on any press outside the card.
   * @param {any} e
   */ key: "_onDocDownEditor",
            value: function _onDocDownEditor(e) {
                var ed = this._editor;
                if (!ed || ed.card.contains(e.target)) return;
                this._closeEditor(true);
            }
        },
        {
            /**
   * Anchor the card to the annotation's label (below it, or above when there
   * is no room), clamped inside the chart wrap. Re-run after each restyle
   * since the label rect changes.
   */ key: "_positionCard",
            value: function _positionCard() {
                var ed = this._editor;
                if (!ed) return;
                var w = this.w;
                var baseEl = w.dom.baseEl;
                var elWrap = w.dom.elWrap;
                var anno = this._annoList(ed.type)[ed.index];
                if (!baseEl || !elWrap || !anno) return;
                var anchor = baseEl.querySelector(".apexcharts-" + ed.type + "-annotation-label." + anno.id) || baseEl.querySelector("." + anno.id);
                if (!anchor) return;
                var wrapRect = elWrap.getBoundingClientRect();
                var aRect = anchor.getBoundingClientRect();
                var cw = ed.card.offsetWidth;
                var ch = ed.card.offsetHeight;
                var left = Math.round(aRect.left - wrapRect.left);
                var top = Math.round(aRect.bottom - wrapRect.top) + 8;
                if (top + ch > elWrap.clientHeight - 4) {
                    top = Math.round(aRect.top - wrapRect.top) - ch - 8;
                }
                if (left + cw > elWrap.clientWidth - 4) left = elWrap.clientWidth - cw - 4;
                ed.card.style.left = Math.max(4, left) + "px";
                ed.card.style.top = Math.max(4, top) + "px";
            }
        },
        {
            /** Reflect the annotation's current style on the card controls. */ key: "_syncCard",
            value: function _syncCard() {
                var ed = this._editor;
                if (!ed) return;
                var anno = this._annoList(ed.type)[ed.index];
                if (!anno) return;
                var style = anno.label && anno.label.style || {};
                var bg = String(style.background || "").toLowerCase();
                ed.card.querySelectorAll(".apexcharts-ink-swatch:not(.apexcharts-ink-swatch--line)").forEach(function(sw) {
                    sw.classList.toggle("apexcharts-ink-swatch--active", (sw.dataset.color || "").toLowerCase() === bg);
                });
                var stroke = String(anno.borderColor || "").toLowerCase();
                ed.card.querySelectorAll(".apexcharts-ink-swatch--line").forEach(function(sw) {
                    sw.classList.toggle("apexcharts-ink-swatch--active", (sw.dataset.color || "").toLowerCase() === stroke);
                });
                var boldBtn = ed.card.querySelector(".apexcharts-ink-btn--bold");
                if (boldBtn) {
                    boldBtn.classList.toggle("apexcharts-ink-btn--active", InkLayer._isBold(style));
                }
                var m = anno.marker || {};
                var sizeOut = ed.card.querySelector(".apexcharts-ink-marker-size");
                if (sizeOut) {
                    sizeOut.textContent = String(typeof m.size === "number" ? m.size : 4);
                }
                var shapeBtn = ed.card.querySelector(".apexcharts-ink-btn--shape");
                if (shapeBtn) {
                    shapeBtn.textContent = SHAPE_GLYPHS[m.shape] || SHAPE_GLYPHS.circle;
                }
            }
        },
        {
            /**
   * Commit the input's text into the annotation (the card stays open). Also
   * runs before any style apply so typed-but-unconfirmed text survives the
   * redraw.
   * @param {any} ed
   */ key: "_commitTextOf",
            value: function _commitTextOf(ed) {
                var anno = this._annoList(ed.type)[ed.index];
                if (!anno) return;
                var text = ed.input.value;
                if (!anno.label) anno.label = {};
                if (anno.label.text === text) return;
                anno.label.text = text;
                this._redrawAnno(ed.type, anno, ed.index);
                this._checkpoint("ink:edit");
                this._fireEdited(ed.type, anno, ed.index);
            }
        },
        {
            /**
   * Close the editor card. commit=true also commits the pending text. Style
   * edits apply immediately and are not rolled back by Escape; use undo.
   * @param {boolean} commit
   */ key: "_closeEditor",
            value: function _closeEditor(commit) {
                var ed = this._editor;
                if (!ed) return;
                this._editor = null;
                var doc = this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
                if (doc) {
                    doc.removeEventListener("mousedown", this._onDocDownEditor, true);
                    doc.removeEventListener("touchstart", this._onDocDownEditor, true);
                }
                if (ed.card.parentNode) ed.card.parentNode.removeChild(ed.card);
                if (commit) this._commitTextOf(ed);
            }
        },
        {
            /**
   * Apply a config mutation from a card control: commit pending text, mutate,
   * redraw, checkpoint for undo, then refresh + re-anchor the card.
   * @param {string} label @param {(anno: any) => void} mutate
   */ key: "_applyStyle",
            value: function _applyStyle(label, mutate) {
                var ed = this._editor;
                if (!ed) return;
                var anno = this._annoList(ed.type)[ed.index];
                if (!anno) return;
                this._commitTextOf(ed);
                if (!anno.label) anno.label = {};
                if (!anno.label.style) anno.label.style = {};
                mutate(anno);
                this._redrawAnno(ed.type, anno, ed.index);
                this._checkpoint(label);
                this._fireStyled(ed.type, anno, ed.index);
                this._syncCard();
                this._positionCard();
            }
        },
        {
            /**
   * Apply an accent color: label chip + marker (points) or line/range fill
   * (axis annotations), with text/border contrast following the luminance.
   * @param {string} c
   */ key: "_applyColor",
            value: function _applyColor(c) {
                var ed = this._editor;
                if (!ed) return;
                var light = InkLayer._isLight(c);
                this._applyStyle("ink:style", function(anno) {
                    anno.label.style.background = c;
                    anno.label.style.color = light ? "#334155" : "#ffffff";
                    anno.label.borderColor = light ? "#cbd5e1" : c;
                    if (ed.type === "point") {
                        if (!anno.marker) anno.marker = {};
                        anno.marker.strokeColor = light ? "#334155" : c;
                        anno.marker.fillColor = light ? "#ffffff" : c;
                    }
                });
            }
        },
        {
            /**
   * Line-stroke color for axis annotations, separate from the label chip.
   * @param {string} c
   */ key: "_applyLineColor",
            value: function _applyLineColor(c) {
                var ed = this._editor;
                if (!ed || ed.type === "point") return;
                this._applyStyle("ink:style", function(anno) {
                    anno.borderColor = c;
                    if (anno.x2 != null || anno.y2 != null) anno.fillColor = c;
                });
            }
        },
        {
            /**
   * Step the label font size through the preset scale.
   * @param {number} dir
   */ key: "_stepFont",
            value: function _stepFont(dir) {
                this._applyStyle("ink:style", function(anno) {
                    var cur = parseFloat(anno.label.style.fontSize) || 11;
                    var i = 0;
                    for(var k = 1; k < FONT_STEPS.length; k++){
                        if (Math.abs(FONT_STEPS[k] - cur) < Math.abs(FONT_STEPS[i] - cur)) i = k;
                    }
                    i = Math.min(FONT_STEPS.length - 1, Math.max(0, i + dir));
                    anno.label.style.fontSize = FONT_STEPS[i] + "px";
                });
            }
        },
        {
            key: "_toggleBold",
            value: function _toggleBold() {
                this._applyStyle("ink:style", function(anno) {
                    anno.label.style.fontWeight = InkLayer._isBold(anno.label.style) ? 400 : 700;
                });
            }
        },
        {
            /**
   * Grow/shrink the point marker.
   * @param {number} dir
   */ key: "_stepMarker",
            value: function _stepMarker(dir) {
                this._applyStyle("ink:style", function(anno) {
                    if (!anno.marker) anno.marker = {};
                    var cur = typeof anno.marker.size === "number" ? anno.marker.size : 4;
                    anno.marker.size = Math.min(14, Math.max(2, cur + dir));
                });
            }
        },
        {
            /** Cycle the point marker shape (circle, square, diamond, triangle). */ key: "_cycleShape",
            value: function _cycleShape() {
                this._applyStyle("ink:style", function(anno) {
                    if (!anno.marker) anno.marker = {};
                    var i = MARKER_SHAPES.indexOf(anno.marker.shape);
                    anno.marker.shape = MARKER_SHAPES[(i + 1) % MARKER_SHAPES.length];
                });
            }
        },
        {
            /** Delete the annotation the editor is open on (undoable via Rewind). */ key: "_deleteAnno",
            value: function _deleteAnno() {
                var ed = this._editor;
                if (!ed) return;
                var list = this._annoList(ed.type);
                var anno = list[ed.index];
                this._closeEditor(false);
                if (!anno) return;
                var baseEl = this.w.dom.baseEl;
                if (baseEl && anno.id) {
                    baseEl.querySelectorAll("." + anno.id).forEach(function(el) {
                        return el.remove();
                    });
                }
                list.splice(ed.index, 1);
                for(var i = ed.index; i < list.length; i++){
                    this._redrawAnno(ed.type, list[i], i);
                }
                this._checkpoint("ink:delete");
                this._fireDeleted(ed.type, anno, ed.index);
            }
        },
        {
            /** @param {string} type @param {any} anno @param {number} index */ key: "_fireEdited",
            value: function _fireEdited(type, anno, index) {
                var args = {
                    type: type,
                    id: anno.id,
                    index: index,
                    text: anno.label ? anno.label.text : ""
                };
                this._fireAnnotationEvent("annotationEdited", args);
            }
        },
        {
            /** @param {string} type @param {any} anno @param {number} index */ key: "_fireStyled",
            value: function _fireStyled(type, anno, index) {
                var args = {
                    type: type,
                    id: anno.id,
                    index: index,
                    label: anno.label,
                    marker: anno.marker
                };
                this._fireAnnotationEvent("annotationStyled", args);
            }
        },
        {
            /** @param {string} type @param {any} anno @param {number} index */ key: "_fireDeleted",
            value: function _fireDeleted(type, anno, index) {
                var args = {
                    type: type,
                    id: anno.id,
                    index: index
                };
                this._fireAnnotationEvent("annotationDeleted", args);
            }
        },
        {
            // ─── lifecycle ────────────────────────────────────────────────────────────
            key: "_teardownDocListeners",
            value: function _teardownDocListeners() {
                var doc = this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
                if (!doc) return;
                doc.removeEventListener("mousemove", this._onMove);
                doc.removeEventListener("touchmove", this._onMove);
                doc.removeEventListener("mouseup", this._onUp);
                doc.removeEventListener("touchend", this._onUp);
            }
        },
        {
            key: "teardown",
            value: function teardown() {
                var _a, _b, _c, _d;
                this._teardownDocListeners();
                this._closeEditor(false);
                this.stopCreate();
                this._drag = null;
                if (this._wired) {
                    (_b = (_a = this.ctx).removeEventListener) == null ? void 0 : _b.call(_a, "mounted", this._onRerender);
                    (_d = (_c = this.ctx).removeEventListener) == null ? void 0 : _d.call(_c, "updated", this._onRerender);
                    this._wired = false;
                }
            }
        }
    ], [
        {
            key: "_isLight",
            value: /**
   * Perceived-luminance check so text/border contrast follows the accent.
   * @param {string} hex
   */ function _isLight(hex) {
                var h = String(hex || "").replace("#", "");
                var full = h.length === 3 ? h.split("").map(function(c) {
                    return c + c;
                }).join("") : h;
                var n = parseInt(full, 16);
                if (isNaN(n) || full.length !== 6) return false;
                var r = n >> 16 & 255;
                var g = n >> 8 & 255;
                var b = n & 255;
                return (0.299 * r + 0.587 * g + 0.114 * b) / 255 > 0.72;
            }
        },
        {
            key: "_isBold",
            value: /** @param {any} style */ function _isBold(style) {
                var fw = style && style.fontWeight;
                return fw === "bold" || parseInt(String(fw), 10) >= 600;
            }
        }
    ]);
    return InkLayer;
}();
ApexCharts.registerFeatures({
    ink: InkLayer
});
var Measure = /*#__PURE__*/ function() {
    "use strict";
    function Measure(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Measure);
        this.w = w;
        this.ctx = ctx;
        this.graphics = new Graphics(w);
        this.pins = [];
        this.drag = null;
        this.armed = false;
        this.persistent = false;
        this.pane = null;
        this._seedActive = false;
        this._onKeyDown = this._onKeyDown.bind(this);
        this._onKeyUp = this._onKeyUp.bind(this);
        this._onDown = this._onDown.bind(this);
        this._onMove = this._onMove.bind(this);
        this._onUp = this._onUp.bind(this);
        this._onSeedDown = this._onSeedDown.bind(this);
        this._onSeedKey = this._onSeedKey.bind(this);
        this._afterRender = this._afterRender.bind(this);
        ctx.addEventListener("mounted", this._afterRender);
        ctx.addEventListener("updated", this._afterRender);
        this._bindKeys();
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Measure, [
        {
            key: "_cfg",
            value: function _cfg() {
                return this.w.config.chart.measure || {};
            }
        },
        {
            key: "_enabled",
            value: function _enabled() {
                return this.w.globals.axisCharts && this._cfg().enabled === true;
            }
        },
        {
            key: "_mode",
            value: function _mode() {
                return this._cfg().mode === "free" ? "free" : "span";
            }
        },
        {
            /**
   * Nearest first-series data point to a data x (used to snap span endpoints
   * onto the series line).
   * @param {number} dataX
   * @returns {{x:number,y:number}|null}
   */ key: "_snapToSeries",
            value: function _snapToSeries(dataX) {
                var s0 = /** @type {any} */ (this.w.config.series || [])[0];
                if (!s0) return null;
                var pts = this._points(s0.data || []);
                if (!pts.length) return null;
                var best = pts[0];
                var bd = Math.abs(pts[0].x - dataX);
                for(var i = 1; i < pts.length; i++){
                    var d = Math.abs(pts[i].x - dataX);
                    if (d < bd) {
                        bd = d;
                        best = pts[i];
                    }
                }
                return best;
            }
        },
        {
            /**
   * Resolve a raw projected point to the endpoint that is actually drawn: span
   * mode snaps x to the nearest first-series data point (y follows the line);
   * free mode keeps the raw point.
   * @param {{x:number,y:number,gx:number,gy:number}} raw
   */ key: "_resolve",
            value: function _resolve(raw) {
                if (this._mode() === "free") return raw;
                var snapped = this._snapToSeries(raw.x);
                if (!snapped) return raw;
                var g = this._dataToGrid(snapped.x, snapped.y);
                return {
                    x: snapped.x,
                    y: snapped.y,
                    gx: g.gx,
                    gy: g.gy
                };
            }
        },
        {
            key: "_doc",
            value: function _doc() {
                return this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
            }
        },
        {
            /** Bind the measure-key listeners once on the owner document. */ key: "_bindKeys",
            value: function _bindKeys() {
                if (this._keysBound) return;
                var doc = this._doc();
                if (!doc) return;
                doc.addEventListener("keydown", this._onKeyDown);
                doc.addEventListener("keyup", this._onKeyUp);
                this._keysBound = true;
            }
        },
        {
            /** @param {any} e */ key: "_onKeyDown",
            value: function _onKeyDown(e) {
                if (!this._enabled() || this.persistent) return;
                var key = this._cfg().key || "m";
                if (e.key && e.key.toLowerCase() === String(key).toLowerCase()) {
                    this._arm();
                } else if (e.key === "Escape") {
                    this._cancelDrag();
                }
            }
        },
        {
            /** @param {any} e */ key: "_onKeyUp",
            value: function _onKeyUp(e) {
                if (this.persistent) return;
                var key = this._cfg().key || "m";
                if (e.key && e.key.toLowerCase() === String(key).toLowerCase()) {
                    if (!this.drag) this._disarm();
                }
            }
        },
        {
            /** Public: arm a sticky measure mode (survives key release) until stopped. */ key: "startMeasure",
            value: function startMeasure() {
                if (!this._enabled()) return;
                this.persistent = true;
                this._arm();
            }
        },
        {
            /** Public: leave measure mode. */ key: "stopMeasure",
            value: function stopMeasure() {
                this.persistent = false;
                this._cancelDrag();
                this._disarm();
            }
        },
        {
            /**
   * Public: begin a measurement anchored at a client-space point (used by the
   * context menu's "Measure from here"). Endpoint A is fixed at (cx,cy), the
   * ruler follows the cursor, and the next pointer press sets B and pins it.
   * Escape cancels. No-op unless the measure tool is enabled.
   * @param {number} cx @param {number} cy
   */ key: "seedFromClient",
            value: function seedFromClient(cx, cy) {
                if (!this._enabled()) return;
                this._cancelDrag();
                this._endSeed(false);
                var a = this._project(cx, cy);
                this.drag = {
                    a: a,
                    b: a
                };
                this._seedActive = true;
                var doc = this._doc();
                if (doc) {
                    doc.addEventListener("mousemove", this._onMove);
                    doc.addEventListener("touchmove", this._onMove, {
                        passive: false
                    });
                    doc.addEventListener("mousedown", this._onSeedDown, true);
                    doc.addEventListener("touchstart", this._onSeedDown, true);
                    doc.addEventListener("keydown", this._onSeedKey, true);
                }
                this._renderLive();
            }
        },
        {
            /** @param {any} e */ key: "_onSeedDown",
            value: function _onSeedDown(e) {
                if (!this._seedActive || !this.drag) return;
                e.preventDefault();
                e.stopPropagation();
                var _this__clientXY = this._clientXY(e), cx = _this__clientXY.cx, cy = _this__clientXY.cy;
                this.drag.b = this._project(cx, cy);
                this._endSeed(true);
            }
        },
        {
            /** @param {any} e */ key: "_onSeedKey",
            value: function _onSeedKey(e) {
                if (e.key === "Escape") this._endSeed(false);
            }
        },
        {
            /**
   * Finish (commit) or cancel a "measure from here" seed and detach listeners.
   * @param {boolean} commit
   */ key: "_endSeed",
            value: function _endSeed(commit) {
                var _a, _b;
                if (!this._seedActive) return;
                this._seedActive = false;
                var doc = this._doc();
                if (doc) {
                    doc.removeEventListener("mousemove", this._onMove);
                    doc.removeEventListener("touchmove", this._onMove);
                    doc.removeEventListener("mousedown", this._onSeedDown, true);
                    doc.removeEventListener("touchstart", this._onSeedDown, true);
                    doc.removeEventListener("keydown", this._onSeedKey, true);
                }
                var d = this.drag;
                this.drag = null;
                this._clearLive();
                if (!commit || !d) return;
                var a = this._resolve(d.a);
                var b = this._resolve(d.b);
                if (Math.abs(a.gx - b.gx) < 2 && Math.abs(a.gy - b.gy) < 2) return;
                var mode = this._mode();
                if (this._cfg().pinOnRelease !== false) {
                    this.pins.push({
                        xa: a.x,
                        ya: a.y,
                        xb: b.x,
                        yb: b.y,
                        mode: mode
                    });
                    this._renderPins();
                    (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, "measure");
                }
                this._fireMeasured(a, b);
            }
        },
        {
            /** Public: remove all pinned rulers. */ key: "clearMeasures",
            value: function clearMeasures() {
                var _a, _b;
                this.pins = [];
                this._renderPins();
                (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, "clear measures");
            }
        },
        {
            /**
   * Snapshot of the pinned rulers as JSON-safe plain data. This is the piece of
   * state ViewState / Perspectives (shareable URL) / Rewind (undo) persist:
   * pins already live in data space, so they round-trip and re-project.
   * Returns a deep copy so callers cannot mutate ours.
   * @returns {Array<{xa:number,ya:number,xb:number,yb:number,mode:string}>}
   */ key: "getPins",
            value: function getPins() {
                return this.pins.map(function(p) {
                    return {
                        xa: p.xa,
                        ya: p.ya,
                        xb: p.xb,
                        yb: p.yb,
                        mode: p.mode === "free" ? "free" : "span"
                    };
                });
            }
        },
        {
            /**
   * Replace the pinned rulers with a restored set and redraw. Accepts the shape
   * getPins() returns; a nullish / non-array value (or an old token without
   * measure state) clears all pins. Non-finite entries are dropped. Does NOT
   * record a Rewind step (the restore itself is the caller's undo boundary).
   * @param {any} pins
   */ key: "setPins",
            value: function setPins(pins) {
                this.pins = Array.isArray(pins) ? pins.filter(function(p) {
                    return p && isFinite(p.xa) && isFinite(p.ya) && isFinite(p.xb) && isFinite(p.yb);
                }).map(function(p) {
                    return {
                        xa: +p.xa,
                        ya: +p.ya,
                        xb: +p.xb,
                        yb: +p.yb,
                        mode: p.mode === "free" ? "free" : "span"
                    };
                }) : [];
                this._renderPins();
            }
        },
        {
            /**
   * Normalize a series `data` array into {x,y} points (numeric/datetime x or
   * category index). Non-finite / non-scalar (range/candle) points drop out.
   * @param {any[]} data
   * @returns {Array<{x:number,y:number}>}
   */ key: "_points",
            value: function _points(data) {
                var out = [];
                for(var i = 0; i < data.length; i++){
                    var p = data[i];
                    var x = void 0;
                    var y = void 0;
                    if (Array.isArray(p)) {
                        x = +p[0];
                        y = +p[1];
                    } else if (p && (typeof p === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(p)) === "object") {
                        x = +p.x;
                        y = +p.y;
                    } else {
                        x = i;
                        y = +p;
                    }
                    if (isFinite(x) && isFinite(y)) out.push({
                        x: x,
                        y: y
                    });
                }
                return out;
            }
        },
        {
            /** Lay the transparent capture pane over the plot so our pointer handlers own
   *  the drag (ZoomPanSelection never sees it). */ key: "_arm",
            value: function _arm() {
                if (this.armed || !this._enabled()) return;
                var w = this.w;
                var parent = w.dom.elGraphical;
                if (!parent) return;
                this.armed = true;
                var pane = this.graphics.drawRect(0, 0, w.layout.gridWidth, w.layout.gridHeight);
                pane.node.setAttribute("class", "apexcharts-measure-capture");
                pane.node.setAttribute("fill", "transparent");
                pane.node.style.cursor = "crosshair";
                pane.node.style.pointerEvents = "all";
                pane.node.addEventListener("mousedown", this._onDown);
                pane.node.addEventListener("touchstart", this._onDown, {
                    passive: false
                });
                parent.add(pane);
                this.pane = pane;
            }
        },
        {
            key: "_disarm",
            value: function _disarm() {
                this.armed = false;
                if (this.pane) {
                    this.pane.node.removeEventListener("mousedown", this._onDown);
                    this.pane.node.removeEventListener("touchstart", this._onDown);
                    var p = this.pane.node;
                    if (p.parentNode) p.parentNode.removeChild(p);
                    this.pane = null;
                }
            }
        },
        {
            /** @param {any} e @returns {{cx:number,cy:number}} */ key: "_clientXY",
            value: function _clientXY(e) {
                var t = e.touches && e.touches[0] ? e.touches[0] : e;
                return {
                    cx: t.clientX,
                    cy: t.clientY
                };
            }
        },
        {
            key: "_gridRect",
            value: function _gridRect() {
                var g = this.w.dom.baseEl.querySelector(".apexcharts-grid");
                return g ? g.getBoundingClientRect() : null;
            }
        },
        {
            /** [min,max] for the primary y-axis, preferring the rendered nice scale. */ key: "_yRange",
            value: function _yRange() {
                var g = this.w.globals;
                var s = g.yAxisScale && g.yAxisScale[0];
                if (s && isFinite(s.niceMin) && isFinite(s.niceMax) && s.niceMax !== s.niceMin) {
                    return [
                        s.niceMin,
                        s.niceMax
                    ];
                }
                return [
                    g.minY,
                    g.maxY
                ];
            }
        },
        {
            /**
   * Client pixel -> { x, y (data), gx, gy (grid-local SVG units) }.
   * @param {number} cx @param {number} cy
   */ key: "_project",
            value: function _project(cx, cy) {
                var w = this.w;
                var rect = this._gridRect();
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var clamp = function clamp(v) {
                    return v < 0 ? 0 : v > 1 ? 1 : v;
                };
                var fx = rect ? clamp((cx - rect.left) / rect.width) : 0;
                var fy = rect ? clamp((cy - rect.top) / rect.height) : 0;
                var _this__yRange = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this._yRange(), 2), ymin = _this__yRange[0], ymax = _this__yRange[1];
                var x = w.globals.minX + fx * (w.globals.maxX - w.globals.minX);
                var y = ymax - fy * (ymax - ymin);
                return {
                    x: x,
                    y: y,
                    gx: fx * gw,
                    gy: fy * gh
                };
            }
        },
        {
            /**
   * Data (x,y) -> grid-local SVG coords, for redrawing pinned rulers.
   * @param {number} x @param {number} y
   */ key: "_dataToGrid",
            value: function _dataToGrid(x, y) {
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var xr = w.globals.maxX - w.globals.minX || 1;
                var _this__yRange = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this._yRange(), 2), ymin = _this__yRange[0], ymax = _this__yRange[1];
                var yr = ymax - ymin || 1;
                return {
                    gx: (x - w.globals.minX) / xr * gw,
                    gy: gh - (y - ymin) / yr * gh
                };
            }
        },
        {
            /** @param {any} e */ key: "_onDown",
            value: function _onDown(e) {
                if (!this.armed) return;
                e.preventDefault();
                e.stopPropagation();
                var _this__clientXY = this._clientXY(e), cx = _this__clientXY.cx, cy = _this__clientXY.cy;
                var a = this._project(cx, cy);
                this.drag = {
                    a: a,
                    b: a
                };
                var doc = this._doc();
                if (doc) {
                    doc.addEventListener("mousemove", this._onMove);
                    doc.addEventListener("mouseup", this._onUp);
                    doc.addEventListener("touchmove", this._onMove, {
                        passive: false
                    });
                    doc.addEventListener("touchend", this._onUp);
                }
                this._renderLive();
            }
        },
        {
            /** @param {any} e */ key: "_onMove",
            value: function _onMove(e) {
                if (!this.drag) return;
                if (e.cancelable) e.preventDefault();
                var _this__clientXY = this._clientXY(e), cx = _this__clientXY.cx, cy = _this__clientXY.cy;
                this.drag.b = this._project(cx, cy);
                this._renderLive();
            }
        },
        {
            /** @param {any} _e */ key: "_onUp",
            value: function _onUp(_e) {
                var _a, _b;
                var doc = this._doc();
                if (doc) {
                    doc.removeEventListener("mousemove", this._onMove);
                    doc.removeEventListener("mouseup", this._onUp);
                    doc.removeEventListener("touchmove", this._onMove);
                    doc.removeEventListener("touchend", this._onUp);
                }
                if (!this.drag) return;
                var rawA = this.drag.a;
                var rawB = this.drag.b;
                this.drag = null;
                this._clearLive();
                if (Math.abs(rawA.gx - rawB.gx) < 2 && Math.abs(rawA.gy - rawB.gy) < 2) {
                    if (!this.persistent) this._disarm();
                    return;
                }
                var mode = this._mode();
                var a = this._resolve(rawA);
                var b = this._resolve(rawB);
                if (this._cfg().pinOnRelease !== false) {
                    this.pins.push({
                        xa: a.x,
                        ya: a.y,
                        xb: b.x,
                        yb: b.y,
                        mode: mode
                    });
                    this._renderPins();
                    (_b = (_a = this.ctx.history) == null ? void 0 : _a.snapshot) == null ? void 0 : _b.call(_a, "measure");
                }
                this._fireMeasured(a, b);
                if (!this.persistent) this._disarm();
            }
        },
        {
            key: "_cancelDrag",
            value: function _cancelDrag() {
                var doc = this._doc();
                if (doc) {
                    doc.removeEventListener("mousemove", this._onMove);
                    doc.removeEventListener("mouseup", this._onUp);
                    doc.removeEventListener("touchmove", this._onMove);
                    doc.removeEventListener("touchend", this._onUp);
                }
                this.drag = null;
                this._clearLive();
            }
        },
        {
            /**
   * @param {{x:number,y:number}} a @param {{x:number,y:number}} b
   * @returns {{dx:number,dy:number,pct:number,slope:number}}
   */ key: "_stats",
            value: function _stats(a, b) {
                var dx = b.x - a.x;
                var dy = b.y - a.y;
                return {
                    dx: dx,
                    dy: dy,
                    pct: a.y !== 0 ? dy / Math.abs(a.y) * 100 : NaN,
                    slope: dx !== 0 ? dy / dx : NaN
                };
            }
        },
        {
            /** @param {number} v */ key: "_fmt",
            value: function _fmt(v) {
                if (!isFinite(v)) return "n/a";
                var a = Math.abs(v);
                if (a !== 0 && (a < 0.01 || a >= 1e6)) return v.toExponential(2);
                return String(Math.round(v * 100) / 100);
            }
        },
        {
            /** getComputedStyle of the graphical layer (browser only), for CSS vars. */ key: "_computedStyle",
            value: function _computedStyle() {
                if (!Environment.isBrowser()) return null;
                var node = this.w.dom.elGraphical && this.w.dom.elGraphical.node;
                if (!node || typeof getComputedStyle !== "function") return null;
                try {
                    return getComputedStyle(node);
                } catch (e) {
                    return null;
                }
            }
        },
        {
            /**
   * Resolve a color: explicit config value, else a `--apx-measure-*` CSS custom
   * property, else the built-in default.
   * @param {any} cfgVal @param {string} varName @param {string} fallback
   * @param {CSSStyleDeclaration|null} [cs]
   */ key: "_resolveColor",
            value: function _resolveColor(cfgVal, varName, fallback, cs) {
                if (cfgVal) return cfgVal;
                var style = cs !== void 0 ? cs : this._computedStyle();
                var v = style ? style.getPropertyValue(varName).trim() : "";
                return v || fallback;
            }
        },
        {
            /** Resolved semantic colors (config -> CSS var -> built-in default). */ key: "_colors",
            value: function _colors() {
                var c = this._cfg().colors || {};
                var cs = this._computedStyle();
                return {
                    up: this._resolveColor(c.up, "--apx-measure-up", "#16a34a", cs),
                    down: this._resolveColor(c.down, "--apx-measure-down", "#dc2626", cs),
                    neutral: this._resolveColor(c.neutral, "--apx-measure-neutral", "#64748b", cs),
                    guide: this._resolveColor(c.guide, "--apx-measure-guide", "#94a3b8", cs)
                };
            }
        },
        {
            /** @param {number} dy */ key: "_dirColor",
            value: function _dirColor(dy) {
                var c = this._colors();
                return dy === 0 ? c.neutral : dy > 0 ? c.up : c.down;
            }
        },
        {
            /** @param {number} dy */ key: "_dirClass",
            value: function _dirClass(dy) {
                return dy === 0 ? "apexcharts-measure-flat" : dy > 0 ? "apexcharts-measure-up" : "apexcharts-measure-down";
            }
        },
        {
            /**
   * Format a percentage, via `measure.format.percent` when set.
   * @param {number} p
   */ key: "_fmtPct",
            value: function _fmtPct(p) {
                if (!isFinite(p)) return "n/a";
                var f = this._cfg().format && this._cfg().format.percent;
                if (typeof f === "function") {
                    try {
                        var s = f(p);
                        if (s != null) return String(s);
                    } catch (e) {}
                }
                return (p >= 0 ? "+" : "") + this._fmt(p) + "%";
            }
        },
        {
            /**
   * The readout lines for a ruler: `measure.label` override, else the default
   * per-mode text.
   * @param {{x:number,y:number}} a @param {{x:number,y:number}} b
   * @param {{dx:number,dy:number,pct:number,slope:number}} st @param {string} mode
   * @returns {string[]}
   */ key: "_label",
            value: function _label(a, b, st, mode) {
                var fn = this._cfg().label;
                if (typeof fn === "function") {
                    var out = fn({
                        from: {
                            x: a.x,
                            y: a.y
                        },
                        to: {
                            x: b.x,
                            y: b.y
                        },
                        dx: st.dx,
                        dy: st.dy,
                        percentChange: st.pct,
                        slope: st.slope,
                        mode: mode
                    });
                    return Array.isArray(out) ? out.map(String) : [
                        String(out)
                    ];
                }
                if (mode === "span") {
                    var arrow = st.dy === 0 ? "" : st.dy > 0 ? " ↑" : " ↓";
                    var pct = isFinite(st.pct) ? "(" + this._fmtPct(st.pct) + ")" : "";
                    return [
                        this._fmtY(st.dy) + "  " + pct + arrow,
                        this._fmtX(a.x) + "  to  " + this._fmtX(b.x)
                    ];
                }
                return [
                    "Δx " + this._fmtDx(st.dx),
                    "Δy " + this._fmtY(st.dy),
                    this._fmtPct(st.pct)
                ];
            }
        },
        {
            /**
   * Format an x delta, treating datetime x as a day count.
   * @param {number} dx
   */ key: "_fmtDx",
            value: function _fmtDx(dx) {
                if (this.w.config.xaxis.type === "datetime") {
                    var days = dx / 864e5;
                    return Math.round(days * 10) / 10 + "d";
                }
                return this._fmt(dx);
            }
        },
        {
            /**
   * Format a y value (a delta) via the y-axis label formatter when present.
   * @param {number} v
   */ key: "_fmtY",
            value: function _fmtY(v) {
                var cf = this._cfg().format && this._cfg().format.y;
                if (typeof cf === "function") {
                    try {
                        var s = cf(v);
                        if (s != null) return String(s);
                    } catch (e) {}
                }
                var f = this.w.globals.yLabelFormatters && this.w.globals.yLabelFormatters[0];
                if (typeof f === "function") {
                    try {
                        var s1 = f(v, {
                            seriesIndex: 0,
                            dataPointIndex: -1,
                            w: this.w
                        });
                        if (s1 != null && s1 !== "") return String(s1);
                    } catch (e) {}
                }
                return this._fmt(v);
            }
        },
        {
            /**
   * Format an x value: a short date for datetime x, else the x-label formatter
   * or a plain number.
   * @param {number} x
   */ key: "_fmtX",
            value: function _fmtX(x) {
                var w = this.w;
                var cf = this._cfg().format && this._cfg().format.x;
                if (typeof cf === "function") {
                    try {
                        var s = cf(x);
                        if (s != null) return String(s);
                    } catch (e) {}
                }
                if (w.config.xaxis.type === "datetime") {
                    var d = new Date(x);
                    return d.toLocaleDateString(void 0, {
                        day: "numeric",
                        month: "short",
                        year: "numeric"
                    });
                }
                var f = w.globals.xLabelFormatter;
                if (typeof f === "function") {
                    try {
                        var s1 = f(x, {
                            w: w
                        });
                        if (s1 != null && s1 !== "") return String(s1);
                    } catch (e) {}
                }
                return this._fmt(x);
            }
        },
        {
            /** The live overlay group, created lazily inside elGraphical. */ key: "_liveGroup",
            value: function _liveGroup() {
                var w = this.w;
                if (this._live && this._live.node && this._live.node.parentNode) {
                    return this._live;
                }
                var g = this.graphics.group({
                    class: "apexcharts-measure-live"
                });
                g.node.style.pointerEvents = "none";
                if (w.dom.elGraphical) w.dom.elGraphical.add(g);
                this._live = g;
                return g;
            }
        },
        {
            key: "_clearLive",
            value: function _clearLive() {
                if (this._live && this._live.node) {
                    var n = this._live.node;
                    if (n.parentNode) n.parentNode.removeChild(n);
                }
                this._live = null;
            }
        },
        {
            key: "_renderLive",
            value: function _renderLive() {
                if (!this.drag) return;
                var g = this._liveGroup();
                while(g.node.firstChild)g.node.removeChild(g.node.firstChild);
                var a = this._resolve(this.drag.a);
                var b = this._resolve(this.drag.b);
                this._drawRuler(g, a, b, false, this._mode());
            }
        },
        {
            /** Redraw all pinned rulers into a fresh group (called after each render). */ key: "_renderPins",
            value: function _renderPins() {
                var _this = this;
                var w = this.w;
                var old = w.dom.baseEl && w.dom.baseEl.querySelector(".apexcharts-measure-pins");
                if (old && old.parentNode) old.parentNode.removeChild(old);
                if (!this.pins.length || !w.dom.elGraphical) return;
                var g = this.graphics.group({
                    class: "apexcharts-measure-pins"
                });
                g.node.style.pointerEvents = "none";
                w.dom.elGraphical.add(g);
                this.pins.forEach(function(p) {
                    var a = __spreadProps(__spreadValues({}, _this._dataToGrid(p.xa, p.ya)), {
                        x: p.xa,
                        y: p.ya
                    });
                    var b = __spreadProps(__spreadValues({}, _this._dataToGrid(p.xb, p.yb)), {
                        x: p.xb,
                        y: p.yb
                    });
                    _this._drawRuler(g, a, b, true, p.mode || "span");
                });
            }
        },
        {
            /**
   * Draw one ruler into `g`, dispatching on style.
   * @param {any} g
   * @param {{gx:number,gy:number,x:number,y:number}} a
   * @param {{gx:number,gy:number,x:number,y:number}} b
   * @param {boolean} [pinned]
   * @param {string} [mode] 'span' (finance-style band, default) | 'free'
   */ key: "_drawRuler",
            value: function _drawRuler(g, a, b, pinned, mode) {
                var st = this._stats(a, b);
                var rg = this.graphics.group({
                    class: "apexcharts-measure-ruler " + this._dirClass(st.dy) + (pinned ? " apexcharts-measure-pinned" : "")
                });
                if ((mode || this._mode()) === "free") this._drawFree(rg, a, b, st);
                else this._drawSpan(rg, a, b, st);
                g.add(rg);
            }
        },
        {
            /** Endpoint dots on the series line (skipped when markers:false).
   * @param {any} g @param {{gx:number,gy:number}} p @param {string} color */ key: "_dot",
            value: function _dot(g, p, color) {
                if (this._cfg().markers === false) return;
                var dot = this.graphics.drawMarker(p.gx, p.gy, {
                    pSize: 4,
                    shape: "circle",
                    pointFillColor: color,
                    pointFillOpacity: 1,
                    pointStrokeColor: "#fff",
                    pointStrokeWidth: 2,
                    pointStrokeOpacity: 1
                });
                g.add(dot);
            }
        },
        {
            /** Draw the readout label box + text into `g` at (bx,by).
   * @param {any} g @param {string[]} lines @param {number} bx @param {number} by
   * @param {number} boxW @param {number} boxH @param {string} color */ key: "_readout",
            value: function _readout(g, lines, bx, by, boxW, boxH, color) {
                var box = this.graphics.drawRect(bx, by, boxW, boxH, 4);
                box.node.setAttribute("class", "apexcharts-measure-label-bg");
                box.attr({
                    fill: "#ffffff",
                    "fill-opacity": 0.95,
                    stroke: color,
                    "stroke-width": 1
                });
                g.add(box);
                var label = this.graphics.drawText({
                    x: bx + 9,
                    y: by + 15,
                    text: lines,
                    textAnchor: "start",
                    fontSize: "11px",
                    foreColor: "#1e293b",
                    cssClass: "apexcharts-measure-label"
                });
                g.add(label);
            }
        },
        {
            /**
   * Finance-style ruler: vertical guides + shaded band + endpoints on the
   * series line + a top readout. Guides/band/markers are individually
   * toggleable via config.
   * @param {any} g
   * @param {{gx:number,gy:number,x:number,y:number}} a
   * @param {{gx:number,gy:number,x:number,y:number}} b
   * @param {{dx:number,dy:number,pct:number,slope:number}} st
   */ key: "_drawSpan",
            value: function _drawSpan(g, a, b, st) {
                var _this = this;
                var w = this.w;
                var cfg = this._cfg();
                var gh = w.layout.gridHeight;
                var color = this._dirColor(st.dy);
                var lx = Math.min(a.gx, b.gx);
                var rx = Math.max(a.gx, b.gx);
                if (cfg.band !== false) {
                    var band = this.graphics.drawRect(lx, 0, Math.max(0, rx - lx), gh, 0);
                    band.node.setAttribute("class", "apexcharts-measure-band");
                    band.attr({
                        fill: color,
                        "fill-opacity": 0.09,
                        stroke: "none"
                    });
                    g.add(band);
                }
                if (cfg.guides !== false) {
                    [
                        a,
                        b
                    ].forEach(function(p) {
                        var vline = _this.graphics.drawLine(p.gx, 0, p.gx, gh, _this._colors().guide, 4, 1);
                        vline.node.setAttribute("class", "apexcharts-measure-vline");
                        g.add(vline);
                    });
                }
                [
                    a,
                    b
                ].forEach(function(p) {
                    return _this._dot(g, p, color);
                });
                var lines = this._label(a, b, st, "span");
                var longest = lines.reduce(function(m, s) {
                    return Math.max(m, s.length);
                }, 0);
                var boxW = Math.max(148, longest * 6.4);
                var boxH = 20 + lines.length * 15;
                var bx = (lx + rx) / 2 - boxW / 2;
                bx = Math.max(2, Math.min(bx, w.layout.gridWidth - boxW - 2));
                this._readout(g, lines, bx, 4, boxW, boxH, color);
            }
        },
        {
            /**
   * Free 2D ruler: a diagonal line between two arbitrary points + a readout.
   * @param {any} g
   * @param {{gx:number,gy:number,x:number,y:number}} a
   * @param {{gx:number,gy:number,x:number,y:number}} b
   * @param {{dx:number,dy:number,pct:number,slope:number}} st
   */ key: "_drawFree",
            value: function _drawFree(g, a, b, st) {
                var _this = this;
                var color = this._dirColor(st.dy);
                var line = this.graphics.drawLine(a.gx, a.gy, b.gx, b.gy, color, 0, 2);
                line.node.setAttribute("class", "apexcharts-measure-line");
                g.add(line);
                [
                    a,
                    b
                ].forEach(function(p) {
                    return _this._dot(g, p, color);
                });
                var lines = this._label(a, b, st, "free");
                var longest = lines.reduce(function(m, s) {
                    return Math.max(m, s.length);
                }, 0);
                var boxW = Math.max(72, longest * 6.2);
                var boxH = 16 + lines.length * 15;
                var bx = (a.gx + b.gx) / 2 + 8;
                var by = (a.gy + b.gy) / 2 - boxH / 2;
                bx = Math.max(2, Math.min(bx, this.w.layout.gridWidth - boxW - 2));
                by = Math.max(2, Math.min(by, this.w.layout.gridHeight - boxH - 2));
                this._readout(g, lines, bx, by, boxW, boxH, color);
            }
        },
        {
            /**
   * @param {{x:number,y:number}} a @param {{x:number,y:number}} b
   */ key: "_fireMeasured",
            value: function _fireMeasured(a, b) {
                var st = this._stats(a, b);
                var payload = {
                    from: {
                        x: a.x,
                        y: a.y
                    },
                    to: {
                        x: b.x,
                        y: b.y
                    },
                    dx: st.dx,
                    dy: st.dy,
                    percentChange: st.pct,
                    slope: st.slope
                };
                var fn = this.w.config.chart.events.measured;
                if (typeof fn === "function") fn(this.ctx, payload);
                this.ctx.events.fireEvent("measured", [
                    this.ctx,
                    payload
                ]);
            }
        },
        {
            /** Re-project the measure pins after each render. */ key: "_afterRender",
            value: function _afterRender() {
                if (!this._enabled()) return;
                if (this.w.interact.measureEnabled && !this.persistent) {
                    this.persistent = true;
                }
                if (this.pins.length) this._renderPins();
                if (this.persistent && !this.drag) {
                    this._disarm();
                    this._arm();
                }
            }
        },
        {
            key: "teardown",
            value: function teardown() {
                this._cancelDrag();
                this._endSeed(false);
                this._disarm();
                var doc = this._doc();
                if (doc && this._keysBound) {
                    doc.removeEventListener("keydown", this._onKeyDown);
                    doc.removeEventListener("keyup", this._onKeyUp);
                }
                this._keysBound = false;
                this.pins = [];
            }
        }
    ]);
    return Measure;
}();
ApexCharts.registerFeatures({
    measure: Measure
});
var ContextMenu = /*#__PURE__*/ function() {
    "use strict";
    function ContextMenu(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, ContextMenu);
        this.w = w;
        this.ctx = ctx;
        this.menu = null;
        this._items = [];
        this._focusIndex = -1;
        this._trigger = null;
        this._onContext = this._onContext.bind(this);
        this._onDocDown = this._onDocDown.bind(this);
        this._onKey = this._onKey.bind(this);
        this._afterRender = this._afterRender.bind(this);
        ctx.addEventListener("mounted", this._afterRender);
        ctx.addEventListener("updated", this._afterRender);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ContextMenu, [
        {
            key: "_cfg",
            value: function _cfg() {
                return this.w.config.chart.contextMenu || {};
            }
        },
        {
            key: "_enabled",
            value: function _enabled() {
                return this._cfg().enabled === true;
            }
        },
        {
            key: "_doc",
            value: function _doc() {
                return this.w.dom.baseEl && this.w.dom.baseEl.ownerDocument;
            }
        },
        {
            /** (Re)attach the contextmenu trigger to the freshly (re)built SVG. */ key: "_afterRender",
            value: function _afterRender() {
                this._detachTrigger();
                this.close();
                if (!this._enabled() || !Environment.isBrowser()) return;
                var svg = this.w.dom.Paper && this.w.dom.Paper.node;
                if (!svg) return;
                svg.addEventListener("contextmenu", this._onContext);
                this._trigger = svg;
            }
        },
        {
            key: "_detachTrigger",
            value: function _detachTrigger() {
                if (this._trigger) {
                    this._trigger.removeEventListener("contextmenu", this._onContext);
                    this._trigger = null;
                }
            }
        },
        {
            /** @param {any} e */ key: "_onContext",
            value: function _onContext(e) {
                if (!this._enabled()) return;
                e.preventDefault();
                this.open(e.clientX, e.clientY);
            }
        },
        {
            /**
   * Client pixel -> data {x,y} via the grid client-rect fraction (scale
   * independent). Null when the grid is not measurable.
   * @param {number} cx @param {number} cy
   * @returns {{x:number,y:number}|null}
   */ key: "_clientToData",
            value: function _clientToData(cx, cy) {
                var w = this.w;
                var grid = w.dom.baseEl && w.dom.baseEl.querySelector(".apexcharts-grid");
                if (!grid) return null;
                var r = grid.getBoundingClientRect();
                if (!r.width || !r.height) return null;
                var clamp = function clamp(v) {
                    return v < 0 ? 0 : v > 1 ? 1 : v;
                };
                var fx = clamp((cx - r.left) / r.width);
                var fy = clamp((cy - r.top) / r.height);
                var x = w.globals.minX + fx * (w.globals.maxX - w.globals.minX);
                var s = w.globals.yAxisScale && w.globals.yAxisScale[0];
                var ymin = s && isFinite(s.niceMin) ? s.niceMin : w.globals.minY;
                var ymax = s && isFinite(s.niceMax) ? s.niceMax : w.globals.maxY;
                var y = ymax - fy * (ymax - ymin);
                return {
                    x: x,
                    y: y
                };
            }
        },
        {
            /**
   * Resolve the configured items into runnable entries, dropping built-ins
   * whose dependency is absent (e.g. measure not enabled).
   * @param {any} context
   * @returns {Array<{id:string,label:string,run:Function}>}
   */ key: "_resolveItems",
            value: function _resolveItems(context) {
                var _this = this;
                var cfg = this._cfg();
                var raw = Array.isArray(cfg.items) && cfg.items.length ? cfg.items : [
                    "annotate",
                    "xline",
                    "yline",
                    "measure"
                ];
                var labels = cfg.labels || {};
                var out = [];
                raw.forEach(function(it) {
                    if (typeof it === "string") {
                        if (it === "annotate") {
                            out.push({
                                id: "annotate",
                                label: labels.annotate || "Add note here",
                                run: function run() {
                                    return _this._annotate(context);
                                }
                            });
                        } else if (it === "xline") {
                            out.push({
                                id: "xline",
                                label: labels.xline || "Annotate here",
                                run: function run() {
                                    return _this._line(context, "x");
                                }
                            });
                        } else if (it === "yline") {
                            out.push({
                                id: "yline",
                                label: labels.yline || "Mark this level",
                                run: function run() {
                                    return _this._line(context, "y");
                                }
                            });
                        } else if (it === "measure") {
                            var m = _this.ctx.measure;
                            var on = m && _this.w.config.chart.measure && _this.w.config.chart.measure.enabled;
                            if (on) {
                                out.push({
                                    id: "measure",
                                    label: labels.measure || "Measure from here",
                                    run: function run() {
                                        return m.seedFromClient(context.clientX, context.clientY);
                                    }
                                });
                            }
                        }
                    } else if (it && (typeof it === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(it)) === "object" && typeof it.onClick === "function") {
                        out.push({
                            id: it.id || "custom",
                            label: it.label || "Action",
                            run: function run() {
                                return it.onClick(_this.ctx, context);
                            }
                        });
                    }
                });
                return out;
            }
        },
        {
            /** @param {any} context */ key: "_annotate",
            value: function _annotate(context) {
                if (context.x == null || context.y == null) return;
                var cfg = this._cfg();
                var ink = this.ctx.ink;
                if (ink && typeof ink.createAt === "function") {
                    ink.createAt(context.x, context.y, {
                        text: cfg.noteText || "Note"
                    });
                    return;
                }
                this.ctx.addPointAnnotation({
                    x: context.x,
                    y: context.y,
                    marker: {
                        size: 5
                    },
                    label: {
                        text: cfg.noteText || "Note",
                        style: {
                            background: "#fff",
                            color: "#334155"
                        }
                    }
                }, true);
            }
        },
        {
            /**
   * The 'xline' / 'yline' items: drop a dashed line annotation at the clicked
   * data point ('x' = vertical line at the clicked x, 'y' = horizontal line
   * at the clicked y). Lines only: no x2/y2 is ever set, so this never
   * creates a range rectangle. Both items share chart.contextMenu.line
   * ({ text, strokeDashArray, color }) for styling.
   * @param {any} context @param {'x'|'y'} axis
   */ key: "_line",
            value: function _line(context, axis) {
                var lc = this._cfg().line || {};
                var val = axis === "x" ? context.x : context.y;
                if (val == null) return;
                var ink = this.ctx.ink;
                if (ink && typeof ink.createLineAt === "function") {
                    ink.createLineAt(axis, val, {
                        text: lc.text,
                        strokeDashArray: lc.strokeDashArray,
                        color: lc.color
                    });
                    return;
                }
                var anno = {
                    strokeDashArray: lc.strokeDashArray != null ? lc.strokeDashArray : 4
                };
                if (lc.text) anno.label = {
                    text: lc.text
                };
                if (lc.color) {
                    anno.borderColor = lc.color;
                    anno.label = Utils$1.extend(anno.label || {}, {
                        borderColor: lc.color
                    });
                }
                if (axis === "x") {
                    this.ctx.addXaxisAnnotation(Utils$1.extend(anno, {
                        x: val
                    }), true);
                } else {
                    this.ctx.addYaxisAnnotation(Utils$1.extend(anno, {
                        y: val
                    }), true);
                }
            }
        },
        {
            /**
   * Open the menu at a client-space point.
   * @param {number} clientX @param {number} clientY
   */ key: "open",
            value: function open(clientX, clientY) {
                var _this = this;
                this.close();
                var w = this.w;
                var elWrap = w.dom.elWrap;
                var doc = this._doc();
                if (!elWrap || !doc) return;
                var data = this._clientToData(clientX, clientY);
                var g = w.globals;
                var context = {
                    x: data ? data.x : null,
                    y: data ? data.y : null,
                    seriesIndex: g.capturedSeriesIndex >= 0 ? g.capturedSeriesIndex : null,
                    dataPointIndex: g.capturedDataPointIndex >= 0 ? g.capturedDataPointIndex : null,
                    clientX: clientX,
                    clientY: clientY
                };
                var items = this._resolveItems(context);
                if (!items.length) return;
                this._items = items;
                var menu = doc.createElement("div");
                menu.className = "apexcharts-context-menu";
                menu.setAttribute("role", "menu");
                menu.style.position = "absolute";
                menu.style.visibility = "hidden";
                items.forEach(function(it, i) {
                    var btn = doc.createElement("button");
                    btn.type = "button";
                    btn.className = "apexcharts-context-menu-item";
                    btn.setAttribute("role", "menuitem");
                    btn.tabIndex = -1;
                    btn.textContent = it.label;
                    btn.addEventListener("click", function(ev) {
                        ev.stopPropagation();
                        _this._activate(i);
                    });
                    btn.addEventListener("mouseenter", function() {
                        return _this._focus(i);
                    });
                    menu.appendChild(btn);
                });
                elWrap.appendChild(menu);
                this.menu = menu;
                var wrapRect = elWrap.getBoundingClientRect();
                var left = clientX - wrapRect.left;
                var top = clientY - wrapRect.top;
                var mw = menu.offsetWidth;
                var mh = menu.offsetHeight;
                var maxLeft = Math.max(0, elWrap.clientWidth - mw);
                var maxTop = Math.max(0, elWrap.clientHeight - mh);
                if (left > maxLeft) left = maxLeft;
                if (top > maxTop) top = maxTop;
                menu.style.left = Math.max(0, left) + "px";
                menu.style.top = Math.max(0, top) + "px";
                menu.style.visibility = "visible";
                doc.addEventListener("mousedown", this._onDocDown, true);
                doc.addEventListener("keydown", this._onKey, true);
                this._focus(0);
            }
        },
        {
            /** @param {number} i */ key: "_focus",
            value: function _focus(i) {
                if (!this.menu) return;
                var btns = this.menu.querySelectorAll(".apexcharts-context-menu-item");
                if (this._focusIndex >= 0 && btns[this._focusIndex]) {
                    btns[this._focusIndex].classList.remove("apexcharts-context-menu-item--active");
                }
                this._focusIndex = i;
                if (btns[i]) {
                    btns[i].classList.add("apexcharts-context-menu-item--active");
                    if (typeof btns[i].focus === "function") btns[i].focus();
                }
            }
        },
        {
            /** @param {number} i */ key: "_activate",
            value: function _activate(i) {
                var it = this._items[i];
                this.close();
                if (it) it.run();
            }
        },
        {
            /** @param {any} e */ key: "_onDocDown",
            value: function _onDocDown(e) {
                if (this.menu && this.menu.contains(e.target)) return;
                this.close();
            }
        },
        {
            /** @param {any} e */ key: "_onKey",
            value: function _onKey(e) {
                if (!this.menu || !this._items.length) return;
                if (e.key === "Escape") {
                    e.preventDefault();
                    this.close();
                } else if (e.key === "ArrowDown") {
                    e.preventDefault();
                    this._focus((this._focusIndex + 1) % this._items.length);
                } else if (e.key === "ArrowUp") {
                    e.preventDefault();
                    this._focus((this._focusIndex - 1 + this._items.length) % this._items.length);
                } else if (e.key === "Enter" || e.key === " ") {
                    e.preventDefault();
                    this._activate(this._focusIndex < 0 ? 0 : this._focusIndex);
                }
            }
        },
        {
            key: "close",
            value: function close() {
                var doc = this._doc();
                if (doc) {
                    doc.removeEventListener("mousedown", this._onDocDown, true);
                    doc.removeEventListener("keydown", this._onKey, true);
                }
                if (this.menu && this.menu.parentNode) {
                    this.menu.parentNode.removeChild(this.menu);
                }
                this.menu = null;
                this._items = [];
                this._focusIndex = -1;
            }
        },
        {
            key: "teardown",
            value: function teardown() {
                this.close();
                this._detachTrigger();
            }
        }
    ]);
    return ContextMenu;
}();
ApexCharts.registerFeatures({
    contextMenu: ContextMenu
});
var BarDataLabels = /*#__PURE__*/ function() {
    "use strict";
    function BarDataLabels(barCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, BarDataLabels);
        this.w = barCtx.w;
        this.barCtx = barCtx;
        this.totalFormatter = this.w.config.plotOptions.bar.dataLabels.total.formatter;
        if (!this.totalFormatter) {
            this.totalFormatter = this.w.config.dataLabels.formatter;
        }
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(BarDataLabels, [
        {
            /** handleBarDataLabels is used to calculate the positions for the data-labels
   * It also sets the element's data attr for bars and calls drawCalculatedBarDataLabels()
   * After calculating, it also calls the function to draw data labels
   * @memberof Bar
   * @param {Record<string, any>} opts - bar properties used throughout the bar drawing function
   * @return {object} dataLabels node-element which you can append later
   **/ key: "handleBarDataLabels",
            value: function handleBarDataLabels(opts) {
                var x = opts.x, y = opts.y, y1 = opts.y1, y2 = opts.y2, i = opts.i, j = opts.j, realIndex = opts.realIndex, columnGroupIndex = opts.columnGroupIndex, series = opts.series, barHeight = opts.barHeight, barWidth = opts.barWidth, barXPosition = opts.barXPosition, barYPosition = opts.barYPosition, visibleSeries = opts.visibleSeries;
                var w = this.w;
                var graphics = new Graphics(this.barCtx.w);
                var strokeWidth = Array.isArray(this.barCtx.strokeWidth) ? this.barCtx.strokeWidth[realIndex] : this.barCtx.strokeWidth;
                var bcx;
                var bcy;
                if (w.axisFlags.isXNumeric && !w.globals.isBarHorizontal) {
                    bcx = x + barWidth * (visibleSeries + 1);
                    bcy = y + barHeight * (visibleSeries + 1) - strokeWidth;
                } else {
                    bcx = x + barWidth * visibleSeries;
                    bcy = y + barHeight * visibleSeries;
                }
                var dataLabels = null;
                var totalDataLabels = null;
                var dataLabelsX = x;
                var dataLabelsY = y;
                var dataLabelsPos = /** @type {any} */ {};
                var dataLabelsConfig = w.config.dataLabels;
                var barDataLabelsConfig = this.barCtx.barOptions.dataLabels;
                var barTotalDataLabelsConfig = this.barCtx.barOptions.dataLabels.total;
                if (typeof barYPosition !== "undefined" && (this.barCtx.isRangeBar || this.barCtx.isPyramid)) {
                    bcy = barYPosition;
                    dataLabelsY = barYPosition;
                }
                if (typeof barXPosition !== "undefined" && this.barCtx.isVerticalGroupedRangeBar) {
                    bcx = barXPosition;
                    dataLabelsX = barXPosition;
                }
                var offX = resolveDataLabelOffset(dataLabelsConfig.offsetX, w, realIndex, j);
                var offY = resolveDataLabelOffset(dataLabelsConfig.offsetY, w, realIndex, j);
                var textRects = {
                    width: 0,
                    height: 0
                };
                if (w.config.dataLabels.enabled) {
                    var yLabel = w.seriesData.series[realIndex][j];
                    textRects = graphics.getTextRects(w.config.dataLabels.formatter ? w.config.dataLabels.formatter(yLabel, __spreadProps(__spreadValues({}, w), {
                        seriesIndex: realIndex,
                        dataPointIndex: j,
                        w: w
                    })) : w.formatters.yLabelFormatters[0](yLabel), parseFloat(dataLabelsConfig.style.fontSize).toString());
                }
                var params = {
                    x: x,
                    y: y,
                    i: i,
                    j: j,
                    realIndex: realIndex,
                    columnGroupIndex: columnGroupIndex,
                    bcx: bcx,
                    bcy: bcy,
                    barHeight: barHeight,
                    barWidth: barWidth,
                    textRects: textRects,
                    strokeWidth: strokeWidth,
                    dataLabelsX: dataLabelsX,
                    dataLabelsY: dataLabelsY,
                    dataLabelsConfig: dataLabelsConfig,
                    barDataLabelsConfig: barDataLabelsConfig,
                    barTotalDataLabelsConfig: barTotalDataLabelsConfig,
                    offX: offX,
                    offY: offY
                };
                if (this.barCtx.isHorizontal) {
                    dataLabelsPos = this.calculateBarsDataLabelsPosition(params);
                } else {
                    dataLabelsPos = this.calculateColumnsDataLabelsPosition(params);
                }
                dataLabels = this.drawCalculatedDataLabels({
                    x: dataLabelsPos.dataLabelsX,
                    y: dataLabelsPos.dataLabelsY,
                    val: this.barCtx.isRangeBar ? [
                        y1,
                        y2
                    ] : w.config.chart.stackType === "100%" ? series[realIndex][j] : w.seriesData.series[realIndex][j],
                    i: realIndex,
                    j: j,
                    barWidth: barWidth,
                    barHeight: barHeight,
                    textRects: textRects,
                    dataLabelsConfig: dataLabelsConfig
                });
                if (w.config.chart.stacked && barTotalDataLabelsConfig.enabled) {
                    totalDataLabels = this.drawTotalDataLabels({
                        x: dataLabelsPos.totalDataLabelsX,
                        y: dataLabelsPos.totalDataLabelsY,
                        barWidth: barWidth,
                        barHeight: barHeight,
                        realIndex: realIndex,
                        textAnchor: dataLabelsPos.totalDataLabelsAnchor,
                        val: this.getStackedTotalDataLabel({
                            realIndex: realIndex,
                            j: j
                        }),
                        dataLabelsConfig: dataLabelsConfig,
                        barTotalDataLabelsConfig: barTotalDataLabelsConfig
                    });
                }
                return {
                    dataLabelsPos: dataLabelsPos,
                    dataLabels: dataLabels,
                    totalDataLabels: totalDataLabels
                };
            }
        },
        {
            /** @param {{realIndex: any, j: any}} opts */ key: "getStackedTotalDataLabel",
            value: function getStackedTotalDataLabel(param) {
                var realIndex = param.realIndex, j = param.j;
                var w = this.w;
                var val = this.barCtx.stackedSeriesTotals[j];
                if (this.totalFormatter) {
                    val = this.totalFormatter(val, __spreadProps(__spreadValues({}, w), {
                        seriesIndex: realIndex,
                        dataPointIndex: j,
                        w: w
                    }));
                }
                return val;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "calculateColumnsDataLabelsPosition",
            value: function calculateColumnsDataLabelsPosition(opts) {
                var _this = this;
                var w = this.w;
                var i = opts.i, j = opts.j, realIndex = opts.realIndex, y = opts.y, bcx = opts.bcx, barWidth = opts.barWidth, barHeight = opts.barHeight, textRects = opts.textRects, dataLabelsX = opts.dataLabelsX, dataLabelsY = opts.dataLabelsY, dataLabelsConfig = opts.dataLabelsConfig, barDataLabelsConfig = opts.barDataLabelsConfig, barTotalDataLabelsConfig = opts.barTotalDataLabelsConfig, strokeWidth = opts.strokeWidth, offX = opts.offX, offY = opts.offY;
                var totalDataLabelsY;
                var totalDataLabelsX;
                var totalDataLabelsAnchor = "middle";
                var totalDataLabelsBcx = bcx;
                barHeight = Math.abs(barHeight);
                var vertical = w.config.plotOptions.bar.dataLabels.orientation === "vertical";
                var zeroEncounters = this.barCtx.barHelpers.getZeroValueEncounters({
                    i: i,
                    j: j
                }).zeroEncounters;
                bcx = bcx - strokeWidth / 2;
                var dataPointsDividedWidth = w.layout.gridWidth / w.globals.dataPoints;
                if (this.barCtx.isVerticalGroupedRangeBar) {
                    dataLabelsX += barWidth / 2;
                } else {
                    if (w.axisFlags.isXNumeric) {
                        dataLabelsX = bcx - barWidth / 2 + offX;
                    } else {
                        dataLabelsX = bcx - dataPointsDividedWidth + barWidth / 2 + offX;
                    }
                    if (!w.config.chart.stacked && zeroEncounters > 0 && w.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
                        dataLabelsX -= barWidth * zeroEncounters;
                    }
                }
                if (vertical) {
                    var offsetDLX = 2;
                    dataLabelsX = dataLabelsX + textRects.height / 2 - strokeWidth / 2 - offsetDLX;
                }
                var valIsNegative = w.seriesData.series[i][j] < 0;
                var newY = y;
                if (this.barCtx.isReversed) {
                    newY = y + (valIsNegative ? barHeight : -barHeight);
                }
                switch(barDataLabelsConfig.position){
                    case "center":
                        if (vertical) {
                            if (valIsNegative) {
                                dataLabelsY = newY - barHeight / 2 + offY;
                            } else {
                                dataLabelsY = newY + barHeight / 2 - offY;
                            }
                        } else {
                            if (valIsNegative) {
                                dataLabelsY = newY - barHeight / 2 + textRects.height / 2 + offY;
                            } else {
                                dataLabelsY = newY + barHeight / 2 + textRects.height / 2 - offY;
                            }
                        }
                        break;
                    case "bottom":
                        if (vertical) {
                            if (valIsNegative) {
                                dataLabelsY = newY - barHeight + offY;
                            } else {
                                dataLabelsY = newY + barHeight - offY;
                            }
                        } else {
                            if (valIsNegative) {
                                dataLabelsY = newY - barHeight + textRects.height + strokeWidth + offY;
                            } else {
                                dataLabelsY = newY + barHeight - textRects.height / 2 + strokeWidth - offY;
                            }
                        }
                        break;
                    case "top":
                        if (vertical) {
                            if (valIsNegative) {
                                dataLabelsY = newY + offY;
                            } else {
                                dataLabelsY = newY - offY;
                            }
                        } else {
                            if (valIsNegative) {
                                dataLabelsY = newY - textRects.height / 2 - offY;
                            } else {
                                dataLabelsY = newY + textRects.height + offY;
                            }
                        }
                        break;
                }
                var lowestPrevY = newY;
                w.labelData.seriesGroups.forEach(function(sg) {
                    var _a;
                    (_a = _this.barCtx[sg.join(",")]) == null ? void 0 : _a.prevY.forEach(function(arr) {
                        if (valIsNegative) {
                            lowestPrevY = Math.max(arr[j], lowestPrevY);
                        } else {
                            lowestPrevY = Math.min(arr[j], lowestPrevY);
                        }
                    });
                });
                if (this.barCtx.lastActiveBarSerieIndex === realIndex && barTotalDataLabelsConfig.enabled) {
                    var ADDITIONAL_OFFY = 18;
                    var graphics = new Graphics(this.barCtx.w);
                    var totalLabeltextRects = graphics.getTextRects(this.getStackedTotalDataLabel({
                        realIndex: realIndex,
                        j: j
                    }), dataLabelsConfig.fontSize);
                    if (valIsNegative) {
                        totalDataLabelsY = lowestPrevY - totalLabeltextRects.height / 2 - offY - barTotalDataLabelsConfig.offsetY + ADDITIONAL_OFFY;
                    } else {
                        totalDataLabelsY = lowestPrevY + totalLabeltextRects.height + offY + barTotalDataLabelsConfig.offsetY - ADDITIONAL_OFFY;
                    }
                    var xDivision = dataPointsDividedWidth;
                    totalDataLabelsX = totalDataLabelsBcx + (w.axisFlags.isXNumeric ? -barWidth * w.globals.barGroups.length / 2 : w.globals.barGroups.length * barWidth / 2 - (w.globals.barGroups.length - 1) * barWidth - xDivision) + barTotalDataLabelsConfig.offsetX;
                }
                if (!w.config.chart.stacked) {
                    if (dataLabelsY < 0) {
                        dataLabelsY = 0 + strokeWidth;
                    } else if (dataLabelsY + textRects.height / 3 > w.layout.gridHeight) {
                        dataLabelsY = w.layout.gridHeight - strokeWidth;
                    }
                }
                return {
                    bcx: bcx,
                    bcy: y,
                    dataLabelsX: dataLabelsX,
                    dataLabelsY: dataLabelsY,
                    totalDataLabelsX: totalDataLabelsX,
                    totalDataLabelsY: totalDataLabelsY,
                    totalDataLabelsAnchor: totalDataLabelsAnchor
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "calculateBarsDataLabelsPosition",
            value: function calculateBarsDataLabelsPosition(opts) {
                var _this = this;
                var _a;
                var w = this.w;
                var x = opts.x, i = opts.i, j = opts.j, realIndex = opts.realIndex, bcy = opts.bcy, barHeight = opts.barHeight, barWidth = opts.barWidth, textRects = opts.textRects, dataLabelsX = opts.dataLabelsX, strokeWidth = opts.strokeWidth, dataLabelsConfig = opts.dataLabelsConfig, barDataLabelsConfig = opts.barDataLabelsConfig, barTotalDataLabelsConfig = opts.barTotalDataLabelsConfig, offX = opts.offX, offY = opts.offY;
                var dataPointsDividedHeight = w.layout.gridHeight / w.globals.dataPoints;
                var zeroEncounters = this.barCtx.barHelpers.getZeroValueEncounters({
                    i: i,
                    j: j
                }).zeroEncounters;
                barWidth = Math.abs(barWidth);
                var dataLabelsY;
                if (this.barCtx.isPyramid) {
                    var centerOffset = (_a = textRects.centerOffset) != null ? _a : 0;
                    dataLabelsY = bcy + barHeight / 2 + offY - centerOffset;
                } else {
                    dataLabelsY = bcy - (this.barCtx.isRangeBar ? 0 : dataPointsDividedHeight) + barHeight / 2 + textRects.height / 2 + offY - 3;
                }
                if (!w.config.chart.stacked && zeroEncounters > 0 && w.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
                    dataLabelsY -= barHeight * zeroEncounters;
                }
                var totalDataLabelsX;
                var totalDataLabelsY;
                var totalDataLabelsAnchor = "start";
                var valIsNegative = w.seriesData.series[i][j] < 0;
                var newX = x;
                if (this.barCtx.isReversed) {
                    newX = x + (valIsNegative ? -barWidth : barWidth);
                    totalDataLabelsAnchor = valIsNegative ? "start" : "end";
                }
                if (this.barCtx.isPyramid) {
                    dataLabelsX = w.layout.gridWidth / 2 + offX;
                } else {
                    switch(barDataLabelsConfig.position){
                        case "center":
                            if (valIsNegative) {
                                dataLabelsX = newX + barWidth / 2 - offX;
                            } else {
                                dataLabelsX = Math.max(textRects.width / 2, newX - barWidth / 2) + offX;
                            }
                            break;
                        case "bottom":
                            if (valIsNegative) {
                                dataLabelsX = newX + barWidth - strokeWidth - offX;
                            } else {
                                dataLabelsX = newX - barWidth + strokeWidth + offX;
                            }
                            break;
                        case "top":
                            if (valIsNegative) {
                                dataLabelsX = newX - strokeWidth - offX;
                            } else {
                                dataLabelsX = newX - strokeWidth + offX;
                            }
                            break;
                    }
                }
                var lowestPrevX = newX;
                w.labelData.seriesGroups.forEach(function(sg) {
                    var _a2;
                    (_a2 = _this.barCtx[sg.join(",")]) == null ? void 0 : _a2.prevX.forEach(function(arr) {
                        if (valIsNegative) {
                            lowestPrevX = Math.min(arr[j], lowestPrevX);
                        } else {
                            lowestPrevX = Math.max(arr[j], lowestPrevX);
                        }
                    });
                });
                if (this.barCtx.lastActiveBarSerieIndex === realIndex && barTotalDataLabelsConfig.enabled) {
                    var graphics = new Graphics(this.barCtx.w);
                    var totalLabeltextRects = graphics.getTextRects(this.getStackedTotalDataLabel({
                        realIndex: realIndex,
                        j: j
                    }), dataLabelsConfig.fontSize);
                    if (valIsNegative) {
                        totalDataLabelsX = lowestPrevX - strokeWidth - offX - barTotalDataLabelsConfig.offsetX;
                        totalDataLabelsAnchor = "end";
                    } else {
                        totalDataLabelsX = lowestPrevX + offX + barTotalDataLabelsConfig.offsetX + (this.barCtx.isReversed ? -(barWidth + strokeWidth) : strokeWidth);
                    }
                    totalDataLabelsY = dataLabelsY - textRects.height / 2 + totalLabeltextRects.height / 2 + barTotalDataLabelsConfig.offsetY + strokeWidth;
                    if (w.globals.barGroups.length > 1) {
                        totalDataLabelsY = totalDataLabelsY - w.globals.barGroups.length / 2 * (barHeight / 2);
                    }
                }
                if (!w.config.chart.stacked) {
                    if (dataLabelsConfig.textAnchor === "start") {
                        if (dataLabelsX - textRects.width < 0) {
                            dataLabelsX = valIsNegative ? textRects.width + strokeWidth - offX : strokeWidth + offX;
                        } else if (dataLabelsX + textRects.width > w.layout.gridWidth) {
                            dataLabelsX = valIsNegative ? w.layout.gridWidth - strokeWidth : w.layout.gridWidth - textRects.width - strokeWidth;
                        }
                    } else if (dataLabelsConfig.textAnchor === "middle") {
                        if (dataLabelsX - textRects.width / 2 < 0) {
                            dataLabelsX = textRects.width / 2 + strokeWidth;
                        } else if (dataLabelsX + textRects.width / 2 > w.layout.gridWidth) {
                            dataLabelsX = w.layout.gridWidth - textRects.width / 2 - strokeWidth;
                        }
                    } else if (dataLabelsConfig.textAnchor === "end") {
                        if (dataLabelsX < 1) {
                            dataLabelsX = textRects.width + strokeWidth;
                        } else if (dataLabelsX + 1 > w.layout.gridWidth) {
                            dataLabelsX = w.layout.gridWidth - textRects.width - strokeWidth;
                        }
                    }
                }
                return {
                    bcx: x,
                    bcy: bcy,
                    dataLabelsX: dataLabelsX,
                    dataLabelsY: dataLabelsY,
                    totalDataLabelsX: totalDataLabelsX,
                    totalDataLabelsY: totalDataLabelsY,
                    totalDataLabelsAnchor: totalDataLabelsAnchor
                };
            }
        },
        {
            /** @param {{x: any, y: any, val: any, i: any, j: any, textRects: any, barHeight: any, barWidth: any, dataLabelsConfig: any}} opts */ key: "drawCalculatedDataLabels",
            value: function drawCalculatedDataLabels(param) {
                var x = param.x, y = param.y, val = param.val, i = param.i, // = realIndex
                j = param.j, textRects = param.textRects, barHeight = param.barHeight, barWidth = param.barWidth, dataLabelsConfig = param.dataLabelsConfig;
                var _a, _b;
                var w = this.w;
                var rotate = "rotate(0)";
                if (w.config.plotOptions.bar.dataLabels.orientation === "vertical") rotate = "rotate(-90, ".concat(x, ", ").concat(y, ")");
                var dataLabels = new DataLabels(this.barCtx.w, this.barCtx.ctx);
                var graphics = new Graphics(this.barCtx.w);
                var formatter = dataLabelsConfig.formatter;
                var elDataLabelsWrap = null;
                var isSeriesNotCollapsed = w.globals.collapsedSeriesIndices.indexOf(i) > -1;
                if (dataLabelsConfig.enabled && !isSeriesNotCollapsed) {
                    elDataLabelsWrap = graphics.group({
                        class: "apexcharts-data-labels",
                        transform: rotate
                    });
                    var dlCfg = w.config.dataLabels;
                    if (((_a = dlCfg.animate) == null ? void 0 : _a.enabled) || ((_b = dlCfg.countUp) == null ? void 0 : _b.enabled)) {
                        elDataLabelsWrap.node.setAttribute("data:dlKey", "".concat(i, "::").concat(datumKey(w, i, j)));
                        elDataLabelsWrap.node.setAttribute("data:dlJ", String(j));
                        if (typeof val === "number" && isFinite(val)) {
                            elDataLabelsWrap.node.setAttribute("data:dlVal", String(val));
                        }
                    }
                    var text = "";
                    if (typeof val !== "undefined") {
                        text = formatter(val, __spreadProps(__spreadValues({}, w), {
                            seriesIndex: i,
                            dataPointIndex: j,
                            w: w
                        }));
                    }
                    if (!val && w.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
                        text = "";
                    }
                    var valIsNegative = w.seriesData.series[i][j] < 0;
                    var position = w.config.plotOptions.bar.dataLabels.position;
                    if (w.config.plotOptions.bar.dataLabels.orientation === "vertical") {
                        if (position === "top") {
                            if (valIsNegative) dataLabelsConfig.textAnchor = "end";
                            else dataLabelsConfig.textAnchor = "start";
                        }
                        if (position === "center") {
                            dataLabelsConfig.textAnchor = "middle";
                        }
                        if (position === "bottom") {
                            if (valIsNegative) dataLabelsConfig.textAnchor = "end";
                            else dataLabelsConfig.textAnchor = "start";
                        }
                    }
                    if (this.barCtx.isRangeBar && this.barCtx.barOptions.dataLabels.hideOverflowingLabels) {
                        var txRect = graphics.getTextRects(text, parseFloat(dataLabelsConfig.style.fontSize).toString());
                        if (barWidth < txRect.width) {
                            text = "";
                        }
                    }
                    if (w.config.chart.stacked && this.barCtx.barOptions.dataLabels.hideOverflowingLabels) {
                        if (this.barCtx.isHorizontal) {
                            if (textRects.width / 1.6 > Math.abs(barWidth)) {
                                text = "";
                            }
                        } else {
                            if (textRects.height / 1.6 > Math.abs(barHeight)) {
                                text = "";
                            }
                        }
                    }
                    var modifiedDataLabelsConfig = __spreadValues({}, dataLabelsConfig);
                    if (this.barCtx.isHorizontal) {
                        if (val < 0) {
                            if (dataLabelsConfig.textAnchor === "start") {
                                modifiedDataLabelsConfig.textAnchor = "end";
                            } else if (dataLabelsConfig.textAnchor === "end") {
                                modifiedDataLabelsConfig.textAnchor = "start";
                            }
                        }
                    }
                    dataLabels.plotDataLabelsText({
                        x: x,
                        y: y,
                        text: text,
                        i: i,
                        j: j,
                        parent: elDataLabelsWrap,
                        dataLabelsConfig: modifiedDataLabelsConfig,
                        alwaysDrawDataLabel: true,
                        offsetCorrection: true
                    });
                }
                return elDataLabelsWrap;
            }
        },
        {
            /** @param {{ x?: any, y?: any, val?: any, realIndex?: any, textAnchor?: any, barWidth?: any, barHeight?: any, dataLabelsConfig?: any, barTotalDataLabelsConfig?: any }} opts */ key: "drawTotalDataLabels",
            value: function drawTotalDataLabels(param) {
                var x = param.x, y = param.y, val = param.val, realIndex = param.realIndex, textAnchor = param.textAnchor, barTotalDataLabelsConfig = param.barTotalDataLabelsConfig;
                var graphics = new Graphics(this.barCtx.w);
                var totalDataLabelText;
                if (barTotalDataLabelsConfig.enabled && typeof x !== "undefined" && typeof y !== "undefined" && this.barCtx.lastActiveBarSerieIndex === realIndex) {
                    totalDataLabelText = graphics.drawText({
                        x: x,
                        y: y,
                        foreColor: barTotalDataLabelsConfig.style.color,
                        text: val,
                        textAnchor: textAnchor,
                        fontFamily: barTotalDataLabelsConfig.style.fontFamily,
                        fontSize: barTotalDataLabelsConfig.style.fontSize,
                        fontWeight: barTotalDataLabelsConfig.style.fontWeight
                    });
                }
                return totalDataLabelText;
            }
        }
    ]);
    return BarDataLabels;
}();
var Helpers$1 = /*#__PURE__*/ function() {
    "use strict";
    function Helpers4(barCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Helpers4);
        this.w = barCtx.w;
        this.barCtx = barCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Helpers4, [
        {
            /**
   * @param {any[]} series
   */ key: "initVariables",
            value: function initVariables(series) {
                var w = this.w;
                this.barCtx.series = series;
                this.barCtx.totalItems = 0;
                this.barCtx.seriesLen = 0;
                this.barCtx.visibleI = -1;
                this.barCtx.visibleItems = 1;
                for(var sl = 0; sl < series.length; sl++){
                    if (series[sl].length > 0) {
                        this.barCtx.seriesLen = this.barCtx.seriesLen + 1;
                        this.barCtx.totalItems += series[sl].length;
                    }
                    if (w.axisFlags.isXNumeric) {
                        for(var j = 0; j < series[sl].length; j++){
                            if (w.seriesData.seriesX[sl][j] > w.globals.minX && w.seriesData.seriesX[sl][j] < w.globals.maxX) {
                                this.barCtx.visibleItems++;
                            }
                        }
                    } else {
                        this.barCtx.visibleItems = w.globals.dataPoints;
                    }
                }
                this.arrBorderRadius = this.createBorderRadiusArr(w.seriesData.series);
                if (Utils$1.isSafari()) {
                    this.arrBorderRadius = this.arrBorderRadius.map(function(brArr) {
                        return(/**
           * @param {any} _
           */ brArr.map(function(_) {
                            return "none";
                        }));
                    });
                }
                if (this.barCtx.seriesLen === 0) {
                    this.barCtx.seriesLen = 1;
                }
            }
        },
        {
            /**
   * @param {number} realIndex
   */ key: "initialPositions",
            value: function initialPositions(realIndex) {
                var w = this.w;
                var x, y, yDivision, xDivision, barHeight, barWidth, zeroH, zeroW;
                var dataPoints = w.globals.dataPoints;
                if (this.barCtx.isRangeBar) {
                    dataPoints = w.labelData.labels.length;
                }
                var seriesLen = this.barCtx.seriesLen;
                if (w.config.plotOptions.bar.rangeBarGroupRows) {
                    seriesLen = 1;
                }
                if (this.barCtx.isHorizontal) {
                    yDivision = w.layout.gridHeight / dataPoints;
                    barHeight = yDivision / seriesLen;
                    if (w.axisFlags.isXNumeric) {
                        yDivision = w.layout.gridHeight / this.barCtx.totalItems;
                        barHeight = yDivision / this.barCtx.seriesLen;
                    }
                    barHeight = barHeight * parseInt(this.barCtx.barOptions.barHeight, 10) / 100;
                    if (String(this.barCtx.barOptions.barHeight).indexOf("%") === -1) {
                        barHeight = parseInt(this.barCtx.barOptions.barHeight, 10);
                    }
                    zeroW = this.barCtx.baseLineInvertedY + w.globals.padHorizontal + (this.barCtx.isReversed ? w.layout.gridWidth : 0) - (this.barCtx.isReversed ? this.barCtx.baseLineInvertedY * 2 : 0);
                    if (this.barCtx.isFunnel) {
                        zeroW = w.layout.gridWidth / 2;
                    }
                    y = (yDivision - barHeight * this.barCtx.seriesLen) / 2;
                } else {
                    xDivision = w.layout.gridWidth / this.barCtx.visibleItems;
                    if (w.config.xaxis.convertedCatToNumeric) {
                        xDivision = w.layout.gridWidth / w.globals.dataPoints;
                    }
                    barWidth = xDivision / seriesLen * parseInt(this.barCtx.barOptions.columnWidth, 10) / 100;
                    if (w.axisFlags.isXNumeric) {
                        var xRatio = this.barCtx.xRatio;
                        if (w.globals.minXDiff && w.globals.minXDiff !== 0.5 && w.globals.minXDiff / xRatio > 0) {
                            xDivision = w.globals.minXDiff / xRatio;
                        }
                        barWidth = xDivision / seriesLen * parseInt(this.barCtx.barOptions.columnWidth, 10) / 100;
                        if (barWidth < 1) {
                            barWidth = 1;
                        }
                    }
                    if (String(this.barCtx.barOptions.columnWidth).indexOf("%") === -1) {
                        barWidth = parseInt(this.barCtx.barOptions.columnWidth, 10);
                    }
                    zeroH = w.layout.gridHeight - this.barCtx.baseLineY[this.barCtx.translationsIndex] - (this.barCtx.isReversed ? w.layout.gridHeight : 0) + (this.barCtx.isReversed ? this.barCtx.baseLineY[this.barCtx.translationsIndex] * 2 : 0);
                    if (w.axisFlags.isXNumeric) {
                        var xForNumericX = this.barCtx.getBarXForNumericXAxis({
                            x: x,
                            j: 0,
                            realIndex: realIndex,
                            barWidth: barWidth
                        });
                        x = xForNumericX.x;
                    } else {
                        x = w.globals.padHorizontal + Utils$1.noExponents(xDivision - barWidth * this.barCtx.seriesLen) / 2;
                    }
                }
                w.globals.barHeight = barHeight;
                w.globals.barWidth = barWidth;
                return {
                    x: x,
                    y: y,
                    yDivision: yDivision,
                    xDivision: xDivision,
                    barHeight: barHeight,
                    barWidth: barWidth,
                    zeroH: zeroH,
                    zeroW: zeroW
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} ctx
   */ key: "initializeStackedPrevVars",
            value: function initializeStackedPrevVars(ctx) {
                var w = ctx.w;
                w.labelData.seriesGroups.forEach(function(group) {
                    if (!ctx[group]) ctx[group] = {};
                    ctx[group].prevY = [];
                    ctx[group].prevX = [];
                    ctx[group].prevYF = [];
                    ctx[group].prevXF = [];
                    ctx[group].prevYVal = [];
                    ctx[group].prevXVal = [];
                });
            }
        },
        {
            /**
   * @param {Record<string, any>} ctx
   */ key: "initializeStackedXYVars",
            value: function initializeStackedXYVars(ctx) {
                var w = ctx.w;
                w.labelData.seriesGroups.forEach(function(group) {
                    if (!ctx[group]) ctx[group] = {};
                    ctx[group].xArrj = [];
                    ctx[group].xArrjF = [];
                    ctx[group].xArrjVal = [];
                    ctx[group].yArrj = [];
                    ctx[group].yArrjF = [];
                    ctx[group].yArrjVal = [];
                });
            }
        },
        {
            /**
   * @param {any[]} series
   * @param {number} i
   * @param {number} j
   * @param {number} realIndex
   */ key: "getPathFillColor",
            value: function getPathFillColor(series, i, j, realIndex) {
                var _a, _b, _c, _d;
                var w = this.w;
                var fill = new Fill(this.barCtx.w);
                var fillColor = null;
                var seriesNumber = this.barCtx.barOptions.distributed ? j : i;
                var useRangeColor = false;
                if (this.barCtx.barOptions.colors.ranges.length > 0) {
                    var colorRange = this.barCtx.barOptions.colors.ranges;
                    colorRange.map(function(range) {
                        if (series[i][j] >= range.from && series[i][j] <= range.to) {
                            fillColor = range.color;
                            useRangeColor = true;
                        }
                    });
                }
                var pathFill = fill.fillPath({
                    seriesNumber: this.barCtx.barOptions.distributed ? seriesNumber : realIndex,
                    dataPointIndex: j,
                    color: fillColor,
                    value: series[i][j],
                    fillConfig: (_a = w.config.series[i].data[j]) == null ? void 0 : _a.fill,
                    fillType: ((_c = (_b = w.config.series[i].data[j]) == null ? void 0 : _b.fill) == null ? void 0 : _c.type) ? (_d = w.config.series[i].data[j]) == null ? void 0 : _d.fill.type : Array.isArray(w.config.fill.type) ? w.config.fill.type[realIndex] : w.config.fill.type
                });
                return {
                    color: pathFill,
                    useRangeColor: useRangeColor
                };
            }
        },
        {
            /**
   * @param {number} i
   * @param {number} j
   * @param {number} realIndex
   */ key: "getStrokeWidth",
            value: function getStrokeWidth(i, j, realIndex) {
                var strokeWidth = 0;
                var w = this.w;
                if (typeof this.barCtx.series[i][j] === "undefined" || this.barCtx.series[i][j] === null || w.config.chart.type === "bar" && !this.barCtx.series[i][j]) {
                    this.barCtx.isNullValue = true;
                } else {
                    this.barCtx.isNullValue = false;
                }
                if (w.config.stroke.show) {
                    if (!this.barCtx.isNullValue) {
                        strokeWidth = Array.isArray(this.barCtx.strokeWidth) ? this.barCtx.strokeWidth[realIndex] : this.barCtx.strokeWidth;
                    }
                }
                return strokeWidth;
            }
        },
        {
            /**
   * @param {any[]} series
   */ key: "createBorderRadiusArr",
            value: function createBorderRadiusArr(series) {
                var _a;
                var w = this.w;
                var alwaysApplyRadius = !this.w.config.chart.stacked || w.config.plotOptions.bar.borderRadius <= 0;
                var numSeries = series.length;
                var numColumns = ((_a = series[0]) == null ? void 0 : _a.length) | 0;
                var output = Array.from({
                    length: numSeries
                }, function() {
                    return Array(numColumns).fill(alwaysApplyRadius ? "top" : "none");
                });
                if (alwaysApplyRadius) return output;
                var chartType = this.w.config.chart.type;
                for(var j = 0; j < numColumns; j++){
                    var positiveIndices = [];
                    var negativeIndices = [];
                    var nonZeroCount = 0;
                    for(var i = 0; i < numSeries; i++){
                        var value = series[i][j];
                        if (value > 0) {
                            positiveIndices.push(i);
                            nonZeroCount++;
                        } else if (value < 0) {
                            negativeIndices.push(i);
                            nonZeroCount++;
                        }
                    }
                    if (positiveIndices.length > 0 && negativeIndices.length === 0) {
                        if (positiveIndices.length === 1) {
                            output[positiveIndices[0]][j] = chartType === "bar" && numColumns === 1 ? "top" : "both";
                        } else {
                            var firstPositiveIndex = positiveIndices[0];
                            var lastPositiveIndex = positiveIndices[positiveIndices.length - 1];
                            var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                            try {
                                for(var _iterator = positiveIndices[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                                    var i1 = _step.value;
                                    if (i1 === firstPositiveIndex) {
                                        output[i1][j] = chartType === "bar" && numColumns === 1 ? "top" : "bottom";
                                    } else if (i1 === lastPositiveIndex) {
                                        output[i1][j] = "top";
                                    } else {
                                        output[i1][j] = "none";
                                    }
                                }
                            } catch (err) {
                                _didIteratorError = true;
                                _iteratorError = err;
                            } finally{
                                try {
                                    if (!_iteratorNormalCompletion && _iterator.return != null) {
                                        _iterator.return();
                                    }
                                } finally{
                                    if (_didIteratorError) {
                                        throw _iteratorError;
                                    }
                                }
                            }
                        }
                    } else if (negativeIndices.length > 0 && positiveIndices.length === 0) {
                        if (negativeIndices.length === 1) {
                            output[negativeIndices[0]][j] = "both";
                        } else {
                            var _Math, _Math1;
                            var highestNegativeIndex = (_Math = Math).max.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(negativeIndices));
                            var lowestNegativeIndex = (_Math1 = Math).min.apply(_Math1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(negativeIndices));
                            var _iteratorNormalCompletion1 = true, _didIteratorError1 = false, _iteratorError1 = undefined;
                            try {
                                for(var _iterator1 = negativeIndices[Symbol.iterator](), _step1; !(_iteratorNormalCompletion1 = (_step1 = _iterator1.next()).done); _iteratorNormalCompletion1 = true){
                                    var i2 = _step1.value;
                                    if (i2 === highestNegativeIndex) {
                                        output[i2][j] = "bottom";
                                    } else if (i2 === lowestNegativeIndex) {
                                        output[i2][j] = "top";
                                    } else {
                                        output[i2][j] = "none";
                                    }
                                }
                            } catch (err) {
                                _didIteratorError1 = true;
                                _iteratorError1 = err;
                            } finally{
                                try {
                                    if (!_iteratorNormalCompletion1 && _iterator1.return != null) {
                                        _iterator1.return();
                                    }
                                } finally{
                                    if (_didIteratorError1) {
                                        throw _iteratorError1;
                                    }
                                }
                            }
                        }
                    } else if (positiveIndices.length > 0 && negativeIndices.length > 0) {
                        var _Math2;
                        var lastPositiveIndex1 = positiveIndices[positiveIndices.length - 1];
                        var _iteratorNormalCompletion2 = true, _didIteratorError2 = false, _iteratorError2 = undefined;
                        try {
                            for(var _iterator2 = positiveIndices[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true){
                                var i3 = _step2.value;
                                if (i3 === lastPositiveIndex1) {
                                    output[i3][j] = "top";
                                } else {
                                    output[i3][j] = "none";
                                }
                            }
                        } catch (err) {
                            _didIteratorError2 = true;
                            _iteratorError2 = err;
                        } finally{
                            try {
                                if (!_iteratorNormalCompletion2 && _iterator2.return != null) {
                                    _iterator2.return();
                                }
                            } finally{
                                if (_didIteratorError2) {
                                    throw _iteratorError2;
                                }
                            }
                        }
                        var highestNegativeIndex1 = (_Math2 = Math).max.apply(_Math2, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(negativeIndices));
                        var _iteratorNormalCompletion3 = true, _didIteratorError3 = false, _iteratorError3 = undefined;
                        try {
                            for(var _iterator3 = negativeIndices[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true){
                                var i4 = _step3.value;
                                if (i4 === highestNegativeIndex1) {
                                    output[i4][j] = "bottom";
                                } else {
                                    output[i4][j] = "none";
                                }
                            }
                        } catch (err) {
                            _didIteratorError3 = true;
                            _iteratorError3 = err;
                        } finally{
                            try {
                                if (!_iteratorNormalCompletion3 && _iterator3.return != null) {
                                    _iterator3.return();
                                }
                            } finally{
                                if (_didIteratorError3) {
                                    throw _iteratorError3;
                                }
                            }
                        }
                    } else if (nonZeroCount === 1) {
                        var index = positiveIndices[0] || negativeIndices[0];
                        output[index][j] = "both";
                    }
                }
                return output;
            }
        },
        {
            /** @param {{ j?: any, i?: any, x1?: any, x2?: any, y1?: any, y2?: any, bc?: any, elSeries?: any }} opts */ key: "barBackground",
            value: function barBackground(param) {
                var j = param.j, i = param.i, x1 = param.x1, x2 = param.x2, y1 = param.y1, y2 = param.y2, elSeries = param.elSeries;
                var w = this.w;
                var graphics = new Graphics(this.barCtx.w);
                var sr = new Series(this.barCtx.w);
                var activeSeriesIndex = sr.getActiveConfigSeriesIndex();
                if (this.barCtx.barOptions.colors.backgroundBarColors.length > 0 && activeSeriesIndex === i) {
                    if (j >= this.barCtx.barOptions.colors.backgroundBarColors.length) {
                        j %= this.barCtx.barOptions.colors.backgroundBarColors.length;
                    }
                    var bcolor = this.barCtx.barOptions.colors.backgroundBarColors[j];
                    var rect = graphics.drawRect(typeof x1 !== "undefined" ? x1 : 0, typeof y1 !== "undefined" ? y1 : 0, typeof x2 !== "undefined" ? x2 : w.layout.gridWidth, typeof y2 !== "undefined" ? y2 : w.layout.gridHeight, this.barCtx.barOptions.colors.backgroundBarRadius, bcolor, this.barCtx.barOptions.colors.backgroundBarOpacity);
                    elSeries.add(rect);
                    rect.node.classList.add("apexcharts-backgroundBar");
                }
            }
        },
        {
            /** @param {{ barWidth?: any, barXPosition?: any, y1?: any, y2?: any, yRatio?: any, strokeWidth?: any, isReversed?: any, series?: any, seriesGroup?: any, realIndex?: any, i?: any, j?: any, w?: any }} opts */ key: "getColumnPaths",
            value: function getColumnPaths(param) {
                var barWidth = param.barWidth, barXPosition = param.barXPosition, y1 = param.y1, y2 = param.y2, strokeWidth = param.strokeWidth, isReversed = param.isReversed, series = param.series, seriesGroup = param.seriesGroup, realIndex = param.realIndex, i = param.i, j = param.j, w = param.w;
                var _a, _b, _c;
                var graphics = new Graphics(this.barCtx.w);
                strokeWidth = Array.isArray(strokeWidth) ? strokeWidth[realIndex] : strokeWidth;
                if (!strokeWidth) strokeWidth = 0;
                var bW = barWidth;
                var bXP = barXPosition;
                if ((_a = w.config.series[realIndex].data[j]) == null ? void 0 : _a.columnWidthOffset) {
                    bXP = barXPosition - w.config.series[realIndex].data[j].columnWidthOffset / 2;
                    bW = barWidth + w.config.series[realIndex].data[j].columnWidthOffset;
                }
                var strokeCenter = strokeWidth / 2;
                var x1 = bXP + strokeCenter;
                var x2 = bXP + bW - strokeCenter;
                var direction = (series[i][j] >= 0 ? 1 : -1) * (isReversed ? -1 : 1);
                y1 += 1e-3 - strokeCenter * direction;
                y2 += 1e-3 + strokeCenter * direction;
                var sl = graphics.line(x2, y1);
                var closing = w.config.plotOptions.bar.borderRadiusApplication === "around" || this.arrBorderRadius[realIndex][j] === "both" ? " Z" : " z";
                var pathTo = graphics.move(x1, y1) + graphics.line(x1, y2) + graphics.line(x2, y2) + sl + closing;
                if (this.arrBorderRadius[realIndex][j] !== "none") {
                    pathTo = graphics.roundPathCorners(pathTo, w.config.plotOptions.bar.borderRadius);
                }
                var pathFrom = null;
                var morphFrom = (_c = (_b = this.barCtx.ctx) == null ? void 0 : _b.morphTypeChange) == null ? void 0 : _c.getInitialPathFor(realIndex, j);
                if (morphFrom) {
                    pathFrom = morphFrom;
                } else if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
                }
                if (pathFrom == null) {
                    pathFrom = graphics.move(x1, y1) + graphics.line(x1, y1) + sl + sl + sl + sl + sl + graphics.line(x1, y1) + closing;
                }
                if (w.config.chart.stacked) {
                    var _ctx = this.barCtx;
                    _ctx = this.barCtx[seriesGroup];
                    _ctx.yArrj.push(y2 - strokeCenter * direction);
                    _ctx.yArrjF.push(Math.abs(y1 - y2 + strokeWidth * direction));
                    _ctx.yArrjVal.push(this.barCtx.series[i][j]);
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom
                };
            }
        },
        {
            /**
   * Build a trapezoidal funnel-stage path. Used when
   * `plotOptions.funnel.shape === 'trapezoid'` is active alongside `isFunnel`.
   *
   * Each stage is a 4-corner polygon whose top width matches the current
   * stage's value and bottom width matches the next stage's value, producing
   * continuous sloped sides between consecutive stages.
   *
   * For the last stage, the bottom width is configurable:
   * - `lastShape: 'flat'`  → bottom width = top width (parallel sides)
   * - `lastShape: 'taper'` → bottom width = 0 (taper to a point)
   *
   * @param {{ barYPosition: number, barHeight: number, series: any[][], i: number, j: number, realIndex: number, strokeWidth: number, w: any }} opts
   */ key: "getFunnelTrapezoidPaths",
            value: function getFunnelTrapezoidPaths(param) {
                var _this = this;
                var barYPosition = param.barYPosition, barHeight = param.barHeight, series = param.series, i = param.i, j = param.j, realIndex = param.realIndex, strokeWidth = param.strokeWidth, w = param.w;
                var _a, _b;
                var graphics = new Graphics(this.barCtx.w);
                var center = w.layout.gridWidth / 2;
                var halfWidthFor = function halfWidthFor(v) {
                    return Math.abs(v / _this.barCtx.invertedYRatio) / 2;
                };
                var topHalf = halfWidthFor(series[i][j]);
                var lastIdx = series[i].length - 1;
                var isLast = j === lastIdx;
                var lastShape = w.config.plotOptions.funnel.lastShape === "taper" ? "taper" : "flat";
                var bottomHalf;
                if (isLast) {
                    bottomHalf = lastShape === "taper" ? 0 : topHalf;
                } else {
                    bottomHalf = halfWidthFor(series[i][j + 1]);
                }
                var strokeCenter = strokeWidth / 2;
                var y1 = barYPosition + strokeCenter;
                var y2 = barYPosition + barHeight - strokeCenter;
                var topLeftX = center - topHalf;
                var topRightX = center + topHalf;
                var bottomLeftX = center - bottomHalf;
                var bottomRightX = center + bottomHalf;
                var pathTo = graphics.move(topLeftX, y1) + graphics.line(topRightX, y1) + graphics.line(bottomRightX, y2) + graphics.line(bottomLeftX, y2) + " Z";
                var pathFrom = null;
                var morphFrom = (_b = (_a = this.barCtx.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.getInitialPathFor(realIndex, j);
                if (morphFrom) {
                    pathFrom = morphFrom;
                } else if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
                }
                if (pathFrom == null) {
                    pathFrom = graphics.move(center, y1) + graphics.line(center, y1) + graphics.line(center, y2) + graphics.line(center, y2) + " Z";
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom,
                    // x is the right edge of the wider (top) side — used by dataLabel
                    // positioning helpers that expect a "right" reference.
                    x: topRightX,
                    x1: topLeftX,
                    barXPosition: center
                };
            }
        },
        {
            /**
   * Pre-compute the per-segment layout for a value-proportional pyramid.
   *
   * Each segment is a horizontal slice of a triangle whose apex sits at the
   * top of the plot area (width = 0) and whose base spans `gridWidth` at the
   * bottom. The vertical extent of each slice is its share of the total
   * series value, so areas track value contribution and segments share
   * edges (no gaps). The first data point is the apex, the last is the base.
   *
   * @param {any[]} seriesData - 1D array of values for a single series row
   * @returns {{ y: number, height: number, topHalf: number, bottomHalf: number }[]}
   */ key: "computePyramidLayout",
            value: function computePyramidLayout(seriesData) {
                var w = this.w;
                var gridHeight = w.layout.gridHeight;
                var gridWidth = w.layout.gridWidth;
                var values = seriesData.map(/** @param {any} v */ function(v) {
                    return Math.abs(Number(v) || 0);
                });
                var total = values.reduce(/** @param {number} a @param {number} b */ function(a, b) {
                    return a + b;
                }, 0);
                if (total === 0 || gridHeight <= 0) {
                    return values.map(function() {
                        return {
                            y: 0,
                            height: 0,
                            topHalf: 0,
                            bottomHalf: 0
                        };
                    });
                }
                var halfWidth = gridWidth / 2;
                var cumulative = 0;
                var layout = [];
                for(var j = 0; j < values.length; j++){
                    var topRatio = cumulative / total;
                    cumulative += values[j];
                    var bottomRatio = cumulative / total;
                    var topY = topRatio * gridHeight;
                    var bottomY = bottomRatio * gridHeight;
                    layout.push({
                        y: topY,
                        height: bottomY - topY,
                        topHalf: topRatio * halfWidth,
                        bottomHalf: bottomRatio * halfWidth
                    });
                }
                return layout;
            }
        },
        {
            /**
   * Build a single pyramid stage path. Geometry is precomputed by
   * `computePyramidLayout`; this method only renders that geometry into an
   * SVG path string plus a `pathFrom` for entry/morph animations.
   *
   * @param {{ barYPosition: number, barHeight: number, topHalf: number, bottomHalf: number, realIndex: number, j: number, strokeWidth: number, w: any }} opts
   */ key: "getPyramidPaths",
            value: function getPyramidPaths(param) {
                var barYPosition = param.barYPosition, barHeight = param.barHeight, topHalf = param.topHalf, bottomHalf = param.bottomHalf, realIndex = param.realIndex, j = param.j, strokeWidth = param.strokeWidth, w = param.w;
                var _a, _b;
                var graphics = new Graphics(this.barCtx.w);
                var center = w.layout.gridWidth / 2;
                var strokeCenter = strokeWidth / 2;
                var y1 = barYPosition + strokeCenter;
                var y2 = barYPosition + barHeight - strokeCenter;
                var topLeftX = center - topHalf;
                var topRightX = center + topHalf;
                var bottomLeftX = center - bottomHalf;
                var bottomRightX = center + bottomHalf;
                var pathTo = graphics.move(topLeftX, y1) + graphics.line(topRightX, y1) + graphics.line(bottomRightX, y2) + graphics.line(bottomLeftX, y2) + " Z";
                var pathFrom = null;
                var morphFrom = (_b = (_a = this.barCtx.ctx) == null ? void 0 : _a.morphTypeChange) == null ? void 0 : _b.getInitialPathFor(realIndex, j);
                if (morphFrom) {
                    pathFrom = morphFrom;
                } else if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
                }
                if (pathFrom == null) {
                    pathFrom = graphics.move(center, y1) + graphics.line(center, y1) + graphics.line(center, y2) + graphics.line(center, y2) + " Z";
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom,
                    x: topRightX,
                    x1: topLeftX,
                    barXPosition: center
                };
            }
        },
        {
            /** @param {{ barYPosition?: any, barHeight?: any, x1?: any, x2?: any, strokeWidth?: any, isReversed?: any, series?: any, seriesGroup?: any, realIndex?: any, i?: any, j?: any, w?: any }} opts */ key: "getBarpaths",
            value: function getBarpaths(param) {
                var barYPosition = param.barYPosition, barHeight = param.barHeight, x1 = param.x1, x2 = param.x2, strokeWidth = param.strokeWidth, isReversed = param.isReversed, series = param.series, seriesGroup = param.seriesGroup, realIndex = param.realIndex, i = param.i, j = param.j, w = param.w;
                var _a, _b, _c;
                var graphics = new Graphics(this.barCtx.w);
                strokeWidth = Array.isArray(strokeWidth) ? strokeWidth[realIndex] : strokeWidth;
                if (!strokeWidth) strokeWidth = 0;
                var bYP = barYPosition;
                var bH = barHeight;
                if ((_a = w.config.series[realIndex].data[j]) == null ? void 0 : _a.barHeightOffset) {
                    bYP = barYPosition - w.config.series[realIndex].data[j].barHeightOffset / 2;
                    bH = barHeight + w.config.series[realIndex].data[j].barHeightOffset;
                }
                var strokeCenter = strokeWidth / 2;
                var y1 = bYP + strokeCenter;
                var y2 = bYP + bH - strokeCenter;
                var direction = (series[i][j] >= 0 ? 1 : -1) * (isReversed ? -1 : 1);
                x1 += 1e-3 + strokeCenter * direction;
                x2 += 1e-3 - strokeCenter * direction;
                var isFunnel = this.barCtx.isFunnel;
                var fromX = isFunnel ? (x1 + x2) / 2 : x1;
                var sl = graphics.line(x1, y2);
                var closing = w.config.plotOptions.bar.borderRadiusApplication === "around" || this.arrBorderRadius[realIndex][j] === "both" ? " Z" : " z";
                var pathTo = graphics.move(x1, y1) + graphics.line(x2, y1) + graphics.line(x2, y2) + sl + closing;
                if (this.arrBorderRadius[realIndex][j] !== "none") {
                    pathTo = graphics.roundPathCorners(pathTo, w.config.plotOptions.bar.borderRadius);
                }
                var pathFrom = null;
                var morphFrom = (_c = (_b = this.barCtx.ctx) == null ? void 0 : _b.morphTypeChange) == null ? void 0 : _c.getInitialPathFor(realIndex, j);
                if (morphFrom) {
                    pathFrom = morphFrom;
                } else if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.barCtx.getPreviousPath(realIndex, j, pathTo);
                }
                if (pathFrom == null) {
                    var slFrom = isFunnel ? graphics.line(fromX, y2) : sl;
                    pathFrom = graphics.move(fromX, y1) + graphics.line(fromX, y1) + slFrom + slFrom + slFrom + slFrom + slFrom + graphics.line(fromX, y1) + closing;
                }
                if (w.config.chart.stacked) {
                    var _ctx = this.barCtx;
                    _ctx = this.barCtx[seriesGroup];
                    _ctx.xArrj.push(x2 + strokeCenter * direction);
                    _ctx.xArrjF.push(Math.abs(x1 - x2 - strokeWidth * direction));
                    _ctx.xArrjVal.push(this.barCtx.series[i][j]);
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom
                };
            }
        },
        {
            /**
   * @param {number} value
   * @param {number} zeroW
   */ key: "getXForValue",
            value: function getXForValue(value, zeroW) {
                var zeroPositionForNull = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
                var xForVal = zeroPositionForNull ? zeroW : null;
                if (typeof value !== "undefined" && value !== null) {
                    xForVal = zeroW + value / this.barCtx.invertedYRatio - (this.barCtx.isReversed ? value / this.barCtx.invertedYRatio : 0) * 2;
                }
                return xForVal;
            }
        },
        {
            /**
   * @param {number} value
   * @param {number} zeroH
   * @param {number} translationsIndex
   */ key: "getYForValue",
            value: function getYForValue(value, zeroH, translationsIndex) {
                var zeroPositionForNull = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : true;
                var yForVal = zeroPositionForNull ? zeroH : null;
                if (typeof value !== "undefined" && value !== null) {
                    yForVal = zeroH - value / this.barCtx.yRatio[translationsIndex] + (this.barCtx.isReversed ? value / this.barCtx.yRatio[translationsIndex] : 0) * 2;
                }
                return yForVal;
            }
        },
        {
            /**
   * @param {string} type
   * @param {number} zeroW
   * @param {number} zeroH
   * @param {number} i
   * @param {number} j
   * @param {number} translationsIndex
   */ key: "getGoalValues",
            value: function getGoalValues(type, zeroW, zeroH, i, j, translationsIndex) {
                var _this = this;
                var w = this.w;
                var goals = [];
                var pushGoal = function pushGoal(value, attrs) {
                    var _obj;
                    goals.push((_obj = {}, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(_obj, type, type === "x" ? _this.getXForValue(value, zeroW, false) : _this.getYForValue(value, zeroH, translationsIndex, false)), (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_define_property$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(_obj, "attrs", attrs), _obj));
                };
                if (w.seriesData.seriesGoals[i] && w.seriesData.seriesGoals[i][j] && Array.isArray(w.seriesData.seriesGoals[i][j])) {
                    w.seriesData.seriesGoals[i][j].forEach(function(goal) {
                        pushGoal(goal.value, goal);
                    });
                }
                if (this.barCtx.barOptions.isDumbbell && w.rangeData.seriesRange.length) {
                    var colors = this.barCtx.barOptions.dumbbellColors ? this.barCtx.barOptions.dumbbellColors : w.globals.colors;
                    var commonAttrs = {
                        strokeHeight: type === "x" ? 0 : w.globals.markers.size[i],
                        strokeWidth: type === "x" ? w.globals.markers.size[i] : 0,
                        strokeDashArray: 0,
                        strokeLineCap: "round",
                        strokeColor: Array.isArray(colors[i]) ? colors[i][0] : colors[i]
                    };
                    pushGoal(w.rangeData.seriesRangeStart[i][j], commonAttrs);
                    pushGoal(w.rangeData.seriesRangeEnd[i][j], __spreadProps(__spreadValues({}, commonAttrs), {
                        strokeColor: Array.isArray(colors[i]) ? colors[i][1] : colors[i]
                    }));
                }
                return goals;
            }
        },
        {
            /** @param {{barXPosition: any, barYPosition: any, goalX: any, goalY: any, barWidth: any, barHeight: any}} opts */ key: "drawGoalLine",
            value: function drawGoalLine(param) {
                var barXPosition = param.barXPosition, barYPosition = param.barYPosition, goalX = param.goalX, goalY = param.goalY, barWidth = param.barWidth, barHeight = param.barHeight;
                var hasGoals = Array.isArray(goalX) && goalX.length > 0 || Array.isArray(goalY) && goalY.length > 0;
                if (!hasGoals) {
                    return null;
                }
                var graphics = new Graphics(this.barCtx.w);
                var lineGroup = graphics.group({
                    className: "apexcharts-bar-goals-groups"
                });
                lineGroup.node.classList.add("apexcharts-element-hidden");
                this.barCtx.w.globals.delayedElements.push({
                    el: lineGroup.node
                });
                lineGroup.attr("clip-path", "url(#gridRectMarkerMask".concat(this.barCtx.w.globals.cuid, ")"));
                var line = null;
                if (this.barCtx.isHorizontal) {
                    if (Array.isArray(goalX)) {
                        goalX.forEach(function(goal) {
                            if (goal.x >= -1 && goal.x <= graphics.w.layout.gridWidth + 1) {
                                var sHeight = typeof goal.attrs.strokeHeight !== "undefined" ? goal.attrs.strokeHeight : barHeight / 2;
                                var y = barYPosition + sHeight + barHeight / 2;
                                line = graphics.drawLine(goal.x, y - sHeight * 2, goal.x, y, goal.attrs.strokeColor ? goal.attrs.strokeColor : void 0, goal.attrs.strokeDashArray, goal.attrs.strokeWidth ? goal.attrs.strokeWidth : 2, goal.attrs.strokeLineCap);
                                lineGroup.add(line);
                            }
                        });
                    }
                } else {
                    if (Array.isArray(goalY)) {
                        goalY.forEach(function(goal) {
                            if (goal.y >= -1 && goal.y <= graphics.w.layout.gridHeight + 1) {
                                var sWidth = typeof goal.attrs.strokeWidth !== "undefined" ? goal.attrs.strokeWidth : barWidth / 2;
                                var x = barXPosition + sWidth + barWidth / 2;
                                line = graphics.drawLine(x - sWidth * 2, goal.y, x, goal.y, goal.attrs.strokeColor ? goal.attrs.strokeColor : void 0, goal.attrs.strokeDashArray, goal.attrs.strokeHeight ? goal.attrs.strokeHeight : 2, goal.attrs.strokeLineCap);
                                lineGroup.add(line);
                            }
                        });
                    }
                }
                return lineGroup;
            }
        },
        {
            /** @param {{prevPaths: any, currPaths: any, color: any, realIndex: any, j: any}} opts */ key: "drawBarShadow",
            value: function drawBarShadow(param) {
                var prevPaths = param.prevPaths, currPaths = param.currPaths, color = param.color, realIndex = param.realIndex, j = param.j;
                var w = this.w;
                var prevX2 = prevPaths.x, prevX1 = prevPaths.x1, prevY1 = prevPaths.barYPosition;
                var currX2 = currPaths.x, currX1 = currPaths.x1, currY1 = currPaths.barYPosition;
                var prevY2 = prevY1 + currPaths.barHeight;
                var graphics = new Graphics(this.barCtx.w);
                var utils = new Utils$1();
                var shadowPath = graphics.move(prevX1, prevY2) + graphics.line(prevX2, prevY2) + graphics.line(currX2, currY1) + graphics.line(currX1, currY1) + graphics.line(prevX1, prevY2) + (w.config.plotOptions.bar.borderRadiusApplication === "around" || this.arrBorderRadius[realIndex][j] === "both" ? " Z" : " z");
                return graphics.drawPath({
                    d: shadowPath,
                    fill: utils.shadeColor(0.5, Utils$1.rgb2hex(color)),
                    stroke: "none",
                    strokeWidth: 0,
                    fillOpacity: 1,
                    classes: "apexcharts-bar-shadow apexcharts-decoration-element"
                });
            }
        },
        {
            /** @param {{i: any, j: any}} opts */ key: "getZeroValueEncounters",
            value: function getZeroValueEncounters(param) {
                var i = param.i, j = param.j;
                var _a;
                var w = this.w;
                var nonZeroColumns = 0;
                var zeroEncounters = 0;
                var seriesIndices = w.config.plotOptions.bar.horizontal ? w.seriesData.series.map(function(_, _i) {
                    return _i;
                }) : ((_a = w.globals.columnSeries) == null ? void 0 : _a.i.map(function(_i) {
                    return _i;
                })) || [];
                seriesIndices.forEach(function(_si) {
                    var val = w.globals.seriesPercent[_si][j];
                    if (val) {
                        nonZeroColumns++;
                    }
                    if (_si < i && val === 0) {
                        zeroEncounters++;
                    }
                });
                return {
                    nonZeroColumns: nonZeroColumns,
                    zeroEncounters: zeroEncounters
                };
            }
        },
        {
            /**
   * @param {number} seriesIndex
   */ key: "getGroupIndex",
            value: function getGroupIndex(seriesIndex) {
                var w = this.w;
                var groupIndex = w.labelData.seriesGroups.findIndex(function(group) {
                    return(// w.config.series[i].name may be undefined, so use
                    // w.seriesData.seriesNames[i], which has default names for those
                    // series. w.labelData.seriesGroups[] uses the same default naming.
                    group.indexOf(w.seriesData.seriesNames[seriesIndex]) > -1);
                });
                var cGI = this.barCtx.columnGroupIndices;
                var columnGroupIndex = cGI.indexOf(groupIndex);
                if (columnGroupIndex < 0) {
                    cGI.push(groupIndex);
                    columnGroupIndex = cGI.length - 1;
                }
                return {
                    groupIndex: groupIndex,
                    columnGroupIndex: columnGroupIndex
                };
            }
        }
    ]);
    return Helpers4;
}();
var Bar = /*#__PURE__*/ function() {
    "use strict";
    function Bar(w, ctx, xyRatios) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Bar);
        this.ctx = ctx;
        this.w = w;
        this.barOptions = w.config.plotOptions.bar;
        this.isHorizontal = this.barOptions.horizontal;
        this.strokeWidth = w.config.stroke.width;
        this.isNullValue = false;
        this.isRangeBar = w.rangeData.seriesRange.length && this.isHorizontal;
        this.isVerticalGroupedRangeBar = !w.globals.isBarHorizontal && w.rangeData.seriesRange.length && w.config.plotOptions.bar.rangeBarGroupRows;
        this.isFunnel = this.barOptions.isFunnel;
        this.isPyramid = this.barOptions.isPyramid;
        this.pyramidLayout = null;
        this.xyRatios = xyRatios;
        this.xRatio = 0;
        this.yRatio = [];
        this.invertedXRatio = 0;
        this.invertedYRatio = 0;
        this.baseLineY = [];
        this.baseLineInvertedY = 0;
        if (this.xyRatios !== null) {
            this.xRatio = xyRatios.xRatio;
            this.yRatio = xyRatios.yRatio;
            this.invertedXRatio = xyRatios.invertedXRatio;
            this.invertedYRatio = xyRatios.invertedYRatio;
            this.baseLineY = xyRatios.baseLineY;
            this.baseLineInvertedY = xyRatios.baseLineInvertedY;
        }
        this.yaxisIndex = 0;
        this.translationsIndex = 0;
        this.seriesLen = 0;
        this.pathArr = [];
        this._prevKeyed = null;
        this._ltCache = null;
        this._layoutShiftCache = null;
        this.series = [];
        this.elSeries = null;
        this.visibleI = 0;
        this.isReversed = false;
        var ser = new Series(this.w);
        this.lastActiveBarSerieIndex = ser.getActiveConfigSeriesIndex("desc", [
            "bar",
            "column"
        ]);
        this.columnGroupIndices = [];
        var barSeriesIndices = ser.getBarSeriesIndices();
        var coreUtils = new CoreUtils(this.w);
        this.stackedSeriesTotals = coreUtils.getStackedSeriesTotals(this.w.config.series.map(function(s, i) {
            return barSeriesIndices.indexOf(i) === -1 ? i : -1;
        }).filter(function(s) {
            return s !== -1;
        }));
        this.barHelpers = new Helpers$1(this);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Bar, [
        {
            /** primary draw method which is called on bar object
   * @memberof Bar
   * @param {any[]} series - user supplied series values
   * @param {number} seriesIndex - the index by which series will be drawn on the svg
   * @return {Element} element which is supplied to parent chart draw method for appending
   **/ key: "draw",
            value: function draw(series, seriesIndex) {
                var _a, _b, _c;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var coreUtils = new CoreUtils(this.w);
                series = coreUtils.getLogSeries(series);
                this.series = series;
                this.yRatio = coreUtils.getLogYRatios(this.yRatio);
                this.barHelpers.initVariables(series);
                var ret = graphics.group({
                    class: "apexcharts-bar-series apexcharts-plot-series"
                });
                if (w.config.dataLabels.enabled) {
                    if (this.totalItems > this.barOptions.dataLabels.maxItems) {
                        console.warn("WARNING: DataLabels are enabled but there are too many to display. This may cause performance issue when rendering - ApexCharts");
                    }
                }
                for(var i = 0, bc = 0; i < series.length; i++, bc++){
                    var x = void 0, y = void 0;
                    var yArrj = [];
                    var xArrj = [];
                    var realIndex = w.globals.comboCharts ? /** @type {any} */ seriesIndex[i] : i;
                    var columnGroupIndex = this.barHelpers.getGroupIndex(realIndex).columnGroupIndex;
                    var elSeries = graphics.group({
                        class: "apexcharts-series",
                        rel: i + 1,
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex]),
                        "data:realIndex": realIndex
                    });
                    Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
                    if (series[i].length > 0) {
                        this.visibleI = this.visibleI + 1;
                    }
                    if (this.yRatio.length > 1) {
                        this.yaxisIndex = w.globals.seriesYAxisReverseMap[realIndex];
                        this.translationsIndex = realIndex;
                    }
                    var translationsIndex = this.translationsIndex;
                    this.isReversed = w.config.yaxis[this.yaxisIndex] && w.config.yaxis[this.yaxisIndex].reversed;
                    if (this.isPyramid) {
                        this.pyramidLayout = this.barHelpers.computePyramidLayout(series[i]);
                    }
                    var initPositions = this.barHelpers.initialPositions(realIndex);
                    var initY = initPositions.y, yDivision = initPositions.yDivision, // yDivision is the GRIDHEIGHT divided by number of datapoints (bars)
                    zeroW = initPositions.zeroW, // zeroW is the baseline where 0 meets x axis
                    initX = initPositions.x, xDivision = initPositions.xDivision, // xDivision is the GRIDWIDTH divided by number of datapoints (columns)
                    zeroH = initPositions.zeroH;
                    var barHeight = initPositions.barHeight;
                    var barWidth = initPositions.barWidth;
                    y = initY;
                    x = initX;
                    if (!this.isHorizontal) {
                        xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
                    }
                    var elDataLabelsWrap = graphics.group({
                        class: "apexcharts-datalabels",
                        "data:realIndex": realIndex
                    });
                    w.globals.delayedElements.push({
                        el: elDataLabelsWrap.node,
                        // On a layout-changing update the labels must stay hidden through the
                        // reflow morph (the updateOptions flow otherwise reveals them at
                        // frame 0, where they float over sliding bars). When dataLabels.animate
                        // is on the labels instead RIDE the morph (see DataLabelTransition), so
                        // keep them visible: holding would hide the very motion we want to show.
                        holdUntilComplete: !((_a = w.config.dataLabels.animate) == null ? void 0 : _a.enabled) && this.isLengthTransition(realIndex)
                    });
                    elDataLabelsWrap.node.classList.add("apexcharts-element-hidden");
                    var elGoalsMarkers = graphics.group({
                        class: "apexcharts-bar-goals-markers"
                    });
                    var elBarShadows = graphics.group({
                        class: "apexcharts-bar-shadows"
                    });
                    w.globals.delayedElements.push({
                        el: elBarShadows.node
                    });
                    elBarShadows.node.classList.add("apexcharts-element-hidden");
                    for(var j = 0; j < series[i].length; j++){
                        var strokeWidth = this.barHelpers.getStrokeWidth(i, j, realIndex);
                        var paths = /** @type {any} */ null;
                        var pathsParams = {
                            indexes: {
                                i: i,
                                j: j,
                                realIndex: realIndex,
                                translationsIndex: translationsIndex,
                                bc: bc
                            },
                            x: x,
                            y: y,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries
                        };
                        if (this.isHorizontal) {
                            paths = this.drawBarPaths(__spreadProps(__spreadValues({}, pathsParams), {
                                barHeight: barHeight,
                                zeroW: zeroW,
                                yDivision: yDivision
                            }));
                            barWidth = this.series[i][j] / this.invertedYRatio;
                        } else {
                            paths = this.drawColumnPaths(__spreadProps(__spreadValues({}, pathsParams), {
                                xDivision: xDivision,
                                barWidth: barWidth,
                                zeroH: zeroH
                            }));
                            barHeight = this.series[i][j] / this.yRatio[translationsIndex];
                        }
                        var pathFill = this.barHelpers.getPathFillColor(series, i, j, realIndex);
                        if (this.isFunnel && !this.isPyramid && this.barOptions.isFunnel3d && ((_b = w.config.plotOptions.funnel) == null ? void 0 : _b.shape) !== "trapezoid" && this.pathArr.length && j > 0) {
                            var barShadow = this.barHelpers.drawBarShadow({
                                color: typeof pathFill.color === "string" && ((_c = pathFill.color) == null ? void 0 : _c.indexOf("url")) === -1 ? pathFill.color : Utils$1.hexToRgba(w.globals.colors[i]),
                                prevPaths: this.pathArr[this.pathArr.length - 1],
                                currPaths: paths,
                                realIndex: realIndex,
                                j: j
                            });
                            elBarShadows.add(barShadow);
                            if (w.config.chart.dropShadow.enabled) {
                                var filters = new Filters(this.w);
                                filters.dropShadow(barShadow, w.config.chart.dropShadow, realIndex);
                            }
                        }
                        this.pathArr.push(paths);
                        var barGoalLine = this.barHelpers.drawGoalLine({
                            barXPosition: paths.barXPosition,
                            barYPosition: paths.barYPosition,
                            goalX: paths.goalX,
                            goalY: paths.goalY,
                            barHeight: barHeight,
                            barWidth: barWidth
                        });
                        if (barGoalLine) {
                            elGoalsMarkers.add(barGoalLine);
                        }
                        y = paths.y;
                        x = paths.x;
                        if (j > 0) {
                            xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
                        }
                        yArrj.push(y);
                        this.renderSeries(__spreadProps(__spreadValues({
                            realIndex: realIndex,
                            pathFill: pathFill.color
                        }, pathFill.useRangeColor ? {
                            lineFill: pathFill.color
                        } : {}), {
                            j: j,
                            i: i,
                            columnGroupIndex: columnGroupIndex,
                            pathFrom: paths.pathFrom,
                            pathTo: paths.pathTo,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries,
                            x: x,
                            y: y,
                            series: series,
                            barHeight: Math.abs(paths.barHeight ? paths.barHeight : barHeight),
                            barWidth: Math.abs(paths.barWidth ? paths.barWidth : barWidth),
                            elDataLabelsWrap: elDataLabelsWrap,
                            elGoalsMarkers: elGoalsMarkers,
                            elBarShadows: elBarShadows,
                            visibleSeries: this.visibleI,
                            type: "bar"
                        }));
                    }
                    w.globals.seriesXvalues[realIndex] = xArrj;
                    w.globals.seriesYvalues[realIndex] = yArrj;
                    if (w.globals.previousPaths.length > 0) {
                        var newKeys = [];
                        for(var j1 = 0; j1 < series[i].length; j1++){
                            newKeys.push(datumKey(w, realIndex, j1));
                        }
                        renderBarExitGhosts({
                            w: w,
                            elSeries: elSeries,
                            record: this._prevRecord(realIndex),
                            newKeys: newKeys,
                            isHorizontal: this.isHorizontal,
                            speed: w.config.chart.animations.dynamicAnimation.speed
                        });
                    }
                    ret.add(elSeries);
                }
                return ret;
            }
        },
        {
            /** @param {{ realIndex?: any, pathFill?: any, lineFill?: any, j?: any, i?: any, columnGroupIndex?: any, pathFrom?: any, pathTo?: any, strokeWidth?: any, elSeries?: any, x?: any, y?: any, y1?: any, y2?: any, series?: any, barHeight?: any, barWidth?: any, barXPosition?: any, barYPosition?: any, elDataLabelsWrap?: any, elGoalsMarkers?: any, elBarShadows?: any, visibleSeries?: any, type?: any, classes?: any }} opts */ key: "renderSeries",
            value: function renderSeries(param) {
                var realIndex = param.realIndex, pathFill = param.pathFill, lineFill = param.lineFill, j = param.j, i = param.i, columnGroupIndex = param.columnGroupIndex, pathFrom = param.pathFrom, pathTo = param.pathTo, strokeWidth = param.strokeWidth, elSeries = param.elSeries, x = param.x, // x pos
                y = param.y, // y pos
                y1 = param.y1, // absolute value
                y2 = param.y2, // absolute value
                series = param.series, barHeight = param.barHeight, barWidth = param.barWidth, barXPosition = param.barXPosition, barYPosition = param.barYPosition, elDataLabelsWrap = param.elDataLabelsWrap, elGoalsMarkers = param.elGoalsMarkers, elBarShadows = param.elBarShadows, visibleSeries = param.visibleSeries, type = param.type, classes = param.classes;
                var _a;
                var w = this.w;
                var graphics = new Graphics(this.w, this.ctx);
                var emit = seriesEmitter(this.ctx, graphics);
                var skipDrawing = false;
                if (!elSeries._bindingsDelegated) {
                    elSeries._bindingsDelegated = true;
                    graphics.setupEventDelegation(elSeries, ".apexcharts-".concat(type, "-area"));
                }
                if (!lineFill) {
                    var fetchColor = function fetchColor(i2) {
                        var exp = w.config.stroke.colors;
                        var c;
                        if (Array.isArray(exp) && exp.length > 0) {
                            c = exp[i2];
                            if (!c) c = "";
                            if (typeof c === "function") {
                                return c({
                                    value: w.seriesData.series[i2][j],
                                    dataPointIndex: j,
                                    w: w
                                });
                            }
                        }
                        return c;
                    };
                    var checkAvailableColor = typeof w.globals.stroke.colors[realIndex] === "function" ? fetchColor(realIndex) : w.globals.stroke.colors[realIndex];
                    lineFill = this.barOptions.distributed ? w.globals.stroke.colors[j] : checkAvailableColor;
                }
                var barDataLabels = new BarDataLabels(this);
                var dataLabelsObj = /** @type {any} */ barDataLabels.handleBarDataLabels({
                    x: x,
                    y: y,
                    y1: y1,
                    y2: y2,
                    i: i,
                    j: j,
                    series: series,
                    realIndex: realIndex,
                    columnGroupIndex: columnGroupIndex,
                    barHeight: barHeight,
                    barWidth: barWidth,
                    barXPosition: barXPosition,
                    barYPosition: barYPosition,
                    visibleSeries: visibleSeries
                });
                if (!w.globals.isBarHorizontal) {
                    if (dataLabelsObj.dataLabelsPos.dataLabelsX + Math.max(barWidth, w.globals.barPadForNumericAxis) < 0 || dataLabelsObj.dataLabelsPos.dataLabelsX - Math.max(barWidth, w.globals.barPadForNumericAxis) > w.layout.gridWidth) {
                        skipDrawing = true;
                    }
                }
                if (/** @type {Record<string,any>} */ w.config.series[i].data[j] && /** @type {Record<string,any>} */ w.config.series[i].data[j].strokeColor) {
                    lineFill = /** @type {Record<string,any>} */ w.config.series[i].data[j].strokeColor;
                }
                if (this.isNullValue) {
                    pathFill = "none";
                }
                var animCfg = w.config.chart.animations;
                var gradCfg = animCfg.animateGradually;
                var staggerEnabled = gradCfg && gradCfg.enabled !== false && !(w.globals.dataChanged && this.isLayoutShift(realIndex));
                var delay = 0;
                if (staggerEnabled) {
                    var totalBars = w.globals.dataPoints || 1;
                    var configStep = gradCfg.delay || 0;
                    var baseDelayMs = Math.min(configStep, animCfg.speed * 0.5 / Math.max(1, totalBars));
                    var delayMs = computeStagger({
                        style: "sequential",
                        index: j,
                        baseDelay: baseDelayMs
                    });
                    if (w.config.chart.stacked) {
                        delayMs += i * baseDelayMs * 0.5;
                    }
                    var delayFactor = configStep || 1;
                    delay = delayMs / delayFactor;
                }
                if (!skipDrawing) {
                    var morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
                    var dataChangeSpeed = morphActive ? this.ctx.morphTypeChange.getSpeed() : w.config.chart.animations.dynamicAnimation.speed;
                    var renderedPath = /** @type {any} */ emit.renderPaths({
                        i: i,
                        j: j,
                        realIndex: realIndex,
                        pathFrom: pathFrom,
                        pathTo: pathTo,
                        stroke: lineFill,
                        strokeWidth: strokeWidth,
                        strokeLineCap: w.config.stroke.lineCap,
                        fill: pathFill,
                        animationDelay: delay,
                        initialSpeed: w.config.chart.animations.speed,
                        dataChangeSpeed: dataChangeSpeed,
                        className: "apexcharts-".concat(type, "-area ").concat(classes),
                        chartType: type,
                        bindEventsOnPaths: false
                    });
                    renderedPath.attr("clip-path", "url(#gridRectBarMask".concat(w.globals.cuid, ")"));
                    var forecast = w.config.forecastDataPoints;
                    if (forecast.count > 0) {
                        if (j >= w.globals.dataPoints - forecast.count) {
                            renderedPath.node.setAttribute("stroke-dasharray", forecast.dashArray);
                            renderedPath.node.setAttribute("stroke-width", forecast.strokeWidth);
                            renderedPath.node.setAttribute("fill-opacity", forecast.fillOpacity);
                        }
                    }
                    if (typeof y1 !== "undefined" && typeof y2 !== "undefined") {
                        renderedPath.attr("data-range-y1", y1);
                        renderedPath.attr("data-range-y2", y2);
                    }
                    var filters = new Filters(this.w);
                    filters.setSelectionFilter(renderedPath, realIndex, j);
                    elSeries.add(renderedPath);
                    renderedPath.attr({
                        cy: dataLabelsObj.dataLabelsPos.bcy,
                        cx: dataLabelsObj.dataLabelsPos.bcx,
                        j: j,
                        val: w.seriesData.series[i][j],
                        barHeight: barHeight,
                        barWidth: barWidth,
                        // Datum identity for the next update's keyed join (see
                        // LengthTransition): survivors match by key, not array position.
                        "data:pathKey": datumKey(w, realIndex, j)
                    });
                    if (emit.kind === "canvas") {
                        if (!w.globals.barCanvasCoords) w.globals.barCanvasCoords = {};
                        if (!w.globals.barCanvasCoords[realIndex]) {
                            w.globals.barCanvasCoords[realIndex] = {};
                        }
                        w.globals.barCanvasCoords[realIndex][j] = {
                            cx: dataLabelsObj.dataLabelsPos.bcx,
                            cy: dataLabelsObj.dataLabelsPos.bcy,
                            barWidth: barWidth
                        };
                    }
                    if (dataLabelsObj.dataLabels !== null) {
                        elDataLabelsWrap.add(dataLabelsObj.dataLabels);
                    }
                    if (dataLabelsObj.totalDataLabels) {
                        elDataLabelsWrap.add(dataLabelsObj.totalDataLabels);
                    }
                    elSeries.add(elDataLabelsWrap);
                    if (elGoalsMarkers) {
                        elSeries.add(elGoalsMarkers);
                    }
                    if (elBarShadows) {
                        elSeries.add(elBarShadows);
                    }
                }
                return elSeries;
            }
        },
        {
            /** @param {{indexes: any, barHeight: any, strokeWidth: any, zeroW: any, x: any, y: any, yDivision: any, elSeries: any}} opts */ key: "drawBarPaths",
            value: function drawBarPaths(param) {
                var indexes = param.indexes, barHeight = param.barHeight, strokeWidth = param.strokeWidth, zeroW = param.zeroW, x = param.x, y = param.y, yDivision = param.yDivision, elSeries = param.elSeries;
                var w = this.w;
                var i = indexes.i;
                var j = indexes.j;
                var barYPosition;
                if (w.axisFlags.isXNumeric) {
                    y = (w.seriesData.seriesX[i][j] - w.globals.minX) / this.invertedXRatio - barHeight;
                    barYPosition = y + barHeight * this.visibleI;
                } else {
                    if (w.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
                        var _this_barHelpers_getZeroValueEncounters = this.barHelpers.getZeroValueEncounters({
                            i: i,
                            j: j
                        }), nonZeroColumns = _this_barHelpers_getZeroValueEncounters.nonZeroColumns, zeroEncounters = _this_barHelpers_getZeroValueEncounters.zeroEncounters;
                        if (nonZeroColumns > 0) {
                            barHeight = this.seriesLen * barHeight / nonZeroColumns;
                        }
                        barYPosition = y + barHeight * this.visibleI;
                        barYPosition -= barHeight * zeroEncounters;
                    } else {
                        barYPosition = y + barHeight * this.visibleI;
                    }
                }
                var useTrapezoid = this.isFunnel && w.config.plotOptions.funnel.shape === "trapezoid";
                var pyramidSeg = this.isPyramid && this.pyramidLayout ? this.pyramidLayout[j] : null;
                var usePyramid = !!pyramidSeg;
                if (pyramidSeg) {
                    barYPosition = pyramidSeg.y;
                    barHeight = pyramidSeg.height;
                } else if (this.isFunnel && !useTrapezoid) {
                    var _zeroW = zeroW != null ? zeroW : 0;
                    zeroW = _zeroW - /** @type {number} */ /** @type {any} */ (this.barHelpers.getXForValue(/** @type {any} */ this.series[i][j], _zeroW) - _zeroW) / 2;
                }
                x = this.barHelpers.getXForValue(/** @type {any} */ this.series[i][j], zeroW != null ? zeroW : 0);
                var paths;
                if (pyramidSeg) {
                    paths = /** @type {any} */ this.barHelpers.getPyramidPaths({
                        barYPosition: barYPosition,
                        barHeight: barHeight,
                        topHalf: pyramidSeg.topHalf,
                        bottomHalf: pyramidSeg.bottomHalf,
                        realIndex: indexes.realIndex,
                        j: j,
                        strokeWidth: strokeWidth,
                        w: w
                    });
                } else if (useTrapezoid) {
                    paths = /** @type {any} */ this.barHelpers.getFunnelTrapezoidPaths({
                        barYPosition: barYPosition,
                        barHeight: barHeight,
                        series: /** @type {any} */ this.series,
                        i: i,
                        j: j,
                        realIndex: indexes.realIndex,
                        strokeWidth: strokeWidth,
                        w: w
                    });
                } else {
                    paths = /** @type {any} */ this.barHelpers.getBarpaths({
                        barYPosition: barYPosition,
                        barHeight: barHeight,
                        x1: zeroW,
                        x2: x,
                        strokeWidth: strokeWidth,
                        isReversed: this.isReversed,
                        series: this.series,
                        realIndex: indexes.realIndex,
                        i: i,
                        j: j,
                        w: w
                    });
                }
                if (useTrapezoid || usePyramid) {
                    zeroW = paths.x1;
                    x = paths.x;
                }
                if (!w.axisFlags.isXNumeric && !usePyramid) {
                    y = y + yDivision;
                }
                if (usePyramid) {
                    y = barYPosition;
                }
                this.barHelpers.barBackground({
                    j: j,
                    i: i,
                    y1: barYPosition - barHeight * this.visibleI,
                    y2: barHeight * this.seriesLen,
                    elSeries: elSeries
                });
                return {
                    pathTo: paths.pathTo,
                    pathFrom: paths.pathFrom,
                    x1: zeroW,
                    x: x,
                    y: y,
                    goalX: this.barHelpers.getGoalValues("x", zeroW, /** @type {any} */ null, indexes.realIndex, j, 0),
                    barYPosition: barYPosition,
                    barHeight: barHeight
                };
            }
        },
        {
            /** @param {{indexes: any, x: any, y: any, xDivision: any, barWidth: any, zeroH: any, strokeWidth: any, elSeries: any}} opts */ key: "drawColumnPaths",
            value: function drawColumnPaths(param) {
                var indexes = param.indexes, x = param.x, y = param.y, xDivision = param.xDivision, barWidth = param.barWidth, zeroH = param.zeroH, strokeWidth = param.strokeWidth, elSeries = param.elSeries;
                var w = this.w;
                var realIndex = indexes.realIndex;
                var translationsIndex = indexes.translationsIndex;
                var i = indexes.i;
                var j = indexes.j;
                var bc = indexes.bc;
                var barXPosition;
                if (w.axisFlags.isXNumeric) {
                    var xForNumericX = this.getBarXForNumericXAxis({
                        x: x,
                        j: j,
                        realIndex: realIndex,
                        barWidth: barWidth
                    });
                    x = xForNumericX.x;
                    barXPosition = xForNumericX.barXPosition;
                } else {
                    if (w.config.plotOptions.bar.hideZeroBarsWhenGrouped) {
                        var _this_barHelpers_getZeroValueEncounters = this.barHelpers.getZeroValueEncounters({
                            i: i,
                            j: j
                        }), nonZeroColumns = _this_barHelpers_getZeroValueEncounters.nonZeroColumns, zeroEncounters = _this_barHelpers_getZeroValueEncounters.zeroEncounters;
                        if (nonZeroColumns > 0) {
                            barWidth = this.seriesLen * barWidth / nonZeroColumns;
                        }
                        barXPosition = x + barWidth * this.visibleI;
                        barXPosition -= barWidth * zeroEncounters;
                    } else {
                        barXPosition = x + barWidth * this.visibleI;
                    }
                }
                y = this.barHelpers.getYForValue(/** @type {any} */ this.series[i][j], zeroH, translationsIndex);
                var paths = /** @type {any} */ this.barHelpers.getColumnPaths({
                    barXPosition: barXPosition,
                    barWidth: barWidth,
                    y1: zeroH,
                    y2: y,
                    strokeWidth: strokeWidth,
                    isReversed: this.isReversed,
                    series: this.series,
                    realIndex: realIndex,
                    i: i,
                    j: j,
                    w: w
                });
                if (!w.axisFlags.isXNumeric) {
                    x = x + xDivision;
                }
                this.barHelpers.barBackground({
                    bc: bc,
                    j: j,
                    i: i,
                    x1: barXPosition - strokeWidth / 2 - barWidth * this.visibleI,
                    x2: barWidth * this.seriesLen + strokeWidth / 2,
                    elSeries: elSeries
                });
                return {
                    pathTo: paths.pathTo,
                    pathFrom: paths.pathFrom,
                    x: x,
                    y: y,
                    goalY: this.barHelpers.getGoalValues("y", /** @type {any} */ null, zeroH, realIndex, j, translationsIndex),
                    barXPosition: barXPosition,
                    barWidth: barWidth
                };
            }
        },
        {
            /** @param {{x: any, barWidth: any, realIndex: any, j: any}} opts */ key: "getBarXForNumericXAxis",
            value: function getBarXForNumericXAxis(param) {
                var x = param.x, barWidth = param.barWidth, realIndex = param.realIndex, j = param.j;
                var w = this.w;
                var sxI = realIndex;
                if (!w.seriesData.seriesX[realIndex].length) {
                    sxI = w.globals.maxValsInArrayIndex;
                }
                if (Utils$1.isNumber(w.seriesData.seriesX[sxI][j])) {
                    x = AxisMapping.dataXToPx(w, w.seriesData.seriesX[sxI][j]) - barWidth * this.seriesLen / 2;
                }
                return {
                    barXPosition: x + barWidth * this.visibleI,
                    x: x
                };
            }
        },
        {
            /**
   * The captured previous-render record for a series (last match wins, same
   * as the historical scan order).
   *
   * @param {number} realIndex
   * @returns {any | null}
   */ key: "_prevRecord",
            value: function _prevRecord(realIndex) {
                var w = this.w;
                var record = null;
                for(var pp = 0; pp < w.globals.previousPaths.length; pp++){
                    var gpp = w.globals.previousPaths[pp];
                    if (gpp.paths && gpp.paths.length > 0 && parseInt(gpp.realIndex, 10) === parseInt(String(realIndex), 10)) {
                        record = gpp;
                    }
                }
                return record;
            }
        },
        {
            /**
   * Previous paths of a series re-keyed by datum key (stamped as
   * `data:pathKey` on each bar path and captured by Series.getPreviousPaths).
   * Returns null when the previous render carries no keys (so the caller
   * falls back to positional matching).
   *
   * @param {number} realIndex
   * @returns {Map<string, {d: string}> | null}
   */ key: "_prevKeyedPaths",
            value: function _prevKeyedPaths(realIndex) {
                if (!this._prevKeyed) this._prevKeyed = {};
                if (this._prevKeyed[realIndex] !== void 0) {
                    return this._prevKeyed[realIndex];
                }
                var record = this._prevRecord(realIndex);
                var map = null;
                if (record && record.paths.every(function(p) {
                    return p.key != null;
                })) {
                    var keyed = /* @__PURE__ */ new Map();
                    record.paths.forEach(function(p) {
                        keyed.set(p.key, p);
                    });
                    map = keyed;
                }
                this._prevKeyed[realIndex] = map;
                return map;
            }
        },
        {
            /**
   * Whether this series' update changes its datum layout (points entered,
   * exited, or changed identity). Layout-changing updates run all survivors
   * on one shared clock (no per-bar stagger) so the reflow reads as a single
   * coordinated motion; pure value updates keep the stagger.
   *
   * @param {number} realIndex
   * @returns {boolean}
   */ key: "isLengthTransition",
            value: function isLengthTransition(realIndex) {
                var _a, _b;
                if (!this._ltCache) this._ltCache = {};
                if (this._ltCache[realIndex] !== void 0) return this._ltCache[realIndex];
                var w = this.w;
                var result = false;
                if (lengthTransitionEnabled(w) && w.globals.previousPaths.length > 0) {
                    var record = this._prevRecord(realIndex);
                    var dataLen = (_b = (_a = w.seriesData.series[realIndex]) == null ? void 0 : _a.length) != null ? _b : 0;
                    if (!record) {
                        result = dataLen > 0;
                    } else if (record.paths.length !== dataLen) {
                        result = true;
                    } else {
                        var keyed = this._prevKeyedPaths(realIndex);
                        if (keyed) {
                            for(var j = 0; j < dataLen; j++){
                                if (!keyed.has(datumKey(w, realIndex, j))) {
                                    result = true;
                                    break;
                                }
                            }
                        }
                    }
                }
                this._ltCache[realIndex] = result;
                return result;
            }
        },
        {
            /**
   * Whether this update moves survivors to new slots: a length change
   * (enter/exit, via isLengthTransition) OR a pure reorder (a "bar chart race"
   * swap, same datum set in a new order). Used to drop the per-bar stagger so
   * all bars slide on one shared clock, staying locked to the axis/data labels.
   * Broader than isLengthTransition, which is deliberately enter/exit-only
   * (exit ghosts / baseline enters must not fire on a plain reorder).
   * @param {number} realIndex
   */ key: "isLayoutShift",
            value: function isLayoutShift(realIndex) {
                if (this.isLengthTransition(realIndex)) return true;
                if (!this._layoutShiftCache) this._layoutShiftCache = {};
                if (this._layoutShiftCache[realIndex] !== void 0) {
                    return this._layoutShiftCache[realIndex];
                }
                var sj = seriesJoin(this.w, realIndex, true, true);
                var result = !!(sj && sj.join.changed);
                this._layoutShiftCache[realIndex] = result;
                return result;
            }
        },
        {
            /**
   * Resolve `pathFrom` for a bar on data update, joining old and new datums
   * by KEY (category label / x value) so survivors keep their identity across
   * inserts, prepends, and removals. Three outcomes:
   *
   *  - survivor with stable shape → the previous `d` (smooth reflow morph);
   *  - survivor whose command count changed (corner state flipped, e.g. bar
   *    became new top-of-stack) → `pathTo` (pathFrom === pathTo, a snap);
   *  - genuinely new datum (key absent from the previous render, or the whole
   *    series is new) → null, telling the path builder to use its
   *    grow-from-baseline enter path.
   *
   * Falls back to positional (index j) matching when the previous render
   * carries no datum keys.
   *
   * @param {number} realIndex - stable series index from `data:realIndex`
   * @param {number} j - data-point index within the series
   * @param {string} pathTo - the freshly-built path for this bar (post-roundPathCorners)
   * @returns {string | null}
   **/ key: "getPreviousPath",
            value: function getPreviousPath(realIndex, j, pathTo) {
                var w = this.w;
                var record = this._prevRecord(realIndex);
                if (!record) {
                    return lengthTransitionEnabled(w) ? null : pathTo;
                }
                var oldD = null;
                var isNewDatum = false;
                var keyed = this._prevKeyedPaths(realIndex);
                if (keyed) {
                    var prev = keyed.get(datumKey(w, realIndex, j));
                    if (prev) {
                        oldD = prev.d;
                    } else {
                        isNewDatum = true;
                    }
                } else if (typeof record.paths[j] !== "undefined") {
                    oldD = record.paths[j].d;
                } else {
                    isNewDatum = true;
                }
                if (oldD && Bar.pathCommandCount(oldD) === Bar.pathCommandCount(pathTo)) {
                    return oldD;
                }
                if (isNewDatum && lengthTransitionEnabled(w)) {
                    return null;
                }
                return pathTo;
            }
        }
    ], [
        {
            key: "pathCommandCount",
            value: /**
   * Count SVG path commands (M, L, C, Q, Z, etc.). Used to detect whether
   * two paths can be morphed safely — SVG.js requires matching command counts.
   *
   * @param {string} d
   * @returns {number}
   */ function pathCommandCount(d) {
                if (!d) return 0;
                var matches = d.match(/[A-Za-z]/g);
                return matches ? matches.length : 0;
            }
        }
    ]);
    return Bar;
}();
var BarStacked = /*#__PURE__*/ function(Bar1) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(BarStacked, Bar1);
    function BarStacked() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, BarStacked);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, BarStacked, arguments);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(BarStacked, [
        {
            /**
   * @param {any[]} series
   * @param {number} seriesIndex
   */ key: "draw",
            value: function draw(series, seriesIndex) {
                var _this, _loop = function(i, bc) {
                    var realIndex = w.globals.comboCharts ? /** @type {any} */ seriesIndex[i] : i;
                    var _this_barHelpers_getGroupIndex = _this.barHelpers.getGroupIndex(realIndex), groupIndex = _this_barHelpers_getGroupIndex.groupIndex, columnGroupIndex = _this_barHelpers_getGroupIndex.columnGroupIndex;
                    _this.groupCtx = /** @type {any} */ _this[/** @type {any} */ w.labelData.seriesGroups[groupIndex]];
                    var xArrValues = [];
                    var yArrValues = [];
                    var translationsIndex = 0;
                    if (_this.yRatio.length > 1) {
                        _this.yaxisIndex = /** @type {any} */ w.globals.seriesYAxisReverseMap[realIndex][0];
                        translationsIndex = realIndex;
                    }
                    _this.isReversed = w.config.yaxis[_this.yaxisIndex] && w.config.yaxis[_this.yaxisIndex].reversed;
                    var elSeries = _this.graphics.group({
                        class: "apexcharts-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex]),
                        rel: i + 1,
                        "data:realIndex": realIndex
                    });
                    Series.addCollapsedClassToSeries(_this.w, elSeries, realIndex);
                    var elDataLabelsWrap = _this.graphics.group({
                        class: "apexcharts-datalabels",
                        "data:realIndex": realIndex
                    });
                    var elGoalsMarkers = _this.graphics.group({
                        class: "apexcharts-bar-goals-markers"
                    });
                    var initPositions = _this.initialPositions(x, y, void 0, void 0, void 0, void 0, translationsIndex);
                    var xDivision = initPositions.xDivision, // xDivision is the GRIDWIDTH divided by number of datapoints (columns)
                    yDivision = initPositions.yDivision, // yDivision is the GRIDHEIGHT divided by number of datapoints (bars)
                    zeroH = initPositions.zeroH, // zeroH is the baseline where 0 meets y axis
                    zeroW = initPositions.zeroW;
                    var barHeight = initPositions.barHeight;
                    var barWidth = initPositions.barWidth;
                    y = initPositions.y;
                    x = initPositions.x;
                    w.globals.barHeight = barHeight;
                    w.globals.barWidth = barWidth;
                    _this.barHelpers.initializeStackedXYVars(_this);
                    if (_this.groupCtx.prevY.length === 1 && /**
       * @param {number} val
       */ _this.groupCtx.prevY[0].every(function(val) {
                        return isNaN(val);
                    })) {
                        _this.groupCtx.prevY[0] = _this.groupCtx.prevY[0].map(function() {
                            return zeroH;
                        });
                        _this.groupCtx.prevYF[0] = _this.groupCtx.prevYF[0].map(function() {
                            return 0;
                        });
                    }
                    for(var j = 0; j < w.globals.dataPoints; j++){
                        var strokeWidth = _this.barHelpers.getStrokeWidth(i, j, realIndex);
                        var commonPathOpts = {
                            indexes: {
                                i: i,
                                j: j,
                                realIndex: realIndex,
                                translationsIndex: translationsIndex,
                                bc: bc
                            },
                            strokeWidth: strokeWidth,
                            x: x,
                            y: y,
                            elSeries: elSeries,
                            columnGroupIndex: columnGroupIndex,
                            seriesGroup: w.labelData.seriesGroups[groupIndex]
                        };
                        var paths = /** @type {any} */ null;
                        if (_this.isHorizontal) {
                            paths = _this.drawStackedBarPaths(__spreadProps(__spreadValues({}, commonPathOpts), {
                                zeroW: zeroW,
                                barHeight: barHeight,
                                yDivision: yDivision
                            }));
                            barWidth = _this.series[i][j] / _this.invertedYRatio;
                        } else {
                            paths = _this.drawStackedColumnPaths(__spreadProps(__spreadValues({}, commonPathOpts), {
                                xDivision: xDivision,
                                barWidth: barWidth,
                                zeroH: zeroH
                            }));
                            barHeight = _this.series[i][j] / _this.yRatio[translationsIndex];
                        }
                        var barGoalLine = _this.barHelpers.drawGoalLine({
                            barXPosition: paths.barXPosition,
                            barYPosition: paths.barYPosition,
                            goalX: paths.goalX,
                            goalY: paths.goalY,
                            barHeight: barHeight,
                            barWidth: barWidth
                        });
                        if (barGoalLine) {
                            elGoalsMarkers.add(barGoalLine);
                        }
                        y = paths.y;
                        x = paths.x;
                        xArrValues.push(x);
                        yArrValues.push(y);
                        var pathFill = _this.barHelpers.getPathFillColor(series, i, j, realIndex);
                        var classes = "";
                        var flipClass = w.globals.isBarHorizontal ? "apexcharts-flip-x" : "apexcharts-flip-y";
                        if (_this.barHelpers.arrBorderRadius[realIndex][j] === "bottom" && w.seriesData.series[realIndex][j] > 0 || _this.barHelpers.arrBorderRadius[realIndex][j] === "top" && w.seriesData.series[realIndex][j] < 0) {
                            classes = flipClass;
                        }
                        elSeries = _this.renderSeries(__spreadProps(__spreadValues({
                            realIndex: realIndex,
                            pathFill: pathFill.color
                        }, pathFill.useRangeColor ? {
                            lineFill: pathFill.color
                        } : {}), {
                            j: j,
                            i: i,
                            columnGroupIndex: columnGroupIndex,
                            pathFrom: paths.pathFrom,
                            pathTo: paths.pathTo,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries,
                            x: x,
                            y: y,
                            series: series,
                            barHeight: barHeight,
                            barWidth: barWidth,
                            elDataLabelsWrap: elDataLabelsWrap,
                            elGoalsMarkers: elGoalsMarkers,
                            type: "bar",
                            visibleSeries: columnGroupIndex,
                            classes: classes
                        }));
                    }
                    w.globals.seriesXvalues[realIndex] = xArrValues;
                    w.globals.seriesYvalues[realIndex] = yArrValues;
                    _this.groupCtx.prevY.push(_this.groupCtx.yArrj);
                    _this.groupCtx.prevYF.push(_this.groupCtx.yArrjF);
                    _this.groupCtx.prevYVal.push(_this.groupCtx.yArrjVal);
                    _this.groupCtx.prevX.push(_this.groupCtx.xArrj);
                    _this.groupCtx.prevXF.push(_this.groupCtx.xArrjF);
                    _this.groupCtx.prevXVal.push(_this.groupCtx.xArrjVal);
                    ret.add(elSeries);
                };
                var w = this.w;
                this.graphics = new Graphics(this.w);
                this.bar = new Bar(this.w, this.ctx, this.xyRatios);
                var coreUtils = new CoreUtils(this.w);
                series = coreUtils.getLogSeries(series);
                this.yRatio = coreUtils.getLogYRatios(this.yRatio);
                this.barHelpers.initVariables(series);
                if (w.config.chart.stackType === "100%") {
                    series = w.globals.comboCharts ? /** @type {any} */ seriesIndex.map(function(_) {
                        return w.globals.seriesPercent[_];
                    }) : w.globals.seriesPercent.slice();
                }
                this.series = series;
                this.barHelpers.initializeStackedPrevVars(this);
                var ret = this.graphics.group({
                    class: "apexcharts-bar-series apexcharts-plot-series"
                });
                var x = 0;
                var y = 0;
                for(var i = 0, bc = 0; i < series.length; i++, bc++)_this = this, _loop(i, bc);
                return ret;
            }
        },
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {number | undefined} xDivision
   * @param {number | undefined} yDivision
   * @param {number | undefined} zeroH
   * @param {number | undefined} zeroW
   * @param {number} translationsIndex
   */ key: "initialPositions",
            value: function initialPositions(x, y, xDivision, yDivision, zeroH, zeroW, translationsIndex) {
                var w = this.w;
                var barHeight, barWidth;
                if (this.isHorizontal) {
                    yDivision = w.layout.gridHeight / w.globals.dataPoints;
                    var userBarHeight = w.config.plotOptions.bar.barHeight;
                    if (String(userBarHeight).indexOf("%") === -1) {
                        barHeight = parseInt(userBarHeight, 10);
                    } else {
                        barHeight = yDivision * parseInt(userBarHeight, 10) / 100;
                    }
                    zeroW = w.globals.padHorizontal + (this.isReversed ? w.layout.gridWidth - this.baseLineInvertedY : this.baseLineInvertedY);
                    y = (yDivision - barHeight) / 2;
                } else {
                    xDivision = w.layout.gridWidth / w.globals.dataPoints;
                    barWidth = xDivision;
                    var userColumnWidth = w.config.plotOptions.bar.columnWidth;
                    if (w.axisFlags.isXNumeric && w.globals.dataPoints > 1) {
                        xDivision = w.globals.minXDiff / this.xRatio;
                        barWidth = xDivision * parseInt(this.barOptions.columnWidth, 10) / 100;
                    } else if (String(userColumnWidth).indexOf("%") === -1) {
                        barWidth = parseInt(userColumnWidth, 10);
                    } else {
                        barWidth *= parseInt(userColumnWidth, 10) / 100;
                    }
                    if (this.isReversed) {
                        zeroH = this.baseLineY[translationsIndex];
                    } else {
                        zeroH = w.layout.gridHeight - this.baseLineY[translationsIndex];
                    }
                    x = w.globals.padHorizontal + (xDivision - barWidth) / 2;
                }
                var subDivisions = w.globals.barGroups.length || 1;
                return {
                    x: x,
                    y: y,
                    yDivision: yDivision,
                    xDivision: xDivision,
                    barHeight: (barHeight != null ? barHeight : 0) / subDivisions,
                    barWidth: (barWidth != null ? barWidth : 0) / subDivisions,
                    zeroH: zeroH,
                    zeroW: zeroW
                };
            }
        },
        {
            /** @param {{indexes: any, barHeight: any, strokeWidth: any, zeroW: any, x: any, y: any, columnGroupIndex: any, seriesGroup: any, yDivision: any, elSeries: any}} opts */ key: "drawStackedBarPaths",
            value: function drawStackedBarPaths(param) {
                var indexes = param.indexes, barHeight = param.barHeight, strokeWidth = param.strokeWidth, zeroW = param.zeroW, x = param.x, y = param.y, columnGroupIndex = param.columnGroupIndex, seriesGroup = param.seriesGroup, yDivision = param.yDivision, elSeries = param.elSeries;
                var _a, _b, _c, _d, _e;
                var w = this.w;
                var barYPosition = y + columnGroupIndex * barHeight;
                var barXPosition;
                var i = indexes.i;
                var j = indexes.j;
                var realIndex = indexes.realIndex;
                var translationsIndex = indexes.translationsIndex;
                var prevBarW = 0;
                for(var k = 0; k < this.groupCtx.prevXF.length; k++){
                    prevBarW = prevBarW + this.groupCtx.prevXF[k][j];
                }
                var gsi = i;
                if (/** @type {Record<string,any>} */ w.config.series[realIndex].name) {
                    gsi = seriesGroup.indexOf(/** @type {Record<string,any>} */ w.config.series[realIndex].name);
                }
                if (gsi > 0) {
                    var bXP = zeroW;
                    if (this.groupCtx.prevXVal[gsi - 1][j] < 0) {
                        bXP = /** @type {any} */ ((_a = this.series[i]) == null ? void 0 : _a[j]) >= 0 ? this.groupCtx.prevX[gsi - 1][j] + prevBarW - (this.isReversed ? prevBarW : 0) * 2 : this.groupCtx.prevX[gsi - 1][j];
                    } else if (this.groupCtx.prevXVal[gsi - 1][j] >= 0) {
                        bXP = /** @type {any} */ ((_b = this.series[i]) == null ? void 0 : _b[j]) >= 0 ? this.groupCtx.prevX[gsi - 1][j] : this.groupCtx.prevX[gsi - 1][j] - prevBarW + (this.isReversed ? prevBarW : 0) * 2;
                    }
                    barXPosition = bXP;
                } else {
                    barXPosition = zeroW;
                }
                if (/** @type {any} */ ((_c = this.series[i]) == null ? void 0 : _c[j]) === null) {
                    x = barXPosition;
                } else {
                    x = barXPosition + /** @type {any} */ ((_d = this.series[i]) == null ? void 0 : _d[j]) / this.invertedYRatio - (this.isReversed ? /** @type {any} */ ((_e = this.series[i]) == null ? void 0 : _e[j]) / this.invertedYRatio : 0) * 2;
                }
                var paths = this.barHelpers.getBarpaths({
                    barYPosition: barYPosition,
                    barHeight: barHeight,
                    x1: barXPosition,
                    x2: x,
                    strokeWidth: strokeWidth,
                    isReversed: this.isReversed,
                    series: this.series,
                    realIndex: indexes.realIndex,
                    seriesGroup: seriesGroup,
                    i: i,
                    j: j,
                    w: w
                });
                this.barHelpers.barBackground({
                    j: j,
                    i: i,
                    y1: barYPosition,
                    y2: barHeight,
                    elSeries: elSeries
                });
                y = y + yDivision;
                return {
                    pathTo: paths.pathTo,
                    pathFrom: paths.pathFrom,
                    goalX: this.barHelpers.getGoalValues("x", zeroW, /** @type {any} */ null, realIndex, j, translationsIndex),
                    barXPosition: barXPosition,
                    barYPosition: barYPosition,
                    x: x,
                    y: y
                };
            }
        },
        {
            /** @param {{indexes: any, x: any, y: any, xDivision: any, barWidth: any, zeroH: any, columnGroupIndex: any, seriesGroup: any, elSeries: any}} opts */ key: "drawStackedColumnPaths",
            value: function drawStackedColumnPaths(param) {
                var indexes = param.indexes, x = param.x, y = param.y, xDivision = param.xDivision, barWidth = param.barWidth, zeroH = param.zeroH, columnGroupIndex = param.columnGroupIndex, seriesGroup = param.seriesGroup, elSeries = param.elSeries;
                var _a, _b, _c, _d, _e, _f, _g, _h, _i;
                var w = this.w;
                var i = indexes.i;
                var j = indexes.j;
                var bc = indexes.bc;
                var realIndex = indexes.realIndex;
                var translationsIndex = indexes.translationsIndex;
                if (w.axisFlags.isXNumeric) {
                    var seriesVal = w.seriesData.seriesX[realIndex][j];
                    if (!seriesVal) seriesVal = 0;
                    x = (seriesVal - w.globals.minX) / this.xRatio - barWidth / 2 * w.globals.barGroups.length;
                }
                var barXPosition = x + columnGroupIndex * barWidth;
                var barYPosition;
                var prevBarH = 0;
                for(var k = 0; k < this.groupCtx.prevYF.length; k++){
                    prevBarH = prevBarH + (!isNaN(this.groupCtx.prevYF[k][j]) ? this.groupCtx.prevYF[k][j] : 0);
                }
                var gsi = i;
                if (seriesGroup) {
                    gsi = seriesGroup.indexOf(w.seriesData.seriesNames[realIndex]);
                }
                if (gsi > 0 && !w.axisFlags.isXNumeric || gsi > 0 && w.axisFlags.isXNumeric && w.seriesData.seriesX[realIndex - 1][j] === w.seriesData.seriesX[realIndex][j]) {
                    var bYP;
                    var prevYValue;
                    var p = Math.min(this.yRatio.length + 1, realIndex + 1);
                    if (this.groupCtx.prevY[gsi - 1] !== void 0 && this.groupCtx.prevY[gsi - 1].length) {
                        for(var ii = 1; ii < p; ii++){
                            if (!isNaN((_a = this.groupCtx.prevY[gsi - ii]) == null ? void 0 : _a[j])) {
                                prevYValue = this.groupCtx.prevY[gsi - ii][j];
                                break;
                            }
                        }
                    }
                    for(var ii1 = 1; ii1 < p; ii1++){
                        if (((_b = this.groupCtx.prevYVal[gsi - ii1]) == null ? void 0 : _b[j]) < 0) {
                            bYP = /** @type {any} */ ((_c = this.series[i]) == null ? void 0 : _c[j]) >= 0 ? prevYValue - prevBarH + (this.isReversed ? prevBarH : 0) * 2 : prevYValue;
                            break;
                        } else if (((_d = this.groupCtx.prevYVal[gsi - ii1]) == null ? void 0 : _d[j]) >= 0) {
                            bYP = /** @type {any} */ ((_e = this.series[i]) == null ? void 0 : _e[j]) >= 0 ? prevYValue : prevYValue + prevBarH - (this.isReversed ? prevBarH : 0) * 2;
                            break;
                        }
                    }
                    if (typeof bYP === "undefined") bYP = w.layout.gridHeight;
                    if (/**
         * @param {number} val
         */ ((_f = this.groupCtx.prevYF[0]) == null ? void 0 : _f.every(function(val) {
                        return val === 0;
                    })) && this.groupCtx.prevYF.slice(1, gsi).every(function(arr) {
                        return arr.every(function(val) {
                            return isNaN(val);
                        });
                    })) {
                        barYPosition = zeroH;
                    } else {
                        barYPosition = bYP;
                    }
                } else {
                    barYPosition = zeroH;
                }
                if (/** @type {any} */ (_g = this.series[i]) == null ? void 0 : _g[j]) {
                    y = barYPosition - /** @type {any} */ ((_h = this.series[i]) == null ? void 0 : _h[j]) / this.yRatio[translationsIndex] + (this.isReversed ? /** @type {any} */ ((_i = this.series[i]) == null ? void 0 : _i[j]) / this.yRatio[translationsIndex] : 0) * 2;
                } else {
                    y = barYPosition;
                }
                var paths = this.barHelpers.getColumnPaths({
                    barXPosition: barXPosition,
                    barWidth: barWidth,
                    y1: barYPosition,
                    y2: y,
                    yRatio: this.yRatio[translationsIndex],
                    strokeWidth: this.strokeWidth,
                    isReversed: this.isReversed,
                    series: this.series,
                    seriesGroup: seriesGroup,
                    realIndex: indexes.realIndex,
                    i: i,
                    j: j,
                    w: w
                });
                this.barHelpers.barBackground({
                    bc: bc,
                    j: j,
                    i: i,
                    x1: barXPosition,
                    x2: barWidth,
                    elSeries: elSeries
                });
                return {
                    pathTo: paths.pathTo,
                    pathFrom: paths.pathFrom,
                    goalY: this.barHelpers.getGoalValues("y", /** @type {any} */ null, zeroH, realIndex, j, 0),
                    barXPosition: barXPosition,
                    x: w.axisFlags.isXNumeric ? x : x + xDivision,
                    y: y
                };
            }
        }
    ]);
    return BarStacked;
}(Bar);
function buildJitterGroups(param) {
    var w = param.w, points = param.points, seedA = param.seedA, seedB = param.seedB, center = param.center, halfExtent = param.halfExtent, alongFn = param.alongFn, isHorizontal = param.isHorizontal, options2 = param.options, clampAt = param.clampAt;
    var opts = options2;
    if (!opts || opts.show === false) return [];
    if (!points || !points.length) return [];
    var maxPoints = opts.maxPoints || 3e3;
    var stride = points.length > maxPoints ? Math.ceil(points.length / maxPoints) : 1;
    var r = opts.size != null ? opts.size : 2.5;
    var jitterFrac = opts.jitter != null ? opts.jitter : 0.5;
    var jitterPx = halfExtent * jitterFrac;
    var constrain = opts.constrainToViolin !== false && typeof clampAt === "function";
    var isSquare = opts.shape === "square";
    var scale = opts.colorScale;
    var useScale = scale && Array.isArray(scale.colors) && scale.colors.length > 0;
    var steps = useScale ? Math.max(2, scale.steps || 24) : 1;
    var sMin = useScale && scale.min != null ? scale.min : w.globals.minY;
    var sMax = useScale && scale.max != null ? scale.max : w.globals.maxY;
    var span = sMax - sMin || 1;
    var buckets = useScale ? new Array(steps).fill("") : [
        ""
    ];
    for(var k = 0; k < points.length; k += stride){
        var v = points[k];
        var a = alongFn(v);
        var off = (hash01(seedA * 7919 + seedB * 100003 + k) - 0.5) * 2 * jitterPx;
        if (constrain) {
            var cap = /** @type {(v:number)=>number} */ clampAt(v);
            if (off > cap) off = cap;
            if (off < -cap) off = -cap;
        }
        var px = isHorizontal ? a : center + off;
        var py = isHorizontal ? center + off : a;
        var sub = isSquare ? squareSubPath(px, py, r) : circleSubPath(px, py, r);
        if (useScale) {
            var t = (v - sMin) / span;
            if (t < 0) t = 0;
            if (t > 1) t = 1;
            buckets[Math.round(t * (steps - 1))] += sub;
        } else {
            buckets[0] += sub;
        }
    }
    if (!useScale) {
        return buckets[0] ? [
            {
                fill: null,
                d: buckets[0]
            }
        ] : [];
    }
    var groups = [];
    for(var b = 0; b < steps; b++){
        if (!buckets[b]) continue;
        groups.push({
            fill: rampColorAt(scale.colors, b / (steps - 1)),
            d: buckets[b]
        });
    }
    return groups;
}
function renderJitter(param) {
    var graphics = param.graphics, w = param.w, elSeries = param.elSeries, pointsByCat = param.pointsByCat, options2 = param.options, distributed = param.distributed, realIndex = param.realIndex, wrapClass = param.wrapClass, pointClass = param.pointClass;
    if (!options2 || options2.show === false || !pointsByCat.length) return;
    var pOpacity = options2.opacity != null ? options2.opacity : 0.9;
    var strokeColor = options2.strokeColor != null ? options2.strokeColor : "#fff";
    var strokeW = options2.strokeWidth != null ? options2.strokeWidth : 1;
    var willAnimateIn = w.config.chart.animations.enabled && !w.globals.resized && !w.globals.dataChanged;
    var elPointsWrap = graphics.group({
        class: willAnimateIn ? "".concat(wrapClass, " apexcharts-element-hidden") : wrapClass
    });
    if (willAnimateIn) {
        w.globals.delayedElements.push({
            el: elPointsWrap.node
        });
    }
    pointsByCat.forEach(function(param) {
        var groups = param.groups, j = param.j;
        var catColor = distributed ? w.globals.colors[j] : w.globals.colors[realIndex];
        var fc = options2.fillColor;
        var defaultFill = fc === "series" ? catColor : fc === "series-dark" ? darkenColor(catColor, 0.45) : fc || darkenColor(catColor, 0.45);
        groups.forEach(function(g) {
            var elPoints = graphics.drawPath({
                d: g.d,
                fill: g.fill != null ? g.fill : defaultFill,
                stroke: strokeW > 0 ? strokeColor : "none",
                strokeWidth: strokeW,
                fillOpacity: pOpacity,
                classes: pointClass
            });
            elPoints.attr("data:realIndex", realIndex);
            elPoints.attr("j", j);
            elPoints.attr("clip-path", "url(#gridRectBarMask".concat(w.globals.cuid, ")"));
            elPoints.node.style.pointerEvents = "none";
            elPointsWrap.add(elPoints);
        });
    });
    elSeries.add(elPointsWrap);
}
function darkenColor(color, amount) {
    var rgb = Utils$1.parseHex(color);
    if (!rgb) return color;
    var f = Math.max(0, 1 - amount);
    return "rgb(".concat(Math.round(rgb[0] * f), ",").concat(Math.round(rgb[1] * f), ",").concat(Math.round(rgb[2] * f), ")");
}
function rampColorAt(colors, t) {
    if (!colors.length) return "#000";
    if (colors.length === 1) return colors[0];
    var x = Math.max(0, Math.min(1, t)) * (colors.length - 1);
    var i = Math.floor(x);
    var frac = x - i;
    var c0 = Utils$1.parseHex(colors[i]) || [
        0,
        0,
        0
    ];
    var c1 = Utils$1.parseHex(colors[Math.min(i + 1, colors.length - 1)]) || c0;
    var mix = function mix(a, b) {
        return Math.round(a + (b - a) * frac);
    };
    return "rgb(".concat(mix(c0[0], c1[0]), ",").concat(mix(c0[1], c1[1]), ",").concat(mix(c0[2], c1[2]), ")");
}
function hash01(n) {
    var h = (n ^ 2654435769) >>> 0;
    h = Math.imul(h ^ h >>> 16, 73244475);
    h = Math.imul(h ^ h >>> 16, 73244475);
    return ((h ^ h >>> 16) >>> 0) / 4294967296;
}
function circleSubPath(px, py, r) {
    return "M ".concat(px - r, " ").concat(py, " a ").concat(r, " ").concat(r, " 0 1 0 ").concat(2 * r, " 0 a ").concat(r, " ").concat(r, " 0 1 0 ").concat(-2 * r, " 0 ");
}
function squareSubPath(px, py, r) {
    return "M ".concat(px - r, " ").concat(py - r, " h ").concat(2 * r, " v ").concat(2 * r, " h ").concat(-2 * r, " z ");
}
var BoxCandleStick = /*#__PURE__*/ function(Bar) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(BoxCandleStick, Bar);
    function BoxCandleStick() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, BoxCandleStick);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, BoxCandleStick, arguments);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(BoxCandleStick, [
        {
            /**
   * @param {any[]} series
   * @param {string} ctype
   * @param {number} seriesIndex
   */ // @ts-ignore -- BoxCandleStick.draw has an extra ctype param compared to Bar.draw
            key: "draw",
            value: function draw(series, ctype, seriesIndex) {
                var _this, _loop = function(i) {
                    var _this2, _loop = function(j) {
                        var strokeWidth = _this2.barHelpers.getStrokeWidth(i, j, realIndex);
                        var paths = /** @type {any} */ null;
                        var pathsParams = {
                            indexes: {
                                i: i,
                                j: j,
                                realIndex: realIndex,
                                translationsIndex: translationsIndex
                            },
                            x: x,
                            y: y,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries
                        };
                        if (_this2.isHorizontal) {
                            paths = _this2.drawHorizontalBoxPaths(__spreadProps(__spreadValues({}, pathsParams), {
                                yDivision: yDivision,
                                barHeight: barHeight,
                                zeroW: zeroW
                            }));
                        } else {
                            paths = _this2.drawVerticalBoxPaths(__spreadProps(__spreadValues({}, pathsParams), {
                                xDivision: xDivision,
                                barWidth: barWidth,
                                zeroH: zeroH,
                                cullBounds: {
                                    lo: -cullBuffer,
                                    hi: gridW + cullBuffer
                                }
                            }));
                        }
                        y = paths.y;
                        x = paths.x;
                        if (j > 0) {
                            xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
                        }
                        yArrj.push(y);
                        if (paths.culled) {
                            return "continue";
                        }
                        var barGoalLine = _this2.barHelpers.drawGoalLine({
                            barXPosition: paths.barXPosition,
                            barYPosition: paths.barYPosition,
                            goalX: paths.goalX,
                            goalY: paths.goalY,
                            barHeight: barHeight,
                            barWidth: barWidth
                        });
                        if (barGoalLine) {
                            elGoalsMarkers.add(barGoalLine);
                        }
                        paths.pathTo.forEach(function(pathTo, pi) {
                            var lineFill = !_this1.isBoxPlot && _this1.candlestickOptions.wick.useFillColor ? paths.color[pi] : w.globals.stroke.colors[i];
                            var pathFill = fill.fillPath({
                                seriesNumber: realIndex,
                                dataPointIndex: j,
                                color: paths.color[pi],
                                value: series[i][j]
                            });
                            _this1.renderSeries({
                                realIndex: realIndex,
                                pathFill: pathFill,
                                lineFill: lineFill,
                                j: j,
                                i: i,
                                pathFrom: paths.pathFrom,
                                pathTo: pathTo,
                                strokeWidth: strokeWidth,
                                elSeries: elSeries,
                                x: x,
                                y: y,
                                series: series,
                                columnGroupIndex: columnGroupIndex,
                                barHeight: barHeight,
                                barWidth: barWidth,
                                elDataLabelsWrap: elDataLabelsWrap,
                                elGoalsMarkers: elGoalsMarkers,
                                visibleSeries: _this1.visibleI,
                                type: w.config.chart.type
                            });
                        });
                        if (boxPointsOpts && boxPointsOpts.show !== false) {
                            var pts = (_a = w.candleData.seriesBoxPoints[realIndex]) == null ? void 0 : _a[j];
                            if (pts && pts.length) {
                                var logVal = function logVal(v) {
                                    return /** @type {any} */ _this1.coreUtils.getLogValAtSeriesIndex(v, realIndex);
                                };
                                var center = void 0, halfExtent = void 0, alongFn = void 0;
                                if (_this2.isHorizontal) {
                                    var yRatio = _this2.invertedYRatio;
                                    var bh = barHeight != null ? barHeight : 0;
                                    var z = zeroW != null ? zeroW : 0;
                                    center = paths.barYPosition + bh / 2;
                                    halfExtent = bh / 2;
                                    alongFn = function alongFn(v) {
                                        return z + logVal(v) / yRatio;
                                    };
                                } else {
                                    var yRatio1 = _this2.yRatio[translationsIndex];
                                    var bw = barWidth != null ? barWidth : 0;
                                    var z1 = zeroH != null ? zeroH : 0;
                                    center = paths.barXPosition + bw / 2;
                                    halfExtent = bw / 2;
                                    alongFn = function alongFn(v) {
                                        return z1 - logVal(v) / yRatio1;
                                    };
                                }
                                var groups = buildJitterGroups({
                                    w: w,
                                    points: pts,
                                    seedA: realIndex,
                                    seedB: j,
                                    center: center,
                                    halfExtent: halfExtent,
                                    alongFn: alongFn,
                                    isHorizontal: _this2.isHorizontal,
                                    options: boxPointsOpts
                                });
                                if (groups.length) pointsByCat.push({
                                    groups: groups,
                                    j: j
                                });
                            }
                        }
                    };
                    _this.isBoxPlot = w.config.chart.type === "boxPlot" || /** @type {Record<string,any>} */ w.config.series[i].type === "boxPlot";
                    var x = void 0;
                    var y = void 0;
                    var yArrj = [];
                    var xArrj = [];
                    var realIndex = w.globals.comboCharts ? /** @type {any} */ seriesIndex[i] : i;
                    var columnGroupIndex = _this.barHelpers.getGroupIndex(realIndex).columnGroupIndex;
                    var elSeries = graphics.group({
                        class: "apexcharts-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex]),
                        rel: i + 1,
                        "data:realIndex": realIndex
                    });
                    Series.addCollapsedClassToSeries(_this.w, elSeries, realIndex);
                    if (series[i].length > 0) {
                        _this.visibleI = _this.visibleI + 1;
                    }
                    var translationsIndex = 0;
                    if (_this.yRatio.length > 1) {
                        _this.yaxisIndex = /** @type {any} */ w.globals.seriesYAxisReverseMap[realIndex][0];
                        translationsIndex = realIndex;
                    }
                    var initPositions = _this.barHelpers.initialPositions(realIndex);
                    var initY = initPositions.y, barHeight = initPositions.barHeight, yDivision = initPositions.yDivision, // yDivision is the GRIDHEIGHT divided by number of datapoints (bars)
                    zeroW = initPositions.zeroW, // zeroW is the baseline where 0 meets x axis
                    initX = initPositions.x, barWidth = initPositions.barWidth, xDivision = initPositions.xDivision, // xDivision is the GRIDWIDTH divided by number of datapoints (columns)
                    zeroH = initPositions.zeroH;
                    y = initY;
                    x = initX;
                    xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
                    var elDataLabelsWrap = graphics.group({
                        class: "apexcharts-datalabels",
                        "data:realIndex": realIndex
                    });
                    var elGoalsMarkers = graphics.group({
                        class: "apexcharts-bar-goals-markers"
                    });
                    var boxPointsOpts = _this.isBoxPlot ? _this.boxOptions.points : null;
                    var pointsByCat = [];
                    var gridW = w.layout.gridWidth;
                    var cullBuffer = barWidth != null ? barWidth : 0;
                    for(var j = 0; j < w.globals.dataPoints; j++)_this2 = _this, _loop(j);
                    if (boxPointsOpts) {
                        renderJitter({
                            graphics: graphics,
                            w: w,
                            elSeries: elSeries,
                            pointsByCat: pointsByCat,
                            options: boxPointsOpts,
                            distributed: w.config.plotOptions.bar.distributed,
                            realIndex: realIndex,
                            wrapClass: "apexcharts-boxPlot-points-wrap",
                            pointClass: "apexcharts-boxPlot-points"
                        });
                    }
                    w.globals.seriesXvalues[realIndex] = xArrj;
                    w.globals.seriesYvalues[realIndex] = yArrj;
                    ret.add(elSeries);
                };
                var _this1 = this;
                var _a;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var type = w.globals.comboCharts ? ctype : w.config.chart.type;
                var fill = new Fill(this.w);
                this.candlestickOptions = this.w.config.plotOptions.candlestick;
                this.boxOptions = this.w.config.plotOptions.boxPlot;
                this.isHorizontal = w.config.plotOptions.bar.horizontal;
                this.isOHLC = this.candlestickOptions && this.candlestickOptions.type === "ohlc";
                this.coreUtils = new CoreUtils(this.w);
                series = this.coreUtils.getLogSeries(series);
                this.series = series;
                this.yRatio = this.coreUtils.getLogYRatios(this.yRatio);
                this.barHelpers.initVariables(series);
                var ret = graphics.group({
                    class: "apexcharts-".concat(type, "-series apexcharts-plot-series")
                });
                for(var i = 0; i < series.length; i++)_this = this, _loop(i);
                return ret;
            }
        },
        {
            /** @param {{indexes: any, x: any, xDivision: any, barWidth: any, zeroH: any, strokeWidth: any, cullBounds?: {lo: number, hi: number}|null}} opts */ key: "drawVerticalBoxPaths",
            value: function drawVerticalBoxPaths(param) {
                var indexes = param.indexes, x = param.x, xDivision = param.xDivision, barWidth = param.barWidth, zeroH = param.zeroH, strokeWidth = param.strokeWidth, _param_cullBounds = param.cullBounds, cullBounds = _param_cullBounds === void 0 ? null : _param_cullBounds;
                var _a, _b;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var i = indexes.i;
                var j = indexes.j;
                var _w_config_plotOptions_candlestick = w.config.plotOptions.candlestick, candleColors = _w_config_plotOptions_candlestick.colors;
                var _this_boxOptions = this.boxOptions, boxColors = _this_boxOptions.colors;
                var realIndex = indexes.realIndex;
                var getColor = function getColor(color2) {
                    return Array.isArray(color2) ? color2[realIndex] : color2;
                };
                var colorPos = getColor(candleColors.upward);
                var colorNeg = getColor(candleColors.downward);
                var yRatio = this.yRatio[indexes.translationsIndex];
                var ohlc = this.getOHLCValue(realIndex, j);
                var l1 = zeroH;
                var l2 = zeroH;
                var color = ohlc.o < ohlc.c ? [
                    colorPos
                ] : [
                    colorNeg
                ];
                if (this.isBoxPlot) {
                    color = [
                        getColor(boxColors.lower),
                        getColor(boxColors.upper)
                    ];
                }
                var y1 = Math.min(ohlc.o, ohlc.c);
                var y2 = Math.max(ohlc.o, ohlc.c);
                var m = ohlc.m;
                if (w.axisFlags.isXNumeric) {
                    x = (w.seriesData.seriesX[realIndex][j] - w.globals.minX) / this.xRatio - barWidth / 2;
                }
                var barXPosition = x + barWidth * this.visibleI;
                if (typeof /** @type {any} */ ((_a = this.series[i]) == null ? void 0 : _a[j]) === "undefined" || /** @type {any} */ ((_b = this.series[i]) == null ? void 0 : _b[j]) === null) {
                    y1 = zeroH;
                    y2 = zeroH;
                } else {
                    y1 = zeroH - y1 / yRatio;
                    y2 = zeroH - y2 / yRatio;
                    l1 = zeroH - ohlc.h / yRatio;
                    l2 = zeroH - ohlc.l / yRatio;
                    m = zeroH - ohlc.m / yRatio;
                }
                if (cullBounds && (barXPosition + barWidth < cullBounds.lo || barXPosition > cullBounds.hi)) {
                    return {
                        pathTo: null,
                        pathFrom: null,
                        x: w.axisFlags.isXNumeric ? x : x + xDivision,
                        y: y2,
                        barXPosition: barXPosition,
                        color: color,
                        culled: true
                    };
                }
                var pathTo;
                if (this.isOHLC) {
                    var centerX = barXPosition + barWidth / 2;
                    var openY = zeroH - ohlc.o / yRatio;
                    var closeY = zeroH - ohlc.c / yRatio;
                    pathTo = [
                        graphics.move(centerX, l1) + graphics.line(centerX, l2) + graphics.move(centerX, openY) + graphics.line(barXPosition, openY) + graphics.move(centerX, closeY) + graphics.line(barXPosition + barWidth, closeY)
                    ];
                } else if (this.isBoxPlot) {
                    pathTo = [
                        graphics.move(barXPosition, y1) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition + barWidth / 2, l1) + graphics.line(barXPosition + barWidth / 4, l1) + graphics.line(barXPosition + barWidth - barWidth / 4, l1) + graphics.line(barXPosition + barWidth / 2, l1) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition + barWidth, y1) + graphics.line(barXPosition + barWidth, m) + graphics.line(barXPosition, m) + graphics.line(barXPosition, y1 + strokeWidth / 2),
                        graphics.move(barXPosition, m) + graphics.line(barXPosition + barWidth, m) + graphics.line(barXPosition + barWidth, y2) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition + barWidth / 2, l2) + graphics.line(barXPosition + barWidth - barWidth / 4, l2) + graphics.line(barXPosition + barWidth / 4, l2) + graphics.line(barXPosition + barWidth / 2, l2) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition, y2) + graphics.line(barXPosition, m) + "z"
                    ];
                } else {
                    pathTo = [
                        graphics.move(barXPosition, y2) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition + barWidth / 2, l1) + graphics.line(barXPosition + barWidth / 2, y2) + graphics.line(barXPosition + barWidth, y2) + graphics.line(barXPosition + barWidth, y1) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition + barWidth / 2, l2) + graphics.line(barXPosition + barWidth / 2, y1) + graphics.line(barXPosition, y1) + graphics.line(barXPosition, y2 - strokeWidth / 2)
                    ];
                }
                var pathFrom = null;
                if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.getPreviousPath(realIndex, j, pathTo[0]);
                }
                if (pathFrom == null) {
                    pathFrom = graphics.move(barXPosition + barWidth / 2, y1) + graphics.move(barXPosition, y1);
                }
                if (!w.axisFlags.isXNumeric) {
                    x = x + xDivision;
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom,
                    x: x,
                    y: y2,
                    goalY: this.barHelpers.getGoalValues("y", /** @type {any} */ null, zeroH, realIndex, j, indexes.translationsIndex),
                    barXPosition: barXPosition,
                    color: color
                };
            }
        },
        {
            /** @param {{indexes: any, y: any, yDivision: any, barHeight: any, zeroW: any, strokeWidth: any}} opts */ key: "drawHorizontalBoxPaths",
            value: function drawHorizontalBoxPaths(param) {
                var indexes = param.indexes, y = param.y, yDivision = param.yDivision, barHeight = param.barHeight, zeroW = param.zeroW, strokeWidth = param.strokeWidth;
                var _a, _b;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var i = indexes.i;
                var j = indexes.j;
                var realIndex = indexes.realIndex;
                var _w_config_plotOptions_candlestick = w.config.plotOptions.candlestick, candleColors = _w_config_plotOptions_candlestick.colors;
                var _this_boxOptions = this.boxOptions, boxColors = _this_boxOptions.colors;
                var getColor = function getColor(color2) {
                    return Array.isArray(color2) ? color2[realIndex] : color2;
                };
                var yRatio = this.invertedYRatio;
                var ohlc = this.getOHLCValue(realIndex, j);
                var color = ohlc.o < ohlc.c ? [
                    getColor(candleColors.upward)
                ] : [
                    getColor(candleColors.downward)
                ];
                if (this.isBoxPlot) {
                    color = [
                        getColor(boxColors.lower),
                        getColor(boxColors.upper)
                    ];
                }
                var l1 = zeroW;
                var l2 = zeroW;
                var x1 = Math.min(ohlc.o, ohlc.c);
                var x2 = Math.max(ohlc.o, ohlc.c);
                var m = ohlc.m;
                if (w.axisFlags.isXNumeric) {
                    y = (w.seriesData.seriesX[realIndex][j] - w.globals.minX) / this.invertedXRatio - barHeight / 2;
                }
                var barYPosition = y + barHeight * this.visibleI;
                if (typeof /** @type {any} */ ((_a = this.series[i]) == null ? void 0 : _a[j]) === "undefined" || /** @type {any} */ ((_b = this.series[i]) == null ? void 0 : _b[j]) === null) {
                    x1 = zeroW;
                    x2 = zeroW;
                } else {
                    x1 = zeroW + x1 / yRatio;
                    x2 = zeroW + x2 / yRatio;
                    l1 = zeroW + ohlc.h / yRatio;
                    l2 = zeroW + ohlc.l / yRatio;
                    m = zeroW + ohlc.m / yRatio;
                }
                var pathTo = [
                    graphics.move(x1, barYPosition) + graphics.line(x1, barYPosition + barHeight / 2) + graphics.line(l1, barYPosition + barHeight / 2) + graphics.line(l1, barYPosition + barHeight / 2 - barHeight / 4) + graphics.line(l1, barYPosition + barHeight / 2 + barHeight / 4) + graphics.line(l1, barYPosition + barHeight / 2) + graphics.line(x1, barYPosition + barHeight / 2) + graphics.line(x1, barYPosition + barHeight) + graphics.line(m, barYPosition + barHeight) + graphics.line(m, barYPosition) + graphics.line(x1 + strokeWidth / 2, barYPosition),
                    graphics.move(m, barYPosition) + graphics.line(m, barYPosition + barHeight) + graphics.line(x2, barYPosition + barHeight) + graphics.line(x2, barYPosition + barHeight / 2) + graphics.line(l2, barYPosition + barHeight / 2) + graphics.line(l2, barYPosition + barHeight - barHeight / 4) + graphics.line(l2, barYPosition + barHeight / 4) + graphics.line(l2, barYPosition + barHeight / 2) + graphics.line(x2, barYPosition + barHeight / 2) + graphics.line(x2, barYPosition) + graphics.line(m, barYPosition) + "z"
                ];
                var pathFrom = null;
                if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.getPreviousPath(realIndex, j, pathTo[0]);
                }
                if (pathFrom == null) {
                    pathFrom = graphics.move(x1, barYPosition + barHeight / 2) + graphics.move(x1, barYPosition);
                }
                if (!w.axisFlags.isXNumeric) {
                    y = y + yDivision;
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom,
                    x: x2,
                    y: y,
                    goalX: this.barHelpers.getGoalValues("x", zeroW, /** @type {any} */ null, realIndex, j, 0),
                    barYPosition: barYPosition,
                    color: color
                };
            }
        },
        {
            /**
   * @param {number} i
   * @param {number} j
   */ key: "getOHLCValue",
            value: function getOHLCValue(i, j) {
                var w = this.w;
                var coreUtils = this.coreUtils;
                var getCandleVal = function getCandleVal(arr) {
                    return arr[i] && arr[i][j] != null ? /** @type {any} */ coreUtils.getLogValAtSeriesIndex(arr[i][j], i) : 0;
                };
                var h = getCandleVal(w.candleData.seriesCandleH);
                var o = getCandleVal(w.candleData.seriesCandleO);
                var m = getCandleVal(w.candleData.seriesCandleM);
                var c = getCandleVal(w.candleData.seriesCandleC);
                var l = getCandleVal(w.candleData.seriesCandleL);
                return {
                    o: this.isBoxPlot ? h : o,
                    h: this.isBoxPlot ? o : h,
                    m: m,
                    l: this.isBoxPlot ? c : l,
                    c: this.isBoxPlot ? l : c
                };
            }
        }
    ]);
    return BoxCandleStick;
}(Bar);
var tangents = function tangents(points) {
    var m = finiteDifferences(points);
    var n = points.length - 1;
    var ε = 1e-6;
    var tgts = [];
    var a, b, d, s;
    for(var i = 0; i < n; i++){
        d = slope(points[i], points[i + 1]);
        if (Math.abs(d) < ε) {
            m[i] = m[i + 1] = 0;
        } else {
            a = m[i] / d;
            b = m[i + 1] / d;
            s = a * a + b * b;
            if (s > 9) {
                s = d * 3 / Math.sqrt(s);
                m[i] = s * a;
                m[i + 1] = s * b;
            }
        }
    }
    for(var i1 = 0; i1 <= n; i1++){
        s = (points[Math.min(n, i1 + 1)][0] - points[Math.max(0, i1 - 1)][0]) / (6 * (1 + m[i1] * m[i1]));
        tgts.push([
            s || 0,
            m[i1] * s || 0
        ]);
    }
    return tgts;
};
var svgPath = function svgPath(points) {
    var p = "";
    for(var i = 0; i < points.length; i++){
        var point = points[i];
        var n = point.length;
        if (n > 4) {
            p += "C".concat(point[0], ", ").concat(point[1]);
            p += ", ".concat(point[2], ", ").concat(point[3]);
            p += ", ".concat(point[4], ", ").concat(point[5]);
        } else if (n > 2) {
            p += "S".concat(point[0], ", ").concat(point[1]);
            p += ", ".concat(point[2], ", ").concat(point[3]);
        }
    }
    return p;
};
var spline = {
    /**
   * Convert 'points' to bezier
   * @param {any[]} points
   * @returns {any[]}
   */ points: function points(_points) {
        var tgts = tangents(_points);
        var p = _points[1];
        var p0 = _points[0];
        var pts = [];
        var t = tgts[1];
        var t0 = tgts[0];
        pts.push(p0, [
            p0[0] + t0[0],
            p0[1] + t0[1],
            p[0] - t[0],
            p[1] - t[1],
            p[0],
            p[1]
        ]);
        for(var i = 2, n = tgts.length; i < n; i++){
            var p2 = _points[i];
            var t2 = tgts[i];
            pts.push([
                p2[0] - t2[0],
                p2[1] - t2[1],
                p2[0],
                p2[1]
            ]);
        }
        return pts;
    },
    /**
   * Slice out a segment of 'points'
   * @param {any[]} points
   * @param {Number} start
   * @param {Number} end
   * @returns {any[]}
   */ slice: function slice(points, start, end) {
        var pts = points.slice(start, end);
        if (start) {
            if (end - start > 1 && pts[1].length < 6) {
                var n = pts[0].length;
                pts[1] = [
                    pts[0][n - 2] * 2 - pts[0][n - 4],
                    pts[0][n - 1] * 2 - pts[0][n - 3]
                ].concat(pts[1]);
            }
            pts[0] = pts[0].slice(-2);
        }
        return pts;
    }
};
function slope(p0, p1) {
    return (p1[1] - p0[1]) / (p1[0] - p0[0]);
}
function finiteDifferences(points) {
    var m = [];
    var p0 = points[0];
    var p1 = points[1];
    var d = m[0] = slope(p0, p1);
    var i = 1;
    for(var n = points.length - 1; i < n; i++){
        p0 = p1;
        p1 = points[i + 1];
        m[i] = (d + (d = slope(p0, p1))) * 0.5;
    }
    m[i] = d;
    return m;
}
var Violin = /*#__PURE__*/ function(Bar) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Violin, Bar);
    function Violin() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Violin);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Violin, arguments);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Violin, [
        {
            /**
   * @param {any[]} series
   * @param {string} ctype
   * @param {number} [seriesIndex]
   */ // @ts-ignore -- Violin.draw has extra ctype param compared to Bar.draw
            key: "draw",
            value: function draw(series, ctype, seriesIndex) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var fill = new Fill(this.w);
                this.violinOptions = w.config.plotOptions.violin;
                this.pointsOptions = this.violinOptions.points;
                this.bandwidthScale = this.violinOptions.bandwidthScale || 1;
                this.normalize = this.violinOptions.normalize || "individual";
                this.distributed = w.config.plotOptions.bar.distributed;
                this.isHorizontal = w.config.plotOptions.bar.horizontal;
                this.coreUtils = new CoreUtils(this.w);
                series = this.coreUtils.getLogSeries(series);
                this.series = series;
                this.yRatio = this.coreUtils.getLogYRatios(this.yRatio);
                this.barHelpers.initVariables(series);
                var ret = graphics.group({
                    class: "apexcharts-violin-series apexcharts-plot-series"
                });
                for(var i = 0; i < series.length; i++){
                    var x = void 0;
                    var y = void 0;
                    var yArrj = [];
                    var xArrj = [];
                    var realIndex = w.globals.comboCharts ? /** @type {any} */ seriesIndex[i] : i;
                    var columnGroupIndex = this.barHelpers.getGroupIndex(realIndex).columnGroupIndex;
                    var elSeries = graphics.group({
                        class: "apexcharts-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex]),
                        rel: i + 1,
                        "data:realIndex": realIndex
                    });
                    Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
                    if (series[i].length > 0) {
                        this.visibleI = this.visibleI + 1;
                    }
                    var translationsIndex = 0;
                    if (this.yRatio.length > 1) {
                        this.yaxisIndex = /** @type {any} */ w.globals.seriesYAxisReverseMap[realIndex][0];
                        translationsIndex = realIndex;
                    }
                    var initPositions = this.barHelpers.initialPositions(realIndex);
                    var initY = initPositions.y, barHeight = initPositions.barHeight, yDivision = initPositions.yDivision, zeroW = initPositions.zeroW, initX = initPositions.x, barWidth = initPositions.barWidth, xDivision = initPositions.xDivision, zeroH = initPositions.zeroH;
                    y = initY;
                    x = initX;
                    xArrj.push(x + (barWidth != null ? barWidth : 0) / 2);
                    var elDataLabelsWrap = graphics.group({
                        class: "apexcharts-datalabels",
                        "data:realIndex": realIndex
                    });
                    this.seriesMaxWeight = 0;
                    if (this.normalize === "group") {
                        var dens = w.violinData.seriesViolinDensity[realIndex] || [];
                        dens.forEach(function(d) {
                            if (d && d.maxWeight > _this.seriesMaxWeight) {
                                _this.seriesMaxWeight = d.maxWeight;
                            }
                        });
                    }
                    var pointsByViolin = [];
                    for(var j = 0; j < w.globals.dataPoints; j++){
                        var strokeWidth = this.barHelpers.getStrokeWidth(i, j, realIndex);
                        var paths = this.isHorizontal ? this.drawHorizontalViolin({
                            indexes: {
                                i: i,
                                j: j,
                                realIndex: realIndex,
                                translationsIndex: translationsIndex
                            },
                            y: y,
                            yDivision: yDivision,
                            barHeight: barHeight,
                            zeroW: zeroW
                        }) : this.drawVerticalViolin({
                            indexes: {
                                i: i,
                                j: j,
                                realIndex: realIndex,
                                translationsIndex: translationsIndex
                            },
                            x: x,
                            xDivision: xDivision,
                            barWidth: barWidth,
                            zeroH: zeroH
                        });
                        x = paths.x;
                        y = paths.y;
                        if (j > 0) {
                            xArrj.push(paths.center);
                        }
                        yArrj.push(paths.alongRepresentative);
                        var pointGroups = this.buildPointsSubPath({
                            realIndex: realIndex,
                            j: j,
                            center: paths.center,
                            halfExtent: paths.halfExtent,
                            alongFn: paths.alongFn,
                            density: paths.density,
                            maxWeight: paths.maxWeight
                        });
                        if (pointGroups.length) pointsByViolin.push({
                            groups: pointGroups,
                            j: j
                        });
                        var pathFill = fill.fillPath({
                            // distributed → color per category (data point) instead of per series
                            seriesNumber: this.distributed ? j : realIndex,
                            dataPointIndex: j,
                            color: this.distributed ? w.globals.colors[j] : void 0,
                            value: series[i][j]
                        });
                        this.renderSeries({
                            realIndex: realIndex,
                            pathFill: pathFill,
                            lineFill: w.globals.stroke.colors[realIndex],
                            j: j,
                            i: i,
                            pathFrom: paths.pathFrom,
                            pathTo: paths.pathTo,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries,
                            x: x,
                            y: y,
                            series: series,
                            columnGroupIndex: columnGroupIndex,
                            barHeight: barHeight,
                            barWidth: barWidth,
                            elDataLabelsWrap: elDataLabelsWrap,
                            visibleSeries: this.visibleI,
                            type: "violin"
                        });
                        var bodyEl = elSeries.node.querySelector("path.apexcharts-violin-area[j='".concat(j, "']"));
                        if (bodyEl && isFinite(paths.alongRepresentative)) {
                            bodyEl.setAttribute(this.isHorizontal ? "cx" : "cy", "".concat(paths.alongRepresentative));
                        }
                    }
                    renderJitter({
                        graphics: graphics,
                        w: w,
                        elSeries: elSeries,
                        pointsByCat: pointsByViolin,
                        options: this.pointsOptions,
                        distributed: this.distributed,
                        realIndex: realIndex,
                        wrapClass: "apexcharts-violin-points-wrap",
                        pointClass: "apexcharts-violin-points"
                    });
                    w.globals.seriesXvalues[realIndex] = xArrj;
                    w.globals.seriesYvalues[realIndex] = yArrj;
                    ret.add(elSeries);
                }
                return ret;
            }
        },
        {
            /** @param {{indexes: any, x: any, xDivision: any, barWidth: any, zeroH: any}} opts */ key: "drawVerticalViolin",
            value: function drawVerticalViolin(param) {
                var _this = this;
                var indexes = param.indexes, x = param.x, xDivision = param.xDivision, barWidth = param.barWidth, zeroH = param.zeroH;
                var _a;
                var w = this.w;
                var realIndex = indexes.realIndex, j = indexes.j, translationsIndex = indexes.translationsIndex;
                var yRatio = this.yRatio[translationsIndex];
                if (w.axisFlags.isXNumeric) {
                    x = (w.seriesData.seriesX[realIndex][j] - w.globals.minX) / this.xRatio - barWidth / 2;
                }
                var barXPosition = x + barWidth * this.visibleI;
                var center = barXPosition + barWidth / 2;
                var halfExtent = barWidth / 2;
                var density = this.getDensity(realIndex, j);
                var maxWeight = this.effectiveMaxWeight(density);
                var alongFn = function alongFn(v) {
                    return zeroH - _this.logVal(v, realIndex) / yRatio;
                };
                var pathTo = this.buildBodyPath({
                    nodes: density.nodes,
                    center: center,
                    halfExtent: halfExtent,
                    maxWeight: maxWeight,
                    vertical: true,
                    alongFn: alongFn,
                    collapsed: false
                });
                var pathFrom = null;
                if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.getPreviousPath(realIndex, j, pathTo);
                }
                if (pathFrom == null) {
                    pathFrom = this.buildBodyPath({
                        nodes: density.nodes,
                        center: center,
                        halfExtent: halfExtent,
                        maxWeight: maxWeight,
                        vertical: true,
                        alongFn: alongFn,
                        collapsed: true
                    });
                }
                if (!w.axisFlags.isXNumeric) {
                    x = x + xDivision;
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom,
                    x: x,
                    y: zeroH,
                    center: center,
                    halfExtent: halfExtent,
                    alongFn: alongFn,
                    density: density,
                    maxWeight: maxWeight,
                    alongRepresentative: alongFn((_a = this.series[indexes.i][j]) != null ? _a : 0)
                };
            }
        },
        {
            /** @param {{indexes: any, y: any, yDivision: any, barHeight: any, zeroW: any}} opts */ key: "drawHorizontalViolin",
            value: function drawHorizontalViolin(param) {
                var _this = this;
                var indexes = param.indexes, y = param.y, yDivision = param.yDivision, barHeight = param.barHeight, zeroW = param.zeroW;
                var _a;
                var w = this.w;
                var realIndex = indexes.realIndex, j = indexes.j;
                var yRatio = this.invertedYRatio;
                if (w.axisFlags.isXNumeric) {
                    y = (w.seriesData.seriesX[realIndex][j] - w.globals.minX) / this.invertedXRatio - barHeight / 2;
                }
                var barYPosition = y + barHeight * this.visibleI;
                var center = barYPosition + barHeight / 2;
                var halfExtent = barHeight / 2;
                var density = this.getDensity(realIndex, j);
                var maxWeight = this.effectiveMaxWeight(density);
                var alongFn = function alongFn(v) {
                    return zeroW + _this.logVal(v, realIndex) / yRatio;
                };
                var pathTo = this.buildBodyPath({
                    nodes: density.nodes,
                    center: center,
                    halfExtent: halfExtent,
                    maxWeight: maxWeight,
                    vertical: false,
                    alongFn: alongFn,
                    collapsed: false
                });
                var pathFrom = null;
                if (w.globals.previousPaths.length > 0) {
                    pathFrom = this.getPreviousPath(realIndex, j, pathTo);
                }
                if (pathFrom == null) {
                    pathFrom = this.buildBodyPath({
                        nodes: density.nodes,
                        center: center,
                        halfExtent: halfExtent,
                        maxWeight: maxWeight,
                        vertical: false,
                        alongFn: alongFn,
                        collapsed: true
                    });
                }
                if (!w.axisFlags.isXNumeric) {
                    y = y + yDivision;
                }
                return {
                    pathTo: pathTo,
                    pathFrom: pathFrom,
                    x: zeroW,
                    y: y,
                    center: center,
                    halfExtent: halfExtent,
                    alongFn: alongFn,
                    maxWeight: maxWeight,
                    density: density,
                    alongRepresentative: alongFn((_a = this.series[indexes.i][j]) != null ? _a : 0)
                };
            }
        },
        {
            /**
   * Read the parsed density for one violin and return sorted, de-duplicated
   * nodes (strictly increasing value — a hard requirement for the spline).
   * @param {number} realIndex
   * @param {number} j
   */ key: "getDensity",
            value: function getDensity(realIndex, j) {
                var _a;
                var w = this.w;
                var d = (_a = w.violinData.seriesViolinDensity[realIndex]) == null ? void 0 : _a[j];
                if (!d || !d.values.length) {
                    return {
                        nodes: [],
                        maxWeight: 0
                    };
                }
                var order = d.values.map(function(_, k) {
                    return k;
                });
                order.sort(function(a, b) {
                    return d.values[a] - d.values[b];
                });
                var nodes = [];
                var prevV = null;
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = order[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var k = _step.value;
                        var v = d.values[k];
                        if (prevV !== null && v === prevV) continue;
                        nodes.push({
                            v: v,
                            w: d.weights[k]
                        });
                        prevV = v;
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                return {
                    nodes: nodes,
                    maxWeight: d.maxWeight
                };
            }
        },
        {
            /**
   * The peak weight used to scale a violin's width: its own ('individual') or
   * the densest violin in the series ('group', preserving relative widths).
   * @param {{maxWeight:number}} density
   */ key: "effectiveMaxWeight",
            value: function effectiveMaxWeight(density) {
                return this.normalize === "group" && this.seriesMaxWeight > 0 ? this.seriesMaxWeight : density.maxWeight;
            }
        },
        {
            /**
   * Build the closed, smooth violin outline. The value axis is the monotonic
   * parameter for the spline (vertical → Y, horizontal → X); the spline is fed
   * with that axis first and the control points swapped back to screen space.
   *
   * @param {{nodes:{v:number,w:number}[], center:number, halfExtent:number, maxWeight:number, vertical:boolean, alongFn:(v:number)=>number, collapsed:boolean}} opts
   */ key: "buildBodyPath",
            value: function buildBodyPath(param) {
                var _this = this;
                var nodes = param.nodes, center = param.center, halfExtent = param.halfExtent, maxWeight = param.maxWeight, vertical = param.vertical, alongFn = param.alongFn, collapsed = param.collapsed;
                var graphics = new Graphics(this.w);
                if (nodes.length === 0) {
                    var a = alongFn(0);
                    return vertical ? graphics.move(center, a) + graphics.line(center, a) : graphics.move(a, center) + graphics.line(a, center);
                }
                var wpxOf = function wpxOf(weight) {
                    if (collapsed || maxWeight <= 0) return 0;
                    var wp = weight / maxWeight * halfExtent * _this.bandwidthScale;
                    return Math.min(halfExtent, Math.max(0, wp));
                };
                var rightPts = [];
                var leftPts = [];
                for(var k = 0; k < nodes.length; k++){
                    var a1 = alongFn(nodes[k].v);
                    var wp = wpxOf(nodes[k].w);
                    if (vertical) {
                        rightPts.push([
                            center + wp,
                            a1
                        ]);
                        leftPts.push([
                            center - wp,
                            a1
                        ]);
                    } else {
                        rightPts.push([
                            a1,
                            center + wp
                        ]);
                        leftPts.push([
                            a1,
                            center - wp
                        ]);
                    }
                }
                leftPts.reverse();
                return this.smoothSegment(rightPts, vertical, false) + this.smoothSegment(leftPts, vertical, true) + "z";
            }
        },
        {
            /**
   * Emit one edge as a smooth (monotone-cubic) path segment, or a polyline
   * when there are too few nodes for a spline.
   *
   * @param {[number,number][]} screenPts ordered screen points for this edge
   * @param {boolean} monotonicIsY true when the value axis is vertical
   * @param {boolean} continued false → start with M; true → start with L (joins the previous edge)
   */ key: "smoothSegment",
            value: function smoothSegment(screenPts, monotonicIsY, continued) {
                var graphics = new Graphics(this.w);
                var first = screenPts[0];
                var d = continued ? graphics.line(first[0], first[1]) : graphics.move(first[0], first[1]);
                var usePolyline = screenPts.length < 3 || !this.strictlyMonotonic(screenPts, monotonicIsY);
                if (usePolyline) {
                    for(var k = 1; k < screenPts.length; k++){
                        d += graphics.line(screenPts[k][0], screenPts[k][1]);
                    }
                    return d;
                }
                var input = screenPts.map(function(param) {
                    var _param = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(param, 2), px = _param[0], py = _param[1];
                    return monotonicIsY ? [
                        py,
                        px
                    ] : [
                        px,
                        py
                    ];
                });
                var bez = spline.points(input);
                var out = monotonicIsY ? bez.map(swapPairs) : bez;
                d += svgPath(out);
                return d;
            }
        },
        {
            /**
   * @param {[number,number][]} screenPts
   * @param {boolean} monotonicIsY
   */ key: "strictlyMonotonic",
            value: function strictlyMonotonic(screenPts, monotonicIsY) {
                var axis = monotonicIsY ? 1 : 0;
                for(var k = 1; k < screenPts.length; k++){
                    if (screenPts[k][axis] === screenPts[k - 1][axis]) return false;
                }
                return true;
            }
        },
        {
            /**
   * Build the jitter sub-paths for one violin, grouped for rendering. Returns
   * `[]` when points are hidden or absent. Normally one group (single dot
   * colour); with `points.colorScale` the dots are bucketed by value into shade
   * groups, each carrying its ramp colour. Offsets are a deterministic index
   * hash (SSR-safe); points beyond maxPoints are stride-thinned.
   *
   * @param {{realIndex:number, j:number, center:number, halfExtent:number, alongFn:(v:number)=>number, density:{nodes:{v:number,w:number}[], maxWeight:number}, maxWeight:number}} opts
   * @returns {{fill:string|null, d:string}[]}
   */ key: "buildPointsSubPath",
            value: function buildPointsSubPath(param) {
                var _this = this;
                var realIndex = param.realIndex, j = param.j, center = param.center, halfExtent = param.halfExtent, alongFn = param.alongFn, density = param.density, maxWeight = param.maxWeight;
                var _a;
                return buildJitterGroups({
                    w: this.w,
                    points: (_a = this.w.violinData.seriesViolinPoints[realIndex]) == null ? void 0 : _a[j],
                    seedA: realIndex,
                    seedB: j,
                    center: center,
                    halfExtent: halfExtent,
                    alongFn: alongFn,
                    isHorizontal: this.isHorizontal,
                    options: this.pointsOptions,
                    // Violin clamps jitter to the density half-width at each value so dots
                    // stay inside the shape.
                    clampAt: function clampAt(v) {
                        return _this.halfWidthAtValue(v, density, halfExtent, maxWeight);
                    }
                });
            }
        },
        {
            /**
   * Density half-width (pixels) at a given value — used to keep jitter inside
   * the violin. Linear interpolation between the two nearest density nodes.
   * @param {number} value
   * @param {{nodes:{v:number,w:number}[], maxWeight:number}} density
   * @param {number} halfExtent
   * @param {number} [maxWeightOverride] use the group max for 'group' normalize
   */ key: "halfWidthAtValue",
            value: function halfWidthAtValue(value, density, halfExtent, maxWeightOverride) {
                var _this = this;
                var nodes = density.nodes;
                var maxWeight = maxWeightOverride != null ? maxWeightOverride : density.maxWeight;
                if (!nodes.length || maxWeight <= 0) return 0;
                var toPx = function toPx(weight) {
                    return Math.min(halfExtent, weight / maxWeight * halfExtent * _this.bandwidthScale);
                };
                if (value <= nodes[0].v) return toPx(nodes[0].w);
                if (value >= nodes[nodes.length - 1].v) return toPx(nodes[nodes.length - 1].w);
                for(var k = 1; k < nodes.length; k++){
                    if (value <= nodes[k].v) {
                        var a = nodes[k - 1];
                        var b = nodes[k];
                        var t = b.v === a.v ? 0 : (value - a.v) / (b.v - a.v);
                        return toPx(a.w + (b.w - a.w) * t);
                    }
                }
                return 0;
            }
        },
        {
            /**
   * Apply the y-axis log transform to a value when that axis is logarithmic,
   * mirroring BoxCandleStick. Linear axes return the value unchanged.
   * @param {number} value
   * @param {number} realIndex
   */ key: "logVal",
            value: function logVal(value, realIndex) {
                return /** @type {any} */ this.coreUtils.getLogValAtSeriesIndex(value, realIndex);
            }
        }
    ]);
    return Violin;
}(Bar);
function swapPairs(arr) {
    var out = [];
    for(var k = 0; k < arr.length; k += 2){
        out.push(arr[k + 1], arr[k]);
    }
    return out;
}
var TreemapHelpers = /*#__PURE__*/ function() {
    "use strict";
    function TreemapHelpers(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, TreemapHelpers);
        this.ctx = ctx;
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(TreemapHelpers, [
        {
            key: "checkColorRange",
            value: function checkColorRange() {
                var w = this.w;
                var negRange = false;
                var chartOpts = w.config.plotOptions[w.config.chart.type];
                if (chartOpts.colorScale.ranges.length > 0) {
                    chartOpts.colorScale.ranges.map(function(range) {
                        if (range.from <= 0) {
                            negRange = true;
                        }
                    });
                }
                return negRange;
            }
        },
        {
            /**
   * @param {string} chartType
   * @param {number} i
   * @param {number} j
   * @param {any} negRange
   */ key: "getShadeColor",
            value: function getShadeColor(chartType, i, j, negRange) {
                var w = this.w;
                var colorShadePercent = 1;
                var shadeIntensity = w.config.plotOptions[chartType].shadeIntensity;
                var colorProps = this.determineColor(chartType, i, j);
                if (/** @type {any} */ w.globals.hasNegs || negRange) {
                    if (w.config.plotOptions[chartType].reverseNegativeShade) {
                        if (colorProps.percent < 0) {
                            colorShadePercent = colorProps.percent / 100 * (shadeIntensity * 1.25);
                        } else {
                            colorShadePercent = (1 - colorProps.percent / 100) * (shadeIntensity * 1.25);
                        }
                    } else {
                        if (colorProps.percent <= 0) {
                            colorShadePercent = 1 - (1 + colorProps.percent / 100) * shadeIntensity;
                        } else {
                            colorShadePercent = (1 - colorProps.percent / 100) * shadeIntensity;
                        }
                    }
                } else {
                    colorShadePercent = 1 - colorProps.percent / 100;
                    if (chartType === "treemap") {
                        colorShadePercent = (1 - colorProps.percent / 100) * (shadeIntensity * 1.25);
                    }
                }
                var color = colorProps.color;
                var utils = new Utils$1();
                if (w.config.plotOptions[chartType].enableShades) {
                    if (this.w.config.theme.mode === "dark") {
                        var shadeColor = utils.shadeColor(colorShadePercent * -1, colorProps.color);
                        color = Utils$1.hexToRgba(Utils$1.isColorHex(shadeColor) ? shadeColor : Utils$1.rgb2hex(shadeColor), w.config.fill.opacity);
                    } else {
                        var shadeColor1 = utils.shadeColor(colorShadePercent, colorProps.color);
                        color = Utils$1.hexToRgba(Utils$1.isColorHex(shadeColor1) ? shadeColor1 : Utils$1.rgb2hex(shadeColor1), w.config.fill.opacity);
                    }
                }
                return {
                    color: color,
                    colorProps: colorProps
                };
            }
        },
        {
            /**
   * @param {string} chartType
   * @param {number} i
   * @param {number} j
   */ key: "determineColor",
            value: function determineColor(chartType, i, j) {
                var w = this.w;
                var val = w.seriesData.series[i][j];
                var chartOpts = w.config.plotOptions[chartType];
                var seriesNumber = chartOpts.colorScale.inverse ? j : i;
                if (chartOpts.distributed && w.config.chart.type === "treemap") {
                    seriesNumber = j;
                }
                var color = w.globals.colors[seriesNumber];
                var foreColor = null;
                var min;
                var max;
                if (!chartOpts.distributed && chartType === "heatmap") {
                    min = w.globals.minY;
                    max = w.globals.maxY;
                } else {
                    var row = w.seriesData.series[i];
                    min = row.length ? row[0] : 0;
                    max = min;
                    for(var k = 1; k < row.length; k++){
                        if (row[k] < min) min = row[k];
                        if (row[k] > max) max = row[k];
                    }
                }
                if (typeof chartOpts.colorScale.min !== "undefined") {
                    min = chartOpts.colorScale.min < w.globals.minY ? chartOpts.colorScale.min : w.globals.minY;
                    max = chartOpts.colorScale.max > w.globals.maxY ? chartOpts.colorScale.max : w.globals.maxY;
                }
                var total = Math.abs(max) + Math.abs(min);
                var percent = total === 0 ? 0 : 100 * val / total;
                if (chartOpts.colorScale.ranges.length > 0) {
                    var colorRange = chartOpts.colorScale.ranges;
                    colorRange.map(function(range) {
                        if (val >= range.from && val <= range.to) {
                            color = range.color;
                            foreColor = range.foreColor ? range.foreColor : null;
                            min = range.from;
                            max = range.to;
                            var rTotal = Math.abs(max) + Math.abs(min);
                            percent = rTotal === 0 ? 0 : 100 * val / rTotal;
                        }
                    });
                }
                return {
                    color: color,
                    foreColor: foreColor,
                    percent: percent
                };
            }
        },
        {
            /** @param {{ text?: any, x?: any, y?: any, i?: any, j?: any, colorProps?: any, fontSize?: any, series?: any }} opts */ key: "calculateDataLabels",
            value: function calculateDataLabels(param) {
                var text = param.text, x = param.x, y = param.y, i = param.i, j = param.j, colorProps = param.colorProps, fontSize = param.fontSize;
                var w = this.w;
                var dataLabelsConfig = w.config.dataLabels;
                var graphics = new Graphics(this.w);
                var dataLabels = new DataLabels(this.w, this.ctx);
                var elDataLabelsWrap = null;
                if (dataLabelsConfig.enabled) {
                    elDataLabelsWrap = graphics.group({
                        class: "apexcharts-data-labels"
                    });
                    var offX = resolveDataLabelOffset(dataLabelsConfig.offsetX, w, i, j);
                    var offY = resolveDataLabelOffset(dataLabelsConfig.offsetY, w, i, j);
                    var dataLabelsX = x + offX;
                    var dataLabelsY = y + parseFloat(dataLabelsConfig.style.fontSize) / 3 + offY;
                    dataLabels.plotDataLabelsText({
                        x: dataLabelsX,
                        y: dataLabelsY,
                        text: text,
                        i: i,
                        j: j,
                        color: colorProps.foreColor,
                        parent: elDataLabelsWrap,
                        fontSize: fontSize,
                        dataLabelsConfig: dataLabelsConfig
                    });
                }
                return elDataLabelsWrap;
            }
        }
    ]);
    return TreemapHelpers;
}();
var HeatMap = /*#__PURE__*/ function() {
    "use strict";
    function HeatMap(w, ctx, xyRatios) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, HeatMap);
        this.ctx = ctx;
        this.w = w;
        this.xRatio = xyRatios.xRatio;
        this.yRatio = xyRatios.yRatio;
        this.dynamicAnim = this.w.config.chart.animations.dynamicAnimation;
        this.helpers = new TreemapHelpers(w, ctx);
        this.rectRadius = this.w.config.plotOptions.heatmap.radius;
        this.strokeWidth = this.w.config.stroke.show ? this.w.config.stroke.width : 0;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(HeatMap, [
        {
            /**
   * @param {any[]} series
   */ key: "draw",
            value: function draw(series) {
                var _a, _b;
                var w = this.w;
                var graphics = new Graphics(this.w, this.ctx);
                var emit = seriesEmitter(this.ctx, graphics);
                var useCanvas = emit !== graphics && typeof emit.drawRectCell === "function";
                var ret = graphics.group({
                    class: "apexcharts-heatmap"
                });
                ret.attr("clip-path", "url(#gridRectMask".concat(w.globals.cuid, ")"));
                var xDivision = w.layout.gridWidth / w.globals.dataPoints;
                var yDivision = w.layout.gridHeight / w.seriesData.series.length;
                var isContinuousX = (w.config.xaxis.type === "numeric" || w.config.xaxis.type === "datetime") && w.axisFlags.isXNumeric && this.xRatio > 0;
                var binPx = xDivision;
                if (isContinuousX) {
                    var diff = w.globals.minXDiff;
                    binPx = Number.isFinite(diff) && diff > 0 ? diff / this.xRatio : xDivision;
                }
                var cellFillOpacity = Array.isArray(w.config.fill.opacity) ? (_a = w.config.fill.opacity[0]) != null ? _a : 1 : (_b = w.config.fill.opacity) != null ? _b : 1;
                var y1 = 0;
                var rev = false;
                this.negRange = this.helpers.checkColorRange();
                var heatSeries = series.slice();
                if (w.config.yaxis[0].reversed) {
                    rev = true;
                    heatSeries.reverse();
                }
                for(var i = rev ? 0 : heatSeries.length - 1; rev ? i < heatSeries.length : i >= 0; rev ? i++ : i--){
                    var elSeries = graphics.group({
                        class: "apexcharts-series apexcharts-heatmap-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[i]),
                        rel: i + 1,
                        "data:realIndex": i
                    });
                    Series.addCollapsedClassToSeries(this.w, elSeries, i);
                    if (!useCanvas) {
                        graphics.setupEventDelegation(elSeries, ".apexcharts-heatmap-rect");
                    }
                    if (w.config.chart.dropShadow.enabled) {
                        var shadow = w.config.chart.dropShadow;
                        var filters = new Filters(this.w);
                        filters.dropShadow(elSeries, shadow, i);
                    }
                    var x1 = 0;
                    var shadeIntensity = w.config.plotOptions.heatmap.shadeIntensity;
                    var j = 0;
                    for(var dIndex = 0; dIndex < w.globals.dataPoints; dIndex++){
                        if (!isContinuousX && w.seriesData.seriesX.length && !w.globals.allSeriesHasEqualX) {
                            if (w.globals.minX + w.globals.minXDiff * dIndex < w.seriesData.seriesX[i][j]) {
                                x1 = x1 + xDivision;
                                continue;
                            }
                        }
                        if (j >= heatSeries[i].length) break;
                        var cellW = isContinuousX ? binPx : xDivision;
                        if (isContinuousX) {
                            var xVal = w.seriesData.seriesX[i] ? w.seriesData.seriesX[i][j] : null;
                            if (xVal == null || xVal !== xVal) {
                                j++;
                                continue;
                            }
                            x1 = (xVal - w.globals.minX) / this.xRatio - binPx / 2;
                        }
                        var heatColor = this.helpers.getShadeColor(w.config.chart.type, i, j, this.negRange);
                        var color = heatColor.color;
                        var heatColorProps = heatColor.colorProps;
                        if (w.config.fill.type === "image") {
                            var fill = new Fill(this.w);
                            color = fill.fillPath({
                                seriesNumber: i,
                                dataPointIndex: j,
                                opacity: /** @type {any} */ w.globals.hasNegs ? heatColorProps.percent < 0 ? 1 - (1 + heatColorProps.percent / 100) : shadeIntensity + heatColorProps.percent / 100 : heatColorProps.percent / 100,
                                patternID: Utils$1.randomId(),
                                width: w.config.fill.image.width ? w.config.fill.image.width : cellW,
                                height: w.config.fill.image.height ? w.config.fill.image.height : yDivision
                            });
                        }
                        var radius = this.rectRadius;
                        var stroke = w.config.plotOptions.heatmap.useFillColorAsStroke ? color : w.globals.stroke.colors[0];
                        if (useCanvas) {
                            emit.drawRectCell(x1, y1, cellW, yDivision, {
                                fill: color,
                                fillOpacity: cellFillOpacity,
                                stroke: stroke,
                                strokeWidth: this.strokeWidth,
                                radius: radius,
                                seriesIndex: i,
                                dataPointIndex: j
                            });
                        } else {
                            var rect = graphics.drawRect(x1, y1, cellW, yDivision, radius);
                            rect.attr({
                                cx: x1,
                                cy: y1
                            });
                            rect.node.classList.add("apexcharts-heatmap-rect");
                            elSeries.add(rect);
                            rect.attr({
                                fill: color,
                                i: i,
                                index: i,
                                j: j,
                                val: series[i][j],
                                "stroke-width": this.strokeWidth,
                                stroke: stroke,
                                color: color
                            });
                            if (w.config.chart.animations.enabled && !w.globals.dataChanged) {
                                var speed = 1;
                                if (!w.globals.resized) {
                                    speed = w.config.chart.animations.speed;
                                }
                                this.animateHeatMap(rect, x1, y1, cellW, yDivision, speed, i, j);
                            }
                            if (w.globals.dataChanged) {
                                var speed1 = 1;
                                if (this.dynamicAnim.enabled && w.globals.shouldAnimate) {
                                    speed1 = this.dynamicAnim.speed;
                                    var colorFrom = w.globals.previousPaths[i] && w.globals.previousPaths[i][j] && w.globals.previousPaths[i][j].color;
                                    if (!colorFrom) colorFrom = "rgba(255, 255, 255, 0)";
                                    this.animateHeatColor(rect, Utils$1.isColorHex(colorFrom) ? colorFrom : Utils$1.rgb2hex(colorFrom), Utils$1.isColorHex(color) ? color : Utils$1.rgb2hex(color), speed1);
                                }
                            }
                        }
                        var formatter = w.config.dataLabels.formatter;
                        var formattedText = formatter(w.seriesData.series[i][j], {
                            value: w.seriesData.series[i][j],
                            seriesIndex: i,
                            dataPointIndex: j,
                            w: w
                        });
                        var dataLabels = this.helpers.calculateDataLabels({
                            text: formattedText,
                            x: x1 + cellW / 2,
                            y: y1 + yDivision / 2,
                            i: i,
                            j: j,
                            colorProps: heatColorProps,
                            series: heatSeries
                        });
                        if (dataLabels !== null) {
                            elSeries.add(dataLabels);
                        }
                        if (!isContinuousX) x1 = x1 + xDivision;
                        j++;
                    }
                    y1 = y1 + yDivision;
                    ret.add(elSeries);
                }
                var yAxisScale = /** @type {any[]} */ w.globals.yAxisScale[0].result.slice();
                if (w.config.yaxis[0].reversed) {
                    yAxisScale.unshift("");
                } else {
                    yAxisScale.push("");
                }
                w.globals.yAxisScale[0].result = yAxisScale;
                return ret;
            }
        },
        {
            /**
   * @param {any} el
   * @param {number} x
   * @param {number} y
   * @param {number} width
   * @param {number} height
   * @param {number} speed
   * @param {number} [row] - series index (heatmap row)
   * @param {number} [col] - data point index (heatmap column)
   */ key: "animateHeatMap",
            value: function animateHeatMap(el, x, y, width, height, speed) {
                var row = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : 0, col = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : 0;
                var w = this.w;
                var animations = new Animations(this.w);
                var animCfg = w.config.chart.animations;
                var gradCfg = animCfg.animateGradually;
                var staggerEnabled = gradCfg && gradCfg.enabled !== false;
                var delay = 0;
                if (staggerEnabled) {
                    var seriesCount = (w.seriesData.series || []).length || 1;
                    var pointsCount = w.globals.dataPoints || 1;
                    var maxDiag = seriesCount + pointsCount - 2;
                    var baseDelay = Math.min(gradCfg.delay || 0, speed * 0.5 / Math.max(1, maxDiag));
                    delay = computeStagger({
                        style: "diagonal",
                        index: col,
                        row: row,
                        col: col,
                        baseDelay: baseDelay
                    });
                }
                animations.animateRect(el, {
                    x: x + width / 2,
                    y: y + height / 2,
                    width: 0,
                    height: 0
                }, {
                    x: x,
                    y: y,
                    width: width,
                    height: height
                }, speed, function() {
                    animations.animationCompleted(el);
                }, delay);
            }
        },
        {
            /**
   * @param {any} el
   * @param {string} colorFrom
   * @param {string} colorTo
   * @param {number} speed
   */ key: "animateHeatColor",
            value: function animateHeatColor(el, colorFrom, colorTo, speed) {
                el.attr({
                    fill: colorFrom
                }).animate(speed).attr({
                    fill: colorTo
                });
            }
        }
    ]);
    return HeatMap;
}();
var Helpers5 = /*#__PURE__*/ function() {
    "use strict";
    function Helpers5(lineCtx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Helpers5);
        this.w = lineCtx.w;
        this.lineCtx = lineCtx;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Helpers5, [
        {
            /**
   * @param {number} i
   * @param {any[]} series
   */ key: "sameValueSeriesFix",
            value: function sameValueSeriesFix(i, series) {
                var w = this.w;
                if (w.config.fill.type === "gradient" || w.config.fill.type[i] === "gradient") {
                    var coreUtils = new CoreUtils(this.lineCtx.w);
                    if (coreUtils.seriesHaveSameValues(i)) {
                        var gSeries = series[i].slice();
                        gSeries[gSeries.length - 1] = gSeries[gSeries.length - 1] + 1e-6;
                        series[i] = gSeries;
                    }
                }
                return series;
            }
        },
        {
            /** @param {{series: any, realIndex: any, x: any, y: any, i: any, j: any, prevY: any}} opts */ key: "calculatePoints",
            value: function calculatePoints(param) {
                var series = param.series, realIndex = param.realIndex, x = param.x, y = param.y, i = param.i, j = param.j, prevY = param.prevY;
                var w = this.w;
                var ptX = [];
                var ptY = [];
                var xPT1st = this.lineCtx.categoryAxisCorrection + w.config.markers.offsetX;
                if (w.axisFlags.isXNumeric) {
                    xPT1st = (w.seriesData.seriesX[realIndex][0] - w.globals.minX) / this.lineCtx.xRatio + w.config.markers.offsetX;
                }
                if (j === 0) {
                    ptX.push(xPT1st);
                    ptY.push(Utils$1.isNumber(series[i][0]) ? prevY + w.config.markers.offsetY : null);
                }
                ptX.push(x + w.config.markers.offsetX);
                ptY.push(Utils$1.isNumber(series[i][j + 1]) ? y + w.config.markers.offsetY : null);
                return {
                    x: ptX,
                    y: ptY
                };
            }
        },
        {
            /** @param {{pathFromLine: any, pathFromArea: any, realIndex: any}} opts */ key: "checkPreviousPaths",
            value: function checkPreviousPaths(param) {
                var pathFromLine = param.pathFromLine, pathFromArea = param.pathFromArea, realIndex = param.realIndex;
                var w = this.w;
                for(var pp = 0; pp < w.globals.previousPaths.length; pp++){
                    var gpp = w.globals.previousPaths[pp];
                    if ((gpp.type === "line" || gpp.type === "area") && gpp.paths.length > 0 && parseInt(gpp.realIndex, 10) === parseInt(realIndex, 10)) {
                        if (gpp.type === "line") {
                            this.lineCtx.appendPathFrom = false;
                            pathFromLine = w.globals.previousPaths[pp].paths[0].d;
                        } else if (gpp.type === "area") {
                            this.lineCtx.appendPathFrom = false;
                            pathFromArea = w.globals.previousPaths[pp].paths[0].d;
                            if (w.config.stroke.show && w.globals.previousPaths[pp].paths[1]) {
                                pathFromLine = w.globals.previousPaths[pp].paths[1].d;
                            }
                        }
                    }
                }
                return {
                    pathFromLine: pathFromLine,
                    pathFromArea: pathFromArea
                };
            }
        },
        {
            /** @param {{i: any, realIndex: any, series: any, prevY: any, lineYPosition: any, translationsIndex: any}} opts */ key: "determineFirstPrevY",
            value: function determineFirstPrevY(param) {
                var i = param.i, realIndex = param.realIndex, series = param.series, prevY = param.prevY, lineYPosition = param.lineYPosition, translationsIndex = param.translationsIndex;
                var _a, _b, _c;
                var w = this.w;
                var stackSeries = w.config.chart.stacked && !w.globals.comboCharts || w.config.chart.stacked && w.globals.comboCharts && (!this.w.config.chart.stackOnlyBar || /** @type {any} */ ((_a = this.w.config.series[realIndex]) == null ? void 0 : _a.type) === "bar" || /** @type {any} */ ((_b = this.w.config.series[realIndex]) == null ? void 0 : _b.type) === "column");
                if (typeof ((_c = series[i]) == null ? void 0 : _c[0]) !== "undefined") {
                    if (stackSeries) {
                        if (i > 0) {
                            lineYPosition = this.lineCtx.prevSeriesY[i - 1][0];
                        } else {
                            lineYPosition = this.lineCtx.zeroY;
                        }
                    } else {
                        lineYPosition = this.lineCtx.zeroY;
                    }
                    prevY = lineYPosition - series[i][0] / this.lineCtx.yRatio[translationsIndex] + (this.lineCtx.isReversed ? series[i][0] / this.lineCtx.yRatio[translationsIndex] : 0) * 2;
                } else {
                    if (stackSeries && i > 0 && typeof series[i][0] === "undefined") {
                        for(var s = i - 1; s >= 0; s--){
                            if (series[s][0] !== null && typeof series[s][0] !== "undefined") {
                                lineYPosition = this.lineCtx.prevSeriesY[s][0];
                                prevY = lineYPosition;
                                break;
                            }
                        }
                    }
                }
                return {
                    prevY: prevY,
                    lineYPosition: lineYPosition
                };
            }
        }
    ]);
    return Helpers5;
}();
var Line = /*#__PURE__*/ function() {
    "use strict";
    function Line(w, ctx, xyRatios, isPointsChart) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Line);
        this.ctx = ctx;
        this.w = w;
        this.xyRatios = xyRatios;
        this.xRatio = 0;
        this.yRatio = [];
        this.zRatio = 0;
        this.baseLineY = [];
        this.pointsChart = !(this.w.config.chart.type !== "bubble" && this.w.config.chart.type !== "scatter") || isPointsChart;
        this.scatter = new Scatter(this.w, this.ctx);
        this.noNegatives = this.w.globals.minX === Number.MAX_VALUE;
        this.lineHelpers = new Helpers5(this);
        this.markers = new Markers(this.w, this.ctx);
        this.prevSeriesY = [];
        this.categoryAxisCorrection = 0;
        this.yaxisIndex = 0;
        this.xDivision = 0;
        this.zeroY = 0;
        this.areaBottomY = 0;
        this.strokeWidth = 0;
        this.isReversed = false;
        this.appendPathFrom = false;
        this.elSeries = null;
        this.elPointsMain = null;
        this.elDataLabelsWrap = null;
        this._elLastPointsWrap = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Line, [
        {
            /**
   * @param {any[]} series
   * @param {string} ctype
   * @param {number} seriesIndex
   * @param {any} seriesRangeEnd
   */ key: "draw",
            value: function draw(series, ctype, seriesIndex, seriesRangeEnd) {
                var _a;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var type = w.globals.comboCharts ? ctype : w.config.chart.type;
                var ret = graphics.group({
                    class: "apexcharts-".concat(type, "-series apexcharts-plot-series")
                });
                var coreUtils = new CoreUtils(this.w);
                this.yRatio = this.xyRatios.yRatio;
                this.zRatio = this.xyRatios.zRatio;
                this.xRatio = this.xyRatios.xRatio;
                this.baseLineY = this.xyRatios.baseLineY;
                series = coreUtils.getLogSeries(series);
                this.yRatio = coreUtils.getLogYRatios(this.yRatio);
                this.prevSeriesY = [];
                var allSeries = [];
                for(var i = 0; i < series.length; i++){
                    series = this.lineHelpers.sameValueSeriesFix(i, series);
                    var realIndex = w.globals.comboCharts ? /** @type {any} */ seriesIndex[i] : i;
                    var translationsIndex = this.yRatio.length > 1 ? realIndex : 0;
                    this._initSerieVariables(series, i, realIndex);
                    var yArrj = [];
                    var y2Arrj = [];
                    var xArrj = [];
                    var x = w.globals.padHorizontal + this.categoryAxisCorrection;
                    var y = 1;
                    var linePaths = [];
                    var areaPaths = [];
                    Series.addCollapsedClassToSeries(this.w, this.elSeries, realIndex);
                    if (w.axisFlags.isXNumeric && w.seriesData.seriesX.length > 0) {
                        x = (w.seriesData.seriesX[realIndex][0] - w.globals.minX) / this.xRatio;
                    }
                    xArrj.push(x);
                    var pX = x;
                    var pY2 = void 0;
                    var prevX = pX;
                    var prevY = this.zeroY;
                    var prevY2 = this.zeroY;
                    var lineYPosition = 0;
                    var firstPrevY = this.lineHelpers.determineFirstPrevY({
                        i: i,
                        realIndex: realIndex,
                        series: series,
                        prevY: prevY,
                        lineYPosition: lineYPosition,
                        translationsIndex: translationsIndex
                    });
                    prevY = firstPrevY.prevY;
                    if (w.config.stroke.curve === "monotoneCubic" && series[i][0] === null) {
                        yArrj.push(null);
                    } else {
                        yArrj.push(prevY);
                    }
                    var pY = prevY;
                    var firstPrevY2 = void 0;
                    if (type === "rangeArea") {
                        firstPrevY2 = this.lineHelpers.determineFirstPrevY({
                            i: i,
                            realIndex: realIndex,
                            series: seriesRangeEnd,
                            prevY: prevY2,
                            lineYPosition: lineYPosition,
                            translationsIndex: translationsIndex
                        });
                        prevY2 = firstPrevY2.prevY;
                        pY2 = prevY2;
                        y2Arrj.push(yArrj[0] !== null ? prevY2 : null);
                    }
                    var pathsFrom = this._calculatePathsFrom({
                        type: type,
                        series: series,
                        i: i,
                        realIndex: realIndex,
                        translationsIndex: translationsIndex,
                        prevX: prevX,
                        prevY: prevY,
                        prevY2: prevY2
                    });
                    var rYArrj = [
                        yArrj[0]
                    ];
                    var rY2Arrj = [
                        y2Arrj[0]
                    ];
                    var iteratingOpts = {
                        type: type,
                        series: series,
                        realIndex: realIndex,
                        translationsIndex: translationsIndex,
                        i: i,
                        x: x,
                        y: y,
                        pX: pX,
                        pY: pY,
                        pathsFrom: pathsFrom,
                        linePaths: linePaths,
                        areaPaths: areaPaths,
                        seriesIndex: seriesIndex,
                        lineYPosition: lineYPosition,
                        xArrj: xArrj,
                        yArrj: yArrj,
                        y2Arrj: y2Arrj,
                        seriesRangeEnd: seriesRangeEnd
                    };
                    var paths = this._iterateOverDataPoints(__spreadProps(__spreadValues({}, iteratingOpts), {
                        iterations: type === "rangeArea" ? series[i].length - 1 : void 0,
                        isRangeStart: true
                    }));
                    if (type === "rangeArea") {
                        var pathsFrom2 = this._calculatePathsFrom({
                            series: seriesRangeEnd,
                            i: i,
                            realIndex: realIndex,
                            prevX: prevX,
                            prevY: prevY2
                        });
                        var rangePaths = this._iterateOverDataPoints(__spreadProps(__spreadValues({}, iteratingOpts), {
                            series: seriesRangeEnd,
                            xArrj: [
                                x
                            ],
                            yArrj: rYArrj,
                            y2Arrj: rY2Arrj,
                            pY: pY2,
                            areaPaths: paths.areaPaths,
                            pathsFrom: pathsFrom2,
                            iterations: seriesRangeEnd[i].length - 1,
                            isRangeStart: false
                        }));
                        var segments = paths.linePaths.length / 2;
                        for(var s = 0; s < segments; s++){
                            paths.linePaths[s] = rangePaths.linePaths[s + segments] + paths.linePaths[s];
                        }
                        paths.linePaths.splice(segments);
                        paths.pathFromLine = rangePaths.pathFromLine + paths.pathFromLine;
                    } else if (!/z\s*$/i.test(paths.pathFromArea)) {
                        paths.pathFromArea += "z";
                    }
                    this._handlePaths({
                        type: type,
                        realIndex: realIndex,
                        i: i,
                        paths: paths
                    });
                    this.elSeries.add(this.elPointsMain);
                    this.elSeries.add(this.elDataLabelsWrap);
                    allSeries.push(this.elSeries);
                }
                if (typeof /** @type {Record<string,any>} */ ((_a = w.config.series[0]) == null ? void 0 : _a.zIndex) !== "undefined") {
                    allSeries.sort(function(a, b) {
                        return Number(a.node.getAttribute("zIndex")) - Number(b.node.getAttribute("zIndex"));
                    });
                }
                if (w.config.chart.stacked) {
                    for(var s1 = allSeries.length - 1; s1 >= 0; s1--){
                        ret.add(allSeries[s1]);
                    }
                } else {
                    for(var s2 = 0; s2 < allSeries.length; s2++){
                        ret.add(allSeries[s2]);
                    }
                }
                return ret;
            }
        },
        {
            /**
   * @param {any[]} series
   * @param {number} i
   * @param {number} realIndex
   */ key: "_initSerieVariables",
            value: function _initSerieVariables(series, i, realIndex) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                this.xDivision = w.layout.gridWidth / (w.globals.dataPoints - (w.config.xaxis.tickPlacement === "on" ? 1 : 0));
                this.strokeWidth = Array.isArray(w.config.stroke.width) ? w.config.stroke.width[realIndex] : w.config.stroke.width;
                var translationsIndex = 0;
                if (this.yRatio.length > 1) {
                    this.yaxisIndex = w.globals.seriesYAxisReverseMap[realIndex];
                    translationsIndex = realIndex;
                }
                this.isReversed = w.config.yaxis[this.yaxisIndex] && w.config.yaxis[this.yaxisIndex].reversed;
                this.zeroY = w.layout.gridHeight - this.baseLineY[translationsIndex] - (this.isReversed ? w.layout.gridHeight : 0) + (this.isReversed ? this.baseLineY[translationsIndex] * 2 : 0);
                this.areaBottomY = this.zeroY;
                if (this.zeroY > w.layout.gridHeight || w.config.plotOptions.area.fillTo === "end") {
                    this.areaBottomY = w.layout.gridHeight;
                }
                this.categoryAxisCorrection = this.xDivision / 2;
                var seriesItem = /** @type {Record<string,any>} */ w.config.series[realIndex];
                this.elSeries = graphics.group({
                    class: "apexcharts-series",
                    zIndex: typeof seriesItem.zIndex !== "undefined" ? seriesItem.zIndex : realIndex,
                    seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex])
                });
                this.elPointsMain = graphics.group({
                    class: "apexcharts-series-markers-wrap",
                    "data:realIndex": realIndex
                });
                this.markers.resetSeriesWrapCache();
                this._elLastPointsWrap = null;
                if (w.globals.hasNullValues) {
                    var firstPoint = this.markers.plotChartMarkers({
                        pointsPos: {
                            x: [
                                0
                            ],
                            y: [
                                w.layout.gridHeight + w.globals.markers.largestSize
                            ]
                        },
                        seriesIndex: i,
                        j: 0,
                        pSize: 0.1,
                        alwaysDrawMarker: true,
                        isVirtualPoint: true
                    });
                    if (firstPoint !== null) {
                        this.elPointsMain.add(firstPoint);
                    }
                }
                this.elDataLabelsWrap = graphics.group({
                    class: "apexcharts-datalabels",
                    "data:realIndex": realIndex
                });
                var longestSeries = series[i].length === w.globals.dataPoints;
                this.elSeries.attr({
                    "data:longestSeries": longestSeries,
                    rel: i + 1,
                    "data:realIndex": realIndex
                });
                this.appendPathFrom = true;
            }
        },
        {
            /** @param {{ type?: any, series?: any, i?: any, realIndex?: any, translationsIndex?: any, prevX?: any, prevY?: any, prevY2?: any }} opts */ key: "_calculatePathsFrom",
            value: function _calculatePathsFrom(param) {
                var type = param.type, series = param.series, i = param.i, realIndex = param.realIndex, translationsIndex = param.translationsIndex, prevX = param.prevX, prevY = param.prevY, prevY2 = param.prevY2;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var linePath, areaPath, pathFromLine, pathFromArea;
                if (series[i][0] === null) {
                    for(var s = 0; s < series[i].length; s++){
                        if (series[i][s] !== null) {
                            prevX = this.xDivision * s;
                            prevY = this.zeroY - series[i][s] / this.yRatio[translationsIndex];
                            linePath = graphics.move(prevX, prevY);
                            areaPath = graphics.move(prevX, this.areaBottomY);
                            break;
                        }
                    }
                } else {
                    linePath = graphics.move(prevX, prevY);
                    if (type === "rangeArea") {
                        linePath = graphics.move(prevX, prevY2) + graphics.line(prevX, prevY);
                    }
                    areaPath = graphics.move(prevX, this.areaBottomY) + graphics.line(prevX, prevY);
                }
                pathFromLine = graphics.move(0, this.areaBottomY) + graphics.line(0, this.areaBottomY);
                pathFromArea = graphics.move(0, this.areaBottomY) + graphics.line(0, this.areaBottomY);
                if (w.globals.previousPaths.length > 0) {
                    var pathFrom = this.lineHelpers.checkPreviousPaths({
                        pathFromLine: pathFromLine,
                        pathFromArea: pathFromArea,
                        realIndex: realIndex
                    });
                    pathFromLine = pathFrom.pathFromLine;
                    pathFromArea = pathFrom.pathFromArea;
                }
                return {
                    prevX: prevX,
                    prevY: prevY,
                    linePath: linePath,
                    areaPath: areaPath,
                    pathFromLine: pathFromLine,
                    pathFromArea: pathFromArea
                };
            }
        },
        {
            /** @param {{type: any, realIndex: any, i: any, paths: any}} opts */ key: "_handlePaths",
            value: function _handlePaths(param) {
                var type = param.type, realIndex = param.realIndex, i = param.i, paths = param.paths;
                var _a, _b, _c, _d, _e, _f, _g;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var emit = seriesEmitter(this.ctx, graphics);
                var fill = new Fill(this.w);
                this.prevSeriesY.push(paths.yArrj);
                var streamScroll = null;
                if ((type === "line" || type === "area") && w.globals.dataChanged) {
                    streamScroll = detectStreamScroll(w, realIndex, paths.xArrj, paths.yArrj);
                }
                var reconcile = null;
                if (!streamScroll && (type === "line" || type === "area")) {
                    reconcile = reconcileSeriesPaths(w, {
                        type: type,
                        realIndex: realIndex,
                        pathFromLine: paths.pathFromLine,
                        pathFromArea: paths.pathFromArea,
                        linePaths: paths.linePaths,
                        areaPaths: paths.areaPaths
                    });
                }
                w.globals.seriesXvalues[realIndex] = paths.xArrj;
                w.globals.seriesYvalues[realIndex] = paths.yArrj;
                var forecast = w.config.forecastDataPoints;
                if (forecast.count > 0 && type !== "rangeArea") {
                    var forecastCutoff = w.globals.seriesXvalues[realIndex][w.globals.seriesXvalues[realIndex].length - forecast.count - 1];
                    var elForecastMask = graphics.drawRect(forecastCutoff, 0, w.layout.gridWidth, w.layout.gridHeight, 0);
                    w.dom.elForecastMask.appendChild(elForecastMask.node);
                    var elNonForecastMask = graphics.drawRect(0, 0, forecastCutoff, w.layout.gridHeight, 0);
                    w.dom.elNonForecastMask.appendChild(elNonForecastMask.node);
                }
                if (!this.pointsChart) {
                    w.globals.delayedElements.push({
                        el: this.elPointsMain.node,
                        index: realIndex
                    });
                    tweenSeriesMarkers(w, {
                        elPointsMain: this.elPointsMain,
                        realIndex: realIndex,
                        speed: w.config.chart.animations.dynamicAnimation.speed
                    });
                    if (seriesJoin(w, realIndex) && ((_a = this.elDataLabelsWrap) == null ? void 0 : _a.node)) {
                        this.elDataLabelsWrap.node.classList.add("apexcharts-element-hidden");
                        w.globals.delayedElements.push({
                            el: this.elDataLabelsWrap.node,
                            holdUntilComplete: true
                        });
                    }
                } else {
                    tweenSeriesMarkers(w, {
                        elPointsMain: this.elPointsMain,
                        realIndex: realIndex,
                        speed: w.config.chart.animations.dynamicAnimation.speed
                    });
                }
                var defaultRenderedPathOptions = {
                    i: i,
                    realIndex: realIndex,
                    animationDelay: i,
                    initialSpeed: w.config.chart.animations.speed,
                    dataChangeSpeed: w.config.chart.animations.dynamicAnimation.speed,
                    className: "apexcharts-".concat(type)
                };
                var numericXY = paths.numericXY;
                if (type === "area") {
                    var pathFill = fill.fillPath({
                        seriesNumber: realIndex
                    });
                    for(var p = 0; p < paths.areaPaths.length; p++){
                        var renderedPath = emit.renderPaths(__spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
                            pathFrom: streamScroll ? projectPathToPrevFrame(paths.areaPaths[p], streamScroll) : (_c = (_b = reconcile == null ? void 0 : reconcile.area) == null ? void 0 : _b.from) != null ? _c : paths.pathFromArea,
                            pathTo: paths.areaPaths[p],
                            pathToNumeric: numericXY ? {
                                xs: numericXY.xs,
                                ys: numericXY.ys,
                                closeY: numericXY.areaCloseY
                            } : void 0,
                            pathToInterp: (_d = reconcile == null ? void 0 : reconcile.area) == null ? void 0 : _d.toInterp,
                            scrollMorph: !!streamScroll,
                            stroke: "none",
                            strokeWidth: 0,
                            strokeLineCap: null,
                            fill: pathFill
                        }));
                        this.elSeries.add(renderedPath);
                    }
                }
                if (w.config.stroke.show && !this.pointsChart) {
                    var lineFill = null;
                    if (type === "line") {
                        lineFill = fill.fillPath({
                            seriesNumber: realIndex,
                            i: i
                        });
                    } else {
                        if (w.config.stroke.fill.type === "solid") {
                            lineFill = w.globals.stroke.colors[realIndex];
                        } else {
                            var prevFill = w.config.fill;
                            w.config.fill = w.config.stroke.fill;
                            lineFill = fill.fillPath({
                                seriesNumber: realIndex,
                                i: i
                            });
                            w.config.fill = prevFill;
                        }
                    }
                    for(var p1 = 0; p1 < paths.linePaths.length; p1++){
                        var pathFill1 = lineFill;
                        if (type === "rangeArea") {
                            pathFill1 = fill.fillPath({
                                seriesNumber: realIndex
                            });
                        }
                        var linePathCommonOpts = __spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
                            pathFrom: streamScroll ? projectPathToPrevFrame(paths.linePaths[p1], streamScroll) : (_f = (_e = reconcile == null ? void 0 : reconcile.line) == null ? void 0 : _e.from) != null ? _f : paths.pathFromLine,
                            pathTo: paths.linePaths[p1],
                            pathToNumeric: numericXY ? {
                                xs: numericXY.xs,
                                ys: numericXY.ys
                            } : void 0,
                            pathToInterp: (_g = reconcile == null ? void 0 : reconcile.line) == null ? void 0 : _g.toInterp,
                            scrollMorph: !!streamScroll,
                            stroke: lineFill,
                            strokeWidth: this.strokeWidth,
                            strokeLineCap: w.config.stroke.lineCap,
                            fill: type === "rangeArea" ? pathFill1 : "none"
                        });
                        var renderedPath1 = emit.renderPaths(linePathCommonOpts);
                        this.elSeries.add(renderedPath1);
                        renderedPath1.attr("fill-rule", "evenodd");
                        if (forecast.count > 0 && type !== "rangeArea") {
                            var renderedForecastPath = emit.renderPaths(linePathCommonOpts);
                            renderedForecastPath.node.setAttribute("stroke-dasharray", forecast.dashArray);
                            if (forecast.strokeWidth) {
                                renderedForecastPath.node.setAttribute("stroke-width", forecast.strokeWidth);
                            }
                            this.elSeries.add(renderedForecastPath);
                            renderedForecastPath.attr("clip-path", "url(#forecastMask".concat(w.globals.cuid, ")"));
                            renderedPath1.attr("clip-path", "url(#nonForecastMask".concat(w.globals.cuid, ")"));
                        }
                    }
                }
            }
        },
        {
            key: "_iterateOverDataPoints",
            value: function _iterateOverDataPoints(param) {
                var _this = this;
                var type = param.type, series = param.series, iterations = param.iterations, realIndex = param.realIndex, translationsIndex = param.translationsIndex, i = param.i, x = param.x, y = param.y, pX = param.pX, pY = param.pY, pathsFrom = param.pathsFrom, linePaths = param.linePaths, areaPaths = param.areaPaths, seriesIndex = param.seriesIndex, lineYPosition = param.lineYPosition, xArrj = param.xArrj, yArrj = param.yArrj, y2Arrj = param.y2Arrj, isRangeStart = param.isRangeStart, seriesRangeEnd = param.seriesRangeEnd;
                var _a, _b;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var yRatio = this.yRatio;
                var prevY = pathsFrom.prevY, linePath = pathsFrom.linePath, areaPath = pathsFrom.areaPath, pathFromLine = pathsFrom.pathFromLine, pathFromArea = pathsFrom.pathFromArea;
                var minY = Utils$1.isNumber(w.globals.minYArr[realIndex]) ? w.globals.minYArr[realIndex] : w.globals.minY;
                if (!iterations) {
                    iterations = w.globals.dataPoints > 1 ? w.globals.dataPoints - 1 : w.globals.dataPoints;
                }
                var getY = function getY(_y, lineYPos) {
                    return lineYPos - _y / yRatio[translationsIndex] + (_this.isReversed ? _y / yRatio[translationsIndex] : 0) * 2;
                };
                var y2 = y;
                var stackSeries = w.config.chart.stacked && !w.globals.comboCharts || w.config.chart.stacked && w.globals.comboCharts && (!this.w.config.chart.stackOnlyBar || /** @type {Record<string,any>} */ ((_a = this.w.config.series[realIndex]) == null ? void 0 : _a.type) === "bar" || /** @type {Record<string,any>} */ ((_b = this.w.config.series[realIndex]) == null ? void 0 : _b.type) === "column");
                var curve = w.config.stroke.curve;
                if (Array.isArray(curve)) {
                    if (Array.isArray(seriesIndex)) {
                        curve = curve[seriesIndex[i]];
                    } else {
                        curve = curve[i];
                    }
                }
                var pathState = 0;
                var segmentStartX;
                var jitterPx = this.pointsChart ? this._scatterJitterPx(realIndex) : null;
                if (curve === "straight" && !this.pointsChart) {
                    var fast = this._fastStraightPath({
                        type: type,
                        series: series,
                        i: i,
                        realIndex: realIndex,
                        translationsIndex: translationsIndex,
                        iterations: iterations,
                        x: x,
                        y: y,
                        pX: pX,
                        pY: pY,
                        pathsFrom: pathsFrom,
                        linePaths: linePaths,
                        areaPaths: areaPaths,
                        xArrj: xArrj,
                        yArrj: yArrj,
                        y2Arrj: y2Arrj,
                        stackSeries: stackSeries
                    });
                    if (fast) return fast;
                }
                for(var j = 0; j < iterations; j++){
                    if (series[i].length === 0) break;
                    var isNull = typeof series[i][j + 1] === "undefined" || series[i][j + 1] === null;
                    if (w.axisFlags.isXNumeric) {
                        var sX = w.seriesData.seriesX[realIndex][j + 1];
                        if (typeof w.seriesData.seriesX[realIndex][j + 1] === "undefined") {
                            sX = w.seriesData.seriesX[realIndex][iterations - 1];
                        }
                        x = (sX - w.globals.minX) / this.xRatio;
                    } else {
                        x = x + this.xDivision;
                    }
                    if (stackSeries) {
                        if (i > 0 && w.globals.collapsedSeries.length < w.config.series.length - 1) {
                            var prevIndex = function prevIndex(pi) {
                                for(var pii = pi; pii > 0; pii--){
                                    if (w.globals.collapsedSeriesIndices.indexOf((seriesIndex == null ? void 0 : seriesIndex[pii]) || pii) > -1) {
                                        pii--;
                                    } else {
                                        return pii;
                                    }
                                }
                                return 0;
                            };
                            lineYPosition = this.prevSeriesY[prevIndex(i - 1)][j + 1];
                        } else {
                            lineYPosition = this.zeroY;
                        }
                    } else {
                        lineYPosition = this.zeroY;
                    }
                    if (isNull) {
                        y = getY(minY, lineYPosition);
                    } else {
                        y = getY(series[i][j + 1], lineYPosition);
                        if (type === "rangeArea") {
                            y2 = getY(seriesRangeEnd[i][j + 1], lineYPosition);
                        }
                    }
                    var xj = x;
                    var yj = y;
                    if (jitterPx) {
                        var seed = realIndex * 100003 + (j + 1);
                        if (jitterPx.x) xj = x + (hash01(seed * 7919 + 13) - 0.5) * 2 * jitterPx.x;
                        if (jitterPx.y) yj = y + (hash01(seed * 6271 + 97) - 0.5) * 2 * jitterPx.y;
                    }
                    xArrj.push(series[i][j + 1] === null ? null : xj);
                    if (isNull && (w.config.stroke.curve === "smooth" || w.config.stroke.curve === "monotoneCubic")) {
                        yArrj.push(null);
                        y2Arrj.push(null);
                    } else {
                        yArrj.push(yj);
                        y2Arrj.push(y2);
                    }
                    var pointsPos = this.lineHelpers.calculatePoints({
                        series: series,
                        x: xj,
                        y: yj,
                        realIndex: realIndex,
                        i: i,
                        j: j,
                        prevY: prevY
                    });
                    var calculatedPaths = this._createPaths({
                        type: type,
                        series: series,
                        i: i,
                        j: j,
                        x: x,
                        y: y,
                        y2: y2,
                        xArrj: xArrj,
                        yArrj: yArrj,
                        y2Arrj: y2Arrj,
                        pX: pX,
                        pY: pY,
                        pathState: pathState,
                        segmentStartX: segmentStartX,
                        linePath: linePath,
                        areaPath: areaPath,
                        linePaths: linePaths,
                        areaPaths: areaPaths,
                        curve: curve,
                        isRangeStart: isRangeStart
                    });
                    areaPaths = calculatedPaths.areaPaths;
                    linePaths = calculatedPaths.linePaths;
                    pX = calculatedPaths.pX;
                    pY = calculatedPaths.pY;
                    pathState = calculatedPaths.pathState;
                    segmentStartX = calculatedPaths.segmentStartX;
                    areaPath = calculatedPaths.areaPath;
                    linePath = calculatedPaths.linePath;
                    if (this.appendPathFrom && !w.globals.hasNullValues && !(curve === "monotoneCubic" && type === "rangeArea")) {
                        pathFromLine += graphics.line(x, this.areaBottomY);
                        pathFromArea += graphics.line(x, this.areaBottomY);
                    }
                    this.handleNullDataPoints(series, pointsPos, i, j, realIndex);
                    this._handleMarkersAndLabels({
                        type: type,
                        pointsPos: pointsPos,
                        i: i,
                        j: j,
                        realIndex: realIndex,
                        isRangeStart: isRangeStart
                    });
                }
                return {
                    yArrj: yArrj,
                    xArrj: xArrj,
                    pathFromArea: pathFromArea,
                    areaPaths: areaPaths,
                    pathFromLine: pathFromLine,
                    linePaths: linePaths,
                    linePath: linePath,
                    areaPath: areaPath
                };
            }
        },
        {
            /** @param {{type: any, pointsPos: any, isRangeStart: any, i: any, j: any, realIndex: any}} opts */ key: "_handleMarkersAndLabels",
            value: function _handleMarkersAndLabels(param) {
                var type = param.type, pointsPos = param.pointsPos, isRangeStart = param.isRangeStart, i = param.i, j = param.j, realIndex = param.realIndex;
                var w = this.w;
                var dataLabels = new DataLabels(this.w, this.ctx);
                if (!this.pointsChart) {
                    var useProgressive = !w.globals.dataChanged && !w.globals.resized;
                    if (!useProgressive && w.seriesData.series[i].length > 1) {
                        this.elPointsMain.node.classList.add("apexcharts-element-hidden");
                    }
                    var elPointsWrap = this.markers.plotChartMarkers({
                        pointsPos: pointsPos,
                        seriesIndex: realIndex,
                        j: j + 1
                    });
                    if (elPointsWrap !== null && elPointsWrap !== this._elLastPointsWrap) {
                        this.elPointsMain.add(elPointsWrap);
                        this._elLastPointsWrap = elPointsWrap;
                    }
                } else {
                    this.scatter.draw(this.elSeries, j, {
                        realIndex: realIndex,
                        pointsPos: pointsPos,
                        zRatio: this.zRatio,
                        elParent: this.elPointsMain
                    });
                }
                var drawnLabels = dataLabels.drawDataLabel({
                    type: type,
                    isRangeStart: isRangeStart,
                    pos: pointsPos,
                    i: realIndex,
                    j: j + 1
                });
                if (drawnLabels !== null) {
                    this.elDataLabelsWrap.add(drawnLabels);
                }
            }
        },
        {
            /**
   * Max scatter-jitter offsets in pixels for this series, or null when jitter is
   * off. The config offsets are in axis units (x: 1 = one category step / x-data
   * unit, y: 1 = one y-data unit); convert each to pixels using the chart's
   * ratios. The actual per-point offset is a deterministic fraction of these
   * (see Scatter.drawPoint).
   * @param {number} realIndex
   * @returns {{ x: number, y: number } | null}
   */ key: "_scatterJitterPx",
            value: function _scatterJitterPx(realIndex) {
                var _a;
                var w = this.w;
                var jt = (_a = w.config.plotOptions.scatter) == null ? void 0 : _a.jitter;
                if (!jt || !jt.enabled || !jt.x && !jt.y) return null;
                var xUnitPx = w.axisFlags.isXNumeric && this.xRatio ? 1 / this.xRatio : this.xDivision;
                var ti = this.yRatio.length > 1 ? realIndex : 0;
                var yUnitPx = this.yRatio[ti] ? 1 / this.yRatio[ti] : 0;
                return {
                    x: (jt.x || 0) * xUnitPx,
                    y: (jt.y || 0) * yUnitPx
                };
            }
        },
        {
            /**
   * Numeric geometry fast path for plain straight line/area series (the
   * render-2026 perf work). Eligibility is strict: everything the per-point
   * slow loop can do beyond plain geometry (null gaps, markers, data labels,
   * discrete markers, stacking, combos, range areas) bails to the state
   * machine. When eligible it produces the SAME outputs as the slow loop
   * (byte-identical d strings via join, the same xArrj/yArrj/y2Arrj
   * pushes, and the same pointsArray tooltip cache) in one tight loop.
   * In canvas mode it additionally emits typed-array coordinates so the
   * renderer can paint via moveTo/lineTo without a Path2D d-string parse.
   *
   * @param {{type: any, series: any, i: number, realIndex: number,
   *   translationsIndex: number, iterations: number, x: number, y: number,
   *   pX: number, pY: number, pathsFrom: any, linePaths: any[],
   *   areaPaths: any[], xArrj: any[], yArrj: any[], y2Arrj: any[],
   *   stackSeries: boolean}} opts
   * @returns {any} the _iterateOverDataPoints result, or null when ineligible
   */ key: "_fastStraightPath",
            value: function _fastStraightPath(param) {
                var type = param.type, series = param.series, i = param.i, realIndex = param.realIndex, translationsIndex = param.translationsIndex, iterations = param.iterations, x = param.x, y = param.y, pX = param.pX, pY = param.pY, pathsFrom = param.pathsFrom, linePaths = param.linePaths, areaPaths = param.areaPaths, xArrj = param.xArrj, yArrj = param.yArrj, y2Arrj = param.y2Arrj, stackSeries = param.stackSeries;
                var w = this.w;
                if (type !== "line" && type !== "area") return null;
                if (w.globals.comboCharts || stackSeries) return null;
                if (w.config.dataLabels.enabled) return null;
                if (w.config.markers.discrete.length) return null;
                if (w.globals.markers.size[realIndex] > 0) return null;
                var s = series[i];
                var n = s.length;
                if (!iterations || n < 2 || n - 1 !== iterations) return null;
                for(var k = 0; k <= iterations; k++){
                    var v = s[k];
                    if (v === null || typeof v === "undefined") return null;
                }
                var isXNumeric = w.axisFlags.isXNumeric;
                var sx = isXNumeric ? w.seriesData.seriesX[realIndex] : null;
                if (isXNumeric && (!sx || sx.length < n)) return null;
                var yR = this.yRatio[translationsIndex];
                var isReversed = this.isReversed;
                var zeroY = this.zeroY;
                var bottomY = this.areaBottomY;
                var xRatio = this.xRatio;
                var minX = w.globals.minX;
                var xDivision = this.xDivision;
                var offX = w.config.markers.offsetX;
                var offY = w.config.markers.offsetY;
                var appendFrom = this.appendPathFrom && !w.globals.hasNullValues;
                var pathFromLine = pathsFrom.pathFromLine, pathFromArea = pathsFrom.pathFromArea;
                var r = this.ctx && this.ctx.renderer;
                var canvasMode = !!(r && r.kind && r.kind !== "svg");
                var buildStrings = !canvasMode || w.globals.dataChanged && !!w.globals.prevStreamFrame;
                var nxs = canvasMode ? new Float64Array(n) : null;
                var nys = canvasMode ? new Float64Array(n) : null;
                if (nxs && nys) {
                    nxs[0] = pX;
                    nys[0] = pY;
                }
                if (typeof w.globals.pointsArray[realIndex] === "undefined") {
                    w.globals.pointsArray[realIndex] = [];
                }
                var pts = w.globals.pointsArray[realIndex];
                var xPT1st = sx ? (sx[0] - minX) / xRatio + offX : this.categoryAxisCorrection + offX;
                pts.push([
                    xPT1st,
                    pathsFrom.prevY + offY
                ]);
                var parts = buildStrings ? new Array(iterations + 1) : [];
                if (buildStrings) parts[0] = "M " + pX + " " + pY;
                var fromParts = buildStrings && appendFrom ? new Array(iterations) : null;
                var xv = x;
                var xj = pX;
                var yj = pY;
                for(var j = 0; j < iterations; j++){
                    if (sx) {
                        xj = (sx[j + 1] - minX) / xRatio;
                    } else {
                        xv = xv + xDivision;
                        xj = xv;
                    }
                    var v1 = s[j + 1];
                    yj = zeroY - v1 / yR + (isReversed ? v1 / yR : 0) * 2;
                    xArrj.push(xj);
                    yArrj.push(yj);
                    y2Arrj.push(y);
                    pts.push([
                        xj + offX,
                        yj + offY
                    ]);
                    if (buildStrings) {
                        parts[j + 1] = " L " + xj + " " + yj;
                        if (fromParts) fromParts[j] = " L " + xj + " " + bottomY;
                    }
                    if (nxs && nys) {
                        nxs[j + 1] = xj;
                        nys[j + 1] = yj;
                    }
                }
                var linePath = "";
                var areaPath = "";
                if (buildStrings) {
                    linePath = parts.join("");
                    areaPath = linePath + " L " + xj + " " + bottomY + " L " + pX + " " + bottomY + "z";
                }
                if (fromParts) {
                    var fromAppend = fromParts.join("");
                    pathFromLine += fromAppend;
                    pathFromArea += fromAppend;
                } else if (!buildStrings && appendFrom) {
                    pathFromLine += " L " + xj + " " + bottomY;
                    pathFromArea += " L " + xj + " " + bottomY;
                }
                linePaths.push(linePath);
                areaPaths.push(areaPath);
                return {
                    yArrj: yArrj,
                    xArrj: xArrj,
                    pathFromArea: pathFromArea,
                    areaPaths: areaPaths,
                    pathFromLine: pathFromLine,
                    linePaths: linePaths,
                    linePath: linePath,
                    areaPath: areaPath,
                    numericXY: nxs ? {
                        xs: nxs,
                        ys: nys,
                        areaCloseY: bottomY
                    } : void 0
                };
            }
        },
        {
            /** @param {{type: any, series: any, i: any, j: any, x: any, y: any, xArrj: any, yArrj: any, y2: any, y2Arrj: any, pX: any, pY: any, pathState: any, segmentStartX: any, linePath: any, areaPath: any, linePaths: any, areaPaths: any, curve: any, isRangeStart: any}} opts */ key: "_createPaths",
            value: function _createPaths(param) {
                var type = param.type, series = param.series, i = param.i, j = param.j, x = param.x, y = param.y, xArrj = param.xArrj, yArrj = param.yArrj, y2 = param.y2, y2Arrj = param.y2Arrj, pX = param.pX, pY = param.pY, pathState = param.pathState, segmentStartX = param.segmentStartX, linePath = param.linePath, areaPath = param.areaPath, linePaths = param.linePaths, areaPaths = param.areaPaths, curve = param.curve, isRangeStart = param.isRangeStart;
                var graphics = new Graphics(this.w);
                var areaBottomY = this.areaBottomY;
                var rangeArea = type === "rangeArea";
                var isLowerRangeAreaPath = type === "rangeArea" && isRangeStart;
                switch(curve){
                    case "monotoneCubic":
                        {
                            var yAj = isRangeStart ? yArrj : y2Arrj;
                            var getSmoothInputs = function getSmoothInputs(xArr, yArr) {
                                return xArr.map(function(_, i2) {
                                    return [
                                        _,
                                        yArr[i2]
                                    ];
                                }).filter(function(_) {
                                    return _[1] !== null;
                                });
                            };
                            var getSegmentLengths = function getSegmentLengths(yArr) {
                                var segLens = [];
                                var count = 0;
                                yArr.forEach(function(_) {
                                    if (_ !== null) {
                                        count++;
                                    } else if (count > 0) {
                                        segLens.push(count);
                                        count = 0;
                                    }
                                });
                                if (count > 0) {
                                    segLens.push(count);
                                }
                                return segLens;
                            };
                            var getSegments = function getSegments(yArr, points) {
                                var segLens = getSegmentLengths(yArr);
                                var segments = [];
                                for(var i2 = 0, len = 0; i2 < segLens.length; len += segLens[i2++]){
                                    segments[i2] = spline.slice(points, len, len + segLens[i2]);
                                }
                                return segments;
                            };
                            switch(pathState){
                                case 0:
                                    if (yAj[j + 1] === null) {
                                        break;
                                    }
                                    pathState = 1;
                                // falls through
                                case 1:
                                    if (!(rangeArea ? xArrj.length === series[i].length : j === series[i].length - 2)) {
                                        break;
                                    }
                                // falls through
                                case 2:
                                    {
                                        var _xAj = isRangeStart ? xArrj : xArrj.slice().reverse();
                                        var _yAj = isRangeStart ? yAj : yAj.slice().reverse();
                                        var smoothInputs = getSmoothInputs(_xAj, _yAj);
                                        var points = smoothInputs.length > 1 ? spline.points(smoothInputs) : smoothInputs;
                                        var smoothInputsLower = [];
                                        if (rangeArea) {
                                            if (isLowerRangeAreaPath) {
                                                areaPaths = smoothInputs;
                                            } else {
                                                smoothInputsLower = areaPaths.reverse();
                                            }
                                        }
                                        var segmentCount = 0;
                                        var smoothInputsIndex = 0;
                                        getSegments(_yAj, points).forEach(function(_) {
                                            segmentCount++;
                                            var svgPoints = svgPath(_);
                                            var _start = smoothInputsIndex;
                                            smoothInputsIndex += _.length;
                                            var _end = smoothInputsIndex - 1;
                                            if (isLowerRangeAreaPath) {
                                                linePath = graphics.move(smoothInputs[_start][0], smoothInputs[_start][1]) + svgPoints;
                                            } else if (rangeArea) {
                                                linePath = graphics.move(smoothInputsLower[_start][0], smoothInputsLower[_start][1]) + graphics.line(smoothInputs[_start][0], smoothInputs[_start][1]) + svgPoints + graphics.line(smoothInputsLower[_end][0], smoothInputsLower[_end][1]);
                                            } else {
                                                linePath = graphics.move(smoothInputs[_start][0], smoothInputs[_start][1]) + svgPoints;
                                                areaPath = linePath + graphics.line(smoothInputs[_end][0], areaBottomY) + graphics.line(smoothInputs[_start][0], areaBottomY) + "z";
                                                areaPaths.push(areaPath);
                                            }
                                            linePaths.push(linePath);
                                        });
                                        if (rangeArea && segmentCount > 1 && !isLowerRangeAreaPath) {
                                            var upperLinePaths = linePaths.slice(segmentCount).reverse();
                                            linePaths.splice(segmentCount);
                                            upperLinePaths.forEach(function(u) {
                                                return linePaths.push(u);
                                            });
                                        }
                                        pathState = 0;
                                        break;
                                    }
                            }
                            break;
                        }
                    case "smooth":
                        {
                            var length = (x - pX) * 0.35;
                            if (series[i][j] === null) {
                                pathState = 0;
                            } else {
                                switch(pathState){
                                    case 0:
                                        segmentStartX = pX;
                                        if (isLowerRangeAreaPath) {
                                            linePath = graphics.move(pX, y2Arrj[j]) + graphics.line(pX, pY);
                                        } else {
                                            linePath = graphics.move(pX, pY);
                                        }
                                        areaPath = graphics.move(pX, pY);
                                        if (series[i][j + 1] === null || typeof series[i][j + 1] === "undefined") {
                                            linePaths.push(linePath);
                                            areaPaths.push(areaPath);
                                            break;
                                        }
                                        pathState = 1;
                                        if (j < series[i].length - 2) {
                                            var p = graphics.curve(pX + length, pY, x - length, y, x, y);
                                            linePath += p;
                                            areaPath += p;
                                            break;
                                        }
                                    // falls through
                                    case 1:
                                        if (series[i][j + 1] === null) {
                                            if (isLowerRangeAreaPath) {
                                                linePath += graphics.line(pX, y2);
                                            } else {
                                                linePath += graphics.move(pX, pY);
                                            }
                                            areaPath += graphics.line(pX, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                                            linePaths.push(linePath);
                                            areaPaths.push(areaPath);
                                            pathState = -1;
                                        } else {
                                            var p1 = graphics.curve(pX + length, pY, x - length, y, x, y);
                                            linePath += p1;
                                            areaPath += p1;
                                            if (j >= series[i].length - 2) {
                                                if (isLowerRangeAreaPath) {
                                                    linePath += graphics.curve(x, y, x, y, x, y2) + graphics.move(x, y2);
                                                }
                                                areaPath += graphics.curve(x, y, x, y, x, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                                                linePaths.push(linePath);
                                                areaPaths.push(areaPath);
                                                pathState = -1;
                                            }
                                        }
                                        break;
                                }
                            }
                            pX = x;
                            pY = y;
                            break;
                        }
                    default:
                        {
                            var pathToPoint = function pathToPoint(curve2, x2, y3) {
                                var path = "";
                                switch(curve2){
                                    case "stepline":
                                        path = graphics.line(x2, null, "H") + graphics.line(null, y3, "V");
                                        break;
                                    case "linestep":
                                        path = graphics.line(null, y3, "V") + graphics.line(x2, null, "H");
                                        break;
                                    case "straight":
                                        path = graphics.line(x2, y3);
                                        break;
                                }
                                return path;
                            };
                            if (series[i][j] === null) {
                                pathState = 0;
                            } else {
                                switch(pathState){
                                    case 0:
                                        segmentStartX = pX;
                                        if (isLowerRangeAreaPath) {
                                            linePath = graphics.move(pX, y2Arrj[j]) + graphics.line(pX, pY);
                                        } else {
                                            linePath = graphics.move(pX, pY);
                                        }
                                        areaPath = graphics.move(pX, pY);
                                        if (series[i][j + 1] === null || typeof series[i][j + 1] === "undefined") {
                                            linePaths.push(linePath);
                                            areaPaths.push(areaPath);
                                            break;
                                        }
                                        pathState = 1;
                                        if (j < series[i].length - 2) {
                                            var p2 = pathToPoint(curve, x, y);
                                            linePath += p2;
                                            areaPath += p2;
                                            break;
                                        }
                                    // falls through
                                    case 1:
                                        if (series[i][j + 1] === null) {
                                            if (isLowerRangeAreaPath) {
                                                linePath += graphics.line(pX, y2);
                                            } else {
                                                linePath += graphics.move(pX, pY);
                                            }
                                            areaPath += graphics.line(pX, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                                            linePaths.push(linePath);
                                            areaPaths.push(areaPath);
                                            pathState = -1;
                                        } else {
                                            var p3 = pathToPoint(curve, x, y);
                                            linePath += p3;
                                            areaPath += p3;
                                            if (j >= series[i].length - 2) {
                                                if (isLowerRangeAreaPath) {
                                                    linePath += graphics.line(x, y2);
                                                }
                                                areaPath += graphics.line(x, areaBottomY) + graphics.line(segmentStartX, areaBottomY) + "z";
                                                linePaths.push(linePath);
                                                areaPaths.push(areaPath);
                                                pathState = -1;
                                            }
                                        }
                                        break;
                                }
                            }
                            pX = x;
                            pY = y;
                            break;
                        }
                }
                return {
                    linePaths: linePaths,
                    areaPaths: areaPaths,
                    pX: pX,
                    pY: pY,
                    pathState: pathState,
                    segmentStartX: segmentStartX,
                    linePath: linePath,
                    areaPath: areaPath
                };
            }
        },
        {
            /**
   * @param {any[]} series
   * @param {any} pointsPos
   * @param {number} i
   * @param {number} j
   * @param {number} realIndex
   */ key: "handleNullDataPoints",
            value: function handleNullDataPoints(series, pointsPos, i, j, realIndex) {
                var w = this.w;
                if (series[i][j] === null && w.config.markers.showNullDataPoints || series[i].length === 1) {
                    var pSize = this.strokeWidth - w.config.markers.strokeWidth / 2;
                    if (!(pSize > 0)) {
                        pSize = 0;
                    }
                    var elPointsWrap = this.markers.plotChartMarkers({
                        pointsPos: pointsPos,
                        seriesIndex: realIndex,
                        j: j + 1,
                        pSize: pSize,
                        alwaysDrawMarker: true
                    });
                    if (elPointsWrap !== null) {
                        this.elPointsMain.add(elPointsWrap);
                    }
                }
            }
        }
    ]);
    return Line;
}();
var CircularChartsHelpers = /*#__PURE__*/ function() {
    "use strict";
    function CircularChartsHelpers(w) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, CircularChartsHelpers);
        this.w = w;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(CircularChartsHelpers, [
        {
            /**
   * @param {number} x
   * @param {number} y
   * @param {number} i
   * @param {string | number} text
   */ key: "drawYAxisTexts",
            value: function drawYAxisTexts(x, y, i, text) {
                var w = this.w;
                var yaxisConfig = w.config.yaxis[0];
                var formatter = w.formatters.yLabelFormatters[0];
                var graphics = new Graphics(this.w);
                var yaxisLabel = graphics.drawText({
                    x: x + yaxisConfig.labels.offsetX,
                    y: y + yaxisConfig.labels.offsetY,
                    text: formatter(text, i),
                    textAnchor: "middle",
                    fontSize: yaxisConfig.labels.style.fontSize,
                    fontFamily: yaxisConfig.labels.style.fontFamily,
                    foreColor: Array.isArray(yaxisConfig.labels.style.colors) ? yaxisConfig.labels.style.colors[i] : yaxisConfig.labels.style.colors
                });
                return yaxisLabel;
            }
        },
        {
            /**
   * Widest rendered width among the given label strings. Used to reserve
   * horizontal room for outer (name) labels so the pie can shrink to fit them.
   * @param {string[]} labels
   * @param {{ fontSize?: string, fontFamily?: string }} style
   * @returns {number}
   */ key: "getMaxLabelWidth",
            value: function getMaxLabelWidth(labels) {
                var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, fontSize = _ref.fontSize, fontFamily = _ref.fontFamily;
                var graphics = new Graphics(this.w);
                var maxWidth = 0;
                labels.forEach(function(text) {
                    if (text === null || typeof text === "undefined" || text === "") return;
                    var rect = graphics.getTextRects("".concat(text), fontSize || "12px", fontFamily, "");
                    maxWidth = Math.max(maxWidth, rect.width);
                });
                return maxWidth;
            }
        },
        {
            /**
   * Draw a single outer (name) label: an optional leader line from the slice
   * edge (anchor -> radial elbow -> label) plus the name text (one or more
   * lines, e.g. name + percent). Geometry is computed by the caller (Pie.js)
   * so it can run a de-overlap pass first. The text block is vertically
   * centered on `labelY`, which is where the connector terminates.
   * @param {{
   *   lines: string[],
   *   lineHeight: number,
   *   anchor: { x: number, y: number },
   *   elbow: { x: number, y: number },
   *   labelX: number,
   *   labelY: number,
   *   side: 'left' | 'right',
   *   connector: { show: boolean, width: number, color: string },
   *   style: { fontSize?: string, fontFamily?: string, fontWeight?: string | number },
   *   foreColor: string,
   * }} opts
   */ key: "drawExternalLabel",
            value: function drawExternalLabel(param) {
                var lines = param.lines, lineHeight = param.lineHeight, anchor = param.anchor, elbow = param.elbow, labelX = param.labelX, labelY = param.labelY, side = param.side, connector = param.connector, style = param.style, foreColor = param.foreColor;
                var graphics = new Graphics(this.w);
                var group = graphics.group({
                    class: "apexcharts-pie-name-label-group"
                });
                if (connector.show) {
                    var d = "M ".concat(anchor.x, " ").concat(anchor.y, " L ").concat(elbow.x, " ").concat(elbow.y, " L ").concat(labelX, " ").concat(labelY);
                    var line = graphics.drawPath({
                        d: d,
                        stroke: connector.color,
                        strokeWidth: connector.width,
                        fill: "none",
                        strokeLinecap: "round"
                    });
                    line.node.classList.add("apexcharts-pie-label-connector");
                    group.add(line);
                }
                var textX = side === "right" ? labelX + 4 : labelX - 4;
                var n = lines.length;
                var startY = labelY - (n - 1) * lineHeight / 2;
                var elText = graphics.drawText({
                    x: textX,
                    y: startY,
                    text: n === 1 ? lines[0] : lines,
                    textAnchor: side === "right" ? "start" : "end",
                    fontSize: style.fontSize,
                    fontFamily: style.fontFamily,
                    fontWeight: style.fontWeight,
                    foreColor: foreColor,
                    dominantBaseline: "central",
                    cssClass: "apexcharts-pie-name-label"
                });
                if (n > 1) {
                    var tspans = elText.node.getElementsByTagName("tspan");
                    for(var li = 0; li < tspans.length; li++){
                        tspans[li].setAttribute("x", "".concat(textX));
                        tspans[li].setAttribute("dy", li === 0 ? "0" : "".concat(lineHeight));
                    }
                }
                group.add(elText);
                return group;
            }
        }
    ]);
    return CircularChartsHelpers;
}();
var D2R$1 = Math.PI / 180;
var R2D$1 = 180 / Math.PI;
function arcPoint(cx, cy, radius, deg) {
    return {
        x: cx + radius * Math.cos((deg - 90) * D2R$1),
        y: cy + radius * Math.sin((deg - 90) * D2R$1)
    };
}
var xy = function xy(p) {
    return "".concat(p.x, " ").concat(p.y);
};
function roundedDonutSegmentPath(param) {
    var cx = param.cx, cy = param.cy, rIn = param.rIn, rOut = param.rOut, a0 = param.a0, a1 = param.a1, r = param.r, spanDeg = param.spanDeg;
    var ptAt = function ptAt(radius, deg) {
        return arcPoint(cx, cy, radius, deg);
    };
    var degOut = r / rOut * R2D$1;
    var degIn = r / rIn * R2D$1;
    var oStart = ptAt(rOut, a0 + degOut);
    var oEnd = ptAt(rOut, a1 - degOut);
    var largeOut = spanDeg - 2 * degOut > 180 ? 1 : 0;
    var ocEnd = ptAt(rOut, a1);
    var rEndOut = ptAt(rOut - r, a1);
    var ocStart = ptAt(rOut, a0);
    var rStartOut = ptAt(rOut - r, a0);
    var iEnd = ptAt(rIn, a1 - degIn);
    var iStart = ptAt(rIn, a0 + degIn);
    var largeIn = spanDeg - 2 * degIn > 180 ? 1 : 0;
    var icEnd = ptAt(rIn, a1);
    var rEndIn = ptAt(rIn + r, a1);
    var icStart = ptAt(rIn, a0);
    var rStartIn = ptAt(rIn + r, a0);
    return [
        "M",
        xy(oStart),
        "A",
        rOut,
        rOut,
        0,
        largeOut,
        1,
        xy(oEnd),
        "Q",
        xy(ocEnd),
        xy(rEndOut),
        "L",
        xy(rEndIn),
        "Q",
        xy(icEnd),
        xy(iEnd),
        "A",
        rIn,
        rIn,
        0,
        largeIn,
        0,
        xy(iStart),
        "Q",
        xy(icStart),
        xy(rStartIn),
        "L",
        xy(rStartOut),
        "Q",
        xy(ocStart),
        xy(oStart),
        "Z"
    ].join(" ");
}
function roundedPieSegmentPath(param) {
    var cx = param.cx, cy = param.cy, rOut = param.rOut, a0 = param.a0, a1 = param.a1, r = param.r, spanDeg = param.spanDeg;
    var ptAt = function ptAt(radius, deg) {
        return arcPoint(cx, cy, radius, deg);
    };
    var degOut = r / rOut * R2D$1;
    var oStart = ptAt(rOut, a0 + degOut);
    var oEnd = ptAt(rOut, a1 - degOut);
    var largeOut = spanDeg - 2 * degOut > 180 ? 1 : 0;
    var ocEnd = ptAt(rOut, a1);
    var rEndOut = ptAt(rOut - r, a1);
    var ocStart = ptAt(rOut, a0);
    var rStartOut = ptAt(rOut - r, a0);
    return [
        "M",
        xy(oStart),
        "A",
        rOut,
        rOut,
        0,
        largeOut,
        1,
        xy(oEnd),
        "Q",
        xy(ocEnd),
        xy(rEndOut),
        "L",
        "".concat(cx, " ").concat(cy),
        "L",
        xy(rStartOut),
        "Q",
        xy(ocStart),
        xy(oStart),
        "Z"
    ].join(" ");
}
function sharpDonutSegmentPath(param) {
    var cx = param.cx, cy = param.cy, rIn = param.rIn, rOut = param.rOut, a0 = param.a0, a1 = param.a1, spanDeg = param.spanDeg;
    var ptAt = function ptAt(radius, deg) {
        return arcPoint(cx, cy, radius, deg);
    };
    var largeArc = spanDeg > 180 ? 1 : 0;
    var A = ptAt(rOut, a0);
    var B = ptAt(rOut, a1);
    var C = ptAt(rIn, a1);
    var Din = ptAt(rIn, a0);
    return [
        "M",
        xy(A),
        "A",
        rOut,
        rOut,
        0,
        largeArc,
        1,
        xy(B),
        "L",
        xy(C),
        "A",
        rIn,
        rIn,
        0,
        largeArc,
        0,
        xy(Din),
        "Z"
    ].join(" ");
}
var Pie = /*#__PURE__*/ function() {
    "use strict";
    function Pie(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Pie);
        this.ctx = ctx;
        this.w = w;
        this.chartType = this.w.config.chart.type;
        this.initialAnim = this.w.config.chart.animations.enabled;
        this.dynamicAnim = this.initialAnim && this.w.config.chart.animations.dynamicAnimation.enabled;
        this.animBeginArr = [
            0
        ];
        this.animDur = 0;
        this.donutDataLabels = this.w.config.plotOptions.pie.donut.labels;
        this.lineColorArr = w.globals.stroke.colors !== void 0 ? w.globals.stroke.colors : w.globals.colors;
        this.defaultSize = Math.min(w.layout.gridWidth, w.layout.gridHeight);
        this.centerY = this.defaultSize / 2;
        this.centerX = w.layout.gridWidth / 2;
        if (w.config.chart.type === "radialBar") {
            this.fullAngle = 360;
        } else {
            this.fullAngle = Math.abs(w.config.plotOptions.pie.endAngle - w.config.plotOptions.pie.startAngle);
        }
        this.initialAngle = w.config.plotOptions.pie.startAngle % this.fullAngle;
        w.globals.radialSize = this.defaultSize / 2.05 - w.config.stroke.width - (!w.config.chart.sparkline.enabled ? w.config.chart.dropShadow.blur : 0);
        this.externalCfg = w.config.plotOptions.pie.dataLabels.external;
        var dlStyle = w.config.dataLabels.style;
        this.externalLabelStyle = {
            fontSize: this.externalCfg.fontSize || dlStyle.fontSize,
            fontFamily: this.externalCfg.fontFamily || dlStyle.fontFamily,
            fontWeight: this.externalCfg.fontWeight || dlStyle.fontWeight
        };
        this.externalLabels = [];
        this.externalLabelMaxLines = 1;
        this.externalLabelLineH = parseFloat(this.externalLabelStyle.fontSize) || 12;
        w.globals.pieExternalLabelMarginY = 0;
        this.showExternalLabels = this.externalCfg.show && this.chartType !== "polarArea";
        if (this.showExternalLabels && !w.globals.noData) {
            this.reserveExternalLabelSpace();
        }
        this.donutSize = w.globals.radialSize * parseInt(w.config.plotOptions.pie.donut.size, 10) / 100;
        var scaleSize = w.config.plotOptions.pie.customScale;
        var halfW = w.layout.gridWidth / 2;
        var halfH = w.layout.gridHeight / 2;
        this.translateX = halfW - halfW * scaleSize;
        this.translateY = halfH - halfH * scaleSize;
        this.dataLabelsGroup = new Graphics(this.w).group({
            class: "apexcharts-datalabels-group",
            transform: "translate(".concat(this.translateX, ", ").concat(this.translateY, ") scale(").concat(scaleSize, ")")
        });
        this.maxY = 0;
        this.sliceLabels = [];
        this.sliceSizes = [];
        this.prevSectorAngleArr = [];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Pie, [
        {
            /**
   * The text shown in an outer (name) label for slice `i`. Applies the
   * user `name.formatter` if provided, otherwise the raw series name. The
   * formatter may return a string or an array of strings (one per line, e.g.
   * `[name, percent]`); normalize via `getExternalLabelLines`.
   * @param {number} i
   * @returns {string | string[]}
   */ key: "getExternalLabelText",
            value: function getExternalLabelText(i) {
                var _a, _b, _c;
                var w = this.w;
                var name2 = w.seriesData.seriesNames[i];
                var fn = this.externalCfg.formatter;
                if (typeof fn === "function") {
                    return fn(name2, {
                        seriesIndex: i,
                        percent: (_b = (_a = w.globals.seriesPercent) == null ? void 0 : _a[i]) == null ? void 0 : _b[0],
                        value: (_c = w.globals.seriesTotals) == null ? void 0 : _c[i],
                        w: w
                    });
                }
                return name2 == null ? "" : "".concat(name2);
            }
        },
        {
            /**
   * Outer label content for slice `i` normalized to an array of line strings.
   * Supports a formatter returning an array, or a string with `\n` separators.
   * @param {number} i
   * @returns {string[]}
   */ key: "getExternalLabelLines",
            value: function getExternalLabelLines(i) {
                var raw = this.getExternalLabelText(i);
                var arr = Array.isArray(raw) ? raw : "".concat(raw == null ? "" : raw).split("\n");
                return arr.map(function(l) {
                    return l == null ? "" : "".concat(l);
                });
            }
        },
        {
            /**
   * Shrink the pie radius (and reposition its center) so outer name labels and
   * their connector lines fit inside the chart area without clipping. Stores
   * the reserved vertical band on `w.globals.pieExternalLabelMarginY` so
   * Core.resizeNonAxisCharts can grow the SVG height to match.
   */ key: "reserveExternalLabelSpace",
            value: function reserveExternalLabelSpace() {
                var _this = this;
                var w = this.w;
                var helpers = new CircularChartsHelpers(w);
                var lineSets = (w.seriesData.seriesNames || []).map(function(_, i) {
                    return _this.getExternalLabelLines(i);
                });
                var maxLabelWidth = helpers.getMaxLabelWidth(lineSets.flat(), {
                    fontSize: this.externalLabelStyle.fontSize,
                    fontFamily: this.externalLabelStyle.fontFamily
                });
                this.externalLabelMaxLines = lineSets.reduce(function(m, s) {
                    return Math.max(m, s.length);
                }, 1);
                this.externalLabelLineH = Math.round((parseFloat(this.externalLabelStyle.fontSize) || 12) * 1.35);
                var cn = this.externalCfg.connector;
                var blockHeight = this.externalLabelMaxLines * this.externalLabelLineH;
                var mh = maxLabelWidth + (cn.length || 0) + (cn.gap || 0) + 12;
                var mv = blockHeight / 2 + (cn.gap || 0) + 6;
                var fitted = Math.min(w.globals.radialSize, w.layout.gridWidth / 2 - mh, w.layout.gridHeight / 2 - mv);
                w.globals.radialSize = Math.max(fitted, this.defaultSize * 0.15);
                w.globals.pieExternalLabelMarginY = mv;
                var heightStr = w.config.chart.height ? String(w.config.chart.height) : "";
                var userSetFixedHeight = heightStr !== "" && heightStr !== "auto";
                this.centerY = userSetFixedHeight ? w.layout.gridHeight / 2 : w.globals.radialSize + mv;
            }
        },
        {
            /**
   * @param {any[]} series
   */ key: "draw",
            value: function draw(series) {
                var _this = this;
                var _a;
                var self = this;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var elPie = graphics.group({
                    class: "apexcharts-pie"
                });
                if (w.globals.noData) return elPie;
                var total = 0;
                for(var k = 0; k < series.length; k++){
                    total += Utils$1.negToZero(series[k]);
                }
                var sectorAngleArr = [];
                var elSeries = graphics.group();
                if (total === 0) {
                    total = 1e-5;
                }
                series.forEach(function(m) {
                    _this.maxY = Math.max(_this.maxY, m);
                });
                if (w.config.yaxis[0].max) {
                    this.maxY = w.config.yaxis[0].max;
                }
                if (w.config.grid.position === "back" && this.chartType === "polarArea") {
                    this.drawPolarElements(elPie);
                }
                for(var i = 0; i < series.length; i++){
                    var angle = this.fullAngle * Utils$1.negToZero(series[i]) / total;
                    sectorAngleArr.push(angle);
                    if (this.chartType === "polarArea") {
                        sectorAngleArr[i] = this.fullAngle / series.length;
                        this.sliceSizes.push(w.globals.radialSize * series[i] / (this.maxY || 1));
                    } else {
                        this.sliceSizes.push(w.globals.radialSize);
                    }
                }
                var morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
                if (w.globals.dataChanged && !morphActive) {
                    var prevTotal = 0;
                    for(var k1 = 0; k1 < w.globals.previousPaths.length; k1++){
                        prevTotal += Utils$1.negToZero(w.globals.previousPaths[k1]);
                    }
                    var previousAngle;
                    for(var i1 = 0; i1 < w.globals.previousPaths.length; i1++){
                        previousAngle = this.fullAngle * Utils$1.negToZero(w.globals.previousPaths[i1]) / prevTotal;
                        this.prevSectorAngleArr.push(previousAngle);
                    }
                }
                if (this.donutSize < 0) {
                    this.donutSize = 0;
                }
                if (this.chartType === "donut") {
                    var circle = graphics.drawCircle(this.donutSize);
                    circle.attr({
                        cx: this.centerX,
                        cy: this.centerY,
                        fill: w.config.plotOptions.pie.donut.background ? w.config.plotOptions.pie.donut.background : "transparent"
                    });
                    elSeries.add(circle);
                }
                var elG = self.drawArcs(sectorAngleArr, series);
                this.sliceLabels.forEach(function(s) {
                    elG.add(s);
                });
                elSeries.attr({
                    transform: "translate(".concat(this.translateX, ", ").concat(this.translateY, ") scale(").concat(w.config.plotOptions.pie.customScale, ")")
                });
                elSeries.add(elG);
                elPie.add(elSeries);
                if (this.donutDataLabels.show) {
                    var shouldFadeInLabels = this.initialAnim && !w.globals.resized && !w.globals.dataChanged && this.animDur > 0;
                    var dataLabels = this.renderInnerDataLabels(this.dataLabelsGroup, this.donutDataLabels, {
                        hollowSize: this.donutSize,
                        centerX: this.centerX,
                        centerY: this.centerY,
                        opacity: shouldFadeInLabels ? 0 : this.donutDataLabels.show
                    });
                    if (shouldFadeInLabels) {
                        var labelsNode = this.dataLabelsGroup.node;
                        labelsNode.style.transition = "opacity 280ms ease-out";
                        setTimeout(function() {
                            labelsNode.style.opacity = "1";
                        }, this.animDur);
                    }
                    elPie.add(dataLabels);
                }
                if (w.config.grid.position === "front" && this.chartType === "polarArea") {
                    this.drawPolarElements(elPie);
                }
                return elPie;
            }
        },
        {
            // core function for drawing pie arcs
            /**
   * @param {any[]} sectorAngleArr
   * @param {any[]} series
   */ key: "drawArcs",
            value: function drawArcs(sectorAngleArr, series) {
                var _this, _loop = function(i) {
                    var elPieArc = graphics.group({
                        class: "apexcharts-series apexcharts-pie-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[i]),
                        rel: i + 1,
                        "data:realIndex": i
                    });
                    g.add(elPieArc);
                    startAngle = endAngle;
                    prevStartAngle = prevEndAngle;
                    endAngle = startAngle + sectorAngleArr[i];
                    prevEndAngle = prevStartAngle + _this.prevSectorAngleArr[i];
                    var angle = endAngle < startAngle ? _this.fullAngle + endAngle - startAngle : endAngle - startAngle;
                    var pathFill = fill.fillPath({
                        seriesNumber: i,
                        size: _this.sliceSizes[i],
                        value: series[i]
                    });
                    var morphFrom = morphActive ? _this.ctx.morphTypeChange.getInitialPathFor(i, 0) : null;
                    var path = morphFrom || _this.getChangedPath(prevStartAngle, prevEndAngle);
                    var elPath = graphics.drawPath({
                        d: path,
                        // Pie/donut/polarArea data is a single series, so a user-supplied
                        // `stroke.colors` shorter than the slice count is NOT padded by the
                        // theme engine (unlike fill colors, which cycle). Without this, only
                        // slice 0 gets the requested color and the rest fall back to a grey
                        // default. Cycle the array — matching fill-color behaviour — so a
                        // single `stroke.colors: ['#fff']` borders every slice as expected.
                        stroke: Array.isArray(_this.lineColorArr) ? (_b = _this.lineColorArr[i]) != null ? _b : _this.lineColorArr[i % _this.lineColorArr.length] : _this.lineColorArr,
                        strokeWidth: 0,
                        fill: pathFill,
                        fillOpacity: w.config.fill.opacity,
                        classes: "apexcharts-pie-area apexcharts-".concat(_this.chartType.toLowerCase(), "-slice-").concat(i)
                    });
                    elPath.attr({
                        index: 0,
                        j: i
                    });
                    filters.setSelectionFilter(elPath, 0, i);
                    if (w.config.chart.dropShadow.enabled) {
                        var shadow = w.config.chart.dropShadow;
                        filters.dropShadow(elPath, shadow, i);
                    }
                    _this.addListeners(elPath, _this.donutDataLabels);
                    var labelPosition = {
                        x: 0,
                        y: 0
                    };
                    var midAngle = (startAngle + angle / 2) % _this.fullAngle;
                    var arcCenter = {
                        x: _this.centerX,
                        y: _this.centerY
                    };
                    if (_this.chartType === "pie" || _this.chartType === "polarArea") {
                        labelPosition = Utils$1.polarToCartesian(_this.centerX, _this.centerY, w.globals.radialSize / 1.25 + w.config.plotOptions.pie.dataLabels.offset, midAngle);
                        arcCenter = Utils$1.polarToCartesian(_this.centerX, _this.centerY, w.globals.radialSize / 2, midAngle);
                    } else if (_this.chartType === "donut") {
                        labelPosition = Utils$1.polarToCartesian(_this.centerX, _this.centerY, (w.globals.radialSize + _this.donutSize) / 2 + w.config.plotOptions.pie.dataLabels.offset, midAngle);
                        arcCenter = Utils$1.polarToCartesian(_this.centerX, _this.centerY, (w.globals.radialSize + _this.donutSize) / 2, midAngle);
                    }
                    Graphics.setAttrs(elPath.node, {
                        "data:angle": angle,
                        "data:startAngle": startAngle,
                        "data:strokeWidth": _this.strokeWidth,
                        "data:value": series[i],
                        "data:cx": arcCenter.x,
                        "data:cy": arcCenter.y
                    });
                    elPieArc.add(elPath);
                    var dur = 0;
                    if (_this.initialAnim && !w.globals.resized && !w.globals.dataChanged) {
                        dur = angle / _this.fullAngle * w.config.chart.animations.speed;
                        if (dur === 0) dur = 1;
                        _this.animDur = dur + _this.animDur;
                        _this.animBeginArr.push(_this.animDur);
                    } else {
                        _this.animBeginArr.push(0);
                    }
                    if (morphActive && morphFrom) {
                        var targetD = _this.getPiePath({
                            me: _this,
                            startAngle: startAngle,
                            angle: angle,
                            size: _this.sliceSizes[i]
                        });
                        var morphSpeed = _this.ctx.morphTypeChange.getSpeed();
                        var animations = _this.ctx.animations;
                        elPath.node.setAttribute("data:pathOrig", targetD);
                        var morphRunner = elPath.animate(morphSpeed).plot(targetD, "polygons").attr({
                            "stroke-width": _this.strokeWidth
                        });
                        if (morphRunner && typeof morphRunner.after === "function") {
                            morphRunner.after(function() {
                                return animations.animationCompleted(elPath);
                            });
                        } else {
                            animations.animationCompleted(elPath);
                        }
                    } else if (_this.dynamicAnim && w.globals.dataChanged) {
                        _this.animatePaths(elPath, {
                            size: _this.sliceSizes[i],
                            endAngle: endAngle,
                            startAngle: startAngle,
                            prevStartAngle: prevStartAngle,
                            prevEndAngle: prevEndAngle,
                            animateStartingPos: true,
                            i: i,
                            animBeginArr: _this.animBeginArr,
                            shouldSetPrevPaths: true,
                            dur: w.config.chart.animations.dynamicAnimation.speed
                        });
                    } else {
                        _this.animatePaths(elPath, {
                            size: _this.sliceSizes[i],
                            endAngle: endAngle,
                            startAngle: startAngle,
                            i: i,
                            totalItems: sectorAngleArr.length - 1,
                            animBeginArr: _this.animBeginArr,
                            dur: dur
                        });
                    }
                    if (w.config.plotOptions.pie.expandOnClick && _this.chartType !== "polarArea") {
                        elPath.node.addEventListener("mouseup", _this.pieClicked.bind(_this, i));
                    }
                    if (typeof w.interact.selectedDataPoints[0] !== "undefined" && w.interact.selectedDataPoints[0].indexOf(i) > -1) {
                        if (_this.initialAnim && !w.globals.resized && !w.globals.dataChanged && _this.animDur > 0) {
                            var _this1 = _this;
                            var _i = i;
                            setTimeout(function() {
                                return _this1.pieClicked(_i);
                            }, _this.animDur);
                        } else {
                            _this.pieClicked(i);
                        }
                    }
                    if (w.config.dataLabels.enabled) {
                        var xPos = labelPosition.x;
                        var yPos = labelPosition.y;
                        var text = 100 * angle / _this.fullAngle + "%";
                        if (angle !== 0 && w.config.plotOptions.pie.dataLabels.minAngleToShowLabel < sectorAngleArr[i]) {
                            var formatter = w.config.dataLabels.formatter;
                            if (formatter !== void 0) {
                                text = formatter(w.globals.seriesPercent[i][0], {
                                    seriesIndex: i,
                                    w: w
                                });
                            }
                            var foreColor = w.globals.dataLabels.style.colors[i];
                            var elPieLabelWrap = graphics.group({
                                class: "apexcharts-datalabels"
                            });
                            var elPieLabel = graphics.drawText({
                                x: xPos,
                                y: yPos,
                                text: text,
                                textAnchor: "middle",
                                fontSize: w.config.dataLabels.style.fontSize,
                                fontFamily: w.config.dataLabels.style.fontFamily,
                                fontWeight: w.config.dataLabels.style.fontWeight,
                                foreColor: foreColor
                            });
                            elPieLabelWrap.add(elPieLabel);
                            if (w.config.dataLabels.dropShadow.enabled) {
                                var textShadow = w.config.dataLabels.dropShadow;
                                filters.dropShadow(elPieLabel, textShadow);
                            }
                            elPieLabel.node.classList.add("apexcharts-pie-label");
                            if (w.config.chart.animations.animate && w.globals.resized === false) {
                                elPieLabel.node.classList.add("apexcharts-pie-label-delay");
                                elPieLabel.node.style.animationDelay = w.config.chart.animations.speed / 940 + "s";
                            }
                            _this.sliceLabels.push(elPieLabelWrap);
                        }
                    }
                    if (_this.showExternalLabels && angle !== 0) {
                        var lines = _this.getExternalLabelLines(i);
                        if (lines.some(function(l) {
                            return l !== "";
                        })) {
                            var anchor = Utils$1.polarToCartesian(_this.centerX, _this.centerY, w.globals.radialSize, midAngle);
                            var elbow = Utils$1.polarToCartesian(_this.centerX, _this.centerY, w.globals.radialSize + (_this.externalCfg.connector.gap || 0), midAngle);
                            var isRight = elbow.x >= _this.centerX;
                            var baseLabelX = isRight ? elbow.x + (_this.externalCfg.connector.length || 0) : elbow.x - (_this.externalCfg.connector.length || 0);
                            _this.externalLabels.push({
                                lines: lines,
                                anchor: anchor,
                                elbow: elbow,
                                side: isRight ? "right" : "left",
                                labelX: baseLabelX + parseFloat(_this.externalCfg.offsetX || 0),
                                idealY: elbow.y + parseFloat(_this.externalCfg.offsetY || 0),
                                connectorColor: _this.externalCfg.connector.color || w.globals.colors[i],
                                foreColor: _this.externalCfg.color || w.config.chart.foreColor
                            });
                        }
                    }
                };
                var _this1 = this;
                var _a, _b;
                var w = this.w;
                var filters = new Filters(this.w);
                var graphics = new Graphics(this.w);
                var fill = new Fill(this.w);
                var g = graphics.group({
                    class: "apexcharts-slices"
                });
                var startAngle = this.initialAngle;
                var prevStartAngle = this.initialAngle;
                var endAngle = this.initialAngle;
                var prevEndAngle = this.initialAngle;
                this.strokeWidth = w.config.stroke.show ? w.config.stroke.width : 0;
                var morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
                for(var i = 0; i < sectorAngleArr.length; i++)_this = this, _loop(i);
                if (this.showExternalLabels && this.externalLabels.length) {
                    this.placeExternalLabels();
                    var revealOnAnimEnd = Environment.isBrowser() && (morphActive || this.dynamicAnim && w.globals.dataChanged || this.initialAnim && !w.globals.resized && !w.globals.dataChanged);
                    this.externalLabels.forEach(function(lbl) {
                        var group = new CircularChartsHelpers(w).drawExternalLabel({
                            lines: lbl.lines,
                            lineHeight: _this1.externalLabelLineH,
                            anchor: lbl.anchor,
                            elbow: lbl.elbow,
                            labelX: lbl.labelX,
                            labelY: lbl.labelY,
                            side: lbl.side,
                            connector: {
                                show: _this1.externalCfg.connector.show,
                                width: _this1.externalCfg.connector.width,
                                color: lbl.connectorColor
                            },
                            style: _this1.externalLabelStyle,
                            foreColor: lbl.foreColor
                        });
                        if (revealOnAnimEnd) {
                            group.node.classList.add("apexcharts-element-hidden");
                            w.globals.delayedElements.push({
                                el: group.node
                            });
                        }
                        g.add(group);
                    });
                }
                return g;
            }
        },
        {
            /**
   * Vertical de-overlap for outer (name) labels: per side, sort by ideal y and
   * push neighbours apart so they keep at least one line-height of spacing.
   * Mutates each entry's `labelY`. Connector lines re-route to the moved y.
   */ key: "placeExternalLabels",
            value: function placeExternalLabels() {
                var _this = this;
                var w = this.w;
                var lineHeight = this.externalLabelMaxLines * this.externalLabelLineH + 2;
                var maxY = this.centerY + w.globals.radialSize + w.globals.pieExternalLabelMarginY;
                [
                    "left",
                    "right"
                ].forEach(function(side) {
                    var items = _this.externalLabels.filter(function(l) {
                        return l.side === side;
                    }).sort(function(a, b) {
                        return a.idealY - b.idealY;
                    });
                    items.forEach(function(l) {
                        l.labelY = l.idealY;
                    });
                    for(var k = 1; k < items.length; k++){
                        if (items[k].labelY - items[k - 1].labelY < lineHeight) {
                            items[k].labelY = items[k - 1].labelY + lineHeight;
                        }
                    }
                    var last = items[items.length - 1];
                    var overflow = last ? last.labelY - maxY : 0;
                    if (overflow > 0) {
                        for(var k1 = items.length - 1; k1 >= 0; k1--){
                            items[k1].labelY -= overflow;
                            if (k1 < items.length - 1 && items[k1 + 1].labelY - items[k1].labelY < lineHeight) {
                                items[k1].labelY = items[k1 + 1].labelY - lineHeight;
                            }
                        }
                    }
                });
            }
        },
        {
            /**
   * @param {any} elPath
   * @param {Record<string, any>} dataLabels
   */ key: "addListeners",
            value: function addListeners(elPath, dataLabels) {
                var graphics = new Graphics(this.w, this.ctx);
                elPath.node.addEventListener("mouseenter", graphics.pathMouseEnter.bind(graphics, elPath));
                elPath.node.addEventListener("mouseleave", graphics.pathMouseLeave.bind(graphics, elPath));
                elPath.node.addEventListener("mouseleave", this.revertDataLabelsInner.bind(this));
                elPath.node.addEventListener("mousedown", graphics.pathMouseDown.bind(graphics, elPath));
                if (!this.donutDataLabels.total.showAlways) {
                    elPath.node.addEventListener("mouseenter", this.printDataLabelsInner.bind(this, elPath.node, dataLabels));
                    elPath.node.addEventListener("mousedown", this.printDataLabelsInner.bind(this, elPath.node, dataLabels));
                }
            }
        },
        {
            // This function can be used for other circle charts too
            /**
   * @param {any} el
   * @param {Record<string, any>} opts
   */ key: "animatePaths",
            value: function animatePaths(el, opts) {
                var w = this.w;
                var me = this;
                var angle = opts.endAngle < opts.startAngle ? this.fullAngle + opts.endAngle - opts.startAngle : opts.endAngle - opts.startAngle;
                var prevAngle = angle;
                var fromStartAngle = opts.startAngle;
                var toStartAngle = opts.startAngle;
                if (opts.prevStartAngle !== void 0 && opts.prevEndAngle !== void 0) {
                    fromStartAngle = opts.prevEndAngle;
                    prevAngle = opts.prevEndAngle < opts.prevStartAngle ? this.fullAngle + opts.prevEndAngle - opts.prevStartAngle : opts.prevEndAngle - opts.prevStartAngle;
                }
                if (opts.i === w.config.series.length - 1) {
                    if (angle + toStartAngle > this.fullAngle) {
                        opts.endAngle = opts.endAngle - (angle + toStartAngle);
                    } else if (angle + toStartAngle < this.fullAngle) {
                        opts.endAngle = opts.endAngle + (this.fullAngle - (angle + toStartAngle));
                    }
                }
                if (angle === this.fullAngle) angle = this.fullAngle - 0.01;
                me.animateArc(el, fromStartAngle, toStartAngle, angle, prevAngle, opts);
            }
        },
        {
            /**
   * @param {any} el
   * @param {number} fromStartAngle
   * @param {number} toStartAngle
   * @param {number} angle
   * @param {number} prevAngle
   * @param {Record<string, any>} opts
   */ key: "animateArc",
            value: function animateArc(el, fromStartAngle, toStartAngle, angle, prevAngle, opts) {
                var me = this;
                var w = this.w;
                var animations = new Animations(this.w);
                var size = opts.size;
                var path;
                if (isNaN(fromStartAngle) || isNaN(prevAngle)) {
                    fromStartAngle = toStartAngle;
                    prevAngle = angle;
                    opts.dur = 0;
                }
                var currAngle = angle;
                var startAngle = toStartAngle;
                var fromAngle = fromStartAngle < toStartAngle ? this.fullAngle + fromStartAngle - toStartAngle : fromStartAngle - toStartAngle;
                if (w.globals.dataChanged && opts.shouldSetPrevPaths) {
                    if (opts.prevEndAngle) {
                        path = me.getPiePath({
                            me: me,
                            startAngle: opts.prevStartAngle,
                            angle: opts.prevEndAngle < opts.prevStartAngle ? this.fullAngle + opts.prevEndAngle - opts.prevStartAngle : opts.prevEndAngle - opts.prevStartAngle,
                            size: size
                        });
                        el.attr({
                            d: path
                        });
                    }
                }
                if (opts.dur !== 0) {
                    el.animate(opts.dur, opts.animBeginArr[opts.i]).after(/** @this {any} */ function() {
                        if (me.chartType === "pie" || me.chartType === "donut" || me.chartType === "polarArea") {
                            this.animate(w.config.chart.animations.dynamicAnimation.speed).attr({
                                "stroke-width": me.strokeWidth
                            });
                        }
                        if (opts.i === w.config.series.length - 1) {
                            animations.animationCompleted(el);
                        }
                    }).during(function(pos) {
                        currAngle = fromAngle + (angle - fromAngle) * pos;
                        if (opts.animateStartingPos) {
                            currAngle = prevAngle + (angle - prevAngle) * pos;
                            startAngle = fromStartAngle - prevAngle + (toStartAngle - (fromStartAngle - prevAngle)) * pos;
                        }
                        path = me.getPiePath({
                            me: me,
                            startAngle: startAngle,
                            angle: currAngle,
                            size: size
                        });
                        el.node.setAttribute("data:pathOrig", path);
                        el.attr({
                            d: path
                        });
                    });
                } else {
                    path = me.getPiePath({
                        me: me,
                        startAngle: startAngle,
                        angle: angle,
                        size: size
                    });
                    if (!opts.isTrack) {
                        w.globals.animationEnded = true;
                    }
                    el.node.setAttribute("data:pathOrig", path);
                    el.attr({
                        d: path,
                        "stroke-width": me.strokeWidth
                    });
                }
            }
        },
        {
            /**
   * @param {number} i
   */ key: "pieClicked",
            value: function pieClicked(i) {
                var w = this.w;
                var me = this;
                var size = me.sliceSizes[i] + (w.config.plotOptions.pie.expandOnClick ? 4 : 0);
                var elPath = w.dom.Paper.findOne(".apexcharts-".concat(me.chartType.toLowerCase(), "-slice-").concat(i));
                if (elPath.attr("data:pieClicked") === "true") {
                    elPath.attr({
                        "data:pieClicked": "false"
                    });
                    this.revertDataLabelsInner();
                    var origPath = elPath.attr("data:pathOrig");
                    elPath.attr({
                        d: origPath
                    });
                    return;
                } else {
                    var allEls = w.dom.baseEl.getElementsByClassName("apexcharts-pie-area");
                    Array.prototype.forEach.call(allEls, function(pieSlice) {
                        pieSlice.setAttribute("data:pieClicked", "false");
                        var origPath = pieSlice.getAttribute("data:pathOrig");
                        if (origPath) {
                            pieSlice.setAttribute("d", origPath);
                        }
                    });
                    w.interact.capturedDataPointIndex = i;
                    elPath.attr("data:pieClicked", "true");
                }
                var startAngle = parseInt(elPath.attr("data:startAngle"), 10);
                var angle = parseInt(elPath.attr("data:angle"), 10);
                var path = me.getPiePath({
                    me: me,
                    startAngle: startAngle,
                    angle: angle,
                    size: size
                });
                if (angle === 360) return;
                elPath.plot(path);
            }
        },
        {
            /**
   * @param {number} prevStartAngle
   * @param {number} prevEndAngle
   */ key: "getChangedPath",
            value: function getChangedPath(prevStartAngle, prevEndAngle) {
                var path = "";
                if (this.dynamicAnim && this.w.globals.dataChanged) {
                    path = this.getPiePath({
                        me: this,
                        startAngle: prevStartAngle,
                        angle: prevEndAngle - prevStartAngle,
                        // @ts-ignore — size is set dynamically during draw()
                        size: this.size
                    });
                }
                return path;
            }
        },
        {
            /** @param {{me: any, startAngle: any, angle: any, size: any}} opts */ key: "getPiePath",
            value: function getPiePath(param) {
                var me = param.me, startAngle = param.startAngle, angle = param.angle, size = param.size;
                var path;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var startDeg = startAngle;
                var endDeg = angle + startAngle;
                if (Math.ceil(endDeg) >= this.fullAngle + this.w.config.plotOptions.pie.startAngle % this.fullAngle) {
                    endDeg = this.fullAngle + this.w.config.plotOptions.pie.startAngle % this.fullAngle - 0.01;
                }
                var spanDeg = endDeg - startDeg;
                var isSliceType = me.chartType === "pie" || me.chartType === "donut" || me.chartType === "polarArea";
                var spacing = w.config.plotOptions.pie.spacing;
                if (isSliceType && spacing > 0 && spanDeg > 0) {
                    var rRef = me.chartType === "donut" ? (size + me.donutSize) / 2 : size;
                    var gapDeg = rRef > 0 ? spacing / rRef * (180 / Math.PI) : 0;
                    var inset = Math.min(gapDeg / 2, Math.max(0, spanDeg / 2 - 0.5));
                    startDeg += inset;
                    spanDeg -= 2 * inset;
                }
                endDeg = startDeg + spanDeg;
                if (Math.ceil(endDeg) > this.fullAngle) endDeg -= this.fullAngle;
                var startRadians = Math.PI * (startDeg - 90) / 180;
                var borderRadius = w.config.plotOptions.pie.borderRadius;
                if (borderRadius > 0 && isSliceType) {
                    var roundedPath = this.getRoundedSlicePath({
                        me: me,
                        startDeg: startDeg,
                        spanDeg: spanDeg,
                        size: size,
                        borderRadius: borderRadius
                    });
                    if (roundedPath) return roundedPath;
                }
                var endRadians = Math.PI * (endDeg - 90) / 180;
                var x1 = me.centerX + size * Math.cos(startRadians);
                var y1 = me.centerY + size * Math.sin(startRadians);
                var x2 = me.centerX + size * Math.cos(endRadians);
                var y2 = me.centerY + size * Math.sin(endRadians);
                var startInner = Utils$1.polarToCartesian(me.centerX, me.centerY, me.donutSize, endDeg);
                var endInner = Utils$1.polarToCartesian(me.centerX, me.centerY, me.donutSize, startDeg);
                var largeArc = spanDeg > 180 ? 1 : 0;
                var pathBeginning = [
                    "M",
                    x1,
                    y1,
                    "A",
                    size,
                    size,
                    0,
                    largeArc,
                    1,
                    x2,
                    y2
                ];
                if (me.chartType === "donut") {
                    path = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(pathBeginning).concat([
                        "L",
                        startInner.x,
                        startInner.y,
                        "A",
                        me.donutSize,
                        me.donutSize,
                        0,
                        largeArc,
                        0,
                        endInner.x,
                        endInner.y,
                        "L",
                        x1,
                        y1,
                        "z"
                    ]).join(" ");
                } else if (me.chartType === "pie" || me.chartType === "polarArea") {
                    path = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(pathBeginning).concat([
                        "L",
                        me.centerX,
                        me.centerY,
                        "L",
                        x1,
                        y1
                    ]).join(" ");
                } else {
                    path = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(pathBeginning).join(" ");
                }
                return graphics.roundPathCorners(path, this.strokeWidth * 2);
            }
        },
        {
            /**
   * Build a slice path with rounded corners (plotOptions.pie.borderRadius).
   *
   * The generic roundPathCorners() only rounds line->line joins, but a slice
   * corner is an arc<->line join, so we construct the fillets explicitly here:
   * every corner is inset by the (clamped) radius along both the arc and the
   * radial edge, and a quadratic Bezier with its control point at the original
   * sharp corner bridges the two inset points. Donut slices round all four
   * corners; pie / polarArea slices round the two outer corners and keep the
   * center apex sharp.
   *
   * Returns null when the slice is too small to round meaningfully, so the
   * caller can fall back to a sharp-corner path.
   *
   * @param {{me: any, startDeg: number, spanDeg: number, size: number, borderRadius: number}} opts
   * @returns {string | null}
   */ key: "getRoundedSlicePath",
            value: function getRoundedSlicePath(param) {
                var me = param.me, startDeg = param.startDeg, spanDeg = param.spanDeg, size = param.size, borderRadius = param.borderRadius;
                if (!(spanDeg > 0)) return null;
                var D2R2 = Math.PI / 180;
                var cx = me.centerX;
                var cy = me.centerY;
                var isDonut = me.chartType === "donut";
                var rOut = size;
                var rIn = isDonut ? me.donutSize : 0;
                var spanRad = spanDeg * D2R2;
                var r = borderRadius;
                r = Math.min(r, spanRad * rOut / 2);
                if (isDonut) {
                    r = Math.min(r, spanRad * rIn / 2);
                    r = Math.min(r, (rOut - rIn) / 2);
                } else {
                    r = Math.min(r, rOut / 2);
                }
                if (!(r > 0.5)) return null;
                var a0 = startDeg;
                var a1 = startDeg + spanDeg;
                if (isDonut) {
                    return roundedDonutSegmentPath({
                        cx: cx,
                        cy: cy,
                        rIn: rIn,
                        rOut: rOut,
                        a0: a0,
                        a1: a1,
                        r: r,
                        spanDeg: spanDeg
                    });
                }
                return roundedPieSegmentPath({
                    cx: cx,
                    cy: cy,
                    rOut: rOut,
                    a0: a0,
                    a1: a1,
                    r: r,
                    spanDeg: spanDeg
                });
            }
        },
        {
            /**
   * @param {any} parent
   */ key: "drawPolarElements",
            value: function drawPolarElements(parent) {
                var w = this.w;
                var scale = new Scales(this.w);
                var graphics = new Graphics(this.w);
                var helpers = new CircularChartsHelpers(this.w);
                var gCircles = graphics.group();
                var gYAxis = graphics.group();
                var yScale = scale.niceScale(0, Math.ceil(this.maxY), 0);
                var yTexts = yScale.result.reverse();
                var len = yScale.result.length;
                this.maxY = yScale.niceMax;
                var circleSize = w.globals.radialSize;
                var diff = circleSize / (len - 1);
                for(var i = 0; i < len - 1; i++){
                    var circle = graphics.drawCircle(circleSize);
                    circle.attr({
                        cx: this.centerX,
                        cy: this.centerY,
                        fill: "none",
                        "stroke-width": w.config.plotOptions.polarArea.rings.strokeWidth,
                        stroke: w.config.plotOptions.polarArea.rings.strokeColor
                    });
                    if (w.config.yaxis[0].show) {
                        var yLabel = helpers.drawYAxisTexts(this.centerX, this.centerY - circleSize + parseInt(w.config.yaxis[0].labels.style.fontSize, 10) / 2, i, yTexts[i]);
                        gYAxis.add(yLabel);
                    }
                    gCircles.add(circle);
                    circleSize = circleSize - diff;
                }
                this.drawSpokes(parent);
                parent.add(gCircles);
                parent.add(gYAxis);
            }
        },
        {
            /**
   * @param {any} dataLabelsGroup
   * @param {Record<string, any>} dataLabelsConfig
   * @param {Record<string, any>} opts
   */ key: "renderInnerDataLabels",
            value: function renderInnerDataLabels(dataLabelsGroup, dataLabelsConfig, opts) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var showTotal = dataLabelsConfig.total.show;
                dataLabelsGroup.node.innerHTML = "";
                dataLabelsGroup.node.style.opacity = opts.opacity;
                var x = opts.centerX;
                var y = !this.donutDataLabels.total.label ? opts.centerY - opts.centerY / 6 : opts.centerY;
                var labelColor, valueColor;
                if (dataLabelsConfig.name.color === void 0) {
                    labelColor = w.globals.colors[0];
                } else {
                    labelColor = dataLabelsConfig.name.color;
                }
                var labelFontSize = dataLabelsConfig.name.fontSize;
                var labelFontFamily = dataLabelsConfig.name.fontFamily;
                var labelFontWeight = dataLabelsConfig.name.fontWeight;
                if (dataLabelsConfig.value.color === void 0) {
                    valueColor = w.config.chart.foreColor;
                } else {
                    valueColor = dataLabelsConfig.value.color;
                }
                var lbFormatter = dataLabelsConfig.value.formatter;
                var val = "";
                var name2 = "";
                if (showTotal) {
                    labelColor = dataLabelsConfig.total.color;
                    labelFontSize = dataLabelsConfig.total.fontSize;
                    labelFontFamily = dataLabelsConfig.total.fontFamily;
                    labelFontWeight = dataLabelsConfig.total.fontWeight;
                    name2 = !this.donutDataLabels.total.label ? "" : dataLabelsConfig.total.label;
                    val = dataLabelsConfig.total.formatter(w);
                } else {
                    if (w.seriesData.series.length === 1) {
                        val = lbFormatter(w.seriesData.series[0], w);
                        name2 = w.seriesData.seriesNames[0];
                    }
                }
                if (name2) {
                    name2 = dataLabelsConfig.name.formatter(name2, dataLabelsConfig.total.show, w);
                }
                if (dataLabelsConfig.name.show) {
                    var elLabel = graphics.drawText({
                        x: x,
                        y: y + parseFloat(dataLabelsConfig.name.offsetY),
                        text: name2,
                        textAnchor: "middle",
                        foreColor: labelColor,
                        fontSize: labelFontSize,
                        fontWeight: labelFontWeight,
                        fontFamily: labelFontFamily
                    });
                    elLabel.node.classList.add("apexcharts-datalabel-label");
                    dataLabelsGroup.add(elLabel);
                }
                if (dataLabelsConfig.value.show) {
                    var valOffset = dataLabelsConfig.name.show ? parseFloat(dataLabelsConfig.value.offsetY) + 16 : dataLabelsConfig.value.offsetY;
                    var elValue = graphics.drawText({
                        x: x,
                        y: y + valOffset,
                        text: val,
                        textAnchor: "middle",
                        foreColor: valueColor,
                        fontWeight: dataLabelsConfig.value.fontWeight,
                        fontSize: dataLabelsConfig.value.fontSize,
                        fontFamily: dataLabelsConfig.value.fontFamily
                    });
                    elValue.node.classList.add("apexcharts-datalabel-value");
                    dataLabelsGroup.add(elValue);
                }
                return dataLabelsGroup;
            }
        },
        {
            /**
   *
   * @param {string} name - The name of the series
   * @param {string} val - The value of that series
   * @param {any} el - Optional el (indicates which series was hovered/clicked). If this param is not present, means we need to show total
   * @param {Record<string, any>} labelsConfig
   */ key: "printInnerLabels",
            value: function printInnerLabels(labelsConfig, name2, val, el) {
                var w = this.w;
                var labelColor;
                if (el) {
                    if (labelsConfig.name.color === void 0) {
                        labelColor = w.globals.colors[parseInt(el.parentNode.getAttribute("rel"), 10) - 1];
                    } else {
                        labelColor = labelsConfig.name.color;
                    }
                } else {
                    if (w.seriesData.series.length > 1 && labelsConfig.total.show) {
                        labelColor = labelsConfig.total.color;
                    }
                }
                var elLabel = w.dom.baseEl.querySelector(".apexcharts-datalabel-label");
                var elValue = w.dom.baseEl.querySelector(".apexcharts-datalabel-value");
                var lbFormatter = labelsConfig.value.formatter;
                val = lbFormatter(val, w);
                if (!el && typeof labelsConfig.total.formatter === "function") {
                    val = labelsConfig.total.formatter(w);
                }
                var isTotal = name2 === labelsConfig.total.label;
                name2 = !this.donutDataLabels.total.label ? "" : labelsConfig.name.formatter(name2, isTotal, w);
                if (elLabel !== null) {
                    elLabel.textContent = name2;
                }
                if (elValue !== null) {
                    elValue.textContent = val;
                }
                if (elLabel !== null) {
                    var elLabelEl = /** @type {HTMLElement} */ elLabel;
                    elLabelEl.style.fill = labelColor;
                }
            }
        },
        {
            /**
   * @param {any} el
   * @param {Record<string, any>} dataLabelsConfig
   */ key: "printDataLabelsInner",
            value: function printDataLabelsInner(el, dataLabelsConfig) {
                var w = this.w;
                var val = el.getAttribute("data:value");
                var name2 = w.seriesData.seriesNames[parseInt(el.parentNode.getAttribute("rel"), 10) - 1];
                if (w.seriesData.series.length > 1) {
                    this.printInnerLabels(dataLabelsConfig, name2, val, el);
                }
                var dataLabelsGroup = w.dom.baseEl.querySelector(".apexcharts-datalabels-group");
                if (dataLabelsGroup !== null) {
                    var dataLabelsGroupEl = /** @type {HTMLElement} */ dataLabelsGroup;
                    dataLabelsGroupEl.style.opacity = "1";
                }
            }
        },
        {
            /**
   * @param {any} parent
   */ key: "drawSpokes",
            value: function drawSpokes(parent) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var spokeConfig = w.config.plotOptions.polarArea.spokes;
                if (spokeConfig.strokeWidth === 0) return;
                var spokes = [];
                var angleDivision = 360 / w.seriesData.series.length;
                for(var i = 0; i < w.seriesData.series.length; i++){
                    spokes.push(Utils$1.polarToCartesian(this.centerX, this.centerY, w.globals.radialSize, w.config.plotOptions.pie.startAngle + angleDivision * i));
                }
                spokes.forEach(function(p, i) {
                    var line = graphics.drawLine(p.x, p.y, _this.centerX, _this.centerY, Array.isArray(spokeConfig.connectorColors) ? spokeConfig.connectorColors[i] : spokeConfig.connectorColors);
                    parent.add(line);
                });
            }
        },
        {
            key: "revertDataLabelsInner",
            value: function revertDataLabelsInner() {
                var w = this.w;
                if (this.donutDataLabels.show) {
                    var dataLabelsGroup = w.dom.Paper.findOne(".apexcharts-datalabels-group");
                    var dataLabels = this.renderInnerDataLabels(dataLabelsGroup, this.donutDataLabels, {
                        hollowSize: this.donutSize,
                        centerX: this.centerX,
                        centerY: this.centerY,
                        opacity: this.donutDataLabels.show
                    });
                    var elPie = w.dom.Paper.findOne(".apexcharts-radialbar, .apexcharts-pie");
                    elPie.add(dataLabels);
                }
            }
        }
    ]);
    return Pie;
}();
var Radar = /*#__PURE__*/ function() {
    "use strict";
    function Radar(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Radar);
        this.ctx = ctx;
        this.w = w;
        this.chartType = this.w.config.chart.type;
        this.initialAnim = this.w.config.chart.animations.enabled;
        this.dynamicAnim = this.initialAnim && this.w.config.chart.animations.dynamicAnimation.enabled;
        this.animDur = 0;
        this.graphics = new Graphics(this.w);
        this.lineColorArr = w.globals.stroke.colors !== void 0 ? w.globals.stroke.colors : w.globals.colors;
        this.defaultSize = w.globals.svgHeight < w.globals.svgWidth ? w.layout.gridHeight : w.layout.gridWidth;
        this.isLog = w.config.yaxis[0].logarithmic;
        this.logBase = w.config.yaxis[0].logBase;
        this.coreUtils = new CoreUtils(this.w);
        this.maxValue = this.isLog ? this.coreUtils.getLogVal(this.logBase, w.globals.maxY, 0) : w.globals.maxY;
        this.minValue = this.isLog ? this.coreUtils.getLogVal(this.logBase, this.w.globals.minY, 0) : w.globals.minY;
        this.polygons = w.config.plotOptions.radar.polygons;
        this.strokeWidth = w.config.stroke.show ? w.config.stroke.width : 0;
        this.size = this.defaultSize / 2.1 - this.strokeWidth - w.config.chart.dropShadow.blur;
        if (w.config.xaxis.labels.show) {
            this.size = this.size - w.layout.xAxisLabelsWidth / 1.75;
        }
        if (w.config.plotOptions.radar.size !== void 0) {
            this.size = w.config.plotOptions.radar.size;
        }
        this.dataRadiusOfPercent = /** @type {any} */ [];
        this.dataRadius = /** @type {any} */ [];
        this.angleArr = /** @type {any} */ [];
        this.dataPointsLen = 0;
        this.disAngle = 0;
        this.yaxisLabelsTextsPos = [];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Radar, [
        {
            /**
   * @param {any[]} series
   */ key: "draw",
            value: function draw(series) {
                var _this = this;
                var w = this.w;
                var fill = new Fill(this.w);
                var allSeries = [];
                var dataLabels = new DataLabels(this.w, this.ctx);
                if (series.length) {
                    this.dataPointsLen = series[w.globals.maxValsInArrayIndex].length;
                }
                this.disAngle = Math.PI * 2 / this.dataPointsLen;
                var halfW = w.layout.gridWidth / 2;
                var halfH = w.layout.gridHeight / 2;
                var translateX = halfW + w.config.plotOptions.radar.offsetX;
                var translateY = halfH + w.config.plotOptions.radar.offsetY;
                var ret = this.graphics.group({
                    class: "apexcharts-radar-series apexcharts-plot-series",
                    transform: "translate(".concat(translateX || 0, ", ").concat(translateY || 0, ")")
                });
                var dataPointsPos = [];
                var elPointsMain = null;
                var elDataPointsMain = null;
                this.yaxisLabels = this.graphics.group({
                    class: "apexcharts-yaxis"
                });
                series.forEach(function(s, i) {
                    var longestSeries = s.length === w.globals.dataPoints;
                    var elSeries = _this.graphics.group().attr({
                        class: "apexcharts-series",
                        "data:longestSeries": longestSeries,
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[i]),
                        rel: i + 1,
                        "data:realIndex": i
                    });
                    _this.dataRadiusOfPercent[i] = [];
                    _this.dataRadius[i] = [];
                    _this.angleArr[i] = [];
                    s.forEach(function(dv, j) {
                        var range = Math.abs(_this.maxValue - _this.minValue);
                        dv = dv - _this.minValue;
                        if (_this.isLog) {
                            dv = _this.coreUtils.getLogVal(_this.logBase, dv, 0);
                        }
                        _this.dataRadiusOfPercent[i][j] = dv / range;
                        _this.dataRadius[i][j] = _this.dataRadiusOfPercent[i][j] * _this.size;
                        _this.angleArr[i][j] = j * _this.disAngle;
                    });
                    dataPointsPos = _this.getDataPointsPos(_this.dataRadius[i], _this.angleArr[i]);
                    var paths = _this.createPaths(dataPointsPos, {
                        x: 0,
                        y: 0
                    });
                    elPointsMain = _this.graphics.group({
                        class: "apexcharts-series-markers-wrap apexcharts-element-hidden"
                    });
                    elDataPointsMain = _this.graphics.group({
                        class: "apexcharts-datalabels",
                        "data:realIndex": i
                    });
                    w.globals.delayedElements.push({
                        el: elPointsMain.node,
                        index: i
                    });
                    var defaultRenderedPathOptions = {
                        i: i,
                        realIndex: i,
                        animationDelay: i,
                        initialSpeed: w.config.chart.animations.speed,
                        dataChangeSpeed: w.config.chart.animations.dynamicAnimation.speed,
                        className: "apexcharts-radar",
                        shouldClipToGrid: false,
                        bindEventsOnPaths: false,
                        stroke: w.globals.stroke.colors[i],
                        strokeLineCap: w.config.stroke.lineCap
                    };
                    var pathFrom = null;
                    if (w.globals.previousPaths.length > 0) {
                        pathFrom = _this.getPreviousPath(i);
                    }
                    for(var p = 0; p < paths.linePathsTo.length; p++){
                        var renderedLinePath = _this.graphics.renderPaths(__spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
                            pathFrom: pathFrom === null ? paths.linePathsFrom[p] : pathFrom,
                            pathTo: paths.linePathsTo[p],
                            strokeWidth: Array.isArray(_this.strokeWidth) ? _this.strokeWidth[i] : _this.strokeWidth,
                            fill: "none",
                            drawShadow: false
                        }));
                        elSeries.add(renderedLinePath);
                        var pathFill = fill.fillPath({
                            seriesNumber: i
                        });
                        var renderedAreaPath = _this.graphics.renderPaths(__spreadProps(__spreadValues({}, defaultRenderedPathOptions), {
                            pathFrom: pathFrom === null ? paths.areaPathsFrom[p] : pathFrom,
                            pathTo: paths.areaPathsTo[p],
                            strokeWidth: 0,
                            fill: pathFill,
                            drawShadow: false,
                            // Radial mask: the area fill blooms outward from the radar's center
                            // (in this group's local coords) instead of the default L→R rect wipe.
                            drawMask: {
                                type: "radial",
                                cx: 0,
                                cy: 0,
                                r: _this.size
                            }
                        }));
                        if (w.config.chart.dropShadow.enabled) {
                            var filters = new Filters(_this.w);
                            var shadow = w.config.chart.dropShadow;
                            filters.dropShadow(renderedAreaPath, Object.assign({}, shadow, {
                                noUserSpaceOnUse: true
                            }), i);
                        }
                        elSeries.add(renderedAreaPath);
                    }
                    s.forEach(function(sj, j) {
                        var markers = new Markers(_this.w, _this.ctx);
                        var opts = markers.getMarkerConfig({
                            cssClass: "apexcharts-marker",
                            seriesIndex: i,
                            dataPointIndex: j
                        });
                        var point = _this.graphics.drawMarker(dataPointsPos[j].x, dataPointsPos[j].y, opts);
                        point.attr("rel", j);
                        point.attr("j", j);
                        point.attr("index", i);
                        point.node.setAttribute("default-marker-size", opts.pSize);
                        var elPointsWrap = _this.graphics.group({
                            class: "apexcharts-series-markers"
                        });
                        if (elPointsWrap) {
                            elPointsWrap.add(point);
                        }
                        elPointsMain.add(elPointsWrap);
                        elSeries.add(elPointsMain);
                        var dataLabelsConfig = w.config.dataLabels;
                        if (dataLabelsConfig.enabled) {
                            var text = dataLabelsConfig.formatter(w.seriesData.series[i][j], {
                                seriesIndex: i,
                                dataPointIndex: j,
                                w: w
                            });
                            dataLabels.plotDataLabelsText({
                                x: dataPointsPos[j].x,
                                y: dataPointsPos[j].y,
                                text: text,
                                textAnchor: "middle",
                                i: i,
                                j: i,
                                // `j` above is the series index (kept for the color lookup), so
                                // pass the real data point index for per-point offsets
                                seriesIndex: i,
                                dataPointIndex: j,
                                parent: elDataPointsMain,
                                offsetCorrection: false,
                                dataLabelsConfig: __spreadValues({}, dataLabelsConfig)
                            });
                        }
                        elSeries.add(elDataPointsMain);
                    });
                    allSeries.push(elSeries);
                });
                this.drawPolygons({
                    parent: ret
                });
                if (w.config.xaxis.labels.show) {
                    var xaxisTexts = this.drawXAxisTexts();
                    ret.add(xaxisTexts);
                }
                allSeries.forEach(function(elS) {
                    ret.add(elS);
                });
                ret.add(this.yaxisLabels);
                return ret;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "drawPolygons",
            value: function drawPolygons(opts) {
                var _this = this;
                var w = this.w;
                var parent = opts.parent;
                var helpers = new CircularChartsHelpers(this.w);
                var yaxisTexts = w.globals.yAxisScale[0].result.reverse();
                var layers = yaxisTexts.length;
                var radiusSizes = [];
                var layerDis = this.size / (layers - 1);
                for(var i = 0; i < layers; i++){
                    radiusSizes[i] = layerDis * i;
                }
                radiusSizes.reverse();
                var polygonStrings = [];
                var lines = [];
                radiusSizes.forEach(function(radiusSize, r) {
                    var polygon = Utils$1.getPolygonPos(radiusSize, _this.dataPointsLen);
                    var string = "";
                    polygon.forEach(function(p, i) {
                        if (r === 0) {
                            var line = _this.graphics.drawLine(p.x, p.y, 0, 0, Array.isArray(_this.polygons.connectorColors) ? _this.polygons.connectorColors[i] : _this.polygons.connectorColors);
                            lines.push(line);
                        }
                        if (i === 0) {
                            _this.yaxisLabelsTextsPos.push({
                                x: p.x,
                                y: p.y
                            });
                        }
                        string += p.x + "," + p.y + " ";
                    });
                    polygonStrings.push(string);
                });
                polygonStrings.forEach(function(p, i) {
                    var strokeColors = _this.polygons.strokeColors;
                    var strokeWidth = _this.polygons.strokeWidth;
                    var polygon = _this.graphics.drawPolygon(p, Array.isArray(strokeColors) ? strokeColors[i] : strokeColors, Array.isArray(strokeWidth) ? strokeWidth[i] : strokeWidth, w.globals.radarPolygons.fill.colors[i]);
                    parent.add(polygon);
                });
                lines.forEach(function(l) {
                    parent.add(l);
                });
                if (w.config.yaxis[0].show) {
                    this.yaxisLabelsTextsPos.forEach(function(p, i) {
                        var yText = helpers.drawYAxisTexts(p.x, p.y, i, yaxisTexts[i]);
                        _this.yaxisLabels.add(yText);
                    });
                }
            }
        },
        {
            key: "drawXAxisTexts",
            value: function drawXAxisTexts() {
                var _this = this;
                var w = this.w;
                var xaxisLabelsConfig = w.config.xaxis.labels;
                var elXAxisWrap = this.graphics.group({
                    class: "apexcharts-xaxis"
                });
                var polygonPos = Utils$1.getPolygonPos(this.size, this.dataPointsLen);
                w.labelData.labels.forEach(function(label, i) {
                    var formatter = w.config.xaxis.labels.formatter;
                    var dataLabels = new DataLabels(_this.w, _this.ctx);
                    if (polygonPos[i]) {
                        var textPos = _this.getTextPos(polygonPos[i], _this.size);
                        var text = formatter(label, {
                            seriesIndex: -1,
                            dataPointIndex: i,
                            w: w
                        });
                        var dataLabelText = dataLabels.plotDataLabelsText({
                            x: textPos.newX,
                            y: textPos.newY,
                            text: text,
                            textAnchor: textPos.textAnchor,
                            i: i,
                            j: i,
                            parent: elXAxisWrap,
                            className: "apexcharts-xaxis-label",
                            color: Array.isArray(xaxisLabelsConfig.style.colors) && xaxisLabelsConfig.style.colors[i] ? xaxisLabelsConfig.style.colors[i] : "#a8a8a8",
                            dataLabelsConfig: __spreadValues({
                                textAnchor: textPos.textAnchor,
                                dropShadow: {
                                    enabled: false
                                }
                            }, xaxisLabelsConfig),
                            offsetCorrection: false
                        });
                        dataLabelText.on("click", function(e) {
                            if (typeof w.config.chart.events.xAxisLabelClick === "function") {
                                var opts = Object.assign({}, w, {
                                    labelIndex: i
                                });
                                w.config.chart.events.xAxisLabelClick(e, _this.ctx, opts);
                            }
                        });
                    }
                });
                return elXAxisWrap;
            }
        },
        {
            /**
   * @param {Array<Record<string, any>>} pos
   * @param {Record<string, any>} origin
   */ key: "createPaths",
            value: function createPaths(pos, origin) {
                var _this = this;
                var linePathsTo = [];
                var linePathsFrom = [];
                var areaPathsTo = [];
                var areaPathsFrom = [];
                if (pos.length) {
                    linePathsFrom = [
                        this.graphics.move(origin.x, origin.y)
                    ];
                    areaPathsFrom = [
                        this.graphics.move(origin.x, origin.y)
                    ];
                    var linePathTo = this.graphics.move(pos[0].x, pos[0].y);
                    var areaPathTo = this.graphics.move(pos[0].x, pos[0].y);
                    pos.forEach(function(p, i) {
                        linePathTo += _this.graphics.line(p.x, p.y);
                        areaPathTo += _this.graphics.line(p.x, p.y);
                        if (i === pos.length - 1) {
                            linePathTo += "Z";
                            areaPathTo += "Z";
                        }
                    });
                    linePathsTo.push(linePathTo);
                    areaPathsTo.push(areaPathTo);
                }
                return {
                    linePathsFrom: linePathsFrom,
                    linePathsTo: linePathsTo,
                    areaPathsFrom: areaPathsFrom,
                    areaPathsTo: areaPathsTo
                };
            }
        },
        {
            /**
   * @param {Record<string, any>} pos
   * @param {number} polygonSize
   */ key: "getTextPos",
            value: function getTextPos(pos, polygonSize) {
                var limit = 10;
                var textAnchor = "middle";
                var newX = pos.x;
                var newY = pos.y;
                if (Math.abs(pos.x) >= limit) {
                    if (pos.x > 0) {
                        textAnchor = "start";
                        newX += 10;
                    } else if (pos.x < 0) {
                        textAnchor = "end";
                        newX -= 10;
                    }
                } else {
                    textAnchor = "middle";
                }
                if (Math.abs(pos.y) >= polygonSize - limit) {
                    if (pos.y < 0) {
                        newY -= 10;
                    } else if (pos.y > 0) {
                        newY += 10;
                    }
                }
                return {
                    textAnchor: textAnchor,
                    newX: newX,
                    newY: newY
                };
            }
        },
        {
            /**
   * @param {number} realIndex
   */ key: "getPreviousPath",
            value: function getPreviousPath(realIndex) {
                var w = this.w;
                var pathFrom = null;
                for(var pp = 0; pp < w.globals.previousPaths.length; pp++){
                    var gpp = w.globals.previousPaths[pp];
                    if (gpp.paths.length > 0 && parseInt(gpp.realIndex, 10) === parseInt(String(realIndex), 10)) {
                        if (typeof w.globals.previousPaths[pp].paths[0] !== "undefined") {
                            pathFrom = w.globals.previousPaths[pp].paths[0].d;
                        }
                    }
                }
                return pathFrom;
            }
        },
        {
            /**
   * @param {any[]} dataRadiusArr
   * @param {any[]} angleArr
   */ key: "getDataPointsPos",
            value: function getDataPointsPos(dataRadiusArr, angleArr) {
                var dataPointsLen = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.dataPointsLen;
                dataRadiusArr = dataRadiusArr || [];
                angleArr = angleArr || [];
                var dataPointsPosArray = [];
                for(var j = 0; j < dataPointsLen; j++){
                    var curPointPos = {};
                    curPointPos.x = dataRadiusArr[j] * Math.sin(angleArr[j]);
                    curPointPos.y = -dataRadiusArr[j] * Math.cos(angleArr[j]);
                    dataPointsPosArray.push(curPointPos);
                }
                return dataPointsPosArray;
            }
        }
    ]);
    return Radar;
}();
var Radial = /*#__PURE__*/ function(Pie) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Radial, Pie);
    function Radial(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Radial);
        var _this;
        _this = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Radial, [
            w,
            ctx
        ]);
        _this.ctx = ctx;
        _this.w = w;
        _this.animBeginArr = [
            0
        ];
        _this.animDur = 0;
        _this.startAngle = w.config.plotOptions.radialBar.startAngle;
        _this.endAngle = w.config.plotOptions.radialBar.endAngle;
        _this.totalAngle = Math.abs(w.config.plotOptions.radialBar.endAngle - w.config.plotOptions.radialBar.startAngle);
        _this.trackStartAngle = w.config.plotOptions.radialBar.track.startAngle;
        _this.trackEndAngle = w.config.plotOptions.radialBar.track.endAngle;
        _this.barLabels = _this.w.config.plotOptions.radialBar.barLabels;
        _this.donutDataLabels = _this.w.config.plotOptions.radialBar.dataLabels;
        _this.radialDataLabels = _this.donutDataLabels;
        if (!_this.trackStartAngle) _this.trackStartAngle = _this.startAngle;
        if (!_this.trackEndAngle) _this.trackEndAngle = _this.endAngle;
        if (_this.endAngle === 360) _this.endAngle = 359.99;
        _this.margin = parseInt(w.config.plotOptions.radialBar.track.margin, 10);
        _this.onBarLabelClick = _this.onBarLabelClick.bind(_this);
        return _this;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Radial, [
        {
            /**
   * @param {any[]} series
   */ key: "draw",
            value: function draw(series) {
                var _a;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var ret = graphics.group({
                    class: "apexcharts-radialbar"
                });
                if (w.globals.noData) return ret;
                var elSeries = graphics.group();
                var centerY = this.defaultSize / 2;
                var centerX = w.layout.gridWidth / 2;
                var size = this.defaultSize / 2.05;
                if (!w.config.chart.sparkline.enabled) {
                    size = size - w.config.stroke.width - w.config.chart.dropShadow.blur;
                }
                var colorArr = w.globals.fill.colors;
                var rb = w.config.plotOptions.radialBar;
                var hasBands = Array.isArray(rb.bands) && rb.bands.length > 0;
                var hideTrack = hasBands && rb.bandsStyle && rb.bandsStyle.hideTrackWhenPresent;
                var isNeedleShape = rb.shape === "needle";
                if (rb.track.show && !hideTrack) {
                    var elTracks = this.drawTracks({
                        size: size,
                        centerX: centerX,
                        centerY: centerY,
                        colorArr: colorArr,
                        series: series
                    });
                    elSeries.add(elTracks);
                }
                if (hasBands) {
                    var elBands = this.drawBands({
                        size: size,
                        centerX: centerX,
                        centerY: centerY,
                        series: series
                    });
                    elSeries.add(elBands);
                }
                var elG = this.drawArcs({
                    size: size,
                    centerX: centerX,
                    centerY: centerY,
                    colorArr: colorArr,
                    series: series,
                    // When `needle.showValueArc` is true, render both the filled value-arc
                    // and the needle on top — required for gauges that want a progress
                    // ring with a pointer indicator (default still hides the arc when in
                    // needle shape, preserving prior behavior).
                    skipValueArc: isNeedleShape && !((_a = rb.needle) == null ? void 0 : _a.showValueArc)
                });
                if (rb.ticks && rb.ticks.show) {
                    var elTicks = this.drawTicks({
                        size: size,
                        centerX: centerX,
                        centerY: centerY,
                        series: series
                    });
                    var isInitialMount = this.initialAnim && !w.globals.dataChanged && !w.globals.resized;
                    if (isInitialMount && Environment.isBrowser() && w.globals.shouldAnimate) {
                        var ticksNode = elTicks.node;
                        ticksNode.style.opacity = "0";
                        ticksNode.style.transition = "opacity 280ms ease-out";
                        var sweepDur = w.config.chart.animations.speed || 800;
                        setTimeout(function() {
                            ticksNode.style.opacity = "1";
                        }, sweepDur);
                    }
                    elSeries.add(elTicks);
                }
                if (isNeedleShape) {
                    var elNeedle = this.drawNeedle({
                        size: size,
                        centerX: centerX,
                        centerY: centerY,
                        series: series
                    });
                    elSeries.add(elNeedle);
                }
                var totalAngle = 360;
                if (w.config.plotOptions.radialBar.startAngle < 0) {
                    totalAngle = this.totalAngle;
                }
                var angleRatio = (360 - totalAngle) / 360;
                w.globals.radialSize = size - size * angleRatio;
                if (this.radialDataLabels.value.show) {
                    var offset = Math.max(this.radialDataLabels.value.offsetY, this.radialDataLabels.name.offsetY);
                    w.globals.radialSize += offset * angleRatio;
                }
                elSeries.add(elG.g);
                if (w.config.plotOptions.radialBar.hollow.position === "front") {
                    elG.g.add(elG.elHollow);
                    if (elG.dataLabels) {
                        elG.g.add(elG.dataLabels);
                    }
                }
                ret.add(elSeries);
                return ret;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "drawTracks",
            value: function drawTracks(opts) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var g = graphics.group({
                    class: "apexcharts-tracks"
                });
                var filters = new Filters(this.w);
                var fill = new Fill(this.w);
                var strokeWidth = this.getStrokeWidth(opts);
                opts.size = opts.size - strokeWidth / 2;
                for(var i = 0; i < opts.series.length; i++){
                    var elRadialBarTrack = graphics.group({
                        class: "apexcharts-radialbar-track apexcharts-track"
                    });
                    g.add(elRadialBarTrack);
                    elRadialBarTrack.attr({
                        rel: i + 1
                    });
                    opts.size = opts.size - strokeWidth - this.margin;
                    var trackConfig = w.config.plotOptions.radialBar.track;
                    var pathFill = fill.fillPath({
                        seriesNumber: 0,
                        size: opts.size,
                        fillColors: Array.isArray(trackConfig.background) ? trackConfig.background[i] : trackConfig.background,
                        solid: true
                    });
                    var startAngle = this.trackStartAngle;
                    var endAngle = this.trackEndAngle;
                    if (Math.abs(endAngle) + Math.abs(startAngle) >= 360) endAngle = 360 - Math.abs(this.startAngle) - 0.1;
                    var elPath = graphics.drawPath({
                        d: "",
                        stroke: pathFill,
                        strokeWidth: strokeWidth * parseInt(trackConfig.strokeWidth, 10) / 100,
                        fill: "none",
                        strokeOpacity: trackConfig.opacity,
                        classes: "apexcharts-radialbar-area"
                    });
                    if (trackConfig.dropShadow.enabled) {
                        var shadow = trackConfig.dropShadow;
                        filters.dropShadow(elPath, shadow);
                    }
                    elRadialBarTrack.add(elPath);
                    elPath.attr("id", "apexcharts-radialbarTrack-" + i);
                    this.animatePaths(elPath, {
                        centerX: opts.centerX,
                        centerY: opts.centerY,
                        endAngle: endAngle,
                        startAngle: startAngle,
                        size: opts.size,
                        i: i,
                        totalItems: 2,
                        animBeginArr: 0,
                        dur: 0,
                        isTrack: true
                    });
                }
                return g;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "drawArcs",
            value: function drawArcs(opts) {
                var _this, _loop = function(i) {
                    var elRadialBarArc = graphics.group({
                        class: "apexcharts-series apexcharts-radial-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[i])
                    });
                    g.add(elRadialBarArc);
                    elRadialBarArc.attr({
                        rel: i + 1,
                        "data:realIndex": i
                    });
                    Series.addCollapsedClassToSeries(_this.w, elRadialBarArc, i);
                    opts.size = opts.size - strokeWidth - _this.margin;
                    var pathFill = fill.fillPath({
                        seriesNumber: i,
                        size: opts.size,
                        value: opts.series[i]
                    });
                    var startAngle = _this.startAngle;
                    var prevStartAngle = void 0;
                    var rb = w.config.plotOptions.radialBar;
                    var domainMin = typeof rb.min === "number" ? rb.min : 0;
                    var domainMax = typeof rb.max === "number" ? rb.max : 100;
                    var domainSpan = domainMax === domainMin ? 1 : domainMax - domainMin;
                    var valueToFraction = function valueToFraction(v) {
                        var clamped = Math.min(Math.max(v, domainMin), domainMax);
                        return Math.max(0, (clamped - domainMin) / domainSpan);
                    };
                    var dataValue = valueToFraction(Utils$1.negToZero(opts.series[i]));
                    var endAngle = Math.round(_this.totalAngle * dataValue) + _this.startAngle;
                    var prevEndAngle = void 0;
                    if (w.globals.dataChanged) {
                        prevStartAngle = _this.startAngle;
                        prevEndAngle = Math.round(_this.totalAngle * valueToFraction(Utils$1.negToZero(w.globals.previousPaths[i]))) + prevStartAngle;
                    }
                    var currFullAngle = Math.abs(endAngle) + Math.abs(startAngle);
                    if (currFullAngle > 360) {
                        endAngle = endAngle - 0.01;
                    }
                    var prevFullAngle = Math.abs(prevEndAngle) + Math.abs(prevStartAngle);
                    if (prevFullAngle > 360) {
                        prevEndAngle = prevEndAngle - 0.01;
                    }
                    var angle = endAngle - startAngle;
                    var dashArray = Array.isArray(w.config.stroke.dashArray) ? w.config.stroke.dashArray[i] : w.config.stroke.dashArray;
                    var morphFrom = morphActive ? _this.ctx.morphTypeChange.getInitialPathFor(i, 0) : null;
                    var morphFromType = morphActive ? _this.ctx.morphTypeChange.getFromType() : null;
                    var morphFromFilled = !!morphFrom && (morphFromType === "bar" || morphFromType === "funnel" || morphFromType === "pyramid" || morphFromType === "pie" || morphFromType === "donut" || morphFromType === "polarArea");
                    var elPath = graphics.drawPath({
                        d: morphFrom || "",
                        stroke: morphFromFilled ? "transparent" : opts.skipValueArc ? "transparent" : pathFill,
                        strokeWidth: morphFromFilled ? 0 : opts.skipValueArc ? 0 : strokeWidth,
                        fill: morphFromFilled ? pathFill : "none",
                        fillOpacity: w.config.fill.opacity,
                        classes: "apexcharts-radialbar-area apexcharts-radialbar-slice-" + i,
                        strokeDashArray: dashArray
                    });
                    var radialMidAngle = startAngle + angle / 2;
                    var radialArcCenter = Utils$1.polarToCartesian(opts.centerX, opts.centerY, opts.size, radialMidAngle);
                    Graphics.setAttrs(elPath.node, {
                        "data:angle": angle,
                        "data:value": opts.series[i],
                        "data:cx": radialArcCenter.x,
                        "data:cy": radialArcCenter.y
                    });
                    if (w.config.chart.dropShadow.enabled) {
                        var shadow = w.config.chart.dropShadow;
                        filters.dropShadow(elPath, shadow, i);
                    }
                    filters.setSelectionFilter(elPath, 0, i);
                    _this.addListeners(elPath, _this.radialDataLabels);
                    elRadialBarArc.add(elPath);
                    elPath.attr({
                        index: 0,
                        j: i
                    });
                    if (_this.barLabels.enabled) {
                        var barStartCords = Utils$1.polarToCartesian(opts.centerX, opts.centerY, opts.size, startAngle);
                        var text = _this.barLabels.formatter(w.seriesData.seriesNames[i], {
                            seriesIndex: i,
                            w: w
                        });
                        var classes = [
                            "apexcharts-radialbar-label"
                        ];
                        if (!_this.barLabels.onClick) {
                            classes.push("apexcharts-no-click");
                        }
                        var textColor = _this.barLabels.useSeriesColors ? w.globals.colors[i] : w.config.chart.foreColor;
                        if (!textColor) {
                            textColor = w.config.chart.foreColor;
                        }
                        var x = barStartCords.x + _this.barLabels.offsetX;
                        var y = barStartCords.y + _this.barLabels.offsetY;
                        var elText = graphics.drawText({
                            x: x,
                            y: y,
                            text: text,
                            textAnchor: "end",
                            dominantBaseline: "middle",
                            fontFamily: _this.barLabels.fontFamily,
                            fontWeight: _this.barLabels.fontWeight,
                            fontSize: _this.barLabels.fontSize,
                            foreColor: textColor,
                            cssClass: classes.join(" ")
                        });
                        elText.on("click", _this.onBarLabelClick);
                        elText.attr({
                            rel: i + 1
                        });
                        if (startAngle !== 0) {
                            elText.attr({
                                "transform-origin": "".concat(x, " ").concat(y),
                                transform: "rotate(".concat(startAngle, " 0 0)")
                            });
                        }
                        elRadialBarArc.add(elText);
                    }
                    var dur = 0;
                    if (_this.initialAnim && !w.globals.resized && !w.globals.dataChanged) {
                        dur = w.config.chart.animations.speed;
                    }
                    if (w.globals.dataChanged) {
                        dur = w.config.chart.animations.dynamicAnimation.speed;
                    }
                    _this.animDur = dur / (opts.series.length * 1.2) + _this.animDur;
                    _this.animBeginArr.push(_this.animDur);
                    if (morphActive && morphFrom) {
                        var morphSpeed = _this.ctx.morphTypeChange.getSpeed();
                        var actualArcD = _this.getPiePath({
                            me: _this,
                            startAngle: startAngle,
                            angle: angle,
                            size: opts.size
                        });
                        if (morphFromFilled) {
                            var targetD = _this.ctx.morphTypeChange.buildRingSegmentPath(opts.centerX, opts.centerY, opts.size, strokeWidth, startAngle, startAngle + angle);
                            elPath.animate(morphSpeed).plot(targetD, "polygons").after(/** @this {any} */ function() {
                                this.attr({
                                    d: actualArcD,
                                    fill: "none",
                                    stroke: opts.skipValueArc ? "transparent" : pathFill,
                                    "stroke-width": opts.skipValueArc ? 0 : strokeWidth
                                });
                            });
                        } else {
                            elPath.animate(morphSpeed).plot(actualArcD, "polygons").attr({
                                "stroke-width": strokeWidth
                            });
                        }
                    } else {
                        _this.animatePaths(elPath, {
                            centerX: opts.centerX,
                            centerY: opts.centerY,
                            endAngle: endAngle,
                            startAngle: startAngle,
                            prevEndAngle: prevEndAngle,
                            prevStartAngle: prevStartAngle,
                            size: opts.size,
                            i: i,
                            totalItems: 2,
                            animBeginArr: _this.animBeginArr,
                            dur: dur,
                            shouldSetPrevPaths: true
                        });
                    }
                };
                var _a;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var fill = new Fill(this.w);
                var filters = new Filters(this.w);
                var g = graphics.group();
                var strokeWidth = this.getStrokeWidth(opts);
                opts.size = opts.size - strokeWidth / 2;
                var hollowFillID = w.config.plotOptions.radialBar.hollow.background;
                var hollowSize = opts.size - strokeWidth * opts.series.length - this.margin * opts.series.length - strokeWidth * parseInt(w.config.plotOptions.radialBar.track.strokeWidth, 10) / 100 / 2;
                var hollowRadius = hollowSize - w.config.plotOptions.radialBar.hollow.margin;
                if (w.config.plotOptions.radialBar.hollow.image !== void 0) {
                    hollowFillID = this.drawHollowImage(opts, g, hollowSize, hollowFillID);
                }
                var elHollow = this.drawHollow({
                    size: hollowRadius,
                    centerX: opts.centerX,
                    centerY: opts.centerY,
                    fill: hollowFillID ? hollowFillID : "transparent"
                });
                if (w.config.plotOptions.radialBar.hollow.dropShadow.enabled) {
                    var shadow = w.config.plotOptions.radialBar.hollow.dropShadow;
                    filters.dropShadow(elHollow, shadow);
                }
                var shown = 1;
                if (!this.radialDataLabels.total.show && w.seriesData.series.length > 1) {
                    shown = 0;
                }
                var dataLabels = null;
                if (this.radialDataLabels.show) {
                    var dataLabelsGroup = w.dom.Paper.findOne(".apexcharts-datalabels-group");
                    dataLabels = this.renderInnerDataLabels(dataLabelsGroup, this.radialDataLabels, {
                        hollowSize: hollowSize,
                        centerX: opts.centerX,
                        centerY: opts.centerY,
                        opacity: shown
                    });
                }
                if (w.config.plotOptions.radialBar.hollow.position === "back") {
                    g.add(elHollow);
                    if (dataLabels) {
                        g.add(dataLabels);
                    }
                }
                var reverseLoop = false;
                if (w.config.plotOptions.radialBar.inverseOrder) {
                    reverseLoop = true;
                }
                var morphActive = ((_a = this.ctx.morphTypeChange) == null ? void 0 : _a.isActive()) === true;
                for(var i = reverseLoop ? opts.series.length - 1 : 0; reverseLoop ? i >= 0 : i < opts.series.length; reverseLoop ? i-- : i++)_this = this, _loop(i);
                return {
                    g: g,
                    elHollow: elHollow,
                    dataLabels: dataLabels
                };
            }
        },
        {
            /**
   * Map a domain value (between `min` and `max`) to the corresponding angle
   * in the gauge's `startAngle`..`endAngle` range. Values outside the
   * domain are clamped.
   *
   * @param {number} value
   * @returns {number}
   */ key: "_angleAtValue",
            value: function _angleAtValue(value) {
                var rb = this.w.config.plotOptions.radialBar;
                var min = typeof rb.min === "number" ? rb.min : 0;
                var max = typeof rb.max === "number" ? rb.max : 100;
                var safeMax = max === min ? min + 1 : max;
                var clamped = Math.max(min, Math.min(safeMax, Number(value)));
                var t = (clamped - min) / (safeMax - min);
                return this.startAngle + t * (this.endAngle - this.startAngle);
            }
        },
        {
            /**
   * Build an SVG arc path from `startAngle` to `endAngle` at radius `r`
   * around `(cx, cy)`. Angles are in degrees, with 0° at the top.
   * Used by drawBands; mirrors the `M ... A ... ` form used elsewhere.
   *
   * @param {number} cx
   * @param {number} cy
   * @param {number} r
   * @param {number} startAngle
   * @param {number} endAngle
   * @returns {string}
   */ key: "_describeArc",
            value: function _describeArc(cx, cy, r, startAngle, endAngle) {
                var start = Utils$1.polarToCartesian(cx, cy, r, endAngle);
                var end = Utils$1.polarToCartesian(cx, cy, r, startAngle);
                var sweep = endAngle - startAngle;
                var largeArc = Math.abs(sweep) > 180 ? 1 : 0;
                return "M ".concat(start.x, " ").concat(start.y, " A ").concat(r, " ").concat(r, " 0 ").concat(largeArc, " 0 ").concat(end.x, " ").concat(end.y);
            }
        },
        {
            /**
   * Draw threshold bands as colored arc segments along the gauge arc.
   * Bands sit behind the value-arc and tick marks. Used for gauges that
   * indicate ranges like 0-30 critical / 30-70 warning / 70-100 healthy.
   *
   * @param {Record<string, any>} opts
   */ key: "drawBands",
            value: function drawBands(opts) {
                var w = this.w;
                var graphics = new Graphics(this.w);
                var rb = w.config.plotOptions.radialBar;
                var bands = rb.bands || [];
                var g = graphics.group({
                    class: "apexcharts-gauge-bands"
                });
                var strokeWidth = this.getStrokeWidth(opts);
                var radius = opts.size - strokeWidth / 2 - strokeWidth - this.margin;
                var bandStroke = strokeWidth * parseInt(rb.bandsStyle.strokeWidth, 10) / 100;
                var min = typeof rb.min === "number" ? rb.min : 0;
                var max = typeof rb.max === "number" ? rb.max : 100;
                var gapDeg = max === min ? 0 : (rb.bandsStyle.gap || 0) * ((this.endAngle - this.startAngle) / (max - min));
                for(var b = 0; b < bands.length; b++){
                    var band = bands[b];
                    if (band.from === void 0 || band.to === void 0) continue;
                    var a1 = this._angleAtValue(band.from);
                    var a2 = this._angleAtValue(band.to);
                    var startA = Math.min(a1, a2) + gapDeg / 2;
                    var endA = Math.max(a1, a2) - gapDeg / 2;
                    if (endA - startA <= 0) continue;
                    var elBand = graphics.drawPath({
                        d: this._describeArc(opts.centerX, opts.centerY, radius, startA, endA),
                        stroke: band.color || "#ccc",
                        strokeWidth: bandStroke,
                        fill: "none",
                        strokeLinecap: rb.bandsStyle.linecap || "butt",
                        classes: "apexcharts-gauge-band"
                    });
                    elBand.node.setAttribute("data-band-index", String(b));
                    g.add(elBand);
                }
                return g;
            }
        },
        {
            /**
   * Draw tick marks (major + minor) along the gauge arc, with optional
   * value labels at each major tick.
   *
   * @param {Record<string, any>} opts
   */ key: "drawTicks",
            value: function drawTicks(opts) {
                var _this = this;
                var _a, _b, _c, _d;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var rb = w.config.plotOptions.radialBar;
                var ticks = rb.ticks;
                var g = graphics.group({
                    class: "apexcharts-gauge-ticks"
                });
                var strokeWidth = this.getStrokeWidth(opts);
                var radius = opts.size - strokeWidth / 2 - strokeWidth - this.margin;
                var min = typeof rb.min === "number" ? rb.min : 0;
                var max = typeof rb.max === "number" ? rb.max : 100;
                var majorCount = Math.max(2, (_b = (_a = ticks.major) == null ? void 0 : _a.count) != null ? _b : 11);
                var minorCount = Math.max(0, (_d = (_c = ticks.minor) == null ? void 0 : _c.count) != null ? _d : 0);
                var drawTickAt = function drawTickAt(value, cfg, isMajor) {
                    var _a2, _b2, _c2;
                    var angle = _this._angleAtValue(value);
                    var length = (_a2 = cfg.length) != null ? _a2 : 8;
                    var inner = cfg.placement === "inside" ? radius - length : radius;
                    var outer = cfg.placement === "inside" ? radius : radius + length;
                    var p1 = Utils$1.polarToCartesian(opts.centerX, opts.centerY, inner, angle);
                    var p2 = Utils$1.polarToCartesian(opts.centerX, opts.centerY, outer, angle);
                    var line = graphics.drawLine(p1.x, p1.y, p2.x, p2.y, cfg.color || (isMajor ? "#666" : "#999"), 0, cfg.width || (isMajor ? 2 : 1));
                    g.add(line);
                    if (isMajor && ((_b2 = ticks.labels) == null ? void 0 : _b2.show)) {
                        var labelRadius = (cfg.placement === "inside" ? inner : outer) + (cfg.placement === "inside" ? -1 : 1) * ((_c2 = ticks.labels.offset) != null ? _c2 : 6);
                        var labelPos = Utils$1.polarToCartesian(opts.centerX, opts.centerY, labelRadius, angle);
                        var labelText = typeof ticks.labels.formatter === "function" ? ticks.labels.formatter(value) : String(value);
                        var elText = graphics.drawText({
                            x: labelPos.x,
                            y: labelPos.y,
                            text: labelText,
                            textAnchor: "middle",
                            dominantBaseline: "middle",
                            fontFamily: ticks.labels.fontFamily,
                            fontSize: ticks.labels.fontSize,
                            fontWeight: ticks.labels.fontWeight,
                            foreColor: ticks.labels.color,
                            cssClass: "apexcharts-gauge-tick-label"
                        });
                        g.add(elText);
                    }
                };
                for(var m = 0; m < majorCount; m++){
                    var t = m / (majorCount - 1);
                    var value = min + t * (max - min);
                    drawTickAt(value, ticks.major || {}, true);
                    if (m < majorCount - 1 && minorCount > 0) {
                        for(var n = 1; n <= minorCount; n++){
                            var tMinor = (m + n / (minorCount + 1)) / (majorCount - 1);
                            var minorValue = min + tMinor * (max - min);
                            drawTickAt(minorValue, ticks.minor || {}, false);
                        }
                    }
                }
                return g;
            }
        },
        {
            /**
   * Draw a rotating needle pointing at the current series value. Only
   * called when `plotOptions.radialBar.shape === 'needle'`. The needle is
   * a tapered polygon inside a `<g>` whose rotation transform is animated
   * from `startAngle` to the value's angle.
   *
   * Renders a single needle for the first series value (gauge use case).
   * Additional series are ignored — drilled-down multi-series gauges are
   * out of scope for this iteration.
   *
   * @param {Record<string, any>} opts
   */ key: "drawNeedle",
            value: function drawNeedle(opts) {
                var _this = this;
                var _a, _b, _c, _d, _e;
                var w = this.w;
                var graphics = new Graphics(this.w);
                var rb = w.config.plotOptions.radialBar;
                var cfg = rb.needle || {};
                var g = graphics.group({
                    class: "apexcharts-gauge-needle"
                });
                if (!opts.series || opts.series.length === 0) return g;
                var strokeWidth = this.getStrokeWidth(opts);
                var arcRadius = opts.size - strokeWidth / 2 - strokeWidth - this.margin;
                var length = typeof cfg.length === "string" && cfg.length.endsWith("%") ? arcRadius * parseInt(cfg.length, 10) / 100 : Number(cfg.length || arcRadius * 0.85);
                var baseW = (_a = cfg.baseWidth) != null ? _a : 4;
                var tipW = (_b = cfg.tipWidth) != null ? _b : 1;
                var color = cfg.color || "#333";
                var cx = opts.centerX;
                var needleOffsetY = Number((_c = cfg.offsetY) != null ? _c : 0);
                var cy = opts.centerY + needleOffsetY;
                var path = "M ".concat(cx + baseW / 2, " ").concat(cy, " A ").concat(baseW / 2, " ").concat(baseW / 2, " 0 0 1 ").concat(cx - baseW / 2, " ").concat(cy, " L ").concat(cx - tipW / 2, " ").concat(cy - length, " L ").concat(cx + tipW / 2, " ").concat(cy - length, " Z");
                var elNeedle = graphics.drawPath({
                    d: path,
                    stroke: color,
                    strokeWidth: 0,
                    fill: color,
                    classes: "apexcharts-gauge-needle-shape"
                });
                g.add(elNeedle);
                var value = Number(opts.series[0]);
                var targetAngle = this._angleAtValue(value);
                var isInitialMount = this.initialAnim && !w.globals.dataChanged && !w.globals.resized;
                var ctx = /** @type {any} */ this.ctx;
                var fromAngle = typeof ctx._lastNeedleAngle === "number" ? ctx._lastNeedleAngle : this.startAngle;
                ctx._lastNeedleAngle = targetAngle;
                var shouldAnimate = Environment.isBrowser() && w.globals.shouldAnimate && (isInitialMount || w.globals.dataChanged);
                if (shouldAnimate && fromAngle !== targetAngle) {
                    var node = g.node;
                    node.setAttribute("transform-origin", "".concat(cx, " ").concat(cy));
                    node.setAttribute("transform", "rotate(".concat(fromAngle, ")"));
                    var speed = ((_d = cfg.animation) == null ? void 0 : _d.duration) && Number(cfg.animation.duration) || cfg.animationSpeed && Number(cfg.animationSpeed) || ((_e = w.config.chart.animations.dynamicAnimation) == null ? void 0 : _e.speed) || w.config.chart.animations.speed || 800;
                    var c1 = 1.70158;
                    var c3 = c1 + 1;
                    var easeOutBack2 = function easeOutBack2(t) {
                        return 1 + c3 * Math.pow(t - 1, 3) + c1 * Math.pow(t - 1, 2);
                    };
                    var easeOutCubic2 = function easeOutCubic2(t) {
                        return 1 - Math.pow(1 - t, 3);
                    };
                    var ease = isInitialMount ? easeOutBack2 : easeOutCubic2;
                    if (w.globals.radialNeedleRAF != null) {
                        BrowserAPIs.cancelAnimationFrame(w.globals.radialNeedleRAF);
                        w.globals.radialNeedleRAF = null;
                    }
                    var startAt = performance.now();
                    var step = function step1(now) {
                        if (_this.w.globals.isDestroyed) {
                            w.globals.radialNeedleRAF = null;
                            return;
                        }
                        var t = Math.max(0, Math.min(1, (now - startAt) / speed));
                        var angle = fromAngle + (targetAngle - fromAngle) * ease(t);
                        node.setAttribute("transform", "rotate(".concat(angle, ")"));
                        if (t < 1) {
                            w.globals.radialNeedleRAF = BrowserAPIs.requestAnimationFrame(step);
                        } else {
                            w.globals.radialNeedleRAF = null;
                        }
                    };
                    w.globals.radialNeedleRAF = BrowserAPIs.requestAnimationFrame(step);
                } else {
                    g.attr({
                        "transform-origin": "".concat(cx, " ").concat(cy),
                        transform: "rotate(".concat(targetAngle, ")")
                    });
                }
                return g;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "drawHollow",
            value: function drawHollow(opts) {
                var _a;
                var graphics = new Graphics(this.w);
                var hollow = this.w.config.plotOptions.radialBar.hollow;
                var circle = graphics.drawCircle(opts.size * 2);
                var attrs = {
                    class: "apexcharts-radialbar-hollow",
                    cx: opts.centerX,
                    cy: opts.centerY,
                    r: opts.size,
                    fill: opts.fill
                };
                if (hollow.stroke || hollow.strokeDasharray) {
                    attrs.stroke = hollow.stroke || "transparent";
                    attrs["stroke-width"] = (_a = hollow.strokeWidth) != null ? _a : 1;
                    if (hollow.strokeDasharray) {
                        attrs["stroke-dasharray"] = hollow.strokeDasharray;
                    }
                }
                circle.attr(attrs);
                return circle;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   * @param {any} g
   * @param {number} hollowSize
   * @param {string} hollowFillID
   */ key: "drawHollowImage",
            value: function drawHollowImage(opts, g, hollowSize, hollowFillID) {
                var w = this.w;
                var fill = new Fill(this.w);
                var randID = Utils$1.randomId();
                var hollowFillImg = w.config.plotOptions.radialBar.hollow.image;
                if (w.config.plotOptions.radialBar.hollow.imageClipped) {
                    fill.clippedImgArea({
                        width: hollowSize,
                        height: hollowSize,
                        image: hollowFillImg,
                        patternID: "pattern".concat(w.globals.cuid).concat(randID)
                    });
                    hollowFillID = "url(#pattern".concat(w.globals.cuid).concat(randID, ")");
                } else {
                    var imgWidth = w.config.plotOptions.radialBar.hollow.imageWidth;
                    var imgHeight = w.config.plotOptions.radialBar.hollow.imageHeight;
                    if (imgWidth === void 0 && imgHeight === void 0) {
                        var image = w.dom.Paper.image(hollowFillImg, /** @this {any} */ function(loader) {
                            this.move(opts.centerX - loader.width / 2 + w.config.plotOptions.radialBar.hollow.imageOffsetX, opts.centerY - loader.height / 2 + w.config.plotOptions.radialBar.hollow.imageOffsetY);
                        });
                        g.add(image);
                    } else {
                        var image1 = w.dom.Paper.image(hollowFillImg, /** @this {any} */ function() {
                            this.move(opts.centerX - imgWidth / 2 + w.config.plotOptions.radialBar.hollow.imageOffsetX, opts.centerY - imgHeight / 2 + w.config.plotOptions.radialBar.hollow.imageOffsetY);
                            this.size(imgWidth, imgHeight);
                        });
                        g.add(image1);
                    }
                }
                return hollowFillID;
            }
        },
        {
            /**
   * @param {Record<string, any>} opts
   */ key: "getStrokeWidth",
            value: function getStrokeWidth(opts) {
                var w = this.w;
                return opts.size * (100 - parseInt(w.config.plotOptions.radialBar.hollow.size, 10)) / 100 / (opts.series.length + 1) - this.margin;
            }
        },
        {
            /**
   * @param {Event} e
   */ key: "onBarLabelClick",
            value: function onBarLabelClick(e) {
                var _a;
                var target = /** @type {Element} */ e.target;
                var seriesIndex = parseInt((_a = target.getAttribute("rel")) != null ? _a : "", 10) - 1;
                var legendClick = this.barLabels.onClick;
                var w = this.w;
                if (legendClick) {
                    legendClick(w.seriesData.seriesNames[seriesIndex], {
                        w: w,
                        seriesIndex: seriesIndex
                    });
                }
            }
        }
    ]);
    return Radial;
}(Pie);
var RangeBar = /*#__PURE__*/ function(Bar) {
    "use strict";
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_inherits$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(RangeBar, Bar);
    function RangeBar() {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, RangeBar);
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_call_super$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, RangeBar, arguments);
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(RangeBar, [
        {
            /**
   * @param {any[]} series
   * @param {number} seriesIndex
   */ key: "draw",
            value: function draw(series, seriesIndex) {
                var _a, _b, _c, _d, _e, _f, _g, _h, _i;
                var w = this.w;
                var graphics = new Graphics(this.w);
                this.rangeBarOptions = this.w.config.plotOptions.rangeBar;
                this.series = series;
                this.seriesRangeStart = w.rangeData.seriesRangeStart;
                this.seriesRangeEnd = w.rangeData.seriesRangeEnd;
                this.barHelpers.initVariables(series);
                var ret = graphics.group({
                    class: "apexcharts-rangebar-series apexcharts-plot-series"
                });
                for(var i = 0; i < series.length; i++){
                    var x = void 0, y = void 0;
                    var realIndex = w.globals.comboCharts ? /** @type {any} */ seriesIndex[i] : i;
                    var columnGroupIndex = this.barHelpers.getGroupIndex(realIndex).columnGroupIndex;
                    var elSeries = graphics.group({
                        class: "apexcharts-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[realIndex]),
                        rel: i + 1,
                        "data:realIndex": realIndex
                    });
                    Series.addCollapsedClassToSeries(this.w, elSeries, realIndex);
                    if (series[i].length > 0) {
                        this.visibleI = this.visibleI + 1;
                    }
                    var translationsIndex = 0;
                    if (this.yRatio.length > 1) {
                        this.yaxisIndex = /** @type {any} */ w.globals.seriesYAxisReverseMap[realIndex][0];
                        translationsIndex = realIndex;
                    }
                    var initPositions = this.barHelpers.initialPositions(realIndex);
                    var initY = initPositions.y, zeroW = initPositions.zeroW, // zeroW is the baseline where 0 meets x axis
                    initX = initPositions.x, zeroH = initPositions.zeroH;
                    var barWidth = (_a = initPositions.barWidth) != null ? _a : 0;
                    var barHeight = (_b = initPositions.barHeight) != null ? _b : 0;
                    var yDivision = (_c = initPositions.yDivision) != null ? _c : 0;
                    var xDivision = (_d = initPositions.xDivision) != null ? _d : 0;
                    y = initY;
                    x = initX;
                    var elDataLabelsWrap = graphics.group({
                        class: "apexcharts-datalabels",
                        "data:realIndex": realIndex
                    });
                    var elGoalsMarkers = graphics.group({
                        class: "apexcharts-rangebar-goals-markers"
                    });
                    for(var j = 0; j < w.globals.dataPoints; j++){
                        var strokeWidth = this.barHelpers.getStrokeWidth(i, j, realIndex);
                        var y1 = this.seriesRangeStart[i][j];
                        var y2 = this.seriesRangeEnd[i][j];
                        var paths = /** @type {any} */ null;
                        var barXPosition = null;
                        var barYPosition = null;
                        var params = {
                            x: x,
                            y: y,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries
                        };
                        var seriesLen = this.seriesLen;
                        if (w.config.plotOptions.bar.rangeBarGroupRows) {
                            seriesLen = 1;
                        }
                        if (typeof /** @type {Record<string,any>} */ ((_e = w.config.series[i].data) == null ? void 0 : _e[j]) === "undefined") {
                            break;
                        }
                        if (this.isHorizontal) {
                            barYPosition = y + barHeight * /** @type {any} */ this.visibleI;
                            var srty = (yDivision - barHeight * seriesLen) / 2;
                            if (/** @type {Record<string,any>} */ (_g = (_f = w.config.series[i].data) == null ? void 0 : _f[j]) == null ? void 0 : _g.x) {
                                var positions = this.detectOverlappingBars({
                                    i: i,
                                    j: j,
                                    barYPosition: barYPosition,
                                    srty: srty,
                                    barHeight: barHeight,
                                    yDivision: yDivision,
                                    initPositions: initPositions
                                });
                                barHeight = positions.barHeight;
                                barYPosition = positions.barYPosition;
                            }
                            paths = this.drawRangeBarPaths(__spreadValues({
                                indexes: {
                                    i: i,
                                    j: j,
                                    realIndex: realIndex
                                },
                                barHeight: barHeight,
                                barYPosition: barYPosition,
                                zeroW: zeroW,
                                yDivision: yDivision,
                                y1: y1,
                                y2: y2
                            }, params));
                            barWidth = paths.barWidth;
                        } else {
                            if (w.axisFlags.isXNumeric) {
                                x = (w.seriesData.seriesX[i][j] - w.globals.minX) / this.xRatio - barWidth / 2;
                            }
                            barXPosition = x + barWidth * /** @type {any} */ this.visibleI;
                            var srtx = (xDivision - barWidth * seriesLen) / 2;
                            if (/** @type {Record<string,any>} */ (_i = (_h = w.config.series[i].data) == null ? void 0 : _h[j]) == null ? void 0 : _i.x) {
                                var positions1 = this.detectOverlappingBars({
                                    i: i,
                                    j: j,
                                    barXPosition: barXPosition,
                                    srtx: srtx,
                                    barWidth: barWidth,
                                    xDivision: xDivision,
                                    initPositions: initPositions
                                });
                                barWidth = positions1.barWidth;
                                barXPosition = positions1.barXPosition;
                            }
                            paths = this.drawRangeColumnPaths(__spreadValues({
                                indexes: {
                                    i: i,
                                    j: j,
                                    realIndex: realIndex,
                                    translationsIndex: translationsIndex
                                },
                                barWidth: barWidth,
                                barXPosition: barXPosition,
                                zeroH: zeroH,
                                xDivision: xDivision
                            }, params));
                            barHeight = paths.barHeight;
                        }
                        var barGoalLine = this.barHelpers.drawGoalLine({
                            barXPosition: paths.barXPosition,
                            barYPosition: barYPosition,
                            goalX: paths.goalX,
                            goalY: paths.goalY,
                            barHeight: barHeight,
                            barWidth: barWidth
                        });
                        if (barGoalLine) {
                            elGoalsMarkers.add(barGoalLine);
                        }
                        y = paths.y;
                        x = paths.x;
                        var pathFill = this.barHelpers.getPathFillColor(series, i, j, realIndex);
                        this.renderSeries({
                            realIndex: realIndex,
                            pathFill: pathFill.color,
                            lineFill: pathFill.useRangeColor ? pathFill.color : w.globals.stroke.colors[realIndex],
                            j: j,
                            i: i,
                            x: x,
                            y: y,
                            y1: y1,
                            y2: y2,
                            pathFrom: paths.pathFrom,
                            pathTo: paths.pathTo,
                            strokeWidth: strokeWidth,
                            elSeries: elSeries,
                            series: series,
                            barHeight: barHeight,
                            barWidth: barWidth,
                            barXPosition: barXPosition,
                            barYPosition: barYPosition,
                            columnGroupIndex: columnGroupIndex,
                            elDataLabelsWrap: elDataLabelsWrap,
                            elGoalsMarkers: elGoalsMarkers,
                            visibleSeries: this.visibleI,
                            type: "rangebar"
                        });
                    }
                    ret.add(elSeries);
                }
                return ret;
            }
        },
        {
            /** @param {{ i?: any, j?: any, barYPosition?: any, barXPosition?: any, srty?: any, srtx?: any, barHeight?: any, barWidth?: any, yDivision?: any, xDivision?: any, initPositions?: any }} opts */ key: "detectOverlappingBars",
            value: function detectOverlappingBars(param) {
                var i = param.i, j = param.j, barYPosition = param.barYPosition, barXPosition = param.barXPosition, srty = param.srty, srtx = param.srtx, barHeight = param.barHeight, barWidth = param.barWidth, yDivision = param.yDivision, xDivision = param.xDivision, initPositions = param.initPositions;
                var _a, _b, _c, _d;
                var w = this.w;
                var overlaps = [];
                var rangeName = (_b = (_a = w.globals.seriesRangeName) == null ? void 0 : _a[i]) == null ? void 0 : _b[j];
                var x = /** @type {Record<string,any>} */ (_d = (_c = w.config.series[i].data) == null ? void 0 : _c[j]) == null ? void 0 : _d.x;
                var labelX = Array.isArray(x) ? x.join(" ") : x;
                var rowIndex = w.labelData.labels.map(function(_) {
                    return Array.isArray(_) ? _.join(" ") : _;
                }).indexOf(labelX);
                var overlappedIndex = w.rangeData.seriesRange[i].findIndex(function(tx) {
                    var _a2;
                    return tx.x === labelX && ((_a2 = tx.overlaps) == null ? void 0 : _a2.size) > 0;
                });
                if (this.isHorizontal) {
                    if (w.config.plotOptions.bar.rangeBarGroupRows) {
                        barYPosition = srty + yDivision * rowIndex;
                    } else {
                        barYPosition = srty + barHeight * this.visibleI + yDivision * rowIndex;
                    }
                    if (overlappedIndex > -1 && !w.config.plotOptions.bar.rangeBarOverlap) {
                        overlaps = Array.from(/** @type {any} */ w.rangeData.seriesRange[i][overlappedIndex].overlaps);
                        if (overlaps.indexOf(rangeName) > -1) {
                            barHeight = initPositions.barHeight / overlaps.length;
                            barYPosition = barHeight * this.visibleI + yDivision * (100 - parseInt(this.barOptions.barHeight, 10)) / 100 / 2 + barHeight * (this.visibleI + overlaps.indexOf(rangeName)) + yDivision * rowIndex;
                        }
                    }
                } else {
                    if (rowIndex > -1 && !w.labelData.timescaleLabels.length) {
                        if (w.config.plotOptions.bar.rangeBarGroupRows) {
                            barXPosition = srtx + xDivision * rowIndex;
                        } else {
                            barXPosition = srtx + barWidth * this.visibleI + xDivision * rowIndex;
                        }
                    }
                    if (overlappedIndex > -1 && !w.config.plotOptions.bar.rangeBarOverlap) {
                        overlaps = Array.from(/** @type {any} */ w.rangeData.seriesRange[i][overlappedIndex].overlaps);
                        if (overlaps.indexOf(rangeName) > -1) {
                            barWidth = initPositions.barWidth / overlaps.length;
                            barXPosition = barWidth * this.visibleI + xDivision * (100 - parseInt(this.barOptions.barWidth, 10)) / 100 / 2 + barWidth * (this.visibleI + overlaps.indexOf(rangeName)) + xDivision * rowIndex;
                        }
                    }
                }
                return {
                    barYPosition: barYPosition,
                    barXPosition: barXPosition,
                    barHeight: barHeight,
                    barWidth: barWidth
                };
            }
        },
        {
            /** @param {{indexes: any, x: any, xDivision: any, barWidth: any, barXPosition: any, zeroH: any}} opts */ key: "drawRangeColumnPaths",
            value: function drawRangeColumnPaths(param) {
                var indexes = param.indexes, x = param.x, xDivision = param.xDivision, barWidth = param.barWidth, barXPosition = param.barXPosition, zeroH = param.zeroH;
                var _a, _b;
                var w = this.w;
                var i = indexes.i, j = indexes.j, realIndex = indexes.realIndex, translationsIndex = indexes.translationsIndex;
                var yRatio = this.yRatio[translationsIndex];
                var range = this.getRangeValue(realIndex, j);
                var y1 = Math.min(range.start, range.end);
                var y2 = Math.max(range.start, range.end);
                if (typeof /** @type {any} */ ((_a = this.series[i]) == null ? void 0 : _a[j]) === "undefined" || /** @type {any} */ ((_b = this.series[i]) == null ? void 0 : _b[j]) === null) {
                    y1 = zeroH;
                } else {
                    y1 = zeroH - y1 / yRatio;
                    y2 = zeroH - y2 / yRatio;
                }
                var barHeight = Math.abs(y2 - y1);
                var paths = this.barHelpers.getColumnPaths({
                    barXPosition: barXPosition,
                    barWidth: barWidth,
                    y1: y1,
                    y2: y2,
                    strokeWidth: this.strokeWidth,
                    series: this.seriesRangeEnd,
                    realIndex: realIndex,
                    i: realIndex,
                    j: j,
                    w: w
                });
                if (!w.axisFlags.isXNumeric) {
                    x = x + xDivision;
                } else {
                    var xForNumericXAxis = this.getBarXForNumericXAxis({
                        x: x,
                        j: j,
                        realIndex: realIndex,
                        barWidth: barWidth
                    });
                    x = xForNumericXAxis.x;
                    barXPosition = xForNumericXAxis.barXPosition;
                }
                return {
                    pathTo: paths.pathTo,
                    pathFrom: paths.pathFrom,
                    barHeight: barHeight,
                    x: x,
                    y: range.start < 0 && range.end < 0 ? y1 : y2,
                    goalY: this.barHelpers.getGoalValues("y", /** @type {any} */ null, zeroH, realIndex, j, translationsIndex),
                    barXPosition: barXPosition
                };
            }
        },
        {
            /**
   * @param {number} val
   */ key: "preventBarOverflow",
            value: function preventBarOverflow(val) {
                var w = this.w;
                if (val < 0) {
                    val = 0;
                }
                if (val > w.layout.gridWidth) {
                    val = w.layout.gridWidth;
                }
                return val;
            }
        },
        {
            /** @param {{indexes: any, y: any, y1: any, y2: any, yDivision: any, barHeight: any, barYPosition: any, zeroW: any}} opts */ key: "drawRangeBarPaths",
            value: function drawRangeBarPaths(param) {
                var indexes = param.indexes, y = param.y, y1 = param.y1, y2 = param.y2, yDivision = param.yDivision, barHeight = param.barHeight, barYPosition = param.barYPosition, zeroW = param.zeroW;
                var w = this.w;
                var realIndex = indexes.realIndex, j = indexes.j;
                var x1 = this.preventBarOverflow(zeroW + y1 / this.invertedYRatio);
                var x2 = this.preventBarOverflow(zeroW + y2 / this.invertedYRatio);
                var range = this.getRangeValue(realIndex, j);
                var barWidth = Math.abs(x2 - x1);
                var paths = this.barHelpers.getBarpaths({
                    barYPosition: barYPosition,
                    barHeight: barHeight,
                    x1: x1,
                    x2: x2,
                    strokeWidth: this.strokeWidth,
                    series: this.seriesRangeEnd,
                    i: realIndex,
                    realIndex: realIndex,
                    j: j,
                    w: w
                });
                if (!w.axisFlags.isXNumeric) {
                    y = y + yDivision;
                }
                return {
                    pathTo: paths.pathTo,
                    pathFrom: paths.pathFrom,
                    barWidth: barWidth,
                    x: range.start < 0 && range.end < 0 ? x1 : x2,
                    goalX: this.barHelpers.getGoalValues("x", zeroW, /** @type {any} */ null, realIndex, j, 0),
                    y: y
                };
            }
        },
        {
            /**
   * @param {number} i
   * @param {number} j
   */ key: "getRangeValue",
            value: function getRangeValue(i, j) {
                var w = this.w;
                return {
                    start: w.rangeData.seriesRangeStart[i][j],
                    end: w.rangeData.seriesRangeEnd[i][j]
                };
            }
        }
    ]);
    return RangeBar;
}(Bar);
function normalize(data, area) {
    var sum = 0;
    for(var i = 0; i < data.length; i++){
        sum += data[i];
    }
    var multiplier = area / sum;
    var result = new Array(data.length);
    for(var i1 = 0; i1 < data.length; i1++){
        result[i1] = data[i1] * multiplier;
    }
    return result;
}
function calculateRatio(rowMin, rowMax, rowSum, length) {
    var lengthSq = length * length;
    var sumSq = rowSum * rowSum;
    return Math.max(lengthSq * rowMax / sumSq, sumSq / (lengthSq * rowMin));
}
function improvesRatio(rowLen, rowMin, rowMax, rowSum, nextNode, length) {
    if (rowLen === 0) return true;
    var currentRatio = calculateRatio(rowMin, rowMax, rowSum, length);
    var newRatio = calculateRatio(Math.min(rowMin, nextNode), Math.max(rowMax, nextNode), rowSum + nextNode, length);
    return currentRatio >= newRatio;
}
function emitCoordinates(coords, row, rowLen, rowSum, xoffset, yoffset, width, height) {
    if (width >= height) {
        var areaWidth = rowSum / height;
        var subY = yoffset;
        for(var i = 0; i < rowLen; i++){
            var h = row[i] / areaWidth;
            coords.push([
                xoffset,
                subY,
                xoffset + areaWidth,
                subY + h
            ]);
            subY += h;
        }
    } else {
        var areaHeight = rowSum / width;
        var subX = xoffset;
        for(var i1 = 0; i1 < rowLen; i1++){
            var w = row[i1] / areaHeight;
            coords.push([
                subX,
                yoffset,
                subX + w,
                yoffset + areaHeight
            ]);
            subX += w;
        }
    }
}
function squarify(data, xoffset, yoffset, width, height) {
    var coords = [];
    var n = data.length;
    if (n === 0) return coords;
    var row = new Array(n);
    var rowLen = 0;
    var rowSum = 0;
    var rowMin = Infinity;
    var rowMax = -Infinity;
    var i = 0;
    while(i < n){
        var length = Math.min(width, height);
        var val = data[i];
        if (improvesRatio(rowLen, rowMin, rowMax, rowSum, val, length)) {
            row[rowLen] = val;
            rowLen++;
            rowSum += val;
            if (val < rowMin) rowMin = val;
            if (val > rowMax) rowMax = val;
            i++;
        } else {
            emitCoordinates(coords, row, rowLen, rowSum, xoffset, yoffset, width, height);
            if (width >= height) {
                var areaWidth = rowSum / height;
                xoffset += areaWidth;
                width -= areaWidth;
            } else {
                var areaHeight = rowSum / width;
                yoffset += areaHeight;
                height -= areaHeight;
            }
            rowLen = 0;
            rowSum = 0;
            rowMin = Infinity;
            rowMax = -Infinity;
        }
    }
    if (rowLen > 0) {
        emitCoordinates(coords, row, rowLen, rowSum, xoffset, yoffset, width, height);
    }
    return coords;
}
function generate(data, width, height) {
    var n = data.length;
    var sums = new Array(n);
    for(var i = 0; i < n; i++){
        var s = 0;
        var series = data[i];
        for(var j = 0; j < series.length; j++){
            s += series[j];
        }
        sums[i] = s;
    }
    var seriesRects = squarify(normalize(sums, width * height), 0, 0, width, height);
    var results = new Array(n);
    for(var i1 = 0; i1 < n; i1++){
        var rect = seriesRects[i1];
        var rx = rect[0];
        var ry = rect[1];
        var rw = rect[2] - rx;
        var rh = rect[3] - ry;
        results[i1] = squarify(normalize(data[i1], rw * rh), rx, ry, rw, rh);
    }
    return results;
}
var TreemapSquared = {
    generate: generate
};
var TreemapChart = /*#__PURE__*/ function() {
    "use strict";
    function TreemapChart(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, TreemapChart);
        this.ctx = ctx;
        this.w = w;
        this.strokeWidth = this.w.config.stroke.width;
        this.helpers = new TreemapHelpers(w, ctx);
        this.dynamicAnim = this.w.config.chart.animations.dynamicAnimation;
        this.labels = [];
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(TreemapChart, [
        {
            /**
   * @param {any[]} series
   */ key: "draw",
            value: function draw(series) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(this.w, this.ctx);
                var fill = new Fill(this.w);
                var ret = graphics.group({
                    class: "apexcharts-treemap"
                });
                if (w.globals.noData) return ret;
                var ser = [];
                series.forEach(function(s) {
                    var d = s.map(function(v) {
                        return Math.abs(v);
                    });
                    ser.push(d);
                });
                this.negRange = this.helpers.checkColorRange();
                w.config.series.forEach(function(s, i) {
                    s.data.forEach(function(l) {
                        if (!Array.isArray(_this.labels[i])) _this.labels[i] = [];
                        _this.labels[i].push(l.x);
                    });
                });
                var nodes = TreemapSquared.generate(ser, w.layout.gridWidth, w.layout.gridHeight);
                nodes.forEach(function(node, i) {
                    var _a;
                    var elSeries = graphics.group({
                        class: "apexcharts-series apexcharts-treemap-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[i]),
                        rel: i + 1,
                        "data:realIndex": i
                    });
                    graphics.setupEventDelegation(elSeries, ".apexcharts-treemap-rect");
                    if (w.config.chart.dropShadow.enabled) {
                        var shadow = w.config.chart.dropShadow;
                        var filters = new Filters(_this.w);
                        filters.dropShadow(ret, shadow, i);
                    }
                    var elDataLabelWrap = graphics.group({
                        class: "apexcharts-data-labels"
                    });
                    var bounds = {
                        xMin: Infinity,
                        yMin: Infinity,
                        xMax: -Infinity,
                        yMax: -Infinity
                    };
                    var animCfg = w.config.chart.animations;
                    var gradCfg = animCfg.animateGradually;
                    var cascadeEnabled = gradCfg && gradCfg.enabled !== false;
                    var cascadeDelays = new Array(node.length).fill(0);
                    if (cascadeEnabled) {
                        var tileCount = node.length || 1;
                        var baseDelay = Math.min(gradCfg.delay || 0, animCfg.speed * 0.5 / tileCount);
                        var ranked = node.map(/** @param {number[]} r @param {number} j */ function(r, j) {
                            return {
                                j: j,
                                area: (r[2] - r[0]) * (r[3] - r[1])
                            };
                        }).sort(/** @param {{j: number, area: number}} a @param {{j: number, area: number}} b */ function(a, b) {
                            return b.area - a.area;
                        });
                        ranked.forEach(/** @param {{j: number, area: number}} item @param {number} rank */ function(item, rank) {
                            cascadeDelays[item.j] = rank * baseDelay;
                        });
                    }
                    node.forEach(function(r, j) {
                        var x1 = r[0];
                        var y1 = r[1];
                        var x2 = r[2];
                        var y2 = r[3];
                        bounds.xMin = Math.min(bounds.xMin, x1);
                        bounds.yMin = Math.min(bounds.yMin, y1);
                        bounds.xMax = Math.max(bounds.xMax, x2);
                        bounds.yMax = Math.max(bounds.yMax, y2);
                        var colorProps = _this.helpers.getShadeColor(w.config.chart.type, i, j, _this.negRange);
                        var color = colorProps.color;
                        var pathFill = fill.fillPath({
                            color: color,
                            seriesNumber: i,
                            dataPointIndex: j
                        });
                        var elRect = graphics.drawRect(x1, y1, x2 - x1, y2 - y1, w.config.plotOptions.treemap.borderRadius, "#fff", 1, _this.strokeWidth, w.config.plotOptions.treemap.useFillColorAsStroke ? color : w.globals.stroke.colors[i]);
                        elRect.attr({
                            cx: x1,
                            cy: y1,
                            index: i,
                            i: i,
                            j: j,
                            width: x2 - x1,
                            height: y2 - y1,
                            fill: pathFill
                        });
                        elRect.node.classList.add("apexcharts-treemap-rect");
                        var fromRect = {
                            x: x1 + (x2 - x1) / 2,
                            y: y1 + (y2 - y1) / 2,
                            width: 0,
                            height: 0
                        };
                        var toRect = {
                            x: x1,
                            y: y1,
                            width: x2 - x1,
                            height: y2 - y1
                        };
                        if (w.config.chart.animations.enabled && !w.globals.dataChanged) {
                            var speed = 1;
                            if (!w.globals.resized) {
                                speed = w.config.chart.animations.speed;
                            }
                            _this.animateTreemap(elRect, fromRect, toRect, speed, cascadeDelays[j] || 0);
                        }
                        if (w.globals.dataChanged) {
                            var speed1 = 1;
                            if (_this.dynamicAnim.enabled && w.globals.shouldAnimate) {
                                speed1 = _this.dynamicAnim.speed;
                                if (w.globals.previousPaths[i] && /** @type {Record<string,any>} */ w.globals.previousPaths[i][j] && /** @type {Record<string,any>} */ w.globals.previousPaths[i][j].rect) {
                                    fromRect = /** @type {Record<string,any>} */ w.globals.previousPaths[i][j].rect;
                                }
                                _this.animateTreemap(elRect, fromRect, toRect, speed1);
                            }
                        }
                        var fontSize = _this.getFontSize(r);
                        var formattedText = w.config.dataLabels.formatter(_this.labels[i][j], {
                            value: w.seriesData.series[i][j],
                            seriesIndex: i,
                            dataPointIndex: j,
                            w: w
                        });
                        if (w.config.plotOptions.treemap.dataLabels.format === "truncate") {
                            fontSize = parseInt(String(w.config.dataLabels.style.fontSize), 10);
                            formattedText = _this.truncateLabels(String(formattedText), fontSize, x1, y1, x2, y2);
                        }
                        var dataLabels = null;
                        if (w.seriesData.series[i][j]) {
                            dataLabels = _this.helpers.calculateDataLabels({
                                text: formattedText,
                                x: (x1 + x2) / 2,
                                y: (y1 + y2) / 2 + _this.strokeWidth / 2 + fontSize / 3,
                                i: i,
                                j: j,
                                colorProps: colorProps,
                                fontSize: fontSize,
                                series: series
                            });
                        }
                        if (w.config.dataLabels.enabled && dataLabels) {
                            _this.rotateToFitLabel(dataLabels, fontSize, formattedText, x1, y1, x2, y2);
                        }
                        elSeries.add(elRect);
                        if (dataLabels !== null) {
                            elSeries.add(dataLabels);
                        }
                    });
                    var seriesTitle = w.config.plotOptions.treemap.seriesTitle;
                    if (w.config.series.length > 1 && seriesTitle && seriesTitle.show) {
                        var sName = /** @type {Record<string,any>} */ w.config.series[i].name || "";
                        if (sName && bounds.xMin < Infinity && bounds.yMin < Infinity) {
                            var offsetX = seriesTitle.offsetX, offsetY = seriesTitle.offsetY, borderColor = seriesTitle.borderColor, borderWidth = seriesTitle.borderWidth, borderRadius = seriesTitle.borderRadius, style = seriesTitle.style;
                            var textColor = style.color || w.config.chart.foreColor;
                            var padding = {
                                left: style.padding.left,
                                right: style.padding.right,
                                top: style.padding.top,
                                bottom: style.padding.bottom
                            };
                            var textSize = graphics.getTextRects(sName, style.fontSize, style.fontFamily);
                            var labelRectWidth = textSize.width + padding.left + padding.right;
                            var labelRectHeight = textSize.height + padding.top + padding.bottom;
                            var labelX = bounds.xMin + (offsetX || 0);
                            var labelY = bounds.yMin + (offsetY || 0);
                            var elLabelRect = graphics.drawRect(labelX, labelY, labelRectWidth, labelRectHeight, borderRadius, style.background, 1, borderWidth, borderColor);
                            var elLabelText = graphics.drawText({
                                x: labelX + padding.left,
                                y: labelY + padding.top + ((_a = textSize == null ? void 0 : textSize.height) != null ? _a : 0) * 0.75,
                                text: sName,
                                fontSize: style.fontSize,
                                fontFamily: style.fontFamily,
                                fontWeight: style.fontWeight,
                                foreColor: textColor,
                                cssClass: style.cssClass || ""
                            });
                            elSeries.add(elLabelRect);
                            elSeries.add(elLabelText);
                        }
                    }
                    elSeries.add(elDataLabelWrap);
                    ret.add(elSeries);
                });
                return ret;
            }
        },
        {
            // This calculates a font-size based upon
            // average label length and the size of the box
            /**
   * @param {number[]} coordinates
   */ key: "getFontSize",
            value: function getFontSize(coordinates) {
                var w = this.w;
                function totalLabelLength(arr) {
                    var i, total = 0;
                    if (Array.isArray(arr[0])) {
                        for(i = 0; i < arr.length; i++){
                            total += totalLabelLength(arr[i]);
                        }
                    } else {
                        for(i = 0; i < arr.length; i++){
                            total += arr[i].length;
                        }
                    }
                    return total;
                }
                function countLabels(arr) {
                    var i, total = 0;
                    if (Array.isArray(arr[0])) {
                        for(i = 0; i < arr.length; i++){
                            total += countLabels(arr[i]);
                        }
                    } else {
                        for(i = 0; i < arr.length; i++){
                            total += 1;
                        }
                    }
                    return total;
                }
                var averagelabelsize = totalLabelLength(this.labels) / countLabels(this.labels);
                function fontSize(width, height) {
                    var area = width * height;
                    var arearoot = Math.pow(area, 0.5);
                    return Math.min(arearoot / averagelabelsize, parseInt(w.config.dataLabels.style.fontSize, 10));
                }
                return fontSize(coordinates[2] - coordinates[0], coordinates[3] - coordinates[1]);
            }
        },
        {
            /**
   * @param {any} elText
   * @param {string | number} fontSize
   * @param {string} text
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   */ key: "rotateToFitLabel",
            value: function rotateToFitLabel(elText, fontSize, text, x1, y1, x2, y2) {
                var graphics = new Graphics(this.w);
                var textRect = graphics.getTextRects(text, String(fontSize));
                if (textRect.width + this.w.config.stroke.width + 5 > x2 - x1 && textRect.width <= y2 - y1) {
                    var labelRotatingCenter = graphics.rotateAroundCenter(elText.node);
                    elText.node.setAttribute("transform", "rotate(-90 ".concat(labelRotatingCenter.x, " ").concat(labelRotatingCenter.y, ") translate(").concat(textRect.height / 3, ")"));
                }
            }
        },
        {
            // This is an alternative label formatting method that uses a
            // consistent font size, and trims the edge of long labels
            /**
   * @param {string} text
   * @param {number} fontSize
   * @param {number} x1
   * @param {number} y1
   * @param {number} x2
   * @param {number} y2
   */ key: "truncateLabels",
            value: function truncateLabels(text, fontSize, x1, y1, x2, y2) {
                var graphics = new Graphics(this.w);
                var textRect = graphics.getTextRects(text, String(fontSize));
                var labelMaxWidth = textRect.width + this.w.config.stroke.width + 5 > x2 - x1 && y2 - y1 > x2 - x1 ? y2 - y1 : x2 - x1;
                var truncatedText = graphics.getTextBasedOnMaxWidth({
                    text: text,
                    maxWidth: labelMaxWidth,
                    fontSize: fontSize
                });
                if (text.length !== truncatedText.length && labelMaxWidth / fontSize < 5) {
                    return "";
                } else {
                    return truncatedText;
                }
            }
        },
        {
            /**
   * @param {any} el
   * @param {Record<string, any>} fromRect
   * @param {Record<string, any>} toRect
   * @param {number} speed
   * @param {number} [delay] - per-tile cascade delay in ms
   */ key: "animateTreemap",
            value: function animateTreemap(el, fromRect, toRect, speed) {
                var delay = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : 0;
                var animations = new Animations(this.w);
                animations.animateRect(el, fromRect, toRect, speed, function() {
                    animations.animationCompleted(el);
                }, delay);
            }
        }
    ]);
    return TreemapChart;
}();
var GOLDEN_ANGLE = Math.PI * (3 - Math.sqrt(5));
function easeOutCubic(t) {
    return 1 - Math.pow(1 - t, 3);
}
function easeOutBack(s) {
    return function(t) {
        return 1 + (s + 1) * Math.pow(t - 1, 3) + s * Math.pow(t - 1, 2);
    };
}
function easeInOutCubic(t) {
    return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2;
}
var Unit = /*#__PURE__*/ function() {
    "use strict";
    function Unit(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, Unit);
        this.ctx = ctx;
        this.w = w;
        this._lastDotR = 1;
        this._gridTrack = null;
        this._gridDenom = 1;
        this._scatterAxis = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(Unit, [
        {
            /**
   * @param {any[]} series - flat count array (non-axis / pie-shaped data)
   * @returns {any} the chart's root group element
   */ key: "draw",
            value: function draw(series) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(w, this.ctx);
                var ret = graphics.group({
                    class: "apexcharts-unit"
                });
                if (w.globals.noData || !Array.isArray(series) || series.length === 0) {
                    return ret;
                }
                var opts = w.config.plotOptions.unit;
                var layout = opts.layout === "packed" ? "packed" : opts.layout === "columns" ? "columns" : opts.layout === "grid" ? "grid" : opts.layout === "scatter" ? "scatter" : opts.layout === "arc" ? "arc" : "grouped";
                var transition = opts.transition;
                var flow = transition === "flow";
                var identity = transition === "identity";
                var unitValue = opts.unitValue > 0 ? opts.unitValue : 1;
                var counts = series.map(function(v) {
                    var n = Math.abs(Utils$1.parseNumber(v)) / unitValue;
                    return n > 0 ? Math.max(1, Math.round(n)) : 0;
                });
                counts = this._applyMaxUnits(counts, opts.maxUnits);
                var total = counts.reduce(function(a, b) {
                    return a + b;
                }, 0);
                var clusters = layout === "packed" ? this._layoutPacked(counts, opts) : layout === "columns" ? this._layoutColumns(counts, opts) : layout === "grid" ? this._layoutGrid(counts, opts) : layout === "scatter" ? this._layoutScatter(opts) : layout === "arc" ? this._layoutArc(counts, opts) : this._layoutGrouped(counts, opts);
                var gridSplit = layout === "grid" && !!(opts.grid && opts.grid.split);
                if (gridSplit) this._drawGridTrack(ret, graphics, opts);
                if (layout === "scatter") this._drawScatterAxes(ret, graphics);
                var dotR = this._lastDotR;
                var animate = this._shouldAnimate();
                var morph = this.ctx && this.ctx.morphTypeChange;
                var morphActive = animate && !!morph && typeof morph.isActive === "function" && morph.isActive() && typeof morph.getInitialCenterFor === "function";
                var prev = animate && !morphActive && this.ctx ? this.ctx._unitPrevDots : null;
                var nextPrev = /* @__PURE__ */ new Map();
                var animDots = [];
                var unitData = w.seriesData.unitData || [];
                var sizeStats = this._bubbleStats(unitData, opts, dotR);
                var gIndex = 0;
                clusters.forEach(function(cluster) {
                    var color = w.globals.colors[cluster.i] || w.globals.colors[0] || "#008FFB";
                    var elSeries = graphics.group({
                        class: "apexcharts-series",
                        seriesName: Utils$1.escapeString(w.seriesData.seriesNames[cluster.i] || "series-".concat(cluster.i + 1)),
                        rel: cluster.i + 1,
                        "data:realIndex": cluster.i
                    });
                    var burst = morphActive ? morph.getInitialCenterFor(cluster.i) : null;
                    var catData = unitData[cluster.i];
                    cluster.dots.forEach(function(d, j) {
                        var datum = catData ? catData[j] : void 0;
                        var dotFill = datum && (typeof datum === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(datum)) === "object" && datum.fillColor ? datum.fillColor : color;
                        var rj = d.r != null ? d.r : sizeStats ? _this._radiusForValue(_this._unitValueOf(datum), sizeStats) : dotR;
                        var el = _this._drawDot(graphics, opts, rj, dotFill, cluster.i, j);
                        elSeries.add(el);
                        var key;
                        if (identity) {
                            var id = datum && (typeof datum === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(datum)) === "object" ? datum.id != null ? datum.id : datum.name : void 0;
                            key = id != null ? "id:".concat(id) : "g:".concat(gIndex);
                        } else if (flow) {
                            key = String(gIndex);
                        } else if (d.slot != null) {
                            key = "slot:".concat(d.slot);
                        } else {
                            key = "".concat(cluster.i, ":").concat(j);
                        }
                        gIndex++;
                        nextPrev.set(key, {
                            x: d.x,
                            y: d.y,
                            fill: dotFill,
                            r: rj
                        });
                        if (animate) {
                            var from = prev && prev.get(key);
                            var anchor = from || burst;
                            var enter = opts.gather && opts.gather.enter || "burst";
                            var inPlace = gridSplit || enter === "fade" || enter === "rise";
                            var cx0 = anchor ? anchor.x : inPlace ? d.x : cluster.cx;
                            var cy0 = anchor ? anchor.y : enter === "rise" && !gridSplit ? d.y + 14 : inPlace ? d.y : cluster.cy;
                            el.node.style.opacity = anchor ? "1" : "0";
                            _this._placeDot(el.node, opts, cx0, cy0);
                            animDots.push({
                                node: el.node,
                                x: d.x,
                                y: d.y,
                                cx0: cx0,
                                cy0: cy0,
                                // Radius tween: an identity-kept dot grows/shrinks from its previous
                                // size to its new one (e.g. bubble sizing turning on) instead of
                                // snapping. Enters/uniform updates keep r0 === r1 (no-op).
                                r0: from && from.r != null ? from.r : rj,
                                r1: rj,
                                // Colour tween: a dot that flows into a differently coloured group
                                // recolours as it travels rather than snapping at the first frame.
                                fill0: from ? from.fill : dotFill,
                                fill1: dotFill,
                                delay: 0,
                                // assigned below (staggered by global order)
                                isEnter: !anchor
                            });
                        } else {
                            _this._placeDot(el.node, opts, d.x, d.y);
                        }
                    });
                    if ((layout === "grouped" || layout === "columns" || gridSplit) && opts.clusterLabels && opts.clusterLabels.show && counts[cluster.i] > 0) {
                        var labelTotal = gridSplit ? _this._gridDenom : total;
                        _this._drawClusterLabel(elSeries, cluster, counts[cluster.i], labelTotal, opts, color);
                    }
                    ret.add(elSeries);
                });
                if (prev) {
                    var exits = this._collectExits(prev, nextPrev, opts);
                    if (exits.length) {
                        var exitGroup = graphics.group({
                            class: "apexcharts-unit-exits"
                        });
                        ret.add(exitGroup);
                        this._runExits(exitGroup, exits, opts);
                    }
                }
                if (this.ctx) this.ctx._unitPrevDots = nextPrev;
                if (animate && animDots.length) {
                    this._runGather(animDots);
                }
                return ret;
            }
        },
        {
            /**
   * Cap total dots to `maxUnits`, scaling every category down proportionally
   * (a non-zero category keeps at least one dot). Warns once when it clips.
   * @param {number[]} counts
   * @param {number} maxUnits
   * @returns {number[]}
   */ key: "_applyMaxUnits",
            value: function _applyMaxUnits(counts, maxUnits) {
                var total = counts.reduce(function(a, b) {
                    return a + b;
                }, 0);
                if (!maxUnits || maxUnits <= 0 || total <= maxUnits) return counts;
                var scale = maxUnits / total;
                console.warn("[ApexCharts] unit chart: ".concat(total, " dots exceeds maxUnits (").concat(maxUnits, "); counts were scaled down proportionally. Raise plotOptions.unit.maxUnits or use plotOptions.unit.unitValue to represent more units per dot."));
                return counts.map(function(c) {
                    return c > 0 ? Math.max(1, Math.round(c * scale)) : 0;
                });
            }
        },
        {
            /**
   * Lay out each category as its own cluster in a horizontal row. All clusters
   * share one dot radius (so dot size is comparable across clusters); the blob
   * radius encodes the count.
   * @param {number[]} counts
   * @param {any} opts
   */ key: "_layoutGrouped",
            value: function _layoutGrouped(counts, opts) {
                var _this = this;
                var _Math;
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var labelSpace = opts.clusterLabels && opts.clusterLabels.show ? 30 : 6;
                var visible = counts.map(function(_, i) {
                    return i;
                }).filter(function(i) {
                    return counts[i] > 0;
                });
                var Kv = Math.max(1, visible.length);
                var slotOf = new Array(counts.length).fill(-1);
                visible.forEach(function(i, s) {
                    return slotOf[i] = s;
                });
                var cellW = gw / Kv;
                var availH = gh - labelSpace;
                var maxCount = (_Math = Math).max.apply(_Math, [
                    1
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(counts)));
                var pad = Math.min(cellW, availH) * 0.08;
                var availR = Math.max(4, Math.min(cellW, availH) / 2 - pad);
                var step = this._resolveStep(opts, availR, maxCount);
                this._lastDotR = this._dotRadiusFromStep(step, opts);
                var dotR = this._lastDotR;
                var cy = labelSpace + availH / 2;
                var outerRs = counts.map(function(n) {
                    return step * Math.sqrt(Math.max(1, n)) + dotR;
                });
                var cellCentre = function cellCentre(i) {
                    return slotOf[i] >= 0 ? cellW * (slotOf[i] + 0.5) : gw / 2;
                };
                var centers = counts.map(function(_, i) {
                    return cellCentre(i);
                });
                var visOuter = visible.map(function(i) {
                    return outerRs[i];
                });
                var overlap = false;
                for(var s = 1; s < Kv; s++){
                    if (centers[visible[s]] - centers[visible[s - 1]] < visOuter[s] + visOuter[s - 1]) {
                        overlap = true;
                        break;
                    }
                }
                if (overlap) {
                    var gap = Math.max(2 * dotR, 8);
                    var totalW = visOuter.reduce(function(a, r) {
                        return a + 2 * r;
                    }, 0) + gap * (Kv - 1);
                    var visCenters;
                    if (totalW <= gw) {
                        var x = (gw - totalW) / 2;
                        visCenters = visOuter.map(function(r) {
                            var c = x + r;
                            x += 2 * r + gap;
                            return c;
                        });
                    } else if (Kv === 1) {
                        visCenters = [
                            gw / 2
                        ];
                    } else {
                        var lo = visOuter[0];
                        var hi = gw - visOuter[Kv - 1];
                        visCenters = visOuter.map(function(_, s) {
                            return lo + (hi - lo) * s / (Kv - 1);
                        });
                    }
                    centers = counts.map(function(_, i) {
                        return slotOf[i] >= 0 ? visCenters[slotOf[i]] : gw / 2;
                    });
                }
                return counts.map(function(n, i) {
                    return {
                        i: i,
                        cx: centers[i],
                        cy: cy,
                        outerR: outerRs[i],
                        dots: _this._spiral(centers[i], cy, n, step, 0)
                    };
                });
            }
        },
        {
            /**
   * Lay out all categories into ONE packed blob. Dots are assigned spiral
   * indices in category order (smallest-first when sortByGroup), so the
   * minority group nests in the centre.
   * @param {number[]} counts
   * @param {any} opts
   */ key: "_layoutPacked",
            value: function _layoutPacked(counts, opts) {
                var _this = this;
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var labelSpace = 6;
                var total = Math.max(1, counts.reduce(function(a, b) {
                    return a + b;
                }, 0));
                var availR = Math.max(4, Math.min(gw, gh - labelSpace) / 2 - Math.min(gw, gh) * 0.06);
                var step = this._resolveStep(opts, availR, total);
                this._lastDotR = this._dotRadiusFromStep(step, opts);
                var cx = gw / 2;
                var cy = labelSpace + (gh - labelSpace) / 2;
                var order = counts.map(function(_, i) {
                    return i;
                });
                if (opts.sortByGroup !== false) {
                    order.sort(function(a, b) {
                        return counts[a] - counts[b];
                    });
                }
                var clusters = counts.map(function(_, i) {
                    return {
                        i: i,
                        cx: cx,
                        cy: cy,
                        outerR: step * Math.sqrt(total) + _this._lastDotR,
                        /** @type {{x:number,y:number,slot?:number}[]} */ dots: []
                    };
                });
                var gi = 0;
                order.forEach(function(catI) {
                    for(var j = 0; j < counts[catI]; j++){
                        var r = step * Math.sqrt(gi + 0.5);
                        var theta = gi * GOLDEN_ANGLE;
                        clusters[catI].dots.push({
                            x: cx + r * Math.cos(theta),
                            y: cy + r * Math.sin(theta),
                            slot: gi
                        });
                        gi++;
                    }
                });
                return clusters;
            }
        },
        {
            /**
   * Lay out all marks as a PARLIAMENT / hemicycle: seats in concentric arced
   * rows across an annulus, filled in category (party) order so each category
   * forms a contiguous angular wedge (the classic seating chart). `arc` controls
   * the sweep (`startAngle`/`endAngle`, radialBar convention: 0 = top, clockwise;
   * default a top semicircle), the donut hole (`innerRadiusRatio`) and the row
   * count (`rows`, or 'auto'). Like `packed` this is ONE shared shape coloured by
   * category, so seats key by physical slot: a seat-count change recolours the
   * party boundary in place and only the rim adds / removes seats.
   * @param {number[]} counts
   * @param {any} opts
   */ key: "_layoutArc",
            value: function _layoutArc(counts, opts) {
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var total = Math.max(1, counts.reduce(function(a, b2) {
                    return a + b2;
                }, 0));
                var acfg = opts.arc || {};
                var startDeg = typeof acfg.startAngle === "number" ? acfg.startAngle : -90;
                var endDeg = typeof acfg.endAngle === "number" ? acfg.endAngle : 90;
                var a0 = startDeg * Math.PI / 180;
                var a1 = endDeg * Math.PI / 180;
                var span = a1 - a0 || Math.PI;
                var innerRatio = Math.max(0, Math.min(0.95, typeof acfg.innerRadiusRatio === "number" ? acfg.innerRadiusRatio : 0.4));
                var ux = function ux(a) {
                    return Math.sin(a);
                };
                var uy = function uy(a) {
                    return -Math.cos(a);
                };
                var b = this._arcBounds(a0, a1);
                var pad = Math.min(gw, gh) * 0.04;
                var boxW = Math.max(1e-6, b.maxX - b.minX);
                var boxH = Math.max(1e-6, b.maxY - b.minY);
                var r1 = Math.max(4, Math.min((gw - 2 * pad) / boxW, (gh - 2 * pad) / boxH));
                var r0 = r1 * innerRatio;
                var cx = gw / 2 - (b.minX + b.maxX) / 2 * r1;
                var cy = gh / 2 - (b.minY + b.maxY) / 2 * r1;
                var alloc = this._arcAllocate(total, r0, r1, span, opts);
                this._lastDotR = alloc.dotR;
                var seats = [];
                for(var r = 0; r < alloc.R; r++){
                    var rho = alloc.radii[r];
                    var n = alloc.seatsPerRow[r];
                    for(var k = 0; k < n; k++){
                        var a = n === 1 ? (a0 + a1) / 2 : a0 + span * (k + 0.5) / n;
                        seats.push({
                            a: a,
                            x: cx + rho * ux(a),
                            y: cy + rho * uy(a)
                        });
                    }
                }
                seats.sort(function(s1, s2) {
                    return s1.a - s2.a;
                });
                var clusters = counts.map(function(_, i) {
                    return {
                        i: i,
                        cx: cx,
                        cy: cy,
                        outerR: r1,
                        /** @type {{x:number,y:number,slot?:number}[]} */ dots: []
                    };
                });
                var ci = 0;
                var used = 0;
                seats.forEach(function(s, slot) {
                    while(ci < counts.length && used >= counts[ci]){
                        ci++;
                        used = 0;
                    }
                    if (ci >= counts.length) return;
                    clusters[ci].dots.push({
                        x: s.x,
                        y: s.y,
                        slot: slot
                    });
                    used++;
                });
                return clusters;
            }
        },
        {
            /**
   * Bounding box of the outer arc (radius 1) over [a0, a1], including the centre
   * and every cardinal angle (multiple of 90deg) inside the range, so a
   * semicircle / full circle / arbitrary sweep is all bounded correctly.
   * @param {number} a0 @param {number} a1
   * @returns {{minX:number,maxX:number,minY:number,maxY:number}}
   */ key: "_arcBounds",
            value: function _arcBounds(a0, a1) {
                var _Math, _Math1, _Math2, _Math3;
                var ux = function ux(a) {
                    return Math.sin(a);
                };
                var uy = function uy(a) {
                    return -Math.cos(a);
                };
                var lo = Math.min(a0, a1);
                var hi = Math.max(a0, a1);
                var xs = [
                    0,
                    ux(a0),
                    ux(a1)
                ];
                var ys = [
                    0,
                    uy(a0),
                    uy(a1)
                ];
                var q = Math.PI / 2;
                for(var k = Math.ceil(lo / q); k * q <= hi; k++){
                    xs.push(ux(k * q));
                    ys.push(uy(k * q));
                }
                return {
                    minX: (_Math = Math).min.apply(_Math, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(xs)),
                    maxX: (_Math1 = Math).max.apply(_Math1, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(xs)),
                    minY: (_Math2 = Math).min.apply(_Math2, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ys)),
                    maxY: (_Math3 = Math).max.apply(_Math3, (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(ys))
                };
            }
        },
        {
            /**
   * Allocate `total` seats across concentric rows of the annulus [r0, r1] sweeping
   * `span` radians: seats per row are proportional to the row radius (a longer arc
   * holds more), summed EXACTLY to total by largest remainder. Row count is
   * `arc.rows` if given, else derived from a fixed dot size, else auto-searched to
   * maximise the dot radius (the largest dots that still pack without overlap,
   * mirroring `size:'auto'` elsewhere).
   * @param {number} total @param {number} r0 @param {number} r1 @param {number} span @param {any} opts
   * @returns {{R:number, radii:number[], seatsPerRow:number[], dotR:number}}
   */ key: "_arcAllocate",
            value: function _arcAllocate(total, r0, r1, span, opts) {
                var spacing = opts.spacing > 0 ? opts.spacing : 1.05;
                var absSpan = Math.abs(span) || Math.PI;
                var fixed = this._fixedRadius(opts);
                var evalR = function evalR(R) {
                    R = Math.max(1, Math.round(R));
                    var radii = [];
                    for(var r = 0; r < R; r++){
                        radii.push(R === 1 ? (r0 + r1) / 2 : r0 + (r1 - r0) * (r / (R - 1)));
                    }
                    var weightSum = radii.reduce(function(a, x) {
                        return a + x;
                    }, 0) || 1;
                    var raw = radii.map(function(rho) {
                        return total * rho / weightSum;
                    });
                    var seatsPerRow = raw.map(function(x) {
                        return Math.floor(x);
                    });
                    var left = total - seatsPerRow.reduce(function(a, x) {
                        return a + x;
                    }, 0);
                    raw.map(function(x, idx) {
                        return {
                            idx: idx,
                            frac: x - Math.floor(x)
                        };
                    }).sort(function(p, qq) {
                        return qq.frac - p.frac;
                    }).forEach(function(o) {
                        if (left > 0) {
                            seatsPerRow[o.idx]++;
                            left--;
                        }
                    });
                    while(left > 0){
                        seatsPerRow[R - 1]++;
                        left--;
                    }
                    var radialPitch = R === 1 ? r1 - r0 || r1 : (r1 - r0) / (R - 1);
                    var minArcPitch = Infinity;
                    for(var r2 = 0; r2 < R; r2++){
                        var n = seatsPerRow[r2];
                        if (n <= 0) continue;
                        var arcPitch = radii[r2] * absSpan / n;
                        if (arcPitch < minArcPitch) minArcPitch = arcPitch;
                    }
                    var pitch = Math.min(radialPitch, minArcPitch);
                    return {
                        R: R,
                        radii: radii,
                        seatsPerRow: seatsPerRow,
                        dotR: Math.max(1, pitch / (2 * spacing))
                    };
                };
                var arcRows = opts.arc && opts.arc.rows;
                var res;
                if (typeof arcRows === "number" && arcRows >= 1) {
                    res = evalR(arcRows);
                } else if (fixed) {
                    var pitch = 2 * fixed * spacing;
                    res = evalR((r1 - r0) / pitch + 1);
                } else {
                    var maxR = Math.max(1, Math.min(40, Math.ceil(Math.sqrt(total)) + 6));
                    res = evalR(1);
                    for(var R = 2; R <= maxR; R++){
                        var cand = evalR(R);
                        if (cand.dotR > res.dotR) res = cand;
                    }
                }
                if (fixed) res.dotR = fixed;
                return res;
            }
        },
        {
            /**
   * Lay out each category as a vertical BAR built from stacked dots (a unit /
   * waffle column). Every bar shares one dot size and one width (the same
   * number of dot columns); the bar's HEIGHT encodes its count. Dots fill each
   * bar bottom-up, row by row. This is the "dot bar" state the circle layouts
   * morph into: with `transition:'flow'` the dots glide straight from their
   * circle slots into these bar slots (see the storyboard sample).
   * @param {number[]} counts
   * @param {any} opts
   */ key: "_layoutColumns",
            value: function _layoutColumns(counts, opts) {
                var _Math;
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var labelsOn = !!(opts.clusterLabels && opts.clusterLabels.show);
                var labelsBelow = labelsOn && opts.clusterLabels.position === "bottom";
                var topPad = labelsOn && !labelsBelow ? 30 : 6;
                var visible = counts.map(function(_, i) {
                    return i;
                }).filter(function(i) {
                    return counts[i] > 0;
                });
                var Kv = Math.max(1, visible.length);
                var slotOf = new Array(counts.length).fill(-1);
                visible.forEach(function(i, s) {
                    return slotOf[i] = s;
                });
                var cellW = gw / Kv;
                var barW = cellW * 0.62;
                var bottomPad = Math.max(8, gh * 0.04) + (labelsBelow ? 30 : 0);
                var availH = Math.max(4, gh - topPad - bottomPad);
                var maxCount = (_Math = Math).max.apply(_Math, [
                    1
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(counts)));
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var colSize = opts.columns ? opts.columns.size : void 0;
                var fixed;
                if (opts.shape !== "image" && colSize === "auto") {
                    fixed = null;
                } else if (opts.shape !== "image" && typeof colSize === "number" && colSize > 0) {
                    fixed = colSize;
                } else {
                    fixed = this._fixedRadius(opts);
                }
                var cols = 1;
                var pitch = 0;
                if (fixed) {
                    pitch = 2 * fixed * spacing;
                    this._lastDotR = fixed;
                    var rowsCap = Math.max(1, Math.floor(availH / pitch));
                    var maxColsByWidth = Math.max(1, Math.floor(barW / pitch));
                    cols = Math.max(1, Math.min(maxColsByWidth, Math.ceil(maxCount / rowsCap)));
                } else {
                    var best = 0;
                    var maxCols = Math.max(1, Math.min(40, Math.round(barW / 4)));
                    for(var c = 1; c <= maxCols; c++){
                        var rows = Math.ceil(maxCount / c);
                        var d = Math.min(barW / c, availH / rows);
                        if (d > best) {
                            best = d;
                            cols = c;
                        }
                    }
                    pitch = best;
                    this._lastDotR = Math.max(1, pitch / (2 * spacing));
                }
                var r = this._lastDotR;
                var maxRows = Math.ceil(maxCount / cols);
                var tallestBarH = Math.min(availH, maxRows * pitch);
                var bottom = topPad + (availH + tallestBarH) / 2;
                return counts.map(function(n, i) {
                    var cx = slotOf[i] >= 0 ? cellW * (slotOf[i] + 0.5) : gw / 2;
                    var rows = Math.ceil(Math.max(1, n) / cols);
                    var barH = rows * pitch;
                    var left = cx - cols * pitch / 2 + pitch / 2;
                    var dots = [];
                    for(var j = 0; j < n; j++){
                        var rowIdx = Math.floor(j / cols);
                        var colIdx = j % cols;
                        dots.push({
                            x: left + colIdx * pitch,
                            y: bottom - r - rowIdx * pitch
                        });
                    }
                    return {
                        i: i,
                        cx: cx,
                        cy: bottom - barH / 2,
                        outerR: barH / 2,
                        // Flag read by _drawClusterLabel: a bar takes a straight label (above
                        // or below per clusterLabels.position), never a curved arc.
                        flat: true,
                        dots: dots
                    };
                });
            }
        },
        {
            /**
   * Lay out ALL categories into ONE regular lattice - a waffle / grid. Dots take
   * sequential slots in DECLARED category order and fill row-major, `columns`
   * wide, so each category owns a contiguous band of cells: a part-to-whole
   * square "pie". `grid.total` (optional) re-allocates the cells to a fixed
   * budget (e.g. 100) by largest remainder, so the grid reads as exact
   * percentages regardless of the raw totals; without it there is one cell per
   * unit (respecting unitValue / maxUnits). `grid.fillFrom` picks the first row.
   * The category bands follow the legend order (no smallest-first sort), and
   * each physical slot is keyed so a proportion change recolours boundary cells
   * in place rather than reshuffling the whole grid.
   * @param {number[]} counts
   * @param {any} opts
   */ key: "_layoutGrid",
            value: function _layoutGrid(counts, opts) {
                if (opts.grid && opts.grid.split) return this._layoutGridSplit(counts, opts);
                this._gridTrack = null;
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var gcfg = opts.grid || {};
                var cols = Math.max(1, Math.round(gcfg.columns > 0 ? gcfg.columns : 10));
                var fillFrom = gcfg.fillFrom === "top" ? "top" : "bottom";
                var cells = gcfg.total > 0 ? this._largestRemainder(counts, Math.round(gcfg.total)) : counts.slice();
                var totalCells = cells.reduce(function(a, b) {
                    return a + b;
                }, 0);
                var rows = Math.max(1, Math.ceil(Math.max(1, totalCells) / cols));
                var labelSpace = 6;
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var availW = Math.max(4, gw);
                var availH = Math.max(4, gh - labelSpace);
                var fixed = this._fixedRadius(opts);
                var pitch = 0;
                if (fixed) {
                    pitch = 2 * fixed * spacing;
                    this._lastDotR = fixed;
                } else {
                    pitch = Math.min(availW / cols, availH / rows);
                    this._lastDotR = Math.max(1, pitch / (2 * spacing));
                }
                var blockW = cols * pitch;
                var blockH = rows * pitch;
                var originX = (gw - blockW) / 2 + pitch / 2;
                var topY = labelSpace + (availH - blockH) / 2;
                var rowY = function rowY(rowIdx) {
                    return fillFrom === "bottom" ? topY + blockH - pitch / 2 - rowIdx * pitch : topY + pitch / 2 + rowIdx * pitch;
                };
                var clusters = counts.map(function(_, i) {
                    return {
                        i: i,
                        cx: gw / 2,
                        cy: labelSpace + availH / 2,
                        outerR: Math.max(blockW, blockH) / 2,
                        /** @type {{x:number,y:number,slot?:number}[]} */ dots: []
                    };
                });
                var k = 0;
                for(var ci = 0; ci < cells.length; ci++){
                    for(var j = 0; j < cells[ci]; j++){
                        var col = k % cols;
                        var rowIdx = Math.floor(k / cols);
                        clusters[ci].dots.push({
                            x: originX + col * pitch,
                            y: rowY(rowIdx),
                            slot: k
                        });
                        k++;
                    }
                }
                return clusters;
            }
        },
        {
            /**
   * Small-multiple ("trellis") waffles: ONE mini-waffle per category, laid out
   * in a near-square grid of tiles. Each tile has `grid.total` cells (default
   * 100 -> a 10x10 tile) and fills a fraction of them equal to the category's
   * value over a denominator (`grid.max`, else the largest count so the leader
   * fills its tile and every other tile stays proportionally full - no empty
   * tiles for arbitrary data). The unfilled cells are drawn as a faint TRACK
   * backdrop (see _drawGridTrack) so each tile reads as a part-to-whole "of N".
   * Only VISIBLE (non-zero) categories claim a tile, so a legend hide drops the
   * tile and the rest re-flow. Each filled cell is keyed by a physical
   * `tile*cells + localCell` slot, so a value change grows/shrinks a tile's fill
   * in place instead of reshuffling.
   * @param {number[]} counts @param {any} opts
   */ key: "_layoutGridSplit",
            value: function _layoutGridSplit(counts, opts) {
                var _Math;
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var gcfg = opts.grid || {};
                var cols = Math.max(1, Math.round(gcfg.columns > 0 ? gcfg.columns : 10));
                var fillFrom = gcfg.fillFrom === "top" ? "top" : "bottom";
                var cellsPerTile = Math.max(1, Math.round(gcfg.total > 0 ? gcfg.total : 100));
                var rowsPerTile = Math.max(1, Math.ceil(cellsPerTile / cols));
                var visible = counts.map(function(_, i) {
                    return i;
                }).filter(function(i) {
                    return counts[i] > 0;
                });
                var K = Math.max(1, visible.length);
                var denom = gcfg.max > 0 ? gcfg.max : (_Math = Math).max.apply(_Math, [
                    1
                ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(counts)));
                var tileCols = Math.max(1, Math.round(gcfg.tileColumns > 0 ? gcfg.tileColumns : Math.ceil(Math.sqrt(K))));
                var tileRows = Math.max(1, Math.ceil(K / tileCols));
                var labelsOn = !(opts.clusterLabels && opts.clusterLabels.show === false);
                var labelsBelow = labelsOn && opts.clusterLabels && opts.clusterLabels.position === "bottom";
                var topBand = labelsOn && !labelsBelow ? 22 : 4;
                var botBand = labelsOn && labelsBelow ? 22 : 4;
                var tileW = gw / tileCols;
                var tileH = gh / tileRows;
                var availTileW = Math.max(4, tileW * 0.86);
                var availTileH = Math.max(4, tileH - topBand - botBand);
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var fixed = this._fixedRadius(opts);
                var pitch = 0;
                if (fixed) {
                    pitch = 2 * fixed * spacing;
                    this._lastDotR = fixed;
                } else {
                    pitch = Math.min(availTileW / cols, availTileH / rowsPerTile);
                    this._lastDotR = Math.max(1, pitch / (2 * spacing));
                }
                var blockW = cols * pitch;
                var blockH = rowsPerTile * pitch;
                var rowY = function rowY(topY, rowIdx) {
                    return fillFrom === "bottom" ? topY + blockH - pitch / 2 - rowIdx * pitch : topY + pitch / 2 + rowIdx * pitch;
                };
                var track = [];
                var clusters = [];
                visible.forEach(function(ci, t) {
                    var tc = t % tileCols;
                    var tr = Math.floor(t / tileCols);
                    var tileX = tc * tileW;
                    var tileYtop = tr * tileH;
                    var originX = tileX + (tileW - blockW) / 2 + pitch / 2;
                    var topY = tileYtop + topBand + (availTileH - blockH) / 2;
                    var cellXY = function cellXY(k) {
                        return {
                            x: originX + k % cols * pitch,
                            y: rowY(topY, Math.floor(k / cols))
                        };
                    };
                    for(var k = 0; k < cellsPerTile; k++)track.push(cellXY(k));
                    var filled = Math.max(0, Math.min(cellsPerTile, Math.round(counts[ci] / denom * cellsPerTile)));
                    var dots = [];
                    for(var k1 = 0; k1 < filled; k1++){
                        var p = cellXY(k1);
                        dots.push({
                            x: p.x,
                            y: p.y,
                            slot: t * cellsPerTile + k1
                        });
                    }
                    clusters.push({
                        i: ci,
                        cx: tileX + tileW / 2,
                        cy: topY + blockH / 2,
                        outerR: blockH / 2,
                        // Straight per-tile label (never a curved arc), placed by position.
                        flat: true,
                        split: true,
                        dots: dots
                    });
                });
                this._gridDenom = denom;
                this._gridTrack = {
                    cells: track
                };
                return clusters;
            }
        },
        {
            /**
   * Draw the faint "track" backdrop for the small-multiple grid: every cell of
   * every tile's full lattice, so the filled (coloured) cells drawn on top read
   * as a fraction of the whole. Static (redrawn each render, never animated);
   * painted BEHIND the series groups. `grid.trackColor` overrides the default
   * theme-neutral grey.
   * @param {any} ret @param {Graphics} graphics @param {any} opts
   */ key: "_drawGridTrack",
            value: function _drawGridTrack(ret, graphics, opts) {
                var track = this._gridTrack;
                if (!track || !track.cells || !track.cells.length) return;
                var r = this._lastDotR;
                var gcfg = opts.grid || {};
                var trackColor = gcfg.trackColor || "rgba(128,128,128,0.14)";
                var g = graphics.group({
                    class: "apexcharts-unit-track"
                });
                track.cells.forEach(function(c) {
                    var el;
                    if (opts.shape === "square") {
                        var side = r * 2;
                        el = graphics.drawRect(0, 0, side, side, opts.borderRadius || 0, trackColor, 1, 0, "none");
                        el.node.setAttribute("fill", trackColor);
                        el.node.setAttribute("x", String(c.x - r));
                        el.node.setAttribute("y", String(c.y - r));
                    } else {
                        el = graphics.drawCircle(r, {
                            fill: trackColor,
                            "stroke-width": 0,
                            stroke: "none"
                        });
                        el.node.setAttribute("fill", trackColor);
                        el.node.setAttribute("cx", String(c.x));
                        el.node.setAttribute("cy", String(c.y));
                    }
                    el.node.classList.add("apexcharts-unit-track-cell");
                    g.add(el);
                });
                ret.add(g);
            }
        },
        {
            /**
   * Scatter / beeswarm layout: position every unit on a real numeric X value
   * axis by its own value (`_unitValueOf`), laned by category on Y. Within a
   * lane an anti-overlap "swarm" pack (or a random jitter) spreads the dots off
   * the centre line so equal / close values do not stack on top of each other.
   * This is the unit chart's answer to "put these on axes": one dot per datum,
   * placed by data, with a drawn value axis + category lanes (see
   * _drawScatterAxes). Needs the per-unit object form (each datum a numeric
   * `value`/`y`); flat counts have no per-unit value, so their lanes stay empty.
   * @param {any} opts
   */ key: "_layoutScatter",
            value: function _layoutScatter(opts) {
                var _this = this;
                var w = this.w;
                var scfg = opts.scatter || {};
                if (scfg.y === "value") return this._layoutScatter2D(opts);
                if (scfg.orientation === "vertical") return this._layoutScatterVertical(opts);
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var unitData = w.seriesData.unitData || [];
                var names = w.seriesData.seriesNames || [];
                var valueOf = function valueOf(d) {
                    return _this._unitValueOf(d);
                };
                var sizeStats = this._scatterSizeStats(scfg, unitData);
                var catVals = unitData.map(function(cat) {
                    return Array.isArray(cat) ? cat.map(valueOf) : [];
                });
                var isNum2 = function isNum2(v) {
                    return v != null && isFinite(v);
                };
                var visible = catVals.map(function(_, i) {
                    return i;
                }).filter(function(i) {
                    return catVals[i].some(isNum2);
                });
                var Kv = Math.max(1, visible.length);
                var vmin = Infinity;
                var vmax = -Infinity;
                catVals.forEach(function(vs) {
                    return vs.forEach(function(v) {
                        if (v != null && isFinite(v)) {
                            if (v < vmin) vmin = v;
                            if (v > vmax) vmax = v;
                        }
                    });
                });
                if (vmin === Infinity) {
                    vmin = 0;
                    vmax = 1;
                }
                var tickAmount = Math.max(2, Math.round(scfg.tickAmount > 0 ? scfg.tickAmount : 5));
                var domain = this._scatterValueDomain(scfg, vmin, vmax, tickAmount);
                var xMin = domain.min;
                var xMax = domain.max;
                var xSpan = xMax - xMin || 1;
                var laneW = scfg.laneLabelWidth != null ? Math.max(0, scfg.laneLabelWidth) : Kv > 1 ? 92 : 8;
                var bottomGutter = 30 + (scfg.xTitle ? 20 : 0);
                var plotL = laneW;
                var plotR = gw - 8;
                var plotT = 10;
                var plotB = gh - bottomGutter;
                var plotW = Math.max(4, plotR - plotL);
                var plotH = Math.max(4, plotB - plotT);
                var plotX = function plotX(v) {
                    return plotL + (v - xMin) / xSpan * plotW;
                };
                var laneH = plotH / Kv;
                var laneCy = function laneCy(slot) {
                    return plotT + laneH * (slot + 0.5);
                };
                var r = 0;
                var fixed = this._fixedRadius(opts);
                if (fixed) {
                    r = fixed;
                } else {
                    var _Math;
                    var maxLane = (_Math = Math).max.apply(_Math, [
                        1
                    ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(visible.map(function(i) {
                        return catVals[i].filter(isNum2).length;
                    }))));
                    r = Math.max(2, Math.min(6, laneH * 0.12, plotW / (2.5 * Math.sqrt(maxLane))));
                }
                this._lastDotR = r;
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var step = Math.max(0.5, r * spacing);
                var jitter = scfg.spread === "jitter";
                var clusters = [];
                var lanes = [];
                var maxR = sizeStats ? sizeStats.rMax : r;
                visible.forEach(function(ci, slot) {
                    var cy = laneCy(slot);
                    lanes.push({
                        i: ci,
                        cy: cy,
                        name: names[ci] || "series-".concat(ci + 1)
                    });
                    var cat = unitData[ci] || [];
                    var pts = cat.map(function(d, j) {
                        var v = valueOf(d);
                        var p = {
                            j: j,
                            px: plotX(isNum2(v) ? v : xMin),
                            y: cy
                        };
                        if (sizeStats) p.r = _this._scatterRadius(d, sizeStats, r);
                        return p;
                    });
                    if (jitter) {
                        var halfLane = Math.max(maxR, laneH / 2 - maxR);
                        pts.forEach(function(p, k) {
                            var t = (k * 9301 + 49297) % 233280 / 233280;
                            p.y = cy + (t * 2 - 1) * halfLane;
                        });
                    } else {
                        _this._beeswarm(pts, cy, r, step, maxR);
                    }
                    clusters.push({
                        i: ci,
                        cx: (plotL + plotR) / 2,
                        cy: cy,
                        outerR: laneH / 2,
                        dots: pts.map(function(p) {
                            return {
                                x: p.px,
                                y: p.y,
                                r: p.r
                            };
                        })
                    });
                });
                var ticks = domain.ticks;
                this._scatterAxis = {
                    mode: "1d",
                    plotL: plotL,
                    plotR: plotR,
                    plotT: plotT,
                    plotB: plotB,
                    xMin: xMin,
                    xMax: xMax,
                    plotX: plotX,
                    ticks: ticks,
                    lanes: lanes,
                    xTitle: scfg.xTitle,
                    formatter: typeof scfg.xFormatter === "function" ? scfg.xFormatter : null,
                    gridlines: scfg.gridlines !== false
                };
                return clusters;
            }
        },
        {
            /**
   * Vertical beeswarm: the transpose of _layoutScatter. The value runs UP the Y
   * axis and each category is a column (lane) across X; the swarm pack spreads
   * dots horizontally off each column's centre line. The value-axis config keys
   * (`xMin`/`xMax`/`xTitle`/`xFormatter`/`tickAmount`) still describe the value
   * axis (now Y), so flipping `orientation` keeps the same value settings.
   * @param {any} opts
   */ key: "_layoutScatterVertical",
            value: function _layoutScatterVertical(opts) {
                var _this = this;
                var w = this.w;
                var scfg = opts.scatter || {};
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var unitData = w.seriesData.unitData || [];
                var names = w.seriesData.seriesNames || [];
                var valueOf = function valueOf(d) {
                    return _this._unitValueOf(d);
                };
                var sizeStats = this._scatterSizeStats(scfg, unitData);
                var catVals = unitData.map(function(cat) {
                    return Array.isArray(cat) ? cat.map(valueOf) : [];
                });
                var isNum2 = function isNum2(v) {
                    return v != null && isFinite(v);
                };
                var visible = catVals.map(function(_, i) {
                    return i;
                }).filter(function(i) {
                    return catVals[i].some(isNum2);
                });
                var Kv = Math.max(1, visible.length);
                var vmin = Infinity;
                var vmax = -Infinity;
                catVals.forEach(function(vs) {
                    return vs.forEach(function(v) {
                        if (v != null && isFinite(v)) {
                            if (v < vmin) vmin = v;
                            if (v > vmax) vmax = v;
                        }
                    });
                });
                if (vmin === Infinity) {
                    vmin = 0;
                    vmax = 1;
                }
                var tickAmount = Math.max(2, Math.round(scfg.tickAmount > 0 ? scfg.tickAmount : 5));
                var domain = this._scatterValueDomain(scfg, vmin, vmax, tickAmount);
                var vMin = domain.min;
                var vMax = domain.max;
                var vSpan = vMax - vMin || 1;
                var leftGutter = 46 + (scfg.xTitle ? 18 : 0);
                var bottomGutter = Kv > 1 ? 26 : 10;
                var plotL = leftGutter;
                var plotR = gw - 10;
                var plotT = 10;
                var plotB = gh - bottomGutter;
                var plotW = Math.max(4, plotR - plotL);
                var plotH = Math.max(4, plotB - plotT);
                var plotY = function plotY(v) {
                    return plotB - (v - vMin) / vSpan * plotH;
                };
                var laneW = plotW / Kv;
                var laneCx = function laneCx(slot) {
                    return plotL + laneW * (slot + 0.5);
                };
                var r = 0;
                var fixed = this._fixedRadius(opts);
                if (fixed) {
                    r = fixed;
                } else {
                    var _Math;
                    var maxLane = (_Math = Math).max.apply(_Math, [
                        1
                    ].concat((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_to_consumable_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(visible.map(function(i) {
                        return catVals[i].filter(isNum2).length;
                    }))));
                    r = Math.max(2, Math.min(6, laneW * 0.12, plotH / (2.5 * Math.sqrt(maxLane))));
                }
                this._lastDotR = r;
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var step = Math.max(0.5, r * spacing);
                var jitter = scfg.spread === "jitter";
                var clusters = [];
                var lanes = [];
                var maxR = sizeStats ? sizeStats.rMax : r;
                visible.forEach(function(ci, slot) {
                    var cx = laneCx(slot);
                    lanes.push({
                        i: ci,
                        cx: cx,
                        name: names[ci] || "series-".concat(ci + 1)
                    });
                    var cat = unitData[ci] || [];
                    var pts = cat.map(function(d, j) {
                        var v = valueOf(d);
                        var p = {
                            j: j,
                            py: plotY(isNum2(v) ? v : vMin),
                            x: cx
                        };
                        if (sizeStats) p.r = _this._scatterRadius(d, sizeStats, r);
                        return p;
                    });
                    if (jitter) {
                        var halfLane = Math.max(maxR, laneW / 2 - maxR);
                        pts.forEach(function(p, k) {
                            var t = (k * 9301 + 49297) % 233280 / 233280;
                            p.x = cx + (t * 2 - 1) * halfLane;
                        });
                    } else {
                        _this._beeswarm(pts, cx, r, step, maxR, true);
                    }
                    clusters.push({
                        i: ci,
                        cx: cx,
                        cy: (plotT + plotB) / 2,
                        outerR: laneW / 2,
                        dots: pts.map(function(p) {
                            return {
                                x: p.x,
                                y: p.py,
                                r: p.r
                            };
                        })
                    });
                });
                var ticks = domain.ticks;
                this._scatterAxis = {
                    mode: "1d",
                    orientation: "vertical",
                    plotL: plotL,
                    plotR: plotR,
                    plotT: plotT,
                    plotB: plotB,
                    vMin: vMin,
                    vMax: vMax,
                    plotY: plotY,
                    ticks: ticks,
                    lanes: lanes,
                    valueTitle: scfg.xTitle,
                    formatter: typeof scfg.xFormatter === "function" ? scfg.xFormatter : null,
                    gridlines: scfg.gridlines !== false
                };
                return clusters;
            }
        },
        {
            /**
   * 2D value-value scatter: each datum is a point at (`x`, `y`) on two numeric
   * axes (a scatter / bubble plot in the unit family - premium, keyed
   * transitions, per-unit colour/tooltip). Category = colour (one series group
   * per category). With `scatter.sizeRange` set, each dot is a BUBBLE scaled (by
   * area) from its `sizeField` (default 'z'). Needs the object form with numeric
   * `x` + `y`.
   * @param {any} opts
   */ key: "_layoutScatter2D",
            value: function _layoutScatter2D(opts) {
                var _this = this;
                var w = this.w;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                var scfg = opts.scatter || {};
                var unitData = w.seriesData.unitData || [];
                var isNum2 = function isNum2(v) {
                    return typeof v === "number" && isFinite(v);
                };
                var xOf = function xOf(d) {
                    return d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object" ? d.x : null;
                };
                var yOf = function yOf(d) {
                    return d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object" ? d.y != null ? d.y : d.value : null;
                };
                var visible = unitData.map(function(_, i) {
                    return i;
                }).filter(function(i) {
                    return (unitData[i] || []).some(function(d) {
                        return isNum2(xOf(d)) && isNum2(yOf(d));
                    });
                });
                var xmn = Infinity;
                var xmx = -Infinity;
                var ymn = Infinity;
                var ymx = -Infinity;
                unitData.forEach(function(cat) {
                    return (cat || []).forEach(function(d) {
                        var x = xOf(d);
                        var y = yOf(d);
                        if (isNum2(x) && isNum2(y)) {
                            if (x < xmn) xmn = x;
                            if (x > xmx) xmx = x;
                            if (y < ymn) ymn = y;
                            if (y > ymx) ymx = y;
                        }
                    });
                });
                if (xmn === Infinity) {
                    xmn = 0;
                    xmx = 1;
                    ymn = 0;
                    ymx = 1;
                }
                var xTicksN = Math.max(2, Math.round(scfg.tickAmount > 0 ? scfg.tickAmount : 5));
                var yTicksN = Math.max(2, Math.round(scfg.yTickAmount > 0 ? scfg.yTickAmount : 5));
                var nx = this._niceScale(scfg.xMin != null ? scfg.xMin : xmn, scfg.xMax != null ? scfg.xMax : xmx, xTicksN);
                var ny = this._niceScale(scfg.yMin != null ? scfg.yMin : ymn, scfg.yMax != null ? scfg.yMax : ymx, yTicksN);
                var xMin = scfg.xMin != null ? scfg.xMin : nx.min;
                var xMax = scfg.xMax != null ? scfg.xMax : nx.max;
                var yMin = scfg.yMin != null ? scfg.yMin : ny.min;
                var yMax = scfg.yMax != null ? scfg.yMax : ny.max;
                var xSpan = xMax - xMin || 1;
                var ySpan = yMax - yMin || 1;
                var leftGutter = 46 + (scfg.yTitle ? 18 : 0);
                var bottomGutter = 30 + (scfg.xTitle ? 20 : 0);
                var plotL = leftGutter;
                var plotR = gw - 12;
                var plotT = 10;
                var plotB = gh - bottomGutter;
                var plotW = Math.max(4, plotR - plotL);
                var plotH = Math.max(4, plotB - plotT);
                var plotX = function plotX(v) {
                    return plotL + (v - xMin) / xSpan * plotW;
                };
                var plotY = function plotY(v) {
                    return plotB - (v - yMin) / ySpan * plotH;
                };
                var sizeStats = this._scatterSizeStats(scfg, unitData);
                var baseR = this._fixedRadius(opts) || 5;
                this._lastDotR = baseR;
                var clusters = [];
                visible.forEach(function(ci) {
                    var cat = unitData[ci] || [];
                    var dots = cat.map(function(d) {
                        var x = xOf(d);
                        var y = yOf(d);
                        return {
                            x: plotX(isNum2(x) ? x : xMin),
                            y: plotY(isNum2(y) ? y : yMin),
                            r: sizeStats ? _this._scatterRadius(d, sizeStats, baseR) : void 0
                        };
                    });
                    clusters.push({
                        i: ci,
                        cx: (plotL + plotR) / 2,
                        cy: (plotT + plotB) / 2,
                        outerR: plotH / 2,
                        dots: dots
                    });
                });
                var mkTicks = function mkTicks(lo, hi, span, spacing, pinned, n) {
                    var out = [];
                    if (pinned) {
                        for(var k = 0; k < n; k++)out.push(lo + span * k / (n - 1));
                    } else {
                        var sp = spacing || span / Math.max(1, n - 1);
                        for(var v = lo; v <= hi + sp * 0.5; v += sp){
                            out.push(Math.abs(v) < sp * 1e-9 ? 0 : v);
                        }
                    }
                    return out;
                };
                this._scatterAxis = {
                    mode: "2d",
                    plotL: plotL,
                    plotR: plotR,
                    plotT: plotT,
                    plotB: plotB,
                    plotX: plotX,
                    plotY: plotY,
                    xTicks: mkTicks(xMin, xMax, xSpan, nx.spacing, scfg.xMin != null || scfg.xMax != null, xTicksN),
                    yTicks: mkTicks(yMin, yMax, ySpan, ny.spacing, scfg.yMin != null || scfg.yMax != null, yTicksN),
                    xTitle: scfg.xTitle,
                    yTitle: scfg.yTitle,
                    xFormatter: typeof scfg.xFormatter === "function" ? scfg.xFormatter : null,
                    yFormatter: typeof scfg.yFormatter === "function" ? scfg.yFormatter : null,
                    gridlines: scfg.gridlines !== false
                };
                return clusters;
            }
        },
        {
            /**
   * Bubble size stats for the scatter layout, or null when `scatter.sizeRange`
   * is not a `[minR, maxR]` pair. Reads the global range of each datum's
   * `sizeField` (default 'z') so a value maps to a radius (area scale) in
   * _scatterRadius.
   * @param {any} scfg @param {any[][]} unitData
   * @returns {{zmin:number,zmax:number,rMin:number,rMax:number,field:string}|null}
   */ key: "_scatterSizeStats",
            value: function _scatterSizeStats(scfg, unitData) {
                var range = scfg && scfg.sizeRange;
                if (!Array.isArray(range) || range.length < 2) return null;
                var rMin = Math.max(0.5, +range[0]);
                var rMax = Math.max(rMin, +range[1]);
                var field = scfg.sizeField || "z";
                var zmin = Infinity;
                var zmax = -Infinity;
                unitData.forEach(function(cat) {
                    return (cat || []).forEach(function(d) {
                        var z = d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object" ? d[field] : null;
                        if (typeof z === "number" && isFinite(z)) {
                            if (z < zmin) zmin = z;
                            if (z > zmax) zmax = z;
                        }
                    });
                });
                if (zmin === Infinity) return null;
                return {
                    zmin: zmin,
                    zmax: zmax,
                    rMin: rMin,
                    rMax: rMax,
                    field: field
                };
            }
        },
        {
            /**
   * Radius for one datum under the bubble size stats: area proportional to the
   * `sizeField` value (so radius grows with sqrt), between rMin and rMax. A
   * missing value collapses to rMin.
   * @param {any} d
   * @param {{zmin:number,zmax:number,rMin:number,rMax:number,field:string}} st
   * @param {number} fallback @returns {number}
   */ key: "_scatterRadius",
            value: function _scatterRadius(d, st, fallback) {
                if (!st) return fallback;
                var z = d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object" ? d[st.field] : null;
                if (typeof z !== "number" || !isFinite(z)) return st.rMin;
                var t = st.zmax > st.zmin ? (z - st.zmin) / (st.zmax - st.zmin) : 1;
                var tc = Math.max(0, Math.min(1, t));
                var aMin = st.rMin * st.rMin;
                var aMax = st.rMax * st.rMax;
                return Math.sqrt(aMin + tc * (aMax - aMin));
            }
        },
        {
            /**
   * One-dimensional anti-overlap "beeswarm" pack: given points with a fixed x
   * (`px`) and a lane centre `cy`, assign each a y so no two dots overlap (centre
   * distance >= r_i + r_j). Greedy in ascending-x order, trying offsets 0, +step,
   * -step, +2step ... and taking the SMALLEST that clears every already-placed
   * neighbour still within reach in x. No-overlap always wins: a very dense lane
   * grows a taller swarm rather than stacking dots (offsets are not hard-clamped
   * to the lane). Each point may carry its own radius `r` (bubble beeswarm),
   * else `rFallback` applies; `maxR` bounds the value-window break. Deterministic
   * (no physics, no randomness).
   *
   * Orientation-agnostic: the "fixed" axis is the value axis and the "spread"
   * axis is the lane thickness. Horizontal (default): fixed = `px`, spread = `y`
   * (mutates `.y`). Vertical: fixed = `py`, spread = `x` (mutates `.x`).
   * @param {any[]} pts @param {number} center lane centre on the spread axis
   * @param {number} rFallback @param {number} step @param {number} [maxR]
   * @param {boolean} [vertical]
   */ key: "_beeswarm",
            value: function _beeswarm(pts, center, rFallback, step, maxR) {
                var vertical = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : false;
                var fk = vertical ? "py" : "px";
                var sk = vertical ? "x" : "y";
                var order = pts.slice().sort(function(a, b) {
                    return a[fk] - b[fk];
                });
                var placed = [];
                var rCap = maxR != null ? maxR : rFallback;
                order.forEach(function(p) {
                    var pr = p.r != null ? p.r : rFallback;
                    var chosen = 0;
                    for(var k = 0; k < 2e3; k++){
                        var off = k === 0 ? 0 : Math.ceil(k / 2) * step * (k % 2 ? 1 : -1);
                        var s = center + off;
                        var ok = true;
                        for(var m = placed.length - 1; m >= 0; m--){
                            var q = placed[m];
                            var df = p[fk] - q.f;
                            if (df > pr + rCap) break;
                            var need = pr + q.r;
                            var ds = s - q.s;
                            if (df * df + ds * ds < need * need) {
                                ok = false;
                                break;
                            }
                        }
                        if (ok) {
                            chosen = off;
                            break;
                        }
                    }
                    p[sk] = center + chosen;
                    placed.push({
                        f: p[fk],
                        s: p[sk],
                        r: pr
                    });
                });
            }
        },
        {
            /**
   * Value-axis domain + ticks for a 1D beeswarm. The domain ALWAYS contains
   * every datum: a swarm that clips a dot outside the plot box is a bug, so an
   * explicit `xMin`/`xMax` only FRAMES the axis and is extended by whole
   * tick-steps whenever the data would otherwise overflow. Orientation-agnostic:
   * the same value axis is X for a horizontal swarm and Y for a vertical one.
   * @param {any} scfg scatter config
   * @param {number} vmin data minimum @param {number} vmax data maximum
   * @param {number} tickAmount desired tick count
   * @returns {{ min:number, max:number, ticks:number[] }}
   */ key: "_scatterValueDomain",
            value: function _scatterValueDomain(scfg, vmin, vmax, tickAmount) {
                var buildTicks = function buildTicks(min, max, spacing2) {
                    var ticks = [];
                    for(var v = min; v <= max + spacing2 * 0.5; v += spacing2){
                        ticks.push(Math.abs(v) < spacing2 * 1e-9 ? 0 : v);
                    }
                    return ticks;
                };
                if (scfg.xMin != null || scfg.xMax != null) {
                    var min = scfg.xMin != null ? scfg.xMin : vmin;
                    var max = scfg.xMax != null ? scfg.xMax : vmax;
                    if (!(max > min)) max = min + 1;
                    var spacing2 = (max - min) / Math.max(1, tickAmount - 1);
                    if (vmin < min) min -= Math.ceil((min - vmin) / spacing2) * spacing2;
                    if (vmax > max) max += Math.ceil((vmax - max) / spacing2) * spacing2;
                    return {
                        min: min,
                        max: max,
                        ticks: buildTicks(min, max, spacing2)
                    };
                }
                var nice = this._niceScale(vmin, vmax, tickAmount);
                var spacing = nice.spacing || (nice.max - nice.min) / Math.max(1, tickAmount - 1);
                return {
                    min: nice.min,
                    max: nice.max,
                    ticks: buildTicks(nice.min, nice.max, spacing)
                };
            }
        },
        {
            /**
   * A "nice" numeric scale [min, max] + tick spacing covering [dataMin, dataMax]
   * with about `ticks` ticks, using rounded 1/2/5 x 10^n steps. Homegrown (no
   * dependency) - lean-core.
   * @param {number} dataMin @param {number} dataMax @param {number} ticks
   * @returns {{min:number,max:number,spacing:number}}
   */ key: "_niceScale",
            value: function _niceScale(dataMin, dataMax, ticks) {
                var lo = dataMin;
                var hi = dataMax;
                if (!(hi > lo)) hi = lo + 1;
                var range = this._niceNum(hi - lo, false);
                var spacing = this._niceNum(range / Math.max(1, ticks - 1), true);
                return {
                    min: Math.floor(lo / spacing) * spacing,
                    max: Math.ceil(hi / spacing) * spacing,
                    spacing: spacing
                };
            }
        },
        {
            /**
   * Round a range to a "nice" 1/2/5 x 10^n number (Heckbert's loose/round label
   * algorithm).
   * @param {number} range @param {boolean} round @returns {number}
   */ key: "_niceNum",
            value: function _niceNum(range, round) {
                var rng = range > 0 ? range : 1;
                var exp = Math.floor(Math.log(rng) / Math.LN10);
                var frac = rng / Math.pow(10, exp);
                var nf;
                if (round) {
                    nf = frac < 1.5 ? 1 : frac < 3 ? 2 : frac < 7 ? 5 : 10;
                } else {
                    nf = frac <= 1 ? 1 : frac <= 2 ? 2 : frac <= 5 ? 5 : 10;
                }
                return nf * Math.pow(10, exp);
            }
        },
        {
            /**
   * Draw the scatter chrome behind the dots, from the geometry the layout
   * stashed on `this._scatterAxis`. 1D (beeswarm): vertical X gridlines +
   * baseline + tick labels (+ x title) + a per-lane category label in the
   * category colour. 2D: both X + Y gridlines, both axes' tick labels, and
   * rotated/placed axis titles (no lane labels - category is colour). Browser-
   * only (SSR renders the dots without the chrome, as with cluster labels).
   * @param {any} ret @param {Graphics} graphics
   */ key: "_drawScatterAxes",
            value: function _drawScatterAxes(ret, graphics) {
                var _this = this;
                var w = this.w;
                if (!Environment.isBrowser()) return;
                var ax = this._scatterAxis;
                if (!ax) return;
                var NS = "http://www.w3.org/2000/svg";
                var g = graphics.group({
                    class: "apexcharts-unit-axis"
                });
                var gridColor = w.config.grid && w.config.grid.borderColor || "rgba(128,128,128,0.18)";
                var axisColor = "rgba(128,128,128,0.5)";
                var labelColor = w.config.xaxis && w.config.xaxis.labels && w.config.xaxis.labels.style && w.config.xaxis.labels.style.colors || "rgba(120,130,140,0.9)";
                var line = function line(x1, y1, x2, y2, stroke) {
                    var l = BrowserAPIs.createElementNS(NS, "line");
                    l.setAttribute("x1", String(x1));
                    l.setAttribute("y1", String(y1));
                    l.setAttribute("x2", String(x2));
                    l.setAttribute("y2", String(y2));
                    l.setAttribute("stroke", stroke);
                    l.setAttribute("shape-rendering", "crispEdges");
                    g.node.appendChild(l);
                };
                var text = function text(str, x, y, anchor, fill, size, weight, cls) {
                    var t = BrowserAPIs.createElementNS(NS, "text");
                    t.setAttribute("class", cls);
                    t.setAttribute("x", String(x));
                    t.setAttribute("y", String(y));
                    t.setAttribute("text-anchor", anchor);
                    t.setAttribute("dominant-baseline", "middle");
                    t.setAttribute("font-size", "".concat(size, "px"));
                    t.setAttribute("font-family", w.config.chart.fontFamily || "inherit");
                    t.setAttribute("font-weight", String(weight));
                    t.setAttribute("fill", fill);
                    t.textContent = str;
                    g.node.appendChild(t);
                };
                if (ax.mode === "2d") {
                    ax.yTicks.forEach(function(v) {
                        var y = ax.plotY(v);
                        if (ax.gridlines) line(ax.plotL, y, ax.plotR, y, gridColor);
                        var label = ax.yFormatter ? String(ax.yFormatter(v)) : _this._formatTick(v);
                        text(label, ax.plotL - 8, y, "end", labelColor, 11, 400, "apexcharts-unit-tick");
                    });
                    ax.xTicks.forEach(function(v) {
                        var x = ax.plotX(v);
                        if (ax.gridlines) line(x, ax.plotT, x, ax.plotB, gridColor);
                        var label = ax.xFormatter ? String(ax.xFormatter(v)) : _this._formatTick(v);
                        text(label, x, ax.plotB + 14, "middle", labelColor, 11, 400, "apexcharts-unit-tick");
                    });
                    line(ax.plotL, ax.plotB, ax.plotR, ax.plotB, axisColor);
                    line(ax.plotL, ax.plotT, ax.plotL, ax.plotB, axisColor);
                    if (ax.xTitle) {
                        text(String(ax.xTitle), (ax.plotL + ax.plotR) / 2, ax.plotB + 32, "middle", labelColor, 12, 600, "apexcharts-unit-axis-title");
                    }
                    if (ax.yTitle) {
                        var yt = BrowserAPIs.createElementNS(NS, "text");
                        yt.setAttribute("class", "apexcharts-unit-axis-title");
                        var tx = 14;
                        var ty = (ax.plotT + ax.plotB) / 2;
                        yt.setAttribute("x", String(tx));
                        yt.setAttribute("y", String(ty));
                        yt.setAttribute("text-anchor", "middle");
                        yt.setAttribute("font-size", "12px");
                        yt.setAttribute("font-family", w.config.chart.fontFamily || "inherit");
                        yt.setAttribute("font-weight", "600");
                        yt.setAttribute("fill", labelColor);
                        yt.setAttribute("transform", "rotate(-90 ".concat(tx, " ").concat(ty, ")"));
                        yt.textContent = String(ax.yTitle);
                        g.node.appendChild(yt);
                    }
                    ret.add(g);
                    return;
                }
                if (ax.orientation === "vertical") {
                    ax.ticks.forEach(function(v) {
                        var y = ax.plotY(v);
                        if (ax.gridlines) line(ax.plotL, y, ax.plotR, y, gridColor);
                        var label = ax.formatter ? String(ax.formatter(v)) : _this._formatTick(v);
                        text(label, ax.plotL - 8, y, "end", labelColor, 11, 400, "apexcharts-unit-tick");
                    });
                    line(ax.plotL, ax.plotT, ax.plotL, ax.plotB, axisColor);
                    if (ax.valueTitle) {
                        var yt1 = BrowserAPIs.createElementNS(NS, "text");
                        yt1.setAttribute("class", "apexcharts-unit-axis-title");
                        var tx1 = 14;
                        var ty1 = (ax.plotT + ax.plotB) / 2;
                        yt1.setAttribute("x", String(tx1));
                        yt1.setAttribute("y", String(ty1));
                        yt1.setAttribute("text-anchor", "middle");
                        yt1.setAttribute("font-size", "12px");
                        yt1.setAttribute("font-family", w.config.chart.fontFamily || "inherit");
                        yt1.setAttribute("font-weight", "600");
                        yt1.setAttribute("fill", labelColor);
                        yt1.setAttribute("transform", "rotate(-90 ".concat(tx1, " ").concat(ty1, ")"));
                        yt1.textContent = String(ax.valueTitle);
                        g.node.appendChild(yt1);
                    }
                    ax.lanes.forEach(function(lane) {
                        var color = w.globals.colors[lane.i] || w.globals.colors[0] || "#008FFB";
                        text(lane.name, lane.cx, ax.plotB + 16, "middle", color, 12, 600, "apexcharts-unit-lane-label");
                    });
                    ret.add(g);
                    return;
                }
                ax.ticks.forEach(function(v, idx) {
                    var x = ax.plotX(v);
                    if (ax.gridlines) line(x, ax.plotT, x, ax.plotB, gridColor);
                    var label = ax.formatter ? String(ax.formatter(v)) : _this._formatTick(v);
                    var anchor = idx === 0 ? "start" : idx === ax.ticks.length - 1 ? "end" : "middle";
                    text(label, x, ax.plotB + 14, anchor, labelColor, 11, 400, "apexcharts-unit-tick");
                });
                line(ax.plotL, ax.plotB, ax.plotR, ax.plotB, axisColor);
                if (ax.xTitle) {
                    text(String(ax.xTitle), (ax.plotL + ax.plotR) / 2, ax.plotB + 32, "middle", labelColor, 12, 600, "apexcharts-unit-axis-title");
                }
                if (ax.plotL > 12) {
                    ax.lanes.forEach(function(lane) {
                        var color = w.globals.colors[lane.i] || w.globals.colors[0] || "#008FFB";
                        text(lane.name, ax.plotL - 8, lane.cy, "end", color, 12, 600, "apexcharts-unit-lane-label");
                    });
                }
                ret.add(g);
            }
        },
        {
            /**
   * Compact tick-value formatting: integers as-is, otherwise trimmed to a short
   * decimal; large magnitudes get a k/M suffix.
   * @param {number} v @returns {string}
   */ key: "_formatTick",
            value: function _formatTick(v) {
                if (!isFinite(v)) return "";
                var a = Math.abs(v);
                if (a >= 1e6) return "".concat(+(v / 1e6).toFixed(1), "M");
                if (a >= 1e4) return "".concat(+(v / 1e3).toFixed(1), "k");
                if (Number.isInteger(v)) return String(v);
                return String(+v.toFixed(2));
            }
        },
        {
            /**
   * Distribute `total` whole cells across `counts` in proportion to each value,
   * using the largest-remainder method so the parts sum to exactly `total`
   * (used by the grid/waffle percentage mode).
   * @param {number[]} counts @param {number} total @returns {number[]}
   */ key: "_largestRemainder",
            value: function _largestRemainder(counts, total) {
                var sum = counts.reduce(function(a, b) {
                    return a + b;
                }, 0);
                if (sum <= 0 || total <= 0) return counts.map(function() {
                    return 0;
                });
                var exact = counts.map(function(c) {
                    return c / sum * total;
                });
                var floors = exact.map(function(v) {
                    return Math.floor(v);
                });
                var used = floors.reduce(function(a, b) {
                    return a + b;
                }, 0);
                var remaining = Math.max(0, total - used);
                var byFrac = exact.map(function(v, i) {
                    return {
                        i: i,
                        frac: v - Math.floor(v)
                    };
                }).sort(function(a, b) {
                    return b.frac - a.frac;
                });
                var out = floors.slice();
                for(var n = 0; n < remaining && n < byFrac.length; n++){
                    out[byFrac[n].i]++;
                }
                return out;
            }
        },
        {
            /**
   * Phyllotaxis (sunflower) placement for `n` points around (cx, cy).
   * @param {number} cx @param {number} cy @param {number} n
   * @param {number} step @param {number} startIndex
   * @returns {{x:number,y:number}[]}
   */ key: "_spiral",
            value: function _spiral(cx, cy, n, step, startIndex) {
                var pts = [];
                for(var k = 0; k < n; k++){
                    var idx = startIndex + k;
                    var r = step * Math.sqrt(idx + 0.5);
                    var theta = idx * GOLDEN_ANGLE;
                    pts.push({
                        x: cx + r * Math.cos(theta),
                        y: cy + r * Math.sin(theta)
                    });
                }
                return pts;
            }
        },
        {
            /**
   * A fixed dot radius, if the shape/size implies one: an explicit numeric
   * `size`, or an `image` shape (sized by its own width/height). Returns null
   * when dots should auto-size to fit the plot.
   * @param {any} opts @returns {number | null}
   */ key: "_fixedRadius",
            value: function _fixedRadius(opts) {
                if (opts.shape === "image" && opts.image) {
                    return Math.max(opts.image.width || 20, opts.image.height || 20) / 2;
                }
                if (this._bubbleActive(opts) && typeof opts.sizeByValue.maxRadius === "number") {
                    return opts.sizeByValue.maxRadius > 0 ? opts.sizeByValue.maxRadius : null;
                }
                if (typeof opts.size === "number" && opts.size > 0) return opts.size;
                return null;
            }
        },
        {
            /**
   * Whether opt-in bubble sizing applies: enabled, and the shape is a circle
   * (squares/images keep a uniform size).
   * @param {any} opts @returns {boolean}
   */ key: "_bubbleActive",
            value: function _bubbleActive(opts) {
                var sbv = opts.sizeByValue;
                return !!(sbv && sbv.enabled && opts.shape !== "image" && opts.shape !== "square");
            }
        },
        {
            /**
   * This datum's numeric value for sizing / tooltip: the number itself, or an
   * object's `value` / `y`. Null when there is no usable number.
   * @param {any} d @returns {number | null}
   */ key: "_unitValueOf",
            value: function _unitValueOf(d) {
                if (typeof d === "number") return d;
                if (d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object") {
                    var v = d.value != null ? d.value : d.y;
                    return typeof v === "number" ? v : null;
                }
                return null;
            }
        },
        {
            /**
   * Radius for one bubble given the value stats. Default 'area' scaling makes
   * a bubble's AREA proportional to its value (radius grows with sqrt); 'linear'
   * scales the radius directly. Missing values collapse to the min radius.
   * @param {number|null} v
   * @param {{min:number,max:number,minR:number,maxR:number,scale:string}} stats
   * @returns {number}
   */ key: "_radiusForValue",
            value: function _radiusForValue(v, stats) {
                if (v == null || !isFinite(v)) return stats.minR;
                var t = stats.max > stats.min ? (v - stats.min) / (stats.max - stats.min) : 1;
                var tc = Math.max(0, Math.min(1, t));
                if (stats.scale === "linear") {
                    return stats.minR + tc * (stats.maxR - stats.minR);
                }
                var aMin = stats.minR * stats.minR;
                var aMax = stats.maxR * stats.maxR;
                return Math.sqrt(aMin + tc * (aMax - aMin));
            }
        },
        {
            /**
   * Value stats + radius bounds for bubble sizing, or null when it does not
   * apply (disabled, non-circle shape, or no per-unit values). `maxR` is the
   * reference radius the layout already spaced the lattice for; `minR` defaults
   * to ~35% of it.
   * @param {any[][]} unitData @param {any} opts @param {number} refR
   * @returns {{min:number,max:number,minR:number,maxR:number,scale:string}|null}
   */ key: "_bubbleStats",
            value: function _bubbleStats(unitData, opts, refR) {
                var _this = this;
                if (!this._bubbleActive(opts)) return null;
                var vmin = Infinity;
                var vmax = -Infinity;
                unitData.forEach(function(cat) {
                    if (!cat) return;
                    cat.forEach(function(d) {
                        var v = _this._unitValueOf(d);
                        if (v != null && isFinite(v)) {
                            if (v < vmin) vmin = v;
                            if (v > vmax) vmax = v;
                        }
                    });
                });
                if (vmin === Infinity || vmax < vmin) return null;
                var sbv = opts.sizeByValue;
                var maxR = refR;
                var minR = Math.max(1, Math.min(maxR, typeof sbv.minRadius === "number" ? sbv.minRadius : maxR * 0.35));
                return {
                    min: vmin,
                    max: vmax,
                    minR: minR,
                    maxR: maxR,
                    scale: sbv.scale === "linear" ? "linear" : "area"
                };
            }
        },
        {
            /**
   * Radial step between successive spiral shells. A fixed radius derives the
   * step directly; 'auto' derives it so a cluster of `count` dots fits `availR`.
   * @param {any} opts @param {number} availR @param {number} count
   * @returns {number}
   */ key: "_resolveStep",
            value: function _resolveStep(opts, availR, count) {
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var fixed = this._fixedRadius(opts);
                if (fixed) return 2 * fixed * spacing;
                return availR / (Math.sqrt(Math.max(1, count)) + 0.5);
            }
        },
        {
            /**
   * @param {number} step @param {any} opts
   * @returns {number}
   */ key: "_dotRadiusFromStep",
            value: function _dotRadiusFromStep(step, opts) {
                var spacing = opts.spacing > 0 ? opts.spacing : 1;
                var fixed = this._fixedRadius(opts);
                if (fixed) return fixed;
                return Math.max(1, step / (2 * spacing));
            }
        },
        {
            /**
   * Corner-anchored shapes (square, image) position by their top-left x/y;
   * circles position by their centre cx/cy.
   * @param {any} opts @returns {boolean}
   */ key: "_isCorner",
            value: function _isCorner(opts) {
                return opts.shape === "square" || opts.shape === "image";
            }
        },
        {
            /**
   * Half-width/height used to convert a centre point to a corner shape's x/y.
   * @param {any} opts @returns {{hx:number, hy:number}}
   */ key: "_halfExtent",
            value: function _halfExtent(opts) {
                if (opts.shape === "image" && opts.image) {
                    return {
                        hx: (opts.image.width || 20) / 2,
                        hy: (opts.image.height || 20) / 2
                    };
                }
                var r = this._lastDotR;
                return {
                    hx: r,
                    hy: r
                };
            }
        },
        {
            /**
   * Draw one dot (circle, square, or image icon) with the category fill +
   * stroke, tagged so the shared non-axis tooltip and hover reuse work.
   * @param {Graphics} graphics @param {any} opts @param {number} dotR
   * @param {string} color @param {number} i @param {number} j
   * @returns {any}
   */ key: "_drawDot",
            value: function _drawDot(graphics, opts, dotR, color, i, j) {
                var w = this.w;
                var strokeW = w.config.stroke.show ? w.config.stroke.width : 0;
                var strokeColor = Array.isArray(w.globals.stroke.colors) ? w.globals.stroke.colors[i] || "none" : "none";
                var fillOpacity = typeof w.config.fill.opacity === "number" ? w.config.fill.opacity : 1;
                var el;
                if (opts.shape === "image" && opts.image && opts.image.src) {
                    var iw = opts.image.width || 20;
                    var ih = opts.image.height || 20;
                    el = w.dom.Paper.image(opts.image.src);
                    el.node.setAttribute("width", String(iw));
                    el.node.setAttribute("height", String(ih));
                    el.node.setAttribute("preserveAspectRatio", "xMidYMid meet");
                    if (opts.image.tint) {
                        el.node.setAttribute("filter", "url(#".concat(this._tintFilter(color), ")"));
                    }
                } else if (opts.shape === "square") {
                    var side = dotR * 2;
                    el = graphics.drawRect(0, 0, side, side, opts.borderRadius || 0, color, 1, strokeW, strokeColor);
                    el.node.setAttribute("fill", color);
                    if (fillOpacity < 1) el.node.setAttribute("fill-opacity", String(fillOpacity));
                } else {
                    el = graphics.drawCircle(dotR, {
                        fill: color,
                        "stroke-width": strokeW,
                        stroke: strokeColor
                    });
                    el.node.setAttribute("fill", color);
                    if (fillOpacity < 1) el.node.setAttribute("fill-opacity", String(fillOpacity));
                }
                el.node.classList.add("apexcharts-unit-area");
                el.node.setAttribute("i", String(i));
                el.node.setAttribute("j", String(j));
                return el;
            }
        },
        {
            /**
   * Ensure (once per colour) an SVG recolour filter exists in the chart's defs
   * and return its id. The filter floods `color` and clips it to the source
   * graphic's alpha (feComposite operator="in"), so an `<image>` referencing a
   * monochrome icon is repainted in `color` while keeping its silhouette. Reused
   * across every dot of the same colour.
   * @param {string} color @returns {string}
   */ key: "_tintFilter",
            value: function _tintFilter(color) {
                var w = this.w;
                var NS = "http://www.w3.org/2000/svg";
                var safe = String(color).replace(/[^a-zA-Z0-9]/g, "");
                var id = "apexcharts-unit-tint-".concat(w.globals.chartID, "-").concat(safe);
                var svg = w.dom.Paper.node;
                if (svg.querySelector("#".concat(id))) return id;
                var defs = svg.querySelector("defs");
                if (!defs) {
                    defs = BrowserAPIs.createElementNS(NS, "defs");
                    svg.insertBefore(defs, svg.firstChild);
                }
                var filter = BrowserAPIs.createElementNS(NS, "filter");
                filter.setAttribute("id", id);
                filter.setAttribute("x", "0%");
                filter.setAttribute("y", "0%");
                filter.setAttribute("width", "100%");
                filter.setAttribute("height", "100%");
                var flood = BrowserAPIs.createElementNS(NS, "feFlood");
                flood.setAttribute("flood-color", color);
                flood.setAttribute("result", "flood");
                var comp = BrowserAPIs.createElementNS(NS, "feComposite");
                comp.setAttribute("in", "flood");
                comp.setAttribute("in2", "SourceAlpha");
                comp.setAttribute("operator", "in");
                filter.appendChild(flood);
                filter.appendChild(comp);
                defs.appendChild(filter);
                return id;
            }
        },
        {
            /**
   * Position a non-animated dot at (x, y). Circles use cx/cy at the centre;
   * corner shapes (square, image) use x/y at the top-left.
   * @param {SVGElement} node @param {any} opts @param {number} x @param {number} y
   */ key: "_placeDot",
            value: function _placeDot(node, opts, x, y) {
                if (this._isCorner(opts)) {
                    var _this__halfExtent = this._halfExtent(opts), hx = _this__halfExtent.hx, hy = _this__halfExtent.hy;
                    node.setAttribute("x", String(x - hx));
                    node.setAttribute("y", String(y - hy));
                } else {
                    node.setAttribute("cx", String(x));
                    node.setAttribute("cy", String(y));
                }
            }
        },
        {
            /**
   * Parse a `#rgb` / `#rrggbb` / `rgb()` / `rgba()` colour to `[r, g, b]`, or
   * null if it cannot be parsed (the colour tween is then skipped).
   * @param {string} str @returns {number[] | null}
   */ key: "_rgb",
            value: function _rgb(str) {
                if (typeof str !== "string") return null;
                var s = str.trim();
                if (s[0] === "#") {
                    if (s.length === 4) s = "#" + s[1] + s[1] + s[2] + s[2] + s[3] + s[3];
                    var n = parseInt(s.slice(1, 7), 16);
                    if (isNaN(n)) return null;
                    return [
                        n >> 16 & 255,
                        n >> 8 & 255,
                        n & 255
                    ];
                }
                var m = s.match(/rgba?\(([^)]+)\)/);
                if (m) {
                    var p = m[1].split(",").map(function(x) {
                        return parseFloat(x);
                    });
                    if (p.length >= 3 && p.every(function(v) {
                        return !isNaN(v);
                    })) return [
                        p[0],
                        p[1],
                        p[2]
                    ];
                }
                return null;
            }
        },
        {
            /**
   * Whether to run the gather / transition animation. Runs on the initial mount
   * and on data-driven updates (keyed old->new tween or cross-type burst).
   * Skipped: in SSR, when animations are off, when the caller passed
   * `animate:false` (shouldAnimate === false), on a PURE window resize (resized
   * with no data change - re-gathering on every resize would be jarring), and
   * when the user prefers reduced motion.
   *
   * Note: `w.globals.resized` is set true on every update (not just window
   * resize), so it must be paired with `!dataChanged` to isolate a real resize.
   * @returns {boolean}
   */ key: "_shouldAnimate",
            value: function _shouldAnimate() {
                var w = this.w;
                var anim = w.config.chart.animations;
                if (!Environment.isBrowser()) return false;
                if (!anim || anim.enabled === false) return false;
                if (w.globals.shouldAnimate === false) return false;
                if (w.globals.resized && !w.globals.dataChanged) return false;
                if (anim.respectReducedMotion && prefersReducedMotion()) return false;
                return true;
            }
        },
        {
            /**
   * One rAF loop that tweens every dot from its start (cx0/cy0 - either the
   * cluster centre on first mount / for entering dots, or its previous slot on
   * an update) to its target slot, staggered by index. Entering dots fade in;
   * moving dots stay opaque. Dots whose group colour changed (a 'flow' regroup)
   * cross-fade their fill from the old colour to the new one over the same ease;
   * dots whose radius changed (bubble sizing) grow/shrink over it too (circles).
   * @param {{ node: SVGElement, x: number, y: number, cx0: number, cy0: number, r0?: number, r1?: number, delay: number, isEnter: boolean, fill0?: string, fill1?: string, _c0?: number[]|null, _c1?: number[]|null }[]} dots
   */ key: "_runGather",
            value: function _runGather(dots) {
                var _this = this;
                var w = this.w;
                var opts = w.config.plotOptions.unit;
                var speed = Math.max(1, w.config.chart.animations.speed || 800);
                var corner = this._isCorner(opts);
                var _this__halfExtent = this._halfExtent(opts), hx = _this__halfExtent.hx, hy = _this__halfExtent.hy;
                var maxDelay = Math.min(speed * 0.6, 450);
                var n = dots.length;
                for(var k = 0; k < n; k++){
                    dots[k].delay = n > 1 ? k / (n - 1) * maxDelay : 0;
                }
                var cxAttr = corner ? "x" : "cx";
                var cyAttr = corner ? "y" : "cy";
                var offX = corner ? hx : 0;
                var offY = corner ? hy : 0;
                if (corner) {
                    for(var k1 = 0; k1 < n; k1++){
                        dots[k1].node.setAttribute(cxAttr, String(dots[k1].cx0 - offX));
                        dots[k1].node.setAttribute(cyAttr, String(dots[k1].cy0 - offY));
                    }
                }
                if (!corner) {
                    for(var k2 = 0; k2 < n; k2++){
                        var d = dots[k2];
                        if (d.r0 != null && d.r1 != null && d.r0 !== d.r1) {
                            d.node.setAttribute("r", String(d.r0));
                        }
                    }
                }
                for(var k3 = 0; k3 < n; k3++){
                    var d1 = dots[k3];
                    if (d1.fill0 && d1.fill1 && d1.fill0 !== d1.fill1) {
                        d1._c0 = this._rgb(d1.fill0);
                        d1._c1 = this._rgb(d1.fill1);
                    }
                }
                var gcfg = opts.gather || {};
                var easePos = gcfg.easing === "outBack" ? easeOutBack(typeof gcfg.overshoot === "number" ? gcfg.overshoot : 1.70158) : gcfg.easing === "inOutCubic" ? easeInOutCubic : easeOutCubic;
                if (this.w.globals.unitGatherRAF != null) {
                    BrowserAPIs.cancelAnimationFrame(this.w.globals.unitGatherRAF);
                    this.w.globals.unitGatherRAF = null;
                }
                var start = performance.now();
                var stepFn = function stepFn1(now) {
                    if (_this.w.globals.isDestroyed) {
                        _this.w.globals.unitGatherRAF = null;
                        return;
                    }
                    var done = true;
                    for(var k = 0; k < n; k++){
                        var d = dots[k];
                        var t = Math.max(0, Math.min(1, (now - start - d.delay) / speed));
                        var e = easePos(t);
                        var ec = easeOutCubic(t);
                        var cx = d.cx0 + (d.x - d.cx0) * e;
                        var cy = d.cy0 + (d.y - d.cy0) * e;
                        d.node.setAttribute(cxAttr, String(cx - offX));
                        d.node.setAttribute(cyAttr, String(cy - offY));
                        if (d.isEnter) d.node.style.opacity = String(Math.min(1, t * 2.5));
                        if (d._c0 && d._c1) {
                            var cr = Math.round(d._c0[0] + (d._c1[0] - d._c0[0]) * ec);
                            var cg = Math.round(d._c0[1] + (d._c1[1] - d._c0[1]) * ec);
                            var cb = Math.round(d._c0[2] + (d._c1[2] - d._c0[2]) * ec);
                            d.node.setAttribute("fill", "rgb(".concat(cr, ",").concat(cg, ",").concat(cb, ")"));
                        }
                        if (!corner && d.r0 != null && d.r1 != null && d.r0 !== d.r1) {
                            d.node.setAttribute("r", String(d.r0 + (d.r1 - d.r0) * ec));
                        }
                        if (t < 1) done = false;
                    }
                    if (done) {
                        for(var k1 = 0; k1 < n; k1++){
                            var d1 = dots[k1];
                            d1.node.style.opacity = "";
                            if (d1._c1 && d1.fill1) d1.node.setAttribute("fill", d1.fill1);
                            if (!corner && d1.r0 != null && d1.r1 != null && d1.r0 !== d1.r1) {
                                d1.node.setAttribute("r", String(d1.r1));
                            }
                        }
                        _this.w.globals.unitGatherRAF = null;
                    } else {
                        _this.w.globals.unitGatherRAF = BrowserAPIs.requestAnimationFrame(stepFn);
                    }
                };
                this.w.globals.unitGatherRAF = BrowserAPIs.requestAnimationFrame(stepFn);
            }
        },
        {
            /**
   * Keys present in the previous render but not the current one, resolved back
   * to their old slot {x, y, fill}. These are the dots that must animate out.
   * @param {Map<string, {x:number,y:number,fill:string}>} prev
   * @param {Map<string, {x:number,y:number,fill:string}>} nextPrev
   * @param {any} opts
   * @returns {{x:number,y:number,fill:string}[]}
   */ key: "_collectExits",
            value: function _collectExits(prev, nextPrev, opts) {
                var cap = Math.max(0, opts.maxUnits || 5e3);
                var exits = [];
                var _iteratorNormalCompletion = true, _didIteratorError = false, _iteratorError = undefined;
                try {
                    for(var _iterator = prev[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true){
                        var _step_value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_sliced_to_array$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(_step.value, 2), key = _step_value[0], slot = _step_value[1];
                        if (!nextPrev.has(key)) {
                            exits.push(slot);
                            if (exits.length >= cap) break;
                        }
                    }
                } catch (err) {
                    _didIteratorError = true;
                    _iteratorError = err;
                } finally{
                    try {
                        if (!_iteratorNormalCompletion && _iterator.return != null) {
                            _iterator.return();
                        }
                    } finally{
                        if (_didIteratorError) {
                            throw _iteratorError;
                        }
                    }
                }
                return exits;
            }
        },
        {
            /**
   * Animate the exit ghosts out, then remove them. Layouts whose positions carry
   * data (a waffle / grid lattice, or a scatter / beeswarm on real axes) fade
   * their ghosts OUT IN PLACE - drifting them toward the plot centre would drag
   * cells across tiles or bubbles across the plane, which reads as wrong. The
   * blob / bar layouts keep the gentle inward collapse so a removal reads as
   * motion rather than a pop.
   * @param {any} group @param {{x:number,y:number,fill:string}[]} exits @param {any} opts
   */ key: "_runExits",
            value: function _runExits(group, exits, opts) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(w, this.ctx);
                var dotR = this._lastDotR;
                var cx = w.layout.gridWidth / 2;
                var cy = w.layout.gridHeight / 2;
                var drift = opts.layout === "grid" || opts.layout === "scatter" || opts.layout === "arc" ? 0 : 0.35;
                var ghosts = [];
                exits.forEach(function(slot) {
                    var el = _this._drawDot(graphics, opts, dotR, slot.fill, 0, 0);
                    el.node.classList.add("apexcharts-unit-exit");
                    _this._placeDot(el.node, opts, slot.x, slot.y);
                    group.add(el);
                    ghosts.push({
                        node: el.node,
                        x0: slot.x,
                        y0: slot.y
                    });
                });
                if (!this._shouldAnimate()) {
                    ghosts.forEach(function(g) {
                        return g.node.remove();
                    });
                    return;
                }
                var speed = Math.max(1, w.config.chart.animations.speed || 800);
                var corner = this._isCorner(opts);
                var _this__halfExtent = this._halfExtent(opts), hx = _this__halfExtent.hx, hy = _this__halfExtent.hy;
                var offX = corner ? hx : 0;
                var offY = corner ? hy : 0;
                var cxAttr = corner ? "x" : "cx";
                var cyAttr = corner ? "y" : "cy";
                if (this.w.globals.unitExitRAF != null) {
                    BrowserAPIs.cancelAnimationFrame(this.w.globals.unitExitRAF);
                    this.w.globals.unitExitRAF = null;
                }
                var start = performance.now();
                var stepFn = function stepFn1(now) {
                    if (_this.w.globals.isDestroyed) {
                        _this.w.globals.unitExitRAF = null;
                        return;
                    }
                    var t = Math.max(0, Math.min(1, (now - start) / speed));
                    var e = easeOutCubic(t);
                    for(var k = 0; k < ghosts.length; k++){
                        var g = ghosts[k];
                        if (drift) {
                            var x = g.x0 + (cx - g.x0) * e * drift;
                            var y = g.y0 + (cy - g.y0) * e * drift;
                            g.node.setAttribute(cxAttr, String(x - offX));
                            g.node.setAttribute(cyAttr, String(y - offY));
                        }
                        g.node.style.opacity = String(1 - e);
                    }
                    if (t < 1) {
                        _this.w.globals.unitExitRAF = BrowserAPIs.requestAnimationFrame(stepFn);
                    } else {
                        _this.w.globals.unitExitRAF = null;
                        group.node && group.node.remove();
                    }
                };
                this.w.globals.unitExitRAF = BrowserAPIs.requestAnimationFrame(stepFn);
            }
        },
        {
            /**
   * A cluster label placed above (default) or below the cluster/bar. A TOP label
   * over a wide grouped/packed blob rides a curved arc (invisible arc path +
   * <textPath>, centred at 50% offset); a bottom label, a 'columns' bar, or a
   * cluster too small for the arc gets a straight centred label instead.
   * `clusterLabels.position` = 'top' | 'bottom'; `offsetY` pushes it further from
   * the blob in either direction.
   * @param {any} elSeries @param {{ i:number, cx:number, cy:number, outerR:number, flat?:boolean }} cluster
   * @param {number} value @param {number} total @param {any} opts @param {string} color
   */ key: "_drawClusterLabel",
            value: function _drawClusterLabel(elSeries, cluster, value, total, opts, color) {
                var w = this.w;
                if (!Environment.isBrowser()) return;
                var NS = "http://www.w3.org/2000/svg";
                var name2 = w.seriesData.seriesNames[cluster.i] || "series-".concat(cluster.i + 1);
                var percent = total > 0 ? value / total * 100 : 0;
                var cfg = opts.clusterLabels;
                var fontSize = parseFloat(cfg.fontSize) || 13;
                var text;
                if (typeof cfg.formatter === "function") {
                    text = cfg.formatter(name2, {
                        seriesIndex: cluster.i,
                        value: value,
                        percent: percent,
                        w: w
                    });
                } else {
                    text = "".concat(name2, " (").concat(percent.toFixed(1), "%)");
                }
                var str = typeof text === "string" ? text : String(text);
                var textEl = BrowserAPIs.createElementNS(NS, "text");
                textEl.setAttribute("class", "apexcharts-unit-label");
                textEl.setAttribute("text-anchor", "middle");
                textEl.setAttribute("font-size", "".concat(fontSize, "px"));
                textEl.setAttribute("font-family", cfg.fontFamily || w.config.chart.fontFamily || "inherit");
                textEl.setAttribute("font-weight", String(cfg.fontWeight || 600));
                textEl.setAttribute("fill", cfg.color || color);
                var bottom = cfg.position === "bottom";
                var R = cluster.outerR + fontSize * 0.6 + 3 + (cfg.offsetY || 0);
                var estWidth = str.length * fontSize * 0.55;
                var curved = !bottom && !cluster.flat && cfg.curved !== false && estWidth <= Math.PI * R * 0.95;
                if (curved) {
                    var yMid = cluster.cy;
                    var x1 = cluster.cx - R;
                    var x2 = cluster.cx + R;
                    var d = "M ".concat(x1, " ").concat(yMid, " A ").concat(R, " ").concat(R, " 0 0 1 ").concat(x2, " ").concat(yMid);
                    var arcId = "apexcharts-unit-label-".concat(w.globals.chartID, "-").concat(cluster.i);
                    var pathEl = BrowserAPIs.createElementNS(NS, "path");
                    pathEl.setAttribute("id", arcId);
                    pathEl.setAttribute("d", d);
                    pathEl.setAttribute("fill", "none");
                    pathEl.setAttribute("stroke", "none");
                    var tp = BrowserAPIs.createElementNS(NS, "textPath");
                    tp.setAttribute("href", "#".concat(arcId));
                    tp.setAttributeNS("http://www.w3.org/1999/xlink", "xlink:href", "#".concat(arcId));
                    tp.setAttribute("startOffset", "50%");
                    tp.textContent = str;
                    textEl.appendChild(tp);
                    elSeries.node.appendChild(pathEl);
                } else {
                    textEl.setAttribute("x", String(cluster.cx));
                    var y = bottom ? cluster.cy + cluster.outerR + fontSize + 6 + (cfg.offsetY || 0) : cluster.cy - cluster.outerR - 6 - (cfg.offsetY || 0);
                    textEl.setAttribute("y", String(y));
                    textEl.textContent = str;
                }
                elSeries.node.appendChild(textEl);
            }
        }
    ]);
    return Unit;
}();
var D2R = Math.PI / 180;
var R2D = 180 / Math.PI;
var SVGNS = "http://www.w3.org/2000/svg";
var XHTML = "http://www.w3.org/1999/xhtml";
var lerp = function lerp(a, b, t) {
    return a + (b - a) * t;
};
var SunburstChart = /*#__PURE__*/ function() {
    "use strict";
    function SunburstChart(w, ctx) {
        (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_class_call_check$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(this, SunburstChart);
        this.ctx = ctx;
        this.w = w;
        var cnf = w.config;
        this.cfg = cnf.plotOptions.sunburst;
        this.strokeWidth = cnf.stroke.show ? cnf.stroke.width : 0;
        this.strokeColor = Array.isArray(cnf.stroke.colors) ? cnf.stroke.colors[0] : cnf.stroke.colors || "#fff";
        this.startAngle = this.cfg.startAngle;
        this.endAngle = this.cfg.endAngle;
        this.maxDepth = 0;
        this.centerX = 0;
        this.centerY = 0;
        this.maxRadius = 0;
        this.total = 1;
        this._focusMaxDepth = 0;
        this._focus = null;
        this._zoomGen = 0;
        this._roots = [];
        this._nodesAll = [];
        this._innerR = function() {
            return 0;
        };
        this._outerR = function() {
            return 0;
        };
        this._tooltipEl = null;
        this._lblSeq = 0;
        this._graphics = null;
        this._ringsG = null;
        this._labelsG = null;
    }
    (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_create_class$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(SunburstChart, [
        {
            /**
   * @param {any[]} series  flattened top-level values (geometry comes from the
   *   config hierarchy; kept for the standard draw(series) signature + noData)
   * @returns {any} SVG group
   */ key: "draw",
            value: function draw(series) {
                var _this = this;
                var w = this.w;
                var graphics = new Graphics(this.w);
                this._graphics = graphics;
                var g = graphics.group({
                    class: "apexcharts-sunburst"
                });
                if (w.globals.noData || !series || !series.length) return g;
                var gw = w.layout.gridWidth;
                var gh = w.layout.gridHeight;
                this.centerX = gw / 2 + (this.cfg.offsetX || 0);
                this.centerY = gh / 2 + (this.cfg.offsetY || 0);
                this.maxRadius = Math.min(gw, gh) / 2.05 - this.strokeWidth - (!w.config.chart.sparkline.enabled ? w.config.chart.dropShadow.blur : 0);
                if (this.maxRadius < 5) return g;
                this._roots = this._buildHierarchy();
                if (!this._roots.length) return g;
                this._roots.forEach(function(r) {
                    return _this._fillValues(r);
                });
                this._validateStrict();
                this._nodesAll = [];
                var colors = w.globals.colors || [];
                this._roots.forEach(function(r, i) {
                    _this._colorPass(r, r.color || colors[i % colors.length] || "#008FFB");
                });
                this.total = this._roots.reduce(function(s, r) {
                    return s + Math.max(0, r.value);
                }, 0) || 1;
                this._ringsG = graphics.group({
                    class: "apexcharts-sunburst-rings"
                });
                this._labelsG = graphics.group({
                    class: "apexcharts-sunburst-labels"
                });
                g.add(this._ringsG);
                g.add(this._labelsG);
                this._focus = null;
                this._relayout(this._focus);
                var anims = w.config.chart.animations;
                var mode = "none";
                if (anims.enabled) {
                    if (w.globals.dataChanged) {
                        if (anims.dynamicAnimation.enabled) mode = "update";
                    } else if (!w.globals.resized) {
                        mode = "intro";
                    }
                }
                this._applyLayout(mode);
                this._renderBreadcrumb();
                return g;
            }
        },
        {
            // ------------------------------------------------------------------ data
            /**
   * Resolve the config into root nodes `{ name, value, color?, children? }`.
   * Each datum may carry `children` (native) or `drilldown: '<id>'` (adapter).
   * @returns {any[]}
   */ key: "_buildHierarchy",
            value: function _buildHierarchy() {
                var _this = this;
                var cfgSeries = /** @type {any} */ this.w.config.series;
                var first = cfgSeries && cfgSeries[0];
                var data = first && Array.isArray(first.data) ? first.data : cfgSeries;
                if (!Array.isArray(data)) return [];
                return data.map(function(d, i) {
                    return _this._toNode(d, i, null, "");
                });
            }
        },
        {
            /**
   * @param {any} d
   * @param {number} i
   * @param {string[]|null} paletteFromParent  per-level colours from a drilldown entry
   * @param {string} parentKey  hierarchical identity of the parent
   * @param {Set<any>|null} [seenIds]  drilldown ids already expanded on this path
   * @returns {any}
   */ key: "_toNode",
            value: function _toNode(d, i, paletteFromParent, parentKey) {
                var _this = this;
                var seenIds = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : null;
                var _a, _b, _c;
                var isObj = d && (typeof d === "undefined" ? "undefined" : (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$swc$2f$helpers$2f$esm$2f$_type_of$2e$js__$5b$app$2d$client$5d$__$28$ecmascript$29$__["_"])(d)) === "object";
                var name2 = isObj ? (_b = (_a = d.x) != null ? _a : d.name) != null ? _b : "" : "";
                var value = isObj ? Number((_c = d.y) != null ? _c : d.value) : Number(d);
                var node = {
                    name: String(name2),
                    value: isNaN(value) ? null : value,
                    color: isObj && d.color ? d.color : void 0,
                    // Identity across data updates: the path of names (indexed so same-named
                    // siblings stay distinct). Update animations morph matched keys in place.
                    _key: "".concat(parentKey, "/").concat(i, ":").concat(name2)
                };
                if (paletteFromParent && !node.color) {
                    node.color = paletteFromParent[i % paletteFromParent.length];
                }
                if (isObj && Array.isArray(d.children) && d.children.length) {
                    node.children = d.children.map(function(c, j) {
                        return _this._toNode(c, j, null, node._key, seenIds);
                    });
                } else if (isObj && d.drilldown != null) {
                    var visited = seenIds || /* @__PURE__ */ new Set();
                    if (!visited.has(d.drilldown)) {
                        var dd = this._drilldownById(d.drilldown);
                        if (dd && Array.isArray(dd.data) && dd.data.length) {
                            var nextSeen = new Set(visited);
                            nextSeen.add(d.drilldown);
                            var palette = Array.isArray(dd.colors) ? dd.colors : null;
                            node.children = dd.data.map(function(c, j) {
                                return _this._toNode(c, j, palette, node._key, nextSeen);
                            });
                        }
                    }
                }
                return node;
            }
        },
        {
            /**
   * @param {string|number} id
   * @returns {any}
   */ key: "_drilldownById",
            value: function _drilldownById(id) {
                var dd = this.w.config.drilldown;
                var list = dd && Array.isArray(dd.series) ? dd.series : [];
                return list.find(function(s) {
                    return s && s.id === id;
                });
            }
        },
        {
            /**
   * Fill a parent's value from its children when missing.
   * @param {any} node
   */ key: "_fillValues",
            value: function _fillValues(node) {
                var _this = this;
                if (node.children && node.children.length) {
                    node.children.forEach(function(c) {
                        return _this._fillValues(c);
                    });
                    if (node.value == null || isNaN(node.value)) {
                        node.value = node.children.reduce(function(s, c) {
                            return s + Math.max(0, c.value || 0);
                        }, 0);
                    }
                }
                if (node.value == null || isNaN(node.value)) node.value = 0;
            }
        },
        {
            /**
   * With `partition: 'strict'`, warn (once) when a parent's value does not
   * match the sum of its children. The angles are still normalized to fill the
   * wedge (strict rendering is a P3 refinement); this just surfaces the data
   * mismatch.
   */ key: "_validateStrict",
            value: function _validateStrict() {
                if (this.cfg.partition !== "strict") return;
                var warned = false;
                var walk = function walk1(node) {
                    if (node.children && node.children.length) {
                        var sum = node.children.reduce(function(s, c) {
                            return s + Math.max(0, c.value || 0);
                        }, 0);
                        if (!warned && node.value != null && Math.abs(sum - node.value) > 0.5) {
                            console.warn("ApexCharts sunburst: partition 'strict' but \"".concat(node.name, '" (').concat(node.value, ") != sum of its children (").concat(sum, "). Angles are normalized to fill the wedge."));
                            warned = true;
                        }
                        node.children.forEach(walk);
                    }
                };
                this._roots.forEach(walk);
            }
        },
        {
            /**
   * Assign a colour to every node (explicit `color` wins, else the parent's
   * colour tinted lighter). Done once so zoom preserves colours.
   * @param {any} node
   * @param {string} color
   */ key: "_colorPass",
            value: function _colorPass(node, color) {
                var _this = this;
                node._color = node.color || color;
                this._nodesAll.push(node);
                if (node.children) {
                    node.children.forEach(function(c) {
                        return _this._colorPass(c, c.color || _this._lighten(node._color, _this.cfg.tint));
                    });
                }
            }
        },
        {
            // ---------------------------------------------------------------- layout
            /**
   * Recompute visibility + angles + radii for a focus node (null = whole tree).
   * @param {any} focus
   */ key: "_relayout",
            value: function _relayout(focus) {
                var _this = this;
                this._nodesAll.forEach(function(n) {
                    n._show = false;
                });
                this._focusMaxDepth = 0;
                if (!focus) {
                    var total = this._roots.reduce(function(s, r) {
                        return s + Math.max(0, r.value);
                    }, 0) || 1;
                    var a = this.startAngle;
                    this._roots.forEach(function(r) {
                        var span = (_this.endAngle - _this.startAngle) * Math.max(0, r.value) / total;
                        _this._placeVis(r, 0, a, a + span, null);
                        a += span;
                    });
                } else {
                    this._placeVis(focus, 0, this.startAngle, this.endAngle, focus._parent);
                }
                var hole = this._parseSize(this.cfg.innerSize, this.maxRadius);
                var ringCount = this._focusMaxDepth + 1;
                var band = (this.maxRadius - hole) / ringCount;
                var radialGap = ringCount > 1 ? 1 : 0;
                this._innerR = function(vd) {
                    return hole + vd * band + (vd > 0 ? radialGap / 2 : 0);
                };
                this._outerR = function(vd) {
                    return hole + (vd + 1) * band - radialGap / 2;
                };
                this._nodesAll.forEach(function(n) {
                    if (!n._show) return;
                    n._iR = _this._innerR(n._vDepth);
                    n._oR = n._leaf && _this.cfg.leaf === "extend" && n._vDepth < _this._focusMaxDepth ? _this.maxRadius : _this._outerR(n._vDepth);
                });
            }
        },
        {
            /**
   * @param {any} node
   * @param {number} vDepth
   * @param {number} a0
   * @param {number} a1
   * @param {any} parent
   */ key: "_placeVis",
            value: function _placeVis(node, vDepth, a0, a1, parent) {
                var _this = this;
                node._show = true;
                node._vDepth = vDepth;
                node._a0 = a0;
                node._a1 = a1;
                node._parent = parent;
                node._leaf = !(node.children && node.children.length);
                if (vDepth > this._focusMaxDepth) this._focusMaxDepth = vDepth;
                if (!node._leaf) {
                    var total = node.children.reduce(function(s, c) {
                        return s + Math.max(0, c.value);
                    }, 0) || 1;
                    var a = a0;
                    node.children.forEach(function(c) {
                        var span = (a1 - a0) * Math.max(0, c.value) / total;
                        _this._placeVis(c, vDepth + 1, a, a + span, node);
                        a += span;
                    });
                }
            }
        },
        {
            // --------------------------------------------------------------- render
            /**
   * Create / update / remove arc elements to match the current layout, with an
   * animation appropriate to the transition:
   *   intro  — pie/donut-style angular clock sweep from startAngle to endAngle
   *            (all rings reveal together as the sweep line passes them)
   *   update — morph every arc from its previous on-screen geometry (matched
   *            by node key across the re-render); new arcs unfurl in place
   *   zoom   — tween angles + radii between focus layouts (same instance)
   * @param {'intro'|'zoom'|'update'|'none'} mode
   */ key: "_applyLayout",
            value: function _applyLayout(mode) {
                var _this = this;
                var w = this.w;
                var anims = w.config.chart.animations;
                var dur = !anims.enabled ? 0 : mode === "none" ? 0 : mode === "update" ? anims.dynamicAnimation.speed || 350 : anims.speed || 500;
                var gen = ++this._zoomGen;
                var prev = mode === "update" ? /** @type {any} */ this.ctx._sunburstPrevGeoms : null;
                this._nodesAll.forEach(function(node) {
                    if (node._show) {
                        var target = {
                            a0: node._a0,
                            a1: node._a1,
                            iR: node._iR,
                            oR: node._oR
                        };
                        if (!node._el) node._el = _this._createArcEl(node);
                        if (mode === "intro" && dur > 0) {
                            _this._sweepArc(node, target, dur, gen);
                        } else {
                            var from;
                            var isNew = false;
                            if (node._cur) {
                                from = node._cur;
                            } else if (prev && prev.get(node._key)) {
                                from = prev.get(node._key);
                            } else {
                                var mid = (target.a0 + target.a1) / 2;
                                from = {
                                    a0: mid,
                                    a1: mid,
                                    iR: target.iR,
                                    oR: target.iR
                                };
                                isNew = true;
                            }
                            _this._animateArc(node, from, target, dur, false, isNew, gen);
                        }
                    } else if (node._el && node._cur) {
                        var mid1 = (node._cur.a0 + node._cur.a1) / 2;
                        var target1 = {
                            a0: mid1,
                            a1: mid1,
                            iR: node._cur.iR,
                            oR: node._cur.iR
                        };
                        _this._animateArc(node, node._cur, target1, dur, true, false, gen);
                    }
                });
                var geoms = /* @__PURE__ */ new Map();
                this._nodesAll.forEach(function(n) {
                    if (n._show) {
                        geoms.set(n._key, {
                            a0: n._a0,
                            a1: n._a1,
                            iR: n._iR,
                            oR: n._oR
                        });
                    }
                });
                this.ctx._sunburstPrevGeoms = geoms;
                this._renderLabels(dur);
            }
        },
        {
            /**
   * Pie/donut-style intro: a clock sweep from startAngle to endAngle. Each
   * arc's end angle is clamped to the sweep line, so arcs appear in angular
   * order and grow until complete — the whole hierarchy unwipes together.
   * @param {any} node
   * @param {{a0:number,a1:number,iR:number,oR:number}} target
   * @param {number} dur
   * @param {number} gen  layout generation; frames stop once superseded
   */ key: "_sweepArc",
            value: function _sweepArc(node, target, dur, gen) {
                var _this = this;
                var el = node._el;
                var br = this.cfg.borderRadius;
                var s0 = this.startAngle;
                var s1 = this.endAngle;
                el.node.style.display = "";
                el.attr({
                    d: "",
                    opacity: 1
                });
                el.animate(dur).during(function(pos) {
                    if (_this._zoomGen !== gen) return;
                    var sweep = s0 + (s1 - s0) * pos;
                    if (sweep <= target.a0 + 0.01) {
                        el.attr({
                            d: ""
                        });
                        return;
                    }
                    var a1 = Math.min(target.a1, sweep);
                    el.attr({
                        d: _this._arcPath(target.iR, target.oR, target.a0, a1, br)
                    });
                    node._cur = {
                        a0: target.a0,
                        a1: a1,
                        iR: target.iR,
                        oR: target.oR
                    };
                }).after(function() {
                    if (_this._zoomGen !== gen) return;
                    node._cur = target;
                });
            }
        },
        {
            /**
   * @param {any} node
   * @returns {any} svg.js path element
   */ key: "_createArcEl",
            value: function _createArcEl(node) {
                var _this = this;
                var path = this._graphics.drawPath({
                    d: "",
                    fill: node._color,
                    stroke: this.strokeColor,
                    strokeWidth: this.strokeWidth,
                    fillOpacity: 1,
                    classes: "apexcharts-sunburst-arc"
                });
                var el = path.node;
                el.setAttribute("data:name", node.name);
                el.setAttribute("data:value", String(node.value));
                this._attachTooltip(el, node);
                if (Environment.isBrowser()) {
                    el.addEventListener("click", function() {
                        return _this._zoomTo(node);
                    });
                    el.style.cursor = "pointer";
                }
                this._ringsG.add(path);
                return path;
            }
        },
        {
            /**
   * @param {any} node
   * @param {{a0:number,a1:number,iR:number,oR:number}} from
   * @param {{a0:number,a1:number,iR:number,oR:number}} to
   * @param {number} dur
   * @param {boolean} hide    shrink + fade out, then hide
   * @param {boolean} fadeIn  fade 0 -> 1 (new arcs only; morphs stay opaque)
   * @param {number} gen  layout generation; frames stop once superseded
   */ key: "_animateArc",
            value: function _animateArc(node, from, to, dur, hide, fadeIn2, gen) {
                var _this = this;
                var el = node._el;
                var br = this.cfg.borderRadius;
                el.attr({
                    fill: node._color
                });
                if (dur === 0) {
                    el.attr({
                        d: this._arcPath(to.iR, to.oR, to.a0, to.a1, br),
                        opacity: hide ? 0 : 1
                    });
                    el.node.style.display = hide ? "none" : "";
                    node._cur = hide ? null : to;
                    return;
                }
                el.node.style.display = "";
                var startOp = hide ? Number(el.attr("opacity")) || 1 : fadeIn2 ? 0 : 1;
                var endOp = hide ? 0 : 1;
                el.attr({
                    opacity: startOp
                });
                el.animate(dur).during(function(pos) {
                    if (_this._zoomGen !== gen) return;
                    var a0 = lerp(from.a0, to.a0, pos);
                    var a1 = lerp(from.a1, to.a1, pos);
                    var iR = lerp(from.iR, to.iR, pos);
                    var oR = lerp(from.oR, to.oR, pos);
                    el.attr({
                        d: _this._arcPath(iR, oR, a0, a1, br),
                        opacity: lerp(startOp, endOp, pos)
                    });
                    node._cur = {
                        a0: a0,
                        a1: a1,
                        iR: iR,
                        oR: oR
                    };
                }).after(function() {
                    if (_this._zoomGen !== gen) return;
                    if (hide) {
                        el.node.style.display = "none";
                        node._cur = null;
                    } else {
                        node._cur = to;
                    }
                });
            }
        },
        {
            // ---------------------------------------------------------------- labels
            /**
   * Labels are overlays on animated paths, so they reveal gradually AFTER the
   * arcs settle (repo convention: overlays never pop in over a moving path).
   * @param {number} dur  arc animation duration (0 = instant labels)
   */ key: "_renderLabels",
            value: function _renderLabels(dur) {
                var _this = this;
                var labelsG = this._labelsG;
                while(labelsG.node.firstChild)labelsG.node.removeChild(labelsG.node.firstChild);
                if (!this.cfg.dataLabels.show) return;
                this._nodesAll.forEach(function(node) {
                    if (!node._show) return;
                    if (node._a1 - node._a0 < _this.cfg.dataLabels.minAngleToShow) return;
                    _this._renderCurvedLabel(node);
                });
                if (dur > 0) {
                    labelsG.attr({
                        opacity: 0
                    });
                    labelsG.animate(250, dur).attr({
                        opacity: 1
                    });
                } else {
                    labelsG.attr({
                        opacity: 1
                    });
                }
            }
        },
        {
            /**
   * Curved label along the arc's mid-radius (flipped on the bottom half so it
   * stays upright). Raw SVG <textPath> — svg.js has no first-class textPath.
   * @param {any} node
   */ key: "_renderCurvedLabel",
            value: function _renderCurvedLabel(node) {
                if (!Environment.isBrowser()) return;
                var style = this.cfg.dataLabels.style;
                var w = this.w;
                var r = (node._iR + node._oR) / 2;
                var mid = (node._a0 + node._a1) / 2;
                var flip = mid > 90 && mid < 270;
                var padDeg = Math.min(r > 0 ? 4 / r * R2D : 0, (node._a1 - node._a0) / 2);
                var from = flip ? node._a1 - padDeg : node._a0 + padDeg;
                var to = flip ? node._a0 + padDeg : node._a1 - padDeg;
                var p1 = this._ptAt(r, from);
                var p2 = this._ptAt(r, to);
                var largeArc = Math.abs(to - from) > 180 ? 1 : 0;
                var sweep = flip ? 0 : 1;
                var id = "apx-sb-lbl-".concat(w.globals.cuid, "-").concat(this._lblSeq++);
                var guide = BrowserAPIs.createElementNS(SVGNS, "path");
                guide.setAttribute("id", id);
                guide.setAttribute("d", "M ".concat(p1.x, " ").concat(p1.y, " A ").concat(r, " ").concat(r, " 0 ").concat(largeArc, " ").concat(sweep, " ").concat(p2.x, " ").concat(p2.y));
                guide.setAttribute("fill", "none");
                guide.setAttribute("stroke", "none");
                var colors = style.colors;
                var fill = (Array.isArray(colors) ? colors[0] : colors) || "#fff";
                var text = BrowserAPIs.createElementNS(SVGNS, "text");
                text.setAttribute("font-size", style.fontSize || "12px");
                if (style.fontFamily) text.setAttribute("font-family", style.fontFamily);
                text.setAttribute("font-weight", String(style.fontWeight || 400));
                text.setAttribute("fill", fill);
                text.setAttribute("dominant-baseline", "central");
                text.style.pointerEvents = "none";
                var tp = BrowserAPIs.createElementNS(SVGNS, "textPath");
                tp.setAttribute("href", "#" + id);
                tp.setAttributeNS("http://www.w3.org/1999/xlink", "xlink:href", "#" + id);
                tp.setAttribute("startOffset", "50%");
                tp.setAttribute("text-anchor", "middle");
                tp.textContent = this._truncate(node.name, r, node._a1 - node._a0, style.fontSize);
                text.appendChild(tp);
                this._labelsG.node.appendChild(guide);
                this._labelsG.node.appendChild(text);
            }
        },
        {
            /**
   * Trim a label to the arc length available at its radius.
   * @param {string} name
   * @param {number} r
   * @param {number} spanDeg
   * @param {string} fontSize
   * @returns {string}
   */ key: "_truncate",
            value: function _truncate(name2, r, spanDeg, fontSize) {
                var arcLen = r * spanDeg * D2R - 8;
                var charW = (parseFloat(fontSize) || 12) * 0.58;
                var maxChars = Math.floor(arcLen / charW);
                if (maxChars >= name2.length) return name2;
                if (maxChars <= 1) return "";
                return name2.slice(0, Math.max(1, maxChars - 1)) + "…";
            }
        },
        {
            // ----------------------------------------------------------------- zoom
            /**
   * Focus a node (zoom in), or zoom out one level when the current focus (the
   * innermost ring) is clicked.
   * @param {any} node
   */ key: "_zoomTo",
            value: function _zoomTo(node) {
                if (this.cfg.zoomOnClick === false) return;
                var nextFocus = node === this._focus ? node._parent || null : node;
                if (nextFocus === this._focus) return;
                this._focus = nextFocus && !(nextFocus.children && nextFocus.children.length) ? nextFocus._parent || null : nextFocus;
                this._relayout(this._focus);
                this._applyLayout("zoom");
                this._renderBreadcrumb();
            }
        },
        {
            /** Root -> focus chain of nodes. */ key: "_focusChain",
            value: function _focusChain() {
                var chain = [];
                var n = this._focus;
                while(n){
                    chain.unshift(n);
                    n = n._parent;
                }
                return chain;
            }
        },
        {
            /**
   * Minimal self-contained breadcrumb (reuses the shared `.apexcharts-breadcrumb`
   * CSS classes, but does NOT depend on the drilldown feature).
   */ key: "_renderBreadcrumb",
            value: function _renderBreadcrumb() {
                var _this = this;
                if (!Environment.isBrowser()) return;
                var w = this.w;
                var elWrap = w.dom.elWrap;
                if (!elWrap) return;
                var existing = elWrap.querySelector(".apexcharts-breadcrumb");
                if (existing && existing.parentNode) existing.parentNode.removeChild(existing);
                if (!this._focus) return;
                var nav = BrowserAPIs.createElementNS(XHTML, "nav");
                nav.setAttribute("class", "apexcharts-breadcrumb");
                nav.setAttribute("aria-label", "Sunburst breadcrumb");
                nav.style.position = "absolute";
                nav.style.top = "0px";
                nav.style.left = "0px";
                var crumbs = [
                    {
                        name: "All",
                        node: null
                    }
                ].concat(this._focusChain().map(function(n) {
                    return {
                        name: n.name,
                        node: n
                    };
                }));
                crumbs.forEach(function(crumb, i) {
                    if (i > 0) {
                        var sep = BrowserAPIs.createElementNS(XHTML, "span");
                        sep.setAttribute("class", "apexcharts-breadcrumb-separator");
                        sep.textContent = " / ";
                        nav.appendChild(sep);
                    }
                    var isCurrent = i === crumbs.length - 1;
                    if (isCurrent) {
                        var cur = BrowserAPIs.createElementNS(XHTML, "span");
                        cur.setAttribute("class", "apexcharts-breadcrumb-item apexcharts-breadcrumb-current");
                        cur.textContent = crumb.name;
                        nav.appendChild(cur);
                    } else {
                        var btn = BrowserAPIs.createElementNS(XHTML, "button");
                        btn.setAttribute("type", "button");
                        btn.setAttribute("class", "apexcharts-breadcrumb-item");
                        if (i === 0) {
                            var arrow = BrowserAPIs.createElementNS(XHTML, "span");
                            arrow.setAttribute("class", "apexcharts-breadcrumb-arrow");
                            arrow.textContent = "←";
                            btn.appendChild(arrow);
                        }
                        var text = BrowserAPIs.createElementNS(XHTML, "span");
                        text.setAttribute("class", "apexcharts-breadcrumb-label");
                        text.textContent = crumb.name;
                        btn.appendChild(text);
                        btn.addEventListener("click", function() {
                            _this._focus = crumb.node;
                            _this._relayout(_this._focus);
                            _this._applyLayout("zoom");
                            _this._renderBreadcrumb();
                        });
                        nav.appendChild(btn);
                    }
                });
                elWrap.appendChild(nav);
                this._avoidChromeOverlap(nav);
            }
        },
        {
            /**
   * The breadcrumb is an absolute overlay at top-left, so it can sit on top of
   * a left-aligned title (or subtitle). After mounting, push it below any chart
   * chrome it intersects. (Duplicated from drilldown's Breadcrumb on purpose —
   * sunburst must not import the drilldown feature.)
   * @param {any} nav
   */ key: "_avoidChromeOverlap",
            value: function _avoidChromeOverlap(nav) {
                var _loop = function(pass) {
                    var nr = nav.getBoundingClientRect();
                    var hit = chrome.find(function(el) {
                        var r = el.getBoundingClientRect();
                        return nr.left < r.right && nr.right > r.left && nr.top < r.bottom && nr.bottom > r.top;
                    });
                    if (!hit) return "break";
                    nav.style.top = "".concat(hit.getBoundingClientRect().bottom - wrapTop + 4, "px");
                };
                var w = this.w;
                var chrome = /** @type {Element[]} */ [
                    ".apexcharts-title-text",
                    ".apexcharts-subtitle-text"
                ].map(function(s) {
                    return w.dom.baseEl.querySelector(s);
                }).filter(function(el) {
                    return el !== null;
                });
                if (!chrome.length) return;
                var wrapTop = w.dom.elWrap.getBoundingClientRect().top;
                for(var pass = 0; pass < chrome.length + 1; pass++){
                    var _ret = _loop(pass);
                    if (_ret === "break") break;
                }
            }
        },
        {
            // ------------------------------------------------------------ geometry
            /**
   * @param {number} r
   * @param {number} deg  0 = top, clockwise
   * @returns {{x: number, y: number}}
   */ key: "_ptAt",
            value: function _ptAt(r, deg) {
                return {
                    x: this.centerX + r * Math.cos((deg - 90) * D2R),
                    y: this.centerY + r * Math.sin((deg - 90) * D2R)
                };
            }
        },
        {
            /**
   * Rounded donut-segment path (inner radius always > 0). Applies `spacing`
   * (angular gap) and `borderRadius` (corner rounding), both clamped so a thin
   * arc never inverts.
   * @param {number} iR
   * @param {number} oR
   * @param {number} a0
   * @param {number} a1
   * @param {number} borderRadius
   * @returns {string}
   */ key: "_arcPath",
            value: function _arcPath(iR, oR, a0, a1, borderRadius) {
                if (oR <= iR + 0.01) return "";
                var spanDeg = a1 - a0;
                var spacing = this.cfg.spacing;
                if (spacing > 0 && spanDeg > 0 && oR > 0) {
                    var gapDeg = spacing / oR * R2D;
                    var inset = Math.min(gapDeg / 2, Math.max(0, spanDeg / 2 - 0.25));
                    a0 += inset;
                    a1 -= inset;
                    spanDeg = a1 - a0;
                }
                if (spanDeg <= 0) return "";
                var spanRad = spanDeg * D2R;
                var cx = this.centerX;
                var cy = this.centerY;
                var r = borderRadius;
                r = Math.min(r, spanRad * iR / 2, spanRad * oR / 2, (oR - iR) / 2);
                if (!(r > 0.5)) {
                    return sharpDonutSegmentPath({
                        cx: cx,
                        cy: cy,
                        rIn: iR,
                        rOut: oR,
                        a0: a0,
                        a1: a1,
                        spanDeg: spanDeg
                    });
                }
                return roundedDonutSegmentPath({
                    cx: cx,
                    cy: cy,
                    rIn: iR,
                    rOut: oR,
                    a0: a0,
                    a1: a1,
                    r: r,
                    spanDeg: spanDeg
                });
            }
        },
        {
            // ------------------------------------------------------------- tooltip
            /** @returns {any} */ key: "_tip",
            value: function _tip() {
                if (!this._tooltipEl) {
                    this._tooltipEl = this.w.dom.baseEl.querySelector(".apexcharts-tooltip");
                }
                return this._tooltipEl;
            }
        },
        {
            /**
   * @param {any} el
   * @param {any} node
   */ key: "_attachTooltip",
            value: function _attachTooltip(el, node) {
                var _this = this;
                if (!this.w.config.tooltip.enabled || !Environment.isBrowser()) return;
                el.addEventListener("mouseenter", function(e) {
                    return _this._showTooltip(e, node);
                });
                el.addEventListener("mousemove", function(e) {
                    return _this._positionTooltip(e);
                });
                el.addEventListener("mouseleave", function() {
                    return _this._hideTooltip();
                });
            }
        },
        {
            /**
   * @param {MouseEvent} e
   * @param {any} node
   */ key: "_showTooltip",
            value: function _showTooltip(e, node) {
                var t = this._tip();
                if (!t) return;
                var w = this.w;
                var pctTotal = (node.value / this.total * 100).toFixed(1);
                var parentVal = node._parent ? node._parent.value : this.total;
                var pctParent = parentVal > 0 ? (node.value / parentVal * 100).toFixed(1) : pctTotal;
                var groupBg = w.config.tooltip.fillSeriesColor ? "background-color:".concat(node._color, ";") : "";
                t.innerHTML = '<div class="apexcharts-tooltip-series-group apexcharts-active" style="display:flex;'.concat(groupBg, '"><span class="apexcharts-tooltip-marker" style="background-color:').concat(node._color, '"></span><div class="apexcharts-tooltip-text"><div class="apexcharts-tooltip-y-group"><span class="apexcharts-tooltip-text-y-label">').concat(node.name, ': </span><span class="apexcharts-tooltip-text-y-value">').concat(node.value, " (").concat(pctParent, "% of parent, ").concat(pctTotal, "% of total)</span></div></div></div>");
                t.classList.add("apexcharts-active");
                t.style.opacity = "1";
                this._positionTooltip(e);
            }
        },
        {
            /**
   * Position beside the cursor, flipping to the opposite side when the box
   * would overflow the chart wrap, and clamping inside it either way.
   * @param {MouseEvent} e
   */ key: "_positionTooltip",
            value: function _positionTooltip(e) {
                var t = this._tip();
                if (!t) return;
                var rect = this.w.dom.elWrap.getBoundingClientRect();
                var tw = t.offsetWidth;
                var th = t.offsetHeight;
                var pad = 12;
                var x = e.clientX - rect.left + pad;
                if (x + tw > rect.width) x = e.clientX - rect.left - tw - pad;
                x = Math.max(0, Math.min(x, rect.width - tw));
                var y = e.clientY - rect.top + pad;
                if (y + th > rect.height) y = e.clientY - rect.top - th - pad;
                y = Math.max(0, Math.min(y, rect.height - th));
                t.style.left = x + "px";
                t.style.top = y + "px";
            }
        },
        {
            key: "_hideTooltip",
            value: function _hideTooltip() {
                var t = this._tip();
                if (!t) return;
                t.classList.remove("apexcharts-active");
                t.style.opacity = "0";
            }
        },
        {
            // --------------------------------------------------------------- utils
            /**
   * @param {string|number} size
   * @param {number} max
   * @returns {number}
   */ key: "_parseSize",
            value: function _parseSize(size, max) {
                if (typeof size === "number") return size;
                var s = String(size).trim();
                if (s.endsWith("%")) return parseFloat(s) / 100 * max;
                var n = parseFloat(s);
                return isNaN(n) ? 0.15 * max : n;
            }
        },
        {
            /**
   * Blend a hex colour toward white by `amount` (0..1). Non-hex returned as-is.
   * @param {string} color
   * @param {number} amount
   * @returns {string}
   */ key: "_lighten",
            value: function _lighten(color, amount) {
                if (typeof color !== "string" || color[0] !== "#") return color;
                var hex = color.slice(1);
                if (hex.length === 3) {
                    hex = hex.split("").map(function(c) {
                        return c + c;
                    }).join("");
                }
                if (hex.length !== 6) return color;
                var num = parseInt(hex, 16);
                if (isNaN(num)) return color;
                var rC = num >> 16 & 255;
                var gC = num >> 8 & 255;
                var bC = num & 255;
                rC = Math.round(rC + (255 - rC) * amount);
                gC = Math.round(gC + (255 - gC) * amount);
                bC = Math.round(bC + (255 - bC) * amount);
                return "#" + ((1 << 24) + (rC << 16) + (gC << 8) + bC).toString(16).slice(1);
            }
        }
    ]);
    return SunburstChart;
}();
ApexCharts.use({
    line: Line,
    area: Line,
    scatter: Line,
    bubble: Line,
    rangeArea: Line,
    bar: Bar,
    column: Bar,
    barStacked: BarStacked,
    rangeBar: RangeBar,
    candlestick: BoxCandleStick,
    boxPlot: BoxCandleStick,
    violin: Violin,
    pie: Pie,
    donut: Pie,
    polarArea: Pie,
    radialBar: Radial,
    radar: Radar,
    heatmap: HeatMap,
    treemap: TreemapChart,
    unit: Unit,
    sunburst: SunburstChart
});
;
}),
]);

//# sourceMappingURL=node_modules_apexcharts_dist_apexcharts_esm_1-tx1gc.js.map